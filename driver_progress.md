# Driver progress

Superseded by `driver_plan.md`.

Use `driver_plan.md` as the live status ledger. All statuses there were reset
to `NOT DONE` first, then promoted only after fresh source/test evidence from
the current tree.
