[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
# the rtld's ld.so.cache encoder (build_cache) + reader (lookup), host-side.
ldso = { path = "../../crates/user/ldso", features = ["hosted"] }
