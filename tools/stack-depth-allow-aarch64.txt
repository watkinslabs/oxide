# Static call paths at or over the stack-depth ceiling when the gate landed
# (aarch64). Tolerated at or below the recorded budget ONLY — a new or deeper
# path FAILS. Each block states why that family is legitimately deep; the
# gate refuses an entry that is not under a reason block.
#
# Burn this down. Regenerate with:
#   make stack-gate-baseline-arm

# Syscall entry and dispatch. These are the ROOTS of every syscall, so the
# walker's longest path is the max over all ~400 of them at once — no single
# call reaches this. Useful only as a ceiling on the deepest syscall; the
# individual syscalls below are the numbers to actually drive down.
14448	oxide_svc_save_block
14160	oxide_syscall_dispatch

# The network TX chain: write/writev/sendmsg/sendto/sendmmsg -> socket::send
# -> NetStack xmit -> raw4/ipv6 framing. Every entry here is a prefix of the
# same chain, which is why they cluster. This is the deepest live path in the
# kernel and the top follow-up candidate — it is genuinely one call sequence,
# not a longest-path artifact.
13728	_RNvNtCshbkh59xwSwY_8syscalls13s307_sendmmsg12sys_sendmmsg
13296	_RNvNtCshbkh59xwSwY_8syscalls12s296_pwritev12sys_pwritev2
13216	_RNvNtCshbkh59xwSwY_8syscalls11s020_writev10sys_writev
13040	_RNvNtCscVwxWUH72qP_6socket4send6writev
13008	_RNvNtCshbkh59xwSwY_8syscalls12s046_sendmsg11sys_sendmsg

# Socket RX and control syscalls: read/readv/recvfrom/recvmsg/recvmmsg and
# connect/shutdown/setsockopt. They share the socket layer and the NetStack
# tail with the TX chain above, so they move together and are one follow-up.
13008	_RNvNtCshbkh59xwSwY_8syscalls12s042_connect11sys_connect

# Batched-submission syscalls. io_uring_enter / io_submit inline one op's
# whole handler, so their depth is that of the deepest op they can dispatch.
13664	_RNvNtNtCshbkh59xwSwY_8syscalls3aio6submit13sys_io_submit
13520	_RNvNtCshbkh59xwSwY_8syscalls19s426_io_uring_enter18sys_io_uring_enter
