# Rows already `IMPL` whose Evidence discloses a divergence.
#
# Recorded by F789 when the overstated-IMPL check was wired into `make ci` for
# the first time. The check existed but was never run by any gate, so these
# predate enforcement. They are listed rather than bulk-flipped to PARTIAL:
# the status a row carries is its owning lane's claim, and F789 has not audited
# any of them. The gate fails on any NEW violation.
#
# An entry leaves this file when the owning lane resolves the row -- by fixing
# the divergence, or by downgrading the status to match its own Evidence.
# A lane may NOT add itself here to make its own row pass.

# 76 (truncate, F726-fs-simple-audit): ...B1441): this row's own Evidence discloses that ETXTBSY on truncating a running executable is absent (no `i_writecount`/`deny_write_access` in-tree). The legend defines IMPL as FULL Linux-ro...
76

# 78 (getdents, F736-affinity-getdents-audit): ...kernfs::PseudoDir::op_readdir` loop); `/proc/self` is DT_DIR not DT_LNK and `thread-self` is absent; 57 sites emit `d_ino == 0` on a transient lookup failure; FUSE re-OPENDIRs inside every...
78

# 101 (ptrace, B1638-ptrace-prctl-personality): ...|0x80` under PTRACE_O_TRACESYSGOOD. Hosted tests: 37 in `s101_ptrace_{decide,perm,regs}`. NOT implemented, honestly EIO: PEEKSIGINFO, GET/SET_SYSCALL_INFO, SECCOMP_GET_FILTER/METADATA, GET_RSEQ_C...
101

# 135 (personality, B1638-ptrace-prctl-personality): ..._64bit` clears READ_IMPLIES_EXEC; arm64 `elf_read_implies_exec` sets it when PT_GNU_STACK is absent — the ELF parser does not report GNU_STACK presence). Harness: `crates/kernel/sched/src/p...
135

# 140 (getpriority, F727-sysinfo-limits-audit): ...nd); ESRCH when nothing matches; PRIO_USER matches on the target's REAL uid (`task_uid`). NOT matched: Linux's PRIO_USER loop also requires `task_pid_vnr(p)` (target visible in the caller's p...
140

# 141 (setpriority, F727-sysinfo-limits-audit): ...1 saturates for shell-friendliness") when Linux saturates too -- a false compliance note. NOT matched: Linux's PRIO_USER loop also requires `task_pid_vnr(p)` (pid-namespace visibility); `secu...
141

# 166 (umount2, B1650): ...L. umount2 has core detach behavior, but MNT_EXPIRE lacks Linux two-pass state, MNT_FORCE is absent, and recursive pseudo-filesystem behavior diverges. B1582 closes the ladder defects: `MNT...
166

# 217 (getdents64, F736-affinity-getdents-audit): ...collisions separated). All 56 `d_ino == 0` sites are gone — a name whose object vanished is absent from the listing rather than emitted as a deleted placeholder. FUSE re-`OPENDIR`ed inside...
217

# 430 (fsopen, B713-linux-path-identity): ...ion is whitelist-shaped, namespace permission mode is incomplete, and direct ABI coverage is absent. Closed in `B1582`: a hardcoded fstype whitelist sat ahead of `get_fs_type()` — a split s...
430
