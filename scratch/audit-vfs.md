# VFS / filesystem / block subsystem audit vs Linux

DRAFT 2026-07-27. Dep:16,17,18,19,52,53.

Scope: `crates/kernel/vfs`, `crates/kernel/fs`, `crates/kernel/ext4`, `crates/kernel/block`,
pseudo filesystems (`procfs`/`sysfs`/`kernfs`/`cgroup`/`devpts`/`tmpfs`), page-cache interaction.
Linux source of truth: `/home/nd/oxide/linux-master` (v7.2.0-rc4). Every row verified by reading
both trees; no row rests on a doc, comment, or spec claim.

Severity: `BLOCKER` (desktop/userspace cannot work correctly) · `SECURITY` (a protection Linux
provides is absent) · `CORRECTNESS` (wrong observable behaviour) · `PERF` (right answer, wrong
cost) · `ABSENT-OK` (Linux omits it under a named `CONFIG`).

## 1 dcache / dentry

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| `d_move` on rename | `__d_move` re-parents and renames the SAME dentry in place, so open fds, cwd and the cached subtree all track the new name | Two problems. (a) `d_move` itself does `d_drop(old)` + `d_add(new_parent,new_name,inode)`, minting a NEW dentry and leaving `old` unhashed with its old parent/name. (b) `d_move` has **ZERO production callers** — `rename` instead `drop_child_cache`s both names. The seqcount discipline around it is correct; the object identity is not | An open fd on a renamed file keeps its now-unhashed dentry ⇒ `/proc/<pid>/fd/N` and `d_path` render the OLD path plus ` (deleted)`; a process whose cwd directory is renamed reports the stale path from `getcwd`; a renamed directory's cached subtree is orphaned | CORRECTNESS | `kernel/vfs` | `fs/dcache.c:3050 __d_move`, `:3144 d_move` | crates/kernel/vfs/src/dcache/rename.rs:54-72 (no caller); crates/kernel/syscalls/src/082_rename.rs:177-184 |
| `RENAME_EXCHANGE` dcache | `__d_exchange` swaps both dentries in place | No `d_exchange`; the exchange arm only `drop_child_cache`s both names | Both dentries dropped rather than swapped — open fds on either side render stale/`(deleted)` paths | CORRECTNESS | `kernel/vfs` | `fs/dcache.c` `__d_exchange` | crates/kernel/syscalls/src/082_rename.rs:176-178 |
| `open(O_CREAT)` dentry | `->create` → `d_instantiate` leaves the dentry POSITIVE and HASHED; the opened `file->f_path.dentry` is that dentry | `open_dentry_at` correctly `d_add`/`d_splice_alias`es, then `drop_child_cache` immediately `d_drop`s it | The `File` from `open(O_CREAT)` holds an UNHASHED dentry ⇒ `is_unlinked()` ⇒ `/proc/self/fd/N` and `d_path` report ` (deleted)` for a freshly created file; next open mints a second dentry for the same inode | CORRECTNESS | `kernel/syscalls`, `kernel/vfs` | `fs/dcache.c:2167 d_instantiate`, `fs/namei.c:4182 vfs_create` | crates/kernel/syscalls/src/257_openat.rs:415-417; crates/kernel/vfs/src/file/open.rs:67 |
| `d_op`/`d_revalidate` coverage | `pid_dentry_operations`, `kernfs_dops`, `fuse_dentry_operations` install `.d_revalidate` | Hook exists and IS invoked from `d_lookup_reval`, but NO production fs installs any `d_op` — every real `DentryOps` is an anon-inode `d_dname`-only vector | `/proc/<pid>` dentries stay positive after task exit; stale sysfs/cgroupfs kernfs nodes; FUSE entry timeouts ignored | CORRECTNESS | `kernel/procfs`, `kernel/sysfs`, `kernel/kernfs` | `fs/proc/base.c:2043 pid_revalidate`, `fs/kernfs/dir.c:1171`, `fs/fuse/dir.c:383` | crates/kernel/syscalls/src/anon_dname.rs:23-46; crates/kernel/procfs/src/ (no dcache calls) |
| `d_revalidate` signature | `(struct inode *dir, const struct qstr *name, struct dentry *, unsigned int flags)` | `fn(d: &Arc<Dentry>, reval: bool)` — no `dir`, no `name`, flags collapsed to a bool | An fs cannot revalidate an in-lookup dentry, nor distinguish `LOOKUP_RCU` from `LOOKUP_REVAL`/`CREATE`/`EXCL` | CORRECTNESS | `kernel/vfs` | `include/linux/dcache.h dentry_operations.d_revalidate` | crates/kernel/vfs/src/dentry/ops.rs:13 |
| `complete_walk` / `d_weak_revalidate` | Fires `d_weak_revalidate` on a jumped final component; ESTALE → `retry_estale` with `LOOKUP_REVAL` | Implemented with ZERO callers; `LookupFlags::reval` never set outside tests; no ESTALE retry loop | Post-jump final component never weak-revalidated; no ESTALE re-walk | CORRECTNESS | `kernel/vfs` | `fs/namei.c:1050 complete_walk` | crates/kernel/vfs/src/dcache/alloc.rs:116 (no caller) |
| `LOOKUP_*` intent flags | `LOOKUP_EXCL/CREATE/OPEN/RENAME_TARGET/MOUNTPOINT/DOWN/JUMPED/NO_AUTOMOUNT` | None exist in `LookupFlags` | Create-vs-open-vs-rename intent is not threaded to backend `->lookup`/`->d_revalidate` | CORRECTNESS | `kernel/vfs` | `include/linux/namei.h LOOKUP_*` | crates/kernel/vfs/src/namei/types.rs:32-118 |
| `d_automount`/`d_manage` | `dentry_operations.d_automount`/`d_manage` gated by `DCACHE_MANAGED_DENTRY`; `LOOKUP_NO_AUTOMOUNT` suppresses | Moved to `i_op.is_automount()`/`i_op.automount()`; no `DCACHE_NEED_AUTOMOUNT`; `AT_NO_AUTOMOUNT` parsed by statx/newfstatat but no walker flag | Automount fires regardless of `AT_NO_AUTOMOUNT`; hook lives on the wrong object | CORRECTNESS | `kernel/vfs` | `fs/namei.c follow_automount` | crates/kernel/vfs/src/namei/walk.rs:355-358 |
| `d_op->d_hash`/`d_compare` (casefold) | `DCACHE_OP_HASH`/`OP_COMPARE` drive `generic_ci_*` for `S_CASEFOLD` ext4 dirs | Hooks + presence bits implemented; no fs installs them; `S_CASEFOLD` defined but unused for lookup | ext4 casefolded directories resolve case-sensitively | CORRECTNESS | `kernel/ext4` | `fs/libfs.c generic_ci_d_hash/compare` | crates/kernel/vfs/src/dentry/constructors.rs:15 |
| Create-path dcache mechanism | `d_instantiate`/`d_splice_alias` converts negative→positive at every `vfs_create`/`mkdir`/`mknod`/`symlink`/`link` | Every create path instead `d_drop`s the leaf (`drop_child_cache`) | Correct (no stale `ENOENT`) but the created name is never cached — the very next lookup takes the blocking `i_op->lookup` slow path | PERF | `kernel/syscalls` | `fs/namei.c:5105 vfs_mknod`, `fs/dcache.c:2167` | crates/kernel/syscalls/src/namei_common.rs:380 |
| Negative-dentry caching scope | Every fs whose `->lookup` returns NULL gets a hashed negative (kernfs, procfs, fuse included) | `neg_cache_ok` hard-codes `"ext4" \| "tmpfs" \| "ramfs"` by fs-type STRING | procfs/sysfs/cgroupfs/devtmpfs/fuse misses re-run `i_op->lookup` every time (dominant cost in `$PATH` / `ld.so` / systemd unit probing); also a string-keyed fs test rather than an `s_d_op`/fs-flag contract | PERF | `kernel/vfs` | `fs/namei.c:1929 lookup_slow` → `d_add(dentry, NULL)` | crates/kernel/vfs/src/namei/traverse.rs:12-17 |
| dcache hash sizing | `alloc_large_system_hash("Dentry cache", …, 13, …)` — RAM-scaled, ≥2^13 buckets | Fixed `DHASH_BITS = 8` (256 buckets); comment says "hosted/test scale" | Bucket chains reach thousands on a desktop workload; `key_matches` is a linear scan | PERF | `kernel/vfs` | `fs/dcache.c:3467 dcache_init` | crates/kernel/vfs/src/dcache/hash.rs:16-18 |
| Lock-free lookup probe | `__d_lookup_rcu` walks the hlist under RCU: no allocation, no refcount traffic | `lookup_rcu` takes the bucket lock and CLONES the whole `Vec<Arc<Dentry>>` (heap alloc + one atomic inc/dec per entry) per component | The "lock-free" probe is strictly more expensive than the locked one | PERF | `kernel/vfs` | `fs/dcache.c __d_lookup_rcu` | crates/kernel/vfs/src/dcache/hash.rs:116-129 |
| RCU-walk (`LOOKUP_RCU`) | Default walk mode; `try_to_unlazy`/`legitimize_*` fall back only on complication | Structurally present (`d_seq`, `rename_lock`, `unlazy_walk`) but `rcu: true` is set ONLY in tests; `d_count` self-admitted "DORMANT / built-but-unwired" so `inc_count_not_zero` conservatively fails | Ref-walk only in production; the entire rcu overlay is dead code | PERF | `kernel/vfs` | `fs/namei.c:935 try_to_unlazy`, `:1843 lookup_fast` | crates/kernel/vfs/src/namei/types.rs:106-117; state.rs:166-184 |
| Parallel lookup | `d_alloc_parallel` + `__d_lookup_done` share one `->lookup` between concurrent walkers | Implemented and tested, ZERO production callers — `walk_inner` runs a plain `i_op->lookup` under shared `i_rwsem` | N concurrent misses of one name each run the fs lookup and race in `cache_child` | PERF | `kernel/vfs` | `fs/dcache.c:2755 d_alloc_parallel` | crates/kernel/vfs/src/dcache/parallel.rs:49,82 |
| dcache shrinker | `prune_dcache_sb` registered as a `struct shrinker`, driven by `do_shrink_slab` under reclaim | LRU is fed (`dput`→`lru_add`) but `shrink_dcache_memory` is NEVER registered with `pmm::shrinker::register_shrinker` and has no caller | dcache grows unbounded; each hashed dentry strong-refs its parent chain and `igrab`s its inode, so the icache grows with it | PERF | `kernel/vfs`, `kernel/mm-pmm` | `fs/dcache.c:1406 shrink_dcache_sb`, `:864 retain_dentry` | crates/kernel/vfs/src/dcache/reclaim.rs:34-66 |
| `I_DONTCACHE` | `I_DONTCACHE` propagates to `DCACHE_DONTCACHE` via `d_mark_dontcache` | `D_DONTCACHE` bit + `set_dontcache` exist; no `I_DONTCACHE` inode flag and nothing sets the dentry bit | `retain_dentry`'s DONTCACHE arm unreachable | PERF | `kernel/vfs` | `fs/dcache.c:864 retain_dentry` | crates/kernel/vfs/src/dentry/flags.rs:16 |
| **Correction to the brief** — `bind()`/`mknod` stale negative | `d_instantiate` at every create | **Already fixed.** `049_bind.rs:38,52` and `133_mknod.rs:88` call `drop_child_cache`; `mkdir`(83), `mkdirat`(258), `symlink`(88), `link`(86), `openat O_CREAT`(257:416), `rename`(82), `unlink`(87), `rmdir`(84) likewise; every `*at` variant delegates to those bodies | Stale-`ENOENT`-after-create is no longer reachable through the syscall path. Residual risk only for kernel-internal creators (devfs/procfs), which are not `neg_cache_ok` so cache no negative | — | `kernel/syscalls` | `fs/namei.c:5105 vfs_mknod` | crates/kernel/syscalls/src/049_bind.rs:38,52; 133_mknod.rs:88 |
| Symlink depth caps | `MAXSYMLINKS` 40 total + `nd->depth` nesting cap | `MAX_SYMLINK_DEPTH` 40 + `MAX_NESTED_LINKS` 8, both enforced | none — matches | — | `kernel/vfs` | `include/linux/namei.h MAXSYMLINKS` | crates/kernel/vfs/src/namei/types.rs:9,17 |
| `d_ancestor` rename loop check | `is_subdir` rejects moving a dir into its own descendant | `d_ancestor`/`is_subdir_of` implemented and wired into rename | none — matches | — | `kernel/vfs` | `fs/dcache.c d_ancestor` | crates/kernel/vfs/src/dentry.rs:361,375 |

## 2 inode lifecycle, `i_writecount`, `may_open`

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| Unlinked-but-open inode | `ext4_unlink` → `ext4_orphan_add`; on-disk free deferred to `iput_final` → `evict` → `ext4_evict_inode` | `unlink` frees data blocks + inode bitmap INLINE the moment `links_count` hits 0, ignoring open fds; the orphan list is only ever populated by `O_TMPFILE` | The POSIX unlink-open-file idiom frees live blocks under an open fd; reallocation then corrupts the reader/writer. Used by every temp-file library, `mkstemp`+`unlink`, systemd, `dpkg`/`rpm` | BLOCKER | `kernel/ext4` | `fs/ext4/namei.c:3299 ext4_unlink`, `:3287 ext4_orphan_add`, `fs/inode.c:1975 iput_final` | crates/kernel/ext4/src/ialloc.rs:389-424; crates/kernel/ext4/src/rootfs/mod.rs:113 |
| `evict_inode` backend override | `ext4_evict_inode` frees blocks/inode at last `iput` | No production fs overrides `evict_inode`/`drop_inode`/`write_inode`; only the generic `clear_inode` default runs | VFS inode-lifecycle teardown is a no-op for the only real fs; on-disk teardown lives entirely in the unlink syscall path | BLOCKER | `kernel/ext4` | `fs/ext4/inode.c:169 ext4_evict_inode` | crates/kernel/ext4/src/rootfs/ops/mountfs.rs:21; crates/kernel/vfs/src/superblock/ops.rs:190,199 |
| `iget_locked` / `I_NEW` / freeing wait | `find_inode_fast` sees `I_FREEING\|I_WILL_FREE` and BLOCKS in `__wait_on_freeing_inode`, then retries | `ilookup` returns `None` for a freeing inode; `iget` then rebuilds OVER the dying slot | Two live in-core `Inode`s for one on-disk ino during eviction → divergent `i_size`/`i_nlink`/mode. `I_NEW` is set and cleared inside `iget` with no waiter, so it carries no synchronisation | CORRECTNESS | `kernel/vfs` | `fs/inode.c:1073 find_inode_fast`, `:802 evict` | crates/kernel/vfs/src/superblock/icache.rs:12-47 |
| Inode reference model | `i_count` is the single lifetime counter; `iput_final` the only free path | Two independent counters: `Arc<Inode>` strong count (actual free) and `i_count` (drives `drop_inode`/`evict_inode`); the icache holds only a `Weak` | An inode whose last `Arc` drops without a matching `sb.iput()` is freed with `evict_inode` never running — split source of truth for inode lifetime, acknowledged in-tree | CORRECTNESS | `kernel/vfs` | `fs/inode.c:2031 iput` | crates/kernel/vfs/src/superblock/icache.rs:20,58; superblock_wb.rs:1-9 |
| Inode dirty tracking | `__mark_inode_dirty` sets `I_DIRTY_*`, calls `s_op->dirty_inode`, moves the inode to the bdi/sb writeback list; `sync_inodes_sb` walks it | Full machinery present (`I_DIRTY_*`, `s_wb` pin list, `wb_reconcile`, `wb_writeback`) with ZERO production callers of `mark_inode_dirty`/`dirty_inode` — tests only | `s_wb` is always empty; `I_DIRTY_PAGES` never set; inode-level writeback in `sync`/`syncfs`/`fsync` is a no-op and durability rests solely on the ext4 shadow-batch commit | CORRECTNESS | `kernel/vfs`, `kernel/ext4` | `fs/fs-writeback.c:2674 __mark_inode_dirty` | crates/kernel/vfs/src/superblock/icache.rs:169; superblock_wb.rs:26 |
| Inode shrinker | `prune_icache_sb` shrinker over `sb->s_inode_lru` | No inode LRU; `drop_caches()` exists with no reclaim caller | icache only shrinks when a dentry alias drops, which never happens (§1 shrinker row) | PERF | `kernel/vfs` | `fs/inode.c prune_icache_sb` | crates/kernel/vfs/src/superblock_wb.rs:75 |
| `i_writecount` — execve side | `deny_write_access(file)` in `begin_new_exec` path → `ETXTBSY` | Field does not exist; zero repo-wide hits for `i_writecount`/`deny_write_access`/`get_write_access` | `execve` of a file currently open for write succeeds; a running binary's text can be rewritten underneath it | SECURITY | `kernel/vfs`, `kernel/exec` | `include/linux/fs.h:2817 deny_write_access`; `fs/exec.c:800 exe_file_deny_write_access` | absent; crates/kernel/vfs/src/inode/model.rs:46 (no field) |
| `i_writecount` — open/truncate side | `get_write_access(inode)` in `do_truncate` and `may_open` → `ETXTBSY` | Absent from both. `vfs_truncate` goes type-gate → `inode_permission` → `do_truncate`, skipping Linux's `get_write_access` + `break_lease` | `open(running_binary, O_WRONLY)` and `truncate()` on a live executable both succeed | SECURITY | `kernel/vfs` | `fs/open.c:104-112 vfs_truncate` (`get_write_access` then `break_lease`) | crates/kernel/fs/src/truncate.rs:79-90 |
| POSIX ACL enforcement on ext4 | `check_acl` → `get_inode_acl` → `ext4_get_acl` reads the on-disk `system.posix_acl_access` xattr; `acl_permission_check` then decides for every non-owner | `acl_decision` reads `inode.simple_xattrs()`, the VFS in-memory `i_xattrs` store. ext4 NEVER populates `i_xattrs` (zero hits in the whole crate) — it overrides the `i_op` xattr ops onto its own on-disk store instead. So `simple_xattrs()` is `None`, `check_acl` returns `None`, and every check falls back to plain mode bits | POSIX ACLs are silently ignored on the root filesystem, which `mkfs` marked `Default mount options: user_xattr acl`. Grants are lost (access denied that should be allowed) AND denials are lost: a named-user `ACL_USER` entry more restrictive than `other` is bypassed entirely | SECURITY | `kernel/vfs`, `kernel/ext4` | `fs/ext4/acl.c ext4_get_acl`; `fs/namei.c acl_permission_check` | crates/kernel/vfs/src/namei/permission.rs:81; crates/kernel/ext4/src/xattr.rs (no `i_xattrs` population) |
| Default-ACL inheritance | `posix_acl_create` applies a directory's `system.posix_acl_default` to every new child at `vfs_create`/`mkdir`/`mknod` | No `posix_acl_create` equivalent anywhere; `system.posix_acl_default` is stored and retrievable but never consulted at create | A directory with a default ACL does not propagate it — the mechanism shared directories are built on | SECURITY | `kernel/vfs` | `fs/posix_acl.c posix_acl_create`; `fs/namei.c vfs_create` | absent |
| `may_open` completeness | `IS_APPEND`→`EPERM`; `path_noexec`→`EACCES`; `may_open_dev` (`MNT_NODEV`)→`EACCES`; dir+`MAY_EXEC`; `O_NOATIME` non-owner→`EPERM` | Only symlink→`ELOOP`, dir+write→`EISDIR`, then `inode_permission(READ\|WRITE)` | `noexec`/`nodev` mount options bypassable at open; append-only and immutable inodes writable; `O_NOATIME` unrestricted | SECURITY | `kernel/vfs` | `fs/namei.c:4236 may_open` | crates/kernel/vfs/src/namei/permission.rs:171-182 |

## 3 file locking and leases

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| POSIX / OFD / flock owner model | `flc_owner` = `files_struct` for POSIX, the `struct file` for OFD, the `struct file` for BSD flock; close of ANY fd drops the process's POSIX locks | Same three-way split implemented: `RecordOwner::Posix(files_id)` / `Ofd(file_id)`, flock keyed on `file_id`; `release_file` drops the description's flock + OFD records | none — matches Linux | — | `kernel/vfs` | `fs/locks.c` `posix_lock_inode`, `flock_lock_inode` | crates/kernel/vfs/src/inode/file_lock/records.rs:23-30; context.rs:152-160 |
| Byte-range split/merge, `F_GETLK`, `F_SETLKW`, `EDEADLK` | `posix_locks_deadlock` walks the blocker graph, `MAX_DEADLK_ITERATIONS`; OFD locks skip detection (`FL_OFDLCK`) | Implemented, including the OFD skip and the iteration cap | none — matches | — | `kernel/fs` | `fs/locks.c:1083,1114` | crates/kernel/vfs/src/inode/file_lock/deadlock.rs:19,42-54 |
| Lease break on OPEN | `break_lease(inode, f->f_flags)` from `do_dentry_open`; blocks for `lease_break_time` (45 s), signals the holder, force-breaks on timeout | Implemented — `break_lease_for_open` signals holders, blocks with an interruptible loop, force-breaks at 45 s, `O_NONBLOCK`→`EWOULDBLOCK`. **Corrects the brief: a blocking lease break DOES exist.** | The wait is a `schedule()` busy-poll rather than a wait-queue sleep — an open blocked on a lease burns its whole timeslice repeatedly | PERF | `kernel/syscalls` | `fs/open.c:938 break_lease`; `fs/locks.c:1676 __break_lease` | crates/kernel/syscalls/src/open_common.rs:183-207 |
| Lease break on TRUNCATE | `vfs_truncate` calls `break_lease(inode, O_WRONLY)` after `get_write_access` | Not called anywhere in `truncate`/`ftruncate` | `truncate()` on a leased file silently bypasses the lease; the holder is never notified and never gets its 45 s to flush | CORRECTNESS | `kernel/fs` | `fs/open.c:112` | crates/kernel/fs/src/truncate.rs:79-107 |
| Lease/delegation break on unlink / rename / link / setattr / xattr | `try_break_deleg` at ~20 call sites in `fs/namei.c`, `fs/attr.c`, `fs/xattr.c`, `fs/posix_acl.c`, `fs/utimes.c` | `lease_break_signal` reachable only from `openat` | Directory mutations and metadata changes never break a lease | CORRECTNESS | `kernel/vfs` | `fs/namei.c:4199,5131,5272,5377,5509,6053…`; `fs/attr.c:550`; `fs/xattr.c:314` | crates/kernel/syscalls/src/257_openat.rs:477 (only caller) |
| Lease grant preconditions | `generic_add_lease` refuses a write lease when the inode has other openers (`check_conflicting_open`), requires `CAP_LEASE` or file ownership, and `F_RDLCK` requires the fd be readable | `F_SETLEASE` stores the type on the description with no conflicting-open check | A write lease can be granted on an inode that already has other open fds — the reverse of Linux's invariant | CORRECTNESS | `kernel/vfs` | `fs/locks.c generic_add_lease`, `check_conflicting_open` | crates/kernel/vfs/src/file/lease.rs:198-206; crates/kernel/syscalls/src/072_fcntl.rs:183 |
| `/proc/locks` | `fs/locks.c locks_seq_operations` renders every POSIX/FLOCK/LEASE record with pid, major:minor, ino, range | Registered as a STATIC EMPTY file (`StaticFileInode::new(b"")`) | Fabricated: readers (`lslocks`, Samba/NFS tooling, admin scripts) see "no locks held" and cannot distinguish that from a missing node | CORRECTNESS | `kernel/procfs` | `fs/locks.c` `locks_seq_operations` | crates/kernel/procfs/src/static_files.rs:90 |
| `/proc/sys/fs/lease-break-time` | Tunable; `lease_break_time` seconds | Constant `LEASE_BREAK_NS = 45 s`, no sysctl node | Not tunable; NFS/Samba setups that lower it cannot | CORRECTNESS | `kernel/vfs` | `fs/locks.c lease_break_time` | crates/kernel/vfs/src/file/lease.rs:35 |
| Mandatory locking | REMOVED upstream — `CONFIG_MANDATORY_FILE_LOCKING` was deleted in v5.15; `MS_MANDLOCK` now returns `EPERM` and no enforcement exists in `fs/locks.c` | Absent | Not a gap. **Correcting the brief: this is `ABSENT-OK` because Linux itself removed it, not because of a CONFIG choice** | ABSENT-OK | `kernel/vfs` | `fs/locks.c` (no `__mandatory_lock` remains); removal commit "fs: remove mandatory file locking support" | n/a |
| `dnotify` (`F_NOTIFY`) | `fcntl_dirnotify` + `dnotify_parent` at VFS chokepoints | Implemented with registry, one-shot vs `DN_MULTISHOT`, `SIGIO` delivery; emitted from create/delete/rename/attrib/fallocate | none material — matches | — | `kernel/vfs` | `fs/notify/dnotify/dnotify.c` | crates/kernel/vfs/src/file/lease.rs:180-195 |

## 4 writeback, sync, page-cache durability

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| `s_wb_err` errseq latch | Each superblock carries an `errseq_t`; `file->f_wb_err` snapshots at open; `fsync`/`syncfs` report a writeback error that occurred since open EXACTLY ONCE per fd via `errseq_check_and_advance` | Zero hits for `errseq`/`wb_err` repo-wide. `fsync` returns `EIO` only for an error it observes synchronously in this call; `syncfs` returns `EIO` only if `sync_filesystem` fails now | A writeback error that happened between `open` and `fsync` is invisible; the "did my data land" contract every database and journald relies on is unenforceable | CORRECTNESS | `kernel/vfs` | `lib/errseq.c errseq_check_and_advance`; `fs/sync.c`; `include/linux/fs.h s_wb_err` | absent; crates/kernel/fs/src/sync/fsync.rs:29-35; crates/kernel/syscalls/src/162_sync.rs:33 |
| `fdatasync` vs `fsync` | `ext4_sync_file(datasync)` skips the inode-metadata journal commit when only timestamps changed (`ext4_should_journal_data`/`EXT4_STATE_*` and the `i_size` test) | Both ext4 `FileOps::fsync` impls take `_datasync: bool` and IGNORE it | `fdatasync` costs exactly as much as `fsync` — the timestamp-only-writeback elision that is its entire purpose is missing | PERF | `kernel/ext4` | `fs/ext4/fsync.c ext4_sync_file`; `fs/libfs.c generic_file_fsync` | crates/kernel/ext4/src/rootfs/inode/regular.rs:210; special.rs:263 |
| Writeback threads / bdi | Per-bdi `wb_workfn` flusher threads; periodic writeback driven by `dirty_writeback_centisecs`; `dirty_expire_centisecs` ages dirty inodes | No bdi, no flusher thread, no periodic writeback anywhere. Data reaches disk only when an explicit `fsync`/`sync`/`syncfs`/batch-size trigger fires | Dirty data can sit in the page cache indefinitely with no ageing; a crash loses arbitrarily old writes that Linux would have flushed within 30 s | CORRECTNESS | `kernel/vfs`, `kernel/block` | `fs/fs-writeback.c wb_workfn`; `mm/page-writeback.c wb_wakeup_delayed` | absent |
| Dirty-page throttling | `balance_dirty_pages_ratelimited` on every buffered write; blocks the writer once dirty exceeds `vm.dirty_ratio`, wakes background writeback at `dirty_background_ratio` | No `balance_dirty_pages` equivalent. `/proc/sys/vm/dirty_ratio` and `dirty_background_ratio` EXIST as writable sysctl nodes with settled defaults (20/10) and are inert | Fabricated tunables: a writer can dirty memory without bound until reclaim or OOM. Tuning them (systemd, `sysctl.conf`, container runtimes) silently does nothing | CORRECTNESS | `kernel/vfs`, `kernel/mm-vmm` | `mm/page-writeback.c balance_dirty_pages` | crates/kernel/procfs/src/ctl.rs:224-225 |
| `vm.dirty_bytes` / `dirty_expire_centisecs` / `dirty_writeback_centisecs` | Present under `/proc/sys/vm/` | Missing entirely from the sysctl table | Readers/writers of these get `ENOENT`; tuning scripts fail | CORRECTNESS | `kernel/procfs` | `mm/page-writeback.c` sysctl table | crates/kernel/procfs/src/ctl.rs:221-227 |
| `sync_file_range(2)` | Pushes page-cache DATA over a byte range and explicitly NOT metadata; `VALID_FLAGS` gate, then `offset < 0`, then `endbyte < 0`, then `endbyte < offset`; `nbytes == 0` means "to EOF"; non-`REG`/`BLK`/`DIR` is `ESPIPE`, not `EINVAL` | Faithful port including the wrapping-sum reproduction, the inclusive `endbyte`, and the `ESPIPE` type gate | none — matches | — | `kernel/fs` | `fs/sync.c:223-292,341-346` | crates/kernel/fs/src/sync/range.rs:40-63 |
| `O_DIRECT` on regular files | `EINVAL` when the user buffer, file offset or length is not logical-block aligned; bypasses the page cache; `invalidate_inode_pages2_range` around the DIO for coherency | The flag is defined and settable through `open`/`F_SETFL`, and is consulted ONLY by `pipe.rs` (where `O_DIRECT` correctly selects packet-mode pipes). On regular files nothing reads it: no alignment gate, no cache bypass, no invalidation | Silently fully buffered. Software that relies on the `EINVAL` to discover its alignment requirements never sees it, and the "this write is not in the page cache" data-integrity assumption (databases, `dd oflag=direct`, `fio`) is false | CORRECTNESS | `kernel/fs`, `kernel/block` | `mm/filemap.c generic_file_direct_write`; `fs/ext4/file.c ext4_dio_write_iter` | crates/kernel/vfs/src/file/mode.rs:63,80; no consumer in fs/ext4/block |
| `sync(2)` / `syncfs(2)` | `ksys_sync` iterates `super_blocks` calling `sync_filesystem`; `syncfs` resolves `f_path` → sb | Implemented and correct in shape, including the bind-mount sb dedup and the deliberate choice NOT to promote `syncfs` on a tmpfs fd into an ext4 root commit | none material — matches | — | `kernel/syscalls` | `fs/sync.c ksys_sync`, `SYSCALL_DEFINE1(syncfs)` | crates/kernel/syscalls/src/162_sync.rs:12-24,33-60 |

## 5 ext4 and JBD2

Root images are built with **`mkfs.ext4 -O ^has_journal`** (`/home/nd/oxide/images/build.sh:111`,
`tools/xtask/src/rootfs_disks.rs:191`). `dumpe2fs -h gnome-x86_64-root.img` confirms the live
feature set: `ext_attr resize_inode dir_index filetype extent 64bit flex_bg metadata_csum_seed
sparse_super large_file huge_file dir_nlink extra_isize metadata_csum`, `signed_directory_hash`,
inode size 256, 4 KiB blocks — and **no `has_journal`**. Everything in the JBD2 rows below is
therefore dead code at boot; the rows still matter because the code is on the mount path for any
journalled image and because the images' journal-less state is itself the top finding.

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| Journal in shipped images | ext4 default is `has_journal` + `data=ordered`; crash consistency is the journal's job | Both image builders pass `-O ^has_journal`; `commit_metadata` detects `journal_inum == 0` and falls through to `apply_staged_to_target` (bare device writes, no ordering, no barrier) | Every metadata update on the booted root fs is unjournalled. An unclean shutdown leaves an inconsistent filesystem needing `e2fsck -f`; there is no crash-consistency guarantee at all on the images we actually boot | BLOCKER | `kernel/ext4`, images repo | `fs/ext4/super.c ext4_load_journal`; `Documentation/filesystems/ext4/journal.rst` | crates/kernel/ext4/src/journal.rs:77-81; /home/nd/oxide/images/build.sh:111; tools/xtask/src/rootfs_disks.rs:191 |
| JBD2 descriptor tag size | `journal_tag_bytes()`: `sizeof(journal_block_tag_t)` = **12** with `INCOMPAT_64BIT`, 8 without, 16 for `CSUM_V3` | Reader uses `let min = if self.bit64 { 16 } else { 8 }`; writer always emits 8-byte tags with no `t_blocknr_high` | Format mismatch in BOTH directions on any 64bit journal: oxide misparses a Linux-written descriptor (wrong `blocknr`s → replay writes journal payload to WRONG LBAs = filesystem destruction), and Linux misparses an oxide-written one. Latent only because the images have no journal | BLOCKER | `kernel/ext4` | `fs/jbd2/journal.c:2674 journal_tag_bytes`; `include/linux/jbd2.h:190 journal_block_tag_t` | crates/kernel/ext4/src/jbd2/descriptor.rs:64; jbd2/emit.rs:55-57 |
| Revoke sequence on replay | `jbd2_journal_test_revoke(blocknr, sequence)` skips a block only when `!tid_gt(sequence, record->sequence)` — "a revoke revokes that transaction and earlier ones; LATER transactions still need replay" | `revoke_set: BTreeSet<u64>` keyed on block number only; the recorded revoke sequence is discarded (the comment claims it is tracked; the type shows it is not) | A block revoked in txn N is also skipped in txn N+1 → recovery silently drops a legitimate later metadata write | CORRECTNESS | `kernel/ext4` | `fs/jbd2/revoke.c:709-721 jbd2_journal_test_revoke` | crates/kernel/ext4/src/jbd2/replay.rs:62,154 |
| Revoke emission | `jbd2_journal_revoke` emits revoke records so a freed-and-reused metadata block is not replayed over its new data | No revoke block is ever written. Justified in-tree by checkpointing (`s_start = 0`) after EVERY commit so only one transaction is ever live | Sound given the design, but it is exactly what forces the full-checkpoint-per-commit cost below — the two are coupled | PERF | `kernel/ext4` | `fs/jbd2/revoke.c jbd2_journal_revoke` | crates/kernel/ext4/src/journal.rs:100-103 |
| Journal checksums | `JBD2_FEATURE_INCOMPAT_CSUM_V2/V3`: per-tag + per-commit crc32c, descriptor/revoke block tails | `commit_metadata` DETECTS a csummed journal and silently falls back to unjournalled direct target writes; the replay path has no csum gate at all and will parse a csummed journal with the wrong tag layout | A journal-checksummed fs mounts with journalling silently disabled (write side) while the recovery side parses it with the wrong tag size (see the tag-size row) | CORRECTNESS | `kernel/ext4` | `include/linux/jbd2.h:282`; `fs/jbd2/recovery.c do_one_pass` | crates/kernel/ext4/src/journal.rs:104-108; jbd2/replay.rs (no csum check) |
| Transaction model | Running vs committing transaction, `start_this_handle` handle reservations, `JBD2_DEFAULT_MAX_COMMIT_AGE` 5 s commit interval, background `kjournald2` | No transaction object and no commit timer. `commit_metadata` performs descriptor + data + commit + `dev.flush` + target writes + `dev.flush` + journal-clean SYNCHRONOUSLY, inline in the caller. Batching exists only as a shadow map drained at `BATCH_MAX_BLOCKS = 512` or on `sync`/`fsync` | Every drain is a full checkpoint with three device flushes; there is no time-based commit, no handle reservation, no concurrent commit. This is the mechanism behind the historically measured per-page synchronous commits | PERF | `kernel/ext4` | `fs/jbd2/transaction.c start_this_handle`; `fs/jbd2/journal.c kjournald2` | crates/kernel/ext4/src/journal.rs:73-171; mount/batch.rs:48-57 |
| `data=ordered` | Default mode: file data is flushed BEFORE the metadata transaction commits, so a crash never exposes stale blocks through a newly-extended file | No data/metadata ordering mode exists; data writes and the metadata shadow are independent paths | Stale-data exposure after a crash on a file extension — the exact class `data=ordered` was introduced to prevent | SECURITY | `kernel/ext4` | `fs/ext4/super.c` `EXT4_MOUNT_ORDERED_DATA`; `fs/ext4/inode.c ext4_jbd2_inode_add_write` | crates/kernel/ext4/src/extent_rw/write.rs; journal.rs (no ordering) |
| Journal inode extent depth | `jbd2_journal_init_inode` maps the journal through the full extent tree | `ExtentLogReader::build` rejects `hdr.depth != 0` with `DepthUnsupported` — inline (≤4) extents only | A journal file fragmented into >4 extents makes `recover_journal` fail the mount AND makes every `commit_metadata` return an error, i.e. every metadata write fails | CORRECTNESS | `kernel/ext4` | `fs/jbd2/journal.c jbd2_journal_init_inode` | crates/kernel/ext4/src/journal.rs:196 |
| Commit block contents | `commit_header` carries `h_commit_sec`/`h_commit_nsec` and, with csum, `h_chksum` | `build_commit_block` writes header + zeros; self-admitted "v2-of-v1 will add those" | Commit timestamps absent; `e2fsck`/`jbd2` tooling reading a commit block sees epoch-0 times | CORRECTNESS | `kernel/ext4` | `include/linux/jbd2.h:164 commit_header` | crates/kernel/ext4/src/jbd2/emit.rs:66-70 |
| Unwritten-extent conversion | `ext4_split_convert_extents` SPLITS the unwritten extent so only the written range is converted; the untouched remainder stays unwritten and costs no I/O | `convert_unwritten_at` zeroes EVERY block of the covering extent (`zero_extent_blocks(e.start_lba(), e.real_len())`) then clears the flag — no split. **Corrects the brief: writes to unwritten extents are NOT rejected; conversion works.** | First write anywhere in a fallocated extent forces a synchronous zeroing of the whole extent. journald's preallocated `system.journal` (8 MiB in one extent) pays 8 MiB of zero-writes on its first log line; a 128 MiB fallocate pays 128 MiB | PERF | `kernel/ext4` | `fs/ext4/extents.c ext4_split_convert_extents`, `ext4_ext_handle_unwritten_extents` | crates/kernel/ext4/src/mount/blocks.rs:324-365,370-378 |
| Delayed allocation | Default `ext4_da_write_begin`/`ext4_writepages`: blocks reserved at write, allocated at writeback, so extents stay contiguous and `ENOSPC` surfaces early via reserved-cluster accounting | No delalloc; every write allocates immediately | Worse extent fragmentation than Linux, and `ENOSPC` timing differs from the reserved-cluster contract | PERF | `kernel/ext4` | `fs/ext4/inode.c ext4_da_write_begin` | crates/kernel/ext4/src/extent_rw/write.rs |
| Orphan list | `s_last_orphan` chain for unlinked-but-open and truncate-in-progress inodes; `ext4_orphan_cleanup` replays it at mount | Orphan list written only for `O_TMPFILE`; `unlink` frees inline instead (§2 row 1) | Combined with the inline free, an interrupted truncate or an unlink-while-open leaks or corrupts; `e2fsck` reports orphan/free-count damage | BLOCKER | `kernel/ext4` | `fs/ext4/orphan.c ext4_orphan_add`, `ext4_orphan_cleanup` | crates/kernel/ext4/src/rootfs/mod.rs:113 |
| Unsupported ro_compat handling | `ext4_feature_set_ok(sb, ro)` mounts READ-ONLY when only ro_compat features are unknown | Comment states "We have no RO-mount path yet"; an unknown ro_compat bit refuses the mount outright | An fs with `bigalloc`/`verity`/`orphan_file`/`ORPHAN_PRESENT` is unmountable rather than read-only-mountable | CORRECTNESS | `kernel/ext4` | `fs/ext4/super.c ext4_feature_set_ok` | crates/kernel/ext4/src/superblock.rs:46-52 |
| Unsupported incompat set | `EXT4_FEATURE_INCOMPAT_SUPP` includes `META_BG`, `MMP`, `INLINE_DATA`, `LARGEDIR`, `CASEFOLD`, `ENCRYPT` | `INCOMPAT_SUPP` = FILETYPE\|RECOVER\|EXTENTS\|64BIT\|FLEX_BG\|CSUM_SEED only; anything else refuses the mount | A resized fs (`meta_bg`), an `inline_data` fs, or a `largedir` fs will not mount. Honest (refuses rather than mis-parses) but a real coverage gap | CORRECTNESS | `kernel/ext4` | `fs/ext4/ext4.h EXT4_FEATURE_INCOMPAT_SUPP` | crates/kernel/ext4/src/superblock.rs:38-41 |
| `fallocate` COLLAPSE_RANGE / INSERT_RANGE | ext4 implements BOTH: `ext4_fallocate` dispatches to `ext4_collapse_range` and `ext4_insert_range`, which shift logical block numbers across the extent tree | Both are listed in `EXT4_SUPPORTED_MODES` but fall through the `match` to `_ => Eopnotsupp`. The in-tree comment asserts "Linux itself returns `EOPNOTSUPP` from these modes on filesystems that lack the shift, so the errno is the honest one" — for ext4 specifically that claim is FALSE | Two `fallocate` modes silently unavailable, defended by an unverified Linux claim of exactly the kind `CLAUDE.md` warns about. `fallocate --collapse-range`, log rotators and video tooling use them | CORRECTNESS | `kernel/ext4` | `fs/ext4/extents.c:4915-4919`, `:5525 ext4_collapse_range`, `:4708 ext4_insert_range` | crates/kernel/ext4/src/rootfs/inode/fallocate.rs:15-19,27-32,56 |
| `fallocate` ZERO_RANGE cost | `ext4_zero_range` converts the range to UNWRITTEN extents — O(1) metadata, no data I/O; reads serve zeros from the unwritten flag | Zeroes eagerly, writing the whole range and producing INITIALIZED extents | `fallocate --zero-range` on a large range writes every byte where Linux writes none. Same root cause as the unwritten-conversion row | PERF | `kernel/ext4` | `fs/ext4/extents.c ext4_zero_range` | crates/kernel/ext4/src/rootfs/inode/fallocate.rs:53-56 |
| Extent tree depth | `EXT4_MAX_EXTENT_DEPTH` 5, full read/write/split/merge | Implemented to depth 5 with insert, split and grow (`extent_rw/insert.rs`, `append.rs`, `truncate.rs`). **Corrects the brief's "inline only" worry — that limit now applies only to the journal reader.** | none material | — | `kernel/ext4` | `fs/ext4/extents.c` | crates/kernel/ext4/src/inode.rs:35; extent_rw/insert.rs |
| htree hash signedness (`s_hash_unsigned`) | The dx_root stores the BASE hash version (0/1/2). At hash time Linux adds `EXT4_SB(sb)->s_hash_unsigned` — 3 when the superblock's `s_flags` (offset 0x160) carries `EXT2_FLAGS_UNSIGNED_HASH`, 0 otherwise — turning `HALF_MD4` into `HALF_MD4_UNSIGNED` and so on | `s_flags` at 0x160 is NOT parsed anywhere in `superblock.rs`, and `dirhash_major` receives the raw dx_root version with no `+3` adjustment. Signedness is inferred solely from the stored version id | Latent: our images were built on x86_64 and carry `signed_directory_hash`, so oxide happens to agree. Any filesystem created or last touched by `mke2fs`/`e2fsck` on an unsigned-char arch (aarch64 — the second lockstep target) carries `unsigned_directory_hash` and will be hashed with the SIGNED variant: htree lookups miss so files are invisible in large indexed directories, and `htree_insert` files dx_entries under the wrong hash, corrupting the index | CORRECTNESS | `kernel/ext4` | `fs/ext4/namei.c:821,1175,2300`; `fs/ext4/super.c:5259-5266`; `fs/ext4/ext4.h:1243-1244,1624` | crates/kernel/ext4/src/superblock.rs:264-268 (0x160 unread); htree.rs:35-60,198 |
| htree directories | `ext4_dx_add_entry` → `do_split` → index growth; `EXT4_INDEX_FL`; hash versions incl. signed variants + `s_hash_seed` | Real htree insert with leaf split (`htree_split`) and index growth (`htree_grow`), dx_root/dx_node walk, legacy/half-md4/tea + signed variants. **Corrects the brief's linear-insert-into-htree worry.** | none material | — | `kernel/ext4` | `fs/ext4/namei.c ext4_dx_add_entry`, `do_split` | crates/kernel/ext4/src/htree.rs:188-280 |
| `metadata_csum` | crc32c on superblock, group descriptors, inode, bitmaps, extent tails, dirent tails, xattr blocks | Verified on read (`BadChecksum` → `EIO`) and recomputed on write across those objects | none material | — | `kernel/ext4` | `fs/ext4/*_csum` | crates/kernel/ext4/src/csum.rs; mount.rs:70-79 |

## 6 fsnotify — inotify / fanotify

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| Event filename | `inotify_handle_inode_event` copies the dirent name into the event; `inotify_read` emits `len` + NUL-padded `name[]` after the header | `Event.len` is hard-wired to 0 ("v1 doesn't track names yet"); `vfs_dirent_create`/`vfs_dirent_delete` receive `_leaf: &str` and DISCARD it; `read()` emits a 16-byte header with `len = 0` and no tail | A directory watch reports THAT something changed but never WHICH file. Every directory watcher is broken: systemd `.path` units / `PathChanged=`, `udev`, GLib `GFileMonitor` (all GNOME file dialogs, Nautilus, gnome-shell), `dconf`, `gio`, `inotifywait` | BLOCKER | `kernel/fs` | `fs/notify/inotify/inotify_fsnotify.c:122-124`; `inotify_user.c:158-212` | crates/kernel/fs/src/inotify/types.rs:143; group.rs:129,154; dispatch.rs:133,138 |
| Emit points | Chokepoints in `fs/namei.c`, `fs/open.c`, `fs/read_write.c`, `fs/attr.c` via `include/linux/fsnotify.h`, so every filesystem gets events for free | Hybrid: read/write/open/close/dirent-create/dirent-delete go through VFS hooks (`vfs::set_*_hook`), but attrib is bolted onto `090_chmod`/`092_chown`/`094_lchown`, move onto `082_rename`, `OPEN_EXEC` onto `059_execve`, xattr onto `fs/xattr/ops.rs` | Syscall-level emission means aliases and alternate paths drop events: `fchmodat`/`fchownat`/`utimensat`/`futimesat` (no `IN_ATTRIB`), `renameat2` exchange, `ftruncate` (no `IN_MODIFY`), `io_uring` file ops, and any in-kernel mutator | CORRECTNESS | `kernel/fs`, `kernel/syscalls` | `include/linux/fsnotify.h fsnotify_change/modify/open` | crates/kernel/fs/src/inotify/dispatch.rs:124-141 vs 090_chmod.rs, 082_rename.rs |
| Queue limits | `/proc/sys/fs/inotify/{max_user_watches,max_user_instances,max_queued_events}` are writable and enforced per user | Only `INOTIFY_DEFAULT_MAX_QUEUED_EVENTS` is real (queue depth). The three sysctl nodes exist as READ-ONLY `Const(b"65536\n")`/`"128"`/`"16384"` strings bound to nothing — not writable, not enforced, no per-user watch or instance accounting | An unprivileged process can create unlimited inotify instances and watches — exactly the resource-exhaustion class those limits bound. GNOME's file-monitor tuning advice (raise `max_user_watches`) silently does nothing | SECURITY | `kernel/fs`, `kernel/procfs` | `fs/notify/inotify/inotify_user.c` sysctl table | crates/kernel/procfs/src/ctl.rs:215-217; crates/kernel/fs/src/inotify/group.rs:97-103 |
| Group registry cost | Per-inode `fsnotify_mark_connector` hlist — an unwatched inode costs one NULL check | `dispatch` walks EVERY live group and EVERY watch in each on every hooked VFS operation, `O(N_groups × N_watches)` | With a desktop's inotify load (GNOME + systemd + dconf ≈ hundreds of watches) every read, write, open and close pays a global lock plus a linear scan | PERF | `kernel/fs` | `fs/notify/fsnotify.c fsnotify` | crates/kernel/fs/src/inotify/dispatch.rs:35-71 |
| **Correction to the brief** — fanotify | `fanotify_init`/`fanotify_mark`, inode/mount/filesystem mark scopes, ignore masks, and BLOCKING permission events (`FAN_OPEN_PERM`/`FAN_ACCESS_PERM`/`FAN_OPEN_EXEC_PERM`) that wait for a `FAN_ALLOW`/`FAN_DENY` response write | All of the above genuinely implemented, including the `PERM_MARK_COUNT` fast-path gate, the pending-response queue, group-teardown auto-allow, and a correctly shaped `fanotify_event_metadata` (`vers = 3`, `metadata_len = 24`) | Not a stub. The residual gap is `FAN_REPORT_FID`/`DFID_NAME`, which need the same filename plumbing as the inotify row above | CORRECTNESS | `kernel/fs` | `fs/notify/fanotify/fanotify_user.c` | crates/kernel/fs/src/inotify/syscalls.rs:95,255; group.rs:57,114-124,221-243 |
| `IN_UNMOUNT` | `fsnotify_unmount_inodes` fires `IN_UNMOUNT` + `IN_IGNORED` on every watched inode of a dying superblock | Not emitted | Watchers on an unmounted fs never learn their watch died | CORRECTNESS | `kernel/fs` | `fs/notify/fsnotify.c fsnotify_unmount_inodes` | absent |

## 7 mounts, propagation, mount namespaces

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| `setns(CLONE_NEWNS)` re-root | `mntns_install` swaps the nsproxy AND calls `set_fs_pwd(fs,&root)` + `set_fs_root(fs,&root)` so cwd and root move to the new namespace's tree | `setns_apply`'s `NsOwner::Mnt` arm calls only `replace_mount_namespace`; no counterpart exists | The joiner keeps its old root/cwd `(mnt_id, dentry)` and keeps resolving in the namespace it left — nominally inside the container, factually outside. `nsenter -m` is broken and container-join is an escape | SECURITY | `kernel/nscg` | `fs/namespace.c:6479 mntns_install`, `:6516-6517` | crates/kernel/nscg/src/proc_ns.rs:279 |
| `MNT_LOCKED` | `lock_mnt_tree` sets `MNT_LOCKED` on every mount when a mount ns is created under a different (unprivileged) userns, so the user cannot umount an inherited mount to reveal what it covers | The constant, the clone-preservation, and FOUR consumer checks (umount, move, pivot_root, expiry) all exist — but **nothing ever sets the bit**. Grep finds only `& MNT_LOCKED` preserve-reads | Worse than absent: a reviewer grepping `is_locked()` concludes it works. Every lock check is dead, so an unprivileged user ns can umount to reveal an underlying mount | SECURITY | `kernel/vfs` | `fs/namespace.c:2439,2213-2214,4723-4725` | crates/kernel/vfs/src/mount/mnt_flags.rs:25; attach.rs:238,321 |
| `mount(2)` flags on a FRESH mount | `path_mount` computes `mnt_flags` from the `MS_*` word and passes it to `do_new_mount` → `do_add_mount` | `sys_mount`'s final branch calls `mount_fstype_at(...)` with NO flags argument; `graft_mount` hardcodes `attach_sb_with_flags_at(..., 0, ...)` | `mount(src,tgt,"ext4",MS_RDONLY\|MS_NOSUID\|MS_NODEV\|MS_NOEXEC,…)` silently produces a rw/suid/dev/exec mount. Only a follow-up `MS_REMOUNT` or `mount_setattr` can set them | SECURITY | `kernel/syscalls` | `fs/namespace.c path_mount` | crates/kernel/syscalls/src/165_mount.rs:341; fsmount_common/mount_dispatch.rs:14 |
| `MNT_NOSUID` / `MNT_NODEV` enforcement | `may_open` device gate + `bprm_fill_uid` setuid suppression on nosuid mounts | Bits stored, typed readback exists, reported through `statvfs` `ST_*` — ZERO consumers in exec, mknod or cred paths | Decorative. Combined with the row above, they are never set by `mount(2)` in the first place either | SECURITY | `kernel/vfs` | `fs/namei.c may_open`; `fs/exec.c bprm_fill_uid` | crates/kernel/vfs/src/mount/mnt_flags.rs:102-104 (no callers) |
| `MNT_NOEXEC` enforcement | Rejected in `do_open_execat` on the `bprm` path | Enforced in `access(X_OK)` and in mmap `PROT_EXEC` widening — but NOT in `execve`/`execveat` | `execve()` of a binary on a `noexec` mount succeeds while `access(X_OK)` on the same file returns `EACCES`. The two answers contradict each other | SECURITY | `kernel/syscalls` | `fs/exec.c do_open_execat` | crates/kernel/syscalls/src/fs_access_common.rs:90 (absent in 059_execve/*) |
| `MNT_NOSYMFOLLOW` | `pick_link` refuses symlinks on the mount | Bit defined, settable via `mount_setattr`, reported through `statvfs` — no namei consumer | Symlinks on a `nosymfollow` mount are still followed | SECURITY | `kernel/vfs` | `fs/namei.c pick_link` | crates/kernel/vfs/src/mount/flags.rs:15 |
| Cross-namespace propagation | `propagate_mnt` walks the peer group and slaves across ALL namespaces | `propagation_targets` skips every mount whose `namespace_id() != ns`; peers are enumerated only from `mounts_in_ns(ns)` | Cross-namespace propagation does not exist. A shared `/` in the host never delivers a new mount into a child mount ns, or the reverse — the primary reason shared subtrees exist | CORRECTNESS | `kernel/vfs` | `fs/pnode.c:311 propagate_mnt` | crates/kernel/vfs/src/mount/propagation.rs:42,107 |
| `copy_mnt_ns` propagation type | `copy_flags |= CL_SLAVE` ONLY when `user_ns != ns->user_ns`; otherwise the copies stay SHARED in the SAME peer group | Every shared mount is cloned as `CloneType::Slave` unconditionally | Same-userns `unshare(CLONE_NEWNS)` demotes the whole tree to slave, losing two-way peer propagation; mountinfo shows `master:` where Linux shows `shared:`. This is the mechanism systemd leans on hardest | CORRECTNESS | `kernel/vfs` | `fs/namespace.c:4262-4264` | crates/kernel/vfs/src/mount/namespace_lifecycle.rs:43-50 |
| Shared AND slave | A mount can be both (`IS_MNT_SHARED` plus `mnt_master`), rendered `shared:X master:Y` | `Propagation` is a single-valued enum; `apply_propagation(Slave)` overwrites `peer_group` with the master's | A slave-of-a-group that is itself shared is unrepresentable, and mountinfo can never emit both tags | CORRECTNESS | `kernel/vfs` | `fs/pnode.c:93 change_mnt_propagation`; `fs/proc_namespace.c:160-165` | crates/kernel/vfs/src/mount/model.rs:4; propagation.rs:267-274 |
| `propagate_umount` / `propagate_mount_busy` | Dedicated set-based umount propagation handling slaves and overmounts; a busy test consulting peer and slave copies | `propagate_umount` exists inline in `unregister_top` (peers only, same-ns only, resolving the mirror by path-STRING plus `descend_mountpoint`). `propagate_mount_busy` is genuinely absent | Slave mirrors are never umounted; umount of a propagated mount never consults peers | CORRECTNESS | `kernel/vfs` | `fs/pnode.c:658,423` | crates/kernel/vfs/src/mount/detach.rs:143-164; expiry.rs:50-55 |
| MS_REC submount propagation | `propagate_mnt` runs for each mount in the recursive fan-out | `bind_submounts_rec_at` never propagates | Recursive binds do not reach peers | CORRECTNESS | `kernel/vfs` | `fs/namespace.c:2600-2603` | crates/kernel/syscalls/src/165_mount.rs:238,253 |
| Mount lookup key | `mount_hashtable` keyed on `(mnt_parent, mnt_mountpoint)`; every lookup carries a `struct path` | `__lookup_mnt(parent,d)` is correct, but `top_mount_on(ns,d)` resolves by DENTRY POINTER ALONE, taking `max(mnt_id)` across the whole namespace | Wrong wherever bind clones share a mountpoint dentry — the repo's own documented gotcha. Affects the `umount2` target, `propagate_mount`'s source, `set_propagation{,_recursive}`, `peer_group_of`, the `has_child_mounts` EBUSY test, `parent_by_dentry`, and `mountpoint_for_root_ptr` (`d_path`/`getcwd`). `mountpoint_of()` and the by-id remount/move/setattr entries ARE keyed correctly | CORRECTNESS | `kernel/vfs` | `fs/namespace.c __lookup_mnt` | crates/kernel/vfs/src/mount/graph.rs:249-263,267-290; model.rs:286-298 |
| umount busy detection | `do_umount`: `EBUSY` unless `propagate_mount_busy(mnt,2)`; the ns root without `MNT_DETACH` is `EBUSY` | Only `has_child_mounts()`; `unregister_top` detaches regardless of `mnt_count` or any task's cwd/root | `umount /mnt` with an open file, or a process whose cwd is inside, succeeds instead of returning `EBUSY` | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:1928,1959-1960` | crates/kernel/syscalls/src/166_umount2.rs:165-171 |
| `MNT_FORCE` | `sb->s_op->umount_begin` plus `ns_capable(s_user_ns, CAP_SYS_ADMIN)` | Validated, then never referenced again. (`MNT_DETACH` IS partially real — SB teardown rides `mnt_count` in `unregister_mount`) | `MNT_FORCE` is a no-op with no `umount_begin` call and no per-userns capability check | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:1915,1953,2034` | crates/kernel/syscalls/src/166_umount2.rs:45-50,164 |
| umount of a pseudo filesystem | Real detach of the procfs/sysfs/devtmpfs instance | `umount /proc`-class targets can return `0` having removed nothing (the synthetic backing is deliberately preserved) | Success reported for an unmount that did not happen; `/proc` stays visible | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:1870 do_umount` | crates/kernel/syscalls/src/166_umount2.rs:102,131,140,172-185 |
| Bind of an unbindable mount | `__do_loopback`: `if (IS_MNT_UNBINDABLE(old)) return -EINVAL` | `register_bind_clone_under` performs no unbindable check (only the MS_REC submount walk does) | `mount --bind` of an `MS_UNBINDABLE` mount succeeds | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:2983` | crates/kernel/syscalls/src/165_mount.rs:237 |
| `MS_SHARED`/`PRIVATE`/`SLAVE`/`UNBINDABLE` errno | `do_change_type` returns `EINVAL` when the path is not a mount root | The result is discarded (`let _ = set_propagation…`) and the branch always returns `0` | `mount(NULL, "/not/a/mount", NULL, MS_SHARED)` reports success | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:2862-2909` | crates/kernel/syscalls/src/165_mount.rs:288-293 |
| `mount(2)` options string | Each fs parses `data` through its `fs_parameter_spec` | Parsed only for tmpfs/ramfs/autofs/fuse; DISCARDED for ext4, proc, sysfs, cgroup2, devpts and the rest | `-o errors=remount-ro,data=ordered`, devpts `-o mode=0620,gid=5`, cgroup2 `-o nsdelegate` all silently ignored. Explains the devpts ownership row in §9 | CORRECTNESS | `kernel/syscalls` | `fs/fs_context.c vfs_parse_fs_string` | crates/kernel/syscalls/src/fsmount_common/registry.rs:121-166 |
| mountinfo MAJ:MIN (field 3) | `MAJOR(sb->s_dev):MINOR(sb->s_dev)` | Literal `0:{mnt_id}` — fabricated, even though `SuperBlock.s_dev` is real and `getattr` derives a correct maj:min from it | Every row claims major 0 with a unique minor, contradicting the same filesystem's `statx` `st_dev`. Breaks `findmnt --real`, `df` device mapping, and systemd `.device`/`.mount` correlation | CORRECTNESS | `kernel/procfs` | `fs/proc_namespace.c:142-144` | crates/kernel/procfs/src/mounts.rs:149-152 |
| mountinfo options (field 6) and `/proc/mounts` | `show_mountinfo`/`show_vfsmnt` emit `ro`/`rw` then the live `mnt_opts[]` table: `nosuid`, `nodev`, `noexec`, `noatime`, `nodiratime`, `relatime`, `nosymfollow`, `idmapped` | Both renderers (they are independent copies) emit `rw`/`ro` then an UNCONDITIONAL hardcoded `,relatime`, then the sb options | Fabricated field. Note the atime flags ARE honoured by `inode_times.rs` — only the rendering lies, and it asserts `relatime` even for a `noatime` mount. systemd `.mount` units compare rendered options against requested ones to decide convergence, so a unit with `Options=nosuid,nodev` never settles | CORRECTNESS | `kernel/vfs`, `kernel/procfs` | `fs/proc_namespace.c:63-83,88-110` | crates/kernel/vfs/src/mount/render.rs:53-60; procfs/src/mounts.rs:78-88 |
| mountinfo `propagate_from:` and `parent_id` | `get_dominating_id` emits `propagate_from:N` when the slave's master is outside the reader's root; field 2 is `mnt_parent->mnt_id` (self for the root mount) | `propagate_from:` never emitted; `parent_id` is a literal `0` for the ns root | Nested-container mountinfo loses the dominating-peer link; the root row's parent is `0` where Linux prints a real id | CORRECTNESS | `kernel/vfs`, `kernel/procfs` | `fs/pnode.c:56`; `fs/proc_namespace.c:142,164-167` | crates/kernel/vfs/src/mount/render.rs:64-72; procfs/src/mounts.rs:142 |
| `/proc/<pid>/mountstats` | `show_vfsstat`: `device X mounted on Y with fstype Z` per mount | Registered as `pc_empty` — always zero bytes | The file exists and is empty; `mountstats` and nfs-utils read nothing | CORRECTNESS | `kernel/procfs` | `fs/proc_namespace.c:193` | crates/kernel/procfs/src/live/pid_dir.rs:97 |
| `statmount` request mask | `ks->mask = kreq->param`; each `statmount_*` helper sets its bit only when requested AND filled | `req->param` is never read; a FIXED mask (`SB_BASIC\|MNT_BASIC\|MNT_ROOT\|MNT_POINT\|FS_TYPE\|MNT_NS_ID`) is returned for every call | The caller cannot select fields, and the returned mask is not derived from what was asked | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:5873` | crates/kernel/syscalls/src/457_statmount.rs:37-46,94-95 |
| `statmount` field truth | `statmount_sb_basic` fills `sb_dev_major/minor`/`sb_flags`; `statmount_mnt_basic` fills `mnt_attr`/`propagation`/`peer_group`/`master`; `statmount_mnt_root` renders the real root path | The mask CLAIMS `SB_BASIC`+`MNT_BASIC` but only `sb_magic`, `mnt_id`, `mnt_parent_id`, `mnt_ns_id` are written; `sb_dev_major/minor`, `sb_flags`, `mnt_attr`, `mnt_propagation`, `mnt_peer_group`, `mnt_master` stay zero. `mnt_root` is the literal `"/"` for every mount including binds | The mask lies about zero-filled fields — a caller cannot distinguish "not set" from "set to zero". Bind roots are misreported | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:5304-5344` | crates/kernel/syscalls/src/457_statmount.rs:88,94-108 |
| `statmount` unpopulated masks | `PROPAGATE_FROM`, `MNT_OPTS`, `FS_SUBTYPE`, `SB_SOURCE`, `OPT_ARRAY`, `OPT_SEC_ARRAY`, `SUPPORTED_MASK`, `MNT_UIDMAP`/`GIDMAP`; `STATMOUNT_BY_FD`; cross-ns via `req->mnt_ns_id` | None populated; `flags != 0` → `EINVAL`; `req->mnt_ns_id` ignored; a foreign-ns mount is `ENOENT` | systemd and `mountfsd` field probes see an empty capability set | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:5330-5510,5708` | crates/kernel/syscalls/src/457_statmount.rs:66,68,77-82 |
| `listmount` | `mnt_find_id_at` tree walk from `LSMT_ROOT`/target, `is_path_reachable` subtree test, `nr_mnt_ids > 1000000` → `EOVERFLOW` | `LSMT_ROOT` and `last_mnt_id` continuation implemented; but subtree membership is a path-STRING PREFIX compare on `mount_point_str()`, ordering comes from BTreeMap iteration, there is no `EOVERFLOW` cap, and `mnt_ns_id` is ignored | String-prefix reachability disagrees with the mount tree exactly where the bind/dentry-sharing gotcha bites; `nr_mnt_ids` allocation is unbounded | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:6026-6083,6139-6140` | crates/kernel/syscalls/src/458_listmount.rs:32-35,62-76 |
| Idmapped mounts | `MOUNT_ATTR_IDMAP` plus a userns fd installs `mnt_idmap`; `vfsuid`/`vfsgid` applied in `getattr`/`notify_change`/`inode_permission` | The `Idmap` type and `Mount.mnt_idmap` exist and are threaded into `statx`/`getattr`, but `mount_setattr` hard-rejects `MOUNT_ATTR_IDMAP` with `EINVAL` and `fsmount` excludes it — NO writer ever leaves `Idmap::identity()` | Idmapped mounts are entirely absent; the plumbing is inert | CORRECTNESS | `kernel/vfs` | `fs/namespace.c build_mount_kattr` | crates/kernel/syscalls/src/442_mount_setattr.rs:54-56; vfs/src/mount/model.rs:387 |
| `mount_setattr` `AT_RECURSIVE` | Applies BOTH the attribute set and `change_mnt_propagation` across the subtree | Option bits recurse (with a whole-subtree writer pre-check for RDONLY, matching `mnt_hold_writers`); the propagation change is applied to the TOP mount only | `mount_setattr(AT_RECURSIVE, .propagation=MS_SLAVE)` leaves every submount shared — half-recursive | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c:4921,4952` | crates/kernel/syscalls/src/442_mount_setattr.rs:137-157 |
| `move_mount(2)` flags | `MOVE_MOUNT_{F,T}_EMPTY_PATH`/`SYMLINKS`/`AUTOMOUNTS`, `MOVE_MOUNT_SET_GROUP`, `MOVE_MOUNT_BENEATH` | `args.a4` is read only for a debug hex dump; empty-path is inferred structurally; no flag is validated | Garbage flag bits are accepted; `SET_GROUP` and `BENEATH` are unimplemented but not rejected; symlink policy is unconfigurable | CORRECTNESS | `kernel/syscalls` | `fs/namespace.c SYSCALL_DEFINE5(move_mount)` | crates/kernel/syscalls/src/429_move_mount.rs:45,55 |
| nsfs ioctls | `NS_GET_PARENT`, `NS_GET_USERNS`, `NS_GET_NSTYPE`, `NS_GET_OWNER_UID`, `NS_GET_MNTNS_ID` | Zero hits repo-wide; only `SIOCGSKNS` exists | A `/proc/pid/ns/mnt` fd cannot be walked to its owning userns; `lsns` and systemd-nspawn introspection are blind | CORRECTNESS | `kernel/syscalls` | `fs/nsfs.c ns_ioctl` | crates/kernel/syscalls/src/016_ioctl/netns.rs (only) |
| `mnt_id` vs `mnt_id_unique` | A 32-bit IDR-recycled `mnt_id` plus a separate never-recycled 64-bit `mnt_id_unique` | ONE 64-bit monotonic never-recycled counter serves both; `statx` picks which to report from `request_mask` | Deliberate and documented, but `STATX_MNT_ID` returns a value wider than 32 bits, which a caller reading `stx_mnt_id` as `int` truncates | CORRECTNESS | `kernel/vfs` | `fs/namespace.c mnt_alloc_id` | crates/kernel/vfs/src/mount/model.rs:16-28; syscalls/src/statx_abi.rs:158 |
| **Correction to the brief** — `unshare`/`clone(CLONE_NEWNS)` | `copy_mnt_ns` re-points `new_fs->root`/`pwd` onto the cloned mounts | HANDLED: `snapshot_ns_map` returns an old→new id map and `remap_task_fs_paths` → `task.remap_fs_mount_ids` translates root/pwd. `clone(CLONE_NEWNS)` funnels through the same `NamespaceChange::CloneChild` path | The re-root gap is `setns`-only, narrower than the brief's "setns/unshare/clone" | — | `kernel/syscalls` | `fs/namespace.c:4290-4300` | crates/kernel/syscalls/src/272_unshare.rs:249-256 |
| **Correction to the brief** — new mount API | `fsopen`/`fsconfig`/`fsmount`/`open_tree`/`fspick`/`move_mount`/`mount_setattr`/`statmount`/`listmount` | All 13 syscalls are dispatch-wired with real handlers; none is `ENOSYS`. `FSCONFIG_SET_FLAG/STRING/BINARY/PATH/PATH_EMPTY/CMD_CREATE/CMD_RECONFIGURE` are real; `open_tree` honours `CLONE`/`AT_RECURSIVE`/`AT_EMPTY_PATH`/`CLOEXEC` | Gaps are field-level, not presence-level. Exceptions: `FSCONFIG_SET_FD` is always `EINVAL` (so fd-valued params, e.g. a loop fd, are impossible) and `CMD_CREATE_EXCL` is collapsed onto `CMD_CREATE` (not exclusive) | CORRECTNESS | `kernel/syscalls` | `fs/fsopen.c SYSCALL_DEFINE5(fsconfig)` | crates/kernel/syscalls/src/431_fsconfig.rs:71-75 |
| `MNT_READONLY` vs `SB_RDONLY` | Per-mount `MNT_READONLY` distinct from superblock `SB_RDONLY`; `MS_REMOUNT\|MS_BIND` touches only the former | Modelled correctly: `remount_flags_by_id` → mount flags only; `remount_super_flags_by_id` → `reconfigure_super` plus mount flags; `mnt_want_write` returns `EROFS` on `MNT_RDONLY` | none — matches | — | `kernel/vfs` | `fs/namespace.c do_reconfigure_mnt` | crates/kernel/vfs/src/mount/attrs.rs:20-44,155 |
| `pivot_root(2)` admission ladder | The 9-rung ladder in `path_pivot_root` | Faithful line-for-line port with hosted (ungated) ordering tests; `may_mount` uses `ns_capable(mnt_ns->user_ns, CAP_SYS_ADMIN)` | Compliant, except that its `MNT_LOCKED` rung is dead (see the `MNT_LOCKED` row) | — | `kernel/vfs` | `fs/namespace.c path_pivot_root:4701,4723` | crates/kernel/vfs/src/mount/pivot_check.rs:61-71 |
| Atime policy | `atime_needs_update`/`touch_atime` honour `MNT_NOATIME`/`NODIRATIME`/`RELATIME`, the `SB_*` equivalents, `S_NOATIME`, the 24 h relatime staleness window and `SB_RDONLY` | Implemented faithfully, including `RELATIME_MAX_AGE_NS` and the strictatime fallback | none — matches | — | `kernel/vfs` | `fs/inode.c relatime_need_update` | crates/kernel/vfs/src/inode_times.rs:28-40 |
| xattr namespace gating | `xattr_permission`: `trusted.*` needs `CAP_SYS_ADMIN` and is list-invisible without it; `security.capability` needs `CAP_SETFCAP`; `user.*` needs write permission and is refused on non-regular/non-dir | Implemented, including list-invisibility and `cap_convert_nscap` validation | none — matches | — | `kernel/fs` | `fs/xattr.c xattr_permission`; `security/commoncap.c` | crates/kernel/fs/src/xattr/policy.rs:53-198 |

## 8 procfs

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| `/proc/<pid>` access control | `proc_pid_permission` → `ptrace_may_access(PTRACE_MODE_READ_FSCREDS)` gates `maps`/`environ`/`mem`/`fd`/`fdinfo`/`io` | No permission hook anywhere in procfs. A comment claiming the check is "enforced at the /proc file open" is false — `may_access` appears only in ptrace/kcmp/setns | Any uid reads any pid's `environ`, `maps`, `smaps`, `fd/`, `fdinfo/`, `io`. systemd passes credentials through the environment | SECURITY | `kernel/procfs` | `fs/proc/base.c:746 proc_pid_permission` | crates/kernel/procfs/src/live/pid_files.rs:212; pid_dir.rs:111 |
| `/proc` file modes and owners | `REG("environ", S_IRUSR,…)`, `auxv` 0400, `pagemap` 0400, `mem` 0600, `clear_refs` 0200; inode owned by the task's uid | Every dynamic proc file is `0o444` world-readable; inode uid/gid never set to the task owner | DAC provides no backstop for the missing ptrace gate above | SECURITY | `kernel/procfs` | `fs/proc/base.c:3291-3325` | crates/kernel/procfs/src/dyn_file.rs:14 |
| `/proc/<pid>/status` `Uid:`/`Gid:` | `from_kuid_munged(cred->uid…)` | Literal `Uid:\t0\t0\t0\t0` / `Gid:\t0\t0\t0\t0`, `Groups:` empty — EVERY process reports root | Fabricated. Breaks `ps -u`, `pgrep -u`, and polkit/logind pid→user mapping, which is a security decision input | SECURITY | `kernel/procfs` | `fs/proc/array.c:188,192` | crates/kernel/procfs/src/pid_status.rs:32 |
| `/proc/<pid>/status` `Cap*` | Rendered from `cred->cap_{inh,prm,eff,bnd}` | Literal `CapPrm/CapEff/CapBnd = 000001ffffffffff` for every task | Fabricated. Self-capability probes (`capsh`, systemd, rsyslog) see full capabilities on unprivileged processes and skip their own privilege drops | SECURITY | `kernel/procfs` | `fs/proc/array.c:302,325-327` | crates/kernel/procfs/src/pid_status.rs:36 |
| `/proc/<pid>/status` `NoNewPrivs`, `Seccomp` | Rendered from `task->no_new_privs` and the live seccomp mode | Hardcoded `NoNewPrivs:\t0`, `Seccomp:\t0`, `Seccomp_filters:\t0` | Fabricated. A process that set `PR_SET_NO_NEW_PRIVS` or installed a seccomp filter still reports neither — sandbox self-verification (systemd, Flatpak, browsers) reads exactly this | SECURITY | `kernel/procfs` | `fs/proc/array.c task_seccomp` | crates/kernel/procfs/src/pid_status.rs:40 |
| `/proc/<pid>/status` `Vm*`, `Threads`, `Sig*`, `Cpus_allowed` | `task_mem` + `task_sig` render VmPeak/VmSize/VmRSS/RssAnon/RssFile/VmData/VmStk/VmExe/VmSwap, thread count, SigBlk/SigIgn/SigCgt, ctxt switches, real affinity mask | All `Vm*` lines absent; `Threads: 1`, `FDSize: 256`, every `Sig*` mask `0`, both ctxt-switch counters `0`, `Cpus_allowed: 1` / `Cpus_allowed_list: 0` — all hardcoded | `top`/`htop`/GNOME System Monitor read no RSS; signal-mask introspection dead; affinity tooling sees a uniprocessor regardless of `smp` | CORRECTNESS | `kernel/procfs` | `fs/proc/array.c task_mem/task_sig/task_cpus_allowed` | crates/kernel/procfs/src/pid_status.rs:34-42 |
| `/proc/<pid>/stat` fields | 52 live fields (pgrp, session, tty_nr, priority, nice, num_threads, vsize, rss, stime…) | The loop emits literal ` 0` for fields 5..52 except 14/22/40/41 and the mm bounds | `ps` TTY/NI/RSS/VSZ columns all zero; the rendered `nice` disagrees with the live scheduler value — a split source of truth | CORRECTNESS | `kernel/procfs` | `fs/proc/array.c do_task_stat` | crates/kernel/procfs/src/pid_stat.rs:60-66 |
| `/proc/<pid>/statm` | `size resident shared text lib data dirty` | `pages pages 0 0 0 0 0` — resident always equals size | Fabricated RSS (always == VSZ) | CORRECTNESS | `kernel/procfs` | `fs/proc/task_mmu.c proc_pid_statm` | crates/kernel/procfs/src/live/pid_files.rs:171 |
| `/proc/<pid>/maps` pathname | `show_map_vma` prints `dev:inode` plus the resolved file path, and `[vdso]`/`[vvar]`/`[stack]`/`[heap]` | Always ` 00000000 00:00 0 ` with no path for file-backed VMAs | Every mapping looks anonymous — `pmap`, gdb, coredump tooling and ld.so introspection are blind | CORRECTNESS | `kernel/procfs` | `fs/proc/task_mmu.c show_map_vma` | crates/kernel/procfs/src/live/pid_files.rs:84 |
| `/proc/self/maps` vs `/proc/<pid>/maps` | One `seq_file` renderer serves both | TWO independent renderers; the `self` version synthesises `[stack]`/`[heap]`, the pid version does not | Split source of truth: the same process reads different text via `self` than via its own pid | CORRECTNESS | `kernel/procfs` | `fs/proc/task_mmu.c` | crates/kernel/procfs/src/self_files.rs:19 vs live/pid_files.rs:62 |
| seq_file partial reads | `seq_read_iter` snapshots at open, buffers, and resumes by record | The body is REGENERATED on every `read()` and the byte offset applied to the fresh copy | Chunked reads of a changing file (`maps`, `stat`, `meminfo`) tear — lines duplicate or vanish mid-read | CORRECTNESS | `kernel/procfs` | `fs/seq_file.c seq_read_iter` | crates/kernel/procfs/src/dyn_file.rs:31 |
| `smaps_rollup`, `numa_maps` | Distinct formats — rollup is one aggregate block; numa_maps is `addr policy N0=…` | `smaps_rollup` aliased to the smaps constructor; `numa_maps` aliased to the maps constructor | Fabricated: the wrong format under a real filename, so parsers mis-read rather than fail | CORRECTNESS | `kernel/procfs` | `fs/proc/task_mmu.c:3324` | crates/kernel/procfs/src/live/pid_dir.rs:74-75 |
| `wchan`, `syscall`, `stack`, `schedstat`, `auxv`, `pagemap` | Live values | `wchan`="0", `syscall`="running", `stack`="", `schedstat`="0 0 0", `auxv`=16 zero bytes, `pagemap`/`kpagecount`/`kpageflags`="" | Fabricated constants. The `auxv` comment claims Linux parity while every exec'd task has a real auxv | CORRECTNESS | `kernel/procfs` | `fs/proc/base.c:3292,3325` | crates/kernel/procfs/src/live/pid_dir.rs:34,43,48,57,131 |
| `loginuid` / `sessionid` | Per-task `task->loginuid`/`sessionid`, audit-gated write | `/proc/self/loginuid` is ONE globally shared writable byte slot; `/proc/<pid>/loginuid` is a fresh slot per lookup so writes are lost. `self/sessionid`="4294967295" vs `<pid>/sessionid`="0" | `pam_loginuid`'s write becomes visible to every process on the system; the two paths disagree | CORRECTNESS | `kernel/procfs` | `kernel/audit.c audit_set_loginuid` | crates/kernel/procfs/src/static_files.rs:198-209 |
| Missing per-pid entries | `tgid_base_stuff[]` | Absent: `mem`, `clear_refs`, `timers`, `map_files/`, `net/`, per-pid `oom_adj` | gdb and CRIU have no `mem`; `clear_refs`-based profilers have no path | CORRECTNESS | `kernel/procfs` | `fs/proc/base.c:3286-3340` | crates/kernel/procfs/src/live/pid_dir.rs:68-109 |
| Missing global nodes | `proc_create` set | Absent: `thread-self`, `slabinfo`, `iomem`, `ioports`, `kmsg`, `sysvipc/*`, `tty/{drivers,ldiscs}`, `consoles`, `schedstat`, `timer_list`, `sched_debug`, `kcore`, `vmallocinfo` | `/proc/thread-self` is glibc- and systemd-visible; `ipcs` and `lsof` lose their sources | CORRECTNESS | `kernel/procfs` | `fs/proc/root.c` | crates/kernel/procfs/src/static_files.rs:65-101 |
| `zoneinfo`, `softirqs`, `cgroups`, `net/*` | Live counters | `zoneinfo`="pages free 1024", `softirqs`="TIMER: 1234", `cgroups` claims 5 controllers at hierarchy 0; `netlink`/`packet`/`snmp6`/`netstat`/`protocols`/`sockstat*`/`igmp`/`wireless` are static headers with zeros | Fabricated constants that read as plausible live data | CORRECTNESS | `kernel/procfs` | `kernel/cgroup/cgroup.c proc_cgroupstats_show` | crates/kernel/procfs/src/static_files.rs:83,86,93,268-342 |
| procfs mount options | `hidepid=`, `gid=`, `subset=pid` via `proc_parse_param` | `ProcfsFs` takes no options at all | systemd `ProtectProc=` / `ProcSubset=` units cannot mount their private `/proc` | CORRECTNESS | `kernel/procfs` | `fs/proc/root.c proc_fs_parameters` | crates/kernel/procfs/src/fs_impl.rs:41-60 |
| `/proc/meminfo` keys | ~50 keys | Missing `Buffers`, `SwapCached`, `Mlocked`, `CommitLimit`, `Committed_AS`, `AnonHugePages`, `HugePages_*`, `DirectMap*` | `free(1)`'s Buffers column and all overcommit tooling | CORRECTNESS | `kernel/procfs` | `fs/proc/meminfo.c` | crates/kernel/procfs/src/meminfo.rs:22-51 |
| `/dev/kmsg` read format | `devkmsg_read`: one record per `read()`, `"<prio>,<seq>,<usec>,<flag>;<text>"`, `EPIPE` on overrun | Returns the raw formatted console byte ring — a free-running byte stream with no prio/seq/usec/flag prefix and no record framing | journald's `dev_kmsg_record` parse fails, so kernel-log import silently drops every record | BLOCKER | `kernel/devfs`, `shared/klog` | `kernel/printk/printk.c:801 devkmsg_read` | crates/kernel/devfs/src/misc.rs:283; crates/shared/klog/src/lib.rs:314 |
| `/proc/pressure/*` | PSI `psi_show` + `O_RDWR` trigger + `POLL_PRI` | Real: live `sched::psi`, trigger parse, `POLL_PRI`-only poll | none — matches | — | `kernel/procfs` | `kernel/sched/psi.c` | crates/kernel/procfs/src/pressure.rs:36-63 |
| `/proc/sys` | `ctl_table` bound to live variables | Real declarative tree whose leaves bind to genuine owners (`nr_open`→fdtable, `suid_dumpable`→cred, `modules_disabled`, mqueue→ipc ns, `ip_forward`/`somaxconn`→net) | Unbound leaves (`swappiness`, `overcommit_*`, `dirty_*`) accept and store without effect — see §4 | CORRECTNESS | `kernel/procfs` | `fs/proc/proc_sysctl.c` | crates/kernel/procfs/src/ctl.rs:1-30,371 |

## 9 sysfs, cgroupfs, devpts, tmpfs, memfd sealing

| Area | Linux mechanism | Oxide state | Gap | Severity | Owner crate | Linux ref | Oxide ref |
|---|---|---|---|---|---|---|---|
| sysfs / kernfs | kernfs nodes bound to kobjects with `show`/`store` | Real dynamic tree over the netdev/cpu/block/drm/input/tty/bus registries; genuine relative `/sys/class/<c>/<n>` symlinks into `../../devices/…`; working `/sys/dev/{char,block}` reverse index; `uevent` WRITES re-emit netlink with env, so `udevadm trigger` works. **Corrects the brief: this is not a flat fabricated tree.** | kernfs is a path-keyed `PseudoDir` with no kobject refcounting; no `power/` dir on device dirs; no bin_attributes (PCI `config`/`resource0` mmap) | CORRECTNESS | `kernel/sysfs`, `kernel/kernfs` | `fs/kernfs/dir.c`, `lib/kobject_uevent.c` | crates/kernel/sysfs/src/lib.rs:110-140; bus/hooks.rs:20-21 |
| cgroup v2 controller enforcement | memory/cpu/io/pids/cpuset enforce | memory + swap genuinely charge and FAIL allocations across slab/pagetables/percpu/kstack/ext4 cache; `cpu.max` genuinely throttles via `cpu_bandwidth_decision`; io charges; `cpu.weight`→CFS. **Corrects the brief: enforcing, not accounting-only.** | none for the implemented controllers | — | `kernel/cgroup` | `kernel/cgroup/cgroup.c`, `mm/memcontrol.c` | crates/kernel/cgroup/src/policy.rs:37-70 |
| cgroup `cpu.stat` | `usage_usec`/`user_usec`/`system_usec` + `nr_periods`/`nr_throttled`/`throttled_usec` from live cputime | Literal `usage_usec 0\nuser_usec 0\nsystem_usec 0`, despite `sched::cpustat` existing | Fabricated zeros: `systemd-cgtop` and `systemctl status` CPU accounting read exactly this | CORRECTNESS | `kernel/cgroup` | `kernel/sched/core.c:10354-10359` | crates/kernel/cgroup/src/tree/files.rs:71 |
| cgroup PSI files | `cpu.pressure`, `memory.pressure`, `io.pressure`, `cgroup.pressure` per cgroup | Absent — only the global `/proc/pressure/*` exists (and those are real) | `systemd-oomd` and per-unit `MemoryPressureWatch=` cannot arm | CORRECTNESS | `kernel/cgroup` | `kernel/cgroup/cgroup.c:3975-4082` | crates/kernel/cgroup/src/tree/controllers.rs:73-99 |
| cgroup interface completeness | v2 base file set | Absent: `memory.peak`, `memory.reclaim`, `memory.numa_stat`, `cpu.idle`, `cpu.weight.nice`, `cpuset.cpus.partition`, `io.prio.class`, `misc.*`, `hugetlb.*`. `cgroup.type` returns a hardcoded `domain\n` with no write, so threaded mode does not exist | Delegation and threaded workloads have no path | CORRECTNESS | `kernel/cgroup` | `kernel/cgroup/cgroup.c cgroup_base_files` | crates/kernel/cgroup/src/tree/files.rs:33 |
| devpts mount options and slave ownership | `Opt_{uid,gid,mode,ptmxmode,max,newinstance}` parsed; `devpts_pty_new` sets the slave's owner to the caller's uid and the mount's gid | No option parsing at all; `PTY_SLAVE_MODE` hardcoded `0o620` and no `set_owner` on the slave inode, so slave nodes are root:root | A non-root `openpt` peer gets a root-owned 0620 node; `mesg`/`wall`/`write` and the `gid=tty` contract are broken | CORRECTNESS | `kernel/devpts` | `fs/devpts/inode.c:87-97,490` | crates/kernel/devpts/src/lib.rs:44 |
| `TIOCGPTPEER` | Open the pty peer without a name lookup | Not implemented anywhere in the tree | systemd's `pty_open_peer` falls back; container pty setup is less robust | CORRECTNESS | `kernel/devpts`, `kernel/tty` | `drivers/tty/pty.c ptm_open_peer` | absent |
| tmpfs mount options | `shmem_parse_options` size/nr_blocks/nr_inodes/mode/uid/gid | Parsed AND enforced, including percent form, k/m/g suffixes and accounting charges | `huge=`, `noswap`, `inode32/64` are accepted and ignored | ABSENT-OK (`CONFIG_TRANSPARENT_HUGEPAGE=n` for `huge=`) | `kernel/fs` | `mm/shmem.c shmem_parse_options` | crates/kernel/fs/src/tmpfs/mount_opts.rs:80-119 |
| `F_SEAL_EXEC` in the seal mask | `F_ALL_SEALS` includes `F_SEAL_EXEC` (0x0020) | `valid = SEAL\|SHRINK\|GROW\|WRITE\|FUTURE_WRITE`; `F_SEAL_EXEC` → `EINVAL` | Confirms the brief. W^X memfd sealing is unusable from userspace | SECURITY | `kernel/syscalls` | `include/uapi/linux/fcntl.h:52`; `mm/memfd.c:223,270` | crates/kernel/syscalls/src/072_fcntl.rs:114 |
| `F_SEAL_WRITE` vs writable mappings | `mapping_deny_writable` at seal time (`EBUSY` if already shared-writable) plus `mapping_map_writable` blocking a later `mmap(PROT_WRITE, MAP_SHARED)` | Neither check exists; `F_ADD_SEALS` just `fetch_or`s the bit | A sealed memfd stays writable through any existing or new shared mapping — the seal's entire guarantee. Not in the brief | SECURITY | `kernel/syscalls`, `kernel/mm-vmm` | `mm/memfd.c:291-301` | crates/kernel/syscalls/src/072_fcntl.rs:119 |
| `F_ADD_SEALS` preconditions | `!(file->f_mode & FMODE_WRITE)` → `EPERM`; `F_SEAL_EXEC` on an `i_mode & 0111` file implies `SHRINK\|GROW\|WRITE\|FUTURE_WRITE` | Both absent | A read-only fd can seal; the implied-seal escalation is missing | CORRECTNESS | `kernel/syscalls` | `mm/memfd.c:268,288-290` | crates/kernel/syscalls/src/072_fcntl.rs:110-122 |
| `F_SEAL_EXEC` chmod enforcement | `shmem_setattr`: `(seals & F_SEAL_EXEC) && (ia_valid & ATTR_MODE)` → `EPERM` | tmpfs has no `setattr`/chmod hook at all; `F_SEAL_EXEC` is not even a constant in `tmpfs/flags.rs` | Confirms the brief. `chmod +x` on a `MFD_NOEXEC_SEAL` memfd succeeds | SECURITY | `kernel/fs` | `mm/shmem.c:1316,1329` | crates/kernel/fs/src/tmpfs/flags.rs:1-6 |
| `vm.memfd_noexec` sysctl | Per-pidns `memfd_noexec_scope`, levels 0/1/2 | Absent; the scope is hardcoded to `MEMFD_NOEXEC_SCOPE_EXEC` (self-admitted in the comment) | Confirms the brief. Container hardening policy unavailable | CORRECTNESS | `kernel/syscalls` | `mm/memfd.c check_sysctl_memfd_noexec` | crates/kernel/syscalls/src/319_memfd_create.rs:52 |
| `F_SEAL_{SHRINK,GROW,WRITE,FUTURE_WRITE}` on write/truncate/fallocate | shmem write/setattr/fallocate checks | Real and enforced | none — matches | — | `kernel/fs` | `mm/shmem.c:3228,3624` | crates/kernel/fs/src/tmpfs/file.rs:401-429 |
| `binfmt_misc` | `search_binary_handler` → `load_misc_binary` consults the registered rules at execve | `register`/`status` parse and store rules; NOTHING consults the table at execve (self-admitted) | Worse than absent: registration succeeds, `systemd-binfmt` reports success, and foreign binaries still `ENOEXEC`. A silent no-op beats a clean `ENOENT` | CORRECTNESS | `kernel/fs` | `fs/binfmt_misc.c load_misc_binary` | crates/kernel/fs/src/binfmt_misc.rs:5,223 |
| tracefs registration | One tracefs instance | TWO writers: `tracefs::init` registers into tracefs's own root, AND `procfs::static_files` registers `/sys/kernel/tracing/*` and `/sys/kernel/debug/tracing/*` as static string inodes into sysfs | Split source of truth — the sysfs copies are dead constants shadowing the live tracefs inodes | CORRECTNESS | `kernel/procfs`, `kernel/tracefs` | `fs/tracefs/inode.c` | crates/kernel/procfs/src/static_files.rs:215-238 |

## Ordering

By impact, highest first.

**Security first — three fabrications that userspace uses to make privilege decisions.**

1. **`/proc/<pid>/status` reports `Uid: 0` and full capabilities for every process** (§8) — two
   hardcoded string literals. polkit and logind map pids to users through this; `capsh` and
   systemd probe their own capabilities through it and skip privilege drops when it says they
   already have none to drop. Fabricated data feeding a security decision is the worst shape in
   this audit.
2. **No `ptrace_may_access` gate on `/proc/<pid>`, and every file is mode 0444** (§8) — any uid
   reads any process's `environ`, `maps`, `fd/`, `io`. Two independent protections, both absent,
   so neither backstops the other. `NoNewPrivs`/`Seccomp` are likewise hardcoded to 0, so sandbox
   self-verification reads a clean lie.
3. **POSIX ACLs are silently ignored on ext4** (§2) — the ACL evaluator is correct but reads the
   VFS in-memory xattr store, which ext4 never populates. The root image is mkfs'd with `acl` in
   its default mount options. A named-user entry more restrictive than `other` is bypassed.
   Default-ACL inheritance does not exist either.
4. **`mount(2)` drops the whole `MS_*` flag word on a fresh mount, and `nosuid`/`nodev`/`noexec`
   are never enforced anyway** (§7) — two independent failures stacking into one hole.
   `mount(…, MS_RDONLY|MS_NOSUID|MS_NODEV|MS_NOEXEC, …)` yields a rw/suid/dev/exec mount, and even
   when the bits ARE set (via `mount_setattr`) nothing reads them. `execve` on a `noexec` mount
   succeeds while `access(X_OK)` on the same file returns `EACCES`.
5. **`MNT_LOCKED` is never set** (§7) — the constant, the clone-preservation and four consumer
   checks all exist, so the code reads as implemented. Nothing sets the bit, so every check is
   dead and an unprivileged user ns can umount an inherited mount to reveal what it covers.
6. **`setns(CLONE_NEWNS)` never re-roots** (§7) — a task enters the mount namespace but keeps
   resolving `/` and `.` through the old tree. Small fix, containment-escape impact.

**Then the things userspace cannot work around.**

7. **inotify events carry no filename** (§6) — one field. Every directory watcher in the desktop
   stack (systemd `.path` units, udev, `GFileMonitor`, Nautilus, dconf) is told THAT something
   changed but never WHICH file. Cheapest large win here.
8. **`/dev/kmsg` returns raw console text, not devkmsg records** (§8) — journald's parser fails on
   every line, so kernel-log import silently drops everything.
9. **ext4 frees an unlinked inode's blocks while fds are still open** (§2) — the POSIX
   unlink-open idiom is data corruption, not a leak. Needs the orphan list wired to `evict_inode`.
10. **`binfmt_misc` registers rules that execve never consults** (§9) — a silent no-op that reports
   success. Either wire it or return `ENOENT` honestly.

**Then durability and the on-disk format.**

11. **Root images ship `-O ^has_journal`** (§5) — the booted filesystem has no crash consistency at
   all. Decide deliberately: enable the journal and fix the JBD2 format bugs below, or record the
   no-journal design as intentional. It cannot stay implicit.
12. **JBD2 descriptor tag size wrong for 64bit** (§5) — would destroy a filesystem the moment a
   journalled image is recovered. Must land before any journal work.
13. **htree hash ignores `s_hash_unsigned`** (§5) — an aarch64-created ext4 filesystem is hashed
    with the wrong variant: files invisible in large directories, and inserts corrupt the index.
    Directly in the path of the ARM lockstep rule.
14. **No `data=ordered`, no revoke sequence on replay** (§5) — stale-data exposure after a crash,
    and recovery silently dropping legitimate later writes.
15. **`s_wb_err` absent, no dirty throttling, no periodic writeback** (§4) — three parts of one
    hole: nothing ages dirty data, nothing bounds it, nothing reports a failure to write it.

**Then correctness with a smaller blast radius.**

16. **Cross-namespace mount propagation does not exist; `unshare` slaves unconditionally** (§7) —
    shared subtrees never cross a namespace boundary, and same-userns `unshare` demotes the whole
    tree to slave. Both are the propagation behaviour systemd depends on.
17. **Mount lookup by dentry alone where bind clones share a dentry** (§7) — the repo's own
    documented gotcha, still live in `umount2`, propagation, `d_path` and the EBUSY test.
18. **`mount(2)` discards the options string for every fs except tmpfs/ramfs/autofs/fuse** (§7) —
    `-o` is silently ignored for ext4, devpts, cgroup2, proc and sysfs. Explains the devpts
    ownership breakage in §9.
19. **mountinfo MAJ:MIN, options, `propagate_from:` and `parent_id` are fabricated or missing**
    (§7) — systemd parses this file to decide whether a mount unit has converged.
20. **`i_writecount` absent** (§2) — `ETXTBSY` impossible in both directions.
21. **`may_open` misses `noexec`/`nodev`/append-only/`O_NOATIME`** (§2) — mount options stored but
    not enforced at open.
22. **`d_move` has no callers and would mint a new dentry anyway** (§1) — renamed files and
    directories render stale ` (deleted)` paths through `/proc/<pid>/fd` and `getcwd`.
23. **procfs renders fabricated `Vm*`/`stat`/`statm`/`maps` fields** (§8) — `ps`, `top` and every
    memory tool read zeros or VSZ-as-RSS.
24. **seq_file regenerates the body on every `read()`** (§8) — chunked reads tear.
25. **Lease break reaches only `open`** (§3) — truncate, unlink, rename and setattr bypass it.
26. **`/proc/locks` empty, `vm.dirty_*` inert, `cpu.stat` zeros** (§3, §4, §9) — nodes that answer
    plausibly and mean nothing are worse than absent ones.
27. **Unwritten-extent conversion zeroes the whole extent** (§5) — the concrete mechanism behind
    journald's historical writeback cost; splitting makes it proportional to the write.

**Then cost.**

28. **dcache/icache never reclaimed, 256 hash buckets, ref-walk only, negative caching limited to
    three fs types by string** (§1) — unbounded growth plus a linear scan on the hottest path in
    the kernel.
29. **fsnotify dispatch is `O(groups × watches)` on every hooked VFS op** (§6).
30. **No JBD2 transaction batching or commit timer** (§5) — every drain is a full checkpoint with
    three device flushes.

## What I could not determine

- Whether the `open(O_CREAT)` unhashed-dentry ` (deleted)` rendering is observable end to end. It
  is a traced code path (`257_openat.rs:415` → `open_dentry_at` → `d_drop` → `is_unlinked()` →
  `dentry_path`), not a measurement — no boot, no hosted repro.
- Whether the ext4 unlink-while-open free manifests as corruption in practice or whether page-cache
  timing shields short-lived cases. `unlink` explicitly invalidates the page cache, which argues
  against shielding, but I built no repro.
- Real-world cost of the 256-bucket dcache hash and the per-lookup bucket clone. The reasoning is
  structural; there is no profile.
- Whether anything invalidates `/proc/<pid>` dentries out of band at task exit. procfs makes no
  dcache calls and installs no `d_op`, but I did not trace the reap path.
- `e2fsck` cleanliness of a filesystem after an oxide write session. The project's stated gate is
  `e2fsck`, and this audit ran no filesystem work — the counter/csum/orphan reasoning above is from
  reading the write paths only.
- Whether the JBD2 tag-size bug is reachable on any image the project actually builds. Every image
  I inspected has no journal, so the path is dead today; I could not rule out a fixture or a
  future image that enables one.
- Lock-rank correctness of the `i_rwsem`(40)/`Dentry`(50)/`Superblock`(60) ordering asserted in
  comments throughout `vfs` — not verified against a lockdep run.
- Whether `cgroup.procs` writes re-charge already-accounted pages on migration, or only affect
  future charges.
- Whether `cgroup.freeze`/`cgroup.kill` writes reach the signal and scheduler paths. The tree
  stores `frozen`; the delivery hook was not traced.
- `pids.max` enforcement at `fork`/`clone` — no `cgroup_can_fork` equivalent was found, but
  `sched::live::spawn` was not exhaustively traced.
- Whether sysfs attribute reads are snapshotted at lookup or re-read live. Both patterns exist
  (`make_body_inode` snapshots; `SysClassNetOps` and zram re-resolve); no per-attribute census.
- devpts multi-instance: whether two `mount -t devpts` produce independent pty number spaces.
- `SEEK_HOLE`/`SEEK_DATA` and xattr support on tmpfs.
- Whether `poll()` on `/proc/<pid>/*` or on sysfs attributes returns anything useful. Only PSI was
  confirmed working.
- Whether propagation's `descend`-based mirror placement handles an OVERMOUNT at a peer's target
  dentry. The code re-materialises the slot per target rather than adopting the source mountpoint
  dentry as Linux does; no test covers it.
- Whether `MS_REMOUNT|MS_BIND` matches Linux's `can_change_locked_flags` rules for unprivileged
  remounts. There is no counterpart, but whether unprivileged `mount(2)` is reachable at all was
  not established — the syscall gates on plain `has_cap(SYS_ADMIN)`, not `ns_capable`.
- Whether a bind mount of a REGULAR FILE works end to end. No explicit rejection and no test were
  found; `graft_bind_realized_with_flags` does not type-check the dentry.
- The runtime effect of the missing cross-namespace propagation on a live systemd boot. The boot
  reportedly works, suggesting systemd's `MS_SLAVE|MS_REC` usage masks it — inference, not
  measurement.
- Whether the `mnt_ns_id` `statmount` reports (the internal `identity.id()`, `0` for the initial
  namespace) is the value systemd expects; `MntNamespace` also carries a distinct `ns_id()` that
  looks like the better match.
