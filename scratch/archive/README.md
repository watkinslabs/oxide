# scratch/archive

Closed history: fixed/superseded ledger rows (`fixed-issues.md`, ids preserved),
finished session docs (`done/`), captured evidence (`evidence/`), journal prose
and overflow evidence moved out of the live ledger. NEVER grep or bulk-read this
directory while working — open a specific file only when chasing a named id or
incident.
