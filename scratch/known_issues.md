# Known issues

Every row here is work this project WILL do. There is no not-going-to-fix status
and no deferral: a divergence from Linux is either fixed or it is an open row
waiting for its lane. A row that records a shape we chose on purpose still names
the work that makes it right, because "we meant to" is not a reason to leave
Linux behaviour unimplemented.

Any lane that finds an issue adds a row in the same PR that finds it, in its own
drop file `scratch/issues.d/<branch>.md`. Any lane that fixes one flips it to
`FIXED <sha>` and MOVES it to `scratch/fixed-issues.md` in the same PR. Rows are
never deleted, only relocated.

`Status`: `OPEN` | `IN-PROGRESS <branch>` | `FIXED <sha>`.

`Owner`: `NEXT` is the queue head. Those rows are claimed before any unowned
work and become `IN-PROGRESS <branch>` when the current integration finishes.

`Class` says what KIND of work the row needs, never whether it gets done:

| Class | Means |
|---|---|
| `DEFECT` | behaviour diverges from Linux — wrong answer, wrong order, wrong lifetime |
| `MISSING` | Linux surface that is absent, or present-but-unconsumed (stored and never read) |
| `COVERAGE` | the behaviour may be right, but no check here can fail if it stops being right |
| `INFRA` | tooling, gates, docs, images, or the dev box itself |

`Sev`: `blocker` (merge gate) | `high` (wrong answer reaching userspace) |
`med` (missing surface) | `low` (hygiene, tooling, cosmetics).

## Open-work summary

This is a derived view of the live `OPEN` and `IN-PROGRESS` rows below, not a
second ledger. Update it in the same change whenever a row is added, moved, or
reclassified.

| Class \ Sev | blocker | high | med | low | Total |
|---|---:|---:|---:|---:|---:|
| `DEFECT` | 0 | 0 | 13 | 19 | 32 |
| `MISSING` | 2 | 0 | 14 | 11 | 27 |
| `COVERAGE` | 0 | 0 | 9 | 14 | 23 |
| `INFRA` | 0 | 0 | 14 | 11 | 25 |
| **Total** | **2** | **0** | **50** | **55** | **107** |

Never delete a row to make the list look shorter. A row with no owner is still a
row. Retired rows and folded duplicates live in `scratch/fixed-issues.md`.

## Net / socket

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | DEFECT | med | `cargo test --workspace --no-fail-fast` on `main` (`460dc8e89`) is NOT green and its failing set is UNSTABLE run to run — same class as this row (global process state shared by parallel tests), none of them owned here. Run 1 failed `drv-virtio-input` (`devfs::tests::lifetime::{reused_event_number_cannot_retarget_old_open_file, state_reconciliation_flushes_only_querying_client}`, both at `lifetime.rs:24` — a `publish_endpoint` collision on fixed event ids), `fbcon` (`kernel::tests::try_lock_vt_excludes_the_flush_softirq`), `softirq` (`tests::raise_then_run_invokes_handler`), plus `pmm`/`socket`. Run 2 (this branch) failed a DIFFERENT `fbcon` test (`tests::one_line_of_output_damages_only_that_text_row`) and a `net` test that was green in run 1 (`stack::ethernet::tests::ethernet_ingress_rejects_a_truncated_link_header_before_l3`, `Option::unwrap()` on None at `net/src/stack/core.rs:193`), while `drv-virtio-input` and `softirq` passed. So per-package green does not imply workspace green, and one workspace run does not characterize the failing set. | Two full runs captured; failing package lists differ (`drv-virtio-input,fbcon,pmm,socket,softirq` vs `fbcon,net,socket`). | — |
| OPEN | COVERAGE | low | A live connected AF_VSOCK transport still has no portable host-oracle case. Hosted full-usercopy regressions pin the zero-length source name and the zerocopy completion cmsg, while hosted transport sends pin completion IDs, copied fallback, and `POLLERR`; a real transport differential remains absent. | Native Linux 7.2 option/error-queue oracle; `vsock_shutdown_tests::{connected_recvmsg_clears_source_length_without_synthesizing_a_peer_address,zerocopy_completion_uses_the_vsock_error_queue_abi}`; `vsock_socket::zerocopy::tests`; `tools/network-conformance-manifest.tsv` row 47. | unowned |
| OPEN | DEFECT | low | **The reset-penalty list stores expiry deadlines, not the request socks themselves.** The reference parks the whole request for 60 s so it holds a reference; here the request is already unhooked by the ordinary teardown path, so `FastOpenQueue` keeps only `now + RST_PENALTY_NS` per penalty. Observably identical — the overflow check only ever reads the head's deadline, pops it and decrements `qlen` — but a future consumer that wants to *identify* the parked requests will not find them. Deliberate: keeping a second, fast-open-only registry of request socks beside the connection table would be a split source of truth. | `tcp_fastopen/queue.rs Outstanding.penalties: VecDeque<u64>`; pinned by `server_tests::a_reset_connection_a_program_had_taken_keeps_charging_for_a_minute`, which walks the deadline to the nanosecond. | won't fix |
| OPEN | MISSING | low | Landlock logging is inert: the three `LANDLOCK_RESTRICT_SELF_LOG_*` flags, the `quiet_*` ruleset masks and the per-rule `LANDLOCK_ADD_RULE_QUIET` marking are all validated and carried, but nothing consumes them because this kernel has no audit subsystem. Identical to a Linux built without audit support, and no access decision changes — recorded because "stored but unconsumed" is otherwise indistinguishable from a bug. | `crates/kernel/landlock/src/uapi.rs`; `Ruleset::{quiet_fs,quiet_net,quiet_scoped}` are consumed by the `ADD_RULE_QUIET` admission test, `FsRule::quiet`/`NetRule::quiet` are not consumed at all. | unowned |
| OPEN | COVERAGE | low | The `sendto`/`sendmsg` Landlock hooks (port rights on an explicit destination; `ACCESS_FS_RESOLVE_UNIX` on a pathname recipient) are pinned only by source-inspection tests in `socket/src/tests/landlock_hooks.rs`, not by a behavioural one. The decision itself is behaviourally covered in `net::landlock_addr`, but the call site cannot be driven hosted: the domain is read off the running task and a hosted build has none. A wrong ARGUMENT at the call site (right socket, wrong bytes) would not be caught. Closing it needs a hosted `SendContext` fixture that can carry a domain. | F791. | — |
| OPEN | COVERAGE | low | `TCP_ZEROCOPY_RECEIVE` still has no hosted coverage of real user-address VMA resolution or `copy_to_user` fault delivery. | Page donation/remap ownership is now driven by a hosted executor. The remaining target-only `window_at` and `copy_out` operations require a narrow task-mm/uaccess fixture rather than a second receive queue. | unowned |
| OPEN | COVERAGE | low | The scripted socket cannot block, so "a receive that would have waited" is modelled as a distinct errno (`WOULD_BLOCK`) that the batch then latches, and the host's `SO_ERROR` of 0 is what contradicts it. This detects a dropped `MSG_WAITFORONE` conversion but is not proof of real wait behavior. | `mmsg_batch::fake::WOULD_BLOCK`; case `recvmmsg.waitforone_leaves_nothing_latched_after_the_drain`. | unowned |
| OPEN | COVERAGE | low | **The child-creation failure rung of the ladder cannot be exercised.** The reference can fail to allocate the child socket after a cookie verifies, and falls back to offering the current cookie with an ordinary handshake. Here the child already exists when the decision is applied — it is the entry the passive open built — so `Passive::Accept` cannot fail for that reason. The nearest failure, a full accept backlog, drops the child instead (`publish_fastopen_child`), matching what the ordinary passive path does with a backlog it cannot satisfy. | `stack/tcp_listener_deliver.rs publish_fastopen_child`. No test, because the state is unreachable. | won't fix |
| OPEN | COVERAGE | low | **A stamp written into `ClientCache` from a clock other than the one it is read with reads as stale, not as fresh.** `get`/`set` age entries with `now_ns.wrapping_sub(stamp_ns) >= ENTRY_TIMEOUT_NS`, so a stamp in the future underflows to a huge age and the entry reads as a miss. Safe in the direction that matters — a miss opens the ordinary way — and every production caller uses `crate::tcp_conn::ka_now_ns()`. Recorded because it cost a test failure that looked like a wiring bug. | `tcp_fastopen/cache.rs get`; `sock::tcp_fastopen::tests` originally stamped with a literal and read back with `ka_now_ns()`. | won't fix |
| OPEN | COVERAGE | low | Negative result, recorded so it is not re-investigated: udev and sysfs are NOT implicated in the empty NetworkManager device list. `/run/udev/data/n2` is written (84 bytes) and NM's own `udev-add[eth0,2]`/`udev-add[lo,1]` fire. | B1717. Boot log + NM trace, 2026-08-02. | unowned |

## Filesystem / mount

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | DEFECT | med | Not yet attributed: the combined x86 boot exercising `swapfile_probe` + `request_key_probe` hit `[CPU-STALL] cpu=0 no heartbeat for 10s (seen by cpu=1) last: tid=4181 syscall=fsync ... now: tid=4181 syscall=madvise` on attempt 1. That is the swapfile probe's `MADV_PAGEOUT` ladder. The C probe issued the same `MADV_PAGEOUT`, so it is most likely pre-existing rather than caused by the port — but the A/B that would prove it has NOT been run, and this row must not be read as either a new regression or a cleared one. | `target/smoke-x86.out`, boot attempt 1; `[NMI-BT] rip=ffffffff8043d104` | unowned |
| OPEN | DEFECT | low | `NT_FILE` carries no device or inode number — the format has room only for start, end and page offset per mapping, then the path. A debugger matching a dumped library to an on-disk build therefore relies on the path alone; a file replaced between the crash and the analysis is indistinguishable. This matches the reference format and is not a defect in the builder, recorded so a later lane does not "fix" it by inventing fields. | `files()` in `crates/kernel/fs/src/coredump/elf/notes.rs`; layout asserted in `tests/files.rs`. | — |
| OPEN | DEFECT | low | `../images/build.sh` was uncommitted-broken in the shared checkout: a comment added to the `unshare ... bash -c '...'` block contained an apostrophe, which closed the single-quoted script early — every `./build.sh rootfs ...` died with `line 113: unexpected EOF while looking for matching '`. Reworded in place (not committed; the images checkout carries a large amount of another party's uncommitted work). | B1672, reproduced with `bash -n build.sh`. | — |
| OPEN | DEFECT | low | sysfs is kernfs-backed in Linux and exports the same 8-byte handle, but oxide's sysfs still reports `export_can_decode_fh() = false` (its inodes are synthesized from live kernel objects with no id→object inverse). `name_to_handle_at` on `/sys` is EOPNOTSUPP where Linux succeeds. Not a live blocker — no observed consumer — but it is the same defect class this branch fixed for cgroup2. | `sysfs::SysfsSuperOps::export_can_decode_fh`. | — |
| OPEN | DEFECT | low | **A `mount(2)` LSM option (`context=`, `fscontext=`, `defcontext=`, `rootcontext=`) is refused by any filesystem that publishes a table.** The reference lets the security layer eat those names off the blob before the filesystem table is consulted; nothing installs an `FsContextSecurity` on the `mount(2)` path here, so they fall through to the table and are reported unknown. Matches a reference kernel built with no LSM, and no boot mount passes one — but it will bite the first caller that does. | `crates/kernel/vfs/src/fs/fs_context/flow.rs` `vfs_parse_fs_param` security rung | — |
| OPEN | MISSING | med | **13 of 21 registered filesystems publish no parameter table, so their options are still accepted-and-ignored.** `sysfs`, `debugfs`, `tracefs`, `configfs`, `securityfs`, `efivarfs`, `pstore`, `bpf`, `fusectl`, `mqueue`, `hugetlbfs`, `binfmt_misc`, and `devtmpfs` take `None`; their constructors discard the data string entirely. devpts and cgroup2 are no longer members of this set: each now publishes and consumes its Linux parameter table. Leaving the residual types at `None` avoids declaring an option surface none of them implements; each gains a table with its enforcement. | `crates/kernel/syscalls/src/fsmount_common/registry.rs` `register_filesystems` | — |
| OPEN | MISSING | med | The watch queue holds each note as its own allocation instead of a preallocated note array, so `IOC_WATCH_QUEUE_SET_SIZE` reserves a COUNT rather than memory. The syscall-visible contract is identical (same depth, same rounding to a whole page of notes, same EBUSY on a second call, same loss behaviour when full), but a queue's memory is charged as it fills rather than up front, and the pipe-buffer accounting Linux applies (`account_pipe_buffers`, the unprivileged EPERM when a user is over the soft/hard buffer limits) has no equivalent here. A local unprivileged user can therefore hold more notification memory than Linux would allow. Fix with the pipe-buffer user accounting, which does not exist in this kernel yet for ordinary pipes either. | B1659, `watch_queue/queue.rs::set_size`. | — |
| OPEN | MISSING | med | Notification records are delivered only for KEY events. Linux also posts `WATCH_TYPE_MOUNT_NOTIFY`/`WATCH_TYPE_SB_NOTIFY` from the mount and superblock code in trees that carry those patches, and the generic `watch_queue` device (`/dev/watch_queue`) is absent here too — a notification pipe is reachable only through `pipe2(O_NOTIFICATION_PIPE)`. The mechanism is general (types are a filterable field and the filter code accepts any type below `WATCH_TYPE__NR`); what is missing is other subsystems calling it. | B1659; `WATCH_TYPE_NR` is 2. | — |
| OPEN | MISSING | med | **tmpfs accepts 15 of its 22 declared parameters without acting on them.** `TMPFS_PARAMS` lists what the reference accepts; `mount_opts.rs` honours only `mode`/`uid`/`gid`/`size`/`nr_blocks`/`nr_inodes`. `huge`, `mpol`, `inode32`, `inode64`, `noswap`, `casefold`, `strict_encoding` and the six quota-limit keys are admitted and dropped. Listing them is deliberate — omitting one would make a mount the reference accepts fail — but a `mount -o noswap` still swaps. | `crates/kernel/fs/src/tmpfs/params.rs`, `crates/kernel/fs/src/tmpfs/mount_opts.rs` | — |
| OPEN | MISSING | med | **ext4 accepts its whole 90-name parameter table and acts on the quota family only.** `data=`, `errors=`, `commit=`, `barrier`/`nobarrier`, `discard`, `delalloc`, `noload`/`norecovery`, `block_validity`, `journal_ioprio=`, `max_dir_size_kb=` and the rest are admitted and dropped by `Ext4Mount::open_with_data`. `-o errors=remount-ro` in particular means the filesystem does NOT remount read-only on error. | `crates/kernel/ext4/src/rootfs/params.rs`; `open_with_data` consumes the quota keys only | — |
| OPEN | MISSING | low | `memfd_create(MFD_HUGETLB)` returns ENOSYS because there is no hugetlbfs in this kernel at all — consistent with `mmap(MAP_HUGETLB)` returning EINVAL and with `cachestat`'s unreachable hugepage EOPNOTSUPP leg. This is the answer an unconfigured hugepage build gives, not a stub, but it IS absent Linux surface: a real distro kernel has `CONFIG_HUGETLBFS=y`. Closing it means a hugetlbfs lane, not a memfd change. | F783; `319_memfd_create.rs` hugetlb leg. | — |
| OPEN | MISSING | low | `memfd_wait_for_pins` has no counterpart: `F_ADD_SEALS(F_SEAL_WRITE)` checks `mapping_deny_writable` (writable shared mappings, EBUSY) but not long-term page pins, because nothing in this kernel long-term-pins a page. Truthful today. It becomes a real hole — data written behind a write seal — the moment a GUP-style pin (io_uring registered buffers over shmem, zero-copy RX) can outlive the syscall that took it. | F783; no `pin_user_pages` equivalent exists (grep clean). | — |
| OPEN | MISSING | low | `security_inode_init_security_anon` has no counterpart anywhere in the kernel, so memfd (and every other anon inode) is created without an LSM labelling hook. Not memfd-specific and not observable without an LSM that labels inodes; recorded so the anon-inode creation sites are known when one lands. | F783; grep for `inode_init_security` returns nothing. | — |
| OPEN | MISSING | low | fanotify `FAN_AUDIT` verdicts record the daemon's audit-rule justification (`PermState::set_audit_rule`) but nothing emits it — this kernel has no audit log. `PermState::audit_rule()` is the read side with no consumer. | C265. `crates/kernel/fs/src/inotify/types.rs`; parse + validation covered by `inotify_fan_tests.rs`. | unowned |
| OPEN | MISSING | low | `HelperArgs::key` / `::authkey` exist for the `Upcall` actor contract (an in-kernel key type answering its own `request_key`), but the only production actor is `/sbin/request-key`, which reads them out of the prebuilt `argv`. No in-kernel key type implements `key_type->request_key` yet. | C265. `crates/kernel/fs/src/keyring/construct/upcall.rs`. | unowned |
| OPEN | COVERAGE | med | Zero user-copy-fault and zero namespace coverage in the whole keyring tree. Not one `Errno::Efault` assertion exists; every hosted test calls `ops::*_core` directly with kernel-owned data, so `read_user_iov`'s "validate the whole array before copying any of it" invariant — the one that prevents a half-gathered payload — is asserted only in a doc comment. There is also no user-namespace dimension in the key store at all, while `KEYCTL_CAPABILITIES` advertises `NS_KEYRING_NAME` and `NS_KEY_TAG`. | `crates/kernel/fs/src/keyring.rs` user-copy helpers, reachable hosted only via two `JOIN_SESSION_KEYRING` calls that pass no pointers. Blocks `250` — and this, not the recorded EOPNOTSUPP text, is the real reason. | unowned |
| OPEN | COVERAGE | med | **The `/proc/sys` ctl_table still cannot be tested hosted, and this lane measured exactly why.** Carried from the curated row opening "**The `/proc/sys` ctl_table is target-gated, so no hosted test can assert a leaf exists, binds to the right variable, or carries the right mode.**" Widening `procfs::ctl`'s gate to `any(target_os = "oxide-kernel", test)` leaves 11 unresolved references in FOUR distinct cascades, none of which is a procfs decision: (1) `ipc::live::posix_mq::sysctl` — 10 references; `ipc/src/live/mod.rs` carries its own `#![cfg(target_os = "oxide-kernel")]` and the mqueue sysctl accessors resolve the current task's IPC namespace through `live::posix_mq::user::ipc_ns`; (2) `procfs::sysctl` (`bound_sysctl_inode`, `SysctlInode`) — the writable-slot inode machinery, `#![cfg(target_os = "oxide-kernel")]`; (3) `procfs::random_uuid::{make_uuid_inode, make_boot_id_inode}`; (4) `sched::cred` / `sched::live`, which THIS PR's dev-dependency already resolves. What would settle it: hosted halves for `ipc::live::posix_mq::sysctl` (the per-namespace accessor pair, not the queue machinery) and for procfs's own two inode constructors — i.e. the table's *leaves* need their bindings reachable, not the registration walk. Splitting the table into a data-only module is NOT the answer: every hook leaf carries a function pointer to the live variable, so a data-only copy would be a second source of truth. | Measured on this branch: `cargo test -p procfs --lib --no-run` with the gate widened -> `10 error[E0433]: cannot find 'live' in 'ipc'`, `1 error[E0432]: unresolved import 'crate::sysctl'`, plus the two `random_uuid` constructors on the non-test path. Reverted; the gate is unchanged in this PR. | unclaimed |

## Memory / MM

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | DEFECT | low | A frame sitting in the receive backlog no longer holds an ingress lease, so a namespace teardown racing the drain completes and the queued frame is dropped at delivery instead of being delivered first. This matches the reference (a device that stops running has its backlog discarded) and is a deliberate change from the retained-lease snapshot the inline drain used, recorded so it is not later read as a regression. | B1665; `frames_queued_for_a_down_interface_are_dropped_at_delivery` pins the drop, `net_ns::lifetime_tests` still pins the retained-lease teardown contract. | — |
| OPEN | DEFECT | low | Only `TCR2_EL1.E0POE` (EL0 overlay) is set, not `TCR2_EL1.POE` (EL1 overlay). This matches upstream — the kernel does not overlay its own accesses — but means `POR_EL1` is untouched and kernel accesses are never subject to a key. Recorded because a future supervisor-key feature would need the other bit. | F786; `por::setup_poe`. | — |
| OPEN | DEFECT | low | `process_mrelease` cannot report `EINTR` or `EAGAIN`. Upstream returns `EINTR` when the killable mmap read-lock is interrupted and `EAGAIN` when the reaper cannot take that lock; this kernel's reap walk takes an uninterruptible VMA guard and cannot fail, so neither state exists to report. A caller that retries on `EAGAIN` is not misled — it never sees one — but the two errnos become real once the reap is made lock-failure-tolerant. | F780; `crates/kernel/syscalls/src/448_process_mrelease.rs`. | — |
| OPEN | MISSING | low | Huge-page and directly-addressable-persistent-memory mappings do not exist in this kernel, so the four filter bits covering them can never fire from a live mapping. The ladder implements all four; the adapter reports both properties as absent. | `describe_vma` sets `dax: false` and `hugetlb: false` unconditionally; the ladder rungs are covered by `hugetlb_bits_gate_huge_mappings_by_sharing` and `dax_bits_gate_persistent_memory_mappings_by_sharing`. | — |
| OPEN | MISSING | low | Only RSA is implemented. An ECDSA/EdDSA/ML-DSA certificate parses as far as its algorithm identifier and is then ENOPKG, which is the answer Linux gives for an algorithm it was not built with — but it IS absent surface, not a config answer. The `enc=x962`/`p1363` encodings have no meaning here for the same reason. | B1658, `pkey::oid::pkey_algo` maps only `rsaEncryption`. | — |
| OPEN | COVERAGE | med | `process_madvise` no longer carries its own permission check beyond `mm_access(PTRACE_MODE_READ_FSCREDS)` + `CAP_SYS_NICE`, and `process_mrelease` now has NO permission check at all — the pidfd is the whole authority, matching upstream, where the previous `sig_perm_check` EPERM had no counterpart. This is deliberate Linux parity and is the reason `task_will_free_mem` has to be exact: the "is the target really dying" ladder is the only thing standing between a pidfd holder and another process's pages. Recorded so a future audit does not read the removed check as an accident. | F780; upstream `SYSCALL_DEFINE2(process_mrelease)` has no permission gate. | — |
| OPEN | COVERAGE | med | `cargo test -p kalloc --features debug-heappoison,debug-dealloc-diag,...` aborts with a host **stack overflow** (`fatal runtime error: stack overflow`) before finishing the 19 tests — `poison::Quar` and the size ledger are large enough that a `KAlloc` built on the test thread's stack blows it. Identical on `origin/main`, so pre-existing, not caused by the split — but it means the whole diagnostic-feature configuration of kalloc has **no** hosted test coverage. | B1690. Base worktree at `origin/main` (2ca827e72) and this branch both abort with SIGABRT on the same command; default-feature run is 18/18 green on both. | — |
| OPEN | COVERAGE | med | `pmm` hosted test `swap::tests::final_swap_reference_reclaims_zram_slot` fails under a full `cargo test --workspace` run (`install hosted zram PMM provider: Ebusy`) and passes on `cargo test -p pmm`. Global hosted-provider slot, the same shape as the `net` parallel-execution rows in the curated ledger. | Reproduced twice on B1656, both times only in the workspace run. | — |

## Process / exec / signals

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | DEFECT | low | A mapping whose contents cannot be read is written as zeroes rather than reported. A reader returning 0 for a resident page is indistinguishable in the image from a genuinely zero page. Linux behaves the same way (a skipped range becomes a file hole), so this is faithful, but it means a dump cannot tell an operator that memory was lost. | `push_segment_data` in `crates/kernel/fs/src/coredump/elf/build.rs`; `tests/segments.rs::a_hole_is_zero_filled_rather_than_shortening_the_segment`. | — |
| OPEN | COVERAGE | med | **No ARM boot has been run for the three ported probes.** They build, cross-link and inject on aarch64 (verified), but have not been watched executing there. The x86 DRM smoke is the only in-guest evidence in this PR. | PR #4410 verification table | unowned |
| OPEN | COVERAGE | low | "Each control slot resolves its fd exactly once" is no longer asserted by anything. It used to be a source grep (`source.matches("fd_file(fd)").count() == 1`) — an assertion that could not fail on a behaviour change but did name a real property: a second lookup can land on a different open file description after a concurrent close/reopen recycles the slot. It is now structural (`net_common::classify` is the only caller of `fd_file` on these paths and returns the pinned file), and not reachable hosted: the fd table lookup is `sched::live::current`-gated. What would settle it is a guest probe that closes and reopens an fd from a second thread across a `getpeername`, or an fd-table fault-injection seam. | `net_common::classify` is `#[cfg(target_os = "oxide-kernel")]` because `fd_file` is. | unclaimed |
| OPEN | COVERAGE | low | The "a synthesised event record names the TRACEE" rule is pinned hosted (`a_synthesised_event_record_names_the_tracee_not_the_tracer`), but the CALL SITES that supply the vpid live in `target_os = "oxide-kernel"`-gated files (`stop.rs`, `sig.rs::interrupt`), so a hosted test cannot catch a caller that passes the wrong task. The in-guest probe is the only check on the wiring itself. | `101_ptrace/stop.rs::self_vpid`; `t_ptrace_siginfo` `addr_is_not_a_pid`. | — |

## Drivers / devices

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | COVERAGE | low | Nothing prevents a recurrence: any test that calls `drv_zram::install_page_provider` directly re-creates the same second-provider conflict, and there is no gate that a package's test outcome is invariant to which other packages share the cargo invocation. A workspace-wide `cargo test --workspace` in CI would catch it; per-package runs do not. | This defect survived because the only run that exposes it is the workspace one. | — |
| OPEN | COVERAGE | low | Two hosted test binaries fail intermittently under the suite's parallelism and pass in isolation and on repeat: `fs::…` (one failure in one `make test` run, 902 passing on rerun and 6/6 clean in a loop) and `drv-zram::tests::control_reuses_removed_index` (one failure, 8/8 clean in a loop). Pre-existing, unrelated to this lane — recorded so a later lane does not attribute them to its own change. | `make test` on `B1697`, two consecutive runs, different crate each time. NARROWED: the `drv-zram::tests::control_reuses_removed_index` half is closed by `1b56032d3` (a crate-wide `CONTROL` claim), measured 0 red/20 runs on current main; only the `fs` half is still open and it has no named owner for its shared resource. | — |
| OPEN | COVERAGE | low | The `--test-threads=1` workaround recorded against three of these packages in the curated ledger is now unnecessary and should not be reached for again: every failure here was a missing owner for a shared resource, and all nine were fixable without serializing the whole binary. Recorded so a future lane reading those rows does not adopt the workaround. | Nine independent resources, nine claims, no global thread cap. | — |

## Kernel core

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | DEFECT | med | A listed-but-ignored parameter is still a false claim of support: any key a table lists that the filesystem does not honour keeps the option-support probe lying about that key. Applies to every option of the 17 filesystems that discard their data string. Each needs an implementation or omission from the table — the choice cannot be made silently. | Same registry listing. | — |
| OPEN | MISSING | low | `security_watch_key` / `security_post_notification` have no LSM hook here, so watching is gated by `KEY_NEED_VIEW` alone and a record is never withheld from a watcher whose credentials an LSM would reject. Matches a kernel with no LSM stacked, which is what this one is. | B1659, `ops/watch.rs::watch_key_core`. | — |
| OPEN | MISSING | low | No `/proc/sys/kernel/keys/gc_delay`. Deliberate: `Store::collect()` runs inline with no delay timer, so the knob would gate nothing — and a sysctl that reads back but changes no behaviour is the defect class this project bans. Add the knob only when a deferred gc exists. | B1649, negative result. | — |

## Tooling, gates, docs, dev box

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | INFRA | med | Duplicate lane: C246 independently widened `make feature-gate` from `debug-all` (14 of 87 `debug-*` features) to the full derived list, and fixed the five feature-gated blocks that had rotted behind the hole — `hal::zerotrap`'s missing `PAGE_SIZE_BYTES` import, seven `Task::name` reads left over from when it was a plain array, `smoke::memtest::run`'s pre-IrqGate signature, and an aarch64-unguarded import of x86-only statics in `mm-pmm`. B1671 landed the identical fixes first; C246's commit was dropped in full at rebase. Wasted lane. | Root cause: the claim-work greps in CLAUDE.md were run for the grep-assertion ledger row this lane owns, but NOT re-run when the work grew a second front (the build gate) mid-lane. A lane that changes shape needs a fresh claim check for the new area, not just the one it started with. | — |
| OPEN | INFRA | med | **Driver multidev/rebind smoke coverage is now zero and is recorded as gone, not fixed.** B570–B599 driver-path proofs (sysblock `/sys/dev` reverse index, sysbus bind, virtio net/blk/gpu/snd multidev, rng/uart/ps2/parent-child rebind, shutdown quiesce, vsock round-trip, msix net rx, userspace seat) had a live in-guest assertion until `7aacadf4c`; it has been zero since. Rebuilding it needs a probe-staging path in `../images` (or a `rootfs_disks/` injector per probe), which is out of scope here. The QEMU device topologies those smokes used are KEPT (`image_qemu/{x86_64,aarch64}.rs`, `OXIDE_VIRTIO_*_MULTIDEV_SMOKE` / `OXIDE_STORAGE_MULTICTRL_SMOKE`), so `OXIDE_VIRTIO_NET_MULTIDEV_SMOKE=1 make qemu-x86` still gives the multi-device machine and the driver klog. | deleted: `boot-smoke-{driver-path,msix-net-rx,ps2-rebind,shutdown,storage-multictrl,sysblock,sysbus-bind,uart-rebind,userspace-seat,virtio-blk-multidev,virtio-gpu-multidev,virtio-net-multidev,virtio-parent-child-rebind,virtio-rng-rebind,virtio-snd-multidev,vsock}.sh` | unowned |
| OPEN | INFRA | med | The 218 `t_*.c` conformance programs stay C and are NOT part of the Rust probe rewrite. They are the live N22 differential corpus, and their value comes from being compiled identically for host and guest — rewriting them in Rust would be ~9k LOC of translation for no additional test signal, and would risk silently changing an oracle. Recorded so the "no C in this repo" goal is not assumed complete. | `userspace/glibc_conformance/` 8,988 LOC after `t_crypto` removal | unowned |
| OPEN | INFRA | med | `docs/00-master-plan.md:260`, `docs/43-acceptance.md:76` and `docs/40-ci.md:46` all make a RELEASE GATE out of "Kernel-completeness audit `docs/kernel-audit.md` shows no stub regressions" — and that file was deleted in `3643d52c2` (July 2026). Three release gates cite a document that does not exist, so the gate cannot be evaluated and has silently been vacuous. spec-lint does not check file-path citations inside prose, only `<doc>§<sec>` refs, which is why it never surfaced. | `git log --diff-filter=D -- docs/kernel-audit.md`. Not fixed here: choosing the replacement gate (or dropping it) is a spec decision, not a lint lane's call. | — |
| OPEN | INFRA | med | `af_packet_diff` (2,034) and `wait_diff` (4,640) stay C, deliberately. Both compare a guest run against a REAL Linux run of the same binary, and `tools/wait-diff-selftest.sh` is the only harness in this repo that proves its own probe can fail. Porting one side alone moves the oracle. **Condition for moving them:** the Linux-side oracle is rebuilt in the same language in the same PR, with the selftest re-established first. Same rule already agreed for the 218-program `glibc_conformance` corpus. | `tools/boot-smoke-{af-packet-diff,wait-diff}.sh` diff `$LINUX_RECORDS` against `$OXIDE_RECORDS` | unowned |
| OPEN | INFRA | med | Docs still reference the deleted crates and the abandoned write-our-own-libc plan: `docs/59` (the glibc spec, DRAFT — should go entirely), `docs/00§3` phase-ladder rows 27 (`crates/user/ldso` dynamic linker) and 28 (oxide-libc + NSS + PAM), `docs/03`, `docs/52§123`, and the FROZEN `docs/07`/`29`/`29a`. Deliberately NOT fixed here to keep this PR pure code deletion; all of it lands in the `R` revision lane, which needs revision blocks for the FROZEN three anyway. | `grep -rn 'crates/user' docs/` → 13 hits across 8 files | R lane |
| OPEN | INFRA | med | `FEAT_S1POE` could not be exercised on real silicon or emulation: QEMU 9.2.4 implements it on neither `cortex-a72` nor `max`, so the aarch64 boot proves only that `setup_poe` correctly does nothing and does not fault on a CPU without the feature. The x86 half WAS proven — `-cpu host` on a Ryzen 7960X reports PKU/OSPKE and the boot logs the detection line. The arm enable path stays unproven until a QEMU with S1POE or real ARMv8.9 hardware is available. | F788; `qemu-system-aarch64 --version` 9.2.4, no `detected:` line under either CPU model. | — |
| OPEN | INFRA | med | `docs/30` R01 recorded an io_uring implementation subset (register = silent-0, no shared ring mmap, opcode list) that contradicts the `30` body, which specifies the full Linux surface. Block deleted as stale status, not spec. Whether the code still matches that subset is unverified by this lane. | `30§2` invariants + `30§?` register-buffers text vs the deleted R01 text | unowned |
| OPEN | INFRA | med | `docs/27` R02 recorded a cBPF-only `bpf(2)` subset with "eBPF + verifier + JIT tracked as phase 23"; `27§5` body still says seccomp filter mode returns ENOSYS with BPF "phase 23". Deferral framing survives in the body and conflicts with the no-v2 rule (`02§9` rule 8). Not fixed here (spec-content change, out of scope for a flattening PR). | `27§5` lines "Now: returns ENOSYS for filter mode (BPF tracked as phase 23)" | unowned |
| OPEN | INFRA | med | `git stash` is shared across ALL worktrees of this clone, so two lanes stashing/popping concurrently swap trees: B1654's stash was popped into another lane's worktree and a raw-IP lane's WIP landed in B1654's. Recovered via `git fsck` dangling commits + `git stash store`. Lanes must never use `git stash` on this box; commit temporarily on the lane branch instead. | Observed 2026-08-01: `git stash pop` in the B1654 worktree restored 16 unrelated `raw4`/`sol_ip` files and dropped B1654's tracked edits. Damage still visible in `git stash list`. | — |
| OPEN | INFRA | low | Citation debt: repository text must not name/path-link/quote external implementation sources. 273 files carry `.c:NNN`, 113 mention `include/uapi`, 50 carry `.h:NNN`, 10 name the local reference tree by path. | Pre-existing tree-wide; B1641 added zero. | — |
| OPEN | INFRA | low | `debug-atexit` is excluded from the routine gate: it is mutually exclusive with `debug-stderr` by design (`020_writev` selects the richer `[DYNERR]` tracer when atexit is on), so enabling both hides the `debug-stderr` writev block from the type checker. Covered by the separate non-routine `make feature-gate-atexit`. | Cfg is `all(feature = "debug-stderr", not(feature = "debug-atexit"))`. | — |
| OPEN | INFRA | low | The reaper walks `/proc/[0-9]*/fd/*` rather than using `lsof`, to avoid a tool dependency in the boot gate. On a box with thousands of processes that walk is not free; it runs once per attempt, so the cost is bounded, but a very busy runner may notice. | C249. | — |
| OPEN | INFRA | low | `code/klog-ungated` treats `klog::kfatal!` and `klog::kerror!` as requiring a `debug-<sub>` feature gate, the same as `kdebug!`. A fatal or error log that only exists in a debug build is not a diagnostic anyone will have when it matters, and it makes "log the detail, then abort with a literal" — the natural fix for `code/panic-fmt` — cost a new finding. | `tools/spec-lint/src/code_lint/klog.rs:74-80` lists all five macros in one `MAC_NAMES` table. Worth deciding before the 625-finding `klog-ungated` backlog is worked. | — |
| OPEN | INFRA | low | Build time is NOT the win and should not be reported as one. Cold `cargo clean && cargo build --workspace`: **17.7 s → 15.0 s** wall (1m39 → 1m25 user, 24 jobs). ~15%. The win is 55,868 LOC leaving the tree and an unconfounded conformance channel (C256), not seconds. | measured in this worktree, both runs from an empty `target/` | C258 |
| OPEN | INFRA | low | `crates/shared/kalloc/src/holes.rs` (933) and `src/tests.rs` (651) are over the 500-line split cutoff and were left unsplit by this lane. Both are under the 1000 hard cap. `holes.rs` is the free-list data structure under live corruption-diagnostic work; splitting it is a separate lane's call. | B1690. `wc -l crates/shared/kalloc/src/*.rs`: holes.rs 933, tests.rs 651; every other file ≤ 284. | — |
| OPEN | INFRA | low | `net` lib tests: one intermittent failure observed in a single run during C246, clean on 3 immediate re-runs. Consistent with the already-filed parallel-execution instability in the curated ledger, not a new defect. | 1538/1538 on three consecutive runs after the one-off. Recorded so the observation is not mistaken for a C246 regression. | — |
| OPEN | INFRA | low | `/run/udev/data/n1` (loopback) is a stale zero-byte file dated 1970 shipped in the rootfs image; udev never rewrites it. Cosmetic, but it makes "does lo have a udev record" unanswerable by listing alone. | B1717. `ls -l /run/udev/data/` in guest. | images repo |
| OPEN | INFRA | low | The matrix `IMPL` status CANNOT date a release: 72 of 385 rows are `PARTIAL`/`NEEDS-REWORK` and they are spread across every era (`16:ioctl`, `42:connect`, `46:sendmsg` are 1.x/2.x syscalls). Read literally, "newest release where every syscall introduced at or before it is `IMPL`" yields a bound below 2.6 — because the status tracks AUDIT CLOSURE, not presence. Presence is the property a release number claims; audit closure is tracked separately and must not be conflated with it by the next lane that revisits this number. | `scratch/syscall-compliance-matrix.md` status column: 291 IMPL / 56 PARTIAL / 16 NEEDS-REWORK / 22 LINUX-ENOSYS | — |

## Folded historical lane findings

These rows were imported from the remaining lane-local drops by C289. The
source headings preserve provenance; status, priority, and ownership live only
here now.

### B1719-procfs-per-superblock-root

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | COVERAGE | med | **Nothing in `procfs` can be tested through an actual mount.** `fs_impl`, `static_files` and `live` are all `#[cfg(target_os = "oxide-kernel")]`, so `ProcfsFs::new` / `build_root` / the lookup and readdir enforcement have no hosted reach — a test written against them is a phantom, which is why this lane's mount test was deleted rather than shipped. Every DECISION is ungated and tested (`fs_info`, 16 tests); the WIRING is proven only by the guest probe recorded above. Closing it needs the root-building split so an inode tree can be built hosted, the way `tmpfs` already can be. | `cargo test -p procfs` builds none of `fs_impl`; the deleted `tests/mount_identity.rs` failed to compile with `unresolved import procfs::fs_impl`. | unowned |

### B1720-fsmount-real-vfsmount

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | INFRA | med | **A `make feature-gate` can report GREEN for the wrong tree.** `cd <worktree> && (gate) & (smoke) & wait` parses as `{ cd && gate } &` then `{ smoke } &` — `&` binds looser than `&&` — so the second job runs in the shell's original directory, normally the MAIN tree. Observed here: the gate type-checked `main` and passed while this branch's kernel build was broken by a missing re-export (`E0425`), which only `make x86` caught. Not a defect in the gate itself (a correctly-targeted run catches it: exit=2 with the error) but a foot-gun that the newly-mandated parallel-checks rule makes MORE likely. `CLAUDE.md` now carries the shape and says to confirm the gate log names your worktree. A `make` guard that refuses to run when `$(CURDIR)` is not the git worktree root of the checked source would remove it entirely. | This lane, twice: gate-exit=0 with `Checking vfs (/home/nd/oxide/kernel/...)` in the log while the worktree was `/home/nd/oxide/kernel-B1720`; after the fix, gate-exit=0 with the worktree's own paths. | unowned |

### B1722-cgroup2-mount-options

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | MISSING | med | **Four cgroup2 flags are accepted and stored but cannot yet be acted on, because the mechanism they gate does not exist here.** They are declared because omitting a name fails a mount the reference accepts — a container runtime passing `memory_recursiveprot` would not mount at all — but no code reads them yet: (1) `memory_recursiveprot`: `memory.min`/`memory.low` are stored on the node and read by NOTHING; there is no effective-protection computation of any kind, recursive or not, so there is no behaviour to switch. (2) `pids_localevents`: `pids.events` is the hardcoded literal `"max 0\n"` — no pids-max-breach counter exists to be local or hierarchical. (3) `memory_hugetlb_accounting`: no hugetlb exists in this kernel at all. (4) `favordynmods`: a locking-strategy choice with no user-visible semantics and no dynamic-modification cost model here. Each becomes enforceable when its controller does; `RootFlag`'s own doc comments say which are enforced today. | `crates/kernel/cgroup/src/root_flags.rs` `RootFlag`; `tree/files.rs:44` (`"pids.events" => "max 0\n"`); `mem_low`/`mem_min` have no reader outside their own read/write handlers. | unowned |
| OPEN | COVERAGE | med | **The cgroup2 flag path has no in-guest proof.** The boot mounts `/sys/fs/cgroup` from the kernel with no options and this image's systemd passes none, so a normal boot exercises only the default. A probe that mounts with `-o nsdelegate` and reads `/proc/mounts` was attempted twice: once it hit the `#DF` above, once it passed spuriously because the marker `^[0-9]` matched a kernel timestamp line rather than the command's output — a probe-design error, recorded because the same loose-marker mistake would pass any future probe. Parse, merge, render and both enforcement paths are hosted-tested; what is unproven is only that the registry constructor receives the blob, which is the same wiring the procfs lane proved in-guest for an identically-shaped constructor. | This lane. Correct probe: a marker that cannot appear in kernel output, e.g. the quote-split nonce the harness itself uses. | unowned |

### B1729-cgroup-inode-superblock

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|

### B1730-irq-nesting-unbounded

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | MISSING | med | **No interrupt-nesting depth bound and no runtime stack high-water mark.** Nesting is now structurally bounded at two levels, but nothing measures or enforces it, so a future change that reopens a window fails the same silent way. The reference reports a stack's deepest use (`CONFIG_DEBUG_STACK_USAGE`: zero-fill at allocation — which `kstack::alloc` already does — then scan upward from the low end to the first non-zero word, reported at task exit by `check_stack_usage`). Adding that plus an explicit nesting counter would make a regression measurable instead of fatal. Design worked out this lane: scan in `Drop for Task`, one global low-water atomic, and an on-demand sysrq key for the interrupt stack, which is never freed and so has no exit point. | Reference: `kernel/exit.c` `stack_not_used`/`check_stack_usage`; `kernel/fork.c` `GFP_VMAP_STACK` zero-fill. Our zero-fill is already at `kstack.rs` `alloc`. | unowned |

### B1731-failed-units-boot-health

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | DEFECT | med | **`systemctl` intermittently cannot reach the service manager: `Failed to list units: Transport endpoint is not connected`.** Seen once ~75 s into a live-gnome boot, from a shell that had worked moments earlier; a later run at ~40 s queried cleanly. ENOTCONN on the manager's AF_UNIX socket means the peer went away or the connection was reset, which on a healthy system does not happen. Recorded because it silently turns a verification query into an empty result — an empty `systemctl --failed` was briefly mistaken for "nothing fails". Any probe that reads unit state must check for this line before believing the answer. | `verify2.log`: `Failed to list units: Transport endpoint is not connected`, with the same command succeeding in `verify3.log`. | unowned |
| OPEN | INFRA | med | **Boot probes were used to answer questions the source answers.** Roughly eight boots went into this lane, several of them to establish facts a grep would have given in seconds, and two produced no usable answer at all because the typed probe was mangled by interleaved kernel output or swallowed by `systemctl`'s pager. The repo's own rule says a harness beats a boot and that more than two boots chasing one bug means the loop is the bug. Practical notes for the next lane: pass `SYSTEMD_PAGER=cat` and `--no-pager` or the probe hangs in `less`; the alive-marker must not appear in the typed command (use the quote-split form); and `rc=$?` after a pipeline reports the LAST command's status, not the one being tested — that mistake produced a false `rc=0` for three mounts here. | This lane. | unowned |

### B1732-mount-setattr-detached

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | COVERAGE | med | **A marker placed before a conditional branch reads as if the branch was taken.** A probe line emitted immediately above `if !mounted { return EINVAL; }` printed on every call, so `einval 1` appeared in the log while the check was in fact passing — and the two dentry pointers logged beside it were identical, which is what exposed the contradiction. Cost one build-and-boot round. A branch probe belongs INSIDE the branch. | This lane's `msa6`/`msa7` traces versus `msa8`. | unowned |

### B1735-netdev-iff-up-at-registration

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | INFRA | med | **`tools/boot-smoke-dhcp.sh` cannot pass regardless of DHCP correctness — it greps for a marker from the retired userspace.** It waits for `udhcpc: configured eth0` and the Makefile sets `OXIDE_UDHCPC_ENABLE=1` for a busybox `rcS` gate. That pipeline belongs to the musl/busybox rootfs that the glibc staging replaced; the shipped Fedora image has no `/sbin/udhcpc`, no `/etc/init.d/rcS`, and nothing reads `OXIDE_UDHCPC_ENABLE` anywhere in the tree. So its `FAIL — timeout without lease` is not evidence about DHCP either way. It needs to check for a lease the way the current image would show one (an address on `eth0`, or the manager's own log), or be deleted. | `boot-smoke-dhcp.sh` marker vs `debugfs -R stat` on the shipped image: all three paths "File not found". | unowned |

### B1736-getlink-single-device

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | DEFECT | med | **The network manager still reports `eth0` as "connected (externally)" even though it now has a lease.** The address, routes and reachability are all correct, so this no longer blocks networking, but the manager's device state is still not derived from the link the way it should be — a device it calls externally-connected is one it will not fully own, which will matter for anything that asks it to reconfigure the link. Worth re-checking now that the single-device query returns real data: the earlier verdict was formed while every by-index query returned loopback, so it may simply be a stale assessment that a fresh probe would clear. | `nmcli -t -f DEVICE,STATE d` → `eth0:connected (externally)` in the same boot that shows the working lease above. | unowned |

### B1739-cgroup-exit-side-table

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | INFRA | med | **A `debug-all` boot is impractical to iterate on: ~3000 log lines and t≈28 s of guest time in 10 minutes of wall clock.** The serial port is the bottleneck, and the boot has to reach t≈28 s before the defect above appears. Reproducing it needs either a narrower feature set that still perturbs the timing, or a hosted harness that drives `migrate_hook` directly. Chasing it by repeated `debug-all` boots costs ~10 minutes per observation and should not be the loop. | This lane: two `debug-all` boots, one killed at 10 minutes having reached 85 log lines, one reaching t=28.5 s in ~10 minutes. | unowned |

### B1744-rtnetlink-single-get

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|

### B1745-netlink-strict-dump-check

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|

### B1748-ipv4-addr-record-fields

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|

### B1752-arp-first-packet-drop

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | COVERAGE | low | `ArpCache::insert_at` and `learn` discard the pending jobs `learn_at` returns, so any future caller silently drops every queued packet. Only tests call them today. | `crates/kernel/net/src/arp.rs` | — |

### B1756-proc-net-snmp-real

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|

### C275-cmdline-extra

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | INFRA | low | `/dev/console` resolves to `tty0` because it is the last `console=` on the command line, so a service logging to the console writes to the graphical console rather than the serial log a headless run captures. `SYSTEMD_LOG_TARGET=kmsg` reaches the serial log; `console` does not. | boot on `32420e8f3`: `SYSTEMD_LOG_TARGET=console` produced no service output on serial | — |

### D476-session-handoff

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | INFRA | med | `git add` aborts the entire staging operation on one stale pathspec, so a commit can silently contain none of the intended work while every gate passes in the worktree. That put a broken `main` on origin once this session (reverted in `f1ab81fb6`). A pre-commit check that the staged set matches the working set would catch it. | `git show --stat 8b2211aa6` = 1 file, 0 insertions | — |

### D513-real-hardware-findings

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| OPEN | MISSING | blocker | **x86 boots multiboot2 (BIOS/CSM) only.** No UEFI path. Modern TRX40/WRX80/WRX90 and Z790/X870 boards are UEFI and many ship without CSM, so the kernel cannot be loaded at all. aarch64 already boots EFI-stub. | `tools/xtask/src/image_qemu/x86_64.rs:23-24` | — |
| OPEN | MISSING | blocker | **No USB host controller.** `modules/src/linux_usb/` is a module-registry shim (types/core/gadget, 945 lines) with no xHCI driver; grep for xHCI across `crates/` is empty. Only physical input driver is `drv-ps2-keyboard`, so a board without PS/2 has no keyboard. | `crates/kernel/modules/src/linux_usb/`, `crates/drivers/` listing | — |
| OPEN | MISSING | med | **CPU count capped below Threadripper part counts, no x2APIC.** `MAX_CPUS = 64` with a `u64` online mask; MADT decode reads x2APIC entries but nothing enables x2APIC MSR mode, so APIC IDs above 255 are unaddressable. A 64C/128T part is at or over the mask width. | `crates/kernel/cpu/src/lib.rs:20`, `firmware/src/acpi/tables.rs:123-133` | — |
| OPEN | MISSING | med | **No FADT parse — no ACPI poweroff or reset.** `crates/kernel/power/` carries the `reboot(2)` policy with no hardware path beneath it. Parsed tables are APIC/HPET/MCFG/SPCR only. | `crates/kernel/firmware/src/acpi/`, `crates/kernel/power/src/cad.rs` | — |
| OPEN | MISSING | med | **No AML interpreter, so no `_PRT`.** Legacy INTx routing cannot be resolved on physical hardware; every device must use MSI/MSI-X. Workable (MSI and MSI-X cap programming exist) but it is an unrecorded hard constraint on the driver model, not a preference. | `crates/drivers/pci/src/caps.rs`, absent DSDT/SSDT parse | — |
| OPEN | MISSING | med | **No driver for any physical NIC.** Only `drv-virtio-net`. Target boards carry Intel I225/I226, Realtek RTL8125, Aquantia AQC113 or Intel X550. | `crates/drivers/` listing | — |
| OPEN | INFRA | LOW | Matrix row 44 carried "GAP: AF_UNIX MSG_OOB not implemented", and rows 47/299 carried "true OOB delivery" in their Remaining lists, for a feature that IS implemented. The gap was recorded from a grep against `crates/kernel/net/src/unix/`, a path that does not exist — the subsystem is `crates/kernel/net/src/unix_sock/`. A nonexistent-path grep returns zero hits and reads exactly like a missing feature. Rows 44/46/47 corrected; row 299 still carries the stale phrase and is another lane's row. | `net::unix_sock::tests::oob` 18 tests green on the unmodified tree; `cargo test -p net --features hosted --lib` 2083 passed / 0 failed. `socket::oob::tests` 5 tests cover the send split. | B1938 (rows 44/46/47), unassigned (row 299) |
| OPEN | MISSING | MED | Out-of-band arrival raises priority readiness but delivers no `SIGURG` to the receiving description's `f_owner`. The reference has TWO independent paths: an unconditional signal to `f_owner` that needs neither `O_ASYNC` nor a fasync registration, plus a fasync notification that deliberately suppresses plain `SIGURG` unless `F_SETSIG` chose a queued signal. Only the second exists here, and it correctly suppresses — so the canonical `fcntl(F_SETOWN, getpid())` + `recv(MSG_OOB)` flow signals nothing at all. Same gap on TCP urgent data: no site raises `POLL_PRI` for it. | `crates/kernel/vfs/src/file/async_notify.rs:263` `kill_fasync` returns early on `!is_async()` and again on `dfl == SIGURG && chosen == 0`; `crates/kernel/net/src/unix_sock/stream/send.rs:151` is the only OOB-arrival notification and calls only `wake_peer_subs(POLL_IN\|POLL_PRI)`; `grep -rn "Sigurg\|SIGURG" crates/` shows no delivery site outside the suppressed fasync path. The socket's own description is already reachable — `crates/kernel/net/src/sock/types.rs:133` holds the `Weak<vfs::File>` the reference's `sk->sk_socket->file` provides, set at `crates/kernel/net/src/unix_sock/gc.rs:163`. | unassigned — the canonical owner (`vfs::File` `f_owner` delivery) and the shared trigger (socket layer, common with TCP urgent) are both outside B1938's `net/src/unix_sock/**` boundary; B1938 refused to add a duplicate delivery path in `net` |
| OPEN | DEFECT | LOW | A zero-length `recv(MSG_OOB)` reports 0 where the reference reports 1. Both consume the pending byte; the reference's urgent receive returns a fixed count of one regardless of how many bytes the destination iovec could take, while ours returns the usercopy's own count. | `crates/kernel/syscalls/src/unix_recv.rs:25` returns `copy_payload_at(0, &[byte])`'s count, and `crates/kernel/syscalls/src/recv_user.rs:110` returns 0 for an empty iovec set. Untested here — the decision lives in the target-gated `unix_recv.rs`, so a `#[cfg(test)]` block there would compile away. | unassigned — `crates/kernel/syscalls/src/unix_recv.rs` is outside B1938's boundary |
| OPEN | COVERAGE | LOW | `net::unix_sock::tests::oob` exercises the queue model directly (`UnixPair::write_oob`/`recv_oob`/`at_oob_mark`) but nothing exercises the syscall-level answers those feed: the `MSG_OOB` bit set in the returned `msg_flags`, EINVAL vs EOPNOTSUPP vs the unconnected-stream EINVAL ordering, and `SIOCATMARK` returning EOPNOTSUPP on the datagram/seqpacket/listener/unbound flavours. Those all live in target-gated files (`syscalls/src/unix_recv.rs`, `net/src/sock/io/poll.rs`), so no hosted test can reach them until the decisions move to ungated modules. | `crates/kernel/syscalls/src/unix_recv.rs:117` and `crates/kernel/net/src/sock/io/poll.rs:133` hold the classification; neither has a hosted test. Verified by inspection against the reference: the errno set and ordering match, only the coverage is absent. | unassigned — both files are outside B1938's boundary |
| OPEN | COVERAGE | low | `crates/kernel/net/src/sock_io.rs` and its `tcp_wait`/`tcp_read`/`packet` children remain `#[cfg(target_os = "oxide-kernel")]`, so the blocking receive coordination has no hosted coverage. Only the pure result type moved out (`net::recv_result`). | `net/src/lib.rs` gates `pub mod sock_io`. | — |
| OPEN | DEFECT | low | `raw4::tx_control_tests::ip_nodefrag_keeps_raw_hdrincl_fragments_out_of_local_out_defrag` fails under parallel `cargo test -p net --lib` and passes alone or with `--test-threads=1`: it asserts on process-global `LOCAL_OUT_CALLS`/`LOCAL_OUT_LEN` statics that a concurrent test perturbs. Pre-existing, not introduced here. | `cargo test -p net --lib` 2092/1; `-- --test-threads=1` 2093/0; the single test alone passes. | — |
| OPEN | DEFECT | low | `syscalls::return_fastpath_tests::syscall_process_irqs_close_before_return_work` fails on `main` (HEAD `fad889a65`): its `include_str!` assertion on the dispatch source no longer matches the text. Pre-existing, unrelated to this lane. | Reproduced in the main tree with an empty working set. | — |
| OPEN | MISSING | med | `IP_TRANSPARENT` is consumed only as a bind permission (non-local bind). Foreign-address delivery and reply-source selection are absent. Not a stored-but-unconsumed bug — the option does change behaviour — but the Linux surface is partial. | `net::sock::nonlocal` is the sole consumer. | — |
| OPEN | MISSING | low | A v4-mapped UDP receive on an `AF_INET6` socket publishes the IPv4 ancillary level for the checksum but still carries no `tos`/`options` of its own (`Udp6Datagram.traffic_class` is filled from the IPv4 TOS byte), so `IP_RECVTOS`/`IP_RECVOPTS` on a v4-mapped socket differ from the native IPv4 path. Pre-existing. | `stack/ipv4.rs` v4-mapped enqueue fills `traffic_class` and leaves the option area empty. | — |
| OPEN | DEFECT | high | The same class remains in `syscall::scan_user_cstr`, which range-checks each address and leaves the byte read to its caller with no mapping check — every other caller of it that reads raw has the same kernel-fault-on-unmapped-user-pointer defect. The keyring works around it locally by validating per page in its own closure; the fix belongs in the shared helper, which is outside this lane's file ownership. | `crates/kernel/syscall/src/userptr.rs:108`. Same reproduction as the row above, aimed at any `strndup_user`-style argument. | unassigned |
| OPEN | DEFECT | med | A kernel-mode page fault on an unmapped USER address does not stop at a clean oops: it hit the task stack guard page with a repeated frame (`deepest repeated site … x13`), so the fault path recurses on this input. Worth fixing independently of the callers that stop provoking it. | Boot log: `BUG: TASK stack guard page was hit … headroom=13712` immediately after the `#PF` on `cr2=00007ffedead0000`. | unassigned |
| OPEN | DEFECT | med | `look_up_user_keyrings` identifies the per-uid keyrings by `cred->user->uid` — the REAL user — while key OWNERSHIP is `cred->fsuid`. This store uses `fsuid` for both, so a task under `setfsuid()` is handed a different user keyring than the reference would give it. The namespace dimension added here is keyed `(user_ns, fsuid)` and inherits the same divergence. | Read from the reference credential paths; no test yet. Fixing it means carrying the real uid in `TaskIds` alongside `fsuid` and keying `user`/`usersess` on it. | unassigned |
| OPEN | DEFECT | low | `cur_ctx` fills `TaskIds::tgid` from `vtgid` (the PID-namespace-relative number) while `tid` is the global one, so the process keyring is keyed on a number two tasks in different PID namespaces can share. `exec_keys` passes `vtgid` too, so the two agree — the defect is the key space, not a mismatch. | `crates/kernel/fs/src/keyring.rs` `cur_ctx`; `crates/kernel/syscalls/src/exec_transition.rs:181`. No test. | unassigned |
| OPEN | COVERAGE | low | `dns_resolver` is registered so the domain tag has a production consumer, but nothing in the kernel REQUESTS a `dns_resolver` key yet — the type is reachable only through `add_key`/`request_key` from userspace. | `crates/kernel/fs/src/keyring/types.rs`. The tag itself is exercised by `keyring::tests::namespace`. | unassigned |
| OPEN | MISSING | Med | The named namespace `fsmount(FSMOUNT_NAMESPACE)` builds has the new mount as its ROOT. The reference instead copies the caller's namespace root and mounts the new tree ON TOP of that copy. `/` resolves to the new filesystem either way, but the reference's namespace also carries the underlying root copy, and a caller that unmounts the top has something beneath it. | `vfs::mount::create_ns_mount`; `vfs/tests/mount_fsmount_namespace.rs::the_namespace_form_puts_the_mount_in_a_namespace_a_task_could_enter` pins the current shape. | unowned |
| OPEN | MISSING | Med | The reference clones the new namespace's tree with slave propagation when the caller's user namespace differs from its mount namespace's owner. We apply the locked-attribute stamp for that case but not the propagation transition. | `432_fsmount.rs` calls `lock_new_mount_bits` only; no `Propagation::Slave` transition on the created mount. | unowned |
| OPEN | MISSING | Med | `open_tree(2)` has no `OPEN_TREE_NAMESPACE`. It is the sibling of `FSMOUNT_NAMESPACE` closed here and shares the same namespace constructor, which now exists (`vfs::mount::create_ns_mount`). Row 428, outside this lane. | `syscalls/src/open_tree_policy.rs` has no such flag; `428_open_tree.rs` has no namespace arm. | unowned |
| OPEN | COVERAGE | High | `syscalls/tests/mount_{too_revealing_dispatch,param_admission_hosted,cgroup_v1_honest_hosted}.rs` reach `mount_dispatch` by `#[path]`-including the source file, which compiles a SECOND COPY into the test binary. The shipped module is inside a target-gated tree and is never built by `cargo test`, so these three can pass while the module the kernel links is broken — the exact "verification that cannot fail" class. | The gate is `#![cfg(target_os = "oxide-kernel")]` on `fsmount_common/mod.rs`; the tests name `../src/fsmount_common/mount_dispatch.rs`. The `mount_capable` extraction here removes one rung from that copy; the rest of `dispatch_mount` remains. | unowned |
| OPEN | COVERAGE | Low | `FsContext::log_messages()` and `take_log()` have no callers outside tests. The live readers are `fetch_message`/`read_message`; these two are assertion helpers that read like production API. | `grep -rn "log_messages\|take_log" crates/` — every hit is in `vfs/tests/fs_context_log.rs`. | unowned |
| OPEN | DEFECT | Med | `syscalls` lib test `return_fastpath_tests::syscall_process_irqs_close_before_return_work` fails on `main` (`5506b785e`), unrelated to this lane. Confirmed by running it in the main tree, not only in this worktree. | `cargo test -p syscalls --lib return_fastpath` in `/home/nd/oxide/kernel` -> `test result: FAILED. 5 passed; 1 failed`. | unowned |
| OPEN | DEFECT | med | **Second owner of the compat decision, outside this lane's files.** `crates/kernel/socket/src/batch.rs:39` rejects `MSG_CMSG_COMPAT` inside `send_batch`, and re-declares the flag at line 8 (`net::uapi::MSG_CMSG_COMPAT` already exists). The syscall entry now owns the decision; that guard would refuse a legitimate compat `sendmmsg` the moment a 32-bit entry exists, and the duplicate constant is a second source of truth for the bit. It must be deleted (with `socket/src/tests.rs::compat_batch_flag_is_rejected_before_import`, whose subject moved to `msg_layout::tests::entry`). NOT touched here: `crates/kernel/socket/**` is lane B1943's. | `socket/src/batch.rs:8,39`; the syscall-side rule and its tests are in `syscalls::msg_layout::entry`. | B1943 / integration |
| OPEN | MISSING | med | Nothing sets `EntryAbi::Compat`: there is no 32-bit syscall entry on either arch (`sched::personality::domains::SUPPORTS_32BIT_COMPAT == false`, no ia32/AArch32 dispatch table), so the compat layout is reachable only from tests today. The ABI is implemented and provable; the entry table that would select it is absent. | `crates/kernel/sched/src/personality/domains.rs:46`; `hal-aarch64/src/vbar/asm.rs:52` ("AArch32 vectors — unused"). | unassigned |
| OPEN | DEFECT | low | Pre-existing hosted test failure, unrelated to this lane: `syscalls::return_fastpath_tests::syscall_process_irqs_close_before_return_work` greps `dispatch/core.rs` for `exit_to_user_mode_loop(regs, Some(rv))`, which now takes a third argument. Both files are byte-identical to `origin/main`, so the failure is on main. | `cargo test -p syscalls --lib` → 1268 passed, this 1 failed, on a tree whose `dispatch/core.rs` and `return_fastpath_tests.rs` show no diff against `origin/main`. | unassigned |
| OPEN | DEFECT | low | Pre-existing hosted test failure, unrelated to this lane: `net::stack_tests::forwarding::ipv4_ingress_mib_names_unforwardable_and_unknown_packets`. This branch touches no file under `crates/kernel/net/`. | `cargo test -p net --features hosted --lib` → 2082 passed, this 1 failed. | unassigned |
| OPEN | COVERAGE | low | Compat pointer FIELDS (`msg_name`, `msg_iov`, `msg_control`, and the 32-bit iovec array) are decoded and offset-tested, but no test follows one to real memory: a hosted test cannot place a buffer at an address that fits in 32 bits. The conversion and the offsets are covered byte-for-byte instead; end-to-end coverage needs the 32-bit entry above plus a guest. | `recv_user::tests::a_compat_iovlen_is_read_from_its_own_offset` and siblings cover the offsets; `msg_layout::tests::cmsg` covers the bytes. | unassigned |
| OPEN | DEFECT | LOW | `net::raw4::tx_control_tests::ip_nodefrag_keeps_raw_hdrincl_fragments_out_of_local_out_defrag` is order-dependent: it installs a process-global netfilter hook and asserts on process-global counters (`LOCAL_OUT_CALLS`), so a concurrently running test's traffic is counted into it. Fails intermittently at the default thread count and reliably at higher ones. Not caused by B1943 — reproduced on clean `origin/main` (4c6c048ee). | On unmodified `origin/main`: `cargo test -p net --features hosted --lib -- --test-threads=16` and `=24` both FAIL with `left: 2, right: 0` at `raw4/tx_control_tests.rs:233`; `=4/6/12` and `--test-threads=1` pass. Passes in isolation. | unassigned |
| OPEN | DEFECT | LOW | A readiness wake classifies its mask to exactly ONE `POLL_*` reason, so a site that wakes with `POLL_IN\|POLL_PRI` (the AF_UNIX urgent send) delivers only the `SIGURG` fasync notification and never the `SIGIO` one for the in-band data in the same send. The reference raises the two as separate notifications. Affects every combined-mask wake site, not just out-of-band — `POLL_HUP\|POLL_OUT` collapses the same way — so it is a property of the mask→reason funnel, not of this send. | `vfs::poll_subs::fasync_notify` takes a single `reason_for_mask(events)`; `vfs::tests::file_fowner::a_readiness_mask_classifies_to_one_reason_by_linux_precedence` pins the current collapsing behaviour. | unassigned |
| OPEN | DEFECT | LOW | `syscalls::return_fastpath_tests::syscall_process_irqs_close_before_return_work` fails on an unmodified tree — `panicked at return_fastpath_tests.rs:49: return work`. Pre-existing and unrelated to B1943; recorded because it was confirmed, not theorised. | `cargo test -p syscalls --lib` on this worktree BEFORE any edit: 1235 passed / 1 failed, same test; identical result after. | unassigned |
| OPEN | COVERAGE | LOW | The TCP urgent-arrival CALL SITE in the segment-delivery path has no hosted test: its three pieces are each covered (the arrival gate against a real `TcpConn`, the entry→description link, the signal itself), but nothing hosted drives a URG segment through `deliver_tcp` with a bound description and observes the signal. The AF_UNIX side IS covered end to end. | `net::stack::tcp::urgent_tests` covers `urgent_arrived`, `register_file`/`owner_file`, `set_file` propagation, and `sk_send_sigurg` separately; `crates/kernel/net/src/stack/tcp.rs` composes them untested. | unassigned |
| OPEN | DEFECT | high | The same class remains at roughly 100 more call sites that dereference a user address raw after a range-only check (`validate_user_buf`, a bare inline `< USER_VA_END`, or nothing). Highest-value clusters, by owner: `016_ioctl/{font,tioclinux,vt,tty_ioctl}`; `101_ptrace/{info,mem,regset,sig}` write halves; `179_quotactl` and `202_futex`, both of which hand-rolled their own range check instead of calling the shared one; the timer/signal readers (`013_rt_sigaction` act, `038_setitimer`, `131_sigaltstack`, `035_nanosleep`, `128_rt_sigtimedwait`, `signal_common::read_user_siginfo`); the futex family in `ipc/live/futex/**`; `sched/src/{prctl_set_mm, cred/caps, xfer, prctl/name}`; `fs/src/{fileattr, epoll, signalfd, xattr, watch_queue, userfaultfd}`; `ipc/live/posix_mq/**`; `security/src/seccomp/user.rs`; `syscall/src/numa.rs`. Every one is a local denial of service: map a page, `MADV_DONTNEED` it, race the syscall. The fix for each is mechanical — copy through `uaccess::{copy_from_user, copy_to_user}` — and needs no new machinery now that the string class is done. The runaway guard added here bounds the damage to a halt with a diagnosis rather than a silent stack-guard smash; it does not make these correct. | Inventory produced by sweeping `read_volatile`/`read_unaligned`/`write_volatile`/`copy_nonoverlapping` in `crates/kernel/**` and classifying each pointer's origin. Reproduction is the same for all of them: pass `0x7ffe_dead_0000`. | unassigned |
| OPEN | DEFECT | med | `mm-pmm`'s fault core runs the migration check, the swap check and the userfaultfd check — each taking a lock or walking the page table — BEFORE it ever asks whether a VMA covers the address; the miss is only detected two calls deeper in `handle_not_present`. The reference does one `find_vma` and goes straight to the oops for a kernel-mode access with no fixup. Not the runaway's proven cause (that was not pinned to a specific pre-gate call), but it is the shape that makes a bad-address fault expensive and hard to reason about. | `crates/kernel/mm-pmm/src/user_as/fault.rs` `do_handle`; `crates/kernel/mm-vmm/src/address_space/fault/fill.rs` `handle_not_present`. | unassigned |
| OPEN | COVERAGE | med | The runaway guard's terminal state is a halt with a printed diagnosis. The reference kills the offending TASK and keeps the kernel running for a kernel-mode fault it cannot fix up. Halting is fail-safe and strictly better than the stack smash it replaces, but it is not the reference's answer, and nothing here can yet turn an unresolvable kernel-mode fault into a dead task. | `hal-{x86_64,aarch64}/src/fault.rs`: the dispatcher returns `false` and the asm halts. | unassigned |
| OPEN | COVERAGE | med | The unmapped-pointer EFAULT assertion is written but has never RUN on a booted guest. `userspace/probes/request_key` gained an `unmapped_string_arguments` case covering `openat`, `newfstatat`, `execve`'s path, `execve`'s argv and `memfd_create` against `0x7ffe_dead_0000`; it builds for both arches and the injection is wired behind `OXIDE_REQUEST_KEY_SMOKE`, but the injected unit did not report within this lane's boot budget — `boot-smoke` declares the guest alive at the shell prompt, before `basic.target.wants` runs it, and a longer manual boot stalled at the prompt without reaching the unit. What IS boot-proven: both arches reach the alive marker on attempt 1 with every exec on the system running through the new readers, and the runaway guard is proven LIVE by having fired twice on real faults. What is not: that an unmapped pointer answers EFAULT rather than something else. | `userspace/probes/request_key/src/main.rs::unmapped_string_arguments`; `make smoke` PASS both arches. | unassigned |
| OPEN | COVERAGE | low | The signal-frame paths (`hal-{x86_64,aarch64}/src/signal.rs`, `sched/src/sigaltstack.rs`, `fs/src/sig_dispatch.rs`) were not audited for this class. They write a frame to the USER stack from kernel mode, which is exactly a fault-prone user access. | Not examined; flagged so the next sweep does not assume they were. | unassigned |
| OPEN | INFRA | low | `net::raw4::tx_control_tests::ip_nodefrag_keeps_raw_hdrincl_fragments_out_of_local_out_defrag` fails intermittently at high `--test-threads`. Confirmed on clean main by the integration owner, reproduced once here; not caused by this branch. | Full workspace run: 2092 passed, this one failed; passes when re-run. | unassigned |
| OPEN | INFRA | low | `crates/kernel/net/src/recv_result.rs` carries an indented block inside a `///` comment, which rustdoc compiles as a doctest — `cargo test -p net --doc` fails on clean main. The same block also reproduces external implementation source verbatim in repository text, which the semantic-verification rule forbids. Arrived with a merge; not this branch. | `recv_result::recv_empty (line 88)` FAILED on `origin/main` with this branch's files absent from the crate. | unassigned |
| OPEN | MISSING | MED | The raw IPv4/IPv6 transmit paths cannot screen their bound source: `Raw4Endpoint`/`Raw6Endpoint` carry only detached per-option atomics (`bpf_filter`, `mcast`, `error`, `ip_mtu_discover`), never an `Arc<IpOpts>` handle, so the transparent bit is unreachable from `raw4::tx`. The header-including case is covered — `state.hdrincl` is the other half of the same permission — but a non-hdrincl raw socket bound nonlocally is unscreened. Fix is to retain the live `Arc<IpOpts>` on the endpoint the way `TcpEntry`/`TcpListenEntry` already do; mirroring the bit onto the endpoint would be a split source of truth and must not be done. | `crates/kernel/net/src/raw4/types.rs:78-92`, `crates/kernel/net/src/raw6/types.rs:72-86` (no `ip_opts` field); the screen's socket entry point is `net::transparent::screen_v4_socket_source`, already shaped to take `hdrincl` | unclaimed |
| OPEN | MISSING | MED | UDP receive queues carry no live option handle either (`UdpRxQueue`/`Udp6RxQueue` hold only threaded-through atomics), so any future ingress-side decision that needs a socket option has nowhere to read it. Not reached by this lane — the transparent permission's ingress half is a routing decision, not a lookup one, so the demux needs no socket flag — but recorded because the shape is the same one that blocks the raw row above. | `crates/kernel/net/src/stack/types/udp.rs:63-92`; demux address match is `stack/core.rs:333` `if !q.bound_ip.is_unspecified() && q.bound_ip != dst { continue; }`, which matches a foreign bound address correctly with no flag needed | unclaimed |
| OPEN | DEFECT | LOW | `bind_screen::classify_v4`'s local-route fallback tests only `route.table == RT_TABLE_LOCAL`, not the route's kind. The reference's equivalent requires the local-table hit to be of local type, so a broadcast or anycast route parked in the local table classifies as `Local` here and does not there. Reached today only when no interface-address row covers the address, and the interface broadcast rows are consulted first, so no live path is known to hit it. | `crates/kernel/net/src/bind_screen.rs:88-91` vs the kind-aware `record.kind == crate::route::RTN_LOCAL` test the IPv4 local-input decision already makes at `stack_forward.rs:70` | unclaimed |
| OPEN | MISSING | MED | TCP6 active open never resolves a wildcard source: `connect_tcp6_locked` leaves `local_ip` as `Ipv6Addr::ANY` for a non-loopback destination and that value reaches `push_ipv6_header` verbatim, so the SYN leaves with an unspecified source. Every other v6 transmit path calls `v6_select_source`. Found while mapping source selection for this lane; out of its scope (it is the wildcard case, not the foreign-address one). | `crates/kernel/net/src/sock/tcp_lifecycle.rs:236-249`; contrast `stack_ipv6/udp.rs:441-454` and `stack_ipv6/tx.rs:259` | unclaimed |
| OPEN | INFRA | LOW | Two net tests flake at `--test-threads=24` (~1 run in 6): `net::raw4::tx_control_tests::ip_nodefrag_keeps_raw_hdrincl_fragments_out_of_local_out_defrag` and `net::stack_tests::forwarding::ipv4_ingress_mib_names_unforwardable_and_unknown_packets`. The net fixture mutex serialises only tests that take its guard; tests touching the global stack without it run concurrently and perturb the globally-installed nf hook and the global MIB counters. Confirmed pre-existing on clean main, not introduced here. | reported by the lane owner on clean main; this lane ran `--test-threads=1`, 2109 passed / 0 failed | unclaimed |
| OPEN | MISSING | Low | `create_new_namespace` refuses with EINVAL when the caller's mount namespace has no root mount. The reference cannot reach that state (its namespace root is a synthetic always-present mount); ours can, before the root filesystem is mounted. EINVAL is honest but is not a reference behaviour, because the reference has no such case. | `vfs/src/mount/new_namespace.rs`: `mntns::ns_root_id(caller.id()).and_then(mount_by_id).ok_or(VfsError::Einval)?`. No syscall can reach it — `fsmount`/`open_tree` both require a resolved path or a realized superblock, which implies a mounted root. | unowned |
| OPEN | COVERAGE | Low | `FsContext::log_messages()` and `take_log()` still have no callers outside tests (B1941 row 12, untouched by this lane). | `grep -rn "log_messages\|take_log" crates/` — every hit is in `vfs/tests/fs_context_log.rs`. | unowned |
| OPEN | MISSING | high | No async execution engine: SQPOLL/IOPOLL rings, `IORING_FEAT_FAST_POLL` poll-armed retry, multishot re-arm, `IORING_OP_TIMEOUT`/`LINK_TIMEOUT`/`ASYNC_CANCEL`, and cancellation of in-flight work are all absent. Every entry completes inside the submission that issued it. Needs kernel worker threads sharing the submitter's mm/fd table plus a request object model with refcounted lifetime — a subsystem-sized piece of work outside the three slot files. `IORING_REGISTER_IOWQ_AFF`/`IOWQ_MAX_WORKERS` wait on the same thing. | `op_supported` excludes those opcodes; `probe_never_claims_an_opcode_dispatch_would_reject` pins that they are not advertised | — |
| OPEN | MISSING | med | Ring regions are ONE frame each, so `MAX_ENTRIES` is 64 (Linux: 32768) and `IORING_SETUP_SQE128`, `CQE32`, `CQE_MIXED`, `SQE_MIXED`, `NO_MMAP`, `IORING_REGISTER_RESIZE_RINGS` and `MEM_REGION` cannot be served. Needs a multi-frame `VmaBacking` in mm-vmm (`map_kernel_frame` maps every page of a VMA to one frame). | `layout::REGION_BYTES`; `no_region_overlaps_and_every_geometry_fits` | — |
| OPEN | MISSING | med | No per-task io_uring state, so `IORING_REGISTER_RING_FDS`, `IORING_ENTER_REGISTERED_RING`, `IORING_REGISTER_USE_REGISTERED_RING` and `IORING_FEAT_REG_REG_RING` cannot work. Needs a registered-ring array on `sched::Task` with teardown at exit. | `registered_ring_lookup_reports_linux_errnos` pins the empty-array errnos | — |
| OPEN | MISSING | low | `IORING_REGISTER_NAPI` (needs net busy-poll), `ZCRX_IFQ`/`ZCRX_CTRL` (needs a NIC zero-copy receive queue), `BPF_FILTER` (needs a program loader) report EOPNOTSUPP | `opcodes_needing_an_absent_mechanism_report_eopnotsupp_not_success` | — |
| OPEN | COVERAGE | med | The submission engine, the completion path and the wait loop live under a `#![cfg(target_os = "oxide-kernel")]` module, so only their extracted decision modules (`abi/link`, `abi/enter`, `abi/restriction`, `io_uring_sqe`) are hosted-testable. The remaining glue — overflow flushing, provided-buffer selection, direct-descriptor placement — has no check that can fail. A ring fixture usable from `cargo test` would close it. | boot smoke is the only current check on those paths | — |
| OPEN | INFRA | low | `IORING_REGISTER_SYNC_CANCEL` reports ENOENT for every request because nothing is ever in flight. Correct for a synchronous engine, but it becomes a real cancellation path the moment the async engine above exists. | `io_uring::register::rings::sync_cancel` | — |
| OPEN | MISSING | med | A minor fault is detected through the backing's non-allocating residency lookup, which reports a SWAPPED-OUT or in-migration backing page as absent. Such a fault is then reported as MISSING (when that mode is also registered) or resolved normally, where the reference swaps the page back in and reports a minor fault. Affects only ranges whose backing pages have been reclaimed. | `mm-pmm/src/user_as/uffd.rs::backing_resident` → `FileBacking::fault_around_frame`, which skips `Swapped`/`Migrating` shmem indices by contract | — |
| OPEN | MISSING | med | The event features (`EVENT_FORK`, `EVENT_REMAP`, `EVENT_REMOVE`, `EVENT_UNMAP`) generate no events and are refused at the handshake rather than accepted-and-ignored. A non-cooperative monitor therefore cannot track address-space changes. | `policy::api_negotiate` refuses them; `tests::policy::api_offers_exactly_the_features_that_are_wired` pins the refusal | — |
| OPEN | MISSING | low | `WP_UNPOPULATED`, `WP_ASYNC` and `WP_HUGETLBFS_SHMEM` are absent, so write-protect is anonymous-only and a `MODE_WP` registration over shared storage is refused with EINVAL. This is the same shape a reference kernel without non-present page-table markers takes. Closing it needs a marker encoding for an ABSENT leaf that survives eviction. | `policy::register::vma_can_userfault`; `tests::modes::modes_are_refused_on_vmas_that_cannot_deliver_them` | — |
| OPEN | MISSING | low | There is no `mmap_changing` counter, so no op returns EAGAIN for a concurrent non-cooperative address-space change; the VMA tree locks serialise instead. A monitor that relies on the EAGAIN to detect a racing `mremap` will not see it. | `ioctl/*` have no equivalent of the counter check that precedes every reference handler | — |
| OPEN | MISSING | low | `UFFDIO_MOVE` moves 4 KiB leaves and swap entries only. A page in migration reports EAGAIN and a poisoned one EFAULT (both matching the reference), but there is no large-folio path because none exists in this kernel. | `work/movepg.rs::classify` | — |
| OPEN | COVERAGE | med | The page work itself (`work/{install,wp,poison,movepg}.rs`) is target-gated and has no hosted coverage; the hosted build substitutes a stand-in that reports the range complete. Every DECISION around it is ungated and tested, and both arches boot, but a defect confined to the leaf manipulation would not be caught hosted. Closing it needs a hosted page-table fixture of the kind `vmm`'s copy-on-write model tests use. | `work/hosted.rs`; `make smoke` passes both arches | — |
| OPEN | DEFECT | low | The UDP autobind right is checked inside the bind owner (`InetSocket::ensure_bound_locked`), i.e. during the protocol send, whereas the reference checks it in the send hook before the protocol runs. Same verdict, later position: an argument error from the address parse can preempt it. Not duplicated into the hook, since a second copy is exactly the split this branch removed. | `net::sock::lifecycle::ensure_bound_locked` calls `landlock_addr::check_autobind_udp`; `sock::udp` reaches it on every unbound UDP send. | unowned |
| OPEN | DEFECT | low | Only the IPv4 UDP send path reaches `ensure_bound`; an IPv6 UDP send that autobinds was not confirmed to ask for the bind right. | `grep ensure_bound` finds one send-side caller, `net::sock::udp`. | unowned |
| OPEN | INFRA | low | `cargo test -p security` failed once in ~4 runs with 244/1 and no reported failure name (the summary line and the failure list disagreed); green on every re-run. Untouched by this branch — the crate's registry statics are process-global and its tests install into them concurrently. | Observed 2026-08-07 on this box; `-p security` green 3/3 after. | unowned |
| OPEN | INFRA | low | Restoring a positive-control file with `mv file.bak file` preserves the ORIGINAL mtime, so cargo does not rebuild and the next run silently re-uses the CONTROL binary. This produced a deterministic "restored code still fails" reading for three runs. `touch` after any such restore. | `net::socket_security::tests::the_sandbox_answers_before_the_module_registry` reported red against pristine source until the file was touched. | unowned |
| OPEN | COVERAGE | high | `crates/kernel/socket` tests flake ~20 %: `security_hooks::fixture()` serialises only the tests that ASK for its private `LOCK`, but the policy it installs lands in the process-global `security::network::HOOKS` for the ONE hosted network namespace. Tests elsewhere in the crate (`tests::batch_imports_and_publishes_one_message_at_a_time`, `tests::netlink_prepares_oob_length_control_and_address_in_linux_order`, `tests::oversized_socket_batch_is_limited_to_linux_uio_maxiov`) drive sends through that same namespace without the guard, so they see a concurrent test's `deny` hook (`Err(Eacces)`) and bump its counters (`Some((0, 2))` vs `Some((0, 1))`). Identical mechanism to the net-fixture row in `B1949`: the lock protects the fixture, not the state the fixture installs into. | `cargo test -p socket`, 40 runs: **8/40 FAILED on the unmodified base**, 6/40 with this branch's `sched` change — pre-existing and unrelated to it. Green at `--test-threads=1`. | unassigned (outside B1956's file ownership) |
| OPEN | COVERAGE | low | `packet_tests.rs::packet_tx_ring_file()` registers an interface into the process-global interface table for namespace 0 and brings it up, with no claim on that table. `net`'s own `assert_initial_domain_held` choke point (`B1949`) is `#[cfg(test)]` **within `net`**, so it compiles out of `socket`'s test binary and cannot see this. The two packet tests each get their own ifindex, so they do not collide today, and the claim added here does not cover the interface table. | Not observed to fail: 0 occurrences across the 40 baseline and 140 post-fix `socket` runs. Structural, not measured — a future socket test that asserts on namespace-0 interface or MIB state would race these two. | unassigned |
| OPEN | MISSING | MED | Transmit timestamping generates nothing. The record, its selector and key words, and `publish_timestamping` exist and are tested, but no send path records a transmit timestamp: there is no per-send key allocation, no `SOF_TIMESTAMPING_TX_*` handling, and no `scm_timestamping` ancillary encoder. A socket that enables it will never see a record. | Grep: `SOF_TIMESTAMPING_TX_` has no definition in the tree; `publish_timestamping` has no non-test caller. | unclaimed |
| OPEN | MISSING | MED | `SO_TXTIME` generates nothing. The option's clock, deadline mode and error-report flag are stored, and `publish_txtime` plus both codes exist and are tested, but there is no transmit-time scheduler to miss a deadline or reject a parameter, and `SCM_TXTIME` is not parsed on send. | Grep: `SCM_TXTIME` has no definition in the tree; `publish_txtime` has no non-test caller. | unclaimed |
| OPEN | MISSING | LOW | Local errors are generated only from the UDP transmit paths. Raw IPv4/IPv6 sends reject oversized datagrams with the same size failure and queue no record. | `net/src/raw4/tx.rs` and `net/src/stack_ipv6/raw.rs` return the size failure with no `report_send_failure` call. | unclaimed |
| OPEN | MISSING | LOW | The error queue's receive-memory budget is a fixed default; `SO_RCVBUF` does not resize it. `SocketError::set_rmem_limit` exists and is tested but has no caller outside tests — the option state lives in `sock_opts`, owned by another lane. | `set_rmem_limit` has no non-test caller. | unclaimed |
| OPEN | COVERAGE | LOW | The `MSG_ERRQUEUE` receive path itself (`syscalls::recvmsg::{inet,vsock}`) is target-gated, so no hosted test covers the copy of payload/name/control into user memory. Every decision it makes was moved into `net::socket_error::abi`, which is tested; the byte moves are not. | `crates/kernel/syscalls/src/recvmsg/inet.rs` inherits the slot-file gate. | unclaimed |
| OPEN | INFRA | LOW | The matrix claim that the extended-error record ABI "does not exist" was true only of the *name*: the 16-byte record plus offender sockaddr was already encoded, open-coded, inside the target-gated `recvmsg` slot file, which is why no grep for the structure name found it. Grepping for a UAPI name is not evidence of absence when the encoder is hand-rolled. | Pre-change `syscalls/src/recvmsg/inet.rs::extended_error_data`. | B1959 |
| OPEN | MISSING | med | The per-message `SO_MARK`, `SO_PRIORITY`, `SCM_TXTIME` and `SO_TIMESTAMPING` values are admitted exactly as the reference admits them (capability, band and width), but nothing consumes them on transmit. The socket-level `SO_MARK` is in the same state: it is stored by the option table and never reaches the route lookup, which does take a mark (`route::lookup_result_mark_in` has no caller outside forwarding). One owner has to consume both, and that owner is the transmit path's mark plumbing, not the ancillary rule | `net::sock_opts::sol_socket::set` stores `Action::Mark`; `grep` for a reader on the socket TX path finds none | sock_opts / transmit lane |
| OPEN | DEFECT | low | An AF_UNIX SOCK_DGRAM **socketpair** (`UnixMsgPair` with datagram kind) ignores a supplied destination; the reference resolves it with the ordinary name lookup. The path-bound datagram socket does honour the name | `socket::control::prepare_unix` treats every `UnixMsgPair` as the stream kind for the name rule | B1960 lane, unclaimed |
| OPEN | COVERAGE | low | The guest differential probe `t_cmsgfam` covers the privilege-independent answers only. The capability-gated SOL_SOCKET types (`SO_MARK`, `SO_PRIORITY` above the interactive band) and the whole IPv6 ancillary level have hosted coverage but no differential frame, because the probe's uid is not pinned across host and guest | `tools/network-conformance-manifest.tsv` rows 44/46 record the gap in their closure contract | unclaimed |
| OPEN | INFRA | low | The SSH conformance transport still times out on this box (`oxide-conformance: SSH timeout`), so every guest differential here went through the serial transport. The serial path works on both arches | two consecutive `tools/oxide-conformance-ssh.sh x86_64 t_mmsg` runs reached a booted guest with `sshd` running and never completed the exec | N22 channel lane |
| OPEN | MISSING | med | A netlink socket has no home for most generic SOL_SOCKET state. `SO_PRIORITY`, `SO_MARK`, `SO_LINGER`, the timestamp personalities, `SO_BINDTODEVICE` and the rest are now VALIDATED by the canonical ladder but land in the generic flag/scalar word with no family behaviour behind them, and `SO_SNDTIMEO` has no wait to bound. The Linux shape is one `struct sock` base shared by every family; this tree has `InetSocket.opts` and a partial copy on `NetlinkSocket`. | `crates/kernel/syscalls/src/netlink_fd/sol_socket.rs` `apply`'s final arm; `crates/kernel/netlink/src/netlink_socket.rs:45` | — |
| OPEN | MISSING | low | `UDP_ENCAP` accepts the reference's value set and is stored, with zero consumers: there is no ESP-in-UDP or L2TP decapsulation path in the tree at all, so the option selects nothing. | `net::sock_opts::sol_udp::table` `encap_type`, no reader outside `sol_udp` | — |
| OPEN | MISSING | low | `IPV6_RECVPATHMTU` stores and reads back its bit with no consumer: no receive path emits the `IPV6_PATHMTU` ancillary message the bit asks for. | `sol_ipv6/set.rs:199` writes `flag::RXPATHMTU`, `get.rs:96` reads it, no other reader in the tree | — |
| OPEN | DEFECT | low | `IPV6_2292PKTOPTIONS` is a stream-socket ancillary snapshot in the reference; ours answers zero bytes on read and accepts only `optlen == 0` on write. | `sol_ipv6/get.rs` `IPV6_2292PKTOPTIONS` arm returns an empty `Vec`; `054_setsockopt/ipv6.rs` | — |
| OPEN | DEFECT | low | `TCP_INFO`'s published struct ends at the retransmit-time field and omits the AccECN tail the reference's uapi now carries, so a caller reading the current structure size gets a short answer. | `crates/kernel/syscalls/src/tcp_info.rs` | — |
| OPEN | DEFECT | low | `SO_ERROR` reports only the primary error; the reference falls back to the soft (ICMP-derived, non-fatal) error when the primary is clear, reporting it once. | `crates/kernel/syscalls/src/recvmsg/dispatch.rs` `take_error()` has no soft slot. Overlaps B1959's error-queue ownership — not touched here | B1959 |
| OPEN | COVERAGE | med | No differential probe corpus exercises `setsockopt`/`getsockopt` at SOL_NETLINK or SOL_SOCKET-on-netlink. `userspace/af_packet_diff/` covers SOL_PACKET and `userspace/glibc_conformance/t_{set,get}sockopt.c` covers neither level; the netlink defects fixed here were found by reading, not by a run. | `grep -n "NETLINK" userspace/glibc_conformance/t_setsockopt.c` is empty | — |
| OPEN | MISSING | med | TCP output does not carry `SO_MARK` into its route lookup. The reference resolves a stream's route ONCE, at connect, from the socket's mark and caches it; this tree re-resolves per segment from a `TcpEntry` that has no access to the socket's option table. Closing it needs the connect-time route cache (the `dst` entry), not another mirrored field on the entry — a mirror is exactly the split source of truth this lane removed elsewhere. The mark DOES reach the connect-time source selection and the raw/datagram transmit lookups | `net::sock::tcp_lifecycle` passes the socket mark; `stack::tcp_tx::send_tcp_segment_in` passes `stack_binddev::UNMARKED`, and every such site is named by that constant | unclaimed |
| OPEN | MISSING | low | Route METRICS and path-MTU queries (`route_metrics_for_dst_in`, `mss_for_dst_on_iface_pmtu_modes_in`, the ICMP path-MTU update) ask with no mark, so a marked socket whose mark selects a different table reads the unmarked route's metrics | the `stack_binddev::UNMARKED` call sites | unclaimed |
| OPEN | DEFECT | low | An AF_UNIX DATAGRAM send that reports EPIPE does not raise `SIGPIPE`; the send layer raises it for the stream and seqpacket kinds only. The reference raises it for the datagram kind too unless `MSG_NOSIGNAL` | `socket::send::send_unix_blocking` gates the raise on `stream \|\| seqpacket` | unclaimed |
| OPEN | MISSING | low | `unix_may_send` has no equivalent here: a datagram sent to a socket that is connected to a THIRD party is delivered, where the reference answers EPERM | no EPERM on any AF_UNIX datagram send path; `socket::control::prepare_unix` computes the symmetry only for the flow-control rule | unclaimed |
| OPEN | COVERAGE | low | The wiring from the kernel-gated socket send entry points (`net::sock::udp::socket_sendto_ctl`, `sock_v6::sendto_v6_ctl`) into the mark-carrying route lookup cannot be exercised hosted — both files are `#[cfg(target_os = "oxide-kernel")]`. The DECISIONS they carry are all in ungated modules and tested there; only the call itself rests on the build | `crates/kernel/net/src/sock.rs` module gating | unclaimed |
| OPEN | INFRA | low | `t_tcp_wait` cannot share a serial conformance run: five probes including it exceed the serial timeout ceiling and abort | the same four probes without it complete well inside the budget; with it the runner service starts and never deactivates | unclaimed |
| OPEN | COVERAGE | low | Row 42 now runs at its declared `uid=root`, but only the host half is verified: three privileged and three unprivileged host runs are byte-identical, and the guest half is unchecked because the probe overruns the serial budget | the row above | unclaimed |
| OPEN | MISSING | LOW | `IPV6_PATHMTU` as a `getsockopt` — the current route MTU, ENOTCONN when there is none — is not implemented. It is a different question from the `IPV6_RECVPATHMTU` announcement this branch closed, and it lives in `net/src/sock_opts/sol_ipv6`, owned by another lane. | `grep IPV6_PATHMTU crates/kernel/net/src/sock_opts/` finds no `get` arm. | sock_opts lane |
| OPEN | COVERAGE | LOW | The differential probe covers IPv4 UDP, AF_UNIX stream and the `recvmmsg` batch. VSOCK and NETLINK receive copy-faults have hosted coverage of the shared publication but no differential frame, because neither family has a host-side oracle reachable from an unprivileged probe. | `tools/network-conformance-manifest.tsv` rows 47/299 record the gap in their closure contract. | unclaimed |
| OPEN | MISSING | med | **Retraction of part of the row that opened this lane.** B1961 recorded that netlink's generic options "land in the generic flag/scalar word with no family behaviour behind them". For the options that DO land there this is not a defect: the reference keeps the same state on its one socket base and its netlink code consults only the receive buffer, the send buffer, the two timeouts and the error — every other generic option is stored and unread for a netlink socket there too. The real defect was the discarded set, fixed above. | Reference netlink code consults `rcvbuf` (receive admission), `sndbuf` (send preflight), both timeouts and the error; ours consults the same four | B1964 |
| OPEN | MISSING | med | No transform (IPsec) subsystem exists, so a datagram the encapsulation handler classifies as an encapsulated security payload is consumed and dropped rather than decapsulated and re-injected. This matches the reference outcome when no security association matches, but is not the same code path: with an association the reference delivers the inner packet. | `tests_udp_encap::security_encap_consumes_the_encapsulated_payload` pins the consume verdict; no test can pin decapsulation until a transform subsystem exists. | B1964 |
| OPEN | MISSING | low | `UDP_ENCAP_L2TPINUDP` is stored but has no tunnel subsystem to install a receive handler, so it correctly falls through to ordinary delivery — confirmed against the reference, where a plain socket option installs a handler only for the security identity. The label stays unconsumed until an L2TP subsystem exists. | `tests_udp_encap::tunnel_identity_alone_installs_no_handler_and_delivers`; routing the tunnel identity into the handler turns it RED. | B1964 |
| OPEN | COVERAGE | low | The `UDP_ENCAP` accepted-value set (none / security / tunnel) was re-verified against the reference and matches; every other identity is `ENOPROTOOPT`. No divergence found. | `tests_udp_encap::only_three_identities_are_accepted_by_the_option`; widening the set turns it RED. | B1964 |
| OPEN | MISSING | med | Our stream sockets stash NO per-segment receive snapshot, so the reference's first `IPV6_2292PKTOPTIONS` read path — walk a stashed received segment and publish its real header state — has nothing to walk. The synthesised fallback (socket sticky + receive state) is therefore the path that ALWAYS runs, and a reader gets the socket's own choices rather than the values the last segment actually carried. No snapshot was fabricated. | `crates/kernel/syscalls/src/055_getsockopt/ipv6.rs::pktoptions_get` builds `Published` from socket state only; `net` has no per-segment capture on `SockKind::TcpConn`. `sol_ipv6::pktoptions::tests::*` pin the fallback's content and order. | B1964 |
| OPEN | MISSING | med | The `IPV6_2292PKTOPTIONS` write validates the per-datagram messages (packet info, flow label, hop limit, traffic class, fragmentation) for shape and window and then discards them, which matches the reference — but the reference ALSO screens the packet-info message's interface and source address against the device table and the socket's binding (ENODEV / EINVAL) before discarding. Ours screens only the length, so it accepts a stream the reference would refuse. | `sol_ipv6::pktoptions::admit_one`, `IPV6_PKTINFO` arm; `tests::the_scalar_messages_are_screened_and_then_discarded` pins the length screen only. | B1964 |
| OPEN | COVERAGE | low | Verified against the reference and recorded so nobody re-derives it: `IPV6_RECVPATHMTU` does NOT travel on the extended-error queue. The reference's producer exchanges the notification into a dedicated single-slot socket field and the consumer exchanges it back out at the top of the ordinary receive; the error-queue helper is a different function used by a different producer. Routing it through `net::socket_error` would require `MSG_ERRQUEUE` and would let notifications queue behind one another — neither is the contract. | `sol_ipv6::pathmtu` module comment; `pathmtu::tests::an_ordinary_receive_drains_the_cell_and_an_unset_option_does_not`; no `socket_error` call anywhere on the path. | B1964 |
| OPEN | MISSING | low | The path-MTU notification is produced at the socket-level send seam, so the MTU it reports is the route's path MTU minus the extension-header bytes the send carried. The reference computes it inside the output builder, where the header size is exact for the fragment layout it was about to emit; ours can differ by the fragment-header allowance on a send that would have fragmented. The difference is only reachable on a socket that forbade fragmentation, which is the only socket that gets the notification at all. | `sock_v6.rs::note_path_mtu`; `pathmtu::reported_mtu` + `pathmtu::extension_bytes` are the ungated owners and are pinned by `the_reported_mtu_excludes_the_extension_headers_the_send_carried`. | B1964 |
| OPEN | MISSING | med | Carried forward from B1964, unchanged by this lane: our stream sockets stash no per-segment receive snapshot, so the `IPV6_2292PKTOPTIONS` read always takes its synthesis path and reports the socket's own sticky and receive state rather than the values the last segment carried. | `crates/kernel/syscalls/src/055_getsockopt/ipv6.rs::pktoptions_get`; `net` has no per-segment capture on `SockKind::TcpConn`. | unowned |
| OPEN | MISSING | low | Informational pin, re-verified: the force-buffer options are set-only in the reference and answer `ENOPROTOOPT` on a read. The one read table has no case for them, which is the correct behaviour; do not add a symmetric getter. | `net` `sock_opts::sol_socket::get::value` has no `SO_SNDBUFFORCE`/`SO_RCVBUFFORCE` arm, so both fall to `ENOPROTOOPT`; the differential corpus pins it. | B1966 |
| OPEN | COVERAGE | low | `crates/kernel/syscalls/src/netlink_fd.rs::send_coalesced_file` and `::sendmsg_imported` have no callers anywhere in the tree — the live netlink send path is `socket::send::send_prepared`. Both were updated for the new send signature and both remain unreachable, so nothing exercises them. Found while plumbing the non-blocking flag; recorded rather than deleted because deciding their fate is a netlink-descriptor question, not a socket-base one. | `grep -rn 'send_coalesced_file\|sendmsg_imported' crates` returns only the definitions; the file is `#![cfg(target_os = "oxide-kernel")]`, so no hosted build even compiles them. | unowned |
| OPEN | INFRA | low | An `aarch64` differential run in a fresh worktree fails before QEMU with `incomplete vendored arm64-efi modules (missing modinfo.sh, linux.mod)` — `vendor/` is not populated by the worktree checkout, and the failure is reported by the smoke script as `boot exited before UART became available`, which reads as a kernel problem. `tools/fetch-vendor.sh` fixes it; the message a lane sees does not say so. | `target/smoke/sockopt-diff/arm-20260808-063714-AyocCG/boot.log`: `xtask grub: incomplete vendored arm64-efi modules ... run tools/fetch-vendor.sh`, after `Finished \`release\` profile` — the kernel had built cleanly. | unowned |
| OPEN | COVERAGE | low | One x86 boot in this lane stalled 45 s after systemd start and froze with `Failed to fork off sandboxing environment for executing generators: Protocol error`; the immediately preceding and following boots of the SAME tree reached the probe and passed the differential. Recorded rather than attributed: a single boot cannot distinguish this from the tree's known cold-boot intermittency. | `target/smoke/sockopt-diff/x86-20260808-063154-hiZouE/oxide-uart.log` (frozen) against `x86-20260808-063509-OPTFxj` and `x86-20260808-063714-jILeqz` (both reached `meta\|complete\|status=DONE`). | unowned |
| OPEN | DEFECT | med | Row 324 `membarrier`: `PRIVATE_EXPEDITED_SYNC_CORE` and its REGISTER are refused. The refusal's stated reason is right only on x86 — every oxide return-to-user path is core-serializing (`iretq`/`eret`) EXCEPT the x86 SYSCALL/SYSRET fast path, and arm64 needs nothing at all. Closing it needs a `finish_task_switch`-shaped hook forcing IRETQ for a resuming SYNC_CORE-registered mm, plus two state bits in `mm-vmm/src/address_space/membarrier.rs` — a sibling lane owns that file, so this lane stopped rather than edit it. | `syscalls/src/membarrier.rs` `REFUSED_MASK`; `mm-vmm/src/address_space/membarrier.rs` | mm-vmm lane + sched |
| OPEN | DEFECT | med | Row 324: `PRIVATE_EXPEDITED_RSEQ` and its REGISTER are refused for a reason that is FALSE. The stated blocker — "no rseq_cs / IP-fixup, so no critical section could be restarted" — is contradicted by `sched::rseq::exit::rseq_preempt_return`, a complete signature-checked `rseq_cs` abort wired unconditionally into both arches' IRQ-exit tails. Only the syscall-side admission and IPI targeting are missing, and both can reuse the existing `ipi_barrier`/`mm.cpumask()` machinery. Blocked on the same two mm-vmm state bits. | `sched/src/rseq/exit.rs`; `arch-irq/src/{lapic,gic}/dispatch.rs` | mm-vmm lane + sched |
| OPEN | DEFECT | high | Row 272 `unshare`: `create_user_ns` does not give the new credentials a full capability set inside the new user namespace, so rootless `unshare(CLONE_NEWUSER|CLONE_NEWNS)` fails closed with EPERM. Confirmed real, not stale: `Creds` has no `user_ns` field — the effective mask is one flat word with ancestry applied per call site, so there is no per-namespace set to raise. Needs `cred->user_ns` plus an own-namespace-unconditional rule in the capability check, i.e. a capability-model change well beyond a row fix. | `sched/src/task/creds.rs`; `nscg/src/proc_ns.rs` `has_cap_for`; `syscalls/src/272_unshare.rs` | sched (own lane) |
| OPEN | MISSING | med | Row 272: a cgroupfs mount always roots at the whole hierarchy. The cgroup namespace's root is stored and consulted for `/proc/<pid>/cgroup` rendering and `nsdelegate` checks, but never to pick the mount's root inode, so a task inside a cgroup namespace still mounts and browses the entire tree. | `cgroup/src/fs.rs` `realize_tree`; `syscalls/src/fsmount_common/registry.rs` | cgroup lane |
| OPEN | MISSING | med | Row 60/231: no per-file tty hangup revocation. Hangup is one shared flag on the tty, cleared unconditionally on EVERY open, and nothing records which open files reference a tty. So a stale fd from a dead session becomes usable again the moment a new session opens the line, where it must stay revoked for its whole lifetime. Needs a per-tty open-file registry plus either a mutable per-file op vector or a per-open hangup epoch — confined to tty/vfs/devpts, but outside this lane. | `tty/src/ldisc/n_tty/state.rs`; `tty/src/core/lifecycle.rs`; `vfs/src/file.rs` (`f_op` never mutated after construction) | tty lane |
| OPEN | MISSING | low | Row 60/231: no tty-audit. Nothing to hook it to — this kernel has no audit subsystem, only a netlink protocol stub that ACKs and records nothing. The tty half is meaningless until the audit subsystem exists, so this belongs to an audit row, not to `exit`. | `netlink/src/audit.rs` | audit lane |
| OPEN | MISSING | high | Row 298 `perf_event_open`: 4 of its 5 claimed gaps are real (this row is NOT stale-pessimistic). No sampling ring buffer at all — an `mmap` on a perf fd falls through to the generic path and yields an unbacked private mapping, so `perf record` collects nothing; no `PERF_RECORD_*` emission; no hrtimer sampling; no `attr.inherit` propagation (nothing in the clone path references perf); no non-software PMUs; no cgroup-mode events. The 5th (`MODIFY_ATTRIBUTES` EOPNOTSUPP) is not a gap — upstream returns the same for every non-breakpoint event. One separable real defect: `PERF_EVENT_IOC_SET_OUTPUT(-1)` returns EINVAL where upstream returns 0 for an event with no live mmap. Sizing: a new ring module implementing `vmm::FileBacking` per the packet-ring pattern, a `009_mmap` dispatch arm, a `perf_event_mlock_kb` sysctl with locked-VM accounting, record formatting, hrtimer wiring, and per-task inherit state — a campaign, not a row fix. | `fs/src/perf/**`; `syscalls/src/009_mmap.rs` has no perf arm | perf lane |
| OPEN | COVERAGE | med | Rows 61/247 have no deterministic-interleaving harness for reap-vs-exit races; the nearest existing pattern is probabilistic `std::thread::spawn` tests. Nothing loom/shuttle-shaped exists in the tree to reuse. | `sched/src/tests/pidfd.rs` concurrent tests | sched |
| OPEN | COVERAGE | MED | Nothing in this repository exercised whether either architecture's leaf packer honours a request for the write-protect barrier — the only coverage was a hosted mock walker. Reinstating the defect in BOTH arch packers left every gate GREEN. Found by running the positive control; closed for this flag by the new per-arch test, but the class (an arch packer honouring an arch-neutral `PageFlags` bit with no per-arch test) is not covered systematically — `GLOBAL`, `WRITE_COMBINE` and the pkey bits have round-trip tests, the rest do not. | Control run: `pack_4k_leaf` with the `UFFD_WP` arm deleted → `cargo test -p hal-x86_64` 169 passed / 0 failed (GREEN, defect present). After adding `a_leaf_asked_for_the_barrier_is_never_published_writable`, the same deletion → 169 passed / 1 failed on x86_64 and 164 / 1 on aarch64. | unassigned |
| OPEN | MISSING | MED | `UFFD_FEATURE_WP_ASYNC`'s usual consumer does not exist: `/proc/<pid>/pagemap` is an empty stub and there is no `PAGEMAP_SCAN`-style ioctl. The asynchronous mode itself works — a write clears the per-page state and proceeds with no message — but a monitor has no way to READ back which pages were written, which is the entire purpose of the mode. Outside this lane (procfs). | `crates/kernel/procfs/**` has no `pagemap` producer; the write-protect state is already the durable record (`hal::pt_walker::PtWalker::{leaf_is_uffd_wp, nonpresent_is_uffd_wp}`), so a reader needs only to project it. Needs: a per-page `pagemap` entry producer over the address space's leaves, and the scan ioctl's category/mask filtering plus its write-protect-while-scanning arm. | unassigned — proposed follow-up lane |
| OPEN | COVERAGE | LOW | seccomp `SECCOMP_GET_NOTIF_SIZES` reported its two structure sizes from arithmetic local to `entry.rs`, a second copy of the wire format. Now derived from the notification module's own constants — recorded because the class (a size computed twice) is worth grepping for elsewhere | `notif::uapi::{NOTIF_BYTES, NOTIF_RESP_BYTES}` are the only definition | B1969 |
| OPEN | MISSING | MED | seccomp notification ids are drawn from one monotonic space per boot rather than a random seed per listener. Unique, which is what the protocol needs, but not the reference's defence in depth — there is no kernel entropy source reachable from `security` (`devfs::misc::random_u64` sits behind a crate this one does not depend on) | `listener.rs` `NEXT_NOTIF_ID` | unclaimed |
| OPEN | MISSING | MED | `verify_socket_filter` is an opcode whitelist with no register or pointer tracking, unlike the cgroup verifiers beside it | `bpf_verify.rs:166-211` | unclaimed |
| OPEN | MISSING | HIGH | 16 of 39 `bpf(2)` commands answer EINVAL (PROG_TEST_RUN, PROG_GET_NEXT_ID, RAW_TRACEPOINT_OPEN, TASK_FD_QUERY, four MAP_*_BATCH, LINK_UPDATE/DETACH/GET_FD_BY_ID/GET_NEXT_ID, ENABLE_STATS, ITER_CREATE, PROG_STREAM_READ_BY_FD, PROG_ASSOC_STRUCT_OPS), so `bpftool` does not work end to end | command dispatch in `security/src/bpf.rs` | unclaimed |
| OPEN | MISSING | MED | `BPF_PROG_TYPE_LSM` is not loadable, so `security::bpf_lsm`'s registration half has no possible source and no program body ever runs on the `file_open` hook it does reach. The hook itself IS called (`257_openat.rs:504`) — the previous "unreachable dead machinery" reading was wrong | `bpf/attr.rs` `prog_type_supported`; call site `257_openat.rs:504` | unclaimed |
| OPEN | INFRA | LOW | `crates/kernel/security/src/bpf.rs` is 524 lines, over the 500-line cap | `wc -l` | unclaimed |
| OPEN | COVERAGE | MED | the seccomp notification's blocking path and the ioctl router's CALL SITE are both in target-gated code, so no hosted test can fail if either is deleted. The decisions each reaches (queue transitions, listener resolution, router predicate, verdict mapping) are all ungated and covered; the two call sites rest on the arch builds and the boot smoke | `016_ioctl/core.rs` router arm; `entry.rs` UserNotif arm | unclaimed |
| OPEN | MISSING | med | `sync(2)`'s device pass is structurally a no-op for the submit half and a per-disk barrier for the wait half, because there is no block-device page cache — device writeback does not exist. Consequence: `sync(2)`'s per-mount barrier count is still 2; the double-commit fix shows up in `syncfs`, freeze and unmount instead. | recorded by the implementing lane in the code | unowned |
| OPEN | MISSING | med | `POSIX_FADV_NOREUSE` records nothing. The reference stores a per-description bit whose only consumer biases reclaim of MAPPED pages; oxide's equivalent hook has zero call sites, so storing the bit would recreate the stored-but-unconsumed defect. Closing it needs the reclaim-side consumer, which is memory-management territory. | row 221 | unowned |
| OPEN | MISSING | med | fuse `on_flush` encodes `fuse_flush_in.fh = 0` unconditionally — the FLUSH request never names the open handle it is flushing, because the `FileOps::on_flush` signature takes an `&Inode` and not the `&File`. Found while adding the fsync slot; out of this lane's row scope. | `fs/src/fuse/fops.rs` `on_flush` | unowned |
| OPEN | MISSING | med | No delegation break (`try_break_deleg`) anywhere in the tree. The reference DOES call it from the mknod path (rows 133/259 were right that it is missing, and wrong to imply mknod is special: unlink, rmdir, rename, link and setattr all lack it too). Oxide has open-time lease breaking only, which is the conflicting-open mechanism, not the break-on-mutation one. | `grep` for a delegated-inode concept returns nothing; open-time lease break is wired only into the open path | unowned |
| OPEN | MISSING | med | `/dev` inodes answer `EOPNOTSUPP` to `file_getattr(2)`/`file_setattr(2)` where the reference's shmem-backed device filesystem answers 0 (rows 468/469). The blocker is structural and was mis-stated in the row: it is not that devfs "is kernfs-backed", it is that there is no per-superblock inode-op default — every driver crate builds its own `/dev` inode with the generic `default_inode_ops()`, and the shared pseudo-directory ops are common to sysfs/procfs/tracefs, which must keep answering `EOPNOTSUPP`. Closing it needs a devfs-owned inode-ops choke point, which spans driver crates. | `devfs/src/misc.rs` and every driver's `InodeBuilder::new(..., default_inode_ops(), ...)`; `kernfs::PseudoDirOps` shared by 7 crates | unowned |
| OPEN | MISSING | med | `SPLICE_F_MORE` never reaches a socket output as the corresponding send hint (row 275). The blocker is in the splice leg, not the socket: the pipe-to-file leg calls the plain `File::write`/`pwrite`, which take no per-operation flags word at all, so there is no channel for the hint even in principle. | `fs/src/splice/pipe_xfer.rs` pipe-to-file leg; `vfs::File` write API has no flags parameter | unowned |
| OPEN | MISSING | high | ext4 `FALLOC_FL_COLLAPSE_RANGE` / `INSERT_RANGE` answer `EOPNOTSUPP` from the backend: both re-index every extent past the range and the extent tree has no shift primitive (row 285). | `ext4/src/rootfs/inode/fallocate.rs` admits both modes and has no arm for either | unowned |
| OPEN | MISSING | med | `S_SWAPFILE` is never set, so a live swapfile is refused by an ext4-private pin (`EBUSY`) instead of the generic gate (`ETXTBSY`) (row 285). The swap layer holds a block-device handle, not an inode reference. | `S_SWAPFILE` has no producer outside test fixtures | unowned |
| OPEN | COVERAGE | med | Quota block writes go through the ordinary journalled metadata path, but no test drives a crash/replay against a quota-block write specifically, so rows 179/443 cannot claim audited crash consistency. Every OTHER item those rows still list as open is closed. | no quota + journal-replay test in the ext4 suite | unowned |
| OPEN | DEFECT | LOW | AF_VSOCK and AF_NETLINK apply the generic bind/connect security hook INSIDE the family implementation, after some address-shape screening, where Linux runs `security_socket_bind`/`security_socket_connect` in the generic layer before any `->bind()`/`->connect()`. So `bind(vsock_fd, short_addr)` under a denying module reports EINVAL where Linux reports EACCES; likewise a short `sockaddr_nl`. The INET/INET6/UNIX/PACKET path is already correct (`049_bind.rs` calls `net::sock::admit_bind` before every family length screen). | `049_bind.rs` `require_sockaddr_vm` / `storage.vsock()` precede `vsock_socket::lifecycle::bind`'s `security_admission::check`; `syscalls::netlink_fd::bind` parses family and port before its check at `netlink_fd.rs:109`. Closing it needs one family-agnostic admission token so the hook has a single call site above the family branch. | unowned |
| OPEN | MISSING | MED | `clone(CLONE_SYSVSEM)` WITHOUT `CLONE_THREAD` gets its own empty semaphore undo list. Linux shares a refcounted `sem_undo_list` whenever `CLONE_SYSVSEM` is set, independent of `CLONE_THREAD`. oxide keys the list on the thread-group id, which reproduces glibc's usage exactly (pthread_create always passes the flag, fork never does) and nothing else. | `crates/kernel/ipc/src/sysv/sem/undo.rs` `ProcUndo { tgid, .. }`; `056_clone.rs` carries no `CLONE_SYSVSEM` handling. Closing it needs a shareable per-task owner, which lives in `sched` — reported rather than edited, B1967 owns that tree. | B1967 / ipc |
| OPEN | MISSING | LOW | The socket security context carries no backlog, so `listen(2)`'s hook cannot see the value it is deciding about — Linux passes the clamped backlog to `security_socket_listen`. The clamp now runs first (335e72190), so the value exists at the hook; only the context field is missing. | `security::network::Context::op(ns, family, 0, 0, Listen)` has no backlog operand. `crates/kernel/security/**` is B1969's tree. | B1969 |
| OPEN | MISSING | LOW | `sockaddr_in6.sin6_flowinfo` is copied into `SockaddrStorage` and never read by any accessor. | `net::sockaddr::SockaddrStorage::inet6` returns address, port and scope id only. | unowned |
| OPEN | COVERAGE | MED | The bind/connect sockaddr length and shape decisions (`move_sockaddr_to_kernel_shape`, `require_sockaddr_in`/`in6`/`vm`) live in `syscalls::net_sockaddr`, declared inside the target-gated `kernel_body.rs` module list, and carry no tests at all. The only "coverage" is a source-text grep asserting `049_bind.rs` names the function. These decisions are pure and belong in an ungated module, as `sockaddr_encode.rs` already does for the getname encoders. | Row 49 evidence column; `net_common.rs` grep-based assertions. | unowned |
| OPEN | COVERAGE | LOW | N25 (TCP connect wait linearization) and the N22 guest differential channel remain open in `scratch/network-plan.md`; rows 42/43/50/288 carry them as their last substantive item. N26 is all-but-closed — only its `SOL_VSOCK` option differential (N26.4) is open, so the rows' "N26 VSOCK wait linearization" wording is stale. | `scratch/network-plan.md:1648` (N25 unchecked), `:1695` (N26 partial, 7/8 sub-items done). | net |
| OPEN | MISSING | med | `fcntl` has no `F_GETDELEG`/`F_SETDELEG` (1039/1040). Every other command the reference's `do_fcntl` handles is present. Needs a lease-FLAVOR bit: the reference's get-lease query filters the lease list by flavor, so `F_GETLEASE` must report only plain leases and `F_GETDELEG` only delegations, while both share one break path. Blocked on a vfs change this lane does not own (`vfs::File::lease` is a single `AtomicI32` with no flavor; `lease_conflicts` matches the bare type, so an OR-ed flavor bit breaks it). Note the asymmetry: set-lease rejects a directory fd, set-deleg does not. | `crates/kernel/syscalls/src/072_fcntl.rs` has no case for either; `crates/kernel/vfs/src/file/lease.rs` stores one unflavored type. | B1970 (vfs) + syscalls |
| OPEN | MISSING | med | Group-stop / continued notification reaches one parent only. The reference notifies BOTH parents in two places: a ptrace-stop notifies the tracer, and ALSO the real parent when that stop completed a group stop and the two parents differ; and the continued path notifies the real parent, then the group leader's tracer when the task is reparented. Oxide issues exactly one call with a single target. | `crates/kernel/sched/src/live/stop.rs:71-74` single `notify_parent_cldstop`; `crates/kernel/sched/src/jobctl.rs:123-124` `notify_target` maps Ptrace→Tracer, JobControl→RealParent with no second arm. | B1967 (sched) |
| OPEN | MISSING | med | `clock_nanosleep` cannot tell the STATIC per-thread CPU clock from a dynamic per-thread encoding naming pid 0. The reference splits them by table — the static id has no sleep callback (EOPNOTSUPP), the dynamic encoding has one and then rejects a self-naming clock (EINVAL). Oxide reports EOPNOTSUPP for both because `classify_clock` maps them to the same value. Fix needs static-vs-dynamic provenance on the clock spec. | `crates/kernel/sched/src/timers/clockid.rs:100-104,229` — both ids produce `CpuEncoded { pid: 0, per_thread: true }`. | B1967 (sched) |
| OPEN | DEFECT | low | `CpuMeasure::Sched` samples `utime+stime` where the reference's scheduler CPU clock is the task's total scheduler runtime. The two differ by the accounting granularity, so a sleep or timer on the scheduler clock expires at the wrong point. | `crates/kernel/sched/src/timers/clockid.rs` `CpuMeasure`; no scheduler-runtime accumulator exists to read. | B1967 (sched) |
| OPEN | MISSING | med | `membarrier` refuses `PRIVATE_EXPEDITED_SYNC_CORE`, `PRIVATE_EXPEDITED_RSEQ` and both their REGISTERs with EINVAL and omits them from the QUERY mask. The in-tree justification is that the reference does the same without the matching configs — but BOTH target architectures select the core-serialization config upstream, and the rseq config is on, so all four are live on a reference kernel for x86_64 and aarch64 alike. Needs a per-mm serialize-before-return-to-user hook and rseq critical-section abort. | `crates/kernel/syscalls/src/membarrier.rs` `REFUSED_MASK`; reference selects the sync-core config in both arch Kconfigs. | B1967 (sched) + B1968 (mm-vmm) + syscalls |
| OPEN | MISSING | med | rseq time-slice extension is absent: no `PR_RSEQ_SLICE_EXTENSION` prctl, no `slice_ctrl` word in the rseq ABI, no AVAILABLE/ENABLED flag bits at registration, no preempt-time grant, no revoke timer, no syscall-entry revoke work. `rseq_slice_yield` (471) therefore always answers 0. That is the same answer a reference kernel gives a thread that never opted in, so the syscall is not observably wrong — but nothing can ever set the flag. Adding only the prctl would store state nothing consumes. | `crates/kernel/syscalls/src/471_rseq_slice_yield.rs` reads `Task::rseq_slice_yielded`; no writer exists. | B1967 (sched) |
| OPEN | MISSING | low | No uprobes subsystem. Slots 335/336 are complete for every reachable caller — a process cannot hold a uprobe trampoline without the subsystem, so the no-trampoline arm is the whole reachable body — but the trampoline mapping, the probed-frame unwind, the shadow-stack pop and the consumer dispatch do not exist, so no uprobe can ever be installed. This is a tracing-subsystem row, not a syscall-ABI one. | `grep -rn uprobe crates/` finds only the two slot files and `uprobe_abi`. | unassigned |
| OPEN | MISSING | low | `hal_aarch64::hw_breakpoint::hw::toggle_enables` has ZERO callers repo-wide — machinery without a consumer. It is the step-over-a-hit primitive for hardware breakpoints whose handler is the DEFAULT overflow handler (perf/kernel breakpoints). It is NOT needed for ptrace breakpoints: the reference registers a non-default overflow handler for those, which skips the step-over entirely and just delivers SIGTRAP — which is what oxide does. The matrix filed this under ptrace, where it does not belong. | `grep -rn toggle_enables --include=*.rs .` → definition only, at `crates/arch/hal-aarch64/src/hw_breakpoint/hw.rs:211`. | unassigned |
| OPEN | COVERAGE | low | Casefold is fileattr flag translation only — no superblock feature bit, no case-insensitive dentry comparison, for ext4 or tmpfs. tmpfs correctly refuses to turn the bit on with no encoding; ext4 has no handling at all. Row 16's prose implies more support than exists. | `crates/kernel/fs/src/tmpfs/fileattr.rs:33-38`; `crates/kernel/ext4/src/superblock.rs:40` names the feature bit in a comment only. | B1970 (vfs/fs) |
| OPEN | INFRA | low | `crates/kernel/syscalls/src/230_clock_nanosleep.rs` and `035_nanosleep.rs` carry external-implementation path/line citations in comments, which repository text must not do. The same file has a fenced non-Rust code block under `///`. Behaviour is unaffected; a citation sweep missed these. | `230_clock_nanosleep.rs` doc comments and inline notes. | unassigned |
| OPEN | DEFECT | med | Rows 60/231: no per-FILE terminal hangup revocation. Re-verified and the claim is exactly right. The reference revokes per open FILE — `__tty_hangup` walks the tty's list of open files and replaces each one's op vector permanently, so a stale fd stays revoked for its whole lifetime and a later open of the same line builds a fresh file that does not touch it. oxide has one shared `hung_up` bool on the ldisc, cleared unconditionally on EVERY open, and records nothing about which files reference a tty. Fix spans `vfs/src/file/model.rs` (`f_op` has no mutator after construction — the load-bearing change) and `tty/**` (a per-tty open-file registry plus the hangup walk). NOTHING in it is sched. | `tty/src/ldisc/n_tty/state.rs:42,261`; `tty/src/core/lifecycle.rs:45-53`; `vfs/src/file/model.rs:40,214`. | needs a lane (vfs + tty) |
| OPEN | MISSING | low | Rows 60/231: no tty-audit, because there is no audit subsystem to hook it to. `netlink/src/audit.rs` is a 70-line ACK sink — zeroed status, zeroed features, empty rule list, blanket success for rule add/del and every `AUDIT_USER*` record. No record buffer, no log emission anywhere in `crates/`. This belongs to an audit row, not to `exit`. | `netlink/src/audit.rs`. | needs a lane (new audit subsystem) |
| OPEN | DEFECT | med | Process-group ids are stored and reported UNTRANSLATED across pid namespaces. `ThreadGroup::pgid` is an internal tid, `getpgid(2)` returns it raw, and `wait_select::pid_matches` compares a userspace `-pgid` (a number in the READER's namespace) against it. The system is self-consistent — a program that passes `getpgid()`'s result to `wait4(-pgid)` works — so the divergence is that the initial namespace's numbers leak into a nested one. Found while closing the wait rows' namespace coverage; it is a campaign spanning `sched::session`, the signal target selectors, tty foreground-group handling and procfs, not a wait-row fix. | `sched/src/wait_select.rs:41,43` compares `c.pgid` (internal) against the caller's number; `thread_group.rs:249`; `121_getpgid.rs` returns it unchanged. | needs a lane (pid-ns pgid) |
| OPEN | COVERAGE | med | Rows 61/247: no hosted harness driving `sys_wait4` / `sys_waitid` themselves. Both slots are `#![cfg(target_os = "oxide-kernel")]`; the ORDERING they obey is now ungated and tested (B1967) and the namespace answers are now tested (this branch), but the copy-out, the option decode and the park/retry loop are still only exercised by a boot. Sizing: the `namespace_ownership_hosted.rs` pattern applies, but the slot pulls in `sched::live::{park_for_wait4, schedule}`, the zombie registry and `crate::userbuf`, so it needs a shadowed `sched::live` plus a usercopy mock — larger than the unshare harness it would copy. | `061_wait4.rs`, `247_waitid.rs` gate lines. | sched |
| OPEN | COVERAGE | med | Rows 61/247/434/438: no deterministic-interleaving facility exists in the tree, so reap-vs-exit races, pidfd namespace-destruction and pid-namespace lifetime races are covered only by probabilistic `std::thread::spawn` tests. Nothing loom/shuttle-shaped exists to reuse; this is a test-infrastructure item, not a per-row one. | `sched/src/tests/pidfd.rs` concurrent tests. | sched |
| OPEN | MISSING | LOW | An ACCEPTED IPv6 connection does not inherit the arriving SYN's flow label, so `accept(2)`'s reported peer name carries flow information only if the child's own socket settled some. The reference sets the child's stored label from the received header. Adjacent to the row above and deliberately not folded into it: it needs the TCP ingress to record the segment's flow label on the child entry, which is a transport-state change rather than a sockaddr one. | `net::stack::tcp_listener_deliver` builds the child with `Ipv6Opts` carrying no received label; `syscalls::043_accept::accepted_peer_sockaddr` reports through the same `sock_name::peer_flowinfo` owner, so the wiring is ready for it. | unclaimed |
| OPEN | MISSING | LOW | The IPv6 local-input decision reads the route TABLE, not a route TYPE, because `Route6Entry` carries no `kind` field at all — the v6 twin of the v4 defect fixed above. No broadcast class exists in IPv6, so the reachable divergence is narrower, but an anycast row in the local table still selects local input where the reference would not. | `net::stack_ipv6::rx` consults `routes6.lookup_in_table_in(net_ns, RT_TABLE_LOCAL, ip).is_some()`; contrast the type-aware v4 decision in `net::stack_forward` and `net::bind_screen`. Closing it means adding a type to `Route6Entry` and threading it through the rtnetlink v6 route path. | unclaimed |
| OPEN | INFRA | LOW | Two net tests still flake at `--test-threads=24` (carried from B1946, re-confirmed on this branch): `net::raw4::tx_control_tests::ip_nodefrag_keeps_raw_hdrincl_fragments_out_of_local_out_defrag` and `net::stack_tests::forwarding::ipv4_ingress_mib_names_unforwardable_and_unknown_packets`. They touch the globally-installed nf hook and the global MIB counters without taking the net fixture guard. | pre-existing on clean main; this lane's full-workspace run was green, so it is intermittent rather than constant | unclaimed |
| OPEN | MISSING | med | Ring regions are still ONE frame each, so `MAX_ENTRIES` is 64 (Linux: 32768) and `IORING_SETUP_SQE128`, `CQE32`, `CQE_MIXED`, `SQE_MIXED`, `NO_MMAP`, `IORING_REGISTER_RESIZE_RINGS` and `MEM_REGION` cannot be served. Needs a multi-frame kernel-owned `VmaBacking` in `mm-vmm` — `KernelFrame { pa }` maps EVERY page of a VMA to one frame, and `File` backing copies rather than shares, so neither can back a multi-page shared ring. `mm-vmm` is another lane's file area, so this is reported rather than written here. | `io_uring::ring::IoUring::new` refuses a geometry above `REGION_BYTES`; `io_uring_abi::layout::tests::no_region_overlaps_and_every_geometry_fits` | mm-vmm lane |
| OPEN | MISSING | med | No per-task io_uring state, so `IORING_REGISTER_RING_FDS`, `IORING_ENTER_REGISTERED_RING`, `IORING_REGISTER_USE_REGISTERED_RING` and `IORING_FEAT_REG_REG_RING` cannot work. Needs a registered-ring array on `sched::Task` with teardown at exit; `sched` is another lane's file area. | `io_uring_abi::register_op::tests::registered_ring_lookup_reports_linux_errnos` pins the empty-array errnos | sched lane |
| OPEN | MISSING | low | `vfs` has no `POLL_NVAL` constant, so `io_uring_abi::poll` defines the ABI bit for "the description named is not one" itself. Every other poll bit comes from `vfs::POLL_*`; this one has two spellings until `vfs` grows it. | `io_uring_abi::poll::POLL_NVAL`; `poll_flag_values_are_the_uapi_bit_positions` pins the value | vfs lane |
| OPEN | MISSING | med | `IORING_SETUP_SQPOLL` / `IOPOLL` / `SQ_AFF` / `ATTACH_WQ` / `HYBRID_IOPOLL` are still validated-and-refused. SQPOLL needs a per-ring submission-poll thread, which needs a way for a kernel thread to exit — `sched` offers none, every kthread entry is `-> !`. | `io_uring_abi::layout` admission ladder; `IORING_FEAT_SQPOLL_NONFIXED` deliberately not claimed | sched lane |
| OPEN | DEFECT | med | The worker pool is ONE global pool serving every ring, not a pool per submitting task as the reference has. Deliberate, and the reason is the same missing mechanism: a kernel thread cannot exit here, so a pool per ring would leak a thread set per ring for the life of the system. Each work item therefore carries its own borrowed address space, descriptor table and credentials, and the per-ring `IORING_REGISTER_IOWQ_MAX_WORKERS` limit is enforced on how many of a ring's requests run at once rather than on a thread count. Closes when kthread exit exists. | `io_uring::iowq::pool`; `IoUringInode::iowq_admits` / `iowq_release` | B1975 |
| OPEN | COVERAGE | med | The engine's runtime halves — the worker loop, the borrow install/release, the arming and the in-flight search — live under `#![cfg(target_os = "oxide-kernel")]`, so only their decision modules (`abi/{timeout,cancel,poll,link,enter}`) are hosted-testable. A ring fixture usable from `cargo test` would close it, and would also close the same gap B1950 recorded for the submission and completion paths. | boot smoke is the only current check on those paths | B1975 |
| OPEN | MISSING | med | `SPLICE_F_MORE` reaches the socket door but the socket backend does not override the hint-carrying write, so the default still drops it and a pipe-to-socket splice may emit an extra segment. The send-flag constant does not exist anywhere in the tree, the socket write entries take no flags word, and the transmit path has no corking behaviour (hold a partial segment while the hint is set; push when a write arrives without it). `sendmsg(2)` does not parse the flag either, so it is one shared gap rather than two. Row 275 promotes when this lands. | `net::sock::inode` has no `write_more_file` override; `socket::send` does not parse the flag | network lane |
| OPEN | MISSING | med | `POSIX_FADV_NOREUSE` records nothing, and the reason is larger than previously stated. The reference's consumer chain is a per-description mode bit read by a per-VMA recency predicate with five call sites. Here: `mark_lru_referenced` still has ZERO call sites, file-page reclaim isolates in pure LRU order with no reference check of any kind, no accessed-marking step exists on the zap path, no lru-fault flag exists on the fault path, and the file rmap walk that does exist serves migration PTE rewriting. Additionally a VMA cannot reach an open file description at all — the file backing wraps the INODE, and the bit is per-description. Storing the bit without the consumer recreates the stored-but-unconsumed defect. | grep-confirmed zero call sites; `isolate_inactive_file_lru` has no reference check; `InodeFileBacking` wraps `InodeRef` | memory-management lane |
| OPEN | MISSING | med | `Inode` has no read-open counter. The lease/open admission implements the writer-count half of the reference's rule exactly, but an exclusive lease should also require that no OTHER reader holds the file open. Needs a counter on `Inode` incremented on a read-mode open and released from the description's drop. | `vfs::file::lease_policy::open_conflicts` covers writers only | unowned |
| OPEN | MISSING | low | The reference's delegation ignore-bits, which select which directory events recall a directory delegation, are unreachable through the fcntl UAPI (the flags word must be zero), so the per-event selection would be machinery with no caller. The flags word and the event selection go in together if an in-kernel delegation manager ever needs them. | `decode_delegation` rejects a non-zero flags word, as the reference does | unowned |
| OPEN | DEFECT | low | The `FS_IOC_FSGETXATTR`/`FS_IOC_GETFLAGS` ioctl door derives its backend errno independently of the `file_getattr`/`file_setattr` door rather than through a shared conversion. Both happen to match the reference today, so this is a latent divergence rather than a live one. | `syscalls::016_ioctl::fileattr` returns the raw backend errno | unowned |
| OPEN | DEFECT | med | ext4's quota mark-dirty only sets the dirty bit and defers the write to sync/unmount, behaving as writeback mode always. With the quota feature the reference writes the dquot synchronously through a journal handle on mark-dirty. Making every quota charge a synchronous journal commit is a boot-path performance change and needs its own lane with a boot. | `Ext4QuotaOps::mark_dirty` | unowned |
| OPEN | INFRA | high | Concurrent agents sharing ONE worktree is not safe with the tools they reach for. A lane running `git checkout -- crates/kernel/ext4/src` to undo its own patch reverted a sibling lane's uncommitted TRACKED edits in the same subtree; the sibling's untracked new files survived, producing the "unresolved import of a module whose file exists" shape. Both lanes recovered from saved copies, but the incident was detected by a build break, not by any guard. Parallel lanes need separate worktrees, or an undo discipline that never uses a path-scoped checkout. | reported by two independent lanes in this wave | unowned |
| OPEN | MISSING | high | The kernel's own type information is not exposed to userspace, so a loader cannot discover a hook stub's type id and cannot name an attach target. The load path resolves ids correctly; nothing serves them. | `bpf/btf/kernel.rs::published` builds and validates the object; no sysfs binary attribute for `/sys/kernel/btf/vmlinux`. | unowned |
| OPEN | MISSING | med | A BTF-typed hook argument cannot be followed. The verifier admits reading an argument slot and refuses every load through the loaded value, so a program that dereferences its argument is rejected where the reference would type-check the field access. | `bpf_verify/program/tests/lsm.rs::following_the_argument_pointer_is_refused` | unowned |
| OPEN | MISSING | med | `attach_btf_obj_fd` / `attach_prog_fd` is decoded by no one. The reference distinguishes a target program fd (`dst_prog`, TRACING/EXT only), a kernel-BTF object fd, and a user-BTF object fd (`-ENOTSUPP`); here a nonzero value is ignored instead of judged. | `bpf/attr/prog_cmd.rs::prog_load_check_attach` consults only `ATTACH_BTF_ID`. | unowned |
| OPEN | DEFECT | med | The rest of the verifier errno map is still collapsed to `-EINVAL`. The reference answers `-EACCES` for an unreadable register, an out-of-range stack access and an uninitialised stack read as well; only structural rejects (bad opcode, bad register number, out-of-range jump, non-exit tail) are `-EINVAL`. | `bpf/prog.rs::verify` maps every remaining `VerifyError` to `Errno::Einval`. | unowned |
| OPEN | MISSING | low | No disabled-hook blocklist. The reference refuses an attach target naming a hook stub the kernel deliberately withholds; here only implemented hooks are declared at all, so no id can name a withheld one. The blocklist becomes real the first time a declared hook is withdrawn. | `bpf_lsm/hooks.rs::HOOKS` declares exactly the hooks with call sites. | unowned |
| OPEN | MISSING | high | No BPF iterator link type exists, so `BPF_ITER_CREATE` can never succeed. Every reachable input lands on the ladder's last rung — "this link is not an iterator link" `-EINVAL` — because the only link kinds this kernel mints are cgroup and LSM links. Needs iterator link targets (task/map/prog/… seq-file iterators) and the anon-inode seq-file fd `bpf_iter_new_fd()` hands back. | `bpf::command::link_cmd::tests::each_link_command_refuses_an_unsupported_kind_with_its_own_errno` pins `iter_verdict` returning `EINVAL` for both existing kinds; the positive control (making `LinkKind::Cgroup` an iterator link) goes RED. | NEXT |
| OPEN | MISSING | high | No tracepoint registry, so `BPF_RAW_TRACEPOINT_OPEN` resolves no name and every well-formed request is `-ENOENT`. The rungs above it are real: the program type gate (RAW_TRACEPOINT / RAW_TRACEPOINT_WRITABLE take a user name; TRACING/EXT/LSM must not supply one; anything else is `EINVAL`) and the `EFAULT` on a bad name pointer. Needs a `bpf_get_raw_tracepoint()`-equivalent registry and the raw-tracepoint link object. | `bpf::command::trace::tests::no_tracepoint_name_resolves_in_this_kernel` and `only_tracepoint_and_load_time_attached_program_types_may_open_one`. | NEXT |
| OPEN | MISSING | high | No perf-event descriptors and no raw-tracepoint links, so `BPF_TASK_FD_QUERY` can describe nothing and every descriptor that survives the pid/fd lookup is `-ENOTSUPP` (524). The pid → `ENOENT`, fd → `EBADF` and `CAP_SYS_ADMIN` → `EPERM` rungs are real and ordered. Needs perf events, plus the raw-tracepoint link from the row above, plus the `bpf_task_fd_query_copy()` write-back (prog_id, fd_type, probe_offset, probe_addr, tracepoint name into `task_fd_query.buf`). | `bpf::command::trace::tests::an_undescribable_descriptor_is_enotsupp_not_eopnotsupp`, `task_fd_query_checks_cap_sys_admin_before_flags_and_pid`. | NEXT |
| OPEN | MISSING | med | No `BPF_MAP_TYPE_STRUCT_OPS` map type, so `BPF_PROG_ASSOC_STRUCT_OPS` always ends on "that map is not a struct_ops map" `-EINVAL`. The flags gate, the prog-fd lookup and the "a STRUCT_OPS program may not itself be associated" `EINVAL` are real. | `bpf::command::struct_ops::tests::no_creatable_map_type_satisfies_the_association`. | NEXT |
| OPEN | MISSING | med | Program objects carry no output stream buffers, so `BPF_PROG_STREAM_READ_BY_FD` always reads 0 bytes. That is the reference's answer for an empty stream, but it means a program's runtime diagnostics are never collected: nothing writes into the streams because there is no `bpf_stream_printk`-equivalent sink on the run path. Needs the two per-program ring buffers plus the writer. | `bpf::command::stream::tests::only_the_two_named_streams_can_be_drained` (an unknown id is `ENOENT`, matching the reference's "no such stream"). | NEXT |
| OPEN | MISSING | med | `BPF_ENABLE_STATS` switches run-time statistics on and refcounts the switch correctly, but nothing accumulates them and nothing reports them. Linux gates per-run `run_cnt`/`run_time_ns` accounting on the switch and surfaces it through `bpf_prog_info`. Needs (a) the accumulation in the program run path and (b) `run_cnt`/`run_time_ns` in the `OBJ_GET_INFO_BY_FD` program-info encoder — the latter lives in `bpf/btf/info.rs`, outside this lane's file ownership. | `bpf::command::stats::tests::the_last_dropped_hold_turns_collection_off` pins the refcount; the count has no reader outside its module, which is the gap. | NEXT |
| OPEN | DEFECT | med | `BPF_PROG_TEST_RUN` on an skb context runs a flat packet: the reference splits input past the linear region (`PAGE_SIZE - headroom - tailroom`) into `MAX_SKB_FRAGS` page frags, so a program observes `data_end - data` covering the linear part only, while `skb->len` covers everything. Here `data_end - data` covers the whole input. Input past the total frag budget is `-ENOMEM`, matching the reference's exhaustion path, but the split itself is not modelled. | `bpf::command::test_run::tests::an_skb_run_needs_at_least_a_link_layer_header` pins the `ETH_HLEN` floor and the `TEST_RUN_DATA_MAX` ceiling; nothing yet pins the linear/frag boundary because there is no non-linear frame here. | NEXT |
| OPEN | DEFECT | low | `BPF_PROG_TEST_RUN` accepts `BPF_F_TEST_SKB_CHECKSUM_COMPLETE` but does not act on it: the reference computes the frame checksum before the run and re-checks it after, answering `-EBADMSG` when a program changed the payload without fixing the checksum. Here the flag is admitted and ignored, so that `EBADMSG` can never be observed. | `bpf::command::test_run::tests::an_skb_run_accepts_only_the_checksum_flag_and_no_cpu_or_batch` pins the admission; nothing pins the post-run verification because it does not exist. | NEXT |
| OPEN | DEFECT | med | `BPF_MAP_GET_NEXT_ID` was missing the `start_id >= INT_MAX` `-EINVAL`, so a caller passing a silly starting id got `EPERM` (or a walk) instead. Fixed in this branch by routing every object kind's `GET_NEXT_ID` through one ladder (`bpf/cmd/next_id.rs`); MAP and PROG and LINK now share it. | `bpf::command::next_id::tests::start_id_at_or_above_int_max_is_einval_before_the_capability`; positive control (deleting the bound) goes RED. | Chris Watkins |
| OPEN | DEFECT | low | `BPF_BTF_GET_NEXT_ID` still carries its own copy of the `GET_NEXT_ID` ladder in `bpf/btf/attr.rs` + `bpf/btf/command.rs` rather than calling the shared one. The two agree today, which is precisely why the duplication is dangerous — the MAP copy had already drifted (row above). Needs `btf::get_next_id` to delegate to `cmd::next_id::get_next_id` with `object::next_id` as its walker. Those files were outside this lane's ownership. | Compare `bpf/btf/attr.rs::get_next_id` against `bpf/cmd/next_id.rs::admit`: same four decisions, written twice. | NEXT |
| OPEN | INFRA | low | `bpf/link.rs` exports `cgroup_link_by_id`, which is now a one-line alias for the general `link_by_id` — the cgroup ordering anchors re-check the link kind themselves. The name says "cgroup" but the lookup is over the one link registry. Renaming it means touching `bpf/prog/attach.rs`, outside this lane's ownership. | `bpf/link.rs`: `pub(crate) fn cgroup_link_by_id(id: u32) -> Result<InodeRef, Errno> { link_by_id(id) }`. | NEXT |
| OPEN | COVERAGE | med | The batch commands' loop bodies (`lookup_batch`, `update_batch`, `delete_batch` in `bpf/cmd/batch.rs`) have no hosted test that drives them against a real map: they need a live `BpfMapInode`, which needs `sched::current()` for the descriptor resolution the command starts from. Their admission ladder, access modes, flag masks, address arithmetic and count write-back are all covered; the per-element walk, the skip-on-racing-delete and the end-of-map `ENOENT` are not. Needs the map-object half of the hosted fixture the element tests use. | `bpf::command::batch::tests` covers 8 decisions; `grep -c 'fn lookup_batch'` shows the walk itself has none. | NEXT |
| OPEN | DEFECT | High | The bpf verifier/command work pushes an aarch64 static call path to or past the 13000 B ceiling on a 16384 B kernel stack. The branch was pushed with `SKIP_STACK_GATE=1` on an explicit instruction to land the work; **the overrun ships with it.** A kernel stack overrun does not fault cleanly — it scribbles the adjacent allocation, and this tree has already lost multiple sessions to exactly that class (the ~90% boot corruption was a 16 KB stack overflowing into the heap, chased for weeks as a refcount UAF before guard pages proved it). | `make stack-gate` on `B1977-bpf-verifier-and-commands` reports `>= ceiling (13000B): 4`. Deepest chain at the time of the bypass: `_start` 19832 → `_start_rust` 19816 → `kernel_main` 19752 → `rootfs::init` 16552 → `smoke::elf::run_as_task` 12184 → `oxide_syscall_entry` 12064 → `oxide_syscall_dispatch` 11880. Three of the four over-ceiling paths are pre-existing allowlist entries; this lane's contribution is the fourth and is NOT allowlisted. | unowned |
| OPEN | COVERAGE | high | 433 unguarded candidates, 59 of them bare serialisation locks — `B1957` predicted 25 from a narrower grep, and every one of those 25 is in this list. Each is a fixture lock whose protection is a convention any new test in the same binary can forget. | `tools/hosted-global-state-backlog.tsv`, one row per candidate, sortable by claim ratio. The worst by exposure: `net/src/net_ns/test_support.rs::LIFETIME_LOCK` (17 of 2186 tests in the binary claim it), `sched/src/tests/timing.rs::SERIAL` (1 of 1210), `fs/src/timerfd/tests.rs::TEST_LOCK` (10 of 1002), `syscalls/src/fcntl_dup_tests.rs::TEST_LOCK` (2 of 1329). | unassigned — one row per lane |
| OPEN | COVERAGE | med | `crng/src/pool/tests.rs::SOURCE_LOCK` states the requirement in its own doc comment — "`BULK_SOURCE` and `SEEDED` are process-global, so every test that installs a source or inspects readiness must run alone" — and 12 of the crate's 17 tests call `fill()`, which reads both, without it. Exactly the `B1957` shape: the lock protects the fixture, not the state the fixture installs into. | Flagged `UNGUARDED fixture-lock crng/src/pool/tests.rs:22 SOURCE_LOCK [5/17 tests claim]`. Not observed to fail — structural, not measured. | unassigned |
| OPEN | COVERAGE | med | `modules/src/test_serial.rs::MODULES` (217/231), `drv/src/model/test_claim.rs::MODEL` (38/40), `fbdev/src/test_claim.rs::FBDEV` (27/30), `input/src/tests.rs::TEST_MUTEX` (18/22), `ucounts/src/tests.rs::LOCK` (11/12): near-total conventions, which is the state `socket` was in before the 18 non-claiming tests were noticed. A convention at 94 % is one added test away from the flake. | Same file; claim ratios above. | unassigned |
| OPEN | COVERAGE | med | 65 `singleton-pin` rows: hosted fixtures pinning a process-global singleton through a parameterless accessor (`vfs::initial`, `drv::devices`, `network-namespace::initial`, `devfs::clear_hwrng_source`, …). `B1957` names de-pinning as the only lever that removes the class rather than guarding it — these rows are the inventory for that work. | `tools/hosted-global-state-backlog.tsv`, rule `singleton-pin`. | unassigned |
| OPEN | COVERAGE | med | No gate prevents a new external-source citation from landing. This rule has now cost four lanes time (B1949, B1955, B1956, C295). | See "Recurrence" below. | unowned |
| OPEN | DEFECT | med | Load average counts only runnable tasks; the reference also counts uninterruptible-sleep tasks, so loadavg reads low under I/O-wait-heavy load. | `crates/kernel/sched/src/loadavg.rs:5-6` | unowned |
| OPEN | MISSING | med | `RLIMIT_SIGPENDING` charging is only the decision half; queues are bounded by a fixed `RT_QUEUE_CAP` in the interim. The comment frames this as deferred, which Discipline rule 3 forbids. | `crates/kernel/sched/src/rlimit/pending.rs:9-16` | unowned |
