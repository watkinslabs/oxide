# Known issues

Running ledger of every known breakage, divergence, and open question. Any lane
that finds one adds a row in the same PR that finds it; any lane that fixes one
flips the row to `FIXED` with the SHA and MOVES it to `scratch/fixed-issues.md`
in the same PR. Rows are never deleted, only relocated.

Status: `OPEN` | `IN-PROGRESS <branch>` | `FIXED <sha>`.
Severity: `blocker` (merge gate) | `high` (wrong answer reaching userspace) |
`med` (missing surface) | `low` (hygiene, tooling, cosmetics).

Never delete a row to make the list look shorter. A row with no owner is still
a row.

## Tooling / gates

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| NOT-A-DEFECT | info | **`extern crate std` is not a charter violation anywhere in the tree — do not re-open on a raw grep count.** 73 sites tree-wide; 19 survive a naive "is the preceding line `#[cfg(test)]`" filter, and all 19 are gated somewhere a line-local grep cannot see. `07§5`'s "`extern crate std` in any kernel binary → fail" is a property of the KERNEL BUILD, and none of these reach it. | The 19: `crates/kernel/ext4/src/lib.rs:22` is `#[cfg(not(target_os = "oxide-kernel"))]`; `crates/shared/pkey/src/lib.rs:27` and `crates/shared/mpi/src/lib.rs:24` are `#[cfg(any(test, feature = "hosted"))]`; 6 × `crates/shared/zstd/src/*.rs`, 5 × `crates/kernel/sched/src/**/tests*.rs`, `crates/kernel/pidfd/src/tests.rs` and `crates/kernel/syscalls/src/auxrandom.rs` sit inside an enclosing `#[cfg(test)] mod`; `crates/kernel/sysfs/src/bus/test_harness.rs` is gated at its `mod` declaration (`bus.rs:22`); `crates/kernel/conformance/src/corpus.rs` is a hosted-only `[dev-dependencies]` harness under the `docs/02` dev-tool carve-out, documented in its own crate doc-comment. Both kernel targets build. | — |
| NOT-A-DEFECT | info | **`panic!(fmt)` is not an unenforced rule — the 113 is a raw-grep number, not a findings count.** 270 `panic!(` sites tree-wide, 113 carrying a `{` placeholder, and `code/panic-fmt` reports **0**. That zero is deliberate, not a hole: `check_panic_fmt` skips off-kernel lines, because every `assert_eq!` in host test code expands to precisely the formatted panic the rule forbids — enforcing it there flagged 110 of the 113 for a rule the `assert!` on the adjacent line breaks anyway. | `tools/spec-lint/src/code_lint.rs` `check_panic_fmt` + its doc comment, which records the 110-of-113 measurement. Spot-checked the survivors that look ungated: `crates/kernel/devfs/src/lib.rs:267` `panic!("{} minted", path)` is inside the `#[cfg(test)] mod` opened at line 218. `cargo run -p spec-lint -- code` → `code/panic-fmt` count 0. | — |
| OPEN | low | Both rows above were reported upstream as violations at scale before anyone ran the gate: a raw `grep -c` was quoted as if it were a findings count. Two such counts were passed along in one session. A grep that does not filter the way the linter filters produces a number that is wrong in the alarming direction, and the cost is a lane opened to fix a non-defect. | This pair. The countermeasure is `make audit-counts` (C-lane, next PR), which reports the FILTERED number for both rules so an audit reads the same figure the gate enforces. | — |
| OPEN | low | Source-grep assertions in `socket_control_tests.rs` / `dispatch.rs` (and `net_common.rs`, `socket_fd.rs`, `send_user/tests.rs`, `recvmsg/vsock_shutdown_tests.rs`, `fcntl_dup_tests.rs`, `poll_ownership_tests.rs`, `getdents_debug_tests.rs`, `sock/packet_options.rs`) fail hard whenever the text they grep moves. Already produced one false "flake" and one real merge break. Convert to behavioural assertions. | B1641 hit this twice. C245 converted the name-query + socketpair cluster: 9 grep tests deleted, 20 behavioural tests added against the new ungated owners `syscalls::sock_name`, `syscalls::socketpair_spec`, `net::sock_v6_name`; 4 positive controls confirmed each new assertion goes red when the behaviour is broken. ~17 grep tests remain in the two named files and ~50 `include_str!` sites elsewhere — C246 continues. | C246 |
| OPEN | low | Citation debt: repository text must not name/path-link/quote external implementation sources. 273 files carry `.c:NNN`, 113 mention `include/uapi`, 50 carry `.h:NNN`, 10 name the local reference tree by path. | Pre-existing tree-wide; B1641 added zero. | — |
| FIXED C247 | low | `metadata/index.md` counters go stale and collide — index read `D 424` while `D432` was already merged. | `tools/next-branch.sh` derives the number from git refs + merge subjects and maxes against the table; `--check` fails when the table is behind, wired into `make ci` as `counter-check`. Positive control: the check reported `C247 already exists in git` before the table was bumped. | C247 |
| OPEN | low | `debug-atexit` is excluded from the routine gate: it is mutually exclusive with `debug-stderr` by design (`020_writev` selects the richer `[DYNERR]` tracer when atexit is on), so enabling both hides the `debug-stderr` writev block from the type checker. Covered by the separate non-routine `make feature-gate-atexit`. | Cfg is `all(feature = "debug-stderr", not(feature = "debug-atexit"))`. | — |
| OPEN | low | `security_watch_key` / `security_post_notification` have no LSM hook here, so watching is gated by `KEY_NEED_VIEW` alone and a record is never withheld from a watcher whose credentials an LSM would reject. Matches a kernel with no LSM stacked, which is what this one is. | B1659, `ops/watch.rs::watch_key_core`. | — |
| OPEN (narrowed) | med | **Serial tty echo duplicates a character periodically** (`grep -E` → `grep --E`, `env` → `ennv`, `timeout 60` → `timeout 660`). NOT in the line discipline or the staging ring: six new byte-exact tests drive the real RX path (`insert_flip` → `flush_to_ldisc` → N_TTY echo → driver) over 4000-byte streams, across every flush-chunk boundary, with interleaved staging, at ring saturation, and against a concurrent writer, and every one asserts exactly one echoed byte per typed byte. Since the DELIVERED bytes are correct while the ECHOED bytes gain a copy, the extra copy enters strictly downstream of the ldisc's read queue — i.e. in the UART TX path. | `cargo test -p tty --lib echo_stream` (6 tests, B1669). Earlier `flip/tests.rs::a_byte_is_delivered_exactly_once` already excluded the ring. | — |
| OPEN | low | `hosted-gate` checks lib/bin targets only, not test targets (`--all-targets` costs 43 s warm against 12 s, too heavy for every push). A test target that compiles only under `--features hosted` would still pass the gate; `make test` covers that case with the feature ON, so a default-feature-only break in a test target is uncovered. | Measured on 48 cores: `--workspace --all-targets` 91 s cold / 43 s warm; per-crate isolated sweep 4.6 s no-op / 12 s after touching `net`. | — |
| OPEN | med | `cargo test --workspace --no-fail-fast` on `main` (`460dc8e89`) is NOT green and its failing set is UNSTABLE run to run — same class as this row (global process state shared by parallel tests), none of them owned here. Run 1 failed `drv-virtio-input` (`devfs::tests::lifetime::{reused_event_number_cannot_retarget_old_open_file, state_reconciliation_flushes_only_querying_client}`, both at `lifetime.rs:24` — a `publish_endpoint` collision on fixed event ids), `fbcon` (`kernel::tests::try_lock_vt_excludes_the_flush_softirq`), `softirq` (`tests::raise_then_run_invokes_handler`), plus `pmm`/`socket`. Run 2 (this branch) failed a DIFFERENT `fbcon` test (`tests::one_line_of_output_damages_only_that_text_row`) and a `net` test that was green in run 1 (`stack::ethernet::tests::ethernet_ingress_rejects_a_truncated_link_header_before_l3`, `Option::unwrap()` on None at `net/src/stack/core.rs:193`), while `drv-virtio-input` and `softirq` passed. So per-package green does not imply workspace green, and one workspace run does not characterize the failing set. | Two full runs captured; failing package lists differ (`drv-virtio-input,fbcon,pmm,socket,softirq` vs `fbcon,net,socket`). | — |
| OPEN | low | Nothing prevents a recurrence: any test that calls `drv_zram::install_page_provider` directly re-creates the same second-provider conflict, and there is no gate that a package's test outcome is invariant to which other packages share the cargo invocation. A workspace-wide `cargo test --workspace` in CI would catch it; per-package runs do not. | This defect survived because the only run that exposes it is the workspace one. | — |
| OPEN | med | `drv-virtio-input` devfs lifetime tests are a parallel global-state race: `devfs::tests::lifetime::state_reconciliation_flushes_only_querying_client` and `..::reused_event_number_cannot_retarget_old_open_file` fail nondeterministically, 1-7 failures per run on identical trees. Same defect class as the `net` parallel-execution row, different crate, and it makes `xtask test` exit 101 on a clean `main`. | B1676 measured it: clean `origin/main` 5 runs = 1 failure each; B1676 branch 5 runs = 1, 1, 1, 7, 2 failures. BOTH trees pass 114/114 with `--test-threads=1`, which isolates it to shared state rather than any code change. | — |
| OPEN | low | `/tmp` on this box is a 32 GiB tmpfs and a cargo build under it fills the whole thing (`rustc-LLVM ERROR: IO failure on output stream: No space left on device`), which fails unrelated lanes. Temporary worktrees used for baselining must live under `/home`, not the session scratchpad. | B1676 hit 100% `/tmp` with one baseline worktree (11 GiB of `target/`); removing it restored the box. `/home` had 1.6 TiB free at the time. | — |
| NOT-A-DEFECT B1677 | med | Curated `## Desktop` row "`polkitd` spins re-issuing syscall 434 (`pidfd_open`) and taking EINVAL every ~200-400 ms" is closed — the argument is `pid = 0`, and the reference rejects a non-positive pid with EINVAL before any lookup, exactly as we do. The caller is not retrying because an earlier answer was wrong: its own `set_pid` path opens the descriptor from the object's *stored* pid field rather than the pid being assigned, so on a freshly constructed subject that field is still 0. EINVAL is the branch that makes it fall back to storing the numeric pid, which is why the sequence repeats per request and is non-fatal. No flag, leader, or exit-race component: the flags word is 0 and no lookup is reached. | B1663 boot log, every line identical: `[EINVAL nr=434 tid=4322 a0=0 a1=0 ... comm=polkitd]` (56x) and the same shape twice from `gnome-shell`; `a0` is `pid`, `a1` is `flags`. Guest `libpolkit-gobject-1.so.0.0.0` disassembly, `polkit_unix_process_set_pid`: the `syscall(434, ...)` site loads its pid argument from the object field, not from the incoming parameter. Admission now pinned by `pidfd::admit::tests` (4 tests). | — |
| OPEN | low | `crates/kernel/pidfd/src/tests.rs` (6 hosted tests) **does not compile** — two `E0308`/`E0432` errors against the current `Task::replace_namespace` signature. Nothing in the default workspace gate enables `pidfd/hosted`, so the breakage is invisible: `cargo test -p pidfd --features hosted` fails to build while `cargo test --workspace` reports success. Same class as the phantom-test rule, one level up: the tests are real, the feature that compiles them is never turned on. Left to the lane that owns the hosted-feature gate rather than duplicated here. | `cargo test -p pidfd --features hosted` → `error[E0308]: mismatched types ... expected NamespaceRef, found Arc<_, _>` at `tests.rs:20`. | `B1674-hosted-check-gate` |
| NOT-A-DEFECT B1678 | med | Curated `## Desktop` row "`fsconfig(FSCONFIG_SET_FD)` returns EINVAL to systemd's `(sd-mkdcreds)` on an `anon_inode:[fscontext]` fd, and the following `mount_setattr` also returns EINVAL" is closed — **both calls are feature probes whose CORRECT answer is EINVAL**, and we give it. (1) The `fsconfig` call is the "has this filesystem been converted to the new mount API" probe: `FSCONFIG_SET_FD` with a deliberately nonexistent option name, no value, and the context's own descriptor as the auxiliary fd. EOPNOTSUPP would mean "unconverted", EINVAL means "converted, option unknown" — the answer the prober wants. (2) The `mount_setattr` call is the "is the new mount API present" probe, `mount_setattr(-EBADF, NULL, 0, NULL, 0)`; `usize = 0` is below the version-0 block size, which the reference rejects with EINVAL before the capability gate, and the prober reads any non-`ENOSYS`, non-success answer as "present". Our size ladder, ordering, no-op short circuit and attribute validation all match. | B1663 boot log: `[EINVAL nr=431 ... a1=5 a4=<same fd as a0> fdpath=anon_inode:[fscontext]]` from `(sd-mkdcreds)`, `(-userdbd)`, `(ostnamed)` and `systemd`, each immediately followed by `[EINVAL nr=442 a0=0xfffffff7 a1=0 a2=0 a3=0 a4=0]` (`0xfffffff7` = `-EBADF`). Probe call sites read in the guest's own systemd 259 sources (`mount_option_supported`, `mount_new_api_supported`). Pinned by `syscalls::fsconfig_abi::tests::the_converted_filesystem_probe_clears_admission_and_is_not_eopnotsupp` and `syscalls::mount_attr_abi::tests` (8 tests). | — |
| OPEN | low | Huge-page and directly-addressable-persistent-memory mappings do not exist in this kernel, so the four filter bits covering them can never fire from a live mapping. The ladder implements all four; the adapter reports both properties as absent. | `describe_vma` sets `dax: false` and `hugetlb: false` unconditionally; the ladder rungs are covered by `hugetlb_bits_gate_huge_mappings_by_sharing` and `dax_bits_gate_persistent_memory_mappings_by_sharing`. | — |
| OPEN | low | Pre-existing, unrelated to this lane: two `/proc/<pid>` inode-number tags collide — the projected-id map file and the personality file both use tag `0x2e`, so the two files of one process share an inode number. | `procfs/src/userns_idmap.rs` `PID_INO_TAG_PROJID_MAP = 0x2e` vs `procfs/src/live/pid_files.rs` `pid_gated_ctor!(make_pid_personality, …, 0x2e, …)`. | — |
| OPEN | med | Duplicate lane: C246 independently widened `make feature-gate` from `debug-all` (14 of 87 `debug-*` features) to the full derived list, and fixed the five feature-gated blocks that had rotted behind the hole — `hal::zerotrap`'s missing `PAGE_SIZE_BYTES` import, seven `Task::name` reads left over from when it was a plain array, `smoke::memtest::run`'s pre-IrqGate signature, and an aarch64-unguarded import of x86-only statics in `mm-pmm`. B1671 landed the identical fixes first; C246's commit was dropped in full at rebase. Wasted lane. | Root cause: the claim-work greps in CLAUDE.md were run for the grep-assertion ledger row this lane owns, but NOT re-run when the work grew a second front (the build gate) mid-lane. A lane that changes shape needs a fresh claim check for the new area, not just the one it started with. | — |
| OPEN | low | `fs/tests/keyring_procfs.rs::a_new_key_appears_in_the_proc_keys_body_procfs_renders` failed ONCE (assertion at line 37) in a combined `-p ... -p net -p fs` run on base `4a110da60`, and did not reproduce on later bases in a 6257-test full run. NOT attributed: it may be the known net global-state test race, or base-dependent. Recorded so the next observer has a prior rather than treating it as new. | F779, one observation, not reproduced. An observation, not a diagnosis. | — |
| OPEN | med | `process_madvise` no longer carries its own permission check beyond `mm_access(PTRACE_MODE_READ_FSCREDS)` + `CAP_SYS_NICE`, and `process_mrelease` now has NO permission check at all — the pidfd is the whole authority, matching upstream, where the previous `sig_perm_check` EPERM had no counterpart. This is deliberate Linux parity and is the reason `task_will_free_mem` has to be exact: the "is the target really dying" ladder is the only thing standing between a pidfd holder and another process's pages. Recorded so a future audit does not read the removed check as an accident. | F780; upstream `SYSCALL_DEFINE2(process_mrelease)` has no permission gate. | — |
| OPEN | MED | `/tmp` on the dev box is a 32 GiB tmpfs and was 100% full during this lane (sibling build in a tmpfs scratchpad), which makes every `Bash` call fail with ENOSPC on output capture. Not a kernel defect; it blocks boot smokes. | `df -h /tmp` → `32G 32G 3.3M 100%` | infra |
| OPEN | MED | `cargo test -p drv` is flaky under parallel test threads: the driver-model publish/bind hooks are process-global `fn` pointers that concurrent tests overwrite, so up to 20 of 40 tests fail depending on interleaving. Reproduces on `main` (1 of 3 runs red) and disappears with `--test-threads=1`. Pre-existing, not this lane's change. | `cargo test -p drv` on `main` → `38 passed; 2 failed`; `cargo test -p drv -- --test-threads=1` → 40 passed, 3/3 | unowned |
| OPEN | med | `cargo test -p kalloc --features debug-heappoison,debug-dealloc-diag,...` aborts with a host **stack overflow** (`fatal runtime error: stack overflow`) before finishing the 19 tests — `poison::Quar` and the size ledger are large enough that a `KAlloc` built on the test thread's stack blows it. Identical on `origin/main`, so pre-existing, not caused by the split — but it means the whole diagnostic-feature configuration of kalloc has **no** hosted test coverage. | B1690. Base worktree at `origin/main` (2ca827e72) and this branch both abort with SIGABRT on the same command; default-feature run is 18/18 green on both. | — |
| OPEN | low | `cargo test -p fs --test sys_pipe2_shape::pollout_tracks_current_pipe_capacity` fails on `origin/main` — `make test` stops there, so the workspace suite cannot go green today. | B1690. Reproduced identically in a detached `origin/main` worktree and on this branch; failure set of `cargo test --workspace --no-fail-fast` on this branch (`fs`) is a subset of base's (`fs`, `drv`). | — |
| OPEN | low | `drv-zram::tests::control_reuses_removed_index` and `drv --lib` fail only under the parallel full-workspace run; `cargo test -p drv-zram` alone is 104/104 green 3/3 runs. Shared global driver-registry state makes these order-dependent, so any lane reading a full `make test` can be handed a failure that is not theirs. | B1690. Branch: FAILED under `make test`, then 3/3 PASS in isolation and PASS with `--test-threads=1`; base `origin/main` shows the same `drv --lib` failure under `cargo test --workspace`. | — |
| OPEN | low | `/tmp` (32 GiB tmpfs) reached 100% mid-session and every `Bash` call failed with ENOSPC until space was freed. Multi-GiB `*_repro.img` boot images from several lanes accumulate there and nothing reaps them; `tools/boot-smoke.sh` hardcodes `/tmp` for its log/pidfile and has no fallback. | B1690. `df -h /tmp` read `32G 32G 4.1M 100%`; `du -sh /tmp/*` showed four 5+ GiB repro images plus 6.7 GiB under the session scratchpad. Freed by deleting two stale multi-GiB images from this session's own scratchpad. | — |
| OPEN | low | The reaper walks `/proc/[0-9]*/fd/*` rather than using `lsof`, to avoid a tool dependency in the boot gate. On a box with thousands of processes that walk is not free; it runs once per attempt, so the cost is bounded, but a very busy runner may notice. | C249. | — |
| OPEN | low | A naive `grep -c` of backticked statuses in `scratch/syscall-compliance-matrix.md` over-counts by exactly the legend rows (291/23/18/58 vs the true 290/22/17/56), because the legend table uses the same `\| \`IMPL\` \|` shape as a data row. Any lane quoting matrix counts from a grep has quoted numbers that are wrong by one per status. | C251. Measured both ways on the same file: grep total 395 vs 385 real rows. `xtask stats` now shells out to `tools/matrix-lint.py --counts`, which reuses the gate's own escape-aware parser and reports `MALFORMED` rather than silently dropping a row it cannot read. | — |
| OPEN | low | `OXIDE_SWAPFILE_SMOKE` is set by nothing — no Makefile target, no script, no CI. The ext4 swapfile + memcg pageout probe therefore cannot run, unlike its five sibling injectors which all have `make smoke-*` targets. Kept and ported rather than deleted (it tests real kernel behaviour), but it is dead until something invokes it. | C252. Whole-tree grep for `OXIDE_SWAPFILE_SMOKE`: one hit, the reader in `rootfs_disks.rs`. Compare `OXIDE_DRM_RENDER_SMOKE` → `make smoke-drm-render` (`Makefile:388`). | — |
| OPEN | MED | **Driver multidev/rebind smoke coverage is now zero and is recorded as gone, not fixed.** B570–B599 driver-path proofs (sysblock `/sys/dev` reverse index, sysbus bind, virtio net/blk/gpu/snd multidev, rng/uart/ps2/parent-child rebind, shutdown quiesce, vsock round-trip, msix net rx, userspace seat) had a live in-guest assertion until `7aacadf4c`; it has been zero since. Rebuilding it needs a probe-staging path in `../images` (or a `rootfs_disks/` injector per probe), which is out of scope here. The QEMU device topologies those smokes used are KEPT (`image_qemu/{x86_64,aarch64}.rs`, `OXIDE_VIRTIO_*_MULTIDEV_SMOKE` / `OXIDE_STORAGE_MULTICTRL_SMOKE`), so `OXIDE_VIRTIO_NET_MULTIDEV_SMOKE=1 make qemu-x86` still gives the multi-device machine and the driver klog. | deleted: `boot-smoke-{driver-path,msix-net-rx,ps2-rebind,shutdown,storage-multictrl,sysblock,sysbus-bind,uart-rebind,userspace-seat,virtio-blk-multidev,virtio-gpu-multidev,virtio-net-multidev,virtio-parent-child-rebind,virtio-rng-rebind,virtio-snd-multidev,vsock}.sh` | unowned |
| OPEN | LOW | `tools/boot-smoke-fs.sh` and `tools/boot-smoke-kbd-login.sh` work (serial-driven, no C probe) but had no Makefile target, so nothing could run them. Given targets in this lane: `make smoke-fs-{x86,arm}`, `make smoke-kbd-login-{x86,arm}`. | `Makefile` | C254 |
| OPEN | LOW | `tools/xtask/src/rootfs_disks/{request_key,swapfile,gnome_input_classify}.rs` are working injectors with no script and no Makefile target — reachable only by hand (`OXIDE_SWAPFILE_SMOKE=1 make qemu-x86` and read serial). Kept because the injector works; noted because they are not gated by anything. | `rootfs_disks.rs:92-101` | unowned |
| OPEN | MED | The 218 `t_*.c` conformance programs stay C and are NOT part of the Rust probe rewrite. They are the live N22 differential corpus, and their value comes from being compiled identically for host and guest — rewriting them in Rust would be ~9k LOC of translation for no additional test signal, and would risk silently changing an oracle. Recorded so the "no C in this repo" goal is not assumed complete. | `userspace/glibc_conformance/` 8,988 LOC after `t_crypto` removal | unowned |
| OPEN | med | `docs/00-master-plan.md:260`, `docs/43-acceptance.md:76` and `docs/40-ci.md:46` all make a RELEASE GATE out of "Kernel-completeness audit `docs/kernel-audit.md` shows no stub regressions" — and that file was deleted in `3643d52c2` (July 2026). Three release gates cite a document that does not exist, so the gate cannot be evaluated and has silently been vacuous. spec-lint does not check file-path citations inside prose, only `<doc>§<sec>` refs, which is why it never surfaced. | `git log --diff-filter=D -- docs/kernel-audit.md`. Not fixed here: choosing the replacement gate (or dropping it) is a spec decision, not a lint lane's call. | — |
| OPEN | low | `code/klog-ungated` treats `klog::kfatal!` and `klog::kerror!` as requiring a `debug-<sub>` feature gate, the same as `kdebug!`. A fatal or error log that only exists in a debug build is not a diagnostic anyone will have when it matters, and it makes "log the detail, then abort with a literal" — the natural fix for `code/panic-fmt` — cost a new finding. | `tools/spec-lint/src/code_lint/klog.rs:74-80` lists all five macros in one `MAC_NAMES` table. Worth deciding before the 625-finding `klog-ungated` backlog is worked. | — |
| `code/klog-ungated` | 625 | 625 |
| OPEN | LOW | Build time is NOT the win and should not be reported as one. Cold `cargo clean && cargo build --workspace`: **17.7 s → 15.0 s** wall (1m39 → 1m25 user, 24 jobs). ~15%. The win is 55,868 LOC leaving the tree and an unconfounded conformance channel (C256), not seconds. | measured in this worktree, both runs from an empty `target/` | C258 |
| OPEN | low | The `memfd_create` matrix row's notes column had been column-broken since B1542 by unescaped `|` characters, splitting the notes across fields 13-15. Status (`$9`) and branch (`$10`) still parsed, so tooling did not notice, but the notes read as truncated. Rejoined in F783. Other rows may carry the same defect — a matrix lint for `NF != 14` would catch it. | F783; the row had 16 pipe-separated fields. | — |
| OPEN | med | Boot smoke NOT taken. Both arches build clean and every decision is hosted-tested, but the `TCR2_EL1`/`CPACR_EL1` writes and the per-switch `POR_EL0` read are exactly what a build cannot prove — and `TCR2_EL1` is a register this kernel has never touched before, on a QEMU CPU model that may not implement FEAT_S1POE at all (in which case `setup_poe` correctly does nothing, which is itself worth confirming). Must boot both arches before merge. | F786; box was under heavy concurrent load throughout the lane. | — |
| OPEN | med | 9 rows are `IMPL` while their own Evidence discloses a divergence (`76:truncate`, `78:getdents`, `101:ptrace`, `135:personality`, `140:getpriority`, `141:setpriority`, `166:umount2`, `217:getdents64`, `430:fsopen`). Row 76's notes even say "DOWNGRADED IMPL->PARTIAL (B1441)" while the status column still reads `IMPL`. The check that catches these already existed but had never run. They are recorded in `tools/matrix-overstated-allow.txt` with a quote each, NOT bulk-flipped: a row's status is its owning lane's claim and F789 audited none of them. The gate fails on any NEW violation, and an entry leaves the file when the owning lane resolves the row. | F789; `make matrix-gate` with the allowlist removed lists all 9 with the offending phrase. | owning lanes (F726, F736, B1638, B1650, B713, F730-ish) |
| OPEN | med | **`napi::skb_page_frag_refill` leaks on every call and refills nothing.** It calls `__napi_alloc_frag_align(sz, 0)` → `alloc_zeroed`, binds the result, null-tests it, and drops the pointer. The `page_frag` argument it is meant to populate is only null-checked, never written. Every invocation leaks `sz` bytes and returns `true`. | `crates/kernel/modules/src/linux_netdev/napi.rs`, `skb_page_frag_refill` body: `p` has no other use, and there is no `dealloc` or store into `page_frag`. The unsafe block itself is sound (no caller pointer is dereferenced), so it was commented honestly and left. | unclaimed |
| OPEN | low | **`linux_sync_wait::reset_wait_cells` (`#[cfg(test)]`) clears `CELLS`,** contradicting the "cells are never removed, so the returned reference is stable" invariant `wait_cell` relies on. Not exploitable today — the parking path is `target_os = "oxide-kernel"`-gated and dead in hosted tests — but a future hosted test holding a `&'static WaitCell` across a reset would be a UAF. | `crates/kernel/modules/src/linux_sync_wait.rs`. | unclaimed |
| OPEN | low | **Pre-existing SAFETY boilerplate that passes the 30-char lint while saying nothing.** `linux_pm/core.rs` has ~15 copies of `// SAFETY: dev points at a caller-owned Linux struct device.`; `linux_platform/core.rs` has `// SAFETY: driver/dev are validated before matching.` and `// SAFETY: callers pass valid NUL-terminated Linux strings.`. These are the laundering pattern the rule exists to prevent — the lint cannot distinguish them from a real argument. Only the blocks the lint actually flagged were in this lane's worklist. | `grep -rn "// SAFETY: dev points at a caller-owned" crates/kernel/modules/`. | unclaimed |
| OPEN | low | **`spec-lint` only scans 4 lines back for the `// SAFETY:` marker** (`tools/spec-lint/src/code_lint.rs:298`), so a multi-line SAFETY comment longer than four lines silently fails to satisfy it, and a genuine argument can be rejected for being thorough. | Observed while writing multi-line comments on this branch. | unclaimed |
| OPEN | low | **`make lint-ratchet-update` deliberately NOT run.** `--update` rewrites the baseline for *every* key from this tree, so on a branch cut before other lanes' reductions merge it would raise their keys back up and weaken their gate. The ratchet passes 381 below baseline either way; tightening belongs to a lane cut from a current `origin/main`. | `make lint-ratchet` → `PASS — 381 finding(s) below baseline`. | unclaimed |
| OPEN | LOW | virtio-gpu runtime `submit_ctrl_for_key` / `present_rect_for_key` treat a submit timeout as a plain `false` and go on to reuse the SAME persistent ctx command frame for the next command while the device may still own the previous descriptor. Not memory-unsafe (the ctx frame is never freed on these paths, and teardown now gates on a confirmed reset) but a device that stalls once can have its late reply read as the next command's status. | `runtime.rs::submit_ctrl_for_key` returns `false` without poisoning the ctx | unowned |
| OPEN | HIGH | 37 further raw user-pointer dereferences remain in the DRM crate outside this lane's lint worklist — `modeset.rs` (11), `node.rs` (9), `kms_ext.rs` (8, the non-cursor ioctls), `atomic/blobs.rs` (3), `atomic/props.rs` (2), `crtc/handlers.rs` (2), `node/virtgpu.rs` (1), `node/client_caps.rs` (1). Same defect as the row above; each is a kernel halt on a bad user pointer. `drm::uarg::read_arg`/`write_arg` is now in place, so the conversion is mechanical, but it touches the compositor hot path and belongs on its own branch with its own both-arch boot. | `grep -c 'read_volatile(arg\|write_volatile(arg' crates/drivers/drm/src/**/*.rs`; drm had ZERO uses of `uaccess` before this lane | unowned |
| OPEN | MED | The ring-pointer bounds argument in EVERY virtio driver now rests on an invariant owned by another crate: one frame per ring area and `queue_size <= 256` (`virtio::queue_cfg::MAX_QUEUE_SIZE`). Raising that constant would silently make each `avail.add(2 + slot)` and `used.add(...)` run off its frame across blk/gpu/rng/vsock/snd/net. No test ties the two together; the SAFETY comments this lane wrote all cite it by name so the coupling is at least visible. | `virtio::queue_cfg::tests::oversized_device_queue_size_is_negotiated_down_to_one_frame` asserts `MAX_QUEUE_SIZE * VRING_DESC_BYTES <= QUEUE_FRAME_BYTES` but nothing checks the consumers | unowned |
| OPEN | HIGH | There is NO hosted coverage of the kernel-only fault-recovery half of `uaccess` — `hal-x86_64/src/uaccess.rs` gates the exception-table asm on `target_os = "oxide-kernel"`, and the hosted shim is a plain `copy_nonoverlapping`. A future lane re-introducing a raw user dereference in a driver passes every hosted gate, exactly as drm and virtio-input did until this lane. A gate that greps driver crates for `read_volatile`/`write_volatile` on a user-supplied address would catch the class. | This lane found the defect in 2 of 13 driver crates by reading, not by any gate firing | unowned |
| OPEN | low | `spec-lint`'s `code/safety-*` rule only scans the 4 lines immediately preceding `unsafe {`, so a block whose SAFETY marker heads a longer comment, or is separated from the block by one declaration, reports as `safety-missing` even though it carries a full audited rationale | `tools/spec-lint/src/code_lint.rs:298` `for back in 1..=4`. 12 of the 147 in-scope findings were this false positive (e.g. `hal-x86_64/src/context.rs` context-switch clobber rationale, 1165 chars). Worked around here by making the SAFETY statement the last line before each block; the linter itself is another lane's file and was not changed. | unassigned |
| OPEN | low | `crates/kernel/net` still holds the last 3 `code/safety-missing` findings (`rtnl.rs:35`, `sock_io.rs:220`, `unix_sock/listener.rs:309`). Not taken by this lane: several `net` lanes were active concurrently and a parallel edit there would conflict. | `cargo run -q -p spec-lint \| grep code/safety-` → 3 rows, all under `crates/kernel/net`. Repo-wide count was 16 before this PR. | unclaimed |
| INFO | info | Audit result: all 13 blocks this lane documented are **sound**. No unsound `unsafe` was found, so no bug row is filed. Each rests on a named invariant — see below. | Per-site prose in the diff; both arches build, `cargo test` green on every touched crate. | B1695 |
| INFO | info | spec-lint's `safety-missing` check only looks at the line immediately preceding `unsafe {`. A correct SAFETY comment above a multi-line `let` binding whose `unsafe` lands on a later line still reports missing. Cost one edit round-trip here (`vt/src/state.rs` `owner_alive`, worked around with a local type alias to keep the block on one line). | Comment at `state.rs:85-87` with `unsafe` at `:88` reported `code/safety-missing`; collapsing the binding cleared it. | unclaimed |
| OPEN | med | **17 of 22 registered filesystems publish no parameter table, so their options are still accepted-and-ignored.** `devpts`, `cgroup2`, `sysfs`, `debugfs`, `tracefs`, `configfs`, `securityfs`, `efivarfs`, `pstore`, `bpf`, `fusectl`, `mqueue`, `hugetlbfs`, `binfmt_misc`, `devtmpfs` take `None` — their constructors discard the data string entirely. `devpts -o gid=5,mode=620,ptmxmode=000` and `cgroup2 -o nsdelegate,memory_recursiveprot` are passed on every boot and do nothing. Left as `None` on purpose: a table for these would reject nothing they are given today, but declaring an option surface none of them implements would be a second lie. Each becomes a table when its enforcement lands. | `crates/kernel/syscalls/src/fsmount_common/registry.rs` `register_filesystems` | — |
| OPEN | low | **`mount -t proc -o hidepid=…` / `-o subset=…` now returns EINVAL.** procfs declares `Some(&[])` (B1686), and that declaration now binds `mount(2)` as well as `fsconfig(2)`. This is the intended fail-closed direction — a caller told "yes" while getting no confinement is worse off than one told "no" — and userspace that probes with `fsconfig` before mounting already saw the same answer. Recorded because it is a mount that used to succeed. | `registry.rs` proc registration; `crates/kernel/syscalls/tests/mount_param_admission_hosted.rs` | — |
| OPEN | med | A `PT_LOAD` whose memory size exceeds its file size still gives up its boundary page: that page is part file content and part `.bss`, which a mapping of the file cannot serve, so it stays kernel-owned and classifies as private-with-nothing-behind-it. The reference kernel keeps the page inside the file-backed mapping and zeroes the tail through the new address space, which this loader cannot do because it runs before that address space is activated. The page is only dumped once a private file mapping acquires an `anon_vma` on first write, which this kernel does not yet do either. | `crates/kernel/exec/src/tests_file_backing.rs::a_data_segment_is_the_file_up_to_its_boundary_page`; `layout::split`. | — |
| OPEN | low | Two hosted test binaries fail intermittently under the suite's parallelism and pass in isolation and on repeat: `fs::…` (one failure in one `make test` run, 902 passing on rerun and 6/6 clean in a loop) and `drv-zram::tests::control_reuses_removed_index` (one failure, 8/8 clean in a loop). Pre-existing, unrelated to this lane — recorded so a later lane does not attribute them to its own change. | `make test` on `B1697`, two consecutive runs, different crate each time. | — |
| OPEN | med | `code/safety-missing` scans only 4 lines back for the `// SAFETY:` marker, so a SAFETY statement heading a longer explanatory comment reads as missing. Lane B1693 hit this on 12 of its 147 blocks and worked around it by putting the SAFETY sentence LAST — i.e. the lint is currently shaping comment layout rather than checking comment content. That layout was reviewed and kept on its merits (rationale first, safety claim at the point it applies), but it was chosen under lint pressure, which is the distortion this row names. | Reported by B1693 (`scratch/issues.d/B1693-safety-comments-kernel-core.md`). Not fixed here: it changes counts, so it belongs in its own PR with its own tightened baseline rather than mixed into a baseline-only change. | spec-lint lane |
| OPEN | MED | `af_packet_diff` (2,034) and `wait_diff` (4,640) stay C, deliberately. Both compare a guest run against a REAL Linux run of the same binary, and `tools/wait-diff-selftest.sh` is the only harness in this repo that proves its own probe can fail. Porting one side alone moves the oracle. **Condition for moving them:** the Linux-side oracle is rebuilt in the same language in the same PR, with the selftest re-established first. Same rule already agreed for the 218-program `glibc_conformance` corpus. | `tools/boot-smoke-{af-packet-diff,wait-diff}.sh` diff `$LINUX_RECORDS` against `$OXIDE_RECORDS` | unowned |
| OPEN | med | Landlock `LANDLOCK_RESTRICT_SELF_TSYNC` is best-effort, not the all-or-nothing protocol. `sync_siblings` walks the thread group and overwrites each sibling's domain under that sibling's own lock. A thread created during the walk is never visited, so it runs unconfined under a policy its process believes is enforced; two racing `TSYNC` calls can interleave to a mixed result. Linux runs a two-barrier task-work protocol and restarts the syscall on a race. | `crates/kernel/syscalls/src/446_landlock_restrict_self.rs` `sync_siblings`; no hosted test covers a concurrent walk (the thread group is not driveable from the hosted suite). Blocks `446` from `IMPL`. | unowned |
| OPEN | low | Landlock erratum 3 (disconnected directories) is not implemented, and is correspondingly NOT advertised in the errata bitmask. A directory moved outside a bind mount's scope does not have its mount-point hierarchy folded into the evaluated rights, so `ACCESS_FS_REFER` restrictions can in principle be widened by a rename or link in that state. | `crates/kernel/landlock/src/{refer.rs,walk.rs}` walk the dentry parent chain only. `uapi::ERRATA` reports bits 1 and 2 and deliberately not bit 3 — a program feature-detecting the fix is told the truth. Test `the_reported_abi_version_matches_the_rights_actually_accepted` pins the value. Blocks `444`/`445` from `IMPL`. | unowned |
| OPEN | low | Landlock logging is inert: the three `LANDLOCK_RESTRICT_SELF_LOG_*` flags, the `quiet_*` ruleset masks and the per-rule `LANDLOCK_ADD_RULE_QUIET` marking are all validated and carried, but nothing consumes them because this kernel has no audit subsystem. Identical to a Linux built without audit support, and no access decision changes — recorded because "stored but unconsumed" is otherwise indistinguishable from a bug. | `crates/kernel/landlock/src/uapi.rs`; `Ruleset::{quiet_fs,quiet_net,quiet_scoped}` are consumed by the `ADD_RULE_QUIET` admission test, `FsRule::quiet`/`NetRule::quiet` are not consumed at all. | unowned |
| OPEN | med | No hosted coverage of the real `copy_from_user` fault rungs for slots 444/445/446. The pure admission decisions (null-attr `EFAULT` before any size check, the size ladder) are tested, but the faulting-copy paths live in `#![cfg(target_os = "oxide-kernel")]` slot files and cannot be driven hosted. Structural, not an oversight — recorded because the matrix's harness rule names user-copy faults as a required category. | `444_landlock_create_ruleset.rs`, `445_landlock_add_rule.rs`; `landlock::abi::attr_buffer_ok` is the ungated half that IS tested. | unowned |
| OPEN | med | `read(2)` on an fscontext fd returns `EINVAL`; the error log it should return is populated and unreachable. `FsContext::log` has six live producers reached from `fsconfig(2)`, and its only readers are in a test. Linux returns one message per read, `ENODATA` when the ring is empty and `EMSGSIZE` when the buffer is shorter than the message. | `crates/kernel/syscalls/src/fsmount_common/objects.rs` builds the inode with `default_file_ops()`, whose `read` is the no-data errno; `crates/kernel/vfs/tests/fs_context_log.rs` is the only consumer of `log_messages()`/`take_log()`. Blocks `431`. | unowned |
| OPEN | med | `request_key(2)` upcall: no construction has ever completed on a booted system. The helper now execs and reads its callout info, but the key is never instantiated, so the request ends in negation. Every hosted completion runs through a `#[cfg(test)]` injected actor that never execs, so the hosted suite cannot see this. **Correction to the row's recorded GAP: `/sbin/request-key` IS present in all four images (verified by inode stat in lite/gnome × x86_64/aarch64), so "no helper in the image, every construction ends ENOENT" is stale.** Suspected cause is token reachability across the helper's grandchildren, i.e. the `fork_keys`/`exec_keys` transitive path — which has real callers but no test for that case. | `crates/kernel/fs/src/keyring/construct/upcall.rs` (`ACTOR` injection point); existing row `scratch/known_issues.md:191`. Blocks `249`. | unowned |
| OPEN | med | Zero user-copy-fault and zero namespace coverage in the whole keyring tree. Not one `Errno::Efault` assertion exists; every hosted test calls `ops::*_core` directly with kernel-owned data, so `read_user_iov`'s "validate the whole array before copying any of it" invariant — the one that prevents a half-gathered payload — is asserted only in a doc comment. There is also no user-namespace dimension in the key store at all, while `KEYCTL_CAPABILITIES` advertises `NS_KEYRING_NAME` and `NS_KEY_TAG`. | `crates/kernel/fs/src/keyring.rs` user-copy helpers, reachable hosted only via two `JOIN_SESSION_KEYRING` calls that pass no pointers. Blocks `250` — and this, not the recorded EOPNOTSUPP text, is the real reason. | unowned |
| STALE-CORRECTED | — | Row `250`'s recorded GAP ("DH_COMPUTE/PKEY_*/WATCH_KEY at EOPNOTSUPP; fork_keys/exec_keys inspection-verified only") is wrong on every clause. All 33 `KEYCTL_*` commands dispatch to a real core; `KEYCTL_CAPABILITIES` is computed from each implementing module's own `SUPPORTED` constant so bit and behaviour cannot disagree; `fork_keys`/`exec_keys` have production callers in `056_clone.rs` and `exec_transition.rs` and 21 hosted tests. 175 keyring tests pass. | Closed by B1657/B1658/B1659. Matrix note rewritten on this branch. | this lane |
| OPEN | low | The `sendto`/`sendmsg` Landlock hooks (port rights on an explicit destination; `ACCESS_FS_RESOLVE_UNIX` on a pathname recipient) are pinned only by source-inspection tests in `socket/src/tests/landlock_hooks.rs`, not by a behavioural one. The decision itself is behaviourally covered in `net::landlock_addr`, but the call site cannot be driven hosted: the domain is read off the running task and a hosted build has none. A wrong ARGUMENT at the call site (right socket, wrong bytes) would not be caught. Closing it needs a hosted `SendContext` fixture that can carry a domain. | F791. | — |
| OPEN | MED | Row 299's differential is closed for the BATCH rules only. Single-entry receive semantics reached through `recvmsg::recv` (extended-error origins, OOB delivery, control-message copyout, VSOCK address parity) have no host-oracle case, and nothing here executes the real uaccess path — the fake supplies the ABI steps. A guest differential is still required for syscall-context copy-fault ordering. | `crates/kernel/syscalls/tests/conformance_mmsg.rs` case list. | unowned |
| OPEN | low | `fs` hosted test `keyring_procfs::a_new_key_appears_in_the_proc_keys_body_procfs_renders` is FLAKY, and it is pre-existing — not introduced by F794. Reproduced on the untouched main tree. | `for i in $(seq 8); do cargo test -q -p fs --test keyring_procfs; done` fails 3/8 in the main checkout at `7c458d89a` and 1/8 in the F794 worktree. The four tests in the binary share the process-wide key store and run in parallel. | — |
| STALE | 50, 288 | TCP_DEFER_ACCEPT completes the handshake and withholds the child; needs a request-sock minisock | FIXED by `B1656`. `net::stack::tcp_reqsk::defers_segment` is called from the production `deliver_tcp_packet_hop` before any state machine; a bare ACK is dropped and the request stays SYN-RECV on its SYN-backlog slot. `cargo test -p net --lib defer` 25/0, ungated. |
| STALE | 54, 55 | IP/IPV6 families wholly unaudited | `B1662`'s three consumers (nonlocal-bind screen, local-port window, IP_ROUTER_ALERT v4 delivery) are genuinely live on real paths with ungated tests. |
| OPEN | listen/accept4 admission ladders are unreachable from `cargo test` | `050_listen.rs` and `043_accept.rs` carry `#![cfg(target_os = "oxide-kernel")]`, and `net::sock::ops` (security hook, `somaxconn` lookup, family branch) is kernel-gated. Blocks promotion of rows 50 and 288 on permissions, error ordering, and user-copy fault cases. Needs the `F792` socket(2) treatment: extract to one ungated owner. |
| OPEN | accept4 copy-out failure relies on `Drop`, untested | On `copy_sockaddr_to_user` failure the INET path returns the errno and drops `accepted.new_sock`, where the VSOCK sibling calls `net::vsock::close(&conn)` explicitly. The child is already popped off `accept_q` with its backlog slot released. No test asserts the errno or that the slot is not leaked. Not shown to be a defect — untested. |
| OPEN | `syscalls::socket_control_tests` proves nothing about behaviour | It "covers" `050_listen.rs` by `include_str!` source-text grep (asserting the file contains `fd_file(fd)`, `Errno::Enotsock`); `043_accept.rs` is not covered at all. A gate that cannot fail on a behaviour change. |
| OPEN | `syscalls/src/tcp_zerocopy/receive.rs` has no hosted coverage | `#![cfg(target_os = "oxide-kernel")]` by design; the remap, copy-out and frame-lifetime code is uncovered and only the pure planner it delegates to is tested. |
| OPEN | low | `07§9` and `39§9` still gate on `xtask qemu`, which is not a subcommand (`tools/xtask/src/main.rs` dispatches `kernel|image|grub|rootfs|artifacts|test|conformance|gc|path|spec-lint|doc-check|stats`); boots run through `make qemu-*` / `make smoke-*`. | `xtask --help` usage string vs the two test contracts. | — |
| Package | Red runs | Failing tests |
| `fs` | 10/10 | `sys_pipe2_shape::pollout_tracks_current_pipe_capacity` every run; plus `inotify::limits_tests::a_new_watch_past_the_per_user_ceiling_is_enospc` and `keyring_procfs::a_new_key_appears_in_the_proc_keys_body_procfs_renders` in one run each |
| `drv` | 3/10 | 20 tests at once in the worst run (`model::tests::lifecycle::*`, `path::tests::*`); `model::tests::hooks::device_add_initial_probe_precedes_add_uevent_without_bind_change` twice |
| `fbdev` | 1/10 | `tests::register_count_roundtrip`, `tests::register_unwinds_record_when_model_publication_conflicts` |
| `drv-virtio-blk` | 1/10 | `tests::lifecycle::remove_blk_selects_only_matching_device_record` |
| `timer` | 1/10 | `tests::oneshot_fires_once_and_unregisters` |
| `crng` | 1/10 | `pool::tests::short_and_unaligned_lengths_are_filled_completely` |
| `sched` | 1/10 | `tests::signals::realtime_queue_is_bounded_by_rlimit_sigpending_not_a_constant` |
| OPEN | med | `ipc`'s `sysv::sem::tests::undo::{setval_and_setall_clear_pending_adjustments, ipc_rmid_invalidates_the_undo_so_a_later_exit_is_a_no_op}` are flaky on `origin/main` itself, independently of this branch: TWO claims over ONE resource, the process-global current-task slot. `F794`'s `sysv_shm/creator/tests.rs` installs a current task (`become_current` sets `CUR` and `sched::set_current_hook`) under the shm claim, while the sem undo tests file their undo records under `current_tgid()` and assume it reports 0 because no task is installed — an assumption they hold under the SEM lock, which does not exclude the shm one. While a creator test holds `CUR`, a sem `semop_in` files its undo record under the creator's tgid and `semadj_snapshot(0, ..)` reads `None`. NOT fixed here: proven pre-existing by A/B, and folding sem's `TEST_LOCK` into the shm claim (one crate-wide IPC claim, ~38 call sites) is scope this PR should not grow after review started. The fix is small and mechanical for whoever takes it — make `sem::tests::common::TEST_LOCK` an alias of the shm claim's mutex, which leaves every call site untouched. | A/B on the same 60-run loop, same binary shape: `origin/main` (`1b80f8855`) **2/60 red**, this branch **1/60 red**, same tests. Both sides report **230 tests**, so the conflict resolution dropped no coverage. | — |
| OPEN | med | `cargo test -p procfs` alone does NOT COMPILE: `error[E0433]: cannot find 'live' in 'sched'`. It only builds under a workspace invocation, where another package's dev-dependency turns the enabling feature on — the feature-unification hazard, in the direction that HIDES a broken package rather than breaking a working one. Same class as the `drv-virtio-input`/`net` note in the B1676 row. Not this lane's to fix: the per-package check gate owns it. | Reproduced on this branch and on a clean `origin/main`; `cargo test --workspace` builds `procfs` fine. | `B1674-hosted-check-gate` lane |
| OPEN | low | The `--test-threads=1` workaround recorded against three of these packages in the curated ledger is now unnecessary and should not be reached for again: every failure here was a missing owner for a shared resource, and all nine were fixable without serializing the whole binary. Recorded so a future lane reading those rows does not adopt the workaround. | Nine independent resources, nine claims, no global thread cap. | — |
| OPEN | med | `ipc`'s `futex_core_hosted::wait_timeout_returns_etimedout_not_a_fake_success` is LOAD-sensitive, not racy on shared state: it bounds a cross-thread handoff with `rx.recv_timeout(Duration::from_secs(5))` and reports `must not hang: Timeout` when the box is saturated by a 48-way workspace run. Seen twice, both times only inside a full workspace run, never in 40 runs of the crate alone. Deliberately NOT fixed here: raising the bound hides load sensitivity rather than removing it, and picking a new number is a judgement the futex lane should make. Same shape to look for in any other hosted test that bounds a handoff with a wall-clock deadline. | 1/12 workspace runs; `finished in 5.00s` where the passing run is 0.00s. | — |
| OPEN | high | The ext4 e2fsck tests still write ~1.8 GB images to FIXED `$TMPDIR` names, unchanged from the B1680 row. Both distributions here were taken with a private `TMPDIR` on `/home` specifically to dodge it. Two concurrent workspace runs, or two lanes on this box, still collide. | Carried forward from B1680; `crates/kernel/ext4/tests/balloc_uninit_e2fsck.rs:136,208`. | — |
| low | `bio_add_page` is now all-or-nothing (Linux semantics). A module that previously got a truncated count from the bounce-buffer arm now gets 0. That is the correct answer — a truncated count read as a completed add is the bug — but it is a behaviour change for any caller that tolerated the truncation. | `core/tests.rs::bio_add_page_without_a_page_is_bounded_by_the_bounce_buffer` |
| med | 17 of 22 registered filesystems publish no parameter table, so their options are accepted-and-ignored. | Closing it honestly means IMPLEMENTING each one's options, not declaring them: `devpts` `gid=`/`uid=`/`mode=` set new pty slave inode ownership and mode, `ptmxmode=` sets the ptmx node's mode (and is rewritten on remount), `max=` caps pty index allocation, `newinstance` is a no-op the reference keeps only so old userspace is not refused. `cgroup2` `nsdelegate`/`memory_recursiveprot` likewise. Each is its own enforcement lane; `devpts` is boot-critical (wrong = no ttys) and must not ride a parameter-plumbing PR. The semantics above are recorded so the next lane does not have to re-derive them. |
| OPEN | low | A named session keyring is not joinable by default, including by its own owner. Left as is deliberately: it matches the reference and is test-pinned. Recorded again only so a future reader does not "fix" it. | `a_named_join_without_search_permission_creates_rather_than_joins`. | — |
| OPEN | med | `tools/oxide-conformance-ssh.sh aarch64 <test>` never reaches a verdict on this box: it builds the kernel and ISO, prints `--build-only, not launching qemu`, then exits **255 with no `oxide-conformance:` line and an empty boot log** — so no aarch64 differential probe can be run at all. Not a kernel fault and not caused by this lane: the same kernel passes `make smoke-arm` (`Reached target basic.target` in 80s, attempt 1), the aarch64 root image is present in `../images/output`, and the missing `vendor/{grub,firmware}` in a fresh worktree (a separate first-run trap, worked around by symlinking the main tree's) only changes the earlier `incomplete vendored arm64-efi modules` failure into this one. The x86_64 arm of the same harness passes end-to-end. Whoever owns the conformance runner should make the aarch64 path report why it aborts instead of exiting 255 silently. | `conf-arm2`/`conf-arm3` runs, `OXIDE_CONFORMANCE_RUN_ID=b1706-arm`; `/tmp/oxide-conformance-*.log` is zero bytes. | — |

## Net / socket

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| OPEN | low | `TCP_ZEROCOPY_RECEIVE` never sets `TCP_CMSG_TS` in `msg_flags`, so `msg_control`/`msg_controllen` are never filled. The receive queue is a byte stream with no per-segment arrival timestamp to publish. Everything else about the option (operand versioning, remap, copy buffer, `inq`, `err`) is implemented. | B1655. `run` clears `msg_flags`; the reference publishes a timestamp only for a segment carrying one. | — |
| OPEN | low | `TCP_ZEROCOPY_RECEIVE` copies stream bytes into freshly allocated window pages instead of handing over the pages the data arrived in — the receive queue has no page-granular segment list to donate. The ABI, the mapping lifetime and every field rule are unaffected; the saving is the user-space copy, not the kernel one. | B1655. | — |
| IN-PROGRESS B1660/B1661 | med | `IP_OPTIONS` on transmit and IPv6 sticky extension headers on transmit: validated and readable, never emitted. IPv4 half DONE on B1660 — `ipv4_options::{compile,emit}` owns the fill pass, the per-fragment copy rule and the variable-length header writer; `xmit_ipv4_l4_with_policy` and `raw4/tx.rs` carry the option area through headroom, fragmentation maths and checksum; a compiled source route retargets route lookup, path MTU, wire destination and transport checksum at its first hop. IPv6 sticky HOPOPTS/DSTOPTS/RTHDR half open on B1661. | B1641 inventory; B1660 tests: `ipv4_options::tests`, `tests_ipv4_options_tx`. | — |
| OPEN | low | Phantom-test gap in `sock_v6.rs` and `sock/{udp,ops,send}.rs` — decision logic sits in target-gated files where `#[cfg(test)]` compiles away silently. | — | — |
| OPEN | low | `IP_MULTICAST_ALL` now defaults on (reference default), so an unjoined group is delivered. We have no host-level multicast gate equivalent to the reference's, which drops unjoined-group traffic at the IP layer before any socket sees it — so our delivery may be broader than the reference's. Boot reaches `basic.target`; a graphical boot exercising mDNS/LLMNR has not been run. | B1641. | — |
| OPEN | med | `pmm` hosted test `swap::tests::final_swap_reference_reclaims_zram_slot` fails under a full `cargo test --workspace` run (`install hosted zram PMM provider: Ebusy`) and passes on `cargo test -p pmm`. Global hosted-provider slot, the same shape as the `net` parallel-execution rows in the curated ledger. | Reproduced twice on B1656, both times only in the workspace run. | — |
| OPEN | low | AF_UNIX `poll` reports `EPOLLIN` while the only thing queued is a spent out-of-band record, which delivers no bytes — a read then returns nothing and blocks. Faithful to the reference (its receive queue is non-empty), and deliberately kept, but it is a readiness report a caller cannot act on. | B1654. `SIOCINQ` discounts the record and answers 0 in the same state; `net::unix_sock::tests::oob::queued_byte_count_discounts_the_spent_record`. | — |
| OPEN | low | `IP_BIND_ADDRESS_NO_PORT` and `IP_LOCAL_PORT_RANGE` wiring lives in `sock/ops.rs`, `sock/tcp_lifecycle.rs` and `sock_v6.rs`, all `#[cfg(target_os = "oxide-kernel")]` — the decisions are covered hosted in `net::local_port::tests` (including the UDP allocator and the TCP reservation), but the bind-path call sites themselves are reachable only by a kernel-target build. Same phantom-test gap the ledger already records for those files. | `net::local_port::tests` (12 tests); both arches build. | — |
| OPEN | med | The report's `print_vma_addr` tail names the backing INODE, not a path — `mm-vmm`'s `FileBacking` exposes `ino()` and no name. Resolving it needs `find /usr -inum N` in the guest. A path would make the line self-contained. | B1667. | — |
| OPEN | low | `vsock_socket_linux_tests::blocked_scalar_io_observes_shutdown_at_retry_boundary` was passing vacuously: its write half installed no transport, so the send failed at admission and the retry hook never ran. Fixed here by installing a transport, but the same shape (a retry/blocking test on a transportless connection) may exist elsewhere in the VSOCK suites — nothing audits for it. | Exposed only because the ENOTCONN misclassification was corrected. | — |
| OPEN | low | Repository comments across `crates/kernel/net` and `crates/kernel/syscalls` name and line-cite external implementation source files (`af_vsock.c:2267`, `af_unix.c:2258`, `sock.h:2755-2761`, …), which the semantic-verification rule forbids. B1670 cleaned only `net/src/vsock_socket/io.rs`; ~15 further sites remain in `sock_intr.rs`, `vsock_socket.rs`, `syscalls/src/043_accept.rs`, `syscalls/src/recvmsg/vsock.rs`, `syscalls/src/054_setsockopt/vsock.rs`. | `grep -rn "af_vsock\|af_unix.c\|sock.h:" crates/` | — |
| OPEN | low | `crates/kernel/net/src/vsock/conn.rs` is past the 500-line split cutoff (`08§7`), so further connection state has nowhere to go. Pre-existing: 503 lines on `origin/main`, 509 after B1653 added one `AtomicBool` plus its doc. | `wc -l` on `origin/main` vs B1653. | — |
| OPEN | low | `sync/hosted` (the yield in `spin_relax::relax`) is opted into per consumer crate. Only `net` and `slab` forward it — `sched` and `klog` do NOT, contrary to this row as originally filed. | B1653 enabled it for `net` only, after reproducing the livelock there. | — |
| OPEN | low | A watch on a key whose notification pipe has been closed keeps posting into a queue nobody can read. The queue stays alive because the watch holds a reference to it, so nothing leaks and nothing is corrupted, but the watch is never torn down: Linux drops the queue's watches when the pipe is released (`watch_queue_clear`). The visible effect is a key carrying a dead watch entry until the key itself dies. Fix: have `detach` walk the queue's own watch list. That list is not kept today — a watch points at its queue, not the other way about. | B1659, `watch_queue/registry.rs::detach`. | — |
| OPEN | med | No IPv6 packet forwarding at all: there is no `forward_ipv6_in` and nothing routes a non-local IPv6 destination onward. `IPV6_ROUTER_ALERT` therefore has no delivery point — this branch gives it real chain-slot state and the correct selector semantics, but the fan-out its IPv4 twin got has nowhere to hang until an IPv6 forwarding path exists. | `grep -rn "forward_ipv6\|forward6" crates/kernel/net/src` finds nothing; the IPv4 twin lives in `stack_forward::forward_ipv4_in`. | — |
| OPEN | low | `IP_ROUTER_ALERT` set on a raw **IPv6** socket stores the option bit but takes no chain slot: the chain holds `Raw4Endpoint`s, since delivery hands the copy to the IPv4 raw receive queue. The reference does not screen the family here. | `054_setsockopt/ip.rs` `Action::RouterAlert` matches `SockKind::Raw4` only. | — |
| OPEN | med | `IPV6_JOIN_ANYCAST` / `IPV6_LEAVE_ANYCAST` still route to the multicast helper — no anycast address state, no per-device anycast list, no capability screen of their own. Untouched by this branch. | `054_setsockopt/ipv6.rs` sends both to `ipv6_mcast_membership`. Part of the curated IP-sockopt-consumers row. | — |
| OPEN | low | The loadable-module `netif_rx` ABI shim delivers a C driver's frame inline on the calling driver's stack instead of queueing it to the per-CPU backlog, so a Linux module's receive path keeps the old TX/RX-on-one-stack shape. Needs the backlog item to carry an L2 frame plus the caller's ingress generation, which the loopback-fed path does not. | Found while converting the loopback path in B1665. `modules::linux_netdev::core::netif_rx` measures 9920 B on aarch64 — under the ceiling, so not urgent, but it is the last inline receive traversal in the tree. | — |
| OPEN | low | A frame sitting in the receive backlog no longer holds an ingress lease, so a namespace teardown racing the drain completes and the queued frame is dropped at delivery instead of being delivered first. This matches the reference (a device that stops running has its backlog discarded) and is a deliberate change from the retained-lease snapshot the inline drain used, recorded so it is not later read as a regression. | B1665; `frames_queued_for_a_down_interface_are_dropped_at_delivery` pins the drop, `net_ns::lifetime_tests` still pins the retained-lease teardown contract. | — |
| OPEN | med | The 16550 FIFO is never enabled — nothing programs FCR — so every output byte is a separate THR write and the receiver holds one byte. Linux enables and clears both FIFOs at port startup and sets a receive trigger level. Also makes RX overrun likely on a pasted burst. Second suspect for the duplication row (per-byte THR writes maximize the window in which a blocked host backend retransmits the byte already in the transmit register). | `drv-uart-16550/src/lib.rs` has no FCR write on any path. | — |
| OPEN | low | A `qemu-system-x86_64` from another lane was still alive 30+ minutes after start (`target/builds/b1663boot1`), far past a normal boot. Wedged QEMUs foul ports and contend for CPU on every later boot in the wave. | B1672 observed it while the box ran 8+ lanes. | — |
| OPEN | med | Data segments still carry timestamps keyed on `ts_enabled` alone (`build_segment_at`), which is correct, but the SACK-block writer in `sack.rs` and the timestamp writer in `segment.rs` each assemble their own option area by hand rather than through `syn_opts`. Two more hand-rolled option writers remain; a non-SYN option assembler should absorb them. | Not a live defect — both currently emit correct bytes. Flagged so the next lane in this area does not add a fourth. | — |
| OPEN | low | Only the dumping thread's identity is per-thread: every `NT_PRSTATUS` past the first repeats the process-wide `pr_ppid`/`pr_pgrp`/`pr_sid`/times and the killing signal, varying only `pr_pid` and the register block. Correct for the fields that are process-wide by definition, but per-thread CPU times are lost. | `prstatus()` takes one `CoreIdentity` for all threads. | — |
| Package | Red runs | Status |
| `drv-virtio-input` | 10/10 | FIXED B1683 |
| `fbcon` | 9/10 | FIXED B1682 |
| `modules` | 5/10 | OPEN |
| `klog` | 3/10 | OPEN |
| `sound` | 2/10 | OPEN |
| `netlink` | 2/10 | OPEN |
| `drv` | 2/10 | OPEN |
| `sysfs` | 1/10 | OPEN |
| `nscg` | 1/10 | OPEN |
| `fbdev` | 1/10 | OPEN |
| `drv-zram` | 1/10 | OPEN |
| `drv-virtio-gpu` | 1/10 | OPEN |
| `softirq` | (earlier run) | FIXED B1681 |
| `net` | (earlier run) | OPEN, root-caused in B1683's drop |
| OPEN | high | The ext4 e2fsck tests write ~1.8 GB images to **fixed** `$TMPDIR` names (`balloc_concurrent_repro.img`, `balloc_uninit_repro.img`, `arm_hwdb_framecache_repro.img`, `arm_hwdb_rewrite_repro.img`, built via `format!("{}/…", std::env::temp_dir())`). Two concurrent workspace runs — or two lanes on this box — collide on the same file, and every run leaves ~7 GB behind. This filled the shared 32 GB `/tmp` tmpfs to 100% during the first baseline attempt, which failed the ext4 tests, aborted 8 of 10 runs, and broke the agent harness's own output files. Same defect class as the rest of this lane, in the filesystem namespace. | `crates/kernel/ext4/tests/balloc_uninit_e2fsck.rs:136,208`; `/tmp` at 100% with four `*_repro.img` totalling ~7.2 GB. | — |
| OPEN | low | `answerback.rs` keeps a third module-private `SERIAL` for its own queue state. It did not appear in any observed failure and guards a different resource, so it is left alone — but the crate now has one lock that owns the console and one that owns the answerback queue, and nothing states which resource a new test must take. | Not observed failing. | — |
| OPEN | med | Two hand-rolled option writers remain (`sack.rs` build_ack_with_sack, `segment.rs` build_segment_at), carried over from the B1673 drop. Neither is a live defect; both should fold into a non-SYN option assembler rather than a third and fourth writer being added beside them. | Carried forward from B1673. | — |
| File | `include_str!` sites |
| `syscalls/src/socket_control_tests.rs` | 18 |
| `syscalls/src/recvmsg/vsock_shutdown_tests.rs` | 6 |
| `syscalls/src/socket_fd.rs` | 4 |
| `syscalls/src/socket_control_tests/dispatch.rs` | 4 |
| `syscalls/src/send_user/tests.rs` | 3 |
| `syscalls/src/fcntl_dup_tests.rs` | 2 |
| `syscalls/tests/select_file_ownership_hosted.rs` | 1 |
| `syscalls/tests/ppoll_sigmask_hosted.rs` | 1 |
| `syscalls/src/poll_ownership_tests.rs` | 1 |
| `syscalls/src/getdents_debug_tests.rs` | 1 |
| `net/src/sock/packet_options.rs` | 1 |
| OPEN | low | `ru_utime`/`ru_stime` and `times(2)`'s `tms_utime`/`tms_stime` are tick-sampled per-task counters, not the scaled pair upstream derives so that user+system exactly equals the task's total run time. The two can therefore differ from `sum_exec_runtime` by up to a tick per thread. Deliberate: the sampled values are the more direct measurement and no known consumer depends on the identity. Revisit if a benchmark or a `getrusage`-based profiler reports the discrepancy. | F779, negative result — no test asserts the identity because it does not hold. | — |
| OPEN | low | Row 103 `syslog` has its status cell as `IMPL` without backticks, where every other row uses `` `IMPL` ``. Any tool that matches the backticked form silently drops this row from its counts — the same "statistic computed over a subset" failure this lane just fixed at larger scale, in miniature. Not fixed here because it is another lane's row (`F732-ptrace-syslog-audit`) and this PR is deliberately insertions-only, so that reviewers can verify no existing row was touched. One-character fix for whoever next edits that row. | F784; `scratch/syscall-compliance-matrix.md:497`. Detect with `p[8].strip()=='IMPL'` over the 14-field split. | F732 lane |
| OPEN | med | PKRU is deliberately kept OUT of `XCR0` (`XCR0_WANT` in `hal-x86_64/src/fpu.rs` does not set bit 9), so `XSAVE`/`XRSTOR` never touch it and the per-task field is the single owner. Upstream puts PKRU in XCR0, which means its signal frame's xstate carries PKRU and `rt_sigreturn` restores it. Consequence here: a handler that changes PKRU keeps that change across `sigreturn`, where upstream would restore the interrupted value. Chosen over the alternative because two owners of one register is the split-source-of-truth this project bans; closing it means adding the `XFEATURE_PKRU` component to the sigframe xstate AND removing the per-task field in the same change, not one of the two. | F785; `crates/arch/hal-x86_64/src/pkru.rs`, `fpu.rs:38`. | — |
| OPEN | low | `setup_pku` leaves `CR4.PKE` set on the BSP even in the impossible case where the hardware does not report OSPKE after the write; the latch stays false so every PKRU access remains inert. Matches upstream's ordering (it cannot know OSPKE before setting PKE) but means CR4 and the latch can in principle disagree. Harmless while nothing reads CR4.PKE directly. | F785; `pkru::setup_pku`. | — |
| OPEN | low | `init_pkru_value` is settable through `set_pkru_init_value` but has no `debugfs` file behind it (upstream exposes `/sys/kernel/debug/x86/init_pkru`). The validation that matters — refusing a default that denies key 0 — is implemented and tested; only the knob is missing. | F785; no debugfs entry wired. | — |
| OPEN | MED | The dumper does not stop for a freezer request, only for a pending `SIGKILL`. The reference stops for either. There is no freezer state to consult from the pipe write path in this tree. | `pipe/ring.rs::write_wait_aborted` handles only `fatal_kill_pending_self` | unowned |
| OPEN | low | `try_fill_iter`'s atomicity guard carries a `total <= cap` term that is unreachable: `round_pipe_size` floors capacity at `PIPE_GROW_STEP` (4096) which equals `PIPE_BUF`, so `total <= PIPE_BUF` already implies `total <= cap`. Harmless and defensive, not a defect; noted so a later reader does not mistake it for live logic. | `crates/kernel/fs/src/pipe/limits.rs` `round_pipe_size`, `PIPE_GROW_STEP == PIPE_BUF == 4096`. | unowned |
| OPEN | low | `crates/shared/kalloc/src/holes.rs` (933) and `src/tests.rs` (651) are over the 500-line split cutoff and were left unsplit by this lane. Both are under the 1000 hard cap. `holes.rs` is the free-list data structure under live corruption-diagnostic work; splitting it is a separate lane's call. | B1690. `wc -l crates/shared/kalloc/src/*.rs`: holes.rs 933, tests.rs 651; every other file ≤ 284. | — |
| OPEN | low | `metadata/index.md` counters drift behind git and nothing repairs them — `tools/next-branch.sh --check` reports `D next=439 but D445 exists`. Harmless now that claims are atomic (the index is only a backstop), but the check will keep reporting STALE until someone either refreshes the table or retires it in favour of the claim refs. | C248, `tools/next-branch.sh --check`. | — |
| OPEN | low | `docs/39-build-and-image.md:47,97`, `docs/29-init-and-userspace.md:29` and `docs/29a-userspace-platform.md:162` still describe a vendored musl fork and "cross-build bash + coreutils + util-linux into the rootfs". All three are **FROZEN**, so correcting them needs an `R<NN>` revision-block branch, not this `C` lane — not silently edited here. The text is contradicted by `docs/59§1` and by the tree (zero musl files). | C252 found them; freeze status checked per spec header (`FROZEN 2026-05-02`). | needs an `R` lane |
| OPEN | LOW | `docs/07§8` still documents `xtask user`, `xtask qemu`, `xtask bench` and the musl orchestration. `07` is FROZEN, so it is fixed on the `R` revision branch, not here. | `docs/07-toolchain-and-targets.md:217-234` | R lane |
| Rule | Before (C255 baseline) | After |
| `code/pub-fn-complexity` | 1223 | 1143 |
| `code/safety-missing` | 652 | 643 |
| `code/panic-fmt` | 113 | 0 |
| `code/safety-short` | 58 | 56 |
| `code/extern-std` | 18 | 0 |
| `manifest/dangling` | 2 | 0 |
| `code/no-std` | 2 | 0 |
| `xref/sec` | 1 | 0 |
| `code/magic-errno` | 1 | 0 |
| `len/over-cap` | 1 | 1 (B1690 lane) |
| **total** | **2696** | **2468** |
| OPEN | MED | Docs still reference the deleted crates and the abandoned write-our-own-libc plan: `docs/59` (the glibc spec, DRAFT — should go entirely), `docs/00§3` phase-ladder rows 27 (`crates/user/ldso` dynamic linker) and 28 (oxide-libc + NSS + PAM), `docs/03`, `docs/52§123`, and the FROZEN `docs/07`/`29`/`29a`. Deliberately NOT fixed here to keep this PR pure code deletion; all of it lands in the `R` revision lane, which needs revision blocks for the FROZEN three anyway. | `grep -rn 'crates/user' docs/` → 13 hits across 8 files | R lane |
| OPEN | med | `POR_EL0` is not saved in or restored from the aarch64 signal frame. Upstream defines a `POE_MAGIC` sigcontext record for exactly this, and `records.rs` already declares that magic in its REJECTED list — so a handler that changes its rights keeps the change across `sigreturn`, and a `POE_MAGIC` record supplied by userspace is refused. Symmetric with the x86 lane's XCR0 divergence (F785) and should close with it, in one change that adds the record on both arches rather than one arch at a time. | F786; `hal-aarch64/src/signal/records.rs:64` declares `POE_MAGIC` as unsupported. | — |
| OPEN | low | Only `TCR2_EL1.E0POE` (EL0 overlay) is set, not `TCR2_EL1.POE` (EL1 overlay). This matches upstream — the kernel does not overlay its own accesses — but means `POR_EL1` is untouched and kernel accesses are never subject to a key. Recorded because a future supervisor-key feature would need the other bit. | F786; `por::setup_poe`. | — |
| OPEN | med | `classify_arm_abort` maps every DFSC outside `0x04..=0x07` to `Protection{access}`, including external aborts (`0x10`), ECC/parity (`0x18`) and alignment faults (`0x21`). Those are then handed to the demand-paging resolver, which cannot fix them, before falling through to the signal path that classifies them correctly. Not a wrong answer reaching userspace — the eventual signal is right — but it is a wasted resolve attempt on a fault class that is never resolvable, and it is the function that would need an explicit arm if a future fault class must NOT be treated as COW-able. | F788; `mm-pmm/src/user_as/teardown.rs` `classify_arm_abort`. | — |
| OPEN | med | `FEAT_S1POE` could not be exercised on real silicon or emulation: QEMU 9.2.4 implements it on neither `cortex-a72` nor `max`, so the aarch64 boot proves only that `setup_poe` correctly does nothing and does not fault on a CPU without the feature. The x86 half WAS proven — `-cpu host` on a Ryzen 7960X reports PKU/OSPKE and the boot logs the detection line. The arm enable path stays unproven until a QEMU with S1POE or real ARMv8.9 hardware is available. | F788; `qemu-system-aarch64 --version` 9.2.4, no `detected:` line under either CPU model. | — |
| OPEN | low | The overstated-IMPL regex matched PAST-TENSE prose describing what a fix removed — "which had no counterpart before" means the counterpart now exists. It falsely flagged rows 82, 264 and 316, all correctly `IMPL`. Narrowed with a past-tense exclusion applied before the negative-claim match. Recorded because the regex is heuristic and will keep producing false positives as note prose grows; treat a new hit as a question, not a verdict. | F789; 12 flagged before the exclusion, 9 after, and the 3 dropped were manually confirmed correct. | — |
| OPEN | high | **Process, named because this lane hit it twice in two PRs.** A check must be able to OBSERVE the failure mode it is chosen to rule out. (1) `F784` verified its own pass with the same parser that produced its error, then reported "insertions only, zero deletions, so nothing was disturbed" — the diffstat was true and the conclusion false, because a duplicate row *is* an insertion. The evidence offered was structurally incapable of showing the defect. (2) While fixing that, F789's first positive control for the unbackticked-status invariant went red — but for an unrelated reason (the mutated row's notes tripped the overstated-IMPL check), and the invariant under test was not implemented at all. Both were caught only by asking "could this check have gone red for the wrong reason, or stayed green for the wrong reason?" Rule: a control must be run on an input that can fail for the tested reason ALONE, and its failure message must name that reason. | F789; the four controls in this PR each name their row and invariant, which is what made the false one visible. | — |
| whole tree | 707 | 329 |
| OPEN | high | **`blk_mq_end_request` re-dereferences `rq` after the driver's `end_io` callback returns**, and discards that callback's return value. Linux's `rq_end_io_fn` returns `RQ_END_IO_FREE` precisely because drivers free the request there; this shim then reads `(*rq).q` and calls the `complete` op with the freed `rq`. `blk_execute_rq` has the same shape — it reads `(*rq).status` after `blk_execute_rq_nowait` has already driven completion. Not sound against a conforming Linux driver. | `crates/kernel/modules/src/linux_block/mq.rs`, `blk_mq_end_request` / `blk_execute_rq`. Both sites now carry an explicit `// Precondition:` line stating the callback must not free `rq`, rather than a comment claiming soundness. Real fix = honour the `RQ_END_IO_FREE` return. | unclaimed |
| OPEN | med | **`usb_ep_free_request` can free a UDC-owned request with the wrong allocator.** The fallback `drop(Box::from_raw(req))` is reached whenever `ep` is null, `(*ep).ops` is null, or `ops->free_request` is `None`. It is only correct if `req` came from `usb_ep_alloc_request`'s `Box` branch, i.e. the same `ep` state. Linux's `usb_ep_free_request` unconditionally calls `ep->ops->free_request`, so it would NULL-deref rather than mis-free; our null-`ep` tolerance converts a NULL-deref into heap corruption. | `crates/kernel/modules/src/linux_usb/gadget.rs`, `usb_ep_free_request`. Documented with an explicit `// Precondition (Linux KPI):` line instead of an assertion of soundness. | unclaimed |
| OPEN | med | **`usb_find_interface` matches on the wrong field, and `UsbInterface` has no `minor`.** It compares `(*intf).registered as i32 == minor`, where `registered` is a 0/1 flag, so the lookup can only ever match `minor == 1` and returns an arbitrary interface. Linux's `struct usb_interface` carries `minor`, assigned by `usb_register_dev`; ours does not, so a C module reading `intf->minor` also reads a different field. | `crates/kernel/modules/src/linux_usb/core.rs` `usb_find_interface`; `linux_usb/types.rs` `struct UsbInterface` (fields: dev, altsetting, cur_altsetting, num_altsetting, usb_dev, intfdata, registered, driver — no minor). Needs a minor allocator + `usb_register_dev`, not a comment. | unclaimed |
| OPEN | med | **`pattern_match` does not handle `%%` and ignores field-width digits.** Linux's `match_one` treats `%%` as a literal percent and parses a width after `%` (for `%3s`-style patterns). Correctness divergence, not memory safety. | `crates/kernel/modules/src/linux_string/match_parser.rs`. Found while auditing the same function as the `MAX_OPT_ARGS` overflow above. | unclaimed |
| OPEN | low | **`free_netdev` reads the `NetdevHeader` before validating `magic`.** A pointer not from `alloc_netdev*` makes `let h = unsafe { *hp }` UB before the magic check can reject it. Unavoidable given the design and matching Linux's own `free_netdev` contract, so the comment states the contract rather than claiming the magic check makes it safe. | `crates/kernel/modules/src/linux_netdev/alloc.rs`. | unclaimed |
| INFO | info | `drv-ps2-keyboard/src/lib.rs` was 498 lines, 2 under the 500 split cutoff — adding SAFETY prose crossed it. Split into `lib.rs` (manifest) + `noop.rs` + `imp/{mod,regs,state,ports,bringup,irq,driver}.rs` in this PR rather than deferring. Largest resulting file is 169 lines. | `wc -l crates/drivers/drv-ps2-keyboard/src/**/*.rs` | B1695 |
| INFO | info | The `vt` hook indirection (`SIGNAL_HOOK`/`OWNER_ALIVE`/`ON_SWITCH` as `AtomicPtr<()>` + `transmute` back to a `fn`) is sound but type-unchecked by construction: soundness rests entirely on every store going through the typed `set_*_hook` setter. An `AtomicPtr` of the concrete `fn` type, or a `Spinlock<Option<fn(..)>>` as `cgroup::state` already uses, would make it checked. Not changed here — this lane is comment-only for `vt`. | `crates/drivers/vt/src/state.rs` `fire_signal` / `owner_alive` / `fire_switch`; contrast `crates/kernel/cgroup/src/state.rs:15`. | unclaimed |
| NOTE | info | A key whose count reaches zero DROPS OUT of `baseline.tsv` rather than being stored as `0`. That is TIGHTER, not coverage loss: a missing key compares as 0, so a reintroduced finding under it is a regression. Do not "helpfully" re-add dropped keys as explicit `0` rows — reading a shrinking baseline as lost coverage is the mistake this row exists to prevent. | `ratchet.rs:92` — "Absent from baseline = 0, so a brand-new unit or rule is a regression." After #4403, `crates/kernel/modules code/safety-missing` and `crates/user/glibc code/safety-missing` are gone from the file for exactly this reason. | C259 |
| OPEN | `docs/30` R01 recorded an io_uring implementation subset (register = silent-0, no shared ring mmap, opcode list) that contradicts the `30` body, which specifies the full Linux surface. Block deleted as stale status, not spec. Whether the code still matches that subset is unverified by this lane. | `30§2` invariants + `30§?` register-buffers text vs the deleted R01 text |
| OPEN | `docs/27` R02 recorded a cBPF-only `bpf(2)` subset with "eBPF + verifier + JIT tracked as phase 23"; `27§5` body still says seccomp filter mode returns ENOSYS with BPF "phase 23". Deferral framing survives in the body and conflicts with the no-v2 rule (`02§9` rule 8). Not fixed here (spec-content change, out of scope for a flattening PR). | `27§5` lines "Now: returns ENOSYS for filter mode (BPF tracked as phase 23)" |
| OPEN | `docs/14` R07 pinned an aarch64 scaffold of 192 B; the shipped code is 288 B (extra `sp_el0`/pad/`x19..x28`) and the x86 scaffold is 184 B, not 136 B. The revision block had drifted from the code. `14§5.6`/`14§6.5` now state the code's layout. | `crates/arch/hal-{x86_64,aarch64}/src/context.rs` doc-comments |
| INFO | No boot run: this PR changes only markdown and Rust comments (`git diff crates/` has zero non-comment lines). | `git diff crates/ \| grep -v '^[+-]\s*//'` empty |
| OPEN | MED | **No ARM boot has been run for the three ported probes.** They build, cross-link and inject on aarch64 (verified), but have not been watched executing there. The x86 DRM smoke is the only in-guest evidence in this PR. | PR #4410 verification table | unowned |
| OPEN | low | Stale comment claims `KEYCTL_CAPS0_{DIFFIE_HELLMAN,PUBLIC_KEY,BIG_KEY}` and `KEYCTL_CAPS1_NOTIFICATIONS` are "features this build reports as absent" with "no reader". All four are read and set. Comment-only, but it is the exact text a reviewer would cite to hold row `250` open. | `crates/kernel/fs/src/keyring.rs` header comment vs `keyring/keyctl.rs` `keyrings_capabilities()`. Not fixed here — different subsystem from this branch's code change. | unowned |
| OPEN | low | `sendmsg(MSG_FASTOPEN)` on a TCP socket does not ask for `ACCESS_NET_CONNECT_TCP` against the supplied address. Upstream checks it before the UDP leg of the same hook. Unreachable here for now — there is no TFO handshake at all (see the fast-open row above) — so this closes with TFO, not before. | F791. | — |
| OPEN | low | A pathname AF_UNIX server's publishing domain is recorded at `bind(2)` (`net/src/sock/ops.rs` `bind_admitted`), whereas upstream reads it off the server socket's file credentials at check time. The two disagree for a socket whose owning task changed domain after binding: upstream would see the newer domain, this kernel keeps the one in force at bind. Same divergence already applies to the abstract-scope check. | F791; `UnixListener::set_owner_domain` / `UnixDgramQueue::set_owner_domain` have exactly one caller each, both in `bind_admitted`. | — |
| OPEN | MED | Row 307's differential is one case (the `UIO_MAXIOV` bound). The per-entry `MSG_BATCH` flag on all but the last entry, the partial-send stop rule, the compat layout, and blocking/signal/restart behavior have no host-oracle case. `socket::send_batch` is reachable hosted, so these are buildable the same way. | `sendmmsg.batch_is_bounded_at_uio_maxiov` is the only send-side case. | unowned |
| OPEN | LOW | The scripted socket cannot block, so "a receive that would have waited" is modelled as a distinct errno (`WOULD_BLOCK`) that the batch then latches, and the host's `SO_ERROR` of 0 is what contradicts it. This detects a dropped `MSG_WAITFORONE` conversion but is not proof of real wait behavior. | `mmsg_batch::fake::WOULD_BLOCK`; case `recvmmsg.waitforone_leaves_nothing_latched_after_the_drain`. | unowned |
| STALE | 54, 55 | TCP_ZEROCOPY_RECEIVE returns ENOPROTOOPT | FIXED by `B1655`. Intercepted in `055_getsockopt/tcp.rs` ahead of the generic value table; drains the receive queue; `mmap(2)` on a TCP socket fd is live. No path returns ENOPROTOOPT for it. |
| STALE | 54, 55 | IP_OPTIONS never emitted on transmit | FIXED by `B1660` for UDP and raw4. Partially stale only: TCP still passes a literal `None`. |
| OPEN | TCP fast-open codec has no production caller | `TcpConn::syn_options` hardcodes `fastopen: None`; `fastopen::parse`/`classify` have zero callers outside their own module. No SYN carries a TFO option, no listener validates a cookie, no data-in-SYN path. Sockopt state is stored and read back only. |
| OPEN | IPv6 sticky extension headers stored-only | `IPV6_HOPOPTS`/`RTHDRDSTOPTS`/`RTHDR`/`DSTOPTS` validated then never emitted; `merge_sticky_raw6_control` merges only `multicast_loop` and `traffic_class`, never the `Raw6Control` header fields. Per-message cmsg ext headers DO work on raw6 — sticky ones do not. |
| OPEN | `IPV6_PKTINFO` sticky has no reader anywhere | Written on the set path; no consumer and no getsockopt readback. `Ipv6Opts::nexthop` likewise has zero callers. |
| OPEN | IPV6 router-alert has no delivery chain | The selector is stored and admission-screened, but `router_alert` implements only `v4_present`/`v4_deliver` — the v6 half is join bookkeeping with no delivery, unlike v4. |
| OPEN | SOL_IP stored-but-unconsumed | `IP_NODEFRAG`, `IP_RECVERR_RFC4884`, `IP_UNICAST_IF` (never influences route/source selection), `IP_TRANSPARENT` (bind permission only — no TPROXY delivery or reply-source behaviour). |
| OPEN | SOL_IPV6 stored-but-unconsumed | `IPV6_ADDR_PREFERENCES`, `IPV6_USE_MIN_MTU`, `IPV6_MTU`, `IPV6_UNICAST_IF`, `IPV6_AUTOFLOWLABEL`, `IPV6_ROUTER_ALERT_ISOLATE`. |
| OPEN | SOL_PACKET stored-but-unconsumed | `PACKET_COPY_THRESH`, `PACKET_TX_HAS_OFF`, `PACKET_QDISC_BYPASS`. Rest of the family is live. |
| OPEN | Options missing entirely | `IP_PROTOCOL` has no set-path arm (settable on raw sockets in Linux); `IPV6_JOIN_ANYCAST`/`IPV6_LEAVE_ANYCAST` are declared in uapi with no set-path arm. |
| NOTE | info | Revision blocks are history and were not rewritten, so `musl` / `crates/user` / `oxide-libc` still appear in the dated R-blocks of `01`,`03`,`07`,`15`,`29`,`29a` and in `54§3.5`. Only the `59§<sec>` references inside them were flattened to plain "spec 59" text, because `xref` resolves every `<doc>§<sec>` and `59` no longer exists. Reading those blocks as current plan is the error; each is superseded by a later block in the same file. | `grep -rn musl docs/` after this PR: every hit is inside a `## Revision` block, an options-considered list (`05§F1`), or a factual note about musl's signal behavior (`54§3.5`). | — |
| OPEN | low | `39§5` still names Limine as the x86 bootloader in the ESP layout; the tree fetches and builds GRUB (`tools/fetch-grub.sh`, `xtask grub`). Adjacent to this item's edits but a bootloader claim, not a userspace one. | `docs/39§5` item 1 vs `vendor/grub` + `xtask grub`. | — |
| HIGH | `open(path, O_WRONLY\|O_CREAT\|O_TRUNC)` on a **dangling symlink whose parent directory exists** returns **EEXIST**. The reference follows the link and creates the target; EEXIST is only correct with `O_EXCL`. Minimal in-guest reproducer: `ln -sf /run/systemd/resolve/stub-resolv.conf /etc/rc2` (that directory exists, the file does not), then `: > /etc/rc2` → `sh: /etc/rc2: File exists`, and `python3 -c "import os;os.open('/etc/rc2',os.O_WRONLY\|os.O_CREAT\|os.O_TRUNC,0o644)"` → `FileExistsError: [Errno 17]`. This is why nothing in the guest can write `/etc/resolv.conf`, which ships as exactly such a dangling symlink — so it is on the path between a working IP stack and working name resolution. Needs its own lane: the fix sits in the `O_CREAT` open path and its errno ordering against `O_EXCL`/`O_NOFOLLOW` has to be audited against the reference. |
| Open flags | Trailing link followed? | Result |
| `O_CREAT` | yes (`LOOKUP_FOLLOW` on the last component) | creates the link's **target** |
| `O_CREAT\|O_EXCL` | no — **`O_EXCL` silently forces `O_NOFOLLOW`** | `EEXIST` |
| OPEN | low | `/sys/kernel/osrelease` and `/sys/kernel/ostype` do not exist in Linux — the kernel version is exposed only under `/proc/sys/kernel/`. They are an oxide-invented surface. Left in place (now carrying the correct value) rather than removed in a version-bump PR: a prober that found them would start failing instead of falling back, and that is a separate behavioural change. | Linux registers `ostype`/`osrelease`/`version` in the utsname sysctl table only; nothing under `/sys/kernel` | — |
| OPEN | med | Landlock advertises ABI 8, which is a POST-6.19 interface (the 6.19 series uapi carries the ABI-7 logging flags and no `LANDLOCK_RESTRICT_SELF_TSYNC`; 7.2-rc is at ABI 10). So the landlock surface EXCEEDS the release claimed here. Landlock is probed by ABI query, not by release, so this costs nothing today — but a future release bump must not be justified by it alone. | `crates/kernel/landlock/src/uapi.rs:104` `ABI_VERSION = 8`, `RESTRICT_SELF_TSYNC` implemented | — |
| OPEN | med | rseq time-slice extension: slot 471 read-and-clears the yielded flag correctly, but the GRANT side (`PR_RSEQ_SLICE_*`) is not implemented. This is what pins the release below 7.0. | matrix row 471 `PARTIAL` (F762) | — |
| OPEN (info) | — | The matrix `IMPL` status CANNOT date a release: 72 of 385 rows are `PARTIAL`/`NEEDS-REWORK` and they are spread across every era (`16:ioctl`, `42:connect`, `46:sendmsg` are 1.x/2.x syscalls). Read literally, "newest release where every syscall introduced at or before it is `IMPL`" yields a bound below 2.6 — because the status tracks AUDIT CLOSURE, not presence. Presence is the property a release number claims; audit closure is tracked separately and must not be conflated with it by the next lane that revisits this number. | `scratch/syscall-compliance-matrix.md` status column: 291 IMPL / 56 PARTIAL / 16 NEEDS-REWORK / 22 LINUX-ENOSYS | — |
| low | `blk_mq_end_request_batch` is still a no-op, so a driver using `io_comp_batch` never completes its requests. Pre-existing; not touched here. | `mq/request.rs` |
| Sev | Issue | Why not this PR |
| low | ext4 admits its whole ~90-name table and acts only on the quota family; `journal_path` is declared `Path` and ignored. | Accepted-but-ignored, and the fix is ext4 enforcement, not admission. Untouched by this PR. |
| OPEN | low | The `request_key` smoke's default marker `REQUEST-KEY-PROBE` matches the probe's FAIL line as well as its PASS line, so a marker-only run reports success on a failed probe. Run it with `SMOKE_MARKER='REQUEST-KEY-PROBE: PASS'`, or read the probe's own line out of the log. Cost one misread run here. | `tools/boot-smoke.sh` greps for the marker; `support::report` prints `<name>: PASS` / `<name>: FAIL`. | unowned |
| OPEN | low | A nested upcall whose inner handler caches its answer in `%S` is EACCES, and that is CONFORMANT rather than a defect: for a nested request `%S` is the outer helper's request keyring (`KEY_POS_ALL \| KEY_USR_VIEW \| KEY_USR_READ`), and a nested token carries the ORIGINAL requester's credentials, which possess no such keyring. The stock debug handler always names `%S`, so the injected nested case uses its own inner handler that names no destination. Recorded so a later lane does not read the EACCES as a bug. | `[KEYRING] instantiate rc=-13` on the inner key with the stock handler; `rc=0` with a handler naming ring 0. | — |
| OPEN | low | `KEYCTL_PKEY_{ENCRYPT,DECRYPT,SIGN}` returns EOVERFLOW where the reference re-sizes the copy from the key and writes past a short caller buffer. Deliberate deviation, kept. | `keyctl/pkey.rs::eds`. | — |
| 284 (med) | `dump_size` is invented by every caller | The walk takes it from the ladder under the mm's own filter word; no caller supplies a policy |
| OPEN | low | `BootInfo.bsp_lapic_id` is still hard-coded 0 by both live boot stubs — no bootloader on either arch surfaces a boot-CPU id. `cpu::smp::set_boot_cpu_id` therefore always records 0, and `drv_serial`/`drv_ps2_keyboard` `configure_probe` always see APIC id 0. Correct on QEMU (BSP is APIC 0) but not derived: the honest source is the LAPIC ID register (x86) / `MPIDR_EL1` (aarch64) read at boot. Kept as-is here because changing the boot-CPU id source is a behavior change independent of removing Limine. | `crates/arch/boot-x86_64/src/boot_info_build.rs` `build_boot_info`; `crates/arch/boot-aarch64/src/boot_info_build.rs`; `crates/kernel/kmain/src/kmain/early.rs` `log_boot_info`. | — |
| OPEN | low | `project-stats.md` still lists a `limine-proto` row (6 files / 564 LOC). It is a hand-committed snapshot of `xtask stats` stdout and is already stale against `main` for unrelated reasons — it reports 5290 tracked files / 116 workspace members / a `crates/user/` group, where the tree has 4828 / 102 / none. Regenerating it in this PR would bury the deletion in unrelated churn. | `make stats` vs `project-stats.md` head; `xtask stats` writes no file. | — |
| OPEN | low | `make qemu-x86-grub` is now byte-identical to `make qemu-x86` except for the feature set (xtask default `debug-boot` vs `QEMU_FEATURES_X86`). It existed to distinguish the GRUB path from the Limine path; that distinction is gone. `tools/boot-smoke.sh` and `tools/run-smokes.sh` still route a `grub` arch alias to it. Left in place to avoid breaking those callers. | `Makefile` `qemu-x86-grub`; `tools/boot-smoke.sh` / `tools/run-smokes.sh` `grub)` case. | — |
| OPEN | low | A passive TCP child still carries no IPv4 option area. This is deliberate and matches the reference, which gives an accepted child the *incoming SYN's reversed* option area, not the listener's sticky one — we implement neither, so a child's area is empty either way. Inheriting the listener's sticky area would be a divergence, not a fix. The missing piece is receive-side option reversal (record-route/timestamp echo), which the curated `IP_RETOPTS` row already tracks. | `stack/tcp_listener_deliver.rs` builds children through `TcpEntry::new_bound_full`, whose area is empty by construction. | — |
| OPEN | low | No IPv6 equivalent: a TCP/IPv6 segment carries no sticky extension headers. Same shape as the curated "IPv6 sticky extension headers stored-only" row, which is claimed by B1661 for the datagram half; the TCP half rides on whatever emitter that lane lands. | `stack/tcp_tx.rs` v6 arm passes no option state to `xmit_ipv6_l4_with_policy`. | — |
| OPEN | low | The connect-path call that hands the socket's `IpOpts` to the new entry lives in `sock/tcp_lifecycle.rs`, which is `#[cfg(target_os = "oxide-kernel")]`, so that one line is reachable only by a kernel-target build. The decision it feeds (`tcp_ext_hdr`, the transmit path, `tcp_sync_mss`) is covered hosted against a real `TcpEntry`. Same phantom-test gap the ledger already records for that file. | Both arches build (`make feature-gate`); `net::tests_tcp_ip_options_tx` drives `send_tcp_entry_segment_in` directly. | — |
| OPEN (not a defect) | low | The `IP_CHECKSUM` half of that row is **correct as it stands**, not an unwired consumer. The reference emits the `IP_CHECKSUM` control message only for a datagram whose device handed up a whole-datagram checksum; every other case produces no message. Our receive metadata has three checksum states — none, partial, valid — and no "complete" state, because neither device class we run (loopback, virtio-net) ever reports one: the reference maps the virtio "data valid" bit to *unnecessary*, not *complete*. So a receive on our devices gives the same answer the reference gives on the same devices: no message. Wiring a producer would require inventing a checksum the device never computed. Re-open only if a device that reports a full receive checksum is added. | `netdev::packet_metadata::PacketChecksum` has no `Complete` variant; `net::cmsg` already emits the message when `RxMeta.checksum` is `Some`, and that consumer is test-covered. | — |
| OPEN | low | Two IPv4 TTL owners disagree, and the ledger row blaming the v4-mapped path has it backwards. `net::inet_tx::ipv4_ttl` resolves an unset `IP_TTL` to a flat 64, which **bypasses a route's hoplimit metric**; the v4-mapped UDP path instead passes 0, the sentinel `send_udp_pmtu_to_bound_opts_owner` resolves against `route.metrics.ipv4_hoplimit(64)` — which is the reference's order (socket value if set, else the route's hoplimit, else the default). So the curated `IP_TTL unset resolves to a wire TTL of 0` row describes a sentinel, not a wire value: no datagram has ever left with TTL 0 through that path. The real divergence is the opposite one, and it is in `ipv4_ttl`. Fixing it needs the u8 channel to carry "unset" distinctly from a legitimately set 0, which today it cannot. | `stack/ipv4.rs` `if ttl == 0 { route.metrics.ipv4_hoplimit(...) }`; `inet_tx::ipv4_ttl` returns `IPV4_DEFAULT_TTL` for the negative sentinel (db7a83151). `net::inet_tx::tests::the_ttl_a_datagram_carries_depends_on_whether_it_is_multicast` pins the current flat-64 answer. | — |
| OPEN | high | **Incremental-compilation MISCOMPILE in `sock_opts::sol_tcp::set::admit`.** Adding a call to a cross-module `pub fn clamp_qlen(a,b) -> i32 { min(a,b) }` in the `TCP_FASTOPEN` arm made the *neighbouring, untouched* `TCP_LINGER2` arm return `Linger2(-5)` for input `-5` — i.e. `if val < 0 { -1 }` produced `-5` with `val == -5`. Instrumenting the arm made it correct again (observation changes the result), which is the signature of a codegen bug, not a source defect. Mitigated in this PR by `#[inline]` on `clamp_qlen`; the mitigation is fragile — any future edit could re-trigger it in another function. | Reproduction on this branch: revert `#[inline]` on `tcp_fastopen::queue::clamp_qlen`, then `cargo test -p net the_orphan` → `FAILED` (`left: Ok(Linger2(-5))`, `right: Ok(Linger2(-1))`); `CARGO_INCREMENTAL=0 cargo test -p net the_orphan` → `ok` with the SAME source. `main` (a90153f0f) is green both ways. Toolchain `nightly-2026-05-01`. | unclaimed |
| OPEN | med | `cargo test -p procfs` does not build at all (pre-existing on `main`): `cannot find 'live' in 'sched'` from `coredump_filter.rs`, because `procfs`'s `hosted` feature does not forward to `sched/hosted`. The tests only run under `cargo test --workspace`, where feature unification enables `sched/hosted` via another crate. A lane running the per-crate command reads this as its own breakage, and a `procfs`-only CI job would never run these tests. | `cargo test -p procfs` and `cargo test -p procfs --features hosted` both fail on `main` at `crates/kernel/procfs/src/coredump_filter.rs:21`; `cargo test --workspace` runs all 146 procfs unit tests. Fix is one line in `crates/kernel/procfs/Cargo.toml`: `hosted = ["sched/hosted"]`. | unclaimed |
| OPEN | med | **The `/proc/sys` ctl_table is target-gated, so no hosted test can assert a leaf exists, binds to the right variable, or carries the right mode.** `procfs::ctl` is `#[cfg(target_os = "oxide-kernel")]`, and `Node`/`Leaf`/`NET_SYSCTLS` with it. A `#[cfg(test)] mod tests` written inside `ctl/net_dir.rs` compiles out entirely and reports `ok` having run nothing — this lane wrote one, saw the workspace count NOT rise, and deleted it. So `net.ipv4.tcp_fastopen` and `net.ipv4.tcp_fastopen_key` being registered under those names, and the key file being 0600, are covered only by the kernel build and by the handler-level tests (`proc_handler_netstr::tests`), not by a test of the table itself. | Workspace test total unchanged at 12871 with the two `net_dir` tests present; the module never compiled. Same class as the `kernel_body.rs` phantom-test rule in `CLAUDE.md`. | unclaimed |
| OPEN | low | The fast-open enable bits are namespace state, but the path that reads them for `setsockopt` (`054_setsockopt/tcp.rs` `env_for`) is target-gated, so "the bits a socket is judged against come from ITS namespace" has no hosted test — only the decision (`set::admit` against a supplied `fastopen_sysctl`) and the namespace accessor (`tcp_fastopen::ns`) are covered, each on its own side of the shim. Same for `init_key_once` firing off `Effects::fastopen_keys`. | `crates/kernel/syscalls/src/054_setsockopt/tcp.rs:5` `#![cfg(target_os = "oxide-kernel")]`. | unclaimed |
| OPEN | low | The sysctl text form is STRICTER than the reference for one malformed shape: a hex group longer than 8 digits (`123456789-2-3-4`) is refused here, where the reference's `%x` scan consumes it and silently overflows. Every well-formed value, and the value the file itself prints, round-trip identically. Deliberate: an overflowing group names no key the writer intended. | `tcp_fastopen::keys::tests::a_write_that_names_no_four_groups_is_refused`, `…::short_groups_are_accepted_and_zero_extended`. | won't fix |
| OPEN | med | `net.ipv4.tcp_fastopen_blackhole_timeout_sec` still does not exist. It is the client-side blackhole-detection timeout, and nothing would consume it until the client deferral lands, so it was left out rather than shipped as a knob that stores and is never read. It arrives with `TCP_FASTOPEN_CONNECT`/`MSG_FASTOPEN` (piece 3 of the fast-open ladder). | Absent from `NetSysctlKey` and from `ctl/net_dir.rs`. | piece 3 |
| OPEN | med | Fast-open enable bits with no consumer yet, so they are NOT defined: `TFO_CLIENT_NO_COOKIE` (4) and `TFO_SERVER_COOKIE_NOT_REQD` (0x200). Both feed the "may fast open without a cookie" rule, which is `per-socket bit OR route metric OR sysctl bit` — the per-socket bit and the route metric already exist and are inert. A write of those bits to `net.ipv4.tcp_fastopen` is stored faithfully and read back, but decides nothing. They arrive with the cookie mint/verify (piece 2). | `tcp_fastopen::flags` defines only the three bits with callers. `route_metrics.rs fastopen_no_cookie` and `TcpOpts::fastopen_no_cookie` are still the inert halves. | piece 2 |
| OPEN | med | **A SYN that carries FIN alongside its fast-open data is accepted as data-only: the FIN is dropped.** The reference queues the SYN's payload and then runs the ordinary FIN handling on the child, moving it to CLOSE-WAIT with `rcv_nxt` past the FIN. Here the FIN is ignored, so the child stays SYN-RECV owing the peer an acknowledgement it will retransmit the FIN for. Not half-implemented on purpose: `tcp_state::transition` has no `(SynRecv, RecvFin)` arm at all, so the state machine cannot express it and adding one is a change to the ordinary passive path, not to fast open. | `crates/kernel/net/src/tcp_state.rs` has no `(SynRecv, RecvFin)`; `tcp_conn/io.rs` fast-open delivery consumes payload only. A fast open carrying FIN is rare (it needs a client that writes and shuts down in one call). | unclaimed |
| OPEN | med | **`TCP_DEFER_ACCEPT` and passive fast open do not compose.** A fast-open child is published to the accept queue at its SYN, while `defers_segment` still drops the bare acknowledgement that would complete its handshake — so a deferring listener that also fast-opens hands the program a connection whose handshake is then held open by the deferral for the full deferring period. The reference makes the fast-open path bypass the deferral. Not fixed here because the composition needs `defers_segment` to consult the child's fast-open state, which is a change to the defer lane's contract. | `stack/tcp_reqsk.rs defers_segment` does not look at `conn.fastopen_child`. No test asserts the composition either way. | unclaimed |
| OPEN | low | **The reset-penalty list stores expiry deadlines, not the request socks themselves.** The reference parks the whole request for 60 s so it holds a reference; here the request is already unhooked by the ordinary teardown path, so `FastOpenQueue` keeps only `now + RST_PENALTY_NS` per penalty. Observably identical — the overflow check only ever reads the head's deadline, pops it and decrements `qlen` — but a future consumer that wants to *identify* the parked requests will not find them. Deliberate: keeping a second, fast-open-only registry of request socks beside the connection table would be a split source of truth. | `tcp_fastopen/queue.rs Outstanding.penalties: VecDeque<u64>`; pinned by `server_tests::a_reset_connection_a_program_had_taken_keeps_charging_for_a_minute`, which walks the deadline to the nanosecond. | won't fix |
| OPEN | low | **The child-creation failure rung of the ladder cannot be exercised.** The reference can fail to allocate the child socket after a cookie verifies, and falls back to offering the current cookie with an ordinary handshake. Here the child already exists when the decision is applied — it is the entry the passive open built — so `Passive::Accept` cannot fail for that reason. The nearest failure, a full accept backlog, drops the child instead (`publish_fastopen_child`), matching what the ordinary passive path does with a backlog it cannot satisfy. | `stack/tcp_listener_deliver.rs publish_fastopen_child`. No test, because the state is unreachable. | won't fix |
| OPEN | low | **The fast-open counters the reference exports do not exist.** `TCPFastOpenPassive`, `TCPFastOpenPassiveFail`, `TCPFastOpenPassiveAltKey`, `TCPFastOpenCookieReqd` and `TCPFastOpenListenOverflow` each name a rung of the ladder that this PR now implements, and none is counted. They belong with the rest of the SNMP TCP counters rather than with fast open, which is why they are not added here. | Absent from the net SNMP tables; every rung they would count is test-pinned in `tcp_fastopen/server_tests.rs` instead. | unclaimed |
| OPEN | med | `net.ipv4.tcp_fastopen_blackhole_timeout_sec` still does not exist, and `TCP_FASTOPEN_CONNECT` still defers nothing. Carried forward from B1710 unchanged: this PR is the server half. `TFO_CLIENT_NO_COOKIE` (4) is now *defined* but still has no consumer — it is the client half of the no-cookie rule, and only the server half (`TFO_SERVER_COOKIE_NOT_REQD`) is read. | `tcp_fastopen::flags::TFO_CLIENT_NO_COOKIE` has no reader; `sock_opts::sol_tcp::apply` stores `fastopen_connect` and nothing consumes it. | piece 3 |
| OPEN | low | **The incremental-compilation miscompile B1710 hit did not recur, and the `#[inline]` mitigation on `clamp_qlen` was left in place.** This PR added a second cross-module helper in the same crate (`tcp_fastopen::cookie::gen`) and touched the neighbouring `sock_opts::sol_tcp` module; every test run here was `CARGO_INCREMENTAL=0`, and a final incremental run was clean too. Negative result: the bug is not reproducible from this PR's shape, so the mitigation is still the only thing standing between it and a recurrence. | `cargo test -p net` — 1824 passed, both with and without `CARGO_INCREMENTAL=0`. B1710's reproduction (reverting the `#[inline]`) was not re-run. | unclaimed |
| OPEN | med | **Two of the four blackhole-detection rungs the reference has are not expressible here, and are not implemented.** Implemented: the third consecutive retransmit timeout on a fast-opened connection, and an out-of-sequence reset on one that has received no data. Not implemented: (a) an out-of-order FIN arriving after `close`, and (b) the ofo-queue check that calls the path a blackhole when the only out-of-order segment is a bare FIN. Both need the out-of-order queue to record which segments carried FIN; `TcpConn::ooo_buf` is a `BTreeMap<u32, Vec<u8>>` of payload only, so a FIN sitting out of order is not represented at all. Adding it is a change to ordinary receive, not to fast open. | `crates/kernel/net/src/tcp_conn/types.rs` `ooo_buf`/`ooo_urgent` carry no flags; `tcp_conn/io.rs` drops a FIN's flag on the out-of-order path. The two rungs that ARE implemented are pinned by `tcp_conn::active_fastopen::tests::a_third_consecutive_timeout_on_a_fast_open_names_the_path_a_blackhole` and `TcpConn::fastopen_reset_is_blackhole`. | unclaimed |
| OPEN | low | **The blackhole reset does not exclude a loopback route.** The reference clears the recurrence count only when the confirming connection's device is not loopback, on the grounds that a loopback success proves nothing about the middlebox on the real path. Here `drain_client` resets on any confirming connection that received data. Deliberate: the alternative is threading the egress device into the drain, and a host whose only fast open is over loopback has no real path to protect. | `stack/tcp_fastopen.rs drain_client`; `tcp_fastopen::ns::tests::a_configured_timeout_makes_a_detection_pause_the_namespace` pins the reset itself, not the device rule. | unclaimed |
| OPEN | low | **`ClientCache` records `syn_loss`/`last_syn_loss_ns` and nothing reads them.** The reference records the same two fields and only exports them through the tcp_metrics netlink family, which does not exist here. They are kept because they are part of the cache's contract and because the netlink family will want them; `ClientCache::syn_loss` exists so the recording is at least observable from a test. | `tcp_fastopen/cache.rs`; `cache::tests::recurring_unanswered_fast_open_syns_are_counted_and_a_success_clears_them`. No production reader. | unclaimed |
| OPEN | med | **The socket-layer arms that carry out a deferral are target-gated, so no hosted test asserts that `connect` returns without a SYN.** The decision (`tcp_fastopen::client::decide`), the state it is fed (`sock::tcp_fastopen::plan`), and the mechanism it drives (`TcpConn::active_open_fastopen`) are each covered hosted. The hop between them — `connect_admission::commit` returning `Ok(())` on `TcpOpen::Deferred`, `commit_write` opening with the payload, `send_fastopen::send` choosing between `EINPROGRESS` and a byte count — is inside `#[cfg(target_os = "oxide-kernel")]` and can only be exercised by a boot. Same class as the B1710 row about `054_setsockopt/tcp.rs`. | `sock/connect_admission.rs:1`, `sock/send_fastopen.rs` (declared under `#[cfg(target_os = "oxide-kernel")]` in `sock.rs`). Workspace test total 6291 -> 6376 with none of those lines compiled by `cargo test`. | unclaimed |
| OPEN | low | **`connect(AF_UNSPEC)` on an unconnected TCP socket returns `EINVAL` where the reference returns 0.** Pre-existing and untouched: `SockKind::TcpInit` falls to `Disc::Bad` in `sock::ops::connect_admitted`. This PR does clear a pending fast-open deferral on that path, so a socket cannot be left holding a destination nothing will open to, but the errno is unchanged. | `sock/ops.rs` `RemoteAddr::Unspec` arm. Confirmed against the reference's `tcp_disconnect` on a `TCP_CLOSE` socket. | unclaimed |
| OPEN | low | **`TCP_INFO` is still mostly unpopulated, and this PR filled exactly two more fields.** `tcpi_data_segs_in` and the `tcpi_fastopen_client_fail` bitfield now carry real values because the client half produced them. The other ~40 zero fields (`tcpi_bytes_acked`, `tcpi_segs_out`, `tcpi_delivery_rate`, …) are untouched and still report zero. | `crates/kernel/syscalls/src/tcp_info.rs populate`. | unclaimed |
| OPEN | low | **A stamp written into `ClientCache` from a clock other than the one it is read with reads as stale, not as fresh.** `get`/`set` age entries with `now_ns.wrapping_sub(stamp_ns) >= ENTRY_TIMEOUT_NS`, so a stamp in the future underflows to a huge age and the entry reads as a miss. Safe in the direction that matters — a miss opens the ordinary way — and every production caller uses `crate::tcp_conn::ka_now_ns()`. Recorded because it cost a test failure that looked like a wiring bug. | `tcp_fastopen/cache.rs get`; `sock::tcp_fastopen::tests` originally stamped with a literal and read back with `ka_now_ns()`. | won't fix |
| OPEN | low | **The B1710 incremental-compilation miscompile did not recur.** This PR added five new cross-module helpers in `net` and touched `sock_opts`'s neighbours; every run here was `CARGO_INCREMENTAL=0`, and a final incremental `cargo test -p net` was clean too. The `#[inline]` mitigation on `tcp_fastopen::queue::clamp_qlen` is untouched and is still the only thing standing between the bug and a recurrence. B1710's reproduction was not re-run. | `cargo test -p net --features hosted` — 1901 passed, incremental and not. | unclaimed |
| Receive-side compile | none — the raw area was copied verbatim | `ipv4_options::area::build_packet`, the packet-present form of the one compile pass |
| Record-route slot | never filled | filled with the address this host answered on, pointer advanced |
| Timestamp slot | never filled | address and/or stamp written per flag nibble, pointer advanced |
| Timestamp overflow | never advanced | advanced when no slot is left; a counter at 15 is a header error |
| Malformed received area | delivered anyway | packet dropped |
| `IP_RETOPTS` | a second private parser in `cmsg::payload` | `ipv4_options::rx::echo` over the compiled area |
| Raw4 / ping option area | re-parsed out of the packet bytes | carried on the receive record, like the compiled area a delivered packet owns |
| The values | never recorded anywhere | a passive open records the opening IPv4 header's interface, hop limit and service class |
| Where they live | — | the multicast interface index and hop limit fields, plus a new `ip_rcv_tos` — the same three slots the reference reads them out of, so `IP_MULTICAST_TTL` on an accepted socket reports the same value it does upstream |
| `getsockopt(IP_PKTOPTIONS)` | `Ok(Bytes(vec![]))` on a stream socket | a control-message stream: `IP_PKTINFO`, `IP_TTL`, `IP_TOS`, each gated on the receive option that would have produced it per datagram |
| Datagram sockets | `ENOPROTOOPT` | unchanged, and the decision still lives in the value table, not the shim |
| cmsg encoding | one encoder in `recv_control::Control::copy_to` | one `encode_entry` shared by the receive cursor and this read, so the header, the truncation rule and the cursor advance are decided once |

## Keyring

Rows found by B1649 (`add_key`/`request_key`/`keyctl`). Retired rows: `fixed-issues.md`.

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| IN-PROGRESS B1667 | high | The `request_key` upcall has NEVER run against a real helper: there is no `/sbin/request-key` in the image (`userspace/` and `vendor/` carry zero keyutils references — `umh`'s own selftest uses that path *because* it is guaranteed absent). Every construction therefore ends `ENOENT` -> negate, which is indistinguishable from a box without keyutils installed. The kernel half is complete and exercised by a helper driving the real `keyctl` cores, but the exec path itself is unproven. Fix: ship keyutils in the image, then boot-verify one real construction. | B1649. `construct/tests.rs` drives 25 tests through an injected actor; none of them exec anything. | — |
| OPEN | high | Only the first servicing thread runs the `debug-umh` boot self-test, and no self-test covers a helper that submits a SECOND helper request while the first still runs — the pool growth that makes that safe is proved hosted, not on a booted system. Closing it needs a helper binary in the image that re-enters `request_key(2)`, which is the keyutils row above. | B1666. | — |
| IN-PROGRESS B1657/B1658/B1659 | med | `KEYCTL_DH_COMPUTE` **DONE (B1657)**: real `mpi` bignum crate + counter-mode derivation over the `crypt::Digest` table; the capability bit is now computed from the implementing module instead of a hand-kept list. Still EOPNOTSUPP: the `KEYCTL_PKEY_*` family (B1658, needs asymmetric-key parsing) and `KEYCTL_WATCH_KEY` (B1659, needs a `watch_queue`), whose capability bits stay clear until they land. | B1657: 16 `mpi` tests, 5 `crypt::digest`/`sha1` tests, 9 `keyring::tests::dh` tests incl. RFC 3526 group-5 known answers. | — |
| OPEN | med | `fork_keys` / `exec_keys` are verified by build and inspection only. Their call sites are in kernel-gated files (`056_clone.rs`, `exec_transition.rs`) with no extractable decision logic, so no hosted test proves either hook actually fires. `exit_keys` IS covered. Same phantom-test class as the `sock_v6.rs` row above. | B1649. | — |
| OPEN | low | No `/proc/sys/kernel/keys/gc_delay`. Deliberate: `Store::collect()` runs inline with no delay timer, so the knob would gate nothing — and a sysctl that reads back but changes no behaviour is the defect class this project bans. Add the knob only when a deferred gc exists. | B1649, negative result. | — |
| OPEN | low | A named session keyring is NOT joinable by default, including by its own owner: `find_keyring_by_name` checks Search WITHOUT possession, and the mask a named keyring is created with grants View/Read/Link only. So `keyctl session <name>` twice yields two distinct keyrings unless the owner widens the mask first. This matches the reference and is test-pinned, but it is surprising enough that a future lane will read it as a bug. | B1649, `a_named_join_without_search_permission_creates_rather_than_joins`. | — |
| OPEN | med | `KEYCTL_RESTRICT_KEYRING` still ignores its restriction STRING, so the `asymmetric` type is registered with `restrictable: false` and naming it is ENOENT. Linux accepts `builtin_trusted`, `builtin_and_secondary_trusted` and `key_or_keyring:<id>[:chain]` there, which is how a keyring is made to accept only certificates signed by a key it already holds. Registering the type as restrictable without parsing that string would have been a stub. Fix: teach `restrict_core` the restriction argument (it is not even copied from userspace today), then flip the type. | B1658. `ops/links.rs::restrict_core` takes `ty: Option<&str>` and no restriction; `types.rs` asymmetric row. | — |
| OPEN | low | The asymmetric key type carries no key-ID list, so `KEYCTL_SEARCH` cannot find a certificate by `id:<hex>` / `ex:<hex>` / `s:<hex>` the way Linux's `asymmetric_key_match_preparse` does — only by the exact description. The subject key identifier IS parsed (it is what the proposed description ends with), so the data is present; what is missing is the match-preparse hook and a place on the key to keep the three ids. | B1658. `pkey::x509::Certificate::skid` is parsed and used for the description only. | — |
| OPEN | low | **Deliberate deviation, safety:** `KEYCTL_PKEY_{ENCRYPT,DECRYPT,SIGN}` returns EOVERFLOW when the produced output is longer than the `out_len` the caller declared in `struct keyctl_pkey_params`. Linux instead re-sizes the copy from the KEY (`params.out_len = info.max_*_size`) and then `copy_to_user`s the produced length, so a caller that declares a short `out_len` — which passes the `out_len > max` check — has bytes written past its buffer. Reproducing that would be writing a user-memory overflow on purpose. Callers that size their buffer from `KEYCTL_PKEY_QUERY`, which is the documented flow, see no difference. | B1658, `keyctl/pkey.rs::eds`; the check is `produced.len() > out_len`. | — |
| OPEN | low | RSA private operations do not use the Chinese-remainder representation, so a decrypt/sign costs a full-width `d` exponentiation (roughly 4× the CRT cost). The primes ARE present in the PKCS#8 blob and are parsed past but not retained. Correctness is unaffected; this is throughput on `KEYCTL_PKEY_DECRYPT`/`_SIGN` only. | B1658, `pkey::rsa::parse_private`. | — |
| OPEN | med | The upcall proof has run on x86_64 only. `../images/out/lite-aarch64-root.img` predates the keyutils profile change, so `OXIDE_REQUEST_KEY_SMOKE=1 make smoke-arm` refuses (the injector checks for the helper and fails rather than producing an ambiguous ENOENT run). Command for the user: `cd ../images && ./build.sh rootfs lite aarch64`, then `OXIDE_QUICKBOOT_PROFILE=lite OXIDE_REQUEST_KEY_SMOKE=1 SMOKE_MARKER='REQUEST-KEY-PROBE' make smoke-arm`. | B1672. | — |
| OPEN | high | The upcall now EXECS the real helper, but a construction still does not COMPLETE: the probe exits non-zero after `/sbin/request-key` runs without complaint. The helper's own `Failed to retrieve callout info` error is gone (that was the special-id gap fixed here), so the remaining failure is downstream — the handler forks `sh` and `keyctl instantiate`, and the answer never reaches the requester. Next step: capture the probe's own message (the service now pipes it through `logger`, which needs a rootfs rebuild to take effect) and follow it into `instantiate_core`. Suspect the token's reachability from the helper's grandchildren, which is the same `fork_keys`/`exec_keys` coverage gap the curated ledger already records. | B1672. Two boots on the lite x86_64 image: BEFORE the fix `request-key: Failed to retrieve callout info (Required key not available)` at t=30.2s; AFTER the fix that line is absent and the service still exits 1 at t=31.0s. | — |
| OPEN | high | **Unknown filesystem parameters are silently ACCEPTED, so the option-support query the probe above guards always answers "yes".** After the `SET_FD` probe succeeds, the prober asks `fsconfig(fd, FSCONFIG_SET_FLAG/SET_STRING, key, ...)` and reads success as "this filesystem supports this option". Every `fsopen`ed context here uses one generic parameter path that pushes ANY flag/string key into the classic mount-option string and returns Consumed; no filesystem declares a parameter set, so no unknown key is ever rejected. The reference's converted filesystems reject an unknown parameter with EINVAL, which is exactly the signal being read. Consequences observed in the desktop image: `ProtectProc=`/`ProcSubset=` are enabled for services because `proc` claims `hidepid` and `subset` — our procfs ignores its mount data entirely, so those confinements silently do nothing; cgroup2 delegation/`memory_recursiveprot` options and tmpfs/ext4 `usrquota` are claimed the same way. Security-relevant: a confinement userspace believes it applied is absent. | Generic path: the only parameter-ops implementation is the classic-mount one, whose flag/string arm stores the key unconditionally; the fs types are registered with option parsers that ignore unrecognised data (`proc`, `sysfs`, `cgroup2`, `devpts`, and the rest ignore mount data outright). Not fixed here: making the parameter set authoritative changes which mounts succeed, and this lane has no boot access to verify a rootfs/tmpfs mount does not start failing. Needs a lane that can boot both arches. | — |
| OPEN | med | A listed-but-ignored parameter is still a false claim of support: any key a table lists that the filesystem does not honour keeps the option-support probe lying about that key. Applies to every option of the 17 filesystems that discard their data string. Each needs an implementation or omission from the table — the choice cannot be made silently. | Same registry listing. | — |

## Desktop

Found by B1663 driving the two user-reported app crashes on a live-gnome x86_64
boot (`debug-boot,debug-execload`, smp=2, kvm, autologin session `c1` on
`seat0`, apps launched from the `systemd.debug_shell` serial root shell via
`setpriv --reuid=1000` with the session's wayland/dbus env).

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| OPEN | med | **Serial tty echo duplicates a character periodically.** Input is delivered to the shell correctly (commands execute as typed), but the ECHO inserts one extra copy of a character roughly every ~40 bytes: `grep -E` echoes as `grep --E`, `/run/user` as `/run//user`, `timeout 60` as `timeout 660`, `env` as `ennv`. Cosmetic on a shell, but it is a real n_tty echo-path defect and makes serial transcripts untrustworthy as a record of what was typed. | B1663, dozens of instances across one session; verified echo-only by the fact that `/usr/bin/gnome-conntrol-center` (as echoed) still exec'd successfully. | — |
| NOT-A-DEFECT B1668 | med | Curated `## Desktop` row "`timerfd_settime` returns EINVAL to gnome-shell for `flags=3` (`TFD_TIMER_ABSTIME\|TFD_TIMER_CANCEL_ON_SET`)" is closed — the flag pair is NOT what we reject. `TFD_SETTIME_FLAGS` is exactly that pair, `timerfd_settime` admits it end to end, and `TFD_TIMER_CANCEL_ON_SET` is fully implemented (realtime-generation watcher, `ECANCELED` on read / on re-arm / on `TFD_IOC_SET_TICKS`, wall-step reprojection). After the flag mask the only EINVAL left on the path is the `itimerspec` import — a `tv_sec < 0` or a `tv_nsec` outside `[0, 1e9)` in either member — and the reference rejects both identically with the same ordering (value before fd lookup). The caller passed an invalid value; the guest wall clock was correct at the time (`Aug 1 14:19:39` in the same log), so this is not a clock-derived miscomputation on our side either. | B1663 boot log `[38.907] [EINVAL nr=286 tid=4463 a0=0x30 a1=3 ... fdpath=anon_inode:[timerfd] comm=gnome-shell]` — one occurrence in the whole session. `a0` is the fd and `a1` the flags, and the fd resolved to a real timerfd, so the `i_private` EINVAL is excluded too. Pinned by `fs::timerfd::tests::abstime_cancel_on_set_pair_is_admitted_and_the_residual_einval_is_the_value` and `::cancel_on_set_needs_abstime_and_a_realtime_clock_but_never_fails_the_call`. | — |
| OPEN | med | UART TX is unserialized: `drv_serial::emit` polls THRE and writes THR with no lock, and is driven concurrently by klog (any context), the N_TTY echo (flush worker, port lock held, IRQs off) and `TtyStruct::write` (task, `tx` lock held, port lock released). Linux serializes every UART TX with the port lock. Two emitters can both observe THRE=1 and write THR. Prime suspect for the echo-duplication row above. | Read of `drv-uart-16550/src/lib.rs::emit` + `tty/src/core/tty.rs::write` (detached-sink path) vs `core/tty/rx.rs::receive_from_driver` (inline path). Needs a boot lane to confirm; not changed here because the console is the device every lane's boot depends on and this lane cannot boot. | — |
| OPEN | low | `set_baud` toggles DLAB and writes the divisor latch at `base+0`/`base+1` with no exclusion against a concurrent `emit`, and readline issues TCSETS on every prompt. A byte written to THR inside that window lands in the divisor latch instead (lost byte + wrong baud). | `drv-uart-16550/src/lib.rs::set_baud`; `SerialTtyDriver::set_termios` calls it on every TCSETS with a non-zero `c_ospeed`. | — |
| OPEN | low | N_TTY echoes `\n` as a bare `\n` rather than through output processing, so ONLCR does not apply to the echoed newline (Linux echoes via the output path, producing `\r\n`). Not the duplication defect; found while proving the echo path. | `ldisc/n_tty/state.rs::echo_byte`. | — |
| OPEN | med | No `NT_X86_XSTATE` is produced for x86-64 even though the note is plumbed: `CoreThread::xstate` is emitted verbatim when supplied and nothing supplies it. Emitting a wrong `xsave` header would make gdb decode garbage registers, so the note is left absent until the FPU lane can hand over a real save area with its `xstate_bv`/`xcomp_bv` intact. | `push_fp_notes` in `crates/kernel/fs/src/coredump/elf/notes.rs`; `xstate: None` at every call site. | — |
| OPEN | low | `NT_FILE` carries no device or inode number — the format has room only for start, end and page offset per mapping, then the path. A debugger matching a dumped library to an on-disk build therefore relies on the path alone; a file replaced between the crash and the analysis is indistinguishable. This matches the reference format and is not a defect in the builder, recorded so a later lane does not "fix" it by inventing fields. | `files()` in `crates/kernel/fs/src/coredump/elf/notes.rs`; layout asserted in `tests/files.rs`. | — |
| OPEN | low | A mapping whose contents cannot be read is written as zeroes rather than reported. A reader returning 0 for a resident page is indistinguishable in the image from a genuinely zero page. Linux behaves the same way (a skipped range becomes a file hole), so this is faithful, but it means a dump cannot tell an operator that memory was lost. | `push_segment_data` in `crates/kernel/fs/src/coredump/elf/build.rs`; `tests/segments.rs::a_hole_is_zero_filled_rather_than_shortening_the_segment`. | — |
| OPEN | MED | The socket (`@`) destination still delivers nothing. Unchanged by this lane — noted because a pattern beginning with `@` silently produces no dump and no file. | `coredump/current.rs` `CoreKind::Socket => {}` | unowned |
| OPEN | LOW | The file destination resolves from the crashing process's namespace root for BOTH the ordinary and the privilege-downgraded case. The reference uses the initial task's root for the downgraded case specifically, so a user namespace that repointed the root could still steer a downgraded dump within that root. The absolute-path rule and the no-unlink rule still apply. | `coredump/file_target.rs::lookup_dir` uses `vfs::mount::current_ns()` | unowned |
| OPEN | MED | `resourceN` is a text triple, not the mmap-able BAR window the reference exposes. A userspace driver that mmaps `/sys/bus/pci/devices/<addr>/resource0` gets a text file instead of the device window. Unchanged by this lane. | `bus/device.rs` `PCI_RESOURCE_ATTRS` render `resource_body` | unowned |
| OPEN | MED | The config-space accessor is dword-addressed with an 8-bit register offset, so only the 256-byte conventional space is reachable. Extended PCIe config space (and therefore `serial_number`/DSN and the extended capability list) cannot be read; those attributes are absent rather than wrong. Sub-dword writes are read-modify-write on the containing dword, which re-emits neighbouring write-1-to-clear bits. | `pci::ConfigSpaceReader::read32(bdf, offset: u8)`; `pci::config_space::write_bytes` | unowned |
| OPEN | LOW | `dma_mask_bits`/`consistent_dma_mask_bits` report the PCI default 32 for every function because no per-device DMA mask is tracked anywhere in the tree. A driver that would set a 64-bit mask is not represented. | `bus/pci_attrs/show.rs` `DEFAULT_DMA_MASK_BITS` | unowned |
| OPEN | LOW | Bridge windows are not modelled as resources, so a bridge's `resource` table shows the same seven endpoint rows rather than the reference's bridge-window rows. | `bus/pci_attrs/show.rs::resource_table` | unowned |
| OPEN | LOW | `/tmp` on the dev box filled to 32/32 GiB mid-lane and every Bash call failed with ENOSPC. Cause: the ext4 e2fsck tests write ~5.3 GB images to FIXED `$TMPDIR` names (`balloc_*_repro.img`, `arm_hwdb_*_repro.img`) and never clean up; the images grew from ~1.8 GB when the GNOME profile went 15 → 1151 packages, so four leftovers now hold 21 GB. Not caused by this lane; recorded because it will recur and it presents as unrelated tooling failure. | `df -h /tmp` → `32G 32G 0 100%`, no process holding the files | infra |


## Memory / page cache

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| OPEN | low | `cachestat`'s `nr_writeback` is structurally 0 on both page-cache backends. Not a missing counter: a flush copies frames to the device synchronously inside the requesting call, so no index is ever left tagged writeback-pending for another task to observe. It becomes a real gap only if writeback is ever made asynchronous — at which point the tag has to be added to the ext4 frame store and reported here. | F778; `crates/kernel/ext4/src/rootfs/framecache/cachestat.rs`. | — |
| OPEN | low | `cachestat` eviction shadows exist only for ext4 (the clean-page shrinker) and shmem (swapped indices). A page dropped by `truncate`/hole-punch correctly leaves no shadow, matching Linux, but there is no shadow-entry cap: a workload that repeatedly evicts and truncates a very large sparse file grows the shadow map without bound until the inode is dropped. Linux bounds this by reclaiming shadows with the inode's own shrinker. | F778; `Ext4FrameStore.shadows`. | — |
| OPEN | med | `git stash` is shared across ALL worktrees of this clone, so two lanes stashing/popping concurrently swap trees: B1654's stash was popped into another lane's worktree and a raw-IP lane's WIP landed in B1654's. Recovered via `git fsck` dangling commits + `git stash store`. Lanes must never use `git stash` on this box; commit temporarily on the lane branch instead. | Observed 2026-08-01: `git stash pop` in the B1654 worktree restored 16 unrelated `raw4`/`sol_ip` files and dropped B1654's tracked edits. Damage still visible in `git stash list`. | — |
| OPEN | low | Only RSA is implemented. An ECDSA/EdDSA/ML-DSA certificate parses as far as its algorithm identifier and is then ENOPKG, which is the answer Linux gives for an algorithm it was not built with — but it IS absent surface, not a config answer. The `enc=x962`/`p1363` encodings have no meaning here for the same reason. | B1658, `pkey::oid::pkey_algo` maps only `rsaEncryption`. | — |
| OPEN | med | The watch queue holds each note as its own allocation instead of a preallocated note array, so `IOC_WATCH_QUEUE_SET_SIZE` reserves a COUNT rather than memory. The syscall-visible contract is identical (same depth, same rounding to a whole page of notes, same EBUSY on a second call, same loss behaviour when full), but a queue's memory is charged as it fills rather than up front, and the pipe-buffer accounting Linux applies (`account_pipe_buffers`, the unprivileged EPERM when a user is over the soft/hard buffer limits) has no equivalent here. A local unprivileged user can therefore hold more notification memory than Linux would allow. Fix with the pipe-buffer user accounting, which does not exist in this kernel yet for ordinary pipes either. | B1659, `watch_queue/queue.rs::set_size`. | — |
| OPEN | low | `../images/build.sh` was uncommitted-broken in the shared checkout: a comment added to the `unshare ... bash -c '...'` block contained an apostrophe, which closed the single-quoted script early — every `./build.sh rootfs ...` died with `line 113: unexpected EOF while looking for matching '`. Reworded in place (not committed; the images checkout carries a large amount of another party's uncommitted work). | B1672, reproduced with `bash -n build.sh`. | — |
| OPEN | med | A private file mapping's modified (copied) pages are indistinguishable from its clean ones: file-backed mappings carry no private anonymous rmap, so the written-to rung never fires for them. Under the default filter such a mapping is reduced to its header page and the process's modifications are lost from the dump. | `describe_vma` can only set `anon_vma` for `VmaBacking::Anonymous`; `Vma::new_with_may` allocates an `anon_vma` for anonymous backings only. | — |
| OPEN | med | io_uring and aio ring mappings use a kernel-frame backing, so they classify as "private with nothing behind it" and are skipped. Linux dumps them under the file-backed shared class. | `syscalls/src/io_uring/ring.rs` and `syscalls/src/aio/ctx.rs` map with `VmaBacking::KernelFrame`. | — |
| OPEN | low | An anonymous mapping whose pages have all been evicted reports zero resident pages and is treated as never written to, so it is skipped under the default filter. | `describe_vma` derives the written-to property from the VMA's resident count. | — |
| OPEN | low | Always-dump detection for the kernel-provided mappings keys on the address space's vDSO image address and the data page below it. A vDSO built with more than one loadable segment would have only the segment containing the image address force-dumped. | `describe_vma` tests `vdso_base` containment; `syscalls/src/vdso.rs` maps one mapping per `PT_LOAD`. | — |
| OPEN | med | The residual flake in that invocation is `net`'s, not this crate's: `udp_gro_endpoint_tests::a_run_stops_at_the_segment_cap_and_the_next_datagram_starts_another` failed once in 30 runs, and `stack::ethernet::tests::ethernet_ingress_rejects_a_truncated_link_header_before_l3` panics at `net/src/stack/core.rs:193` on an `unwrap()` of the loopback's own address row. Root cause identified, NOT fixed here: `hosted_fixture::init_net_domain()`'s `Drop` calls `iface_addr::restore_ns(0, ...)`, which replaces ALL of namespace 0's rows — wiping rows written by a concurrent test that did not take the guard. `register_loopback()` keys every hosted `NetStack` into the one shared initial namespace, and 165 call sites reach it while only some files take the guard. The disjoint-`NetIfaceId`-block fix in `netdev.rs` does not help, because `restore_ns` is namespace-wide, not iface-scoped. | Own lane; see the comment already in `netdev.rs:215`. | — |
| OPEN | high | **`hidepid=` / `subset=pid` are NOT a parameter-table problem — procfs cannot express them at all.** Of the 22 registered filesystem types only 5 (`tmpfs`, `ramfs`, `ext4`, `autofs`, `fuse`) even receive the mount data string; the other 17, `proc` included, take `_` and discard it. And `procfs::static_files::proc_root()` returns a **process-global cached `Arc`**, so every `mount -t proc` in every namespace shares ONE root inode, leaving nowhere for per-mount option state. Per-mount `hidepid=` needs per-superblock root inodes first. **Consequence for the security framing: listing `hidepid` in a parameter table would NOT make `ProtectProc=` work — only implementing it does. Conversely a table that OMITS `hidepid` makes the support probe answer "no", and systemd then correctly stops claiming a confinement it is not getting.** Omission is the cheap honest fix; implementation is the real one. | `crates/kernel/syscalls/src/fsmount_common/registry.rs:118` (proc ctor discards data); `crates/kernel/procfs/src/static_files.rs:73-76` (cached singleton root). | B1686 |
| NOT-A-BUG | low | `socketpair(AF_UNIX, SOCK_RAW)` succeeds for an unprivileged caller while `AF_INET`/`AF_PACKET` `SOCK_RAW` return EPERM. Looks like a missing capability screen; it is not. | Verified against the reference: the screen belongs to each family's own create operation, and only the INET pair and AF_PACKET install one. `unix_create` accepts SOCK_RAW from any caller and remaps it to SOCK_DGRAM; AF_NETLINK's NORMAL type IS SOCK_RAW. A family-independent SOCK_RAW screen would break every netlink socket in userspace. Pinned by `net::socket_args::tests::only_the_inet_families_and_af_packet_screen_raw_sockets`, whose comment states the trap. | — |
| OPEN | low | `net` lib tests: one intermittent failure observed in a single run during C246, clean on 3 immediate re-runs. Consistent with the already-filed parallel-execution instability in the curated ledger, not a new defect. | 1538/1538 on three consecutive runs after the one-off. Recorded so the observation is not mistaken for a C246 regression. | — |
| `syscalls/src/net_common.rs` | 26 |
| OPEN | low | `process_mrelease` cannot report `EINTR` or `EAGAIN`. Upstream returns `EINTR` when the killable mmap read-lock is interrupted and `EAGAIN` when the reaper cannot take that lock; this kernel's reap walk takes an uninterruptible VMA guard and cannot fail, so neither state exists to report. A caller that retries on `EAGAIN` is not misled — it never sees one — but the two errnos become real once the reap is made lock-failure-tolerant. | F780; `crates/kernel/syscalls/src/448_process_mrelease.rs`. | — |
| OPEN | low | `process_mrelease`'s mm-sharer scan is O(all tasks) per call: it walks the registry snapshot to find CLONE_VM peers outside the thread group, because there is no per-mm task list. Correct, but it makes a syscall meant for OOM-response paths scale with total process count. A back-pointer list on `AddressSpace` would make it O(sharers). | F780; `sys_process_mrelease`. | — |
| OPEN | med | The 66 new rows are `NEEDS-AUDIT`, meaning a route exists but NO semantic audit against the Linux contract has been performed. This is not a backlog of small items: it includes `read`, `mmap`, `clone`, `execve`, `fcntl`, `ptrace`, `prctl`, `pipe`, `nanosleep`, `accept4`, `getrandom`, `memfd_create` and `execveat` — several of them among the highest-traffic entry points in the tree. Owner/deps/model columns are `-` by design and the auditing lane fills them. | F784. Full list is the 66 rows carrying branch `F784-matrix-untracked-syscalls`. | — |
| OPEN | med | This branch enables the hardware and switches the register, but no PTE carries a protection key yet, so nothing userspace does can be denied by one. `pkey_alloc` still reports the no-OSPKE answer because `PkeyArch::X86_64` has not been re-derived from `arch_max_pkey()`. Both close in the fault-classification + syscall-semantics lane (F787); until then the row stays PARTIAL and this PR changes no user-visible behaviour. | F785, by construction. | F787 |
| OPEN | low | `memfd_create(MFD_HUGETLB)` returns ENOSYS because there is no hugetlbfs in this kernel at all — consistent with `mmap(MAP_HUGETLB)` returning EINVAL and with `cachestat`'s unreachable hugepage EOPNOTSUPP leg. This is the answer an unconfigured hugepage build gives, not a stub, but it IS absent Linux surface: a real distro kernel has `CONFIG_HUGETLBFS=y`. Closing it means a hugetlbfs lane, not a memfd change. | F783; `319_memfd_create.rs` hugetlb leg. | — |
| OPEN | low | `memfd_wait_for_pins` has no counterpart: `F_ADD_SEALS(F_SEAL_WRITE)` checks `mapping_deny_writable` (writable shared mappings, EBUSY) but not long-term page pins, because nothing in this kernel long-term-pins a page. Truthful today. It becomes a real hole — data written behind a write seal — the moment a GUP-style pin (io_uring registered buffers over shmem, zero-copy RX) can outlive the syscall that took it. | F783; no `pin_user_pages` equivalent exists (grep clean). | — |
| OPEN | low | `security_inode_init_security_anon` has no counterpart anywhere in the kernel, so memfd (and every other anon inode) is created without an LSM labelling hook. Not memfd-specific and not observable without an LSM that labels inodes; recorded so the anon-inode creation sites are known when one lands. | F783; grep for `inode_init_security` returns nothing. | — |
| OPEN | low | `AT_HWCAP2`'s `HWCAP2_POE` bit is not published, so a userspace library probing the auxv for permission-overlay support sees nothing even once the overlay is on. glibc's `pkey_get`/`pkey_set` already emit the raw `POR_EL0` encodings, so they will work; anything gating on the hwcap will not. Closes with the syscall-semantics lane. | F786; no `POE` bit in `isar0_hwcap()`/hwcap publication. | F787 |
| OPEN | med | Same as the x86 lane: this branch enables the hardware and switches the register, but no descriptor carries a Permission Overlay index yet, so nothing userspace does can be denied by a key, and `pkey_alloc` still reports the feature-absent answer. Closes in F787. | F786, by construction. | F787 |
| OPEN | med | `SEGV_PKUERR` is now reported on x86 from the authoritative `PF_PK` error-code bit, but `si_pkey` is still passed as 0 — `force_sig_fault`'s fourth argument. Filling it needs the VMA's key, which needs the PTE/VMA plumbing that is the remaining half of this lane. A handler reading `si.si_pkey` therefore sees 0 rather than the key that denied it. | F788; `mm-pmm/src/user_as/signal.rs` `raise()`. | this lane |
| OPEN | med | No descriptor or PTE carries a protection key yet, so `PF_PK` cannot actually be raised by this kernel and the new `SEGV_PKUERR` path is unreachable in practice. Remaining lane-3 work: `hal::PageFlags` (or the leaf packer) carrying a key on both arches, `Vma` gaining a key field, `pkey_mprotect` writing it, `PkeyArch` re-derived from `arch_max_pkey()`, and `arch_set_user_pkey_access` wired to the real registers. | F788, by construction. | this lane |
| OPEN | low | **`linux_alloc::free_bytes` and `linux_page_phys` have the same safe-fn-derefs-raw-pointer shape** as the functions tightened above, but their call sites live in `linux_alloc_cache.rs`, `linux_alloc_vmalloc.rs` and `linux_dma.rs`. They now carry comments naming the KPI contract (`kfree`'s caller must pass an `alloc_bytes` pointer; the header sits immediately below the returned pointer) instead of claiming an invariant the code enforces. Conversion needs a lane that may touch those three files. | `crates/kernel/modules/src/linux_alloc.rs`. | unclaimed |
| OPEN | MED | Nothing enforces a LOWER bound on a device's `queue_size`. `drv-virtio-vsock::rx::prepost_all` assumes `rxq.size >= RX_RING_BUFS` (8); `drv-virtio-snd::submit_ctl` uses descriptor slots 0-1 and `tx_period`/`rx_period` slots 0-2; virtio-blk's sync path uses slots 0-2; virtio-gpu uses 0-1. `program_queue` clamps only the upper bound. Not memory-unsafe — the ids stay inside the 4 KiB descriptor frame — but publishing descriptor ids >= `size` is a protocol violation a conforming device may reject. | `virtio/src/queue_cfg.rs::program_queue` accepts any nonzero size | unowned |
| OPEN | MED | Not yet attributed: the combined x86 boot exercising `swapfile_probe` + `request_key_probe` hit `[CPU-STALL] cpu=0 no heartbeat for 10s (seen by cpu=1) last: tid=4181 syscall=fsync ... now: tid=4181 syscall=madvise` on attempt 1. That is the swapfile probe's `MADV_PAGEOUT` ladder. The C probe issued the same `MADV_PAGEOUT`, so it is most likely pre-existing rather than caused by the port — but the A/B that would prove it has NOT been run, and this row must not be read as either a new regression or a cleared one. | `target/smoke-x86.out`, boot attempt 1; `[NMI-BT] rip=ffffffff8043d104` | unowned |
| 283 (high) | `write_for_current` passes an empty mapping list and a zero register block | The selection ladder runs over `mm.snapshot_vmas()`; the register block comes from the thread's own entry frame, threaded in from the return path that tears the process down |


## Filesystem / mount

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| OPEN | med | Notification records are delivered only for KEY events. Linux also posts `WATCH_TYPE_MOUNT_NOTIFY`/`WATCH_TYPE_SB_NOTIFY` from the mount and superblock code in trees that carry those patches, and the generic `watch_queue` device (`/dev/watch_queue`) is absent here too — a notification pipe is reachable only through `pipe2(O_NOTIFICATION_PIPE)`. The mechanism is general (types are a filterable field and the filter code accepts any type below `WATCH_TYPE__NR`); what is missing is other subsystems calling it. | B1659; `WATCH_TYPE_NR` is 2. | — |
| `procfs` | 3/10 | OPEN |
| OPEN | high | Long tail of the same root cause, one owner per package: `modules` (`linux_string`/`linux_runtime`/`linux_pm` `export_symbols_registers_*_surface` — a global symbol registry), `procfs` (`devices_body_*` — global input records), `klog` (`concurrent_emitters_do_not_splice_lines`), `netlink` (`genetlink::tests::quota::*`), `drv` (`bus::tests::device_index::*`, `model::tests::hooks::*`), `sysfs` (`block::tests::block_uevent_*`), `nscg` (`proc_ns::setns_perm_tests::*`), `sound`, `fbdev` (`fb_ops_are_per_instance`, `register_count_roundtrip`), `drv-zram`, `drv-virtio-gpu`. Each is a process-global registry with several tests mutating it and either no lock or a per-module lock that excludes nothing. | Failing test names captured per run. | — |
| OPEN | low | cgroupfs control-file inode numbers pack `(cgid << 8) | slot` into a region 2^27 wide, so the packing wraps for cgroup ids ≥ 2^19 and two cgroups' control files then share inode numbers (and file handles). Directory numbers invert across the full 2^24 region. Pre-existing; pinned by `the_inode_number_derivations_invert`, which asserts both the working range and the wrap. | `cgroup::ids` + `vfs::pseudo_ino::{CGROUP_DIR, CGROUP_FILE}`. | — |
| OPEN | low | sysfs is kernfs-backed in Linux and exports the same 8-byte handle, but oxide's sysfs still reports `export_can_decode_fh() = false` (its inodes are synthesized from live kernel objects with no id→object inverse). `name_to_handle_at` on `/sys` is EOPNOTSUPP where Linux succeeds. Not a live blocker — no observed consumer — but it is the same defect class this branch fixed for cgroup2. | `sysfs::SysfsSuperOps::export_can_decode_fh`. | — |
| NOTE | info | `mm-pmm` `debug-fwm` / `debug-mount` diagnostics read a user VA through the HHDM alias of a present leaf rather than through `uaccess`; sound because the leaf is read out of the live root first, but it is the same shape as the two rows above and would break if a caller ever passed an address whose page could be reclaimed mid-read | `mm-pmm/src/user_as/debug.rs` `dump_u64_at` + the WATCH_VA read. Both are single-feature debug hooks with a fixed 8/16-byte read at a known page offset; documented in place rather than rewritten. | unassigned |
| OPEN | med | **tmpfs accepts 15 of its 22 declared parameters without acting on them.** `TMPFS_PARAMS` lists what the reference accepts; `mount_opts.rs` honours only `mode`/`uid`/`gid`/`size`/`nr_blocks`/`nr_inodes`. `huge`, `mpol`, `inode32`, `inode64`, `noswap`, `casefold`, `strict_encoding` and the six quota-limit keys are admitted and dropped. Listing them is deliberate — omitting one would make a mount the reference accepts fail — but a `mount -o noswap` still swaps. | `crates/kernel/fs/src/tmpfs/params.rs`, `crates/kernel/fs/src/tmpfs/mount_opts.rs` | — |
| OPEN | med | **ext4 accepts its whole 90-name parameter table and acts on the quota family only.** `data=`, `errors=`, `commit=`, `barrier`/`nobarrier`, `discard`, `delalloc`, `noload`/`norecovery`, `block_validity`, `journal_ioprio=`, `max_dir_size_kb=` and the rest are admitted and dropped by `Ext4Mount::open_with_data`. `-o errors=remount-ro` in particular means the filesystem does NOT remount read-only on error. | `crates/kernel/ext4/src/rootfs/params.rs`; `open_with_data` consumes the quota keys only | — |
| OPEN | med | **autofs and fuse admit their full tables but their constructors re-parse the blob themselves.** `AutofsFs::new(data)` and `fuse::mount_from_data(data)` each scan the option string a second time. Admission and the backend now agree on WHICH keys are legal, but the backend still owns value parsing, so a value admission accepts can still be rejected (or silently defaulted) one layer down. | `crates/kernel/fs/src/autofs.rs`, `crates/kernel/fs/src/fuse/fs.rs` | — |
| OPEN | low | **A `mount(2)` LSM option (`context=`, `fscontext=`, `defcontext=`, `rootcontext=`) is refused by any filesystem that publishes a table.** The reference lets the security layer eat those names off the blob before the filesystem table is consulted; nothing installs an `FsContextSecurity` on the `mount(2)` path here, so they fall through to the table and are reported unknown. Matches a reference kernel built with no LSM, and no boot mount passes one — but it will bite the first caller that does. | `crates/kernel/vfs/src/fs/fs_context/flow.rs` `vfs_parse_fs_param` security rung | — |
| OPEN | `docs/26` R84 recorded a known gap kept out of the body: `getuid` family, `stat`/`chown` owner munging, `setresuid`/`setresgid` bound checks and `/proc` cross-ns visibility still operate on host ids with no user-ns translation step, so `26§2` invariant 6 is not enforced at those call sites. Engine + procfs views are complete; the call-site wiring is not. | deleted `26` R84 block; `crates/kernel/user-namespace` has the engine, callers do not use it |
| OPEN | high | `fsmount(2)` returns an anon-inode fd over a deferred mount object, not an `O_PATH` fd over a real vfsmount in an anonymous mount namespace. The fd carries `mnt_id == 0`, is `FileType::Regular` so it is `ENOTDIR` as a dirfd, is invisible to `statmount`/`listmount`, and between `fsmount` and `move_mount` the mount exists nowhere and can be neither observed nor referenced. `FSMOUNT_NAMESPACE` is unimplementable until that changes. Also missing: the `SB_NOUSER → EINVAL` check after mount creation, and `FMODE_NEED_UNMOUNT`/dissolve-on-close semantics. | `fsmount_common/fd.rs` `install_fd` → `vfs/src/file/model.rs` `File::new` (`mnt_id = 0`); `pathresolve/at.rs` rejects a non-directory dirfd. `move_mount` mode (a) grafts the superblock through a private-inode side channel instead. Blocks `432`. | unowned |
| OPEN | med | `sendmsg` on a UDP socket does not check the IMPLICIT local port an autobind settles. Upstream asks for the socket's bind right whenever a send forces the socket to acquire a source port, in the same hook that checks the explicit destination. F791 wired the destination half (`ACCESS_NET_CONNECT_SEND_UDP`, `Op::Send`); the autobind half has no counterpart here, so a sandbox that names only `ACCESS_NET_BIND_UDP` ports cannot confine which source port an unbound datagram socket takes. | F791. `socket/src/send.rs` `prepare` calls `net::landlock_addr::check_send_addr` once, for `message.name` only; nothing asks for a bind right on the send path. | — |
| OPEN | med | `29§2` invariants 1, 3, 4 still describe an initramfs CPIO root with `pivot_root` to the real root, while `51§3` (and the actual boot) mount an ext4 root image and exec `/sbin/init` directly. Pre-existing contradiction, not caused by the musl story; left alone to keep this PR to one item. | `29§2` invariant 1 "Kernel exec's `/init` … from initramfs" vs `51§3` step 2 "kernel mount ext4, find `/sbin/init`". | — |
| low | `bio_add_page`'s fallback arm (page descriptor does not resolve) silently ignores `off` and points the bio at the owner's bounce buffer from offset 0. The bound is now honest, but the data is not the caller's page. The shim's `LinuxBio` has no `bi_io_vec` array, so it cannot represent a real multi-page bio at all. | `core/bio.rs`, the `page_data.is_null()` arm. |
| high | `hidepid=` / `subset=pid` are not a table problem — procfs cannot express them at all; `proc_root()` is a process-global cached `Arc`, so every `mount -t proc` in every namespace shares ONE root inode. | Per-superblock procfs root inodes are a procfs-identity change, not a mount-API change: nothing in this PR's mechanism touches where per-mount state lives. It needs its own lane, and it interacts with `scratch/pseudo-inode-identity.md`. Unchanged here: `mount -t proc -o hidepid=2` still returns EINVAL, which stays the honest answer until the confinement is implemented. |
| high | `fsmount(2)` returns an anon-inode fd over a deferred mount object, not an `O_PATH` fd over a real vfsmount in an anonymous mount namespace. | Requires anonymous mount namespaces and a real detached `vfsmount` — a mount-namespace change of comparable size to this whole PR, sharing no code with the parameter mechanism. Own lane. |
| low | A `mount(2)` LSM option (`context=`, `fscontext=`, `defcontext=`, `rootcontext=`) is refused by any filesystem that publishes a table. | Re-verified against the reference: the LSM rung returns "not my parameter" when no security module claims the key, and the option then falls to the filesystem table and is reported unknown. We register no module that claims them, so EINVAL is the correct answer for this configuration. It becomes a defect the moment a module that parses them is registered, not before. Row should be reworded rather than closed. |


## Process / exec

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| OPEN | med | A position-independent image with no interpreter keeps the kernel-owned backing for all of its segments: the loader applies its `R_*_RELATIVE` entries before it runs, so those bytes differ from the file and cannot be a mapping of it. The reference kernel applies none of them — such an image relocates itself from its own entry point — so the durable fix is to stop pre-applying and confirm the userspace images self-relocate. | `layout::relocs_precede_file_backing`; `only_a_biased_dyn_image_the_loader_relocates_loses_its_file_backing`. | — |
| OPEN | low | `shmctl(IPC_SET)`, `SHM_LOCK`/`SHM_UNLOCK` and the `IPC_RMID` mark path mutate the segment through `Arc::get_mut` and return `EAGAIN` when any other reference exists (a concurrent `shmat`/`shmdt` holds one). `EAGAIN` is not a Linux errno for those commands. Pre-existing; F794 did not widen it but did add a fourth `get_mut` field write (`key`). | `crates/kernel/ipc/src/sysv_shm/shmctl.rs` `set_segment` / `lock_segment` / `rmid_segment`. Needs the mutable fields moved to atomics or the whole segment behind one lock. | — |
| med | `blk_execute_rq` has no wait primitive. It is synchronous for every completion path this shim drives, but a driver whose `queue_rq` completes asynchronously would return before the completion runs. That case returns `BLK_STS_IOERR` and deliberately leaks the small `SyncWait` record rather than leave the driver's `end_io_data` dangling. A real wait (completion + sleep) is the correct fix. | `mq/request.rs`, the `if !done` arm. No in-tree caller exercises it. |
| 289 (high) | nothing calls the selection ladder; the image has no `PT_LOAD` | `coredump::plan::plan_mappings` walks the tree through `describe_vma` → `vma_dump_verdict` → `resolve_elf_probe` and yields one planned segment per mapping |
| 290 (low) | `process_mrelease` reads `coredumping` as always false | The mm latches a dumping flag for the length of the write and `exit_state` reads it |
| 368 (low) | the end-to-end observation was never made | `tools/guest-coredump-check.py` crashes a real program in the guest and compares the dumped bytes at the crashing program counter against the on-disk object |
| OPEN | low | aarch64 has no independent `orig_ax` equivalent and x86_64 here deliberately keeps ONE `rax` slot serving both roles (`PtRegs` header documents the split of Linux's overloaded `orig_ax` into `vector`/`error`). A tracer therefore cannot set `orig_rax` and `rax` to independent values on a trap frame: the write side picks the slot by entry tag. Linux has two distinct slots and would honour both. Not fixed because giving the frame a second slot changes an asm-coupled layout on both arches; recorded so the next lane touching `PtRegs` sees the cost. | `from_user_regs` in `crates/kernel/syscalls/src/101_ptrace/regs.rs`; pinned by `x86_setregs_on_a_trap_frame_installs_the_architectural_rax`. | — |
| INFO | — | aarch64 was NOT affected by the offset drift. Verified by index-by-index comparison of the old ptrace table against `SvcFrame`'s pinned `offset_of!` asserts (`gp[18]`@0, `x18_x29`@18/19, `x30`@20, `elr`@22, `spsr`@23, `sp_el0`@24, `retval`@25, `x19_x28`@26..36, size 0x120) — every entry matched, and the base already preferred the per-task frame pointer the SVC save block records. It is now derived from `SvcFrame` anyway, so it cannot drift later. | `arm64_entry_frame_is_36_quadwords`, `arm64_scattered_frame_flattens_to_x0_through_x30`. | — |
| INFO | — | Both ptrace arch mappings stay hosted-testable on either host: `syscalls` gained `hal-x86_64` + `hal-aarch64` as **dev**-dependencies so the arch-gated modules also compile under `cargo test`. Gating them on `target_arch` alone would have deleted the only check that catches a wrong mapping, which is silent at runtime. | `crates/kernel/syscalls/Cargo.toml`; `cargo test -p syscalls --lib s101_ptrace_regs` runs both arms. | — |
| INFO | — | The other consumers of the same event AGREED once the writer was shared, so no further fix was needed: `SA_SIGINFO` delivery (both arches' `build_signal_frame`), `rt_sigtimedwait`/`waitid` copy-out, the core-dump `NT_SIGINFO` capture and `signalfd` all already routed through `hal::write_siginfo` or the classifier. The three that did NOT were the ptrace ones, which is why only ptrace reported a fault as a kill. | `crates/kernel/fs/src/coredump/capture.rs`, `hal-x86_64/src/signal.rs`, `hal-aarch64/src/signal.rs`, `syscalls/src/128_rt_sigtimedwait.rs`. | — |
| INFO | — | aarch64 carries the fault address the same way and needed no arch change: `si_addr` originates in the arch-neutral `force_sig_fault` path (`sched::sigsend::fault_info`), which both arches' fault classifiers (`hal::fault_class`) feed, and the `_sifields` offsets are identical on the two arches (one writer serves both). The fix is therefore in shared code on both. | `crates/kernel/mm-pmm/src/user_as/signal.rs` (arch-neutral), `hal::siginfo` offsets shared by both `build_signal_frame`s. | — |
| OPEN | low | The "a synthesised event record names the TRACEE" rule is pinned hosted (`a_synthesised_event_record_names_the_tracee_not_the_tracer`), but the CALL SITES that supply the vpid live in `target_os = "oxide-kernel"`-gated files (`stop.rs`, `sig.rs::interrupt`), so a hosted test cannot catch a caller that passes the wrong task. The in-guest probe is the only check on the wiring itself. | `101_ptrace/stop.rs::self_vpid`; `t_ptrace_siginfo` `addr_is_not_a_pid`. | — |
| OPEN | low | `PTRACE_SETSIGINFO` now accepts every union arm, including `_sigsys`, matching the reference kernel's verbatim union copy-in. It does NOT implement the "unknown si_code with a non-zero expansion area is `E2BIG`" check, so a tracer installing a record whose si_code selects no known layout gets the tail silently zeroed rather than an error. Nothing in the tree produces such a record; recorded because it is a real ABI divergence. | `signal_common::read_user_siginfo` copies `KERNEL_SIGINFO_BYTES` and zeroes the rest. | — |
