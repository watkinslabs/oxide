# Known issues

**Live issue count: 299** — 294 `OPEN`, 5 `IN-PROGRESS`.

| Id | Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|---|
| KI-0320 | IN-PROGRESS B3234-windows-native-sync-runtime | DEFECT | high | [CLAIMED B3234-windows-native-sync-runtime 2026-09-03] **Native PE Notepad reaches the NT personality and then faults in Wine's Unix-side runtime at a null return path.** B3057 removed the unsafe fallback to a patched relay EAT entry; the remaining end-to-end runtime behavior is not yet proven. | F1485 dedicated `make smoke-windows-notepad-x86`: `[WINDOWS-PE-COMMIT]` was reached, then relay targets for the Wine Unix module were invoked before the fault. B3057 adds fail-closed selection and hosted precedence tests; a final x86_64 runtime verification is still required before this issue can be marked fixed. | NT runtime |
| KI-0003 | OPEN | DEFECT | med | **A pathname lookup alternates found/ENOENT for a file that exists — previously blamed for the resolver's failed bus join; the actual cause was the bind-mode EACCES row above. The found/ENOENT flip observations remain real and unexplained in themselves (the 16.647 'ENOENT' predates the errno fix and may have been EACCES all along — re-verify before chasing).** Post-fix timeline on a failing boot (`x86_64-20260831-202937.log`): `bind(2)` creates the socket at 16.629; a watch-install lookup of the same path SUCCEEDS at 16.644 (the B3021 negative-insert race fix working, its test red/green-verified) and an identical lookup answers ENOENT at 16.647. Found-then-not-found 3ms apart means TWO resolution states coexist — the shape of a second dentry alias for `/run/dbus` holding a stale negative child that `drop_child_cache` (which flushes only under the alias the create resolved) never removes. **Next probe, instruments already in tree:** add the parent-dentry pointer to `[INOTIFY-ENOENT]`/`[INOTIFY-ADD]` and to `d_drop_child`; one failing boot shows the alias directly. Sequence to reproduce: boot with `debug-eio,debug-inotify,debug-epoll`; ~1/3 of boots where `dbus.socket` starts after resolved's first connect. `systemctl restart systemd-resolved` heals every observed instance. | Timelines: `13.189-create/13.214-ENOENT/13.218-OK` (pre-fix, x86_64-20260831-195038.log) and `16.629/16.644-OK/16.647-ENOENT` (post-fix). Boot rate with the fix so far 6/9. | vfs/dcache lane |
| KI-0004 | OPEN | DEFECT | critical | **The last DNS/D-Bus failure is one unretried startup step in systemd-resolved, now isolated to a one-boot instrumentation of `net::sock::connect_admitted`.** On failing boots (~1/3): the system bus is fully populated — `systemd-oomd` holds `:1.0`, logind/homed/NM/polkitd all present — and ONLY `org.freedesktop.resolve1` has no owner. resolved is alive and its varlink listener is ACCEPTING (49 `[UXACCEPT comm=…systemd-resolved]` on one failing boot), every socket in the guest shows zero Recv-Q/Send-Q (`ss -x`, `ss -u`, `ss -t`), and the AF_UNIX stream/dgram/listener wakes are hosted-proven with red/green controls. `systemctl restart systemd-resolved` HEALS it: `resolvectl query one.one.one.one` then answers in full. So resolved's first bus attempt fails once, the error vanishes into the journal (which persists nothing, see the journald row), and nothing retries. The failing boot shows NO kernel-side unix connect trace from resolved at all — the pathname connect route is `042_connect` → `net::sock::connect_admitted(RemoteAddr::Unix)`, which has no failure trace; `connect_pair_addr`'s `[DBUSCONN]` never fires for anyone. **Next lane: log every AF_UNIX `connect_admitted` failure (comm, path, errno) under `debug-dbus`; one failing boot names the errno, then fix that path against the reference.** | 2026-08-31 probe captures in `target/boot-rate/rate-*.log` (B3021): full `busctl list` on a failing boot; restart-heals transcript; `ss` queue sweeps all zero; ordering identical on passing and failing boots (resolved ~7.5s, dbus-broker ~10s, both outcomes). | net/sock lane |
| KI-0005 | OPEN | DEFECT | high | **journald persists nothing on failing boots — clean start, flush OK, and `journalctl` answers `-- No entries --` for everything, including a fresh `systemd-cat` round-trip.** `/run/log/journal/` has no machine directory; `/var/log/journal/<machine>/` is the image's prebuilt one. No error is logged anywhere, which is what hid the resolved failure above. Matches the standing journald-empty/ext4-writeback lead (mmap writes into the prebuilt journal silently lost). The passing-boot control was captured but its serial output clipped, so whether persistence works on passing boots is still UNKNOWN — do not assume it discriminates. | 2026-08-31 `rate-*.log`: `echo probe-alive \| systemd-cat -t oxideprobe; journalctl -t oxideprobe` → `-- No entries --` on failing boots; journald's own startup lines clean; its kmsg cursor advances tens of KB between sweeps. | journald/ext4 lane |
| KI-0006 | OPEN | DEFECT | critical | **A group preallocation hands out a block the on-disk bitmap already marks used, and ext4 answers ENOSPC — reaching the writing process as EIO. This is the sole remaining ext4 cause of the boot failures.** `claim_prealloc_block` (`balloc/prealloc.rs`) refuses when the PA and the bitmap disagree. That is not a space condition: it is a corrupted preallocation. The reference discards a stale PA (`ext4_mb_use_preallocated` / `ext4_mb_discard_*`) and allocates normally instead of failing. **Fix shape, established:** make the claim report the block unusable rather than erroring, and have the caller fall back to a normal allocation. That requires changing the return contract at all three call sites — `extent_rw/append.rs:123`, `append.rs:135`, `extent_rw/write.rs:583` — each of which currently rolls back and propagates. Needs its own lane and >= 8 boots. | Named 2026-08-31 by `FEATURES=debug-eio`: `[EXT4-NOSPACE] site=prealloc-block-already-used want=1 free=592 reserved=41` and `... free=160 reserved=40` (the last two fields are bit and group), one per failing boot at ~7.9s, immediately before the `bad-checksum` cascade. Eight other refusal sites were instrumented first and all stayed silent, which is what located this one. | ext4 balloc lane |
| KI-0007 | OPEN | COVERAGE | med | **`alloc_data_blocks_best_effort` is test-proven but boot-unproven.** The all-or-nothing contiguous request is a real defect — `balloc_short_run_image` fails without the fallback (`write of 256 blocks with 23019 free: NoSpace`) and passes with it, positive control verified. But no boot series has shown it helping, because the boot failures turned out to be the preallocation row above. Keep the fix, do not claim a boot-level benefit for it, and re-measure once that row is closed. | This lane, 2026-08-31. Attempt 1 (halve the request) was a measured regression and is reverted; attempt 2 initially broke the extent-tree split by letting a 2-block metadata request come back short, now opt-in. | ext4 |
| KI-0009 | OPEN | COVERAGE | high | **Three epoll probes in a row could not fail, each for a different reason — the pattern is worth keeping.** (1) A probe inside `rescan_levels` cannot observe a permanently stuck epoll, because a parked task never reaches that pass again; its zero count was reported as a disproof and was not one. (2) An external sweep that ignores `queued` counts items already on the ready list: 635 hits on a passing boot. (3) A sweep that ignores `EPOLLET` counts edge interests that are supposed to sit ready-and-unqueued. Only the fourth version discriminated passing from failing boots. Any new readiness diagnostic should be checked against a passing boot before its output is believed. | This lane, 2026-08-31. | fs/epoll |
| KI-0010 | OPEN | INFRA | high | **CORRECTION to the accumulating-rootfs-damage row on the B3019 lane (PR #6610): it describes harness runs that set `OXIDE_SKIP_ROOTFS=1`, not an ordinary boot.** `make qemu-<arch>` restages the build namespace's disks every run — `xtask rootfs` reflinks the pristine image and rebuilds the side disks in ~1.2s, and `build_glibc_root_img`'s own comment says every boot must start from a fresh copy for exactly this reason. Only a run that sets the skip flag inherits the previous boot's damaged filesystem, and `guest-resolved-check.py` invocations had been setting it — so every measurement taken that way was reading accumulated damage rather than the change under test. Fold this into that row when the lanes merge. | Measured 2026-08-31: `cargo run -p xtask -- rootfs --arch x86_64 --id ratechk` completes in 1.204s on btrfs. `tools/boot-rate.sh` deliberately does not set the flag. | tooling |
| KI-0011 | OPEN | DEFECT | critical | **NEGATIVE RESULT: the obvious fix for the row below is a measured regression. Do not re-apply it.** Halving the request down to a single block and taking the short grant (a6c1196ae, reverted in 7b1c297ea) took journalled boots from **4/8 to 1/8** and boots showing `kind=no-space` from **2/8 to 5/8**. The hosted reproducer it came with was sound and its positive control was clean, so the underlying defect below is real; accepting arbitrarily short grants is what is wrong. Taking a 1-block grant for a 32-block cluster scatters the file's extents, and the deeper extent tree costs more metadata than the allocation saved. A real fix needs the reference's normalization/goal machinery (`ext4_mb_normalize_request`, criteria passes) rather than a bare retry ladder, and must be measured over >= 8 boots before it is believed. | 2026-08-31 eight-boot series `scratch/B3020-alloc.out` (1/8, ns on boots 3,4,6,7,8) against `scratch/B3020-ns.out` (4/8, ns on none). Hosted reproducer, worth rebuilding: 96 MiB volume, fill with 128 KiB files, unlink every other one, write 1 MiB -- pre-change `write of 256 blocks with 23019 free: NoSpace`, post-change clean. | ext4 balloc lane |
| KI-0012 | OPEN | DEFECT | critical | **The block allocator treats a multi-block request as all-or-nothing contiguous, so a write gets ENOSPC on a filesystem with 2.3 GB free.** `try_alloc_run_in_group` returns exactly `count` CONTIGUOUS blocks or nothing, and `alloc_blocks_for_inode_goal` scans every group and then returns `NoSpace`. `reserve_hole_runs` falls back from a normalized/preallocated `reserve_count` to the exact `count`, but never below it. Writeback maps `NoSpace` to EIO, so a 128 KiB cluster that no group can serve as one run fails the whole write. The reference treats an allocation request as a maximum, not a minimum: `ext4_mb_new_blocks` returns whatever extent it found and the caller loops for the rest. Intermittent because it depends on free-space fragmentation, which the leaked-run row above keeps making worse. | 2026-08-31 journalled boot `target/boot-logs/x86_64-20260831-062109.log`: `[EXT4-EIO] op=writeback kind=no-space` at 9.195 s with **no** `[EXT4-NOSPACE]` line, so the refusal came from neither journal site — it is the allocator. Then `[EXT4-ERROR] kind=bad-checksum` and 118 more. `dumpe2fs`: 588246 free blocks. | ext4 balloc lane |
| KI-0013 | OPEN | DEFECT | critical | **NEGATIVE RESULT: epoll is NOT losing wakeups. The row below stands as a symptom, but its stated mechanism is disproved.** Instrumenting `rescan_levels` to name any wired (`poll_source`) interest that is ready while its epoll is about to park -- the exact shape a swallowed `notify_mask` would take -- reports **zero** across boots. `EPOLL_CTL_ADD` and `_MOD` were also read and both subscribe before checking readiness and queue if already ready, so there is no registration race either. A first version of the probe reported ~135 hits per boot; it counted items that were ready AND already on the ready list, which `scan_once` returns normally. Corrected, the count is 0. Look at D-Bus name registration / routing instead of the wakeup path. | 2026-08-31 `scratch/B3021-ep2.out`, `FEATURES=debug-epoll`: `eplost=0` on every boot, passing and failing alike. Superseded probe: `scratch/B3021-eplost.out`. | fs/epoll |
| KI-0014 | OPEN | DEFECT | critical | **A D-Bus method call to `org.freedesktop.resolve1` times out with no filesystem error anywhere, on roughly a third of boots.** Independent of the allocator row: in the 2026-08-31 six-boot series, two failures carried 2 EIO each (allocator) and two carried **zero**, all four reporting `Call failed: Connection timed out` 5/5. `dbus-broker` has started successfully and `systemd-resolved` is alive, parked in `epoll_wait`. The epoll wakeup path is ruled out (see the disproof row). Look at whether resolved ever takes its bus name, and at dbus-broker's activation/routing. | `scratch/B3021-ep2.out`: boots 5 and 6 rc=1, eio=0, eplost=0. Guest `ps` on a failing boot: `100 systemd-resolve S crates/kernel/fs/src/epoll.rs:364`. | net/af_unix + dbus lane |
| KI-0015 | OPEN | DEFECT | critical | **SUPERSEDED by the two rows above — the symptom is real, the stated mechanism is not.** With the ext4 EIO class gone (B3020), 4 of 7 journalled boots still fail: `systemd-resolved` is alive and parked at `crates/kernel/fs/src/epoll.rs:364`, `dbus-broker` has started successfully, and `busctl --system call org.freedesktop.resolve1 ... Ping` answers `Connection timed out`. The bus is up; the peer never wakes. Same shape as the earlier tmpfiles/userdbd stall. | 2026-08-31 eight-boot series `scratch/B3020-post2.out`: rc=0 on boots 2,3,7; boots 1,4,5,6 all show 10-12 `Connection timed out` and 32 tasks at `epoll.rs:364`, with 0 EIO, 0 `kind=no-space`, 0 `kind=bad-checksum` in every one. Guest `ps` on a failing boot: `100 systemd-resolve S crates/kernel/fs/src/epoll.rs:364`. | fs/epoll + af_unix lane |
| KI-0017 | OPEN | DEFECT | critical | **A journalled root filesystem still fails roughly a third of fresh boots with an EIO cascade, and always fails once a previous boot has damaged it.** With the ordered-data commit fix (B3020) a fresh `tune2fs -O has_journal` copy of the boot image booted PASS / FAIL(28 EIO) / PASS; a reused copy failed 4/4 with 44-45 EIO each. So the journal — the only configuration in which a killed VM is recoverable — is still not usable, and the shipped `^has_journal` image stays the status quo. The EIO does NOT come from either errno mapper or the writeback owner: a `FEATURES=debug-eio` build printed zero `[EXT4-EIO]` lines on a 44-EIO boot, so at least one more path converts a filesystem failure to EIO without going through them. Find that path first. | 2026-08-31 five-boot series `scratch` `B3020-rate-fixed.out`: 28/45/45/44/44 EIO. Instrumented boot `target/boot-logs/x86_64-20260831-034254.log`: 44 EIO, 0 `[EXT4-EIO]`. Hosted `journal_live_boot_image` is green, so the remaining path is not the one the hosted fixture drives. | ext4 jbd2 lane |
| KI-0018 | OPEN | DEFECT | high | **A `FEATURES=debug-boot` kernel panics before userspace: `crates/kernel/devpts/src/smoke.rs:36: slave read: Einval`.** The lowest-volume diagnostic build in the tree cannot boot, so the one feature meant for diagnosing a boot is unusable on the boot it is for. | 2026-08-30, x86 `make qemu-x86 FEATURES=debug-boot`: 37 serial lines, `[PANIC] crates/kernel/devpts/src/smoke.rs:36: slave read: Einval` / `[PANIC] halted`. The same tree boots to GNOME with no features. | devpts / tty |
| KI-0019 | OPEN | INFRA | high | **Four pre-push gates are red on `main` at once — `lint-ratchet`, `hosted-gate`, `test-build-gate` and `stack-gate` — so no branch can push without four bypass variables.** That is the whole gate set turned into a formality; the next real regression in any of them is invisible. `hosted-gate`/`test-build-gate` fail on the same crate as below; `stack-gate` fails on `oxide_fault_print_rust` 6208 B against a 7200 B exception-entry ceiling, with the ext4 create/rename chain at ~18.7 KB. | Measured 2026-08-30 in the MAIN tree at `72c310fd2`, all four reproduced there with no local changes. | tooling |
| KI-0020 | OPEN | INFRA | high | **`crates/drivers/drv-virtio-pmem` does not type-check on its own, so `make hosted-gate` — and therefore every branch's pre-push hook — fails on `main`.** Same shape as the red lint ratchet: a gate that is red for everyone is a gate nobody can use, and each lane learns to pass `SKIP_HOSTED_GATE=1`. Errors are all in `src/device.rs` (lines 16-62). | Measured 2026-08-30 on a branch whose diff touches only `crates/kernel/ext4` and `scratch/`; `make hosted-gate` names only `drv-virtio-pmem`. | drivers / tooling |
| KI-0021 | OPEN | INFRA | high | **The spec-lint ratchet is already 13 keys above baseline on `main`, so every branch's pre-push hook fails on findings it did not introduce.** The ratchet is only a gate while it is green; red-on-main trains every lane to pass `SKIP_LINT_RATCHET=1`, which is exactly how the next real regression gets through. Keys above baseline: `drv-virtio-pmem` klog-ungated 2>0 and pub-fn-complexity 6>0, `block` klog-ungated 20>13 and pub-fn-complexity 9>8, `ext4` pub-fn-complexity 71>28 and safety-missing 6>1, `mm-vmm` 18>17, `net` 228>227, `netlink` 14>13, `syscalls` 88>87, `vfs` 151>120, `docs` forbidden 16>9 and manifest/missing-row 1>0. | Measured 2026-08-30 in the MAIN tree at `72c310fd2`: `cargo run -p spec-lint -- ratchet` → 13 regressions, identical set to a fresh branch off it. | tooling |
| KI-0022 | OPEN | DEFECT | critical | **The boot rootfs accumulates permanent ext4 damage across boots, and once it does, `systemd-resolved` and `dbus-broker` never start — the intermittent "DNS does not work" symptom.** The image is built `mkfs.ext4 -O ^has_journal` (`../images/build.sh`) and every harness run kills QEMU instead of shutting down, so each torn metadata update is permanent; the harness also reuses `target/builds/<ns>/root-<arch>.img` under `OXIDE_SKIP_ROOTFS=1`, so the damage compounds. A damaged image answers `mkdir` under `/var/tmp` with EIO, which systemd reports as `Failed to spawn 'start' task: Input/output error` for every `PrivateTmp=` unit (resolved, oomd, dbus-broker, logind, upower, power-profiles-daemon) — no system bus, no resolver. | Control, 2026-08-30: pristine `gnome-x86_64-root.img` copied over the boot image → `guest-resolved-check.py x86` PASS, 0 EIO, resolved active, D-Bus Ping 5/5, stub DNS OK. `e2fsck -fn` on the SAME image after that one successful boot: `Deleted inode 89813 has zero dtime`, `Inodes that were part of a corrupted orphan linked list`, inode-bitmap differences and 36 leaked block runs. The preceding (reused-image) boot logged 43 EIO and 5 `systemd-resolved.service: Failed to spawn` and e2fsck named the exact two victims from its own log. | ext4 / images |
| KI-0023 | OPEN | DEFECT | high | **Enabling a journal on the boot rootfs breaks the boot, so ext4's write-ahead log is unusable where it matters.** `tune2fs -O has_journal` on the pristine image (64 MiB journal) then one boot: 30 EIO, D-Bus Ping 0/5, stub DNS FAIL. The journaled configuration is the only one in which an interrupted eviction is recoverable (hosted sweep below is clean journaled, strands inodes unjournaled), so this blocks the real fix for the row above. | 2026-08-30 `guest-resolved-check.py x86` on a `tune2fs -O has_journal` copy: FAIL, `/tmp/oxide-resolved-uart-x86-71641.log`. Unjournaled control on the same kernel: PASS. | ext4 jbd2 lane |
| KI-0024 | OPEN | DEFECT | med | **Every boot leaks ~48-50 block runs at the head of each of the last ~14 groups, with no owning inode.** Present in a clean-image boot that otherwise succeeded, and in the journaled boot; the runs start at or one past each group's first block (`917505--917552`, `950273--950320`, `983041--983088`, … `1409024--1409071`). `init_block_bitmap_for_group` does not mark them, so they are genuine allocations whose owner lost them. | `e2fsck -fn` after a single successful x86 boot, 2026-08-30. Not reproduced by the hosted `ialloc_uninit_group_image` fixtures or `balloc_uninit_e2fsck`, so the trigger is above `Mount`. | ext4 balloc lane |
| KI-0026 | OPEN | COVERAGE | med | **No check fails when a boot corrupts the root filesystem.** One boot on a pristine image leaves e2fsck dirty and nothing reports it; the damage is only visible to someone who runs `e2fsck -fn` on the build namespace's image by hand. | Introduced with the rows above, 2026-08-30. A boot-smoke post-step that fscks a throwaway copy of the booted image would have caught every one of them. | test-infra |
| KI-0027 | OPEN | INFRA | high | **The spec-lint ratchet is above baseline on pristine `origin/main`, so the pre-push hook rejects every branch.** Nine keys are over: `ext4 [code/pub-fn-complexity] 66 > 28`, `ext4 [code/safety-missing] 5 > 1`, `vfs [code/pub-fn-complexity] 151 > 120`, `block [code/klog-ungated] 20 > 13`, and one-over drifts in `block`, `mm-vmm`, `net`, `netlink`, `syscalls`. Every lane must reach for `SKIP_LINT_RATCHET=1`, which is exactly how the ratchet stops being a ratchet. | `make lint-ratchet` on a detached-`origin/main` worktree at `31ea9351f`: identical nine REGRESSION lines and `2787 finding(s) ... baseline total 2707` as on a branch adding one `scratch/` line, so it is not branch-introduced. | unowned |
| KI-0029 | OPEN | COVERAGE | med | **`systemctl is-enabled` cannot detect a deleted enablement symlink, so a gate built on it cannot fail.** A unit whose own `.wants` symlink is removed still answers `enabled` while the `Also=` partner it names is enabled. The images compose gate was built on it and passed with the enablement deleted; it now gates on the symlink, which is what the guest boot reads. Any other gate in this tree asking `is-enabled` has the same hole. | Positive control on the composed micro tree: symlink present -> PASS, `systemd-networkd.service` wants-link removed -> `is-enabled` still `enabled` rc=0, symlink-based gate -> FAIL, restored -> PASS. | images |
| KI-0032 | OPEN | COVERAGE | med | **Nothing gates the live image, which is the only artifact that gets distributed.** `make smoke-network-appliance-x86` boots the ext4-root path; the two defects above were both live-image-only and were found by hand. A live boot needs `systemd.debug_shell=ttyS0` AND `systemd.mask=serial-getty@ttyS0.service` on the boot line, or the probe's commands are typed into a login prompt and every check fails on a working guest. | Both rows above; the getty collision cost one full verification cycle here and is documented in `guest-network-check.py` already. | unowned |
| KI-0033 | OPEN | PERF | med | **Ext4 pathname lookup now uses the shared metadata block and a direct byte-name matcher for ordinary linear directories.** Indexed-directory lookup still descends the on-disk htree, and casefolded directories retain the generic equivalence path. Slow symlink bodies now use a Linux-shaped per-inode cache, while fast symlinks remain VFS-owned. The aggregate `newfstatat` ratio remains open. | E4-08 fast-match repeats: **1,368,054 / 6,487 ms / 18,622 ns / 24x** and **1,367,794 / 6,863 ms / 18,188 ns / 23x**, both reached GNOME Shell; the B2997 symlink phase fell from ~2.1 s to **322/271 ms** in two valid repeats, and merged-main B2998 measured **380 ms**. A fresh attribution run measured **3,010 actual inode-writer holds / 2,096 ms / ~696 us each**, separate from generic file-position holds; mutation hold time is now the next owner. | ext4 / perf lane |
| KI-0034 | OPEN | PERF | med | **Small SMP unmaps now use one Linux-shaped range TLB request per gather instead of one synchronous cross-CPU request per page; the aggregate gain is not closed yet.** The 33-page threshold still selects a full flush, and every gather still waits for remote invalidation before releasing frames. | B2812 candidate: two identical `SMP=2` GNOME runs reached `GNOME Shell started`; `munmap` measured 24,814 ns and 24,928 ns versus 22,006 ns in the immediately preceding run, while block/read workload variance also changed substantially. The structural range path is covered by HAL payload tests and both custom images build; retain this row until an isolated or repeated measurement demonstrates a durable gain. | mm / arch-irq / perf lane |
| KI-0035 | OPEN | PERF | med | **The dcache-hit walk now uses a lockless RCU chain for ordinary cached lookups and retains one inode snapshot per component, matching Linux's `__d_lookup()`/`d_is_positive()` shape; the syscall ratio remains open because downstream work dominates.** | B2812 current perf: GNOME Shell started; dcache probe **208 ns → 166 ns** and hash walk **190 ns → 149 ns** versus the preceding measured run, while `newfstatat` was 21,018 ns / 27x versus the prior 20,856 ns / 26x. All 394 VFS tests passed and both arches built. The lookup-path phase gained about 20%, but the end-to-end syscall result is within harness variance, so this is not a closed ratio. | vfs / perf lane |
| KI-0037 | OPEN | PERF | med | **Current block state supersedes the older queue-depth and downstream-syscall hypotheses below.** Ext4 file reads now use the block layer's owned-request wait path, so the device is no longer globally limited to one outstanding read; synchronous metadata/journal operations still retain their synchronous contract. The remaining syscall ratios must be attributed from current phase data, not from the historical one-request experiment. | B2812 `4f12f91c9` and current `crates/kernel/ext4/src/mount/io.rs::read_byte_range`; fresh GNOME perf boot: 526 block reads / 240 ms, `newfstatat` 22,503 ns / 29x, `recvfrom` 12,295 ns / 16x, and GNOME Shell started. Rows 248, 256, and 257 below are retained as audit history and are not current descriptions of the live read path. | block / perf lane |
| KI-0038 | OPEN | PERF | med | **`recvfrom` now carries the already-selected INET receive shape into the protocol backend instead of locking `sock.kind` twice to rediscover packet/TCP state.** This follows Linux's `__sys_recvfrom` → `sock_recvmsg` ownership: the pinned socket is classified once, then the protocol owner handles the receive. The security admission remains in place and the mutable transport state is not cached. The result is a measured optimization, not a closed ratio. | B2812: fresh `make perf-report` reached GNOME Shell; `recvfrom` was 12,295 ns / 16x host Linux versus the prior 16,582 ns / 21x run. Phase checkpoints put import at 0.01 us, lookup at 0.12 us, admission at 0.51 us, and backend at 11.1 us; socket rows vary by tens of percent, so repeat before declaring the gain durable. Hosted recv-admission 4/4 and socket-security 6/6 passed. | syscalls / net / perf lane |
| KI-0039 | OPEN | PERF | med | **B2812's current GNOME boot no longer supports the block-queue-depth theory as the cause of the slow syscall rows.** The owned virtio path completed the desktop workload with 526 reads (431 ms total in this run); the remaining hot path is dcache lookup work inside `newfstatat`, not serialized block submission. The locked dcache hit path now keeps corruption-count validation diagnostic-only, matching Linux's `lookup_fast()` production shape; this is a measured optimization, not a claim that the syscall gap is closed. | B2812 `397fc126a` baseline: `newfstatat` 21205 ns / 27x; B2812 dcache probe: 20704 ns / 26x; both reached `GNOME Shell started`. `cargo test -p vfs --lib --no-default-features`: 394 passed. | vfs / perf lane |
| KI-0040 | OPEN | COVERAGE | med | **The live image's size budget is not measured anywhere, so every discussion of it re-derives the numbers.** Measured 2026-08-28 on x86_64, one real `live-image.sh` run per configuration: GRUB + ESP + GPT overhead is **7.7 MB**; the stripped kernel was **14.6 MB** and is now **6.5 MB** gzipped; the `micro` slim squashfs is **61.6 MB**. A CLI-only Fedora root (110 packages: systemd, bash, coreutils, util-linux, procps-ng, htop, iproute, iputils, net-tools, curl, vim-minimal, less) packs to **32.1 MB** with the same zstd/128K settings, giving a **46.3 MB** total image. Under 40 MB is NOT reachable with systemd and Fedora RPMs: the remaining levers are xz/1M squashfs (32.1 -> 27.7 MB, at a per-fault decompression cost this project already rejected once) and dropping systemd+udev (-4.5 MB compressed). `usr/share/locale` alone is 35 MB uncompressed because `glibc-minimal-langpack` only covers glibc and every other package ships its own translations. | Sizes from `mksquashfs -comp zstd -Xcompression-level 19 -b 128K` over trees extracted from the resolved RPM set, and from `ls` on the images `live-image.sh` produced. | unowned |
| KI-0041 | OPEN | DEFECT | high | **The `micro` LIVE image (squashfs + tmpfs overlay) does not reach systemd at all on some boots — no banner, no `Reached target`, no signal.** One boot 2026-08-28 on `main` + F1265: the kernel parsed its command line at 0.79 s and was still alive at 41.5 s emitting task dumps, with `kernel_init` R and `khungtaskd` D, and systemd never printed a line. Distinct in shape from the row above, where systemd starts and then stops. | Serial log of a 75 s bounded run of `tools/live-image.sh micro x86_64` output booted under `-accel kvm -smp 1`. The kernel image was gzipped for that boot; that is NOT the cause — the gzip round-trip is byte-identical to the stripped ELF (`cmp` clean), and a kernel GRUB failed to decompress could not have printed its command line. | unowned |
| KI-0042 | OPEN | DEFECT | high | **A squashfs+overlay root stalls in early userspace on roughly four boots in five.** systemd starts, prints its banner, and stops — usually at guest time ~1.64 s, sometimes at ~1.95 s after `bpf-restrict-fs`. The guest is IDLE, not spinning, so it is a lost wakeup rather than a loop. A kernel built with `debug-taskdump debug-watchdog` gets much further, which is what a periodic tick papering over a missed wake looks like. Not SMP: `-smp 1` stalls identically. | F1263. Five boots of the same image, serial as `/dev/console`: reached-targets 0, 0, 0, 14, 0; last kernel timestamps 1.668, 1.636, 1.640, 2.601, 1.951. The passing boot reaches `Reached target` fourteen times and keeps going. Adjacent to the open B2685/B2690 finding that making the page-fault path sleep more often produces intermittent early-boot failures — this root makes every file read sleep, where an ext4 root's mostly do not. | unowned |
| KI-0043 | OPEN | DEFECT | high | **PID 1 can take a spurious SIGSEGV at address 0 in early boot — reproducible on `main`, and NOT caused by any live-image work.** `main`'s own `target/builds/micro` ISO with its ext4 root: `init[1]: segfault at 0000000000000000 ip 00007f29046787b5 sp 00007ffeab46fc70 error 0000000000000004`, then `[PANIC] Attempted to kill init!`, on **three boots of three**, before systemd prints its banner. error=4 is a user-mode READ of a non-present page, and the report printed no `print_vma_addr` tail, so the VMA holding the faulting IP carries no file. | F1263, measured while chasing what looked like a squashfs-root defect and is not one. Same profile on the same branch with an EXT4 root: 1 of 3 boots reaches `Reached target`. Same userspace as a squashfs+overlay root: 1-2 of 5. The live image gets FURTHER than the ext4 one — it prints systemd's banner and dies later, where systemd's own SIGSEGV handler is installed, which is why no `segfault` line appears there and the exit status is the only evidence (`[INIT] Attempted to kill init! exitcode=0x18b` — signal 11, core dumped). Not the short-fill class: `debug-shortfill` reports zero `[SHORT-FILE-FAULT]` across the failing boots. B3325 extends the signature to GNOME/aarch64: `make smoke` attempt 1 reached the ext4 root then died with the same `exitcode=0x0000018b`; the harness's second attempt passed. x86_64 GNOME passed first attempt. | unowned |
| KI-0044 | OPEN | INFRA | high | **`main` is RED on the spec-lint ratchet, so the pre-push hook blocks every branch.** Two keys are above baseline on `main` itself with a clean tree: `crates/kernel/net [code/pub-fn-complexity] 228 > 227` and `crates/kernel/syscalls [code/pub-fn-complexity] 89 > 87`. Whoever last raised them pushed with a bypass instead of fixing them or updating the baseline, and every lane since has had to do the same. | F1263. `make lint-ratchet` at `41b90cd36` in the main tree: those two lines and `FAIL — 2 key(s) above baseline`. F1263's branch shows `syscalls 88 > 87` — one BETTER than main, because simplifying the init and interpreter readers removed a branch — and still cannot push without `SKIP_LINT_RATCHET=1`. | unowned |
| KI-0045 | OPEN | DEFECT | high | **The one-shot timer is re-armed only from paths the timer itself drives, so once a system goes fully idle nothing ever wakes it again — a SPLIT SOURCE OF TRUTH between `sched::timers::runtime::ARMED_NS` and the hardware.** `ARMED_NS[cpu]` records what the scheduler believes it programmed; the arch side (`arch_irq::deadline::program`) writes `IA32_TSC_DEADLINE` only `if crate::lapic::timer_deadline_mode()` and has NO else branch, so a failed arm leaves the scheduler believing a deadline is pending that the hardware will never deliver. Nothing outside the timer path recomputes it, so the state is unrecoverable: the tick would re-arm the tick. Same circular shape as the ktimers wedge fixed by B1348. | F1263, measured by GDB on a frozen guest with a desktop up. After **275 s** of uptime: `ARMED_NS[0]=0x74f44006` (1.962 s) and `ARMED_NS[1]=0x7515cfbc` (1.964 s) — the last arm the scheduler recorded is **273 seconds in the past** — `NEXT_TICK_NS` identical, and `arch_irq::lapic::dispatch::TICK_COUNT=4`: the LAPIC timer has fired four times since boot. Both CPUs halted with **IF SET** (`eflags 0x282` / `0x246`), one in `cpuidle::idle::generic::enter`, so they are waiting for an interrupt that is never programmed, not spinning and not masked. `TSC_KHZ=0x4017b7` (4.2 GHz), so the clock source is fine. **Pre-existing, not live-image specific:** a stock ext4 `gnome` boot of the same kernel shows `TICK_COUNT=3` frozen across 25 s too — it survives only because continuous I/O keeps generating interrupts. The live image exposes it because it actually reaches idle. User-visible as a desktop that draws, runs, and then locks solid: JWM's clock stops advancing, X's cursor stops moving (X draws it, so a frozen guest freezes the pointer), and the guest burns 1 s of CPU per 70 s of wall clock. | unowned |
| KI-0046 | OPEN | INFRA | high | **`main` is RED on BOTH pre-push gates, so the hook blocks every branch and every lane is pushing with a bypass.** `make lint-ratchet` at `41b90cd36`: `crates/kernel/net [code/pub-fn-complexity] 228 > 227` and `crates/kernel/syscalls [code/pub-fn-complexity] 89 > 87`. `make stack-gate` at the same commit: `FAIL — 11 static path(s) reach >= 13000 B on a 16384 B kernel stack`, all of them ext4 directory operations (`Ext4StatInodeOps::rename` at 14312 B, `rename_sides` at 13960, `mkdir`, `create`, `mknod`). A gate everyone bypasses stops being a gate. | F1263, measured in the MAIN tree with no local changes. F1263's branch is one BETTER on the syscalls ratchet key (88) and reaches 13 stack paths against main's 11 — the two extra are ext4 `create_mknod_inner` frames that crossed the threshold on inlining drift, not new code: the branch adds no ext4 code at all, and its own deepest addition is a 256-byte line buffer. | unowned |
| KI-0047 | OPEN | DEFECT | high | **The `micro` profile's systemd still dies or stalls in early boot after the stack fix, on roughly four boots in five.** Two shapes remain, and neither is the stack: a genuine NULL READ (`init[1]: segfault at 0000000000000000 ... error 0000000000000004`, no `print_vma_addr` tail), and a silent stall just after `bpf-restrict-fs` with no signal at all. `init=/bin/bash` on the same image reaches an interactive prompt three times out of three, so PID 1 loading, the root filesystem and the dynamic linker are all sound — this is specific to what systemd does next. The `gnome` profile is unaffected. | F1263. Five boots after the GROWSDOWN fix: 0, 0, 0 (one with the NULL read), 0 targets; `init=/bin/bash` on the identical image: prompt 3/3. Before the fix this row also covered the stack fault, which is now separately FIXED. A dangling-library audit of the micro tree is CLEAN — every `DT_NEEDED` of systemd, bash, Xorg, jwm, xterm and login resolves transitively (87 objects, 0 missing), so the profile'"'"'s `rpm -e --nodeps` removals are not the cause. | unowned |
| KI-0049 | OPEN | DEFECT | med | **(Root cause identified — see the idle-timer row above.) The live image's X session reaches the screen and then locks solid.** `/init` runs every time — its own line is on the console on every boot — and the shell it keeps is reliable, but `startx` reaches a modeset and a drawn JWM desktop only sometimes: observed drawn at 210 s on one boot, and no `[DRM-SETCRTC]` at all by 230 s on the next, from a byte-identical image. Nothing here distinguishes "still loading X's several megabytes off a compressed root" from "X failed and the log is inside the guest". | F1263. Five boots of the same image: one full desktop (JWM taskbar, a titled xterm with a live `bash-5.2#`), two with a modeset but a black screen at 95 s, two with no modeset. `/var/log/startx.log` exists in the guest and nothing outside it reads it — the first thing the next lane should do is get that file out, which needs either the host share or a serial dump from `/init`. | unowned |
| KI-0050 | OPEN | COVERAGE | med | **The live image takes about three minutes to reach its desktop, and nothing measures it.** The kernel is up in 2 s; the rest is userspace reading a compressed root with no readahead — every page fault decompresses a 128 KiB block and there is nothing above the block cache to fetch ahead of the fault. An ext4 root of the same tree reaches a comparable point far sooner. | F1263. Screendumps of the same image at 95 s (modeset done, screen still black) and at 210 s (JWM with a titled xterm and a live prompt). The block cache added here removed the per-fault re-decompression; what remains is the absence of readahead on a file-backed fault, which is a mm concern rather than a squashfs one. | unowned |
| KI-0053 | OPEN | DEFECT | high | **The three block filesystems that are not ext4 hold a spinlock across block I/O.** `squashfs` was fixed here (its volume lock is now a sleeping mutex, which is what Linux's squashfs holds across the same work); `fatfs::mount` and `ntfs3::mount` still declare `volume: sync::Spinlock<Volume<BlockSource>, sync::TaskList>` and read the device under it. Every read from one of them sleeps in atomic context. | F1263. Before the fix, one squashfs-root boot logged `[BUG] scheduling while atomic ... held=[100@crates/kernel/squashfs/src/mount/node.rs:60]` and again at `ops.rs:37/40/80` — one pair per lookup and per page fill, flooding the console. Same declaration in `crates/kernel/fatfs/src/mount.rs:51` and `crates/kernel/ntfs3/src/mount.rs:38`; neither is on a boot path today, so neither has been observed. | unowned |
| KI-0057 | OPEN | COVERAGE | med | **`block::crypto::tests::medium` fails under host CPU contention, and only then.** `a_read_at_the_wrong_number_does_not_recover_it` and `async_submit_serves_a_context_the_device_cannot` return `Eio` when the crate's tests run beside a heavy build. A test that answers a different result depending on how busy the box is cannot be trusted to fail for the reason it names, and a lane that hits it will read it as a real regression on whatever it just changed. | Observed 2026-08-28 while validating the queued perf PRs: `cargo test -p block --lib` run concurrently with two kernel cross-compiles gave `269 passed; 2 failed`, panicking at `crates/kernel/block/src/crypto/tests/medium.rs:157` on `Result::unwrap()` of `Err(Eio)`. The same command on the same tree with the box idle gave `271 passed; 0 failed` four times running, including `--test-threads=1`. `main` produced the same one-test blip (`264 passed; 1 failed`) under the same load and was green immediately after. | unowned |
| KI-0058 | OPEN | INFRA | high | **A lane blocked on a shared-image defect it did not own opened twenty PRs and merged none of them.** Every PR body from #6273 to #6292 records gates green and a red boot baseline — systemd failing to resolve `default.target` and falling to `rescue.target` — and concludes that the change cannot be merged. The blocker was in the shared micro-image, not in any of the changes, and was fixed on `main` the next morning by #6314. The queue was never revisited; the work sat unmerged for two days behind a condition its authors could not clear. A boot baseline that is already red must not gate an unrelated merge, and a lane that stops on one must hand the blocker off rather than continue queueing behind it. | PRs #6273-#6292, opened 2026-08-26 19:37-23:46, merged 2026-08-28 after re-verification: 18 cherry-picked onto current `main` as one stack with two intra-set conflicts, `xtask kernel --check` PASS on both arches, `syscalls` 2017 / `vfs` 470 / `vmm` 394 / `drv-virtio-blk` 84 all green. #6301 was in the same state and its rebased head passes `boot-smoke.sh` on both arches, first attempt. | unowned |
| KI-0059 | OPEN | DEFECT | med | **`overlayfs::mount::tests::credentials::chmod_forwards_the_acl_rewrite_to_the_copied_up_inode` fails on `main`, deterministically and in isolation.** A forwarded `chmod` is expected to rewrite the copied-up inode's ACL and does not: the test reads 6 where it requires 0. Not a flake — it fails alone and it fails in a full crate run, on a tree with no local changes. | Found by F1263 while adding `overlayfs::volatile`. `cargo test -p overlayfs chmod_forwards` at `41b90cd36`: `0 passed; 1 failed`, panicking at `crates/kernel/overlayfs/src/mount/tests/tests/credentials.rs:31`. Unrelated to that branch, which adds a module and touches no ACL or copy-up code. | unowned |
| KI-0060 | OPEN | MISSING | med | **A volatile root is composed by the kernel from a `rootovl=` boot parameter, because there is no initramfs to compose it in.** Linux has no such kernel parameter: dracut mounts the image, mounts a tmpfs and mounts the overlay of the two inside the initramfs, then switches root. We have no initramfs and no cpio/`initrd=` support at all, so the boot path does the composition itself. The deviation closes when an initramfs exists and the composition moves into it. | F1263. `cmdline::root_fstype::root_overlay_in` owns the parameter; `kmain::root_mount::volatile_over` composes `overlayfs::volatile_over(lower, upper, work)` over the mounted root. Grep for `initramfs`/`cpio` finds only kexec's bzImage/Image loaders — nothing that populates a root from one. | unowned |
| KI-0061 | OPEN | MISSING | low | **The overlay layer-disjointness check cannot see through resolved layers, so an in-kernel caller supplying inodes gets no containment refusal.** `mount::check_paths` compares the option string's PATH names, which is what Linux compares; `overlayfs::volatile_over` names three distinct labels and resolves each to an inode, so a work directory genuinely inside the upper layer would mount. Harmless for the one caller today (the boot path builds both directories itself) and a trap for the next one. | F1263. `volatile/tests.rs::an_upper_layer_that_is_not_a_directory_is_refused` covers the refusal that DOES survive resolution; a containment test was written, observed to pass a case it should refuse, and replaced rather than left asserting a guarantee that does not hold. | unowned |
| KI-0062 | OPEN | COVERAGE | med | **Block queue depth is not the dominant cost in the slow syscall rows, contrary to the working theory.** Raising ext4 reads off the one-at-a-time turnstile cut block read requests from 17,981 to 526 and time in block reads from 7,189ms to 181-338ms, reproduced across two runs -- and the syscall ratios did not follow. Only `mprotect` moved (16x -> 6x, both runs); `newfstatat`, `read`, `openat`, `mmap` and `close` are unchanged inside the report's own noise bar. Whatever dominates those rows has not been identified, and profiling them by assuming block I/O will keep mispointing. The later B2709 page-table change independently cut `munmap` from 71.4 us to 21.4 us; the block queue-depth issue itself remains open. | `make perf-report` on cf60ec30c (2026-08-26), two runs: `/tmp/B2699-perf.log`, `/tmp/B2699-perf2.log`; B2709 comparison is recorded in `scratch/perf-history.md`. | perf lane |
| KI-0064 | OPEN | DEFECT | high | **A page fault whose file backing fails reaches userspace as SIGBUS with no diagnosis of the underlying read.** A boot on 2026-08-26 logged 11 `[SHORT-FILE-FAULT-FATAL]` fills while the block layer reported no timeout, no poisoned device and no stuck chain, so the failing read is not accounted for by any current block-side probe. The fault path reports the symptom; nothing names which read failed or why. | `/tmp/B2696-blkwait3-serial.log` (2026-08-26): `SHORT-FILE-FAULT-FATAL`=11, `BLK-TIMEOUT`=0, `BLK-STUCK`=0. The same harness on B2697 booted to `graphical.target` with 0 of each, so the fault is intermittent and not yet tied to a cause. | block / mm-vmm lane |
| KI-0065 | OPEN | COVERAGE | med | **The synchronous virtio-blk wait is target-gated, so its give-up behaviour cannot be exercised hosted.** `modern/wait.rs::wait_for_completion` is reachable only under `target_os = "oxide-kernel"`; the change removing its deadline-poison path therefore has no hosted test that can fail, and was verified only by a boot. The hosted arm of the same function still returns `Eio` after `IO_FALLBACK_SPINS`, an unshared second policy. | `crates/drivers/drv-virtio-blk/src/modern/wait.rs`; the `#[cfg(not(target_os = "oxide-kernel"))]` arm bounds the spin, the kernel arm no longer bounds the wait. | virtio-blk lane |
| KI-0066 | OPEN | DEFECT | med | **Overlayfs chmod does not rewrite the copied-up POSIX ACL mask.** The overlay setattr path copies the inode up and forwards the mode change, but the upper ACL retains its previous `ACL_MASK` permissions instead of applying the chmod result. The lower layer remains unchanged, as required; the missing behavior is the upper-layer ACL rewrite. | `cargo test -p overlayfs mount::tests::credentials::chmod_forwards_the_acl_rewrite_to_the_copied_up_inode --lib` on 2026-08-25: expected ACL mask `0`, observed `6`; `crates/kernel/overlayfs/src/inode/ops.rs::setattr` forwards `Iattr` without a POSIX-ACL mode update. | overlayfs / vfs lane |
| KI-0067 | OPEN | COVERAGE | med | **The hosted futex core harness does not currently compile its included PI production sources.** The mock `Task` exposes `pi_base_class` as a top-level field, while `sched::pi_prio` and `sched::live::pi_boost` require `Task.security.pi_base_class`; the five resulting field errors block the integration test before execution. | `cargo test -p ipc --test futex_core_hosted` on 2026-08-25; `futex_core_hosted.rs` is the harness and `sched/src/{pi_prio.rs,live/pi_boost.rs}` are the included consumers. | ipc / futex lane |
| KI-0068 | IN-PROGRESS B2621-nftables-packet-path | MISSING | high | **The live nftables context is wired for the packet subsystems this kernel owns, but three production sources remain incomplete.** The hook now supplies conntrack state and direction, route state, native IPv4/IPv6 socket ownership and identity, packet metadata, synproxy access, canonical set lookup, persistent counter/quota/limit/connlimit/ct-helper/synproxy/ct-timeout/ct-expect/secmark object state, OS fingerprint database access, and object-set references bound into the published generation; conntrack tracking and action consumption are interleaved at the Linux priority boundary so `notrack` is effective before tracking. Production xfrm state, tunnel metadata, and the remaining stateful object types still have no owning subsystem and therefore remain explicit `None`/break results rather than fabricated values. | B2621. `crates/kernel/netfilter/src/eval.rs` implements the live packet accessors and `LiveObjects`; `crates/kernel/netfilter/src/state/generation.rs` binds set elements to object-owned state; `crates/kernel/netfilter/src/nft_expr/stateful.rs` owns persistent object state; `nf_hook_packet_in` performs raw-stage tracking and consumes actions. Commit `054e94cd9` adds Linux-shaped connlimit object flow ownership, tuple fallback, and closed/expired-flow reclamation. Commit `9274e6b0c` attaches helper objects through the canonical `CtNet::helpers` registry and unconfirmed `Conn` owner. Commit `8039a56bd` adds the canonical OSF fingerprint list, exact wire validation, OSF add/remove nfnetlink control messages, packet matcher, and Linux unmatched-SYN `unknown` result. The current source change adds the Linux SYNPROXY object ABI, shared packet evaluator, action propagation through named and set object references, and object regression coverage. The latest source change adds the conntrack-owned TCP/UDP timeout extension, Linux nested timeout parsing, and state-specific refresh selection. The current source change adds the Linux CT expectation object ABI, opposite-direction master tuple construction, per-master size admission, and canonical expectation-table insertion. The current source change adds strict SELinux context/SID resolution, packet relabel permission checking, and packet secmark application. The remaining absent seams are `XfrmAccess` and `TunnelAccess`. | unowned |
| KI-0070 | OPEN | DEFECT | high | **AArch64 reaches `graphical.target`, but the GDM Wayland session has not yet been proven registered.** The desktop session remains an open ARM issue even though systemd reports the graphical target. | The original SIGILL cause was `mrs x3, ctr_el0`: the EL0 sysreg trap path sent Linux's cache-geometry read to SIGILL because it only emulated counter registers. The permanent handler now returns Linux-shaped effective `CTR_EL0` state and advances ELR; pure HAL tests pass, both architecture checks pass, and the 2026-08-25 post-fix capture reaches active `gdm.service`, `graphical.target`, and `session-1.scope` without `[ARM-UNDEF]`, `Session never registered`, or a GDM core dump. The capture timed out before a positive GNOME session-registration marker, so this row stays OPEN pending that proof. | gdm / session lane |
| KI-0071 | OPEN | DEFECT | high | **A soft lockup with interrupts off can wedge the GNOME session before it presents.** A GNOME/virtio-gpu boot logged `soft lockup: no reschedule for 10s` on a `[pango] fontcon` thread inside `close`, with `irqs_off=y`, `preempt_count=0x10102` and two locks held at rank 135, then repeated it 30s later. gnome-shell stayed alive — its KMS thread, Mutter input thread and calendar server were all running at 81s — but nothing reached the scanout and the screen still showed fbcon. The same commit renders the desktop normally on a quiet box, so this is intermittent and load-sensitive rather than deterministic; a task holding two locks across a 10s spin with interrupts disabled is a defect whether or not the desktop survives it. | B2706, 2026-08-26, `/tmp/claude-1000/.../gnomegpu2-serial.log`: `[51.041] [WATCHDOG] soft lockup ... tid=4796 ([pango] fontcon) last_syscall=close preempt_count=0x0000000000010102 resched=y irqs_off=y interrupt=y kernel_pc=0xffffffff804b54df held_lock_rank=135 held_lock_depth=2`, repeated at `[81.041]`; `khungtaskd` in `D` at both dumps. The host was running a 24-way `test-build-check.sh` fleet at the time. A capture of the same tree on a quiet box: desktop rendered, 0 watchdog lines. | sched / mm lane |
| KI-0075 | OPEN | DEFECT | critical | **Opening Firefox, navigating to `youtube.com`, and starting a video can halt the desktop/kernel during page rendering.** The exact trigger is Firefox → YouTube → play; the responsible GPU, shared-memory, fault, or process-scheduling path has not yet been captured in a kernel log. | User reproduction reported 2026-08-25; no corresponding Firefox/YouTube trace is present in `target/boot-logs/` yet. Reproduce with a serial kernel log and preserve the last kernel event before changing code. | desktop / graphics lane |
| KI-0076 | OPEN | COVERAGE | high | **The full hosted `net` test binary can hang in unrelated `vsock_socket` lifecycle tests, preventing a complete package test result.** The netfilter-action tests pass independently, but the package-wide run does not terminate reliably. | 2026-08-25: `cargo test -p net --lib` reported multiple `vsock_socket` lifecycle tests running beyond 60 seconds; the run was stopped after the hang persisted. Focused `netfilter_action::tests` completed 12/12. | net / vsock lane |
| KI-0077 | OPEN | DEFECT | high | **The hosted `net` test suite currently fails its `TcpConn` stack-size assertion.** `TcpConn` measures 704 bytes and `TcpEntry` 880 bytes, exceeding the passive-open softirq-stack budget asserted by the test. This is an existing net-stack sizing defect exposed while validating the `NetStack` module split, not caused by the split itself. | `cargo test -p net tcp_conn --lib -- --test-threads=1`: 159 passed, 1 failed at `crates/kernel/net/src/tcp_conn/types.rs:380`; failure message reports `TcpConn is 704 bytes, TcpEntry is 880 bytes`. | net / tcp lane |
| KI-0078 | OPEN | MISSING | med | **Two nftables expression families still lack complete production sources.** `xfrm` needs IPsec security-path/transform state (`XfrmAccess::state`); `tunnel` needs receive tunnel metadata (`TunnelAccess`); and stateful object references now have owners for counter, quota, limit, connlimit, ct-helper, synproxy, ct-timeout, ct-expect, secmark, and OSF. These objects evaluate persistent state, share the Linux SYNPROXY packet/action operation with the expression path, install protocol timeout extensions owned by conntrack, insert expectations into the canonical expectation table, resolve and apply security labels through SELinux, match the canonical fingerprint database, or mutate the canonical conntrack owner through the object store and live packet context, including object references carried by object sets. Connlimit uses the canonical tracked flow owner or packet tuple identity and reclaims dead/expired tracked flows. Ct-helper validates the helper name, family, and protocol against `CtNet::helpers` and only attaches to an unconfirmed, non-template flow. CT-timeout validates the L4 tuple and installs Linux-shaped TCP/UDP state values only on an unconfirmed flow; later conntrack refreshes select the installed state/reply value. CT-expect validates the protocol, destination port, timeout, and per-master size, then constructs the opposite-direction related tuple through `CtNet::expect`. SECMARK rejects unresolved contexts and denied `packet:relabelto` requests at object creation, then writes the resolved SID to packet metadata. OSF owns the fingerprint list and its nfnetlink add/remove path; valid unmatched opening IPv4 SYN packets write `unknown` rather than breaking. Their evaluators break when the requested source/object is absent, matching failed lookup behavior; installing a rule does not fabricate any source. Software flow offload is no longer in this absence set. | B2621. `crates/kernel/netfilter/src/eval.rs` supplies `LiveObjects` and the live OSF source; `crates/kernel/netfilter/src/state/generation.rs` binds `NFTA_SET_ELEM_OBJREF` names and set object types to the immutable packet generation; `crates/kernel/netfilter/src/nft_expr/stateful.rs` owns counter/quota/limit/connlimit/ct-helper/synproxy/ct-timeout/ct-expect/secmark state. Commit `054e94cd9` wires the live conntrack owner through `NfHookCtx` for object evaluation and covers tracked and untracked connlimit identities. Commit `9274e6b0c` carries the canonical `CtNet` owner through `NfHookCtx` and implements registry-backed ct-helper attachment. Commit `8039a56bd` carries the OSF database through the canonical nfnetlink subsystem and covers add/match/remove and unmatched behavior. The current source changes add SYNPROXY object parsing/action propagation, conntrack-owned TCP/UDP timeout policy installation and refresh selection, canonical CT expectation insertion, and SELinux-backed SECMARK resolution/packet application. The reference source families are the xfrm, tunnel, osf, objref, stateful-object, and SELinux security hook implementations. | unowned |
| KI-0079 | OPEN | MISSING | low | Mesh point and the neighbour-awareness protocol appear in the nl80211 interface-type space that `62` puts in scope, and have no state machine. Scheduled scan and wake-on-wireless likewise have attribute space and no implementation. | `docs/66-wireless.md` OQ 2 and 3. `crates/kernel/wireless/src/uapi/enums.rs` advertises the types; nothing serves `JOIN_MESH`, `START_NAN`, `START_SCHED_SCAN` or `SET_WOWLAN`. | unowned |
| KI-0080 | OPEN | MISSING | low | No fullmac wireless driver exists, so `Cfg80211Ops` has exactly one implementation (the softmac bridge in `mac80211`) and the fullmac path — a driver that runs its own connect sequence — has never been exercised. | `66§3` rule 3. Every default body in `crates/kernel/wireless/src/ops.rs` returns `EOPNOTSUPP`; the only override is `mac80211`. | unowned |
| KI-0081 | OPEN | MISSING | high | **The ONC RPC (`sunrpc`) client landed by F1188 has NO caller anywhere in the tree: NFS and SMB do not exist, so nothing can mount through it.** F1188 found the branch carrying a `sunrpc` crate, an `nfs` crate whose entire content was `pub fn placeholder() {}`, and no SMB crate at all. The `nfs` placeholder and eight NFS-only errno variants were dropped rather than landed (present-but-fake is worse than absent, `docs/15`); `sunrpc` was kept because it is complete, reference-matched and test-covered, but it is a library nothing consumes. Closing this row means the phase-37 NFS client that calls `RpcClient::call`. Until then `sunrpc` is inert: it is not in `kmain`'s dependency tree, so it enters no kernel binary. | F1188, measured: `grep -rn sunrpc --include=*.toml --include=*.rs crates/ tools/ kernel/` excluding `crates/kernel/sunrpc/` returns ZERO lines. `cargo test -p sunrpc` = 98 passed; nine positive controls (reply-verifier ordering, fragment reassembly, duplicate-xid refusal, XDR padding on both encode and decode, the two-deadline retransmission schedule, the credential-retry ladder, machine-name truncation, verifier-flavour checking) each go RED then GREEN. | unowned |
| KI-0082 | OPEN | MISSING | high | **Phase 37 (NFS / CIFS / FUSE) has NO spec, and `sunrpc` was written without one.** `docs/00-master-plan.md` line 112 lists phase 37 as `open` with dependency `new spec`; the `docs/` directory contains no NFS, SMB/CIFS or RPC spec and `docs/MANIFEST.md` has no entry for one (checked by listing `docs/`, not by grep). `docs/03-modernity.md:79` additionally records NFSv2/v3 as dropped with only NFSv4.2 tracked - a constraint any NFS lane must design to, since it fixes the COMPOUND/session shape of the client, though not of the RPC layer beneath it, which the reference shares across all NFS versions. Discipline rule 1 is spec-before-code; F1188 landed `sunrpc` against no spec and this row records that debt. | F1188. `ls docs/` has no such file; `grep -in 'nfs\|sunrpc\|smb\|cifs' docs/MANIFEST.md` returns nothing; `docs/00-master-plan.md:112`. | unowned |
| KI-0083 | OPEN | MISSING | med | **9P: no zero-copy read or write.** Every payload is copied through the staging buffer in both directions, so a large read costs two copies rather than none. The protocol reserves the headroom for a direct scatter-gather path (`P9_ZC_HDR_SZ`), and the reference uses it above a size threshold. Needs a scatter-gather submit the split-queue owner does not expose. | F1184, `67§10` row 1. `crates/drivers/drv-virtio-9p/src/transport.rs` `exchange()` copies `req.tc` in and the reply out byte by byte. | — |
| KI-0084 | OPEN | MISSING | med | **9P: `trans=fd`, `trans=tcp` and `trans=unix` have no factory.** The option set parses all three, the mount-option tests cover their validation (both descriptors required together, port range, empty source), and the transport directory admits them — but nothing registers a factory, so such a mount fails `ENOPROTOOPT`. Only `trans=virtio` is served. | F1184. `ninep::opts::Trans` has all four; `ninep::transport::registry::available()` returns only `Virtio` (registered by `drv-virtio-9p`). | — |
| KI-0085 | OPEN | MISSING | med | **virtiofs: no DAX window.** The device's shared-memory region is neither negotiated nor mapped, so file pages are copied through the request queue instead of being mapped from host memory. Requires a shared-memory-region capability in the virtio transport and an mmap path that installs host pages. | F1184, `67§10` row 4. `crates/drivers/drv-virtio-fs/src/consts.rs` `wanted_features()` asks for `VIRTIO_F_VERSION_1` only. | — |
| KI-0086 | OPEN | MISSING | low | **9P: `cache=fscache` is parsed and reported but has no backing store.** The mode resolves to the right bit set and `/proc/mounts` shows it, so a caller asking for a persistent cache is told they got one. Nothing persists. The other four cache modes are honoured. | F1184, `67§10` row 6. `ninep::opts::cache_modes::FSCACHE_MODE` is set and shown; no consumer of the `FSCACHE` bit exists. | — |
| KI-0087 | OPEN | COVERAGE | med | **9P/virtiofs: no test drives a real device.** The protocol, client, mount options and attribute translation are covered hosted against a scripted server (108 + 12 tests) and the tag parsers are covered directly (11 + 9), but nothing exercises a descriptor chain: the DMA staging path, the device-length clamping and the queue submit are proven only by a boot. A defect in `exchange()` shows up as a failed mount, not as a red test. | F1184. `drv-virtio-9p`/`drv-virtio-fs` tests cover `config.rs` and the profile only; `transport.rs` has no hosted coverage because it needs a virtqueue. | — |
| KI-0088 | OPEN | MISSING | high | **NTFS `$LogFile` is neither replayed nor written.** A volume whose dirty flag is set mounts READ-ONLY (unless `force`), which is the reference's fallback when it cannot replay, not its normal path: `ntfs3` replays the journal at mount and refuses read-write only when replay fails. Nothing here writes journal records either, so a power loss mid-operation leaves a volume only Windows or `chkdsk` can repair. | F1178. `mount::open_with` sets read-only on `was_dirty()`; `crates/kernel/ntfs3` has no `fslog` module. `docs/62§9` OQ 2. | — |
| KI-0089 | OPEN | MISSING | med | **NTFS cannot write encrypted data.** `write_stream` and `truncate_file` refuse `ATTR_FLAG_ENCRYPTED`; this remains correct until an encryption owner exists. Native compressed streams are now writable and `compress` creates them. | F1178. `volume/write.rs` still returns `EOPNOTSUPP` for encrypted attributes; Linux also refuses encrypted I/O without an encryption implementation. | — |
| KI-0090 | OPEN | COVERAGE | med | **The exFAT built-in up-case table is rule-derived, not the specification's own, so a volume MISSING its up-case entry folds a handful of characters differently from the reference.** Reached only by a malformed volume — every formatter writes the table, and a volume that has one is folded by it. The rules cover Basic Latin, Latin-1, Latin Extended-A and Additional, Greek, Cyrillic, Armenian, Glagolitic, the Roman-numeral and circled forms and Fullwidth; Latin Extended-B's irregular pairs and Greek Extended are identity. NTFS's built-in table has the same shape and the same limit. | F1178. `crates/kernel/exfatfs/src/upcase/default.rs`, `crates/kernel/ntfs3/src/upcase_rules.rs`. `upcase::tests` cover every block the rules DO name. | — |
| KI-0091 | OPEN | COVERAGE | med | **Neither exFAT nor NTFS has been read against a medium a real formatter produced.** Every test drives an image this repo's own builder lays out from the format's written rules, which proves the reader and the builder agree. A `mkfs.exfat` or `mkntfs` image would test the reader against a third party; the harness has no path to carry a binary fixture. | F1178. `crates/kernel/exfatfs/src/tests/image.rs`, `crates/kernel/ntfs3/src/tests/image.rs`. | — |
| KI-0092 | OPEN | MISSING | low | **FAT multibyte codepages are not implemented.** Linux's `932`, `936`, `949`, and `950` NLS decoders are variable-width, so admitting them requires stream-aware short-name byte accounting; the current one-byte `CodePage` interface and short-name paths cannot represent that contract. | Linux `fs/nls/nls_cp932.c`, `nls_cp936.c`, `nls_cp949.c`, and `nls_cp950.c`; `fatfs::name::CodePage` exposes one-byte decoding and `short.rs`/`shortgen` use byte-oriented name slots; `codepage=932` remains refused by mount-option admission. | unowned |
| KI-0093 | OPEN | COVERAGE | low | **STILL UNATTRIBUTED after a second campaign: `fs::keyring::tests::payload::big_key_accepts_payloads_past_the_user_ceiling`.** One observation, ever. B2043 re-hunted it with the load profile that DID surface this binary's other flakes — eight concurrent copies of the `fs` lib binary, 2000 runs, plus 600 runs at `--test-threads=32` and 1500 runs at default parallelism — and it did not reproduce once in ~4100 further runs. That same campaign found and closed three OTHER families in this binary (perf sideband task ids, the perf wheel/registry walk, the timerfd clock step), measured 6/2000 -> 0/2000, so the load profile is known to expose contamination in this binary. Candidates unchanged and neither confirmed: the case shares quota uid 0 with the only other uid-0 case, and a sibling writes all four global quota knobs and restores them. What is still needed is the failing return value, which the assertion prints and no run has yet captured. | B2043. `crates/kernel/fs/src/keyring/tests/payload.rs:55`; `crates/kernel/fs/src/keyring/report/tests.rs`. | fs / keyring lane |
| KI-0094 | OPEN | MISSING | med | **ext4 still accepts more mount parameters than it acts on, but the former “quota family only” description is stale.** The current owner acts on `data=` (journal data ordering), `errors=`/`warn_on_error`, `barrier`/`nobarrier`, `commit=` (periodic journal commit), `journal_ioprio=`, `journal_checksum=`/`nojournal_checksum`, `max_dir_size_kb=` (all directory-growth paths), `discard` (post-transaction block trim), `noload`/`norecovery` (decision made before journal replay), `block_validity` (reserved system-block ranges are rejected by inode extent resolution), `delalloc`/`nodelalloc` (buffered-write allocation timing, with delalloc keeping allocation in writeback), `resuid=`/`resgid=` (reserved-block admission), `init_itable`/`noinit_itable` (paced inode-table zeroing), `mb_optimize_scan=` (allocator group-scan order), `acl` (the live POSIX-ACL superblock policy), `user_xattr` (the live ext4 user.* xattr-handler policy, enabled by Linux default), and `dax=` (live mount policy plus the kernel-target block/DAX provider). The remaining legacy names are still accepted into the opaque `other` list without a consumer and remain open. This row remains open for those real consumers, not for behavior already implemented. | `crates/kernel/ext4/src/mount_opts/{behaviour,parse,recovery}.rs`; `crates/kernel/ext4/src/rootfs/inode/dax.rs`; `crates/drivers/drv-virtio-pmem/src/device.rs`; Linux ext4/virtio-pmem reference paths. | B3015; provider/ext4 suites and x86_64 SMP1 DAX image harness green; SMP2/AArch64 runtime evidence remains |
| KI-0095 | OPEN | MISSING | low | `memfd_wait_for_pins` has no counterpart: `F_ADD_SEALS(F_SEAL_WRITE)` checks `mapping_deny_writable` (writable shared mappings, EBUSY) but not long-term page pins, because nothing in this kernel long-term-pins a page. Truthful today. It becomes a real hole — data written behind a write seal — the moment a GUP-style pin (io_uring registered buffers over shmem, zero-copy RX) can outlive the syscall that took it. | F783; no `pin_user_pages` equivalent exists (grep clean). | — |
| KI-0096 | OPEN | COVERAGE | med | **The `/proc/sys` ctl_table still cannot be tested hosted, and this lane measured exactly why.** Carried from the curated row opening "**The `/proc/sys` ctl_table is target-gated, so no hosted test can assert a leaf exists, binds to the right variable, or carries the right mode.**" Widening `procfs::ctl`'s gate to `any(target_os = "oxide-kernel", test)` leaves 11 unresolved references in FOUR distinct cascades, none of which is a procfs decision: (1) `ipc::live::posix_mq::sysctl` — 10 references; `ipc/src/live/mod.rs` carries its own `#![cfg(target_os = "oxide-kernel")]` and the mqueue sysctl accessors resolve the current task's IPC namespace through `live::posix_mq::user::ipc_ns`; (2) `procfs::sysctl` (`bound_sysctl_inode`, `SysctlInode`) — the writable-slot inode machinery, `#![cfg(target_os = "oxide-kernel")]`; (3) `procfs::random_uuid::{make_uuid_inode, make_boot_id_inode}`; (4) `sched::cred` / `sched::live`, which THIS PR's dev-dependency already resolves. What would settle it: hosted halves for `ipc::live::posix_mq::sysctl` (the per-namespace accessor pair, not the queue machinery) and for procfs's own two inode constructors — i.e. the table's *leaves* need their bindings reachable, not the registration walk. Splitting the table into a data-only module is NOT the answer: every hook leaf carries a function pointer to the live variable, so a data-only copy would be a second source of truth. | Measured on this branch: `cargo test -p procfs --lib --no-run` with the gate widened -> `10 error[E0433]: cannot find 'live' in 'ipc'`, `1 error[E0432]: unresolved import 'crate::sysctl'`, plus the two `random_uuid` constructors on the non-test path. Reverted; the gate is unchanged in this PR. | unclaimed |
| KI-0097 | OPEN | MISSING | low | **The case-folding table carries no per-codepoint Unicode age.** The generator's data source does not expose the Age property, so a filesystem declaring an older encoding version still gets the table's mappings for codepoints assigned after that version. A version NEWER than the table is refused outright, so the failure direction is the safe one. Closing it needs the age ranges in the blob and a per-codepoint version check on lookup. | B1981. `crates/shared/utf8` blob header records the generated version; documented on the encoding loader. | unowned |
| KI-0098 | OPEN | DEFECT | low | **The remaining strict-encoding gap is at the raw-name boundary, not in tmpfs admission or validation.** The live validator now runs from tmpfs create and lookup and rejects byte sequences its selected encoding cannot represent. But inode operations receive `&str`, so malformed or otherwise non-UTF-8 names can never reach that owner; closing the remaining divergence requires carrying raw name bytes through the inode-op boundary while retaining the existing validator. | B2527/B2538. `tmpfs::casefold::name_ok` calls `vfs::dentry::casefold::generic_ci_validate_strict_name`; current inode operations still accept `&str`, so the raw-byte refusal ladder is not reachable. | unowned |
| KI-0099 | OPEN | COVERAGE | med | **That a warm reset preserves the persistent-store region is unproven on this harness.** The mechanism is pinned hosted — the reboot is modelled by dropping every kernel structure and re-attaching to the same bytes, which is what a warm reset does — and both arches boot with the new init calls. What is not shown is QEMU honouring a reboot at all: the guest reaches the restart path (triple fault), QEMU stays alive and never boots again, with `-action reboot=reset` overriding the repo's `-no-reboot` making no difference. Closing it needs a reboot path this QEMU honours, or hardware. | B1981. 4 boots; `q.poll()` still None after the restart request, no second kernel banner in 400 s. | unowned |
| KI-0100 | OPEN | MISSING | med | **ext4's multiblock allocator is still only a subset of Linux mballoc.** Fresh multi-block `fallocate` and fresh multi-block `write_at` requests reserve one contiguous run and prefer a stripe-aligned start, so `stripe=` affects real production allocation paths. B2816 adds a validated per-mount block-bitmap cache, matching the resident bitmap half of Linux's buddy state; B2817 adds a validated largest-free-order summary for groups already loaded by this mount, so optimized scans can prefer a group with a suitably large free extent. B2822 adds reusable locality-group data preallocation for small regular-file writes, with disk-free tails masked in the in-memory bitmap and consumed by another file in the same group. B2823 makes regular-file append consume an existing inode PA first and a locality-group PA second. B2824 makes a fresh aligned regular-file append retain an inode-owned PA tail; directory and symlink append remain one-block allocations. B2825 batches the two metadata blocks needed by an inline-root extent split into one atomic metadata allocation request. B2826 batches all replacement leaf metadata blocks when a punch/truncate rebuilds a depth-1 extent tree. B2827 adds Linux's average-free-fragment order summary and uses it to prioritize loaded groups that can satisfy a multiblock request. B2828 adds an order-indexed group set so suitable loaded groups are selected without scanning every group. B2829 adds the companion largest-free-order index for power-of-two requests. B2830 applies Linux's size-based regular-file preallocation growth through the 16 KiB/power-of-two phase and bounded larger-window fallback. **B2850 makes locality preallocation ownership per CPU, matching Linux's `s_locality_groups[smp_processor_id()]` and carrying the owner through claim after blocking operations. B2852 adds inode-hashed stream/global-goal group reuse for large regular-file allocations, recording the actual successful group in canonical mount allocator state. B3004 adds indexed candidate scanning, B3005 restores physical-goal-first ordering, B3006 orders locality-PA reclaim by filesystem group, and B3007 filters PA masking to the target group.** The remaining gap is buddy/bitmap preallocation lifecycle transitions and request normalization; the extent-tree insertion sites that allocate one metadata block at a time are reference-shaped and are not a missing batch. | B2816, B2817, B2820, B2822, B2823, B2824, B2825, B2826, B2827, B2828, B2829, B2830, B2850, B2852, B3004, B3005, B3006, B3007. `MountState::block_bitmap_cache`, `group_free_order`, `group_free_order_index`, `group_avg_fragment_order`, and the order-indexed group set are updated only through successful allocation/free paths; `balloc_image`, `balloc_uninit_e2fsck`, all ext4 unit tests, all ext4 image suites, and both-arch boot smoke pass. Linux `fs/ext4/mballoc.c` remains the reference for the remaining mballoc lifecycle and normalization. | ext4 lane |
| KI-0101 | OPEN | INFRA | med | **The spec-lint ratchet fails on `main` itself: 66 keys above baseline, 2380 findings against a 1702 baseline.** It is not a branch's regression — the REGRESSION line set is byte-identical between a clean `main` checkout and a branch that touches only f2fs, including keys in crates the branch never opens (`sched`, `syscalls`, `modules`, `docs`). Several are whole categories reading `N > 0` (`code/safety-missing` 383 in `modules`, `code/panic-fmt` 8 in `netfilter`), which is the shape of a baseline file that lost entries rather than of code that grew them. Every push therefore needs `SKIP_LINT_RATCHET=1`, which is how a ratchet stops being a ratchet. | F1209. `diff` of `make lint-ratchet \| grep REGRESSION \| sort` between `/home/nd/oxide/kernel` (main) and this branch is empty. | unowned |
| KI-0102 | OPEN | COVERAGE | low | **No f2fs workload reaches a second node-flush pass, so the drain's loop is pinned only at the extracted decision.** Re-verified F1225 and still true: `writeback_node_block` -> `allocate_block` dirties no node page (it writes `nat_dirty` and the segment table, neither of which is node state) and does not enter the cleaner, so `drain_nodes` always converges in one pass through the volume. The 20-pass and stop-at-first-error tests drive `drain_nodes` with closures instead. If a future placement path can dirty a node, add the volume-level test. | F1213, re-verified F1225. | unowned |
| KI-0103 | OPEN | INFRA | low | **f2fs's zoned write-pointer check is driven by a hosted fake drive only.** `mount/wp.rs` issues zone reset and finish and a zero-fill tail write against a test block device; no real zoned device has executed the path, so a driver whose `zone_mgmt` disagrees with the trait's contract -- rounding a mid-zone start instead of refusing it, say -- would be invisible here. | F1198. 6 hosted tests over a fake reporting drive; the virtio-blk zoned implementor is the only real one and is unexercised by them. **F1219 narrowed the specific worry and left the row open.** The fake drive now records ordinary discards as well as management commands, and 4 new tests assert WHICH of the two a freed run produces. The named "rounds a mid-zone start" divergence is disproved by reading: `drv_virtio_blk::modern::zoned::issue_zone_mgmt` refuses a non-zone-start with `Einval` before submitting, which is what `BlockDevice::zone_mgmt` documents. Still no real zoned device has run the path. | unowned |
| KI-0104 | OPEN | MISSING | low | **The f2fs `LockOp` fault site still has no truthful owner because this port has no checkpoint rwsem or filesystem-operation lock.** The port intentionally has one volume-wide lock, so adding a fake trylock site would create a second locking truth. The timeout types are now wired at their owning operations: `AtomicTimeout` is consumed by atomic-write commit, `LockTimeout` immediately before checkpoint volume-lock acquisition, and both route through a VFS hook installed by the scheduler-owning mount layer. `AllocBio` and `Discard` remain reserved ABI requests for failures this implementation cannot produce. | F1219, re-verified F1225. `fault::site_tests::every_site_is_either_wired_or_named_as_having_no_counterpart` keeps the remaining `LockOp` divergence explicit; `fault::Info::timeout()` now reaches the VFS timeout hook from both production sites. | unowned |
| KI-0105 | OPEN | MISSING | high | **Three filesystem crates are still compiled by nothing and reachable from nowhere: `erofs`, `xfs`, and `btrfs`.** `squashfs` is now a production dependency of `syscalls` and is registered as a read-only block filesystem, but these three remain bare `lib.rs` scaffolds with no mount owner. The original row is narrowed rather than closed: registering a complete reader is real progress, while registering these three would fabricate support. | B2248; B2621. `crates/kernel/syscalls/src/fsmount_common/registry.rs` now resolves the source block device, mounts `squashfs::SquashFs`, publishes its root, and forces `SB_RDONLY`, matching Linux's `squashfs_get_tree`/`squashfs_fs_type` (`FS_REQUIRES_DEV \| FS_ALLOW_IDMAP`). `erofs`/`xfs`/`btrfs` remain workspace-only scaffolds with no production caller. | unowned |
| KI-0106 | OPEN | COVERAGE | low | **Nothing hosted can fail if the f2fs checkpoint merge stops PARKING correctly.** The queue, the exemptions, the one-write-per-batch pass and the dispatch are each covered on a hosted build, the last by lifting the handing-over out into a closure. What is not covered is `bg::run::kernel::delegate_checkpoint` itself -- the enrolment, the park on the batch counter, and the refusal that sends a caller released by a stop back to writing its own -- because there is no thread hosted and the file carries the kernel-only gate. A lost wake or a caller released by the stop reporting the previous batch's result would show only on a real boot. | F1225. `bg/run/kernel.rs::delegate_checkpoint`; `bg/run/hosted.rs` answers `None`. | unowned |
| KI-0107 | OPEN | COVERAGE | low | **Nothing hosted can fail if the f2fs halt path stops being INSTALLED.** `vfs::fs_halt` and f2fs's demand for it are both covered, and `syscalls::mount::install_vfs_hooks` is where the real halt is installed -- but that file carries `#![cfg(target_os = "oxide-kernel")]`, so a hosted test compiles none of it and removing the `set_fs_halt_hook` line reddens nothing. Only the kernel build catches its absence, and only if the symbol goes unused. The same hole covers every other hook installed there. | F1225. `crates/kernel/syscalls/src/mount.rs:6` is the gate; `install_vfs_hooks` has 14 hook installations and no test. | unowned |
| KI-0108 | OPEN | COVERAGE | med | **The whole f2fs verity suite depends process-wide on `require_signatures()` reading false, so no test can exercise the machine's knob against a real mount.** 28 tests open unsigned verity files under a default policy — which correctly defers to the machine — and go red the moment any test sets the knob. Two attempts measured: flipping the knob under the existing verity test lock reddens 2 tests; making the two obvious dependants state `set_require(false)` explicitly then reddens 28, because the dependency is everywhere the suite mounts. Closing it means either every verity test stating its own answer, or every one of them taking the same lock. | F1226. `cargo test -p f2fs`: 3577 passed, 28 failed with one knob-flipping mount-level test added; 0 failed with it removed. | unowned |
| KI-0109 | OPEN | MISSING | med | **Several f2fs sysfs attributes remain absent because their owning policy or accounting does not exist in this tree.** Remaining controls are `iostat_period_ms` and four lock-timing controls (`max_lock_elapsed_time`, `lock_duration_priority`, `adjust_lock_priority`, `critical_task_priority`). The distinct Linux reserved-block pool is now live: `reserved_blocks` and `current_reserved_blocks` have one owner for admission/statfs accounting, and `carve_out` changes only the reported total as Linux does. Request, discard, and GC idle thresholds are now live in the shared background owner and use the canonical activity clock. `reserved_segments` is now a live mount-owned allocator reserve, separate from the on-disk checkpoint value. `allocate_section_hint` and `allocate_section_policy` are now live volume-owned controls; regular new-section allocation applies Linux's NOHINT, WITHIN_HINT, and FROM_HINT boundary rules. `cp_interval` is now a live volume-owned checkpoint interval used by the periodic balance decision. `umount_discard_timeout` is now a live volume-owned unmount deadline; the final discard drain issues bounded batches and drops parked work after the deadline. `blkzone_alloc_policy` is now a live zoned allocator control; regular section search applies Linux's sequential/conventional preference and only-sequential fallback rules. `lifetime_write_kbytes` is now a live read-only report backed by physical block-write accounting and the durable hot-node summary journal extra-info field. `bggc_io_aware` is now a live background-GC policy; ordinary GC consults the configured all-I/O/read-I/O/none value against physical requests in flight, while urgent modes retain Linux's idle override. | Linux reserved-block/statfs behavior was traced through the live allocator and release paths; current owners are `crates/kernel/f2fs/src/volume/{space,write,segmap}.rs`, `volume/recover/data.rs`, `sysfs/{tunables,volume}.rs`, with focused statfs/sysfs tests. Atomic peak and defrag behavior are covered by focused atomic, defrag, and sysfs tests. The three idle thresholds are owned by `bg/state.rs`, published through `sysfs/knobs.rs`, and exercised by background and sysfs tests. `reserved_segments` is owned by `Volume::reserved_segments`; allocator, pinning, placement facts, and statistics read `gc_reserve()`, with a focused sysfs test. `unusable` now derives data/node holes from the loaded SIT and subtracts overprovision holes using the live reserve, with a focused clean-mount report test. Foreground and background checkpoint calls now increment the canonical counter at their actual entry owners and are published as read-only reports, with a focused sysfs test. GC foreground/background calls increment at the dispatch owners; moved data/node blocks accumulate at the segment migration owner and are published as read-only reports, with a focused sysfs test. `allocate_section_hint` and `allocate_section_policy` are published through `sysfs/tunables.rs`, validated there, and read by `zonewp::open_new_section`; the focused sysfs test proves the FROM_HINT boundary reaches the search. `cp_interval` is published through `sysfs/tunables.rs`, stored on `Volume`, and read by `cp_time_over`; the focused sysfs test proves the live interval changes the due decision. `umount_discard_timeout` is published through `sysfs/tunables.rs`, stored on `Volume`, and consumed by `bg::round::drain_discards`; the focused tests prove the live control and deadline predicate. `blkzone_alloc_policy` is published through `sysfs/tunables.rs`, stored on `Volume`, and consumed by regular `zonewp` section search; a zoned mount t … [trimmed; full text: scratch/archive/ledger-overflow.md KI-0109] | unowned |
| KI-0110 | OPEN | COVERAGE | med | 55 plain `.lock()` acquisitions remain in `mm-pmm`. The one on the page-free path deadlocked the machine; nothing audits the rest for reachability from interrupt context, and no gate can fail if another one joins that path. | B2233. `grep -c '\.lock()' crates/kernel/mm-pmm/src` excluding tests. The fixed case was found by a watchdog `kernel_pc`, not by any check in the tree. | unowned |
| KI-0111 | OPEN | MISSING | high | Ext4 DAX owns mount policy, persistent inode flags, partition offsets, shared-only physical-frame mapping, page-mkwrite allocation/conversion, and kernel-target direct byte I/O. The virtio-pmem child supplies `DaxRegion`, config/shared-memory discovery, DMA-backed flush requests, bounded polled completion, and config fallback. Real PMEM image coverage proves both `dax=inode` and `dax=always` mount successfully on SMP=1; SMP=2 graphical/runtime closure remains blocked by the independent scheduler idle-timer wedge. | `crates/drivers/drv-virtio-pmem/src/device.rs`; `crates/kernel/block/src/blockdev.rs::DaxRegion`; `crates/kernel/ext4/src/rootfs/inode/dax.rs`; Linux virtio-pmem and ext4 DAX reference paths. | B3015; provider/ext4 suites, both-arch checks, and real DAX image harness green; scheduler lane remains |
| KI-0112 | OPEN | MISSING | low | RSA and P-256 ECDSA are implemented. EdDSA and ML-DSA certificates still stop at their algorithm identifier with ENOPKG, and those algorithms' signature encodings remain absent. P-256 `id-ecPublicKey` certificates now validate `prime256v1` parameters and support Linux's X9.62 certificate signatures plus P1363 verification requests. | B1658, updated by `ae271219f`: `pkey::oid`, X.509 SPKI parsing, and `pkey::AsymmetricKey` now own P-256 ECDSA; `p256::ecdsa` carries the curve verification. EdDSA/ML-DSA remain unimplemented and keep this row open. | — |
| KI-0113 | OPEN | MISSING | low | **Only two HugeTLB granules are modeled.** The pool exposes 2 MiB and 1 GiB hstates; no additional hstates are available to the allocator. Boot-time `hugepagesz=`/`hugepages=` sizing is implemented and is no longer part of this gap. | B1981. `pmm::hugetlb::sizes` defines only `Huge2M` and `Huge1G`; `cmdline::hugepages` and `initialize_from_cmdline` own early sizing. | unowned |
| KI-0114 | OPEN | COVERAGE | med | **A huge shared-memory segment has never been mapped on a running kernel.** The decisions, errnos and their ordering, the span and the permission gate are all hosted-tested; that the segment actually maps huge frames rests on it reusing the `MAP_HUGETLB` path, which IS boot-proven — but that is an inference, not an observation of `shmat`. | B1981. No QEMU run exercised `shmget`/`shmat` with the huge flag. | unowned |
| KI-0115 | OPEN | COVERAGE | med | **The hugetlb cgroup controller has never run on a booted kernel.** Hosted tests drive the real tree and the real ledger, but nothing hosted can allocate a huge page, so four things stay unproven by test: that hugetlbfs faults route through the charged allocation, that the charging cgroup resolves under the kernel build, that the new lock ordering holds under real contention, and that a live cgroup directory shows the new files. The branch's smoke passes with the code in, which shows it does not break the boot — not that the controller works. | B1981. | unowned |
| KI-0116 | OPEN | DEFECT | low | **`v4l_id` dies on an indirect call through a null pointer during udev device processing.** The fault is an instruction fetch at address 0 (`error 0x14` = user + instruction fetch), so a function pointer the program expected to be filled in is zero — most likely a `dlopen`/`dlsym` result nothing checked, or a v4l2 ioctl that left a table unfilled. It fires once per boot and is not on the gdm path, but it is a real userspace crash our kernel's return values may be causing. | B2257, measured on one live-gnome x86_64 boot: `[49.930] v4l_id[112]: segfault at 0000000000000000 ip 0000000000000000 sp 00007ffe477268e0 error 0000000000000014 tid 4220`. The report carries no ` in <file>` tail because ip 0 is in no mapping, which is the reference's behaviour. | unowned |
| KI-0117 | OPEN | COVERAGE | low | The "a synthesised event record names the TRACEE" rule is pinned hosted (`a_synthesised_event_record_names_the_tracee_not_the_tracer`), but the CALL SITES that supply the vpid live in `target_os = "oxide-kernel"`-gated files (`stop.rs`, `sig.rs::interrupt`), so a hosted test cannot catch a caller that passes the wrong task. The in-guest probe is the only check on the wiring itself. | `101_ptrace/stop.rs::self_vpid`; `t_ptrace_siginfo` `addr_is_not_a_pid`. | — |
| KI-0118 | OPEN | MISSING | high | **Device-mapper's remaining thin metadata and full snapshot lifecycle semantics are incomplete.** `/dev/mapper/control` now creates and publishes `253:<minor>` mapper block devices, and table load/resume reaches `linear`, `striped`, `crypt`, `zero`, `error`, `delay`, `snapshot`, `snapshot-origin`, `snapshot-merge`, `thin-pool`, and `thin`. Snapshot state is shared per origin, origin chunks are preserved before snapshot writes, persistent exception metadata handles chunk zero, merge copies COW chunks back before retiring metadata, and the supported discard feature pair is validated and applied rather than ignored. Thin-pool owns Linux-sized block validation, thin IDs, virtual-block allocation, internal snapshot sharing, zero-on-first-write, discard unmapping, COW on shared writes, and versioned metadata reload/commit across pool reopen. The thin metadata format is not yet Linux's persistent-data btree with transaction abort/commit, space-map accounting, resize, or metadata snapshot APIs. The snapshot path still lacks Linux's full async exception handoff and merge lifecycle state machine. `DM_DEV_WAIT` owns a per-device event wait list and blocks on kernel builds until the event counter changes. The control file snapshots a global event number per open description, `DM_DEV_ARM_POLL` arms that description, and table events wake its shared poll queue. These remaining metadata and lifecycle gaps are observable LVM/device-mapper ABI gaps, not successful no-ops. | F1186. `crates/kernel/device-mapper/{control.rs,device,targets}`; `syscalls/src/016_ioctl/device_mapper.rs`. Linux `drivers/md/{dm-ioctl.c,dm-snap.c,dm-exception-store.c,dm-thin.c,dm-thin-metadata.c}`; source `cdc252ab0` publishes table-swap events before waking waiters, `3f932fd37` retries deferred removal from the block registry’s final-opener close hook, `5ad16acae` implements file-owned poll arming, `e290c0303` adds the shared snapshot-origin/COW foundation, `5d2db9a4b` adds merge metadata retirement and persistent chunk-zero encoding, `e209cca64` adds thin-pool/thin ownership and COW mappings, `9f82d34a9` adds thin metadata reload/commit, and `5adca108b` enforces snapshot feature/discard policy. Hosted device-mapper tests: 8/8; block tests: 264/264; full x86_64 kernel dependency check passes after snapshot policy changes. Linux persistent-data btree/space-map transaction semantics, thin resize/metadata-snapshot APIs, and full snapshot async/merge lifecycle semantics remain open. | unowned |
| KI-0119 | OPEN | MISSING | high | **MD's mutable array-management lifecycle remains incomplete beyond final stop.** `RAID_VERSION`, `GET_ARRAY_INFO`, and `GET_DISK_INFO` report canonical boot-published v1 arrays, including their actual partition device numbers, persistent roles, timestamps, layout and clean state. `GET_BITMAP_FILE` now returns Linux's zeroed 4096-byte pathname for an assembled array with no configured bitmap. `md::Array` owns a runtime member-fault transition: `SET_DISK_FAULTY` marks the selected assembled member once, serializes the control transition, synchronously advances the metadata event and update time, rewrites the version-1 role table on surviving members, excludes the member from I/O and flush, reports `FAULTY`, updates failed/active counts, and refuses to fault the last RAID1 member. `SET_ARRAY_INFO` now decodes and validates the Linux struct and accepts the exact no-change case; unsupported geometry/bitmap changes still return `EINVAL`. `md::Array` otherwise serves linear, RAID0 and RAID1 mappings under major 9. Boot-time `md::init` accepts MBR `0xfd` and GPT Linux-RAID partitions, validates version-1 metadata at all three placements, groups current sets by UUID, permits a degraded RAID1 set with missing active roles while still requiring complete linear/RAID0 sets, claims present parent disks, and publishes supported arrays as free `mdN` nodes. `mdadm` create/add/remove control, recovery/rebuild, bitmaps, spares, reshape, unequal-component RAID0 zones, RAID4/5/6/10, and full member-failure/replacement state machines remain absent. | F1231; F1236; F1237. Final `STOP_ARRAY` is fixed below; `9fed99fa1` adds Linux-shaped `SET_DISK_FAULTY` ownership and runtime I/O/reporting state; `2ba7da3a0` adds Linux-shaped `SET_ARRAY_INFO` struct decoding and no-change validation; `8124133b2` adds Linux-shaped `GET_BITMAP_FILE` empty-path reporting; `e266ff024` persists the faulty role and advanced event counter to surviving version-1 members; `34a7b9fe8` persists the Linux update timestamp and exposes it through `GET_ARRAY_INFO`. `crates/kernel/md/{lib,assembly,autostart,control,superblock,uapi}.rs`; `syscalls/src/016_ioctl/md.rs`; Linux `drivers/md/md.c` `md_ioctl`/`update_array_info`/`set_disk_faulty`/`get_disk_info`/`get_bitmap_file` and `include/uapi/linux/raid/md_u.h`. Focused MD tests: 14/14; `cargo check -p syscalls --lib` compiles the bridge. Recovery/rebuild, hot add/remove, and reshape remain open. | unowned |
| KI-0120 | OPEN | MISSING | high | **The shared SCSI layer now discovers USB logical units, provides SG_IO v3 for USB BOT-backed `sd*`, and routes ATA PASS-THROUGH(12/16/32) on AHCI-backed `sd*`, but is not yet a complete host mid-layer.** USB BOT obtains its bounded max-LUN value and directs each command to that LUN. The shared scanner identifies every reported LUN, obtains its capacity with the required 10-byte/16-byte transition, and publishes direct-access units as `sd*`; the SG_IO handler preserves the native v3 completion contract, validates admission against the open mode and raw-I/O capability, carries BOT failure as CHECK CONDITION plus REQUEST SENSE, and accepts flat or vectored transfers. AHCI wraps the same disk publication in the ATA owner, which validates and translates raw ATA CDBs, samples taskfile result registers, and bounds CDB length per transport so USB remains at 16 bytes. Dynamic host/target lifecycle, host-reset/error escalation beyond local safe retry, mode-page policy, tagged queueing, and virtio-SCSI remain absent. | F1229, F1232, F1234. F1235 added Linux-shaped fixed/descriptor sense normalization and bounded recovery to ordinary USB block I/O: retryable transport completions and safe current NOT READY conditions use a concrete host wait; removable-media unit attention fails; raw SG_IO retains its native CHECK CONDITION and sense without automatic retry. `crates/kernel/{scsi,ata}`; `crates/drivers/drv-{xhci,ahci}`; `syscalls/src/016_ioctl/{scsi,ata}.rs`. Hosted tests cover SG completion layout, permission admission, timeout normalization, CHECK CONDITION retention, transfer bounds, missing-LUN handling, the READ CAPACITY(16) transition, ATA CDB/taskfile layouts, ATA result sense, fixed/descriptor sense normalization, retry admission, and retry exhaustion; both kernel-target checks pass. | unowned |
| KI-0121 | OPEN | MISSING | med | **A PCR extend authorises with an empty password, not an HMAC session.** The reference has both paths: its integrity-protected extend wraps the command in an HMAC session derived through a salted P-256 ECDH exchange, and its `disable_pcr_integrity` path authorises with an empty password and is otherwise byte-identical. This implements the password path — a supported upstream branch, not a shortcut — because the session layer needs KDFa, HMAC and P-256 wired into `crypt`, which has none of the three. Without it a local attacker who can reach the chip's bus could forge an extend. | F1191. `tpm::device` 8 tests; the command image is asserted on the wire. | unowned |
| KI-0122 | OPEN | MISSING | high | No Bluetooth hardware transport on either arch. `btusb` cannot be written as an independent class driver: `crates/drivers/usb-core` is a 97-line control-packet builder with no device model, no probe/match registry and no endpoint abstraction, and every class (HID, hub, mass storage) is hand-wired inside `drv-xhci`'s own device state machine. An H4 UART transport cannot be written either: `drv-uart-16550` and `drv-uart-pl011` are process-wide singletons bound to `platform/serial0` with one `static DELIVER` RX callback, so a second port cannot be claimed. Closing either needs the owning subsystem restructured first | `crates/drivers/usb-core/src/{lib,control}.rs` (two files, no `Device`); `drv-xhci/src/probe.rs` `discover_hid`/`discover_hub`/`discover_mass_storage` called inline; `drv-uart-16550/src/lib.rs` `static BASE`/`static PORT`/`static DELIVER`, `matches` binds only `serial0` | — |
| KI-0123 | OPEN | COVERAGE | high | No boot can exercise a real Bluetooth controller. QEMU describes no Bluetooth device on either arch — `tools/xtask/src/image_qemu/{x86_64,aarch64}.rs` name none, and upstream QEMU removed its Bluetooth device model years ago — so the only transport a boot can reach is `/dev/vhci`, which is driven by a userspace process rather than hardware. Every claim about controller behaviour rests on hosted tests against the packet contract, not on a controller | `grep -i 'bt\|bluetooth\|hci' tools/xtask/src/image_qemu/*.rs` returns nothing; `drv-vhci` is the sole `HciTransport` implementation in the tree | — |
| KI-0124 | OPEN | MISSING | med | `drv-xhci` supports only bulk and interrupt endpoints (`context.rs` `enum EndpointType { Bulk, Interrupt }`); there is no isochronous support anywhere in the tree. A USB Bluetooth controller carries SCO voice on isochronous endpoints, so even once a class-driver boundary exists, a headset's audio link would have no transport | repo-wide grep for `isoch` returns zero; `crates/drivers/drv-xhci/src/context.rs:79` | — |
| KI-0125 | OPEN | COVERAGE | high | No available aarch64 QEMU machine describes an SCMI SMC service, so the direct shared-memory SMC call and its `a2p` completion interrupt have not run against firmware. The complete FDT graph (including GIC SPI/PPI completion specifiers), generic Performance wire protocol, GIC registration, per-AP PPI enablement, interrupt completion state, scheduler wait, token validation and 30 ms timeout are tested or target-built; only a platform service can prove the hardware/firmware exchange. Performance-limit notifications are not part of this SMC transport: it has no platform-to-agent receive channel. | `tools/xtask/src/image_qemu/aarch64.rs` has no SCMI service; `fdt::scmi::tests::{a2p_completion_resolves_its_named_gic_interrupt,a2p_completion_keeps_a_named_gic_ppi}`; `scmi` v3/v4 tests; `cargo run --quiet -p xtask -- kernel --arch aarch64 --check`. | unowned |
| KI-0126 | OPEN | COVERAGE | med | No boot exercises any ACPI power provider: QEMU q35 and virt describe no thermal zone, no `_PSS` and no `_CST`, so the thermal provider's namespace scan finds nothing and publishes nothing. The decode and ladder assembly are covered by hosted tests only; the AML path itself has never run | `firmware::acpi::init_devices` reports `thermal-zones=0` on both arches; same shape as the battery provider's coverage row | — |
| KI-0127 | OPEN | MISSING | high | **No USB Video Class driver, because the host controller cannot do isochronous transfers.** A real webcam is a UVC device and almost every one of them streams over an isochronous endpoint; the driver picks the smallest alternate setting whose periodic payload covers the negotiated `dwMaxPayloadTransferSize` and switches to it. `drv-xhci` implements control and bulk only — its transfer ring builds Normal TRBs, its endpoint contexts are configured for control/bulk/interrupt, and there is no isochronous TRB, no frame-id scheduling and no periodic bandwidth accounting. The bulk-only UVC fallback some devices offer is reachable, but a driver that supports only that would fail on most hardware, so none is written yet. With the V4L2 core and `drv-vivid` in place, the remaining work is the isochronous transfer type in the host-controller driver plus the UVC descriptor parser, probe/commit negotiation over EP0 class requests, and the payload-header depacketiser. | F1179. `grep -ri 'iso' crates/drivers/usb-core/src crates/drivers/drv-xhci/src` returns one unrelated comment in `ports.rs`; `usb-core` is 97 lines and owns setup-packet policy only. `docs/64§12`. | unowned |
| KI-0128 | OPEN | MISSING | med | `VIDIOC_EXPBUF` refuses with `EINVAL` on every queue: there is no exporter that can turn a buffer plane into a descriptor another subsystem imports. The buffer type and index are still validated first, so a caller learns which of the two was wrong before it learns exporting is unavailable, and an application falls back to the mapped model — but a zero-copy pipeline from camera to compositor is not possible. | F1179. `v4l2::ioctl::bufs::expbuf`; hosted test `expbuf_refuses_rather_than_returning_a_descriptor_that_names_nothing`. | unowned |
| KI-0129 | OPEN | MISSING | med | No media-request support. `V4L2_CTRL_WHICH_REQUEST_VAL` is refused with `EINVAL` rather than answered with the live values, and the `InRequest` buffer state exists in the state machine with nothing that can put a buffer into it. A stateless codec, which is what requests exist for, cannot be driven. | F1179. `v4l2::ctrl::access::check_which`; `v4l2::vb2::state::BufState::InRequest` has no producer. | unowned |
| KI-0130 | OPEN | MISSING | low | Only the `videoN` minor range is populated. `radioN`, `vbiN` and `v4l-subdevN` have their ranges reserved in `v4l2::ids` and no node type behind them, and there is no media controller (`/dev/media0`) or sub-device API. A camera whose sensor and ISP are separate entities cannot be configured. | F1179. `v4l2::ids::{RADIO,VBI,SUBDEV}_MINOR_BASE` have no consumer. | unowned |
| KI-0131 | OPEN | COVERAGE | med | The target-gated half of `v4l2` has no hosted test and no boot assertion beyond the node appearing: the `mmap(2)` cookie-to-page walk, the blocking-dequeue park and wake, the plane page allocator, and the inode's poll subscription are exercised only by a running kernel. The cookie walk in particular scans backwards from the faulting offset to find a plane base, and a wrong bound there is a mapping that silently resolves to the wrong buffer. Closing this needs either a userspace capture probe under `userspace/` asserting mapped frame contents, or the resolution moved into an ungated function over a plain plane table. | F1179. `crates/kernel/v4l2/src/node/{fileops,ctx,frames}.rs` are `#[cfg(target_os = "oxide-kernel")]` and contain no `#[cfg(test)]`. | unowned |
| KI-0132 | OPEN | MISSING | high | **There is no ACPI embedded-controller owner, so the common laptop battery/adapter event route still stops before `Notify`.** The direct FADT `_Lxx`/`_Exx` GPE path can execute a method, but real laptop firmware commonly assigns a GPE to a `PNP0C09` controller: the handler must drive the EC command/status protocol, issue Query (`0x84`) when `SCI_EVT` is set, and invoke the returned `_Qxx` method, which then emits the battery/AC notification. EmbeddedControl OperationRegions are likewise rejected by the sole backend, so provider methods that read battery state through the EC fail. | `pci_boot::config_access::operation_region_access` has no `RegionSpace::EmbeddedControl` arm and repo search finds no `PNP0C09`, EC status/command protocol, Query command, or `_Qxx` dispatcher. | unowned |
| KI-0133 | OPEN | COVERAGE | med | The ACPI power and backlight providers have no boot that exercises them: QEMU's default x86 and aarch64 machines describe no `PNP0C0A`, no `ACPI0003` and no `_BCL`, so `firmware::acpi::init_devices` publishes zero devices on every smoke boot. The direct SCI → `_Lxx`/`_Exx` → `Notify` → provider chain is therefore component-tested but no machine fixture asserts the complete interrupt-to-uevent boundary. Decode, unit scaling, level normalisation and class rules are hosted-covered; AML evaluation of `_BIF`/`_BIX`/`_BST`/`_BCL` against a provider-bearing namespace is not. | F1171, B2279. `firmware::acpi::devices::init_devices`; event tests prove GPE mask/defer, the AML test proves direct-device `Notify`, and provider/class tests prove refresh/reconfiguration separately. A synthetic DSDT plus fake register backend and observable class hook is needed to make the whole boundary fail as one check. | unowned |
| KI-0134 | OPEN | MISSING | low | The reference registers a tripless thermal zone for every power supply that declares `temp`, and a set of LED triggers driven by charge state. The generic power-supply thermal projection now exists; the remaining gap is the LED-trigger owner, which still does not exist under `crates/kernel`. | F1171; Linux `drivers/power/supply/power_supply_core.c::psy_register_thermal` and `power_supply_read_temp`; `power_supply::registry::register` creates an event-driven `thermal::ZoneDesc` for declared `Property::Temp`, converts tenths of °C to millidegrees, and unregister removes it. Hosted power-supply 41/41 and thermal 123/123 pass. | unowned |
| KI-0135 | OPEN | MISSING | low | `BL_CORE_SUSPENDRESUME` is declared by the backlight class and consulted by nothing: there is no system-suspend path to call `update_status` across (`T1-c` in `scratch/system-compat.md` — no suspend owner of any kind). The bit becomes live when suspend does. | F1171. `backlight::uapi::BL_CORE_SUSPENDRESUME` and `BacklightOps::options` have no caller. | unowned |
| KI-0136 | OPEN | MISSING | med | `drv-hda` has no power-management callbacks: no runtime suspend of the codec or the controller, and nothing that survives a system suspend. After a resume the controller would need its link reset, its rings restarted and every pin, amplifier and selector reprogrammed. Boundary with the suspend/resume framework lane. | F1173. `drv_hda::probe::HdaDriver` implements `probe`/`remove`/`shutdown` only. | unowned |
| KI-0137 | OPEN | MISSING | high | **AQC113 (`1d6a:04c0`) has a native PCI/net lifecycle but is not yet proved usable on hardware.** It owns BAR and bus-master lifetime, MSI/MSI-X/INTx binding, hard-IRQ masking, deferred RX polling, controller teardown, resident-firmware reset, transaction-consistent capability snapshot, host-active acknowledgement, firmware-selected primary MAC filter, resolver-table setup, link-rate programming, and IOMMU-mapped queue memory. The remaining closure is multi-queue RSS/QoS programming, complete receive/transmit data paths, and real-device traffic evidence; it must not be marked supported before all exist. | `drv-atlantic::{atl2_reset,atl2_mailbox,atl2_dma,atl2_program,atl2_filter,atl2_controller,imp}` is registered by `pci_boot`; 20 hosted tests; both custom kernel targets compile `pci-boot`. | fix/debug-boot-ahci-trait |
| KI-0138 | OPEN | MISSING | high | **External DRM modules cannot yet use the complete atomic modeset helper family.** DMA-BUF now has file-backed export/get/put and attach/detach ownership, and shmem PRIME import creates an imported GEM object with the attachment and reservation ownership required for teardown. Atomic-state construction, default validation, and asynchronous-update validation now have canonical owners; commit and legacy bridge paths remain separate owners, as do suspend/resume state duplication. | The host Bochs module now has 9 unresolved symbols: `drm_atomic_helper_{commit,disable_plane,page_flip,set_config,shutdown,update_plane}`, `drm_{fbdev_shmem_driver_fbdev_probe,mode_config_helper_resume,mode_config_helper_suspend}`. | unowned |
| KI-0139 | OPEN | MISSING | med | Atomic default validation does not yet alter transactions for panel self-refresh exit or queue self-refresh entry work. Drivers that initialize self-refresh state therefore cannot use the completed general helper as their final atomic-check callback until the self-refresh owner and its work lifecycle exist. | `linux_drm_atomic_helper_check::drm_atomic_helper_check` has the modeset/zpos/plane/async sequence; no self-refresh helper owner is registered. | unowned |
| KI-0140 | OPEN | MISSING | high | **The host `atlantic.ko` cannot yet load unchanged.** Its 178 imports now resolve 161/178 against Oxide's Linux KPI. The net-device ABI is BTF-matched and traffic-class fields/operations now use the Linux offsets and validation rules. Remaining imports include IRQ affinity programming, PTP, BPF/static-key, XDP packet conversion, hardware-monitor ownership, and platform MAC retrieval; no partial loader admission may claim this module until each ABI and lifetime contract is present. | Host Fedora `atlantic.ko.xz`; `tools/kpi-audit --fail-on-missing` reports 17 missing. `--host-btf /sys/kernel/btf/vmlinux --fail-on-abi-mismatch` reports every supported KPI layout MATCH-PINNED; `linux_netdev::core::tests` pins traffic-class offsets and behavior. | fix/debug-boot-ahci-trait |
| KI-0141 | OPEN | MISSING | high | **PCI identity must remain an exact `(segment, requester-id)` key through every PCI and IOMMU boundary.** `pci::Bdf`, canonical addresses, selected MCFG window, ECAM access, PCI scan, driver-model registration, virtio transport teardown and virtio-gpu DRM identity now retain the segment, and out-of-window accesses are refused rather than aliased. Firmware now publishes a complete bounded MCFG inventory atomically. F940 makes bus-master admission an exact `Bdf` membership decision published only after the IOMMU/DMA ownership transition, so equal requester IDs in separate segments cannot authorize each other. The remaining closure is mapping/selecting every host-bridge window and carrying the exact segment through the Linux `pci_dev` facade. No device may be attached to a DMAR/IVRS unit by requester ID alone. | F865; F940. `drivers/pci/src/types.rs::Bdf`; `iommu::bus_master_admitted`; `firmware::acpi::tables::decode_mcfg`; `pci::enumerate_segment_buses`; `pci_boot::virtio_transport`; `drv_virtio_gpu::VirtioGpuDev`; validated DMAR/IVRS units carry `segment`. | F865-pci-segment-identity |
| KI-0142 | OPEN | MISSING | high | **AMD-Vi and VT-d create boot identity domains, attach every scoped requester before driver probing, invalidate the published hardware tables, and only then enable translation. VT-d selects domains at enabled PCI isolation boundaries, keeps unisolated downstream requesters and multifunction siblings together, and gives every resulting group its own IOVA allocator and page-table root. AMD-Vi first forms each complete IVRS-alias group, maps every member's firmware-required range into the shared group domain, and only then attaches its DTEs. F941 routes every bound Linux PCI `dma_map_*`/`dma_unmap_*` request through a requester-keyed IOMMU identity-domain owner; it retains the mapping until an exact requester/IOVA/length unmap, while non-PCI devices remain the Linux direct-DMA case. The initial IOVA equals PA by design, preserving device compatibility while no dynamic IOVA allocator exists. VT-d now selects queued invalidation on units that advertise it, publishing global context and IOTLB descriptors after each live PTE mutation; units without that capability retain the register path. Generic PCI DMA-alias construction, fault handling, capability-dependent page-table depths, teardown, non-identity dynamic IOVA allocation, and a transactional multi-unit failure path remain absent.** Translation must consume PCI segment/requester identities through the existing DMA symbols, not add a parallel driver API. | F865; F941; F1069; F1070. `iommu::{activate_amd_vi,activate_vtd,map_dma,unmap_dma}` run before `pci_boot::register_pci_model_drivers`; `modules::linux_dma` resolves the bound exact BDF before every PCI mapping. Hosted IOMMU and modules tests plus both kernel-target `pci-boot` checks pass; no physical activation evidence yet. | F865-pci-segment-identity |
| KI-0143 | OPEN | MISSING | high | **x2APIC admission now uses one all-active-IOMMU contract: every live AMD-Vi unit must advertise GA+XT, and every live VT-d unit must advertise EIM, before the LAPIC may enter x2APIC mode.** AMD-Vi GA uses the 128-bit IRTE format and XT is the required full 32-bit destination extension; a GA-only or legacy unit keeps the BSP in xAPIC mode. AMD and VT-d route owners retain `u32` destinations, while an unremapped IOAPIC fallback rejects a destination above 255 rather than truncating it. Remaining closure is physical AMD scale coverage with a wide APIC ID and a mixed-capability negative case. | `iommu::{AmdViIrMode::x2apic_capable,amd_vi_x2apic_capable,x2apic_interrupt_remapping_capable}`; `arch_irq::lapic::enable_x2apic`; `arch_irq::ioapic` direct fallback. Hosted IOMMU decision-table and format tests pass; physical AMD scale coverage absent. | F1091-amd-vi-x2apic-remap |
| KI-0144 | OPEN | MISSING | high | **VT-d primary faults now have a complete interrupt owner:** a platform MSI is allocated independently of PCI, each active DRHD receives its fault-message registers, the hard handler drains valid capability-indexed records, clears each record before progressing, and acknowledges the primary-fault status bits. The common x86 MSI owner now targets the BSP APIC rather than hard-coding APIC ID zero; direct PCI delivery refuses a destination above 255, while a VT-d fault message uses its dedicated high-address destination field. Remaining closure is physical fault injection and delivery to a driver-visible fault-reporting owner. | `iommu::{VtdFault,VtdRegisters::drain_primary_faults,handle_vtd_fault_interrupt}`; `pci_boot::vtd_faults`; `arch_irq::{request_platform_msi,msi}`. Hosted format, capability-layout, and destination-encoding tests pass; no physical DRHD fault record yet. | F1092-vtd-fault-telemetry |
| KI-0145 | OPEN | COVERAGE | med | **Nothing on this box can distinguish the two class numberings the `/sys/fs/selinux` transaction nodes accept, so the check for it skips.** `selinuxfs::nodes::transaction::file_tests::a_class_whose_two_numberings_differ_is_answered_in_the_published_one` scans the loaded Fedora `policy.34` for a class whose policy value differs from the kernel classmap value AND whose two readings produce different labels; several classes differ in value (`system` is policy 3, kernel 4) but none produced a different answer for the pairing tried, so the test prints `skipping` and asserts nothing. The numbering fix is pinned only by the synthetic-fixture test in the `selinux` crate. A policy that reorders classes relative to the kernel classmap is what would expose it, and none is in the tree. | B2263. Run with `--nocapture`: `skipping: no class on this policy distinguishes the two numberings`. The fixture test that DOES fail on the defect is `selinux::services::transition::tests::a_class_written_by_userspace_is_read_in_the_policys_own_numbering`. | unowned |
| KI-0146 | OPEN | MISSING | high | **There is no TPM character device, so no userspace can reach the chip.** The driver landed with both wire transports and a `Chip` that marshals and transmits real commands, but nothing probes a chip and nothing publishes `/dev/tpm0` or `/dev/tpmrm0`. Everything a Fedora userspace uses a TPM for is therefore unavailable: `systemd-cryptenroll` and `systemd-cryptsetup` TPM2 disk unlock, `systemd-pcrphase`/`pcrlock`, tpm2-tss and tpm2-tools, clevis. The missing pieces are a probe and a chardev, not the protocol. | F1191 merged the driver; `grep -rn tpm crates/kernel/devfs/src crates/drivers/` returns nothing. `crates/kernel/tpm/src/{tis,crb}.rs` carry the transports. | unowned |
| KI-0147 | OPEN | MISSING | high | **IMA measures nothing: it has no hooks and no securityfs surface.** The measurement list, template digests and the extend path into a real chip are implemented and tested, and `ima` depends on `tpm` — but no crate depends on `ima`, so nothing measures at exec, mmap or open, nothing appraises a signature, and `/sys/kernel/security/ima/` does not exist. A measured boot is therefore not merely unattested, it is not attempted. Blocked on the two rows above: it needs LSM hooks to be called from, and it needs the chip to be reachable. | `grep -rln '^ima *=' --include=Cargo.toml crates/` returns nothing. `crates/kernel/ima` has 170 passing tests and no caller. | unowned |
| KI-0148 | OPEN | MISSING | med | **AppArmor and TEE do not exist.** Branch `F1191-ima-tpm-tee-apparmor` planned IMA, TPM, TEE and AppArmor as one group; only IMA and TPM were built. AppArmor is a security module the LSM framework would carry alongside SELinux and Landlock, and a Fedora userspace expects one of the two to be present. There is no `crates/kernel/apparmor` and no `crates/kernel/tee`. | `ls crates/kernel/{apparmor,tee}` — both absent. Recorded so the branch name is not mistaken for the delivered scope. | unowned |
| KI-0149 | OPEN | MISSING | med | **The TPM extend authorises with an empty password rather than an HMAC session.** Both are real upstream paths — the reference's own no-integrity branch is byte-identical apart from the auth area — so this is a weaker choice rather than an invented one. The session variant needs KDFa, HMAC and P-256 key exchange. Also: IMA counts a refused extend in `tpm_failures` but does not audit it, where the reference emits an audit record naming the failure. | F1191. `crates/kernel/tpm/src/device.rs` header records the choice; `ima::MeasurementList::tpm_failures`. | unowned |
| KI-0150 | OPEN | DEFECT | med | **One aarch64 boot lost its root filesystem mid-sysinit: `mkdirat /var/lib/systemd/pstore` answered EIO, five udev workers were killed by SIGBUS, and `systemd-zram-setup@zram0` exited 1.** SIGBUS on a userspace fault is the mmap'd-page-cannot-be-faulted answer, and EIO on a plain `mkdirat` says the same thing from the syscall side, so both point at the block/ext4 read path rather than at any one service. Seen ONCE, in the only aarch64 boot so far that got past journald, and with an x86 image build running concurrently on the same host — so it is not established whether it is a kernel defect or host contention. Recorded because the shape (a filesystem that starts answering EIO under load) is one nothing else in the tree would catch. | B2244 arm probe boot, guest log kept at `scratch/evidence/b2244/arm-probe-3.log`: `[55.195] [NAMEI] mkdirat path="/var/lib/systemd/pstore" err=5`, `[55.617] systemd-udevd[81]: 0000:00:05.0: Worker [93] terminated by signal 7 (BUS).` and four more, `[55.859] systemd-zram-setup@zram0.service: Main process exited, code=exited, status=1/FAILURE`. Three other aarch64 boots of the same image in the same session show none of it. | unowned |
| KI-0151 | OPEN | COVERAGE | med | **`/proc/<pid>/stack` still cannot provide Linux's symbolized task trace.** It now walks the saved off-CPU frame-pointer chain within the task's guarded kernel stack and emits bounded raw `[<0>] 0x<return-address>` lines, with the Linux ptrace and `CAP_SYS_ADMIN` gates. The remaining gap is the kernel symbol resolver and Linux `%pB` symbol/offset rendering; a sleeping-task wedge is no longer silently represented by an empty stub. | B2621. `sched::stack_trace::saved` owns the saved-context/guarded-stack walk; `procfs::live::pid_files::pid_stack_body` owns the procfs gates and renderer; sched 1558/1558 and procfs 253/253 pass. Linux `fs/proc/base.c::proc_pid_stack` uses `lock_trace`, `stack_trace_save_tsk`, and `%pB`. | unowned |
| KI-0152 | OPEN | MISSING | low | **`/proc/<pid>/wchan` answers a `file:line` source position where the reference answers a kernel SYMBOL NAME, because this kernel ships no complete `kallsyms` equivalent.** The file answers because each blocking entry point records its own `&'static Location` rather than unwinding and resolving an address, so the value is a real answer to the same question in a different spelling — but a tool that greps `wchan` for a function name (`ps -o wchan`, some `top` builds) still will not match. Closing this needs the in-kernel symbol resolver and stack unwinder; the Linux blocked-task gate itself now matches `get_wchan`: current tasks and queued/runnable tasks return `0`, while a blocked task is not rejected merely because its context-switch window still reports `on_cpu`. | B2247, B2621. `../reference/kernel/sched/core.c::get_wchan`; `sched::park_site::reportable` and `procfs::live::pid_files::pid_wchan_body`; sched 1558/1558 and procfs 253/253 pass. | unowned |
| KI-0153 | OPEN | INFRA | high | **Whether this branch boots to the login marker is UNDETERMINED: every boot attempt tonight ran against a sibling lane's concurrent boots.** Four attempts on two arches all timed out at their deadline with systemd spinning in `sched_yield`, but `ps` showed worktree `kernel-F1179` running its own x86 AND aarch64 boots for the whole window, which is exactly the contention CLAUDE.md warns produces false failures. A control boot of the same kernel with `selinux=0` ALSO timed out, under the same contention, so it distinguishes nothing either. What the boots DID establish, because contention cannot suppress it, is the absence of the atomic storm above. This needs one uncontended boot on an idle box before anything is concluded about the login marker. | F1180. `ps` during the window: `qemu-system-x86_64 -pidfile /home/nd/oxide/kernel-F1179/...` and `qemu-system-aarch64 -pidfile /home/nd/oxide/kernel-F1179/...` plus `boot-smoke.sh arm 900` / `x86 900`. Task dump at the timeout: `1 3235774466 systemd R ... sched_yiel 3056186724 561082`. | F1180 |
| KI-0154 | OPEN | DEFECT | high | **A boot stops with PID 1 parked in `clone3` behind a vfork child parked in `execve`, and neither ever returns.** This is the remaining every-boot gate failure. The signature is identical in BOTH captured x86 occurrences, pre-fix and post-fix, down to the tid and the syscall count: `40 4147 systemd S execve nsysc=3 cputime_ms=0 ptid=1` beside `1 systemd S clone3`. `cputime_ms=0` says the child has never accumulated a full millisecond of CPU, so it blocks EARLY in `execve` — long before the `vfork_done` publication at the end of it — and the parent is a victim rather than the bug: parking a vfork parent until the child execs or exits IS the reference's behaviour. **The rest of the machine is healthy**, which is new and is what makes this tractable: post-fix, both CPUs tick (heartbeat ages 2-8 ms), `preempt_count` is `0x0` on the parked CPU, `resched=0`, `ttwu_pending=0`, `wake_list=0`, and every task in the registry is `S` with nothing runnable. So this is a LOST WAKEUP or a lock nobody releases inside early `execve` — not a spinning CPU, not a stranded deferred wake, and not a CPU that cannot be interrupted. **Checked and excluded:** the deferred-wake-list stranding B2244 suspected (the new per-CPU dump reads `ttwu_pending=0 wake_list=0` on both CPUs), and the `irq_exit` preempt-count underflow another lane reported (`preempt_count=0x0` on the parked CPU; the `0x10000` on the peer is that CPU's own hard-IRQ field while it runs the dump). **B2265 (this lane) made the answer arrive by itself and did NOT reproduce it.** The wchan instrument (#5561) is in place and would print for this exact task — it is `S`, `on_rq=n`, `on_cpu=n`, so `park_site::reportable` admits it — but the retained logs predate the instrument and the wedge is 1-in-12, so no capture names the sleep yet. Rather than boot for it, B2265 shipped the reference's `khungtaskd`: any task in an UNINTERRUPTIBLE sleep for `hung_task_timeout_secs` with no context switch now logs `INFO: task <comm>:<pid> blocked for more than N seconds. tid=… last-syscall-nr=… wchan=<file>:<line>`, unconditionally (no debug feature), and `tools/boot-smoke.sh` treats that line as a fail marker. Every candidate sleep in early `execve` is uninterruptible, so the NEXT occurrence names its own sleep site with no operator at the console and no extra boot. **Enumerated and excluded by reading:** there is no `cred_guard_mutex` analogue held across early exec — our `exec_update_lock` equivalent (`ThreadGroup::try_exec_update`) is a `try_write` taken LATE (`059_execve/x86_64.rs`, after the ELF load) and returns `restart_nointr` on contention rather than sleeping, so the shape that classically produces this cannot; `sys_clone` holds no lock across `wait_for_vfork_done`; the sleeping `Mutex`, `WaitList` and `wait_event` families all enqueue under the condition gate before testing, closing the lost-wakeup window. **Leading suspect, unproven:** `BlkState::acquire_turn` (`crates/drivers/drv-virtio-blk/src/modern/wait.rs`) parks with `deadline_ns = 0` — the ONLY untimed wait on the path early `execve` takes to read the exec image, and the only virtio-blk wait without the `IO_TIMEOUT_NS` deadline its twin `wait_for_completion` uses to poison a stuck device. Its condition (`!busy && pending.is_empty() && deferred.is_empty()`) is republished by exactly one `BLK_TURN.wake_one()` per event from two sites; a single lost or mis-targeted one strands the waiter forever on an otherwise idle machine, which is the observed shape. The driver already carries a fixed lost-wakeup of this family (B1426). NOT diagnosed — do not "fix" it by adding a timeout, which would be a workaround; find whether a wake is lost and where. **Next step:** run the lockstep smoke as normal. The first occurrence now fails the gate with the sleep site in the log; take the `wchan=` line to the owning subsystem. | B2245. Post-fix `scratch/evidence/b2245/x86-m2-7.log`: the pair at 111.3 s and again at 114.3 s, unchanged; `[sysrq] per-cpu heartbeats` at 116.3 s and 122.3 s showing both CPUs alive with zero counts; `[NMI-BT] cpu=1 rip=…8008f959 rflags=0x286` — the instruction after `sti; hlt`, interrupts admitted. Pre-fix `x86-m1-4.log` carries the same task pair. Rate: pre-fix x86 5 of 6 valid runs clean, post-fix 11 of 12 — the same order, so the fix did not close this. | unowned |
| KI-0155 | OPEN | DEFECT | high | **An aarch64 boot stops at ~49.7 s mid-sysinit and answers no typed SysRq at all — the hard-wedge half of the every-boot gate.** Distinct from the x86 row above, which answers SysRq and produces a full dump. Here the serial log ends at `Starting modprobe@dm_mod.service` and the injected `<NUL>t`/`<NUL>w`/`<NUL>p`/`<NUL>l` sequence produces nothing — not even the NMI/FIQ backtrace, which is delivered through a masked interrupt mask and should answer a CPU that is merely spinning. Nothing answering the FIQ poke either means both CPUs are stopped in a way that does not take a Group-0 SGI, or the serial receive path itself is gone. B2245's idle-park fix did not remove it (it reproduced once in 8 post-fix boots). | B2245, `scratch/evidence/b2245/arm-m2-8.log`, 45 kernel lines, last line 49.686 s, 0 `[sysrq]` lines after injection. Post-fix aarch64 rate 6 of 8 clean; the other failure is the separate `[BADSTACK]` stack-overflow row, not this. | unowned |
| KI-0156 | OPEN | MISSING | MED | `kexec_file_load` does not build the ELF core headers that describe the OLD kernel's memory, and passes no `elfcorehdr=` to the crash kernel. The reference constructs them in the kernel for a file-loaded crash image. Without them a crash kernel that boots has no way to find what it is supposed to dump. Not needed for the crash kernel to COME UP, which is why it is separate from the boot itself. | B2048. `kexec::file_load::kexec_file_load` appends nothing to the command line; no `/proc/vmcore` exists to read the headers either. | unowned |
| KI-0157 | OPEN | DEFECT | high | **A uniprocessor x86_64 boot wedges in early userspace: a task spins forever in a kernel spinlock whose owner is not running.** Measured with the boot gate's own liveness probe on one prebuilt image: `-smp 1` answers 2/5 boots, `-smp 2` answers 3/3. A wedged guest stops at the same place every time — right after journald's runtime-journal flush, while udev workers and systemd are opening devices — and never switches task again (`[WATCHDOG] soft lockup: no reschedule for 10s`, then `no-progress: 0 context switches for 40s`, with `switches=` identical in both dumps). Two independent captures name the stall site exactly: a `sys_openat` of a console device parked in `Spinlock::lock_irqsave::<PortInner<VtConsoleDriver>, Tty>` from `TtyStruct::open_inner`, and a `block::registry::open_by_dev` parked in `Spinlock::lock::<Vec<Arc<Disk>>, Devices>`. The owner is off-CPU in both: with one CPU nothing can run it, and the spinner holds interrupts masked so no tick, no wakeup and no reschedule can reach it. A second CPU picks the owner up, which is why every gate in the tree — all of which run `-smp 2` — passes. Not yet root-caused: the specific point at which an owner gives up the CPU while holding the lock is unidentified. Ruled out with evidence, so the next lane need not retry them: cross-CPU call/TLB-shootdown wait (`call_function_many` returns immediately when `online_count() <= 1`, so no shootdown is ever issued); a reschedule taken from `preempt_enable`/`preempt_check_resched` with interrupts masked (fixed on this branch and measured — post-fix `-smp 1` is 1/5, unchanged within the sample). | B2047. Captures under `scratch/evidence/` are reproducible with `OXIDE_SMP=1 ./tools/boot-smoke.sh x86`; a live guest attached with gdb shows `#3 Spinlock::lock_irqsave<PortInner<VtConsoleDriver>,Tty> #4 TtyStruct::open_inner #6 vt_console::on_open_file #8 vfs::file::open::open_file_at #13 sys_openat`, and QMP register sampling pins RIP inside that one `cmpxchg`/`pause` loop across every sample. `make smoke-up-x86` is the gate that fails on it. | — |
| KI-0158 | OPEN | DEFECT | CRITICAL | The final scheduler-change smoke reached early systemd on x86_64, then a `systemd-udevd` worker faulted at guest time 12.887 s and no liveness response followed. This is userspace memory/ABI failure evidence, not a scheduler conclusion: the serial log names the faulting user instruction but contains no kernel fault or scheduler diagnostic. Current main does not reproduce that udev failure: one fresh SMP boot reached `basic.target` and answered serial RX; the next stopped on the separately tracked task-stack guard fault at 9.295 s, with no udev segfault. The original retained `/tmp` log is gone, so this remains open pending a fresh reproduction or a kernel-side causality proof. | Historical F1090 x86 smoke attempt 1: `(udev-worker)[89]: segfault` after journald flush; final liveness marker absent. Fresh campaign 2026-08-24: `/tmp/oxide-boot-smoke-x86-UBmkEt.log` passed; `/tmp/oxide-boot-smoke-x86-Jn3eAO.log` hit `[FAULT] BUG: TASK stack guard page was hit` and no udev segfault. | unowned |
| KI-0159 | OPEN | COVERAGE | high | **Nothing in this tree ran a uniprocessor boot until B2047 added one, and CI still runs no boot at all.** `tools/boot-smoke.sh` defaults `OXIDE_SMP=2` and every derived smoke target inherits it, so any defect whose correctness depends on a second CPU making progress was invisible to every check simultaneously — B2057 was exactly that defect, and it reached a user's interactive `make qemu-x86` (which defaults `SMP ?= 1`) while every gate passed. `make smoke-up-x86` / `smoke-up-arm` / `smoke-up` exist and run in the pre-push hook after the SMP pair. Remaining: neither pair runs in CI, and a hook is not a gate — a lane that pushes with `SKIP_SMOKE=1` still gets no uniprocessor boot. | B2047 added the targets; B2057 is the first defect they caught and the first observation of the aarch64 half at one vCPU (PASS, 157 s). `Makefile` `smoke-up*`; `.githooks/pre-push` gate 3. | unclaimed |
| KI-0160 | OPEN | INFRA | low | The preempt gate `sync`'s locks take is an `AtomicPtr` load plus an indirect call on EVERY acquire and release, and the count it drives is an atomic read-modify-write on a per-CPU slot. The reference spends one non-atomic per-CPU increment. Correct but not cheap, and it is now on the hottest path in the kernel (every `kalloc`). The Linux-shaped end state is the count living low enough in the dependency order to be incremented inline. | B2057. `sync::preempt_gate::{acquire, release}`; `sched::preempt::preempt_count_add_local`. No measured regression — `-smp 1` x86 boots 64-77 s across 5 runs, against 62-84 s recorded for `-smp 2` before the change — so this is a shape note, not an observed cost. | unclaimed |
| KI-0161 | OPEN | DEFECT | med | **A relocated kernel comes up with ONE processor: the secondary cannot be restarted after this kernel's machine stop.** Both aarch64 relocation paths print `psci: failed to boot CPU1 (-22)` and `smp: Brought up 1 node, 1 CPU`, on a guest booted with two. The stop parks the secondaries somewhere the firmware's processor-start call will not take them back from; the reference stops them in a state the next kernel can start. Invisible unless the relocated kernel's own CPU count is read, and it halves the machine. Confirmed SEPARATE from the LPI defect above (F859) and still open after it: the line appears in runs that now reach userspace. It is not independent of it, though — the relocating CPU becomes the new kernel's boot CPU, which decides which redistributor that kernel inherits, so this row's behaviour is what made the LPI defect intermittent. `-22` is the firmware's already-on answer, consistent with a secondary that was never taken down. | F858, F859. `scratch/evidence/kexec-arm-relocation-userspace.log`, `kexec-arm-file-load.log` and `kexec-arm-file-load-userspace.log` all carry the line; the same guest boots two processors before the relocation. | — |
| KI-0162 | OPEN | COVERAGE | med | The published `/sys/firmware/devicetree` tree is a snapshot taken once at init, not a live view of the nodes: a runtime device-tree change would not be reflected. Nothing in this tree mutates the device tree yet, so the snapshot is currently indistinguishable from a live view. | F847. `sysfs::devicetree::init` registers `plan(blob)` once from `sysfs::init`; there is no re-registration path and no per-node object for a driver to attach to. | — |
| KI-0163 | OPEN | MISSING | low | The kexec identity tables now cover the firmware description ranges the boot memory map described, alongside usable RAM and every segment destination — an image that reads them before building any mapping of its own no longer faults. TWO sources remain uncovered. (a) The EFI system table: its physical address is not retained anywhere past early boot, so there is nothing to map; retaining it is a boot-handoff change owned by the boot-stack lane, not this one. (b) An MMIO debug console: on x86_64 the debug console is port-addressed and needs no mapping, but on aarch64 it is MMIO and is not in the plan. | F849. `kexec::machine::plan::ranges_for` takes RAM, segments AND firmware ranges; `pmm::setup::firmware_regions` retains them at memmap ingestion. Positive controls: firmware ranges dropped from the plan -> 1 RED; a firmware range not aligned down to its containing block -> 1 RED; restored -> GREEN. Grep for a retained EFI system table address returns nothing, and the path checked (`boot-info`) has no field for one. | boot-stack lane for (a) |
| KI-0164 | OPEN | COVERAGE | low | The aarch64 syscall root clears the stack-depth ceiling by 40 B (12960 of 13000). Any single 48-byte frame added anywhere on the UDPv6-transmit-to-AF_PACKET-tap chain re-fails the gate, and the chain has ~20 frames spread over six crates, so the next lane to touch any of them trips it. The margin is thin because the chain itself is long, not because the ceiling is wrong: it is a ladder of working sets that are each live across their callee, which is why splitting made it worse rather than better. Wants a structural pass — the reference delivers the egress tap inline over an RCU list, where this materialises four `Vec`s and two `Observation` copies. | B2038. `make stack-gate-arm`, deepest path via `--show-path`. | — |
| KI-0165 | OPEN | MISSING | low | `/proc/kcore`'s note segment carries the process-status and process-information notes and the core-information note, but not the third process note a core file normally carries (the one holding the subject's task structure). Nothing that reads this file consults it — the text base comes from the core-information note — but its absence changes the segment's byte length from what a consumer computing sizes from a template would expect. | F849, rechecked against Linux 7.2.0-rc4 `fs/proc/kcore.c`: the reference appends `NT_TASKSTRUCT` with the live `current` task and sizes it with `arch_task_struct_size`. Oxide has no Linux-layout task structure or task-note snapshot owner; serializing the Rust `sched::Task` object or padding the note would fabricate an incompatible descriptor. Closing this needs an explicit task-note ABI/source, not only a note-count increment. | — |
| KI-0166 | OPEN | MISSING | low | `/proc/kcore` describes the kernel image and every usable RAM range through the direct map, but not the vmalloc range: this kernel publishes no stable bounds for it, so memory allocated there is invisible to a debugger reading the file, which sees a hole of zeroes instead. | F849. `procfs::kcore::live::regions` takes `pmm::setup::usable_regions` plus the image extent and nothing else. | — |
| KI-0167 | OPEN | COVERAGE | med | Nothing checks that `/proc/kcore` describes THIS machine's real regions. The ELF assembly is covered against a synthetic region list, but the fetch that names the live regions and the copy that reads their bytes are kernel-only, so a wrong direct-map base or a wrong image extent would produce a perfectly well-formed file describing the wrong memory and no hosted test could fail. Wants one boot observation: read the file, walk its program headers, and confirm a described address reads back the bytes at that address. | F849. `procfs::kcore::live` is `#[cfg(target_os = \"oxide-kernel\")]`; the 24 hosted tests all drive a synthetic map. | — |
| KI-0168 | OPEN | MISSING | med | `KEXEC_PRESERVE_CONTEXT` is refused with EINVAL. RECORDED PREVIOUSLY AS PARITY; IT IS NOT. The bit is live in the ABI on the platform class this kernel targets, where a load carrying it is accepted and the loaded image returns control to the kernel that started it — so the refusal is OURS. It stands only because nothing here can bring this kernel back: the contract needs the whole return path — user processes frozen, devices suspended, core system state saved on the way out and restored on the way in — and NONE of it exists (`crates/kernel/power` has no suspend path of any kind; grep for a process freeze or a device-suspend pass over the driver model returns nothing). Accepting the bit and ignoring it would turn 'resume afterwards' into 'never resume' silently, which is worse than an errno, so EINVAL is the right answer TODAY and the row stays open until the return path is built. Severity raised from low: this is a missing subsystem, not a flag. | F849 corrected the record. `kexec::uapi::KEXEC_FLAGS` omits the bit; `tests::order::preserve_context_is_refused_until_this_kernel_can_be_resumed` pins the errno and states in the test what has to change when the return path lands. | needs its own lane — suspend/resume, not kexec |
| KI-0169 | OPEN | COVERAGE | HIGH | A crash-kernel boot has NEVER been observed. Everything up to the jump is host-tested — the grammar, the placement, the region-bound staging, the try-locking on the panic path — and the ordinary kexec relocation IS observed end to end, but nothing has yet panicked a machine that had an image staged and watched a second kernel come up. Until that happens the claim is "each step is checked", not "it works". What it needs: a guest booted with the parameter, `kexec -p` against it, a deliberate panic, and the second kernel's own banner on the same serial stream — the same shape as the evidence already captured for the reboot path. | F849. `scratch/evidence/` has the reboot-path relocation and nothing for the crash path. | — |
| KI-0170 | OPEN | DEFECT | low | This kernel is 48-bit throughout: the translation-control value every CPU installs fixes both the address-space size and the output-address size at 48 bits, so a machine with more physical address bits than that cannot use them. kexec inherits the limit rather than causing it — its identity map now derives its reach from that register (row above) and refuses a plan past it with an errno. Lifting it needs the large-physical-address table format and a wider output-size field programmed everywhere the register is written, which is a translation-layer change, not a kexec one. | F849. `hal_aarch64::regs::TCR_EL1_BASE` pins the size fields; `kexec::machine::plan::arm_idmap_limit` reads them and is the only consumer that currently asks. | — |
| KI-0171 | OPEN | INFRA | med | Repository prose describes behaviour by ATTRIBUTION rather than by stating it — comments and doc-comments across the tree explain what the code does by pointing at an external implementation instead of naming the ABI, the errno ordering or the invariant. That is exactly backwards for a contract that is supposed to be re-checkable from the tests alone, and it is a documentation rule this repository already has. 52 such sites remain in `crates/kernel/kexec` alone after F849 cleaned the files it touched; the pattern is repo-wide, not specific to that crate. | F849 rewrote every such site in the files it edited (`machine/{plan,x86,arm}.rs`, `frames.rs`, `validate.rs`, `store.rs`, `uapi.rs`, `tests/{order,file}.rs`) into statements of the behaviour, with no test change — the tests were already the provenance. The remainder is a mechanical sweep nobody has owned. | — |
| KI-0172 | OPEN | DEFECT | med | A listed-but-ignored parameter is still a false claim of support: any key a table lists that the filesystem does not honour keeps the option-support probe lying about that key. Applies to every option of the 17 filesystems that discard their data string. Each needs an implementation or omission from the table — the choice cannot be made silently. | Same registry listing. | — |
| KI-0173 | OPEN | MISSING | low | `security_watch_key` / `security_post_notification` have no LSM hook here, so watching is gated by `KEY_NEED_VIEW` alone and a record is never withheld from a watcher whose credentials an LSM would reject. Matches a kernel with no LSM stacked, which is what this one is. | B1659, `ops/watch.rs::watch_key_core`. | — |
| KI-0174 | OPEN | MISSING | low | No `/proc/sys/kernel/keys/gc_delay`. Deliberate: `Store::collect()` runs inline with no delay timer, so the knob would gate nothing — and a sysctl that reads back but changes no behaviour is the defect class this project bans. Add the knob only when a deferred gc exists. | B1649, negative result. | — |
| KI-0175 | OPEN | MISSING | low | No `refresh_tss_limit` equivalent: the reference re-loads TR when a VMX exit has truncated the TSS descriptor limit to 0x67, which would otherwise put the I/O permission windows outside the limit and #GP a legitimately granted port. This port runs no VMX guest and nothing here truncates the limit, so the condition cannot arise today; it becomes live the moment a hardware-virtualisation host path is added. | B2023. `hal-x86_64/src/tss/io_bitmap.rs`; the reference's own call is conditional on a per-CPU `tss_limit_invalid` flag that only VMX sets. | — |
| KI-0176 | OPEN | MISSING | low | `crashkernel=<size>,cma` is parsed and reported but has no owning contiguous-memory allocator. Its size is neither lent to the page allocator during normal operation nor reclaimed as a crash-time CMA range, so accepting the suffix does not provide the reserved movable memory Linux exposes. The separate `crashkernel=<size>,low` handoff is complete: ARM crash loads publish the reserved crash range followed by the low-memory window. | Linux `kernel/crash_reserve.c` reserves the CMA range and `kernel/crash_core.c` carries it through crash shrink/release; `crates/kernel/kexec/src/crashk/parse.rs` stores `CrashKernelSpec::cma` but `crashk::boot::reserve` has no CMA owner. The low-memory handoff is fixed by `27e596679` and covered by `file_load::arm_image` tests. | — |
| KI-0177 | OPEN | COVERAGE | med | `kexec::crashk::boot::reserve` — the one side effect in the crash-reservation path — has no test that can fail. Its two inputs (the boot line and the usable ranges) and its decision (`parse_line` + `place`) are covered by 55 hosted cases, but the wiring between them, the `reserve_early` call and the `publish` are exercised only by a real boot. A reservation that silently did nothing would look identical to a machine booted without the parameter. | F849. `crashk/boot.rs` reads `pmm::setup::pmm_static()`, which is `None` hosted, so every path returns early. | — |
| KI-0178 | OPEN | COVERAGE | med | **No gate in this repo compiles any crate's `cfg(target_os = "oxide-kernel")` code unless that crate is in `kmain`'s dependency tree.** `make feature-gate` passes `-p kmain -p <boot> -p <bin>` only (`tools/xtask/src/cmds.rs:95-107`), so it reaches a crate solely through the dependency graph; `hosted-gate` and `test-build-gate` are host-target and compile none of it. A workspace crate that nothing depends on therefore has its entire kernel-only surface type-checked by nothing. Demonstrated on `sunrpc/src/clnt/park_kernel.rs`, whose 60 lines of `sched` wait/wake integration no routine gate builds. The fix is a per-crate kernel-target check paralleling `tools/hosted-check.sh` - a repo-wide tooling change, so F1188 filed it rather than crossing the boundary from a filesystem lane. | F1188, positive control: `let _: u32 = "THIS IS NOT A u32";` appended to `park_kernel.rs` gives `error[E0308]` under `cargo check -Z build-std=... --target targets/x86_64-unknown-oxide-kernel.json -p sunrpc`, while `cargo test -p sunrpc` reports `98 passed; 0 failed` and `make feature-gate-x86` returns rc=0. Same class as the documented `cargo check -p <crate>` null gate, one level out. | unowned |
| KI-0179 | OPEN | INFRA | high | **The composed rootfs image carries NO extended attributes at all, so every file is unlabelled while `/etc/selinux/config` says `SELINUX=enforcing SELINUXTYPE=targeted`.** A Fedora install labels the tree at install time; this image never was. Consequence: with a policy loaded, every file resolves to the mount's `fs_use_xattr` default rather than to its real type, so any enforcing decision is made against the wrong label — and the "SELinux is enforcing" claim in the guest's own config is not what the filesystem can support. Owned by the sibling `../images` repo (composition needs sudo), not by the kernel. | B2253, measured read-only: `debugfs -R "ea_list /usr/lib/systemd/system/polkit.service" images/out/gnome-x86_64-root.img` prints nothing and `ea_get ... security.selinux` reports "Extended attribute key not found". `debugfs -R "cat /etc/selinux/config"` on the same image prints `SELINUX=enforcing` / `SELINUXTYPE=targeted`. | ../images |
| KI-0180 | OPEN | INFRA | med | **A `CARGO_TARGET_DIR` that two worktrees have both built into produces bogus compile errors that look exactly like a real defect.** A mis-parsed parallel launch (`cd <wt> && (job1) & (job2) & wait` runs job2 in the ORIGINAL cwd — the trap CLAUDE.md already documents) built the aarch64 kernel from the MAIN tree into this lane's `tgt-arm`. The next aarch64 check, run correctly inside the worktree against that same directory, failed with `error[E0432]: unresolved imports selinux_runtime::inode::{answers_getsecurity, getsecurity_value}` — items that were present on disk and compiled fine on x86_64 and on the host. `rm -rf tgt-arm` and re-running gave rc=0 with 150 log lines naming the worktree and 0 naming the main tree. Lesson to pair with the existing rule: after a mis-parsed launch, DELETE the target dir it polluted; do not trust its next result either way. | B2253, measured: `arm2.log` (rc=101, the E0432) vs `arm3.log` (rc=0) on identical sources, the only difference being a wiped target dir. | unowned |
| KI-0181 | OPEN | INFRA | med | `cargo test --workspace` produces false ext4 failures under the default parallel harness: eleven quota and extent tests across `quota_cleanup_failures_image`, `quota_internal_cleanup_failures_image`, `quota_internal_cleanup_more_image` and `quota_remount_image` fail together, and every one of them passes when the same command is re-run with `--test-threads=1`. The integration tests share on-disk image fixtures and race each other over them. A lane that reads a red workspace run as a regression in its own change will be chasing nothing. B2815 removes the large-copy `/tmp` failure, but does not yet give every integration fixture an isolated workspace. | F1176; B2815 `cargo test -p ext4 --tests --no-fail-fast -- --test-threads=4` passed after the large-image storage fix. The workspace-wide shared-fixture race remains unclosed. | unowned |
| KI-0183 | OPEN | INFRA | med | **Driver multidev/rebind smoke coverage is now zero and is recorded as gone, not fixed.** B570–B599 driver-path proofs (sysblock `/sys/dev` reverse index, sysbus bind, virtio net/blk/gpu/snd multidev, rng/uart/ps2/parent-child rebind, shutdown quiesce, vsock round-trip, msix net rx, userspace seat) had a live in-guest assertion until `7aacadf4c`; it has been zero since. Rebuilding it needs a probe-staging path in `../images` (or a `rootfs_disks/` injector per probe), which is out of scope here. The QEMU device topologies those smokes used are KEPT (`image_qemu/{x86_64,aarch64}.rs`, `OXIDE_VIRTIO_*_MULTIDEV_SMOKE` / `OXIDE_STORAGE_MULTICTRL_SMOKE`), so `OXIDE_VIRTIO_NET_MULTIDEV_SMOKE=1 make qemu-x86` still gives the multi-device machine and the driver klog. | deleted: `boot-smoke-{driver-path,msix-net-rx,ps2-rebind,shutdown,storage-multictrl,sysblock,sysbus-bind,uart-rebind,userspace-seat,virtio-blk-multidev,virtio-gpu-multidev,virtio-net-multidev,virtio-parent-child-rebind,virtio-rng-rebind,virtio-snd-multidev,vsock}.sh` | unowned |
| KI-0184 | OPEN | INFRA | med | The 218 `t_*.c` conformance programs stay C and are NOT part of the Rust probe rewrite. They are the live N22 differential corpus, and their value comes from being compiled identically for host and guest — rewriting them in Rust would be ~9k LOC of translation for no additional test signal, and would risk silently changing an oracle. Recorded so the "no C in this repo" goal is not assumed complete. | `userspace/glibc_conformance/` 8,988 LOC after `t_crypto` removal | unowned |
| KI-0185 | OPEN | INFRA | med | `docs/00-master-plan.md:260`, `docs/43-acceptance.md:76` and `docs/40-ci.md:46` all make a RELEASE GATE out of "Kernel-completeness audit `docs/kernel-audit.md` shows no stub regressions" — and that file was deleted in `3643d52c2` (July 2026). Three release gates cite a document that does not exist, so the gate cannot be evaluated and has silently been vacuous. spec-lint does not check file-path citations inside prose, only `<doc>§<sec>` refs, which is why it never surfaced. | `git log --diff-filter=D -- docs/kernel-audit.md`. Not fixed here: choosing the replacement gate (or dropping it) is a spec decision, not a lint lane's call. | — |
| KI-0186 | OPEN | INFRA | med | `af_packet_diff` (2,034) and `wait_diff` (4,640) stay C, deliberately. Both compare a guest run against a REAL Linux run of the same binary, and `tools/wait-diff-selftest.sh` is the only harness in this repo that proves its own probe can fail. Porting one side alone moves the oracle. **Condition for moving them:** the Linux-side oracle is rebuilt in the same language in the same PR, with the selftest re-established first. Same rule already agreed for the 218-program `glibc_conformance` corpus. | `tools/boot-smoke-{af-packet-diff,wait-diff}.sh` diff `$LINUX_RECORDS` against `$OXIDE_RECORDS` | unowned |
| KI-0187 | OPEN | INFRA | med | Docs still reference the deleted crates and the abandoned write-our-own-libc plan: `docs/59` (the glibc spec, DRAFT — should go entirely), `docs/00§3` phase-ladder rows 27 (`crates/user/ldso` dynamic linker) and 28 (oxide-libc + NSS + PAM), `docs/03`, `docs/52§123`, and the FROZEN `docs/07`/`29`/`29a`. Deliberately NOT fixed here to keep this PR pure code deletion; all of it lands in the `R` revision lane, which needs revision blocks for the FROZEN three anyway. | `grep -rn 'crates/user' docs/` → 13 hits across 8 files | R lane |
| KI-0188 | OPEN | INFRA | med | `FEAT_S1POE` could not be exercised on real silicon or emulation: QEMU 9.2.4 implements it on neither `cortex-a72` nor `max`, so the aarch64 boot proves only that `setup_poe` correctly does nothing and does not fault on a CPU without the feature. The x86 half WAS proven — `-cpu host` on a Ryzen 7960X reports PKU/OSPKE and the boot logs the detection line. The arm enable path stays unproven until a QEMU with S1POE or real ARMv8.9 hardware is available. | F788; `qemu-system-aarch64 --version` 9.2.4, no `detected:` line under either CPU model. | — |
| KI-0189 | OPEN | INFRA | med | `docs/27` R02 recorded a cBPF-only `bpf(2)` subset with "eBPF + verifier + JIT tracked as phase 23"; `27§5` body still says seccomp filter mode returns ENOSYS with BPF "phase 23". Deferral framing survives in the body and conflicts with the no-v2 rule (`02§9` rule 8). Not fixed here (spec-content change, out of scope for a flattening PR). | `27§5` lines "Now: returns ENOSYS for filter mode (BPF tracked as phase 23)" | unowned |
| KI-0190 | OPEN | INFRA | med | **Project scope is internally contradictory.** `docs/00§3,§9` says every Linux subsystem is in scope except 32-bit and big-endian support; `docs/03§2,§5,§6` explicitly drops many syscall, filesystem, protocol, and architecture families. `scratch/system-compat.md` can therefore identify present and absent systems, but cannot truthfully classify all absences as either required work or deliberate exclusions until one authority wins. | D553 source audit: `docs/00` lists full Linux scope, while `docs/03` excludes cgroup v1, many address families/protocols, legacy filesystems, 32-bit ABI, and more. | unowned |
| KI-0191 | OPEN | INFRA | med | `git stash` is shared across ALL worktrees of this clone, so two lanes stashing/popping concurrently swap trees: B1654's stash was popped into another lane's worktree and a raw-IP lane's WIP landed in B1654's. Recovered via `git fsck` dangling commits + `git stash store`. Lanes must never use `git stash` on this box; commit temporarily on the lane branch instead. | Observed 2026-08-01: `git stash pop` in the B1654 worktree restored 16 unrelated `raw4`/`sol_ip` files and dropped B1654's tracked edits. Damage still visible in `git stash list`. | — |
| KI-0192 | OPEN | INFRA | low | Citation debt: repository text must not name/path-link/quote external implementation sources. 273 files carry `.c:NNN`, 113 mention `include/uapi`, 50 carry `.h:NNN`, 10 name the local reference tree by path. | Pre-existing tree-wide; B1641 added zero. | — |
| KI-0193 | OPEN | INFRA | low | `debug-atexit` is excluded from the routine gate: it is mutually exclusive with `debug-stderr` by design (`020_writev` selects the richer `[DYNERR]` tracer when atexit is on), so enabling both hides the `debug-stderr` writev block from the type checker. Covered by the separate non-routine `make feature-gate-atexit`. | Cfg is `all(feature = "debug-stderr", not(feature = "debug-atexit"))`. | — |
| KI-0194 | OPEN | INFRA | low | The reaper walks `/proc/[0-9]*/fd/*` rather than using `lsof`, to avoid a tool dependency in the boot gate. On a box with thousands of processes that walk is not free; it runs once per attempt, so the cost is bounded, but a very busy runner may notice. | C249. | — |
| KI-0195 | OPEN | INFRA | low | `code/klog-ungated` treats `klog::kfatal!` and `klog::kerror!` as requiring a `debug-<sub>` feature gate, the same as `kdebug!`. A fatal or error log that only exists in a debug build is not a diagnostic anyone will have when it matters, and it makes "log the detail, then abort with a literal" — the natural fix for `code/panic-fmt` — cost a new finding. | `tools/spec-lint/src/code_lint/klog.rs:74-80` lists all five macros in one `MAC_NAMES` table. Worth deciding before the 625-finding `klog-ungated` backlog is worked. | — |
| KI-0196 | OPEN | INFRA | low | Build time is NOT the win and should not be reported as one. Cold `cargo clean && cargo build --workspace`: **17.7 s → 15.0 s** wall (1m39 → 1m25 user, 24 jobs). ~15%. The win is 55,868 LOC leaving the tree and an unconfounded conformance channel (C256), not seconds. | measured in this worktree, both runs from an empty `target/` | C258 |
| KI-0197 | OPEN | INFRA | low | `crates/shared/kalloc/src/holes.rs` (933) and `src/tests.rs` (651) are over the 500-line split cutoff and were left unsplit by this lane. Both are under the 1000 hard cap. `holes.rs` is the free-list data structure under live corruption-diagnostic work; splitting it is a separate lane's call. | B1690. `wc -l crates/shared/kalloc/src/*.rs`: holes.rs 933, tests.rs 651; every other file ≤ 284. | — |
| KI-0198 | OPEN | INFRA | low | `/run/udev/data/n1` (loopback) is a stale zero-byte file dated 1970 shipped in the rootfs image; udev never rewrites it. Cosmetic, but it makes "does lo have a udev record" unanswerable by listing alone. | B1717. `ls -l /run/udev/data/` in guest. | images repo |
| KI-0199 | OPEN | INFRA | low | The matrix `IMPL` status CANNOT date a release: 72 of 385 rows are `PARTIAL`/`NEEDS-REWORK` and they are spread across every era (`16:ioctl`, `42:connect`, `46:sendmsg` are 1.x/2.x syscalls). Read literally, "newest release where every syscall introduced at or before it is `IMPL`" yields a bound below 2.6 — because the status tracks AUDIT CLOSURE, not presence. Presence is the property a release number claims; audit closure is tracked separately and must not be conflated with it by the next lane that revisits this number. | `scratch/syscall-compliance-matrix.md` status column: 291 IMPL / 56 PARTIAL / 16 NEEDS-REWORK / 22 LINUX-ENOSYS | — |
| KI-0200 | OPEN | COVERAGE | low | **Which parameter table each filesystem is registered with is provable only by the kernel build.** `fsmount_common/registry.rs` is target-gated, so no hosted test can fail if a type is wired to the wrong table or to none. Measured, not assumed: breaking a symbol in it left `cargo test -p syscalls` green at 1423 passed while the kernel check went red. The tables and their enforcement are fully hosted-tested; only the wiring is not. | B1981, positive control 3 of the parameter-table lane. | unowned |
| KI-0201 | OPEN | INFRA | med | **The ratchet baseline drops a key the moment its count reaches zero, so a fully-cleared rule silently stops being a ceiling.** `write_baseline` emits no zero rows by design, so a crate+rule that a burndown lane cleared has no entry at all -- and `regressions()` reads an absent key as baseline 0. That is correct for catching the next violation, but it means the gate cannot distinguish "this rule never applied here" from "this was clean and has now regressed", and nothing warns when a cleared rule silently reacquires findings. Emitting `0` rows for previously-tracked keys would have surfaced the current 678-finding pile incrementally instead of in one lump. | B2248. `tools/spec-lint/src/ratchet.rs` `regressions()`: `let b = base.get(k).copied().unwrap_or(0);`. `code/safety-missing`, `code/safety-short`, `code/panic-fmt` and `len/over-cap` all carried real nonzero baselines as late as `1fac426bf`, were burned to zero that week (`bbec7ea8a`, `00c001b03`, `f57e60a1c`), and vanished from the file. | unowned |
| KI-0202 | OPEN | COVERAGE | med | **Nothing in `procfs` can be tested through an actual mount.** `fs_impl`, `static_files` and `live` are all `#[cfg(target_os = "oxide-kernel")]`, so `ProcfsFs::new` / `build_root` / the lookup and readdir enforcement have no hosted reach — a test written against them is a phantom, which is why this lane's mount test was deleted rather than shipped. Every DECISION is ungated and tested (`fs_info`, 16 tests); the WIRING is proven only by the guest probe recorded above. Closing it needs the root-building split so an inode tree can be built hosted, the way `tmpfs` already can be. | `cargo test -p procfs` builds none of `fs_impl`; the deleted `tests/mount_identity.rs` failed to compile with `unresolved import procfs::fs_impl`. | unowned |
| KI-0203 | OPEN | MISSING | med | **Four cgroup2 flags are accepted and stored but cannot yet be acted on, because the mechanism they gate does not exist here.** They are declared because omitting a name fails a mount the reference accepts — a container runtime passing `memory_recursiveprot` would not mount at all — but no code reads them yet: (1) `memory_recursiveprot`: `memory.min`/`memory.low` are stored on the node and read by NOTHING; there is no effective-protection computation of any kind, recursive or not, so there is no behaviour to switch. (2) `pids_localevents`: `pids.events` is the hardcoded literal `"max 0\n"` — no pids-max-breach counter exists to be local or hierarchical. (3) `memory_hugetlb_accounting`: no hugetlb exists in this kernel at all. (4) `favordynmods`: a locking-strategy choice with no user-visible semantics and no dynamic-modification cost model here. Each becomes enforceable when its controller does; `RootFlag`'s own doc comments say which are enforced today. | `crates/kernel/cgroup/src/root_flags.rs` `RootFlag`; `tree/files.rs:44` (`"pids.events" => "max 0\n"`); `mem_low`/`mem_min` have no reader outside their own read/write handlers. | unowned |
| KI-0204 | OPEN | COVERAGE | med | **The cgroup2 flag path has no in-guest proof.** The boot mounts `/sys/fs/cgroup` from the kernel with no options and this image's systemd passes none, so a normal boot exercises only the default. A probe that mounts with `-o nsdelegate` and reads `/proc/mounts` was attempted twice: once it hit the `#DF` above, once it passed spuriously because the marker `^[0-9]` matched a kernel timestamp line rather than the command's output — a probe-design error, recorded because the same loose-marker mistake would pass any future probe. Parse, merge, render and both enforcement paths are hosted-tested; what is unproven is only that the registry constructor receives the blob, which is the same wiring the procfs lane proved in-guest for an identically-shaped constructor. | This lane. Correct probe: a marker that cannot appear in kernel output, e.g. the quote-split nonce the harness itself uses. | unowned |
| KI-0205 | OPEN | MISSING | blocker | **Native PCI xHCI now exists and is registered before PCI driver probing:** class `0x0c0330`, IOMMU-mapped DMA pages, controller reset/run, MSI/MSI-X binding, root-port address/configure, USB HID boot keyboard/mouse reports, platform-keyed evdev publication, and root-port connect/disconnect handling through the USB softirq. The old claim that no xHCI or bus-agnostic input existed is false. USB mass-storage descriptor discovery, Bulk-Only/SCSI CBW/CSW contracts, generic bulk endpoint-context construction, retained requester-keyed bulk DMA owners, non-overwriting transfer-completion handoff, native configuration sequencing, native CBW/data/CSW transfer submission, initial INQUIRY/READ CAPACITY transaction sequencing, canonical `sd*` block-device publication, and page-vector payload transfers now exist. One transaction can map up to 255 IOMMU DMA pages and submits chained Normal TRBs with a completion only on the terminal buffer. Root addressing now flows through an xHCI topology owner that preserves a physical root port plus route string. Hub-class status endpoints are parsed, configured, submitted, completion-validated, DMA-invalidated, and rearmed through that same transport owner; the hub descriptor is fetched in two stages, then its protocol/TT facts update the xHCI hub slot context. Hub initialization powers every downstream port, waits its one descriptor-provided power-good interval, reads changed/initial port status in a process worker, acknowledges change features, resets connected ports, and publishes descendant slots with the parent route string. The port reset-control transaction drops controller/device locks before completion waits and recovery delay. Descendant detach and recursive topology are still absent. The adapter owns the SCSI disk reservation until detach, rejects reuse when the canonical registry cannot detach an open disk, and serializes every command through the controller-then-device transaction gate. The shared block registry owns the reusable SCSI `sd*` identity allocator, so SATA and USB storage do not publish through competing counters; a shared SCSI midlayer, full hub topology, HID report-descriptor support beyond boot protocol, and physical xHCI exercise remain. The Linux module facade remains a shim and is not the native-driver path. | `drv-xhci::{probe,probe_hub,storage_block,irq,completion,usb,storage,context,hid,device}`; `block::{registry,BlockDevice}`; `pci-boot::register_pci_model_drivers`; `input::{InputDeviceKey::platform,install,publish_evdev}`; `irq::take_port_changes` → `probe::service_port_changes`. | B2111-xhci-linux-audit; B2112-usb-mass-storage; B211301-xhci-bulk-endpoints; B2113-xhci-storage-dma; B2114-xhci-completion-queue; B2115-xhci-storage-probe; B2116-xhci-bot-execution; B2118-block-sd-name-allocator; B2119-xhci-controller-ownership; B2120-xhci-controller-handle; B2121-xhci-storage-transaction; B2122-xhci-storage-block-device; B2124-xhci-storage-scatter-gather; B2125-xhci-hub-topology; B2126-xhci-hub-endpoint; B2127-xhci-hub-port-control; B2128-xhci-hub-port-status; B2129-xhci-hub-child-enumeration |
| KI-0206 | OPEN | MISSING | med | **VT-d interrupt remapping now resolves the firmware-selected IOAPIC to its exact DMAR source ID, allocates a source-verified IRTE, and programs the IOAPIC's remappable RTE format through the shared x86 IRQ owner.** VT-d activation now retains every published VT-d unit so the IOAPIC-owning unit has live IR state even when it owns no scanned PCI endpoint. Remaining x2APIC prerequisites are HPET remapping and scalar CPU-targeting migrations. | `firmware::{ioapic_id,acpi}`; `iommu::{vtd,vtd_ir,vtd_manager}`; `arch-irq::program_x86_ioapic`; `hal-x86_64::ioapic::program_remapped_redirect` | B2100-vtd-ioapic-interrupt-remapping |
| KI-0207 | OPEN | MISSING | med | **HPET firmware identity and VT-d source ownership are now retained as one contract.** The ACPI HPET decoder publishes its MMIO base and block number; VT-d resolves that block number through the DMAR HPET scope, allocates an HPET-qualified IRTE, and returns a remapped FSB message only after selective invalidation. A published HPET with no trustworthy scope is refused. Remaining closure is a real HPET clockevent owner using this message path instead of the current LAPIC-only timer path. | `firmware::{hpet_pa,hpet_id}`; `iommu::{intel_vtd_hpet_source,allocate_vtd_hpet,VtdIrte::hpet}`; `smoke/src/device_map/x86.rs` | B210101-vtd-hpet-interrupt-remapping |
| KI-0208 | OPEN | MISSING | HIGH | x86 PCI MSI/MSI-X delivery no longer exhausts after 16 device vectors: the IDT now provides 48 contiguous external-device heads from `0x50` through `0x7f`, and the existing allocator/handler/line owners scale with that pool. Linux still owns a per-CPU IRQ-domain vector matrix with affinity migration and a broader vector space; Oxide must replace the global fixed pool with that mechanism before claiming full high-device-count hardware coverage. | `hal-x86_64::irq::{irq_stub_addr,VEC_MSI_POOL_LEN}`; `arch-irq::msi::tests::x86_allocator_exposes_the_entire_vector_pool`; x86 HAL target build. | F996-x86-msi-vector-capacity |
| KI-0209 | OPEN | MISSING | med | **Physical RTL8125 support now has a loadable-driver path, not a native partial-driver claim.** The target `10ec:8125` is covered by the host Fedora `r8169` module: all 187 undefined symbols resolve against the supported KPI, host BTF matches every pinned C layout, and its x86 relocations are all loader-supported. `finit_module` now honors the Linux force-vermagic flag and records the forced-load taint, so an unmodified host module can be admitted after the ABI audit even though its distro release string differs. The remaining closure is a physical-module boot with the matching firmware files and link traffic; Intel I225/I226, Aquantia, and X550 still need their own selected module/ABI audits. | F865; B2093. `tools/kpi-audit --fail-on-missing` reports 187/187 symbols exported for Fedora `r8169.ko`; `--host-btf /sys/kernel/btf/vmlinux --fail-on-abi-mismatch` reports every supported layout MATCH-PINNED; `readelf -rW` reports only `R_X86_64_{64,PC32,PLT32,32S}`. `modules::linux_netdev::core::tests` and `modules::linux_pm::core::tests` pin managed netdev and PM/wakeup behavior. | F865-pci-segment-identity |
| KI-0210 | OPEN | COVERAGE | low | `dns_resolver` is registered so the domain tag has a production consumer, but nothing in the kernel REQUESTS a `dns_resolver` key yet — the type is reachable only through `add_key`/`request_key` from userspace. | `crates/kernel/fs/src/keyring/types.rs`. The tag itself is exercised by `keyring::tests::namespace`. | unassigned |
| KI-0211 | OPEN | MISSING | med | Nothing sets `EntryAbi::Compat`: there is no 32-bit syscall entry on either arch (`sched::personality::domains::SUPPORTS_32BIT_COMPAT == false`, no ia32/AArch32 dispatch table), so the compat layout is reachable only from tests today. The ABI is implemented and provable; the entry table that would select it is absent. | `crates/kernel/sched/src/personality/domains.rs:46`; `hal-aarch64/src/vbar/asm.rs:52` ("AArch32 vectors — unused"). | unassigned |
| KI-0212 | OPEN | COVERAGE | low | Compat pointer FIELDS (`msg_name`, `msg_iov`, `msg_control`, and the 32-bit iovec array) are decoded and offset-tested, but no test follows one to real memory: a hosted test cannot place a buffer at an address that fits in 32 bits. The conversion and the offsets are covered byte-for-byte instead; end-to-end coverage needs the 32-bit entry above plus a guest. | `recv_user::tests::a_compat_iovlen_is_read_from_its_own_offset` and siblings cover the offsets; `msg_layout::tests::cmsg` covers the bytes. | unassigned |
| KI-0213 | OPEN | COVERAGE | low | The eleven `016_ioctl` files B2051 converted are target-gated, so the conversions themselves have no hosted check. What IS covered is the accessor layer they all now call (`syscalls::ioctl_user::tests`, 10 cases including EFAULT at null, at `USER_VA_END`, on a span that straddles it, and on an overflowing length) plus the three files the `fs` integration harnesses `#[path]`-include (`common`, `fileattr`, `blk`). Nothing hosted drives `vt`, `font`, `tioclinux`, `tty_ioctl` or `fiemap` end to end. | `cargo test -p syscalls --lib ioctl_user` = 10 passed; the seven `cargo test -p fs --test sys_ioctl_*` harnesses = 182 passed. | unassigned |
| KI-0214 | OPEN | COVERAGE | med | The runaway guard's terminal state is a halt with a printed diagnosis. The reference kills the offending TASK and keeps the kernel running for a kernel-mode fault it cannot fix up. Halting is fail-safe and strictly better than the stack smash it replaces, but it is not the reference's answer, and nothing here can yet turn an unresolvable kernel-mode fault into a dead task. | `hal-{x86_64,aarch64}/src/fault.rs`: the dispatcher returns `false` and the asm halts. | unassigned |
| KI-0215 | OPEN | COVERAGE | low | The signal-frame paths (`hal-{x86_64,aarch64}/src/signal.rs`, `sched/src/sigaltstack.rs`, `fs/src/sig_dispatch.rs`) have now been audited. Frame objects, siginfo, masks, stack metadata, XSAVE/FP state, and AArch64 reserved context are staged in kernel-owned storage and crossed with the HAL's exception-table usercopy owner, so a vanished user page returns `EFAULT` instead of faulting the kernel. Remaining work is coverage: the target-gated builders need boot or equivalent live signal-delivery coverage, including a faulting frame copy and restore path. | Source commits `e2a761f43` and `6c76ebc78`; `cargo test -p hal-x86_64 --lib signal` = 21 passed and `cargo test -p hal-aarch64 --lib signal` = 22 passed; both arch kernel target checks pass. | unassigned — live signal-delivery coverage |
| KI-0216 | OPEN | MISSING | high | No async execution engine: SQPOLL/IOPOLL rings, `IORING_FEAT_FAST_POLL` poll-armed retry, multishot re-arm, `IORING_OP_TIMEOUT`/`LINK_TIMEOUT`/`ASYNC_CANCEL`, and cancellation of in-flight work are all absent. Every entry completes inside the submission that issued it. Needs kernel worker threads sharing the submitter's mm/fd table plus a request object model with refcounted lifetime — a subsystem-sized piece of work outside the three slot files. `IORING_REGISTER_IOWQ_AFF`/`IOWQ_MAX_WORKERS` wait on the same thing. | `op_supported` excludes those opcodes; `probe_never_claims_an_opcode_dispatch_would_reject` pins that they are not advertised | — |
| KI-0217 | OPEN | MISSING | med | Row 428: the per-task registered-ring ARRAY now exists and is torn down (`sched::Task::io_uring_ring_{install,remove,lookup}` / `io_uring_rings_drain`, 16 slots, reference errno ladder: EINVAL past the end, EBADF for an empty in-range slot, EBUSY for an occupied one or a full any-slot request, an empty-slot clear a silent no-op; drained at `exit(2)`, at fatal-signal termination and at `execve`). What is still absent is the CONSUMER half in the ring layer: `IORING_REGISTER_RING_FDS`/`UNREGISTER_RING_FDS` have no handler, `IORING_ENTER_REGISTERED_RING` resolves against nothing, and `IORING_FEAT_REG_REG_RING` is advertised with nothing behind it. Only the ring layer can tell an io_uring descriptor from any other file, so the `EOPNOTSUPP` rung and the `io_uring_rsrc_update` wire struct belong to it, not to `sched`. | `sched::task::io_uring` + its 6 hosted tests; `io_uring_abi::register_op::tests::registered_ring_lookup_reports_linux_errnos` still pins the empty-array errnos because nothing populates the array yet | io_uring lane (`syscalls/src/io_uring/**`) |
| KI-0218 | OPEN | COVERAGE | med | The submission engine, the completion path and the wait loop live under a `#![cfg(target_os = "oxide-kernel")]` module, so only their extracted decision modules (`abi/link`, `abi/enter`, `abi/restriction`, `io_uring_sqe`) are hosted-testable. The remaining glue — overflow flushing, provided-buffer selection, direct-descriptor placement — has no check that can fail. A ring fixture usable from `cargo test` would close it. | boot smoke is the only current check on those paths | — |
| KI-0219 | OPEN | INFRA | low | `IORING_REGISTER_SYNC_CANCEL` reports ENOENT for every request because nothing is ever in flight. Correct for a synchronous engine, but it becomes a real cancellation path the moment the async engine above exists. | `io_uring::register::rings::sync_cancel` | — |
| KI-0220 | OPEN | MISSING | low | `WP_UNPOPULATED`, `WP_ASYNC` and `WP_HUGETLBFS_SHMEM` are absent, so write-protect is anonymous-only and a `MODE_WP` registration over shared storage is refused with EINVAL. This is the same shape a reference kernel without non-present page-table markers takes. Closing it needs a marker encoding for an ABSENT leaf that survives eviction. | `policy::register::vma_can_userfault`; `tests::modes::modes_are_refused_on_vmas_that_cannot_deliver_them` | — |
| KI-0221 | OPEN | MISSING | low | `UFFDIO_MOVE` moves 4 KiB leaves and swap entries only. A page in migration reports EAGAIN and a poisoned one EFAULT (both matching the reference), but there is no large-folio path because none exists in this kernel. | `work/movepg.rs::classify` | — |
| KI-0222 | OPEN | COVERAGE | med | The page work itself (`work/{install,wp,poison,movepg}.rs`) is target-gated and has no hosted coverage; the hosted build substitutes a stand-in that reports the range complete. Every DECISION around it is ungated and tested, and both arches boot, but a defect confined to the leaf manipulation would not be caught hosted. Closing it needs a hosted page-table fixture of the kind `vmm`'s copy-on-write model tests use. | `work/hosted.rs`; `make smoke` passes both arches | — |
| KI-0223 | OPEN | INFRA | low | `cargo test -p security` failed once in ~4 runs with 244/1 and no reported failure name (the summary line and the failure list disagreed); green on every re-run. Untouched by this branch — the crate's registry statics are process-global and its tests install into them concurrently. | Observed 2026-08-07 on this box; `-p security` green 3/3 after. | unowned |
| KI-0224 | OPEN | INFRA | low | Restoring a positive-control file with `mv file.bak file` preserves the ORIGINAL mtime, so cargo does not rebuild and the next run silently re-uses the CONTROL binary. This produced a deterministic "restored code still fails" reading for three runs. `touch` after any such restore. | `net::socket_security::tests::the_sandbox_answers_before_the_module_registry` reported red against pristine source until the file was touched. | unowned |
| KI-0225 | OPEN | COVERAGE | low | `packet_tests.rs::packet_tx_ring_file()` registers an interface into the process-global interface table for namespace 0 and brings it up, with no claim on that table. `net`'s own `assert_initial_domain_held` choke point (`B1949`) is `#[cfg(test)]` **within `net`**, so it compiles out of `socket`'s test binary and cannot see this. The two packet tests each get their own ifindex, so they do not collide today, and the claim added here does not cover the interface table. | Not observed to fail: 0 occurrences across the 40 baseline and 140 post-fix `socket` runs. Structural, not measured — a future socket test that asserts on namespace-0 interface or MIB state would race these two. | unassigned |
| KI-0226 | OPEN | MISSING | MED | `SO_TXTIME` generates nothing. The option's clock, deadline mode and error-report flag are stored, and `publish_txtime` plus both codes exist and are tested, but there is no transmit-time scheduler to miss a deadline or reject a parameter, and `SCM_TXTIME` is not parsed on send. | Grep: `SCM_TXTIME` has no definition in the tree; `publish_txtime` has no non-test caller. | unclaimed |
| KI-0227 | OPEN | COVERAGE | LOW | The `MSG_ERRQUEUE` receive path itself (`syscalls::recvmsg::{inet,vsock}`) is target-gated, so no hosted test covers the copy of payload/name/control into user memory. Every decision it makes was moved into `net::socket_error::abi`, which is tested; the byte moves are not. | `crates/kernel/syscalls/src/recvmsg/inet.rs` inherits the slot-file gate. | unclaimed |
| KI-0228 | OPEN | INFRA | low | The SSH conformance transport still times out on this box (`oxide-conformance: SSH timeout`), so every guest differential here went through the serial transport. The serial path works on both arches | two consecutive `tools/oxide-conformance-ssh.sh x86_64 t_mmsg` runs reached a booted guest with `sshd` running and never completed the exec | N22 channel lane |
| KI-0229 | OPEN | MISSING | low | `UDP_ENCAP` is no longer stored-with-zero-consumers: the receive classifier now splits the reference's three cases exactly (a one-byte keepalive eaten, an all-zero four-byte prefix passed through as control traffic, everything else classified an encapsulated security payload) and is wired to both UDP receive paths. What is absent is everything downstream of that classification — there is no transform subsystem, so a security payload is consumed and dropped where the reference would decapsulate and re-inject it, and `UDP_ENCAP_L2TPINUDP` has no tunnel subsystem to install a handler for. | `net::sock_opts::sol_udp::encap::rx_verdict` and its `tests_udp_encap` cases; nothing consumes the security-payload verdict. | — |
| KI-0230 | OPEN | COVERAGE | med | No differential probe corpus exercises `setsockopt`/`getsockopt` at SOL_NETLINK or SOL_SOCKET-on-netlink. `userspace/af_packet_diff/` covers SOL_PACKET and `userspace/glibc_conformance/t_{set,get}sockopt.c` covers neither level; the netlink defects fixed here were found by reading, not by a run. | `grep -n "NETLINK" userspace/glibc_conformance/t_setsockopt.c` is empty | — |
| KI-0231 | OPEN | COVERAGE | low | The wiring from the kernel-gated socket send entry points (`net::sock::udp::socket_sendto_ctl`, `sock_v6::sendto_v6_ctl`) into the mark-carrying route lookup cannot be exercised hosted — both files are `#[cfg(target_os = "oxide-kernel")]`. The DECISIONS they carry are all in ungated modules and tested there; only the call itself rests on the build | `crates/kernel/net/src/sock.rs` module gating | unclaimed |
| KI-0232 | OPEN | INFRA | low | `t_tcp_wait` cannot share a serial conformance run: five probes including it exceed the serial timeout ceiling and abort | the same four probes without it complete well inside the budget; with it the runner service starts and never deactivates | unclaimed |
| KI-0233 | OPEN | COVERAGE | low | Row 42 now runs at its declared `uid=root`, but only the host half is verified: three privileged and three unprivileged host runs are byte-identical, and the guest half is unchecked because the probe overruns the serial budget | the row above | unclaimed |
| KI-0234 | OPEN | COVERAGE | LOW | The differential probe covers IPv4 UDP, AF_UNIX stream and the `recvmmsg` batch. VSOCK and NETLINK receive copy-faults have hosted coverage of the shared publication but no differential frame, because neither family has a host-side oracle reachable from an unprivileged probe. | `tools/network-conformance-manifest.tsv` rows 47/299 record the gap in their closure contract. | unclaimed |
| KI-0235 | OPEN | COVERAGE | low | Verified against the reference and recorded so nobody re-derives it: `IPV6_RECVPATHMTU` does NOT travel on the extended-error queue. The reference's producer exchanges the notification into a dedicated single-slot socket field and the consumer exchanges it back out at the top of the ordinary receive; the error-queue helper is a different function used by a different producer. Routing it through `net::socket_error` would require `MSG_ERRQUEUE` and would let notifications queue behind one another — neither is the contract. | `sol_ipv6::pathmtu` module comment; `pathmtu::tests::an_ordinary_receive_drains_the_cell_and_an_unset_option_does_not`; no `socket_error` call anywhere on the path. | B1964 |
| KI-0236 | OPEN | COVERAGE | low | One x86 boot in this lane stalled 45 s after systemd start and froze with `Failed to fork off sandboxing environment for executing generators: Protocol error`; the immediately preceding and following boots of the SAME tree reached the probe and passed the differential. Recorded rather than attributed: a single boot cannot distinguish this from the tree's known cold-boot intermittency. | `target/smoke/sockopt-diff/x86-20260808-063154-hiZouE/oxide-uart.log` (frozen) against `x86-20260808-063509-OPTFxj` and `x86-20260808-063714-jILeqz` (both reached `meta\|complete\|status=DONE`). | unowned |
| KI-0237 | OPEN | COVERAGE | low | The per-task context synchronisation runs from the `PerfDeferred` bottom half, which keeps ONE parked switch per CPU: back-to-back switches before a drain leave only the latest, so an intervening parent/child pair can go unsynchronised. No count is lost (every value is derived from live per-thread state at read time); what is delayed is the republication into the mapped control page, so a consumer polling the page without a syscall can see a stale snapshot under a burst of switches. Filed rather than fixed because the alternative — synchronising on the switch path itself — allocates, and that path may not. | F856. `sched::perf_sw::deferred`'s `NoteSlot` is a single slot per CPU by construction (`note_switch` overwrites); `fs::perf::context::sched_out` is its only new consumer. | perf lane |
| KI-0238 | OPEN | MISSING | MED | `UFFD_FEATURE_WP_ASYNC`'s usual consumer does not exist: `/proc/<pid>/pagemap` is an empty stub and there is no `PAGEMAP_SCAN`-style ioctl. The asynchronous mode itself works — a write clears the per-page state and proceeds with no message — but a monitor has no way to READ back which pages were written, which is the entire purpose of the mode. Outside this lane (procfs). | `crates/kernel/procfs/**` has no `pagemap` producer; the write-protect state is already the durable record (`hal::pt_walker::PtWalker::{leaf_is_uffd_wp, nonpresent_is_uffd_wp}`), so a reader needs only to project it. Needs: a per-page `pagemap` entry producer over the address space's leaves, and the scan ioctl's category/mask filtering plus its write-protect-while-scanning arm. | unassigned — proposed follow-up lane |
| KI-0239 | OPEN | COVERAGE | MED | the seccomp notification's blocking path and the ioctl router's CALL SITE are both in target-gated code, so no hosted test can fail if either is deleted. The decisions each reaches (queue transitions, listener resolution, router predicate, verdict mapping) are all ungated and covered; the two call sites rest on the arch builds and the boot smoke | `016_ioctl/core.rs` router arm; `entry.rs` UserNotif arm | unclaimed |
| KI-0240 | OPEN | MISSING | med | `POSIX_FADV_NOREUSE` records nothing. The reference stores a per-description bit whose only consumer biases reclaim of MAPPED pages; oxide's equivalent hook has zero call sites, so storing the bit would recreate the stored-but-unconsumed defect. Closing it needs the reclaim-side consumer, which is memory-management territory. | row 221 | unowned |
| KI-0242 | OPEN | COVERAGE | LOW | N25 (TCP connect wait linearization) and the N22 guest differential channel remain open in `scratch/network-plan.md`; rows 42/43/50/288 carry them as their last substantive item. N26 is all-but-closed — only its `SOL_VSOCK` option differential (N26.4) is open, so the rows' "N26 VSOCK wait linearization" wording is stale. | `scratch/network-plan.md:1648` (N25 unchecked), `:1695` (N26 partial, 7/8 sub-items done). | net |
| KI-0243 | OPEN | MISSING | low | No uprobes subsystem. Slots 335/336 are complete for every reachable caller — a process cannot hold a uprobe trampoline without the subsystem, so the no-trampoline arm is the whole reachable body — but the trampoline mapping, the probed-frame unwind, the shadow-stack pop and the consumer dispatch do not exist, so no uprobe can ever be installed. This is a tracing-subsystem row, not a syscall-ABI one. | `grep -rn uprobe crates/` finds only the two slot files and `uprobe_abi`. | unassigned |
| KI-0244 | OPEN | MISSING | low | `hal_aarch64::hw_breakpoint::hw::toggle_enables` has ZERO callers repo-wide — machinery without a consumer. It is the step-over-a-hit primitive for hardware breakpoints whose handler is the DEFAULT overflow handler (perf/kernel breakpoints). It is NOT needed for ptrace breakpoints: the reference registers a non-default overflow handler for those, which skips the step-over entirely and just delivers SIGTRAP — which is what oxide does. The matrix filed this under ptrace, where it does not belong. | `grep -rn toggle_enables --include=*.rs .` → definition only, at `crates/arch/hal-aarch64/src/hw_breakpoint/hw.rs:211`. | unassigned |
| KI-0245 | OPEN | DEFECT | med | The worker pool is ONE global pool serving every ring, not a pool per submitting task as the reference has. Deliberate, and the reason is the same missing mechanism: a kernel thread cannot exit here, so a pool per ring would leak a thread set per ring for the life of the system. Each work item therefore carries its own borrowed address space, descriptor table and credentials, and the per-ring `IORING_REGISTER_IOWQ_MAX_WORKERS` limit is enforced on how many of a ring's requests run at once rather than on a thread count. Closes when kthread exit exists. | `io_uring::iowq::pool`; `IoUringInode::iowq_admits` / `iowq_release` | B1975 |
| KI-0246 | OPEN | COVERAGE | med | The engine's runtime halves — the worker loop, the borrow install/release, the arming and the in-flight search — live under `#![cfg(target_os = "oxide-kernel")]`, so only their decision modules (`abi/{timeout,cancel,poll,link,enter}`) are hosted-testable. A ring fixture usable from `cargo test` would close it, and would also close the same gap B1950 recorded for the submission and completion paths. | boot smoke is the only current check on those paths | B1975 |
| KI-0247 | OPEN | MISSING | med | `POSIX_FADV_NOREUSE` records nothing, and the reason is larger than previously stated. The reference's consumer chain is a per-description mode bit read by a per-VMA recency predicate with five call sites. Here: `mark_lru_referenced` still has ZERO call sites, file-page reclaim isolates in pure LRU order with no reference check of any kind, no accessed-marking step exists on the zap path, no lru-fault flag exists on the fault path, and the file rmap walk that does exist serves migration PTE rewriting. Additionally a VMA cannot reach an open file description at all — the file backing wraps the INODE, and the bit is per-description. Storing the bit without the consumer recreates the stored-but-unconsumed defect. | grep-confirmed zero call sites; `isolate_inactive_file_lru` has no reference check; `InodeFileBacking` wraps `InodeRef` | memory-management lane |
| KI-0248 | OPEN | MISSING | med | A BTF-typed hook argument cannot be followed. The verifier admits reading an argument slot and refuses every load through the loaded value, so a program that dereferences its argument is rejected where the reference would type-check the field access. | `bpf_verify/program/tests/lsm.rs::following_the_argument_pointer_is_refused` | unowned |
| KI-0249 | OPEN | MISSING | high | An attached perf-event program is never RUN. The reference's `bpf_overflow_handler` runs `event->prog` at every overflow and drops the sample when it returns 0; here `event->prog` is stored, held and reported, and the overflow path (`fs::perf::emit`) never consults it. The blocker is the context: `bpf_perf_event_data` is a `pt_regs` view, and the software-counter sites carry only `SwSite { ip, addr, user }` — a full trap frame does not reach them, so the context cannot be built honestly today. | `fs/src/perf/emit.rs::deliver` has no program call; `fs/src/perf/bpf.rs` stores `EventState::prog` and only `attached_prog()` reads it | unclaimed |
| KI-0250 | OPEN | MISSING | med | `PERF_EVENT_IOC_QUERY_BPF` still answers `-ENOENT` unconditionally. The reference's `perf_event_query_prog_array` is the tracing-event program ARRAY, which needs the tracepoint registry (row above); the non-tracing single-program attach that F862 built is not what that ioctl reports. | `fs/src/perf/glue.rs` `PerfIoctl::QueryBpf => err(Errno::Enoent)` | unclaimed |
| KI-0251 | OPEN | MISSING | med | What a tracepoint registry actually needs, recorded so the blocked raw-tracepoint rows do not each re-derive it: a `TRACE_EVENT`-equivalent definition site (there are no tracepoint definitions anywhere in the tree, so the registry would have nothing to register until call sites are instrumented), a name→`bpf_raw_event_map` lookup for `bpf_get_raw_tracepoint()`, a per-tracepoint program list with the call-site hook that runs it, a raw-tracepoint link kind holding `{prog, btp}` for `objfd::link_kind`, and the event-flags bits (`TRACE_EVENT_FL_TRACEPOINT` / `_KPROBE` / `_UPROBE`) that `bpf_get_perf_event_info()` switches on to pick `BPF_FD_TYPE_*`. Three consumers are already written against it: `BPF_RAW_TRACEPOINT_OPEN`, `BPF_TASK_FD_QUERY`'s link arm, and `PERF_EVENT_IOC_SET_BPF` for tracing events. | `security/src/bpf/cmd/trace.rs::tracepoint_by_name` returns `None` unconditionally; `grep -rn "TRACE_EVENT\|tracepoint" crates/ --include=*.rs` finds no definition site. | unclaimed |
| KI-0252 | OPEN | COVERAGE | high | 433 unguarded candidates, 59 of them bare serialisation locks — `B1957` predicted 25 from a narrower grep, and every one of those 25 is in this list. Each is a fixture lock whose protection is a convention any new test in the same binary can forget. | `tools/hosted-global-state-backlog.tsv`, one row per candidate, sortable by claim ratio. The worst by exposure: `net/src/net_ns/test_support.rs::LIFETIME_LOCK` (17 of 2186 tests in the binary claim it), `sched/src/tests/timing.rs::SERIAL` (1 of 1210), `fs/src/timerfd/tests.rs::TEST_LOCK` (10 of 1002), `syscalls/src/fcntl_dup_tests.rs::TEST_LOCK` (2 of 1329). | unassigned — one row per lane |
| KI-0253 | OPEN | COVERAGE | med | 65 `singleton-pin` rows: hosted fixtures pinning a process-global singleton through a parameterless accessor (`vfs::initial`, `drv::devices`, `network-namespace::initial`, `devfs::clear_hwrng_source`, …). `B1957` names de-pinning as the only lever that removes the class rather than guarding it — these rows are the inventory for that work. | `tools/hosted-global-state-backlog.tsv`, rule `singleton-pin`. | unassigned |
| KI-0254 | OPEN | DEFECT | High | A process that loads the GIO module `libdconfsettings.so` intermittently dies on the FIRST read of the dconf database it has just mapped. The faulting instruction is `mov (%rax),%edx` at module offset `0x5245`, where `%rax` is the pointer `g_mapped_file_new()` → `g_mapped_file_get_bytes()` → `g_bytes_get_data()` just handed back and the size check `cmpq $0x17,0x10(%rbx)` has already passed. Older observations predate the fault-reason classifier and appear as `SIGSEGV`; the current `gsd-power` observation is `SIGBUS`/`BUS_ADRERR`, which proves the file-fill path returned an error rather than merely lacking a VMA. When the victim is `gnome-shell` the whole desktop dies to a bare console; when it is a session service (`evolution-source-registry` or `gsd-power`) the desktop survives. **Not introduced by the 2026-08-08 wave** — it reproduces on `5506b785e`, the last-known-good commit, at the SAME module offset. | Paired live-gnome boots, same host, same qemu invocation, run concurrently: `910470808` 0 faults / 5 boots, `5506b785e` 1 fault / 5 boots (plus one earlier `910470808` boot under a wider device set, which faulted — so 1/6 vs 1/5, indistinguishable at this sample size). Both older fault lines carry `ino 0x6e54000000003e14` (ext4 inode 15892 = `/usr/lib64/gio/modules/libdconfsettings.so`) and `off 0x5245`; only the victim process and the ASLR bases differ. Current `boot.txt` has `gsd-power[495]: bus error ... in libdconfsettings.so[...+0x5245]` at 113.052 s. `configured_root_dconf_databases_read_through_framecache` passes on the composed root image through ext4's `i_mapping` path, so the static system dconf databases are valid under the ordinary frame-cache path; it does not exercise live device I/O or a mutable per-user database. | B2281-boot-compat-failures |
| KI-0255 | OPEN | COVERAGE | med | Nothing in the tree measures whether a live-gnome boot reaches the desktop, so a desktop-visible regression is only ever found by a human looking at a screen. This triage had to build a one-off harness (boot the profile's ISO with a QMP socket, `screendump` at a fixed deadline, count `segfault at` lines in serial) to get a comparable number out of two commits. | `make` has smoke targets for login and for individual subsystems, none for `PROFILE=live-gnome` reaching `graphical.target`; `tools/boot-report.py` reports `graphical.target: no` for a boot whose serial console is taken over by `debug-shell.service`, which is every live-gnome boot driven through `tools/boot-capture.sh`. | unowned |
| KI-0256 | OPEN | DEFECT | High | The observed victim is `libdconfsettings.so` +0x5245, `mov (%rax),%edx`, on the pointer `g_mapped_file_new()` returned with its size check already passed. B1985 is merged: a current `SIGBUS`/`BUS_ADRERR` at this instruction means the file-fill path failed, not that the mapping was absent. The remaining cause is still not identified: it can be the frame store's inode read, extent/data read, or the underlying block request; a normal hosted read of the current static dconf databases is green. Keep open until runtime error provenance identifies that source and a live GNOME run verifies the correction. | Older evidence: `gnome-shell[398]: segfault at 00007f257af04000 ip 00007f257a65b245 ... ino 0x6e54000000003e14 (ext4 inode 15892) off 0x5245`. Current evidence: `boot.txt` at 113.052, `gsd-power[495]: bus error ... in libdconfsettings.so[...+0x5245]`. `vmm::tests_shortfill::failed_file_fill_classifies_as_a_bus_error` pins the classification; `ext4::configured_root_dconf_databases_read_through_framecache` pins the ordinary composed-image `i_mapping` read. A controlled B2281 diagnostic boot of a fresh GNOME root reached `graphical.target` at 67.179 s and logged through 143.021 s with neither `gsd-power` SIGBUS nor `[FILLVERIFY]`; this is one clean sample, not a correction. | B2281-boot-compat-failures |
| KI-0257 | OPEN | INFRA | med | Three harness traps, each of which produced a wrong answer here. (1) The one boot that faulted during A/B was also the only one of ten to log `[WATCHDOG] soft lockup: no reschedule for 10s` and `no-progress: 0 context switches for 40s` — host swap was 7/7 GB full under two concurrent 4 GB guests, so contention manufactures the fault and any paired-boot comparison is invalid. (2) `nvme-<arch>.img`/`ahci-<arch>.img` are created by `xtask grub` at boot, NOT by `xtask image`, so a hand-rolled qemu line exits in under a second while a sleep-based harness counts it as a clean boot. (3) `tools/boot-capture.sh` runs `pkill -9 -f qemu-system`, which kills every other lane's boot on this shared box. | Logs under the B1979 scratch dir; `free -g` during the run; `image_qemu/common.rs::ensure_storage_img`. | unowned |
| KI-0258 | OPEN | MISSING | med | `SO_TXTIME` still generates nothing. The option's clock, deadline mode and error-report flag are stored, the per-message departure time now rides the packet, and both error-queue codes exist and are tested — but the transmit path is strict-priority FIFO by band with no deadline-ordered structure, no watchdog, and no missed-deadline report, so `publish_txtime` still has no non-test caller. The reference holds a deadline-ordered queue with a watchdog and reports a late packet on the error queue when the socket asked for error reports. Closing it is a transmit-scheduling lane, not a socket-option one. | `net::netdev::tx_dispatch` orders by band only; `net::socket_error::queue::publish_txtime` has no non-test caller. | unclaimed |
| KI-0259 | OPEN | COVERAGE | med | Nothing exercises a real kernel-thread exit end to end. The decision halves are hosted-tested (the auto-reap disposition, the exit publication point, the registered-ring teardown), but the actual `kthread_exit` -> final `schedule()` -> post-switch tail -> `kthread_stop` join sequence needs a live runqueue and cannot run hosted, so the first real consumer will be the first execution of that path. The obvious first consumer is a per-ring io_uring submission poller. | `sched::live::kthread::exit`; `live/schedule/switch.rs` reap drain. | io_uring lane (first consumer) |
| KI-0260 | OPEN | MISSING | med | An iterator walk emits nothing: the program runs once per object and once at the end of the walk, its answer drives the walk, but no helper can put bytes into the sequence, so every read of a `BPF_ITER_CREATE` descriptor reports end of file. The reference's `bpf_seq_write`/`bpf_seq_printf` copy from the program's stack into the sequence, and the interpreter's helper ABI hands a helper only registers and the shared run state — it has no view of the program's memory, so no helper here could copy a buffer even if it were declared. Needs the helper ABI widened to carry the run's memory, then the two emitting helpers and their argument contracts. | `bpf::iter::seq` runs the walk and returns `Vec::new()`; `bpf_interp::HelperFn` takes `(&mut HelperState, i64 × 5)`. | unclaimed |
| KI-0261 | OPEN | MISSING | low | The iteration meta record an iterator program receives carries the step number only. The reference's record also carries the session identity and the sequence handle, and neither is represented here because no helper can read through the meta pointer yet — the verifier refuses every load through it. Both become observable in the same change that gives the walk an emitting helper (row above). | `bpf::iter::seq::IterMeta`; `bpf_verify::program::tests::iter::following_the_object_pointer_is_refused`. | unclaimed |
| KI-0262 | OPEN | MISSING | med | Iterator targets cover the three object kinds this kernel registers by id (programs, maps, links). The reference also iterates tasks, task files, task vmas, sockets, and map elements; each needs its own walk over the owning subsystem's registry plus its published stub. The mechanism is in place — a target is one row in the published table plus a walk — so each further target is additive, not structural. | `bpf::iter::targets::TARGETS` declares three rows; `bpf::iter::targets::tests::an_unpublished_target_name_resolves_to_nothing` pins that `bpf_iter_task` resolves to nothing. | unclaimed |
| KI-0263 | OPEN | MISSING | med | **No EFI runtime services are retained after boot**, on either arch. The aarch64 stub and the x86 UEFI path both exit boot services and keep no runtime-services pointer, so the reference's EFI reboot rung has no counterpart (B1984's reset ladder therefore has three rungs where the reference has five — the EFI one and the real-mode BIOS one are absent rather than skipped), and `efivarfs` has no possible backing: variable get/set/list would need `GetVariable`/`SetVariable`. Recorded because "the ladder is shorter than the reference's" is otherwise indistinguishable from an omission. | `crates/kernel/power/src/reset.rs` module comment; `crates/arch/boot-aarch64` stub calls ExitBootServices and records only the RSDP; no `RuntimeServices` symbol anywhere in the tree. | — |
| KI-0264 | OPEN | COVERAGE | low | **The UEFI boot gate is not in `make smoke` and not in CI.** `make smoke-uefi-x86` exists and can fail (B1984 proved it with a BIOS-only ISO), but it boots the same kernel a second time, so it was deliberately left out of the default gate to avoid doubling every push's wall clock. That means the UEFI path is only checked when someone remembers to check it. It belongs in the PR-time CI matrix, where the extra boot is not competing with a developer's other lanes. | `Makefile` targets `smoke` vs `smoke-uefi-x86`; `docs/40§2` PR-time gate list does not name it. | — |
| KI-0265 | OPEN | MISSING | med | `slub_debug` flags other than the global `P` form (`F`, `Z`, `U`, `T`, `A`, `O`, cache-specific lists) and `debug_pagealloc=` remain unhonoured. SLUB redzoning/metadata/quarantine and page-table unmapping still need their respective runtime owners; neither is represented by the supported poison path. | Linux `mm/slub.c` flag parser and `mm/debug_page_alloc.c`; current parser reports these forms as unsupported, and no runtime direct-map unmap/re-map owner exists. |  |
| KI-0266 | OPEN | INFRA | MED | The syscall-compliance matrix's reference column names external implementation files (e.g. row 324 carried four such paths) on essentially every row. The repository-text rule in CLAUDE.md forbids naming, path-linking or quoting external implementation files anywhere in repository text, and the matrix is repository text. This lane left row 324's column untouched rather than unilaterally diverging from a ~385-row convention, and wrote the new evidence prose with no external paths. LANE SCOPE: decide the convention once — either drop the column repo-wide, or replace each entry with a non-path descriptor (spec section, UAPI header name only, or the behaviour under test) — then update `tools/matrix-lint.py` to enforce it so it cannot regress. Needs a decision before the edit; it is a convention change, not a mechanical sweep. | `scratch/syscall-compliance-matrix.md`, reference column, 385 rows. Row 324 is representative. `tools/matrix-lint.py` currently does not check this column's contents. | unowned |
| KI-0267 | OPEN | COVERAGE | MED | The `[WATCHDOG] soft lockup: no reschedule for 10s on tid=4555 (Mutter Input Th) last_syscall=read` in `boot.txt` has NO established mechanism. Ruled OUT by reading: the evdev `read_file` retry loop was suspected of spinning past `schedule()` via its `park -> cancel_current_park -> continue` branch, but `has_pending()` (`is_empty()` -> `ready == 0`) and `try_pop_bytes()` (`count = min(dst/EVENT, ready)`, `None` iff `count == 0`) gate on the SAME `ready` field under the same lock, and the only other `None` paths (`revoked`/`disconnected`) are caught by `!is_live()`, which returns ENODEV at the top of the loop. Every exit terminates, so that loop is not the spin. What IS established: the kernel is not tick-preemptive in kernel mode (the timer IRQ only sets `need_resched`), and `charge_exec_runtime` runs only from `update_curr()` inside `schedule()` — which is why the victim shows 42ms cputime after a 10s stall, and why 'low cputime' must NOT be read as 'starved'. Still needed: identify what the task was actually executing. A separately observed guest pegging 99.9% host CPU after journald starts, unresponsive to userspace AND to kernel-level sysrq, may or may not be the same thing. Confound to eliminate first: the recorded case of a soft lockup manufactured by host memory contention. | `boot.txt:264-645`; `crates/drivers/drv-virtio-input/src/devfs/fileops.rs:64-82`; `crates/drivers/drv-virtio-input/src/evdev_queue.rs:107,183`; `crates/kernel/sched/src/diag/watchdog.rs:71-101`; `crates/kernel/sched/src/cputime.rs:72-76`; `crates/kernel/sched/src/live/schedule/switch.rs:2`. | unowned |
| KI-0268 | OPEN | MISSING | HIGH | **Both AMD-Vi and VT-d now own live DMA mappings.** B2071 and B2072 retain each active unit beneath the common requester-keyed DMA owner. Mapping preserves an unaligned offset, allocates IOVA space, installs backend PTEs, completes IOTLB invalidation, then returns the IOVA; unmapping removes PTEs, completes the same invalidation, then releases IOVA space. The common map record is never held while entering a backend owner, preventing a map/unmap lock inversion. The remaining gap is Linux-shaped DMA-mask-aware IOVA allocation and migration of every native PCI DMA driver through this owner; no native driver may retain a direct PA-to-device path. | B2071 `d4aff0eb7`; B2072 `a7e513438`. AMD-Vi uses its command ring; VT-d retains exact BDF ownership after firmware scan and globally invalidates context/IOTLB state after each live PTE change. `iommu` 24/24; both kernel targets; final smoke invoked. B2070 migrated e1000 as first native consumer. | B2072-live-vtd-dma |
| KI-0269 | OPEN | COVERAGE | LOW | `InodeFileBacking::noreuse` is a SNAPSHOT of `f_mode` taken at `mmap(2)` time, not a live read of `vfs::File`: `fadvise64(POSIX_FADV_NOREUSE)` called AFTER a file is already mapped does not retroactively suppress recency on that mapping's already-resident pages (a later `mmap` of the same fd sees the current state). Matches how this same backing already snapshots readahead-state hints rather than tracking `vfs::File` live — `mm-vmm` carries no `Arc<vfs::File>` reference by design (`52`). Also, `mark_lru_referenced` is now wired ONLY at the file-backed minor-fault "already resident, install PTE" arm (`fault/fill.rs`'s `direct` branch) — the reference's OTHER `vma_has_recency` consumers (`rmap.c:1039` `folio_referenced` during reclaim, `memory.c:1656` unmap-time accessed-bit fold) have no analog here because oxide's reclaim/rmap has no PTE-accessed-bit scan at all yet (grep of `mm-vmm/src/rmap.rs` for `Accessed`/`referenced`/`young` returns nothing) — a larger, pre-existing gap this row does not attempt. | B2018; `vmm::FileBacking::noreuse` doc; `mm-vmm/src/rmap.rs` has no accessed-bit walk. | unowned |
| KI-0270 | OPEN | MISSING | MED | `perf_event_open` has no ring buffer / mmap / `PERF_RECORD_*` sampling path (large), and no `inherit`-on-fork propagation (medium). The module is NOT target-gated, so both are hosted-testable when built. Non-software event types returning ENOENT and cgroup mode returning EINVAL are CORRECT (they match a reference built without those facilities) — not gaps. | Syscall-matrix row 298 re-audit. | unowned |
| KI-0271 | OPEN | MISSING | low | io_uring NAPI busy-polling drives the WHOLE device poll list rather than the specific queue identifiers a ring registered: `net::backlog::napi` is a set of driver poll routines with no identifier key, so `IO_URING_NAPI_STATIC_ADD_ID`'s list gates WHETHER a ring busy-polls but not WHICH queue it polls. Relatedly the receive path records no `sk_napi_id` (every `SockView.napi_id` is a literal 0), so `IO_URING_NAPI_TRACKING_DYNAMIC` has nothing to learn and its list stays empty — faithful for a stack that assigns no identifiers, but it means dynamic tracking never busy-polls. Both close when the receive path assigns queue identifiers and the poll list is keyed on them. | `io_uring::wait::busy_poll` calls `net::backlog::napi::poll_all()`; `io_uring_abi::napi::busy_poll_wanted` + its tests pin the empty-list gate | net lane |
| KI-0272 | OPEN | MISSING | med | A bundled receive makes ONE pass over the run it mapped. The reference, when the transfer filled every mapped segment AND the description still reports more data immediately available, retries into the rest of the run and appends to the same completion. Nothing here reports "bytes still queued" out of a receive, so the condition that would trigger the second pass cannot be evaluated and the pass is never attempted. Not an ABI divergence — the reference takes the same single-pass branch whenever no more data is queued, and the completion is well-formed either way — but a caller receiving a burst gets more completions than it would on the reference. Closes when the receive path returns the queued-byte count alongside the transfer. | `io_uring::dispatch::bundle_io::run` returns after one `recvmsg::recv`; `io_uring_abi::bundle` has no retry decision to test because there is no input for one | io_uring lane (needs the net receive path to report queued bytes) |
| KI-0273 | OPEN | MISSING | med | No storage target here exposes an integrity profile, so `IORING_RW_ATTR_FLAG_PI` is decoded, validated and then refused with EINVAL on every description. This matches the reference on hardware without protection information, and the admission ladder that would admit one is built and tested — what is absent is a backend that answers yes. Closes when a block backend reports its integrity capability and the transfer path carries the integrity buffer beside the data. | `io_uring::dispatch::rw::has_metadata` is the single point that answers; `io_uring_abi::rw_attr::admit_target` + `a_target_without_integrity_metadata_is_a_different_refusal_from_a_cached_one` pin both rungs (EINVAL vs EOPNOTSUPP) | block lane, then io_uring lane |
| KI-0274 | OPEN | COVERAGE | med | io_uring BPF filters are enforced in `io_uring::filter::admit`, which is kernel-gated, so the ENFORCEMENT point has no hosted check — the decision halves (the record layout, the verifier bound, the verdict table, fail-closed on an unrunnable program) are all tested, but nothing proves `submit::admit` consults them before an opcode takes its side effect. The per-opcode payload extraction (`pdu_of`, `connect_pdu`) is likewise gated and untested, including the short-`addr_len` rule that keeps stale address bytes away from a filter. Same ring fixture as rows 345/614 would close all three. | `io_uring::filter`; `io_uring_abi::bpf_filter::tests` (31) cover only the ungated halves | io_uring lane |
| KI-0275 | OPEN | INFRA | med | **aarch64 boot smoke wedged twice at `modprobe@drm.service`, then passed on the third attempt.** Attempts 1 and 2 both froze at guest time 13.997 s with no further kernel output at all — no panic, no fault, no timer line — and QEMU stayed alive to the 600 s timeout; attempt 3 booted to the liveness marker in 74 s. Two identical stalls at the same guest timestamp is not the usual random-flake shape, so it is worth a look, but it is NOT attributable to the branch that observed it: the code that branch changed is arch-independent, x86 booted in 68 s on the first attempt in the same run, and an earlier run of the same branch passed aarch64 in 135 s on the first attempt. | F850. Frozen serial log `/tmp/oxide-boot-smoke-arm-44KQFY.log` (112 lines, last line `[13.997] systemd[1]: Starting modprobe@drm.service`, `grep -icE 'panic\|BUG:\|oops\|unhandled'` = 0). `boot-smoke: PASS — arm ... in 74s (attempt 3)`. | unowned |
| KI-0276 | OPEN | MISSING | LOW | `sk_reuseport_md`'s `data`, `data_end`, `sk` and `migrating_sk` are refused at load rather than served as the zeroes this kernel has no source for — the same discipline `__sk_buff` already keeps, and deliberate, but it means a program compiled against the upstream ABI using direct packet access (`data`/`data_end` bounds checks) will not load here; it must use `bpf_skb_load_bytes` against `len`. Closing it needs the verifier to prove packet-pointer arithmetic, which no program type in this tree has yet. | `security::bpf_verify::program::context::sk_reuseport_access`; pinned by `sk_reuseport::tests::the_pointer_members_are_refused_rather_than_served_as_zero`. | unowned |
| KI-0277 | OPEN | MISSING | LOW | No reuseport socket MIGRATION. `migrating_sk` is permanently null because nothing here moves a half-open request or an unaccepted child off a listener that is closing; the reference runs the selection program a second time with `migrating_sk` set and hands the request to the chosen survivor. Today a closing listener destroys both populations. | `net::stack::tcp_listener::tcp_unlisten_entry` closes the accept queue and drops every listener-owned child; `net.ipv4.tcp_migrate_req` is not a sysctl here. | unowned |
| KI-0278 | OPEN | MISSING | HIGH | Loadable DRM drivers require a complete DRM core ABI, not isolated symbols. The canonical owner now provides managed driver-private allocation using the caller's exact object size and embedded DRM-device offset, one-shot release, KMS object construction, connector probing, firmware-only modeset admission, primary DRM-node publication/withdrawal, persistent external `file_operations` dispatch, owned connector EDID blobs, base-EDID detailed-timing publication, fixed-slot KMS property ownership on connectors/planes/CRTCs, and a client lifecycle gated on KMS plus dumb-buffer support. Each client owns a private GEM file and per-CRTC modeset/connector arrays; registration links the ABI client node into its device list before its one-shot hotplug callback, and release unlinks/frees it before the client callback and device put. GEM/shmem, atomic helpers, standard-property allocation/ownership, complete EDID parsing/properties, fbdev emulation, DRM core ioctls, render nodes, and vblank remain to be completed with their matching layouts and lifetimes. | `modules::linux_drm` tests cover embedded allocation offset, invalid layout rejection, release, KMS object ownership, connector probing, primary-node publication, BTF-verified connector EDID/blob lifetime, generic object-property attach/get/set behavior, and DRM client gate/modeset/list/hotplug/release ownership; `linux_chrdev` tests pin open-file context preservation through external ioctl dispatch; `linux_video` tests pin exact bare `nomodeset` handling and symbol export. | B2133-linux-drm-kmi |
| KI-0279 | OPEN | MISSING | HIGH | Standard atomic connector-state reset/duplicate/destroy now owns its CRTC-commit reference as well as the connector reference for the externally used null-resource state, but complete state ownership still needs `drm_property_blob_get/put` and `drm_writeback_cleanup_job`. States containing writeback/HDR resources are refused rather than copied or freed unsafely. | F1101 `linux_drm_crtc_commit` and `linux_drm_atomic_connector`; connector commit release is pinned by the shared commit lifetime tests. | B2133-linux-drm-kmi |
| KI-0280 | OPEN | MISSING | HIGH | Standard CRTC atomic state now releases its CRTC-commit reference and preserves Linux's transient-field clearing, but blob ownership remains absent. A state carrying mode/color blobs is refused rather than copied or released incorrectly. | F1101 `linux_drm_crtc_commit`; `linux_drm_atomic_crtc::tests::standard_crtc_states_reset_duplicate_and_clear_transient_fields` exercises state destruction with an owned commit. | B2133-linux-drm-kmi |
| KI-0281 | OPEN | MISSING | MED | `drm_edid_read_custom` now matches Linux block acquisition (retry, header repair, CTA checksum exception, HF-EEODB expansion, and invalid-extension filtering), but Oxide has no `drm_edid_override_get` equivalent: debugfs/firmware EDID overrides, `connector->edid_corrupt`, bad-EDID counters/checksum accounting, and their connector-field ABI must be implemented before the complete Linux read path is present. | `linux_drm_edid_read::drm_edid_read_custom` deliberately has no connector-field access; compare `../linux-master/drivers/gpu/drm/drm_edid.c::_drm_do_get_edid`. | B2133-linux-drm-kmi |
| KI-0282 | OPEN | MISSING | HIGH | `drm_edid_connector_update` owns and replaces the BTF-verified connector EDID blob and `drm_edid_connector_add_modes` publishes valid base-block detailed timings, but it does not yet implement Linux's CTA/DisplayID detailed timings, CEA VICs, CVT, standard/established timings, inferred GTF/CVT modes, display-info/ELD/HDR/tile/non-desktop properties, or global DRM property ownership. Treating base DTD publication as a complete EDID/KMS parser would leave physical displays with incomplete mode sets. | `linux_drm_edid_connector`; compare `../linux-master/drivers/gpu/drm/drm_edid.c::_drm_edid_connector_add_modes` and `_drm_edid_connector_property_update`. | B2133-linux-drm-kmi |
| KI-0283 | OPEN | MISSING | HIGH | Generic `drm_object_attach_property` now uses Linux's BTF-verified 64-slot object owner; mode-config owns the standard `EDID` and `FB_DAMAGE_CLIPS` blob properties and their connector/plane helpers. The remaining standard atomic CRTC/plane properties, generic driver-created property constructors/destructors, property ioctls, blob reference ownership, and atomic property callbacks/state routing are still absent. | `linux_drm_properties`; compare `../linux-master/drivers/gpu/drm/drm_property.c`, `drm_mode_config.c`, and `drm_mode_object.c`. | B2133-linux-drm-kmi |
| KI-0284 | OPEN | MISSING | HIGH | Client initialization owns modeset storage, links both the ABI client node and its private file into the canonical device lists, and device unregister invokes the unregister-or-release path. `drm_client_setup` now allocates, prepares, registers, and tears down the standard fbdev client using the canonical device helper slot. The remaining blocker is complete `drm_fb_helper` initial configuration plus `drm_fbdev_shmem` buffer/probe/framebuffer publication, without which an external DRM driver cannot expose an emulated fbdev node. | `linux_drm_fbdev_client::client_setup_allocates_registers_and_releases_one_fbdev_client`; external Bochs KPI audit. | B2133-linux-drm-kmi |
| KI-0285 | OPEN | MISSING | LOW | `__drm_dev_dbg` has the exact external variadic ABI but follows Linux's default-disabled debug gate until Oxide implements the DRM debug-category mask and its module/debugfs control plane; external driver debug records are therefore suppressed rather than selectively emitted. | `linux_drm_print::drm_dev_dbg`; compare `../linux-master/drivers/gpu/drm/drm_print.c::__drm_dev_dbg` and `_drm_debug_enabled(category)`. | B2133-linux-drm-kmi |
| KI-0286 | OPEN | MISSING | HIGH | Native PCI graphics has a QEMU std-VGA/Bochs model driver and a DRM/KMS dumb-buffer presentation path, but generic physical GPU coverage and external Linux DRM driver loading remain incomplete. The Vulkan probe now completes the standard count-and-handle enumeration contract (PR #7163); the broader DRM KPI deficit remains open. | The Vulkan probe contract test rejects empty/null physical-device handles; host Bochs audit still has 9 unresolved imports. | unowned |
| KI-0287 | OPEN | INFRA | med | **`make lint-ratchet` is tree-wide red, so it blocks every unrelated push without distinguishing a branch's own regression.** The current result is 55 keys above its baseline (1967 findings across 107 unit+rule keys against a baseline total of 1702), including the three existing docs keys but also drivers, `sched`, `syscalls`, `mm-pmm`, `modules`, and `hal-x86_64`. The actual findings must be burned down by their owners; re-baselining would merely waive them. Until then the pre-push hook needs `SKIP_LINT_RATCHET=1`, which means a genuine regression can be hidden among the standing failures. | B2281: `make lint-ratchet` reports 55 `REGRESSION` lines, byte-identical on `origin/main`, and names no `crates/kernel/ext4` key, so this branch adds none. The prior three-doc finding report was stale. | unowned |
| KI-0288 | OPEN | MISSING | med | **No per-packet security label exists, so `SCM_SECURITY` on a received INET datagram can never report one and `SO_PEERSEC` on a TCP socket only ever reports "unlabelled".** The reference derives a datagram's peer label from the packet via NetLabel (CIPSO/CALIPSO) or an IPsec security association, and sets a connected INET socket's peer label at `inet_conn_established` from the same source. Neither source exists here: no NetLabel, no labelled IPsec. AF_UNIX is unaffected, which is why the session bus works. | B2262, established while wiring `SO_PEERSEC`: `selinux_socket_getpeersec_dgram` needs `selinux_skb_peerlbl_sid`, and `selinux_inet_conn_established` needs `asoc->peer_secid`. Our `syscalls/src/recvmsg/inet.rs::peer_label` now correctly reports nothing rather than mis-reporting a stream peer's label. | unowned |
| KI-0289 | OPEN | COVERAGE | med | **Nothing fails if the socket-labelling module is never installed at boot.** `net::selinux_glue::init()` is called from one place in `kmain/src/kmain/early.rs`; deleting that call leaves every hosted test green (each installs the module itself) and every arch build green, and `SO_PEERSEC` silently returns to answering ENOPROTOOPT — the exact defect B2262 fixed. The same hole covers the two label-source installers beside it. A boot-reachability check is needed, not another unit test. | B2262. Confirmed by construction: the wiring tests in `net/src/sock_opts/peersec/tests.rs` call `install_socket_label` directly, so they cannot observe the boot path. | unowned |
| KI-0290 | OPEN | DEFECT | high | **Something decrements `preempt_count` once more than it incremented it, wrapping the count one below zero — the seed of the B2264 livelock, still unattributed.** The scheduler now repairs the count instead of livelocking on it, so the symptom is survivable, but the unpaired decrement is a real accounting bug: while the count is wrapped, `should_resched()` and `preemptible()` both read a non-zero word, so that CPU cannot take a reschedule at any preempt-enable until a `schedule()` repairs it. Intermittent: the F1241 post-merge x86 smoke named `local_bh_enable` subtracting `0x200` from zero at `sched/src/bh.rs`, while its retry and later diagnostic boots passed. | B2264 added the pre-subtract detector. F1242 established that the reported `bh.rs` line is the outer subtraction funnel, not an acquisition: `LockBhGuard::drop` had discarded its held-lock trace before reaching it. The pair is now checked while that trace is live, so a recurrence from `LockBhGuard` prints its exact `rank@file:line`. B2313 audited every production entry: core `lock_bh` users all funnel through `SchedBh`; the only raw pair was the Linux-module ABI, and the boot image contains no `.ko`; no unmatched direct `local_bh_enable` exists. The module ABI now gives raw spin locks Linux's preemption credit, returns it on failed trylock, and balances the combined `_bh` credit; this closes that parity gap but does not identify the intermittent underflow. The only production count replacements are the per-handler restore and B2264 scheduler recovery. Linux 7.2-rc4 has the same per-handler snapshot/restore, and B2264 preserves every count at or above the schedule-entry level, so neither can erase a live `0x200` credit in the observed path. Linux additionally tracks BH-disable depth on `current` and disables migration; this tree keeps only a per-CPU count, but no task switch or migration while the credit is live was found, so changing ownership would be speculation. Five independent B2313 x86 boots passed first attempt, userspace alive at 45–47 s, with no `[PREEMPT-LEAK]`, lost-credit, handler-repair, or `[BUG]` line. No exact acquisition recurred and no root-cause fix is claimed. | unowned |
| KI-0291 | OPEN | COVERAGE | med | **`block::registry::tests::published_disk_indices_do_not_reuse_a_live_disk_identity` is flaky under a full `cargo test --workspace` run.** `by_index(live)` returned `None` for a disk the test had just registered, so a concurrently-running sibling test evicted or re-indexed it: the registry is process-global and the tests that register into it are not serialised. Isolation hides it — the test passes alone and passes the crate's own full `--lib` run twice in a row. | B2669: one `cargo test --workspace` run failed at `crates/kernel/block/src/registry/tests.rs:95` (`left: None`, `right: Some("registry-index-live")`); `cargo test -p block --lib published_disk_indices` and two full `-p block --lib` runs are green on the same tree. Unrelated to that branch's changes, which touch no block code. | unowned |
| KI-0292 | OPEN | INFRA | high | **The 500-line file-cap split wave and the task core/security split merged to `main` in a state that does not compile on either arch, and no gate caught it.** Eight distinct break classes reached `main`: child modules written as siblings of their parent (`drv-hda` `mod lifecycle`/`mod routes`), `use super::*` in a file re-parented to the crate root (`drv-xhci` `storage_command.rs`), private items a split turned cross-module (`send_inet`, `submit_with_timeout`, `sq_thread`), `super::` paths stale at the new nesting depth (io_uring `iowq`/`iopoll`/`submit`, `fs_surfaces`, `wait_one_ms`, ioctl `uapi`), unconditional `use` of cfg-gated items (`kmain/early.rs`), a duplicated import block (`boot-x86_64` `mb2/info.rs`), a test harness that did not mirror the production `security` split (`ipc` futex), and a blanket `.security.` rename applied to receivers that are not `Task` (`Sqe`, `InetSocket`, `CoreContext`, `ExecTransition`, `Status`, io_uring registration state, netlink/unix control messages). | B2669: `cargo run -p xtask -- kernel --arch x86_64 --check` and `--arch aarch64 --check` both failed on `origin/main` at 45a46cbea; both clean after the repair commit, as is `cargo check --workspace --all-targets`. Commit `0e60e61cf` is the source of the `.security.` misapplication. | unowned |
| KI-0294 | OPEN | INFRA | med | **`/tmp` on the dev box is a 32G tmpfs that concurrent lanes fill and never reclaim, and a full `/tmp` makes healthy tests report as failures.** With it at 100%, 66 `ext4` image tests failed with `StorageFull (os error 28)` and the agent shell itself could not write command output. Reclaimed 14G by deleting four abandoned lane build trees (`kernel-B2319-build-artifacts-20260820` at 7.1G, two `oxide-b2330-*-target` at 5.1G, `oxide-F1237-artifacts` at 1.5G); ~9.5G of stale per-session scratch under `/tmp/claude-1000/-home-nd-oxide-kernel` remains. Lane build trees belong on `/home` (3.7T, 60% used), not on the tmpfs. | B2669: `df -h /tmp` read 32G/32G/1.6M free; failures cited `crates/kernel/ext4/tests/balloc_uninit_e2fsck.rs:185` `copy image: StorageFull`. | unowned |
| KI-0295 | OPEN | DEFECT | high | **Three `umh::spawn` paths reach ≥13000 B of a 16384 B kernel stack, and the gate that would have caught them has been blind since the split wave.** `stack-depth-gate` matches its one scheduling point by mangled path; the split moved `schedule_once` into `sched::live::schedule::switch::round`, so the matcher found nothing and the gate aborted instead of reporting. With the matcher repaired it fails: `umh::spawn::queue::khelper` 13216 B, `umh::spawn::queue::run_inline` 13104 B, `umh::spawn::child::start` 13040 B, none allowlisted. Whether they predate the split is unestablished — the gate could not run to say. | B2669: `make stack-gate` first died with "no scheduling point matching '5sched4live8schedule6switch13schedule_once'"; after pointing it at `…6switch5round13schedule_once` it reports `FAIL — 3 static path(s) reach >= 13000 B`. | unowned |
| KI-0297 | OPEN | MISSING | med | **The BPF LSM hook set is absent, so systemd cannot restrict filesystem access via BPF.** systemd reports `bpf-restrict-fs: BPF LSM hook not enabled in the kernel, BPF LSM not supported` at PID 1 start and silently drops that confinement for every unit that asks for it. | B2670 boot log at 4.013s. | unowned |
| KI-0298 | OPEN | DEFECT | med | **`/sys/kernel/debug` and `/sys/kernel/tracing` are already populated before their filesystems are mounted, so systemd mounts over live content.** Both report `Directory /sys/kernel/<x> to mount over is not empty, mounting anyway`; the reference ships those as empty mount points in sysfs and puts the content behind the mount. | B2670 boot log at 4.466s and 4.476s. | unowned |
| KI-0299 | OPEN | DEFECT | high | **`gnome-shell` cannot write dconf: `write() failed: Input/output error` on `/home/oxide/.config/dconf/user.*`.** `/home` is a 512 MiB ext4 image built by `xtask rootfs`, and the write returns EIO rather than short-writing or succeeding, so every settings change the desktop makes is lost. Likely the same ext4 write-path defect family as the 62 failing `ext4` image tests. | B2670 boot log at 157.066s and 157.074s, twice with different temp names, ~80s after the shell starts. | unowned |
| KI-0300 | OPEN | COVERAGE | high | **The `sysfs` unit suite is broadly flaky: a different test fails on almost every parallel run, on `main` as much as on any branch.** The crate's tests share one process-global sysfs tree and block/bus registry and are not serialised, so registrations and inode-cache drops from one test are visible to another. Any real sysfs regression is now indistinguishable from the noise, and the gate cannot be trusted to fail for the right reason. | B2670: three consecutive `cargo test -p sysfs --lib` runs on `main` failed 1, 2 and 2 tests, never the same set — `root::…drop_cached_invalidates_*`, `bus::…retained_device_inode_does_not_alias_same_name_replacement`, `bus::block_index::…`, `char_class::…`. Each passes alone. | unowned |
| KI-0301 | OPEN | DEFECT | high | **The virtio-blk engine runs at queue depth ONE, and that serialisation is the boot's largest single cost.** `modern/request.rs::submit_in_header` calls `acquire_turn`, which blocks until the request queue is completely idle (`!busy && pending.is_empty() && deferred.is_empty()`), so the device never has more than one request outstanding and every waiter pays a park/wake round-trip per request. The reference drives virtio-blk through blk-mq with per-CPU hardware queues and a queue depth of the virtqueue size, with many requests in flight. | B2671 measured a desktop boot with the new `debug-faultcost` profiler: 15,246 coalesced page-cache fills costing **10,202 ms — 669 us each** for a 64 KiB read that fits in one virtio request (`BOUNCE_DATA_BYTES` is 128 KiB, so no chunk splitting). Read-absent page faults, which is what a process start-up is, average 81 us and total 7.7 s; all syscalls together total 21 s over the same boot. | unowned |
| KI-0302 | OPEN | COVERAGE | med | **Nothing measured the page-fault path until B2671, so a fault-bound boot read as idle.** The syscall profiler (`debug-syscost`) cannot see the architecture fault vector, where demand paging, COW and file fill run — the work that dominates process start-up. `debug-faultcost` now counts and times user faults, splits them into the four architectural classes, and reports the filesystem fills they trigger. | B2671. With it armed, one boot named the block layer as the bottleneck in a single run. | unowned |
| KI-0303 | OPEN | PERF | high | **Ext4 journal commits now retain one published transaction for a Linux-shaped background checkpoint, but the broader write amplification remains open.** The commit owner durably publishes `s_start` and retains the staged home blocks; the periodic journal owner writes those blocks and advances the journal to clean, while explicit sync paths drain it before returning. A second commit checkpoints the retained transaction first, so journal space is never reused without a home-write owner. Direct non-batched commits keep their synchronous API contract. The remaining work is to replace the conservative one-transaction handoff with the full checkpoint list/batched tail accounting where it produces a measured gain. | B2812: hosted journal replay, checksum, batch, freeze and quota crash tests pass; the new background-vs-explicit barrier test passes. Three post-change GNOME runs measured **36.557 s / 36.785 s / 38 s** to the shell marker, about **1.366M syscalls** and **6.37–6.52 s CPU**; the deferred-pass run showed **8,269 writes** and no checkpoint flush before the marker. Against the earlier **20.563 s** run, this is not a demonstrated boot-time gain; the current result remains OPEN pending controlled repeated runs and full checkpoint-list work. | ext4 / journal lane |
| KI-0304 | OPEN | DEFECT | high | **A desktop boot issues ~490,000 block requests against a device that accepts one at a time.** With the queue depth of one (its own row) the write and flush traffic above is fully serialised, so the two defects multiply. | B2672: 412,620 requests at the 300k-fault mark, average 28.6 us waiting for the turnstile and 129 us in the device. | unowned |
| KI-0305 | OPEN | COVERAGE | high | **The ~390,000 block writes a desktop boot issues are not attributed to a caller, and the first guess was wrong.** They were assumed to be journal body blocks plus checkpoint target writes, on the reasoning that ~845 commits x ~460 blocks matches the count. B2675 coalesced the journal body into contiguous device runs — 512 separate `submit_sync` calls per transaction became a handful — and the write count did not move (393,036 -> 389,310, inside run-to-run variance). So the bulk of the write traffic comes from somewhere else: candidates are framecache writeback, `data=ordered` file-data writes, and zram swap. The block profiler needs a per-caller split before any more work is aimed at write traffic. | B2675: `[FAULTCOST] blk-write cnt=389310 ms=30558` after the coalescing, against `cnt=393036 ms=31789` before. The journal-body coalescing is retained — it is what the reference does and it removes real requests — but it is not the write path that matters. | unowned |
| KI-0306 | OPEN | DEFECT | high | **A desktop boot writes on the order of ten gigabytes to a device it should be writing a few hundred megabytes to.** Splitting the block profile by request size shows 150,942 writes in the 32-128 KiB band costing 18,317 ms, plus 98,309 in the 4-32 KiB band and 164,165 single-block writes — call it 9-19 GB moved in 95 seconds. A Fedora desktop boot writes a small fraction of that. The write path is amplifying, and the multiplier is large enough that no per-request optimisation will matter next to finding it. Candidates, none yet checked: `data=journal` behaviour routing file data through `metadata_write` (`mount/io.rs::write_data_byte_range` takes that branch when `behaviour().data.journals_data()`), framecache writeback rewriting whole files rather than dirty pages, and the journal's checkpoint writing every staged block again after the log copy. | B2676 `[FAULTCOST] wr-size` split, one boot to `GNOME Shell started`. Sizes are per virtio request; the 128 KiB ceiling is the driver's bounce chunk, so the band is truncated at the top and the real volume may be higher. | unowned |
| KI-0309 | OPEN | DEFECT | high | **Routing filesystem reads onto the block layer's owned-request path breaks the boot — and the "corrupts data" wording below is RETRACTED, see the B2682 note.** `submit_sync` is the compatibility entry the virtio engine serialises behind a turnstile that waits for the queue to fall completely idle, so exactly one request is ever in flight; `BlockDevice::submit` posts to the hardware queue with a per-request bounce buffer and descriptor head, and its own doc says new callers should use it. Switching `ext4`'s `read_byte_range` onto it (via a `submit_and_wait` helper) removed the turnstile — `blk-turn` fell from 26,613 waits to 1,456 — and the boot then failed with `nm-dispatcher: bus error ... in libp11-kit.so`, `... in libgnutls.so`, services answering `Connection refused`, and no desktop. A bus error executing a mapped library page is what a read returning the wrong bytes looks like. The owned path's existing callers (devbridge, page writeback, partitions) are all low-volume, so it has probably never run with many requests in flight; the completion match is by descriptor head against the `pending` list, which is where to look. NOT isolated — the change was reverted rather than shipped. | B2679, reverted. Turnstile wait was measured at 1.84 ms against 0.40 ms of device time once the B2677 write flood was gone, so this is the boot's dominant remaining cost and the payoff for fixing the owned path is large. First attempt also panicked in `RunqueueInner::enqueue: idle` because the wait slept unconditionally — the idle task reads blocks; a `can_sleep` guard fixed that and is worth keeping in any retry. **B2680 narrowed it by reading, and four theories are now dead:** descriptor ownership is correct (heads are strided by `MAX_REQUEST_DESCRIPTORS`, so a request owns `head..head+3` exclusively); the synchronous and owned paths DO exclude each other (`post_owned_request_inner` defers while `ring.busy`, `drain_admitted` refuses to drain while `busy`, and `acquire_turn` waits for `pending` and `deferred` to empty, so a used entry is never claimed by the wrong observer); the interrupting queue IS drained, by `run_completion_bottom_half` in the block softirq; and the head pool is large enough (`size/3`, ~85 for a one-frame queue), so deferral is not the common path. What remains, and it is written in the engine itself: `run_completion_bottom_half`'s doc says *"The current request engine has one owner at a time... Keeping the IRQ side to a bit raise is required before the engine can safely grow to multiple outstanding descriptor chains."* The experiment confirmed the code's own warning. The next lane should start at what makes a second outstanding chain unsafe — the deferral path (`start_deferred_requests`, which runs inside the drain loop and re-posts with the caller's retained completion) is the least-exercised piece and the first place to look. **B2682 RETRACTS the corruption claim.** A completion cross-check (`debug-blk-verify`) compares every used-ring entry's `len` against what the request should have moved. Re-running the failing experiment with it armed: **zero mismatches, and th … [trimmed; full text: scratch/archive/ledger-overflow.md KI-0309] | unowned |
| KI-0310 | OPEN | INFRA | med | **The remaining syscall/host ratios are downstream of the block device, not of syscall code — measured, not argued.** A per-phase profiler (`debug-syscost`, `[SYSCOST] phase` rows) splits each span into the part that ran and the part that slept. Path resolution: **141,330 spans that stayed on CPU cost 9.3 us each, and 1,020 spans that slept cost 7,186 us each — 0.7% of the calls holding 85% of the time.** `getname` is 525 ns and `getattr` 903 ns. So the walk is not what makes `openat` 11x or `newfstatat` 29x the host; waiting for directory and inode reads is, on a device that accepts one request at a time. Micro-optimising the walk would chase 9.3 us while 7.3 seconds sits in a thousand blocking reads. | B2681, one boot. This is the third time a guess about where cost sat was wrong and instrumentation settled it; the first two were the journal body and the write path. Fix the block layer's owned-request path (its own row) before spending another lane on per-syscall work. | unowned |
| KI-0311 | OPEN | MISSING | low | **The inline-encryption choke point covers only `submit_sync`; the async `submit` path has no equivalent, so the invariant holds today only because no caller takes that path.** `crypto/submit.rs`'s own module doc states the invariant it exists to hold: *"there is exactly one of these on purpose... the decision is made here, once, and there is no path that reaches a driver with a context still unserved."* `BlockDevice::submit` is exactly such a path — it has no crypto wrapper at all. A request carrying `req.crypt` submitted through it reaches the device with the context unserved: a read comes back as ciphertext and a write goes down as plaintext, and the doc says the device reports success either way so nothing downstream can tell. Not reachable on the current boot image, which is unencrypted, and not the cause of the owned-path failure above. | B2682, found while auditing every `BlockDevice` wrapper for a transform applied on one path and not the other (partition rebase and page-cache coherence both check out; crypto does not). **B2683 corrects the severity.** The first filing said the async path "can hand a driver an unserved crypto context", which read as a live defect; it is not one. A grep for `crypto::submit::submit_sync` missed the production callers, which use the re-exported `block::crypto::submit_sync` — and the only place a context is ever attached, `sectors/src/device.rs`, routes both of its call sites through it. So no unserved context reaches a driver today. The hazard is that nothing stops the next caller from attaching one and calling `BlockDevice::submit`, where it would be silently ignored. B2683 adds the async choke point so that caller has the right thing to call. | unowned |
| KI-0313 | OPEN | DEFECT | high | **A short fill on a mid-file page is ruled fatal and reported as SIGBUS, and one cause was three sizes where the reference keeps one — that part is fixed; the rest is not.** The fault path computes how much of a page is file-valid from the VFS inode's `i_size`, while `read_framed` clamps what it supplies to the `Ext4FrameStore`'s own `size` and short-reads past it. Three sites moved the inode's size without the store's (`data.rs::refresh_inode_usage`, `meta.rs:254`, `meta.rs:373`), leaving a window where the fault asked for more than the backing would give. B2691 routes all of them through one `publish_size` publisher. | B2691, measured on the block experiment that provokes it: bus errors **18, 17 -> 2, 0, 0** and `[SHORT-FILE-FAULT-FATAL]` **37, 24 -> 2, 18, 0`. Reduced, NOT eliminated — one run still logged 18 short fills with no bus error, so a second cause remains. The `fatal` test has two arms (`read_err` and `filled < valid`); the size divergence was the second, and the remaining cases are most likely the first, a fill that errored rather than one that stopped at a stale size. ext4 721 passed / 64 failed, the same 64 names as `origin/main`. | unowned |
| KI-0314 | OPEN | DEFECT | high | **The remaining SIGBUS cause is a short fill whose source is still unidentified, and `[SHORT-FILE-FAULT-FATAL]` now tracks it one-for-one.** B2692 measured 0/0, 10 bus errors beside 11 fatal fills, and 5 beside 5 — the fatal short fill IS the bus error, confirmed. Two causes have been removed and neither was the whole of it: the three-sizes divergence (B2691) and the collapse of a transient backing failure into an I/O error (B2692, which taught the fill loop to retry a `FileBackingError::Again` within its existing attempt bound rather than rule it fatal). Neither eliminated the fault. What remains is the `filled < valid` arm reached without a transient and without a stale size — a backing that returns fewer bytes than the fault's `valid` for a mid-file page, for a third reason. | B2692. The always-on fatal log names ino, offset, valid and size on every occurrence, so the next lane can read the cause off a single failing boot rather than re-deriving it. | unowned |
| KI-0315 | OPEN | DEFECT | high | **The remaining SIGBUS is a fill whose `read_at` FAILS outright — not a short read — and the failure originates inside ext4, not in the block layer.** B2693 added `filled`, `err` and `desync` to the always-on fatal log, and every occurrence across four boots reads the same: `filled=0 err=1 desync=0`. The backing supplied nothing and reported an error. Measured on the same boots: **zero block-read failures or timeouts** (the experiment's helper prints every one) and **zero `[EXT4-CSUM-FIRST]`** with `debug-csum-origin` armed, so neither the device read nor a metadata checksum is the source. | B2693. That leaves a five-way question rather than a mystery — `read_framed` returns `Err` only from `lock_cache_page` or a `frame_ptr(pa)` that resolves to `None`, and `ensure_page` only from `begin_fill` refusing (now `Eagain`, which no longer sets `err`), a failed `read_inode`, a non-regular `dinode`, or a failed `fill_window`. Since the device read and the checksum are both excluded, `frame_ptr(pa) == None` — a published cache page whose frame no longer resolves — is the one worth instrumenting first. One discriminator log at those sites names it on the next failing boot. | unowned |
| KI-0316 | OPEN | DEFECT | high | **The owned-request block path genuinely stalls: completions fail to arrive within five seconds, and that is what produces the SIGBUS.** B2695 traced the last hop. The fill fails at `run-read`, the device read, which returns EIO because the waiter timed out. The first timeouts were an artefact of the experiment's own helper — it treated `wait_event_uninterruptible_until` RETURNING as the completion having landed, without re-checking the predicate, so it reported a timeout microseconds after submitting; the driver's own `park_blk_checked` loops for exactly that reason. With the wait corrected to loop until the flag is set or the deadline truly passes, **timeouts still occur** (three boots: 0, >=10, >=10) alongside 7, 1 and 4 bus errors. So a real stall remains underneath the helper's bug. | B2695. Full chain, every hop measured: completion never delivered -> waiter times out -> `read_byte_range` EIO -> `read_file_range` `BlockIo` -> `fill_window` -> `ensure_page` EIO -> `read_at` err -> `filled=0 err=1` -> fill fatal -> `Error::Io` -> `FaultFailure::BusError` -> SIGBUS. The next lane instruments the driver side of a timed-out chain: whether its descriptor head is still in `pending`, whether the used ring ever carried it, and whether the softirq that would drain it ran. | unowned |
| KI-0317 | OPEN | DEFECT | high | **reproducible on `main`, and NOT caused by any live-image work.** `main`'s own `target/builds/micro` ISO with its ext4 root: `init[1]: segfault at 0000000000000000 ip 00007f29046787b5 sp 00007ffeab46fc70 error 0000000000000004`, then `[PANIC] Attempted to kill init!`, on **three boots of three**, before systemd prints its banner. error=4 is a user-mode READ of a non-present page, and the report printed no `print_vma_addr` tail, so the VMA holding the faulting IP carries no file. The `gnome` profile is unaffected — `make smoke-x86` answers on serial in 6 s. | F1263, measured while chasing what looked like a squashfs-root defect and is not one. Same profile on the same branch with an EXT4 root: 1 of 3 boots reaches `Reached target`. Same userspace as a squashfs+overlay root: 1-2 of 5. The live image gets FURTHER than the ext4 one — it prints systemd's banner and dies later, where systemd's own SIGSEGV handler is installed, which is why no `segfault` line appears there and the exit status is the only evidence (`[INIT] Attempted to kill init! exitcode=0x18b` — signal 11, core dumped). Not the short-fill class: `debug-shortfill` reports zero `[SHORT-FILE-FAULT]` across the failing boots. | unowned |
| KI-0318 | OPEN | INFRA | high | Hosted per-crate check fails on main itself: sched/nt_object* uses crate::live paths that are gated out of the bare hosted build (E0432/E0433 in completion.rs, mutant.rs, nt_object.rs), and drv-virtio-pmem device.rs is all dead code under -D warnings. Every push must bypass with SKIP_HOSTED_GATE=1 until fixed; likely introduced by the recent NT-object merges. | C320 push: ./tools/hosted-check.sh red on cargo check -p kmain / boot-x86_64 / tty at origin/main e01678b2b; branch touches no crate, so pre-existing on main. | unowned |
| KI-0319 | OPEN | INFRA | high | feature-gate is red on main itself: a debug-feature block in syscalls dispatch/core.rs uses return_task outside its scope (E0425 x2 at lines 264/272; binding declared at 199 in a different scope), so make feature-gate-x86 fails and every push needs SKIP_FEATURE_GATE=1. | C320 push at origin/main e01678b2b: make feature-gate-x86 -> E0425 in crates/kernel/syscalls/src/dispatch/core.rs; branch touches no crate. Plain xtask kernel --check is green, so the break is only in the debug-feature build. | unowned |
| KI-0331 | OPEN | INFRA | med | audit-counts rejects zero enforced violations, so a clean scoped rule makes the PR gate red | B3325 on current origin/main: make audit-counts reports code/extern-std=0, code/panic-fmt=3, code/static-mut=0, then FAIL — a scoped rule left zero. This branch changes only scheduler selection/comments/tests and cannot affect extern-std or static-mut counts. | unowned |
| KI-0332 | OPEN | DEFECT | high | install_default_runqueue reserves an 8216-byte x86_64 stack frame above the 8192-byte frame gate ceiling | B3325 make frame-gate on current origin/main-derived code: frame-size-gate reports sched::live::schedule::lifecycle::install_default_runqueue at 8216 bytes, NEW relative to tools/frame-size-baseline-x86_64.txt. This branch does not edit lifecycle.rs or stack-bearing locals. | unowned |
| KI-0333 | OPEN | DEFECT | high | x86_64 interrupt path exceeds the 12000-byte nested-interrupt stack budget | B3325 direct irq gate on a fresh current-origin/main kernel reports oxide_irq_dispatch at 13856 bytes; aarch64 passes at 4752 bytes. This branch changes no interrupt code, indirect-edge map, or stack-bearing scheduler function. | unowned |
| KI-0335 | IN-PROGRESS B3331-windows-path-unicode-casefold | MISSING | med | [CLAIMED B3331-windows-path-unicode-casefold 2026-09-04] Windows DOS path lookup keys fold only ASCII, so Unicode case-insensitive names can miss their existing Windows file | Current userspace/probes/windows-path lookup_key uses to_ascii_lowercase. Wine path matching uses Unicode towupper when case-insensitive; the Windows Phase 7 contract requires case-insensitive lookup while preserving display spelling. Add Unicode lookup-key coverage and implementation. | windows-path |
| KI-0337 | IN-PROGRESS H1541-windows-exception-unwind-chain | DEFECT | high | [CLAIMED H1541-windows-exception-unwind-chain 2026-09-04] Phase 9 x86-64 chained unwind records are rejected instead of following the IMAGE_RUNTIME_FUNCTION_ENTRY chain, preventing canonical RtlVirtualUnwind behavior for functions split across chained prolog records. | Current shared PE unwind_x64 rejects UNW_FLAG_CHAININFO. Wine resolves the chained runtime function and continues unwind; focused regression will cover the chain and reject malformed chain targets. | NT runtime |
| KI-0339 | IN-PROGRESS L1541-windows-audio-extensible-pr | MISSING | med | [CLAIMED L1541-windows-audio-extensible-pr 2026-09-04] Phase 11 audio boundary lacks canonical WAVEFORMATEXTENSIBLE validation | Current windows-audio rejects every nonzero cbSize. The Wine-derived audio boundary accepts WAVEFORMATEXTENSIBLE extension only for PCM/IEEE-float subtype, valid bits, and channel mask that fit native format. Add hosted ABI/semantic tests and preserve the existing native audio owner. | windows-audio |

## Ext4 master program

The authoritative dependency-ordered ext4 inventory and execution plan is [`scratch/ext4.md`](ext4.md). The older ext4 rows above retain historical measurements and are mapped to the IDs below; they are not a second completion list.

| ID | Status | Remaining issue | Evidence / closure requirement |
|---|---|---|---|
| E4-00 | DONE | The ext4 baseline and fixture ownership are frozen. The complete package suite passes in parallel, all workspace test targets build independently, and the real-root harness uses the caller-selected image without shared mutable fixtures. | 375 ext4 unit tests plus image suite; 177 isolated workspace test-target builds; serial real-root workload on both GNOME images. |
| E4-01 | DONE | Ext4 ordinary file reads use the block layer's owned-request wait path. The focused concurrency harness, byte-for-byte real-image reads, serial 7/7 real-root GNOME workloads on x86_64 and AArch64, target checks, and current boot smokes are green. | `mount/io.rs::read_byte_range`; owned concurrent-read harness; serial 7/7 real-root workloads on both images; real-image content comparison; both-arch target checks and boot-smokes. |
| E4-02 | DONE | Canonical metadata reads are shared and generation-fenced, frame publication retries on replacement, failed owners are removed for retry, duplicate owned completions retain the first result, and concurrent waiters now receive one published failure before a later retry. The synchronous metadata-read contract has no cancellation point: the owner waits for completion, publishes the result, then removes in-flight ownership. | metadata-owner tests cover the concurrent failed-owner window; real-root x86_64/AArch64 workloads, target checks, and current x86/AArch64 boot-smokes pass. The reference metadata path likewise waits uninterruptibly for the buffer completion. |
| E4-03 | DONE | Canonical size publication and short-fill retry handling are in place. The complete ignored real-root harness passes serially on both GNOME root images, including VFS journald writes, merged-usr symlink walks, metadata churn, and remount checks; x86 full GNOME/Firefox and ARM SMP=2 graphical resolver checks pass. | serial 7/7 real-root workload on x86_64 and AArch64; repeated boot, resolver, and live GNOME verification. |
| E4-04 | DONE | Journal replay invalidates pre-replay metadata cache and refreshes the GDT/counter mirror; quota crash/replay coverage passes. The complete ignored real-root harness passes serially on both GNOME root images, including batched journald persistence, writeback, and remount checks. A real dconf database overwrite and a newly allocated dconf database both survive frame-cache writeback, journal flush, and remount. | serial 7/7 real-root journal workload on x86_64 and AArch64; quota replay matrix; `livegnome_so_read::configured_root_dconf_write_survives_remount` passes on both images. |
| E4-05 | DONE | The journal retains pending checkpoint images, coalesces repeated targets, and publishes clean state only after home writes. Commit records use a distinct device-write phase. The crash/replay matrix covers power loss before/after commit, during a torn commit record, before/after publish, after home checkpoint, during a partial home checkpoint, and after partial descriptor/data body writes; quota replay and write-amplification coverage are green. | 18 journal image tests; 5 quota replay tests; 40-page writeback at 0.25 device writes/page; batched-create accounting; repeated boot/perf evidence. |
| E4-06 | DONE | `journal_async_commit` has live option validation, on-disk feature transition, and async JBD2 commit ordering. Shared crash/replay coverage remains under E4-05. | E4-05 crash/replay matrix. |
| E4-07 | DONE | Exact physical preallocation ownership, failed-claim rollback, contiguous locality-tail consumption with Linux-style size re-bucketing, Linux's `fls(len)-2` average-fragment buckets, the bounded three-order best-available scan fallback, bounded multiblock extent selection, and inode-PA release on truncate, punch-hole, range shifts, final orphan eviction, last-writer release, ENOSPC discard, and mount teardown are covered. | merged ownership/rollback/scan/lifecycle fixes; fragmentation, ENOSPC, migration, remount, and e2fsck-clean coverage. |
| E4-08 | OPEN | Htree selection improved and Linux RCU now removes much of the cached-walk overhead; the rwsem reader fast path avoids the wait-lock round trip for uncontended shared readers, delayed-allocation buffered writes no longer reread the inode table before returning, and linear-directory byte matching now avoids per-entry object and closure work while borrowing the shared metadata block. Linear scans retain Linux's per-directory last-successful-block hint. Special inode construction and directory mutation now reuse parsed inode fields rather than rereading the inode table. Create/mkdir/symlink/mknod and ordinary VFS unlink/rmdir/link/rename/writeback now durably publish journal records before returning and leave home checkpointing to the background owner, matching the reference journal-handle boundary. Victim-aware VFS unlink/rmdir now retain the already-resolved same-mount inode identity instead of rescanning the parent; direct name-only callers retain the fallback lookup. Victim-aware unlink also reuses the VFS inode type and avoids a duplicate pre-transaction inode read. Unlink/rmdir now retain the cached directory lookup image because those mutations do not change its size, flags, or inode generation. Ext4 inode xattrs now load lazily behind a sleepable per-inode owner on the first xattr, ACL, or list operation, matching Linux's on-demand path. Checkpoint backpressure makes the periodic owner skip a busy transaction gate and retry on the next tick. Rename now reuses its outer transaction for delete/rmdir/link-count/timestamp/dotdot/whiteout operations. Fresh create fanout attribution reached GNOME in three controlled SMP=1 runs: prepare/quota/post work stayed below 1 ms aggregate, create backend measured 1,604–2,596 ms, and transaction-gate accumulation reached 2,142 ms across mutators. Linux admits concurrent handles to one running transaction with protected metadata buffers and allocation-group state; this tree now releases the mount-wide gate during unrelated operation bodies and atomically retires locality-PA ownership at selection. Aggregate closure remains open at ~12–15x host; the blocking ext4 lookup path, residual mutation work, and repeatable controlled performance evidence remain the owners. |
| E4-08 status update | OPEN | Per-metadata-block writer ownership, per-handle rollback frames, allocation-group ownership, and gate release during unrelated operation bodies are implemented. Locality-PA selection retires its claimed prefix atomically under the mount state lock, preventing duplicate claims under concurrent handles. Canonical VFS regular-file create, mkdir, symlink, and mknod now reuse the locked parent inode image for inheritance and directory insertion, matching Linux's in-core ownership; noncanonical/test callers retain the explicit fallback. Syscall symlink/mknod callbacks now pass the already-resolved negative dentry proof through unchecked hooks, and fresh symlink inode bytes are carried forward instead of rereading the just-written inode; direct helpers remain checked. `mkdir` now advances the live VFS parent `i_nlink`, cached ext4 parent image, and serialized link count together, matching Linux `ext4_mkdir`/`inc_nlink`; its initial directory-block append also publishes the updated live extent image, fixing first-child creation after inode reuse. Commit `b1426119e` plus the current live-extent lane; the ext4 suite is 378/378; the create/readback harness is 8/8; ACL is 4/4; the allocator/e2fsck harness is 7/7; the real-root harness is 7/7, including systemd symlink walks and metadata/remount checks. This correctness change claims no aggregate speed gain. |
| E4-09 | DONE | `inode_readahead_blks` warms a Linux-bounded inode-table window through the canonical metadata cache and excludes each group's `bg_itable_unused` tail. The ext4 address-space readahead worker publishes multiple resident file pages from both real GNOME root images. | `livegnome_so_read::configured_root_regular_file_readahead_populates_frames` passes for x86_64 and AArch64 images; both-arch target checks, boot smokes, and perf comparison pass. |
| E4-10 | OPEN | Ext4 admits extent-based regular-file synchronous and polled DIO through one filesystem-owned mapping/coherency path, with Linux alignment, journal-data, RWF sync, unwritten conversion, cache invalidation, and legacy direct/single/double/triple-indirect read mapping. The image harness covers deferred completion, errors, hole allocation, remount persistence, and inline regular-file/directory/symlink operations. Multi-block inline conversion and legacy-indirect mutation are covered by the generated and mounted-image harnesses. DAX filesystem and virtio-pmem provider owners are implemented. Shared mappings now dirty only at the Linux `page_mkwrite` boundary; frame-coherency is 13/13. A fresh boot reproduced stale page-store reuse after inode-number recycling; canonical stores now key by `(ino, i_generation)`, with a hosted regression proving same-generation reuse and cross-generation separation. The fixed instrumented x86_64 SMP=2 GNOME boot reached GNOME Shell, colord, and fwupd with no fill diagnostics or early EIO cascade. Real DAX image/runtime evidence and AArch64 graphical closure remain. | Remaining E4-10 closure is final DAX image/runtime, AArch64 graphical, and final GNOME evidence. |
| E4-11 | DONE | `prefetch_block_bitmaps` eagerly validates and publishes allocation bitmaps. External xattr blocks now have a Linux-style hash index, byte-identity check, on-disk refcount sharing, and final-release free; `nombcache`/`no_mbcache` disables that owner. EA-inode xattr values use the Linux hidden-inode format, metadata hash, ctime/i_version reference count, reusable value cache, collision comparison, and final-release teardown. The Linux COMPAT orphan-file format is also parsed, checksummed, slot-managed, and recovered; legacy `s_last_orphan` remains the fallback when the orphan file is full. | EA-inode image round-trip/sharing and full ext4 matrix pass; real Linux-image EA-inode compatibility remains part of runtime closure. |

E4-10 status correction (B2979): multi-block inline regular-file conversion is
covered. Legacy indirect inodes preserve the legacy pointer tree for mapped
writes, direct/single/double/triple branch geometry, direct/single/double
allocation, truncate, punch-hole, and keep-size fallocate; a mounted-image
triple-indirect chain is also read through the same mapper. DIO now uses the
same inode-aware mapping and zero-prepares legacy holes, and nodelalloc no
longer rejects legacy inodes before writeback. DAX provider implementation is now present; real-image `dax=inode` and `dax=always` mounts pass on SMP=1. The remaining runtime failure is the separate SMP idle-timer wedge.
This correction supersedes the stale E4-10 wording in the inventory row above:
multi-block inline conversion and legacy-indirect mutation are covered by
canonical owners. Collapse/insert range remains extent-only, matching Linux;
the remaining E4-10 closure item is final boot evidence under SMP=2; DAX
provider and real-image mount evidence are complete. SMP=2 can still wedge
after idle because the scheduler's clockevent retry path is a separate owner.

E4-10 status update (B2989): inline data is also converted before fallocate,
punch-hole, and truncate, so those operations cannot dispatch inline payload
bytes to the indirect-pointer owner. The focused inline-image operation test
and the full ext4 suite pass on merged main.

E4-10 status update (B2990): polled DIO now admits device-aligned offsets and
lengths, translates sub-filesystem-block ranges to the correct device sectors,
and delegates partial writes into unwritten extents to the synchronous RMW
owner. Deferred-device image coverage and the full ext4 suite pass.

E4-08 status update (B2994): linear directory lookup now reads the current
directory block through the canonical metadata owner before queuing only the
following bounded readahead window. This removes the prior duplicate extent
mapping walk for the current block and matches Linux's batch-read/consume
ordering. Focused and full ext4 tests plus both architecture checks pass; the
aggregate performance item remains open because the fresh GNOME-marker run did
not reach a valid desktop sample.
| E4-12 | DONE | Every known admitted ext4 option without a live consumer now refuses explicitly; generic VFS mount tokens remain pass-through. | Add a real owner before admitting any refused filesystem option. |
| E4-13 | DONE | All ext4 e2fsck image fixtures use PID/sequence-unique temporary paths and clean up after each run; the large allocator harness uses a per-process directory, and serial/parallel runs agree. | Workspace-wide fixture audit plus serial and four-thread harness runs. |
| E4-14 | DONE | The previously reported ARM sysinit EIO/SIGBUS event did not reproduce in the controlled ARM boot-smoke run; userspace answered the systemd probe and the serial RX probe passed in 22 seconds. | Controlled ARM boot-smoke evidence; retain broader ARM desktop validation under E4-03. |
| E4-15 | DONE | The controlled GNOME/SMP=1 comparison and phase attribution are recorded. The result does not claim a whole-boot gain: the current run remains within the documented workload variance and attributes the dominant resolution cost to parent-lock waits, not inode-table lookup selection. | `scratch/perf-history.md` E4-15 perf-r1 and perf-r2; `target/perf-report-x86_64.log`; fresh-main repeatability run. |
| E4-16 | DONE | Historical ext4 ledger rows are explicitly historical, mapped to the E4 inventory, and no longer carry the stale pending SHA or old current-suite count. | `scratch/perf-history.md` E4-08 maps to merged `6da0ed7c0`; the superseded 62-failure row records the current 373-unit/full-image harness result. |

Windows NT frontier update (2026-09-02): the canonical NT object namespace now
owns named event creation/opening over the native event wait object. Named
events reuse one object identity across handles, report type collisions, and
are enumerated as Event entries; missing parent directories cannot be created.
The graph's `NtOpenEvent` boundary is now native rather than an explicit
unsupported result.

Windows NT frontier update: the Notepad import graph now resolves through
`NtAdjustGroupsToken`; its bounded replacement/reset path is implemented, while
previous-state/return-length buffers and `NtAdjustPrivilegesToken` remain open.
Windows NT frontier update (2026-09-01): `NtAdjustPrivilegesToken` now has
bounded privilege mutation, disable-all, and prior-state output; the graph
advances to `ntdll.dll!NtAllocateLocallyUniqueId`.
Windows NT frontier update (2026-09-01): `NtAllocateLocallyUniqueId` now
validates the output pointer and allocates monotonic LUID values; the graph
advances to `ntdll.dll!NtAllocateVirtualMemoryEx`.
Windows NT frontier update (2026-09-01): `NtAllocateVirtualMemoryEx` now
accepts the native seven-argument ABI, validates the seventh stack argument,
and delegates zero-extended-parameter allocations to the existing VM owner;
the graph advances to `ntdll.dll!NtCancelIoFile`. Extended parameter records
remain unsupported until their VM semantics are implemented.
Windows NT frontier update (2026-09-01): `NtCancelIoFile` and
`NtCancelIoFileEx` validate NT file handles and publish IO status blocks;
the synchronous NT file adapter has no pending request queue, so Ex-specific
request cancellation reports `STATUS_NOT_FOUND` and synchronous-I/O
cancellation remains open. The graph advances to
`ntdll.dll!NtCancelSynchronousIoFile`.
Windows NT frontier update (2026-09-01): `NtCancelSynchronousIoFile` now
validates current/real NT thread handles and publishes the reference
`STATUS_NOT_FOUND` result when no synchronous request is pending; the graph
advances to `ntdll.dll!NtCompareObjects`.
Windows NT frontier update (2026-09-01): `NtCompareObjects` now distinguishes
current-process/current-thread pseudo handles, validates process-local handles,
and compares duplicated handles by canonical NT object identity; the graph
advances to `ntdll.dll!NtConvertBetweenAuxiliaryCounterAndPerformanceCounter`.
Windows NT frontier update (2026-09-01):
`NtConvertBetweenAuxiliaryCounterAndPerformanceCounter` now exposes the native
four-argument ABI and the Wine-compatible unsupported-counter result (including
the required null-input access violation); the graph advances to
`ntdll.dll!NtCreateKey`.
Windows NT frontier update (2026-09-01): the existing `CreateKey` service is
now reachable from the synthetic NTDLL export page as `NtCreateKey`; registry
key object semantics are not yet implemented, and the graph advances to
`ntdll.dll!NtCreateNamedPipeFile`.
Windows NT frontier update (2026-09-01): `NtCreateNamedPipeFile` now has an
explicit native export and an explicit `STATUS_NOT_IMPLEMENTED` boundary;
named-pipe object and connection semantics remain pending. The graph advances
to `ntdll.dll!NtCreateSectionEx`.
Windows NT frontier update (2026-09-01): `NtCreateSectionEx` now has an
explicit native export and a fail-closed `STATUS_NOT_IMPLEMENTED` boundary;
extended section parameter records remain pending. The graph advances to its
`ntdll.dll!NtCreateSymbolicLinkObject`.
Windows NT frontier update (2026-09-01): `NtCreateSymbolicLinkObject` now has
an explicit native export and fail-closed `STATUS_NOT_IMPLEMENTED` boundary;
symbolic-link object semantics remain pending. The graph advances to its next
unresolved native import, `ntdll.dll!NtCreateUserProcess`.
Windows NT frontier update (2026-09-02): `NtCreateUserProcess` now validates
the x86-64 attribute-list ABI, resolves the UTF-16 image name, prepares a
fresh PE address space with inherited Wine module catalog, installs typed
process/thread handles, and publishes the child only after PEB/TEB setup.
`CREATE_SUSPENDED` is represented through the native suspend depth and resumed
through the existing scheduler wake path. The normalized command line,
double-NUL environment, current directory, console handle, and standard
handles are now copied into the child-owned PEB. The complete 88-byte x64
`PS_CREATE_INFO` success union now returns the normalized parameter and PEB
pointers; section and manifest handles remain explicitly zero because those
owners are not yet present.
The graph advances to `ntdll.dll!NtDelayExecution`.
Windows NT frontier update (2026-09-01): `NtDelayExecution` now uses the
existing scheduler interruptible wait path with native relative/absolute
100-ns timeout conversion and alertable-result mapping; the graph advances to
`ntdll.dll!NtDeleteKey`.
Windows NT frontier update (2026-09-01): `NtDeleteKey` now has an explicit
native export and fail-closed `STATUS_NOT_IMPLEMENTED` boundary; registry key
deletion semantics remain pending. The graph advances to its next unresolved
native import, `ntdll.dll!NtDeleteValueKey`.
Windows NT frontier update (2026-09-01): `NtDeleteValueKey` now has an
explicit native export and fail-closed `STATUS_NOT_IMPLEMENTED` boundary;
registry value deletion semantics remain pending. The graph advances to its
next unresolved native import, `ntdll.dll!NtDuplicateToken`.
Windows NT frontier update (2026-09-01): `NtDuplicateToken` now validates
token-handle access, requested access, token type, and output memory, then
returns a new process-local handle retaining the token state. The graph
advances to `ntdll.dll!NtEnumerateKey`.
Windows NT frontier update (2026-09-03): `NtEnumerateKey` and
`NtEnumerateValueKey` now route through the canonical userspace registry owner
with Wine-shaped basic/full/partial record encoding and access checks. The
remaining registry gap is hive load/save serialization, not enumeration.
Windows NT frontier update (2026-09-03): `NtFilterToken` now creates an
independent native token object, removes requested group SIDs, and disables
requested privileges. Unsupported flags and restricted-SID filtering remain
explicit until those token owners are defined. The graph advances to
`ntdll.dll!NtFlushBuffersFile`.
Windows NT frontier update (2026-09-01): `NtFlushBuffersFile` now resolves
through the native file adapter and flushes the backing VFS file with NT
handle-access and `IO_STATUS_BLOCK` completion semantics. The graph advances
to `ntdll.dll!NtFlushInstructionCache`.
Windows NT frontier update (2026-09-01): `NtFlushInstructionCache` now has
an explicit native export and validates the current-process handle before the
x86/x86_64 coherent-cache no-op. The graph advances to `ntdll.dll!NtFlushKey`.
Windows NT frontier update (2026-09-01): `NtFlushKey` now has an explicit
native export and fail-closed boundary while registry state remains owned by
the userspace registry service. The graph advances to
`ntdll.dll!NtFlushVirtualMemory`.
Windows NT frontier update (2026-09-01): `NtFlushVirtualMemory` now validates
the current-process scope, rounds and verifies the requested mapped range,
flushes file-backed VMA ranges through the canonical backing owner, and
publishes the adjusted address/size. `NtGetContextThread` now exposes the
current x86_64 task's integer/control context from the canonical `PtRegs`
owner; remote, debug, floating-point, segment, and XSTATE context remain
explicitly unsupported. The graph advances to
`ntdll.dll!NtGetNlsSectionPtr`, which now resolves Wine-compatible NLS data
files from `/usr/share/wine/nls` through the normal read-only file-backed VMA
path. `NtGetTickCount` now uses the monotonic timekeeper with Windows'
32-bit millisecond wrap semantics. The graph advances to
`ntdll.dll!NtGetWriteWatch`. The write-watch ABI is exposed with strict
current-process validation but remains `STATUS_NOT_IMPLEMENTED` until VMA
and page-fault dirty tracking has a dedicated owner; the graph advances to
`ntdll.dll!NtImpersonateAnonymousToken`. Wine’s own implementation is a
`STATUS_NOT_IMPLEMENTED` stub, so Oxide exposes the same explicit boundary;
the graph advances to `ntdll.dll!NtIsProcessInJob`. That call now consults
the existing NT job association and returns Wine-compatible in-job status;
the graph advances to `ntdll.dll!NtLoadKey`. Its ABI/export are exposed, but
hive loading remains `STATUS_NOT_IMPLEMENTED` until the userspace registry
owner gains a typed load transaction. The graph advances to
`ntdll.dll!NtLockVirtualMemory`.
`NtLockVirtualMemory` now follows Wine’s current-process path: it rounds and
publishes the range, applies the VMM-owned locked VMA state, and populates
resident pages through the PMM path. Remote-process locking remains an
explicit `STATUS_INVALID_PARAMETER` boundary until the NT APC/process-memory
owner exists. The graph advances to the next unimplemented NT export.
The next graph export, `ntdll.dll!NtMakeTemporaryObject`, is exposed with an
explicit `STATUS_NOT_IMPLEMENTED` boundary because the NT handle table does
not yet own named-object permanence state. The graph then reaches
`ntdll.dll!NtMapViewOfSectionEx`; its extended parameter and APC mapping
protocol remain an explicit `STATUS_NOT_IMPLEMENTED` boundary until those NT
owners exist.
The graph now reaches `ntdll.dll!NtNotifyChangeDirectoryFile`; the supported
non-subtree create/delete watch path is backed by the VFS dirent observer and
an NT event. Watch records are owned by the directory handle and issuing TID,
`NtCancelIoFile[Ex]` completes matching records with `STATUS_CANCELLED`, and
handle close completes them with `STATUS_HANDLES_CLOSED`. Subtree and the
remaining notify filters, plus APC/completion-port delivery for these watches,
remain explicit work.
The graph then reaches `ntdll.dll!NtNotifyChangeKey`; the supported
non-subtree `REG_NOTIFY_CHANGE_LAST_SET` watch is now owned by the native key
object and issuing TID/IOSB, with cancellation and final-handle teardown
completion. APC delivery, subtree traversal, and the remaining filters remain
explicit work until their owners exist.
`NtOpenEvent` is now implemented through the canonical named-event namespace;
the earlier unsupported-boundary note is superseded by the 2026-09-02 update.
`NtQueryAttributesFile` is implemented through the canonical VFS resolver and
inode metadata owner, with Windows `FILE_BASIC_INFORMATION` time and type
translation.
`NtQueryDefaultLocale` is implemented with strict NT-personality/pointer
validation and the current en-US baseline LCID (`0x0409`).
`NtQueryDefaultUILanguage` is implemented against the same validated en-US
baseline (`LANGID 0x0409`).
`NtQueryDirectoryObject` is now implemented through the canonical object
directory namespace with restart/context and variable-record ABI handling.
`NtOpenProcess` now resolves cross-process NT `CLIENT_ID` values through the
canonical scheduler registry, normalizes a target to its process-group leader,
rejects Linux-personality targets, and installs a process-local handle with
validated access rights. Remote memory access remains a separate boundary.
Windows NT frontier update (2026-09-02): the canonical NT object namespace now
owns named semaphore creation and opening over the native scheduler semaphore.
Named handles reuse one object identity, report collision/type/missing-parent
statuses, and enumerate as `Semaphore` entries.
Windows NT frontier update (2026-09-02): named section creation publishes
existing anonymous or file-backed section objects into the canonical NT
namespace, while `NtOpenSection` resolves typed handles with validated access.
Section mapping continues through the existing VMM-backed view owner.
`NtOpenSemaphore` now resolves named semaphore handles through the canonical
NT object namespace; semaphore wait/release behavior remains scheduler-owned.
Windows NT frontier update (2026-09-02): NT symbolic-link objects now have a
canonical target owner with create/open/query services, typed access checks,
Unicode target copy-out, and `\\DosDevices` namespace seeding. They remain
distinct from VFS symlinks; path traversal integration is the next boundary.
The graph now reaches `ntdll.dll!NtOpenThread`; its `CLIENT_ID` now resolves a
live NT scheduler task, verifies the owning process, and installs a typed
thread handle. Zero/out-of-range IDs, mismatched ownership, Linux targets, and
unknown access bits fail before publication. Thread query/termination and
handle lifetime remain owned by the existing canonical thread object path.
Windows NT frontier update (2026-09-02): named timer creation and opening
now reuse canonical scheduler-backed timer identity with collision,
type-mismatch, and missing-parent status handling; arming and cancellation
remain owned by the native monotonic timer primitive.
`NtPrivilegeCheck` is implemented against the canonical NT token privilege
owner, including enabled-privilege matching and all-required semantics.
The graph now reaches `ntdll.dll!NtPulseEvent`; native transient pulse
generation, wakeup, and manual/synchronization consumer semantics are now
implemented by the NT event owner.
`NtOpenKey` and `NtOpenKeyEx` now resolve typed key handles through the
canonical userspace registry owner; options outside the supported zero form
remain fail-closed. The Notepad handoff stages and starts that same owner
before launching the PE image; no kernel registry shadow database exists.
Windows NT frontier update (2026-09-02): named mutant creation and opening
now reuse canonical scheduler-backed mutant identity with collision,
type-mismatch, and missing-parent status handling; recursive ownership and
release remain owned by the native mutant primitive.
`NtQueryFullAttributesFile` now translates the Wine-shaped network-open
information record from canonical VFS metadata, including timestamps, size,
and regular-file or directory attributes.
`NtQueryInstallUILanguage` now returns the same en-US LANGID baseline as Wine's
system LCID fallback.
`NtQueryKey` is exposed at the NT boundary; registry key state remains owned by
the planned userspace registry service, as required by the Windows architecture
plan, rather than being duplicated inside the kernel.
`NtQueryPerformanceCounter` now shares the monotonic counter and 10 MHz
frequency owner used by the existing RTL performance-counter implementation.
`NtQuerySymbolicLinkObject` is exposed at the object-manager boundary; its
target data must come from the canonical NT symbolic-link namespace, not a
second interpretation of VFS symlinks.
`NtQuerySystemInformationEx` implements Wine's x86-64 supported-architecture
query with the native AMD64 record; other information classes remain explicit
until their owning kernel data source is available.
`NtQueryValueKey` is exposed for the userspace registry-service owner; the
kernel does not duplicate registry values or persistence state.
`NtQueryVolumeInformationFile` is exposed at the typed file boundary; its
implementation is pending a public VFS file-to-superblock accessor for the
canonical `statfs` owner.
`NtQueueApcThread` remains an explicit boundary until the scheduler owns a
Windows APC queue and x86-64 user APC return path; it must not be conflated
with Linux signal pending state.
`NtQueueApcThreadEx2` is exposed alongside the base APC boundary; reserve
handles, special APC flags, and callback delivery require that same owner.
`NtRaiseException` is exposed at the exception boundary; complete behavior
requires a canonical exception-record dispatcher and resumable x86-64 user
context owner, still absent from the current exception module.
`NtReadFileScatter` is exposed at the file boundary; segmented user-buffer
validation and completion require a scatter-read owner in the NT file adapter.
`NtReadVirtualMemory` is exposed at the process-memory boundary; cross-address-
space copying and partial-copy accounting require an address-space-aware owner.
`NtRemoveIoCompletionEx` now removes multiple packets through the canonical NT
completion-port queue; APC interruption remains dependent on the future APC
delivery owner.
`NtResetWriteWatch` now validates the current-process range before exposing the
honest `STATUS_NOT_IMPLEMENTED` result; per-page dirty/write-protect ownership
remains a VMM task shared by both architectures.
`NtResumeThread` now uses a task-owned saturating NT suspend-depth transition
and encodes the prior depth; `NtSuspendThread` now increments that same owner,
while scheduler safe-point consumption remains to be wired.
`NtSaveKey` now validates the NT personality and non-null key/file handles
before returning `STATUS_NOT_IMPLEMENTED`; a canonical registry-key object and
hive serializer over the VFS are still required.
`NtSetContextThread` now mutates the current x86_64 task's canonical syscall
frame for validated integer/control context fields; remote stopped-register
snapshots and non-x86 context owners remain unimplemented.
`NtSetInformationObject` now validates and stores the implemented
`ObjectHandleFlagInformation` class in the canonical handle table;
protect-from-close is enforced by every user-close path. Cross-process handle
inheritance and the remaining object-information classes remain explicit.
`NtSetInformationToken` now owns TokenSessionId mutation in the canonical
token object; default-DACL serialization and integrity-level SID ownership
remain explicit gaps.
`NtSetInformationVirtualMemory` now exposes the prefetch class boundary with
argument validation; VMM prefetch and per-page write-watch ownership remain
unimplemented.
`RtlCreateTimer` and `RtlCreateTimerQueue` validate output and callback
boundaries; callback dispatch, cancellation, and queue lifetime remain
unimplemented.
`RtlCreateUserStack` validates stack sizing and the INITIAL_TEB output boundary;
VMM allocation, guard-page policy, and NT-thread teardown remain unimplemented.
`RtlDeactivateActivationContext` and `RtlReleaseActivationContext` validate
their opaque handles/cookies; activation-frame and reference-count ownership
remain unimplemented.
`RtlDeleteAce` validates and compacts variable-sized caller-owned ACL entries;
concurrent ACL mutation remains outside the current native ownership model.
`RtlDeleteBarrier` preserves the reference no-op destruction boundary; barrier
waiter state and cancellation ownership remain unimplemented.
`RtlDeleteSecurityObject` releases descriptors through the canonical NT heap
owner; invalid foreign allocations are not yet reported with native fidelity.
`RtlDeleteTimer` and `RtlDeleteTimerQueueEx` validate queue/timer boundaries;
callback completion and timer-queue ownership remain unimplemented.
`RtlDeregisterWaitEx` removes the canonical wait record; completion-event
signaling remains unimplemented when a non-null event is supplied.
`RtlDeriveCapabilitySidsFromName` derives the reference UTF-16/SHA-256 SID
layouts for ASCII case folding; full Unicode uppercase mapping remains absent.
`RtlDestroyEnvironment` releases the environment block through the canonical
NT heap owner; invalid foreign allocations are not yet reported natively.
`RtlDestroyProcessParameters` releases the process-parameter block through the
canonical NT heap owner; parameter-record validation remains absent.
`RtlDoesFileExists_U` performs bounded UTF-16 conversion and queries the
canonical VFS resolver; full Windows path-device translation remains absent.
`RtlDosSearchPath_U` searches semicolon-separated paths through the canonical
VFS resolver and writes bounded UTF-16 output; full DOS search/current-directory
semantics remain absent.
`RtlDowncaseUnicodeChar` follows the reference ASCII fallback; full NLS
lowercase-table mapping is not yet connected.
`RtlCreateProcessParametersEx` validates its result-pointer boundary; native
string capture, environment ownership, normalization, and destruction remain
unimplemented.
`RtlCreateEnvironment` validates the native output-pointer boundary; inherited
environment cloning and NT environment-block allocation/lifetime remain
unimplemented.
`RtlCopySid` validates the native SID header and destination capacity, then
copies the bounded SID through the focused SID owner.
`RtlCreateActivationContext` validates the native `ACTCTXW` header and flag
mask; manifest parsing and activation-context object ownership remain absent.
`NtSetSystemInformation` accepts the Wine-compatible time-adjustment shape as
an explicit no-op; host clock ownership and privilege enforcement remain in
the kernel timekeeper/security layers.
`NtSetSystemTime` validates the NT personality and input pointers before an
explicit unsupported result; realtime offset ownership and privilege mapping
still need to be connected to the canonical timekeeper.
`NtSetValueKey` now exposes and validates the native six-argument ABI without
confusing it with the internal registry request record; canonical key/value
storage and VFS persistence remain unimplemented.
`NtUnloadKey` now validates its native `OBJECT_ATTRIBUTES` pointer boundary;
registry hive namespace and persistence ownership remain unimplemented.
`NtUnlockVirtualMemory` now rounds the current-process range and releases the
canonical VMA/PMM lock state; remote-process APC delivery remains unimplemented.
`NtUnmapViewOfSectionEx` supports the documented zero-flags current-process
path through the canonical VMA unmapper; extended flags and remote processes
remain unimplemented.
`NtWriteFileGather` validates its native I/O-status and segment-array pointer
shape; scatter/gather segment copying and file-owner integration remain
unimplemented.
`NtWriteVirtualMemory` validates native process/source/count/output pointers;
target address-space ownership and safe partial-copy reporting remain
unimplemented.
`NtYieldExecution` now routes through the canonical scheduler-yield owner for
the NT personality; the precise `STATUS_NO_YIELD_PERFORMED` distinction is not
yet surfaced.
`RtlActivateActivationContext` validates the native flags, opaque context, and
cookie-pointer boundary; per-thread activation-context stack and cookie
ownership remain unimplemented.
`RtlActivateActivationContextEx` validates the native TEB, opaque activation
context, and cookie-pointer boundary; TEB stack linking and cookie ownership
remain unimplemented.
`RtlAddAccessAllowedObjectAce` is cataloged with the reference implementation's
explicit unsupported result; canonical ACL mutation remains unimplemented.
`RtlAddAccessDeniedObjectAce` is cataloged with the reference implementation's
explicit unsupported result; canonical ACL mutation remains unimplemented.
`RtlAddAuditAccessObjectAce` is cataloged with the reference implementation's
explicit unsupported result; canonical ACL mutation remains unimplemented.
`RtlAddMandatoryAce` validates the mandatory-label ACE type and flag mask at
the NT security owner; self-relative ACL insertion remains unimplemented.
`RtlAddRefActivationContext` is cataloged as a void native boundary; the
activation-context object and its reference-count owner remain in userspace.
`RtlAllocateAndInitializeSid` allocates through the canonical NT heap owner and
writes the native SID layout; SID validation/free-lifetime edge cases remain.
`RtlAreAllAccessesGranted` implements the reference bitmask predicate in the
RTL owner; generic-access mapping remains the caller's responsibility.
`RtlAreAnyAccessesGranted` implements the companion reference bitmask
predicate; generic-access mapping remains the caller's responsibility.
`RtlBarrier` remains an explicit unsupported userspace synchronization boundary;
wait-on-address ownership and safe caller-memory barrier state are not present.
`RtlClearBits` mutates the caller-owned RTL bitmap with reference-compatible
range no-op behavior; atomicity across concurrent bitmap users remains absent.
`RtlCompactHeap` preserves the reference's harmless zero-byte compaction stub;
the canonical NT heap has no compaction operation to expose.
`RtlCompareUnicodeStrings` compares bounded UTF-16 code units with the
reference ASCII fallback for case-insensitive calls; full NLS case mapping is
not yet connected.
`RtlConvertToAutoInheritSecurityObject` preserves the reference implementation's
explicit `STATUS_NOT_IMPLEMENTED` result; auto-inheritance descriptor ownership
remains unimplemented.
`RtlCopyContext` implements the reference AMD64 flag validation and selected
legacy-context copy ranges; extended XSTATE and i386 context support remain
unimplemented.
`RtlEqualPrefixSid` validates native SID layouts and compares the prefix through
the final shared subauthority, preserving the reference count requirement.
`RtlEqualSid` validates native SID layouts and compares the complete SID byte
representation, including every subauthority.
