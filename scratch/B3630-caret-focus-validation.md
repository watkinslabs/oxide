# Scrollbar focus and canonical gray caret

Status: source, hosted and both-architecture build validation complete; full Notepad acceptance open. Branch: B3630-paint-region-collapse.

- KI-0398 gray bitmap1 requests now use a pattern stored in the existing queue caret and its committed transitions. Raw creation reaches the same live owner as control focus. The compositor receives its existing RGB XOR snapshot: zero at bitmap (0,0), alternating inversion horizontally/vertically, anchored to bitmap origin across movement. Hidden snapshots carry no mask. Custom bitmap handles remain unsupported; no solid substitution.
- Signed caret dimensions stay in queue geometry; their absolute bitmap extent is validated before mutation. Equal signed source/destination axes clip a negative bitmap axis at source zero to one drawable pixel. Zero dimensions select the border extent. Publisher clips that mask without changing client caret coordinates. This covers thumb_size−2 when a small/disabled scrollbar has no thumb.
- KI-0885 WM_SETFOCUS creates gray caret, sets its client position, then shows it. WM_KILLFOCUS hides, invalidates the thumb without erase, then destroys it. Both return0; individual caret operation failures do not stop subsequent operations. Shared canonical geometry serves paint records and focus; drawing visibility does not suppress geometry queries.
- Added explicit Cargo target nt-window-caret-publisher, previously an unlisted test file. Actual raw gray route RED returnedSome0; `/tmp/B3630-caret-pattern-red.log`. Actual control focus route RED returnedSTATUS_NOT_IMPLEMENTED; `/tmp/B3630-control-focus-red.log`. Fixture compile error corrected before obtaining this RED; a first expected thumb size assumed17 while configured metric is16, corrected to14-pixel caret/16-pixel thumb after inspecting geometry inputs.
- Scroll fixture now compiles real caret live owner, publication, transition and geometry code; only scheduler/transport are instrumented. Tests retain parent Send/callback seams.56 scrollbar tests and10 caret publisher tests pass, including both orientations, small/disabled thumbs, zero extent, signed axes, real XOR masks, client offsets, movement, hide/show, blink, replacement and invalid request preservation.
- Positive controls compile copied production publisher/procedure into test-only module paths while kernel builds read untouched production files. Wrong pattern selection and missing focus dispatch hook both produce assertion failures, exit101: `/tmp/B3630-{gray,focus}-positive-control.log`. Fixtures restored in finally; final suite log `/tmp/B3630-control-focus-restored.log`.
- Full IPC1532/syscalls3270 tests pass. Adjacent redraw/raw paint44/9/29 pass. Logs `/tmp/B3630-control-focus-{ipc,syscalls,boundaries}.log`. No new VM, no Wine PE drawing thunk execution or desktop acceptance claim from hosted tests. Caret mask transport is the existing wire format; backend raster implementation unchanged.
- Remaining KI-0885: pointer tracking, state setters/accessibility and final dialog acceptance. KI-0398 custom bitmap mask copy/lifetime remains; KI-0904 EnableWindow notifications remain. Stripe mechanism remains unproved. Build/stack evidence below.

- Both release builds and debug feature checks pass: `/tmp/B3630-control-focus-{build,feature}.log`. Initial feature check found the uniform wrapper had lost its production caller; raw solid creation retains that caller, gray creation uses the typed pattern entry. No dead-code suppression. Both final frame gates pass; static primary rows335x86/278ARM, no added/increased path against refresh baseline. Existing KI-0019 stack/exception failures remain, no new exception. Logs `/tmp/B3630-control-focus-{frame,stack}-{x86,arm}.log`, comparison `/tmp/B3630-control-focus-compare.log`.
- ELFs `target/B3630-control-focus-x86_64.elf` SHA256 `ca1c74a5b3c1f48843884621a8194dd8d3f378f6f03ecce4301d0f6c978b0ec0`; `target/B3630-control-focus-aarch64.elf` SHA256 `ab3d67af4313ff7fc42aa4648f02d115a840ec69fad227f9ed4b679e3c7a5acb`.
