# B1968 — mm/vmm PARTIAL rows (323, 329, 330, 331, 447, 453)

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| FIXED B1968 | MISSING | med | There was no execute-only protection key. A plain `mprotect(PROT_EXEC)` left the mapping on the default key, so on hardware whose rights register is live a PROT_EXEC-only mapping stayed readable — the one thing the key exists to prevent. The key is now allocated lazily on the first such call, denied in the rights register, stored in the mm, and given back when the mapping stops being execute-only. | `vmm::pkeys::exec_only::tests` (13 tests); positive control: making the arming unconditional reddens `the_first_execute_only_mapping_allocates_and_arms_one_key` | — |
| FIXED B1968 | DEFECT | med | `mm_pkey_is_allocated` consulted an IMMUTABLE per-arch descriptor field for the execute-only key, so once a key was dedicated the admission test would still have reported it free — a program could have freed or re-keyed the mapping its own execute-only pages depend on. The live value now lives in the mm beside the allocation map, where the lazy dedication writes it. | `vmm::pkeys::exec_only::tests::the_execute_only_key_is_invisible_to_every_user_interface`; `vmm::pkeys::tests::a_fresh_context_starts_with_no_execute_only_key_dedicated` | — |
| FIXED B1968 | MISSING | med | `arch_vma_access_permitted` did not exist. The per-key decode helpers had no consumer outside their own unit tests, so a protection key was enforced only where the hardware faults — every access that reaches a user mapping WITHOUT faulting had no key check at all. Added as an ungated ladder with the two arch shapes distinguished (one arch never denies an instruction fetch, the other does), and wired to the paths that pin or discard pages. | `vmm::pkeys::access::tests` (6 tests); `vmm::tests_pkey_consumers` (4 tests); positive control: dropping the instruction-fetch term reddens `instruction_fetch_is_key_checked_on_one_arch_and_not_the_other`, and removing the consumer call reddens two `tests_pkey_consumers` tests | — |
| FIXED B1968 | DEFECT | low | A page pinned on the caller's own behalf (the mempolicy node lookup) checked the mapping's read permission but not its protection key, where the reference refuses both with EFAULT. | `vmm::tests_pkey_consumers::a_pin_of_a_key_denied_page_is_refused_even_though_the_vma_is_readable` | — |
| FIXED B1968 | DEFECT | low | A destructive `madvise` on a SEALED anonymous mapping was permitted whenever the mapping carried write permission, ignoring whether the caller's protection key still allowed that write. The reference permits it only when the caller could write through the mapping *for real*. | `syscalls::s028_madvise::tests::a_key_that_denies_writes_makes_a_sealed_discard_eperm` | — |
| CORRECTED B1968 | INFRA | — | Matrix row `453 map_shadow_stack` recorded a claim worth re-checking: that with no control-flow enforcement the single EOPNOTSUPP answer is the whole contract. Verified on BOTH arches — each arch's shadow-stack allocator opens with a hardware-support test returning EOPNOTSUPP, and slot 453 is in the unified number range so aarch64 reaches the same body. The claim holds; the row is complete. | `syscall::arm_abi::tests::unified_range_is_identity` pins the aarch64 routing | — |
| CORRECTED B1968 | DEFECT | med | Matrix row `447 memfd_secret` claimed the ENOSYS answer was "exactly what the reference gives on such a config" because the linear map is not page-granular. That is wrong for one of the two arches: it does not gate on the linear map's granularity at all — it is unconditionally capable, because its change-page-attribute path SPLITS a large linear-map leaf on demand. Only the other arch has a real capability gate, and there it reflects a boot-time choice with no split fallback. The oxide predicate modelled the wrong thing. | see the B1968 lane's secretmem work | — |
| FIXED B1968 | MISSING | high | `memfd_secret(2)` had no mapping-time policy: a mapping that is not shared was accepted (there is no private copy to make of a page absent from the kernel's linear map), the mapping was not locked or excluded from core dumps, and it was not charged against the memlock limit. The mapping path now refuses a non-shared mapping with EINVAL and imposes the file's own VMA properties, which is also what makes the memlock ladder charge it. | `syscalls::secretmem::tests::{a_private_mapping_of_secret_memory_is_refused, a_shared_mapping_is_locked_and_never_dumped_whether_asked_for_or_not}`; positive control: making the decision unconditional reddens both | — |
| FIXED B1968 | MISSING | high | A secret-memory page could be PINNED on a caller's behalf and could be UNLOCKED, either of which defeats the point of removing it from the kernel's linear map. Both are now refused, keyed off a VMA marker set only by the mapping path — the same shape this kernel already uses where the reference compares an operations table. | `vmm::tests_pkey_consumers::{a_pin_of_secret_memory_is_refused_however_permissive_the_key_is, a_secret_memory_mapping_can_never_be_unlocked}`; positive controls: removing each refusal reddens its own test | — |
