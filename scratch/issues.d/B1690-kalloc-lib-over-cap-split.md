# B1690-kalloc-lib-over-cap-split

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| FIXED B1690 | low | `crates/shared/kalloc/src/lib.rs` was 1202 lines, over the 1000-line hard cap (`docs/08§7`) and the only `len/over-cap` finding in the tree. Filed OPEN by C251 (`scratch/issues.d/C251-xtask-stats-kernel-metrics.md`); no lane had acted on it. | B1690. Pure move into 11 ownership-named modules; `lib.rs` is now an 87-line manifest. `make lint` kalloc rows are a strict subset of `origin/main`'s: `len/over-cap` gone, 4 pre-existing `pub-fn-complexity` rows fixed, none added. Public API proven identical by rustdoc-JSON symbol diff (65 symbols, `debug-heappoison,debug-heapwalk,debug-dealloc-diag,debug-hw-watchpoint,debug-efence`). `cargo test -p kalloc` 18 passed before and after. `make hosted-gate` 117 crates PASS, `make feature-gate` green both arches, `make smoke-x86` PASS 72s attempt 1, `make smoke-arm` PASS 84s attempt 1. | B1690 |
| OPEN | low | `crates/shared/kalloc/src/holes.rs` (933) and `src/tests.rs` (651) are over the 500-line split cutoff and were left unsplit by this lane. Both are under the 1000 hard cap. `holes.rs` is the free-list data structure under live corruption-diagnostic work; splitting it is a separate lane's call. | B1690. `wc -l crates/shared/kalloc/src/*.rs`: holes.rs 933, tests.rs 651; every other file ≤ 284. | — |
| OPEN | med | `cargo test -p kalloc --features debug-heappoison,debug-dealloc-diag,...` aborts with a host **stack overflow** (`fatal runtime error: stack overflow`) before finishing the 19 tests — `poison::Quar` and the size ledger are large enough that a `KAlloc` built on the test thread's stack blows it. Identical on `origin/main`, so pre-existing, not caused by the split — but it means the whole diagnostic-feature configuration of kalloc has **no** hosted test coverage. | B1690. Base worktree at `origin/main` (2ca827e72) and this branch both abort with SIGABRT on the same command; default-feature run is 18/18 green on both. | — |
| OPEN | low | `cargo test -p fs --test sys_pipe2_shape::pollout_tracks_current_pipe_capacity` fails on `origin/main` — `make test` stops there, so the workspace suite cannot go green today. | B1690. Reproduced identically in a detached `origin/main` worktree and on this branch; failure set of `cargo test --workspace --no-fail-fast` on this branch (`fs`) is a subset of base's (`fs`, `drv`). | — |
| OPEN | low | `drv-zram::tests::control_reuses_removed_index` and `drv --lib` fail only under the parallel full-workspace run; `cargo test -p drv-zram` alone is 104/104 green 3/3 runs. Shared global driver-registry state makes these order-dependent, so any lane reading a full `make test` can be handed a failure that is not theirs. | B1690. Branch: FAILED under `make test`, then 3/3 PASS in isolation and PASS with `--test-threads=1`; base `origin/main` shows the same `drv --lib` failure under `cargo test --workspace`. | — |
| OPEN | low | `/tmp` (32 GiB tmpfs) reached 100% mid-session and every `Bash` call failed with ENOSPC until space was freed. Multi-GiB `*_repro.img` boot images from several lanes accumulate there and nothing reaps them; `tools/boot-smoke.sh` hardcodes `/tmp` for its log/pidfile and has no fallback. | B1690. `df -h /tmp` read `32G 32G 4.1M 100%`; `du -sh /tmp/*` showed four 5+ GiB repro images plus 6.7 GiB under the session scratchpad. Freed by deleting two stale multi-GiB images from this session's own scratchpad. | — |
