# B1749 — /proc/net/netlink

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| FIXED B1749 | MISSING | med | `/proc/net/netlink` was a header-only static stub listing no sockets. The reference reports one line per live socket, and its `Rmem` column is the only observable outside the kernel that separates a notification the kernel never delivered from one the process never read. Three investigations into a service that appeared to ignore its rtnetlink notifications had nothing to measure. | `crates/kernel/netlink/src/proc_seq.rs` (4 hosted tests over the rendering) + `ports::proc_rows`; `/proc/net/netlink` now lists the reading namespace's sockets | — |
| OPEN | MISSING | low | `Wmem` and `Dump` are reported as zero. The reference reports `sk_wmem_alloc` and `cb_running`; neither has a source here yet — netlink sends are synchronous, and dumps are built and enqueued in one call rather than continued across reads. | `proc_rows` | — |
| OPEN | MISSING | low | `/proc/net/packet` is still a header-only stub with the same shape of defect: `ss -0` and `packet(7)` diagnostics list nothing. | `crates/kernel/procfs/src/static_files.rs` | — |
| OPEN | DEFECT | med | `net::netdev::set_iface_carrier` has no caller. Carrier is fixed at `true` from registration, so a virtio-net link that negotiated `VIRTIO_NET_F_STATUS` never reports a real carrier transition — `IFF_RUNNING`/`IFF_LOWER_UP` are derived from a constant. The driver negotiates the feature (`drv-virtio-net/src/modern.rs`) and then never reads the status field. | `grep -rn set_iface_carrier` finds only the definition | — |
