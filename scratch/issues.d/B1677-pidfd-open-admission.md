# B1677 — pidfd_open admission

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| NOT-A-DEFECT B1677 | med | Curated `## Desktop` row "`polkitd` spins re-issuing syscall 434 (`pidfd_open`) and taking EINVAL every ~200-400 ms" is closed — the argument is `pid = 0`, and the reference rejects a non-positive pid with EINVAL before any lookup, exactly as we do. The caller is not retrying because an earlier answer was wrong: its own `set_pid` path opens the descriptor from the object's *stored* pid field rather than the pid being assigned, so on a freshly constructed subject that field is still 0. EINVAL is the branch that makes it fall back to storing the numeric pid, which is why the sequence repeats per request and is non-fatal. No flag, leader, or exit-race component: the flags word is 0 and no lookup is reached. | B1663 boot log, every line identical: `[EINVAL nr=434 tid=4322 a0=0 a1=0 ... comm=polkitd]` (56x) and the same shape twice from `gnome-shell`; `a0` is `pid`, `a1` is `flags`. Guest `libpolkit-gobject-1.so.0.0.0` disassembly, `polkit_unix_process_set_pid`: the `syscall(434, ...)` site loads its pid argument from the object field, not from the incoming parameter. Admission now pinned by `pidfd::admit::tests` (4 tests). | — |
| FIXED B1677 | low | `pidfd_open` rejected a flags word whose high 32 bits were set, where the reference narrows to `unsigned int` first and ignores them (same class as the `timerfd` high-word narrowing already pinned in `fs::timerfd`). The pid argument likewise now narrows to `pid_t` before the sign test. Both decisions moved out of the target-gated slot file into ungated `pidfd::admit`, so they are testable at all. | `pidfd::admit::tests::both_arguments_narrow_to_their_c_widths_before_the_checks`; `cargo test -p pidfd` 0 -> 4 tests. | — |
| OPEN | low | `crates/kernel/pidfd/src/tests.rs` (6 hosted tests) **does not compile** — two `E0308`/`E0432` errors against the current `Task::replace_namespace` signature. Nothing in the default workspace gate enables `pidfd/hosted`, so the breakage is invisible: `cargo test -p pidfd --features hosted` fails to build while `cargo test --workspace` reports success. Same class as the phantom-test rule, one level up: the tests are real, the feature that compiles them is never turned on. Left to the lane that owns the hosted-feature gate rather than duplicated here. | `cargo test -p pidfd --features hosted` → `error[E0308]: mismatched types ... expected NamespaceRef, found Arc<_, _>` at `tests.rs:20`. | `B1674-hosted-check-gate` |
