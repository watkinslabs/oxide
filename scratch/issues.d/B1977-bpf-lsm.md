# B1977 — BPF LSM loadability

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| FIXED | MISSING | high | `bpf_attr.prog_load` carried no `attach_btf_id` offset, so `BPF_PROG_LOAD` never read the attach target a program names. | `bpf/uapi/off.rs::prog_load::ATTACH_BTF_ID`; ladder in `bpf/attr/prog_cmd.rs::prog_load_check_attach`, pinned by `bpf::attr::tests::{only_a_program_type_that_attaches_to_a_target_may_name_one, an_attach_target_above_the_type_id_ceiling_is_refused}` | B1977 |
| FIXED | MISSING | high | `BpfProgInode` carried no attach target, so no LSM link could resolve a hook and the `file_open` chain had no registrant. | `BpfProgInode::attach_btf_id`, read by `bpf/prog/attach.rs::attach_target_hook` | B1977 |
| FIXED | MISSING | med | `BPF_PROG_TYPE_LSM` was absent from `attr::prog_type_supported`. | `bpf::attr::tests::lsm_programs_are_loadable_and_need_both_bpf_and_perfmon` | B1977 |
| FIXED | COVERAGE | med | `read_license` read the license and discarded it, so no program type could refuse a load released under other terms. | `bpf/prog.rs::gpl_compatible`, pinned by `prog::load_tests::{the_kernels_own_terms_are_the_ones_a_hook_accepts, an_lsm_program_under_other_terms_is_refused}` | B1977 |
| OPEN | MISSING | high | The kernel's own type information is not exposed to userspace, so a loader cannot discover a hook stub's type id and cannot name an attach target. The load path resolves ids correctly; nothing serves them. | `bpf/btf/kernel.rs::published` builds and validates the object; no sysfs binary attribute for `/sys/kernel/btf/vmlinux`. | unowned |
| OPEN | MISSING | med | A BTF-typed hook argument cannot be followed. The verifier admits reading an argument slot and refuses every load through the loaded value, so a program that dereferences its argument is rejected where the reference would type-check the field access. | `bpf_verify/program/tests/lsm.rs::following_the_argument_pointer_is_refused` | unowned |
| OPEN | MISSING | med | `attach_btf_obj_fd` / `attach_prog_fd` is decoded by no one. The reference distinguishes a target program fd (`dst_prog`, TRACING/EXT only), a kernel-BTF object fd, and a user-BTF object fd (`-ENOTSUPP`); here a nonzero value is ignored instead of judged. | `bpf/attr/prog_cmd.rs::prog_load_check_attach` consults only `ATTACH_BTF_ID`. | unowned |
| FIXED | DEFECT | med | A context access the program's type does not admit answered `-EINVAL` where the reference answers `-EACCES`; the program is well formed and the access is refused, which is a different answer from "this is not a program". Affected every verified program type, not only LSM. | `bpf/prog.rs::verify` errno map; `prog::load_tests::an_access_the_context_does_not_admit_is_a_refusal_not_a_malformed_program` | B1977 |
| OPEN | DEFECT | med | The rest of the verifier errno map is still collapsed to `-EINVAL`. The reference answers `-EACCES` for an unreadable register, an out-of-range stack access and an uninitialised stack read as well; only structural rejects (bad opcode, bad register number, out-of-range jump, non-exit tail) are `-EINVAL`. | `bpf/prog.rs::verify` maps every remaining `VerifyError` to `Errno::Einval`. | unowned |
| OPEN | MISSING | low | No disabled-hook blocklist. The reference refuses an attach target naming a hook stub the kernel deliberately withholds; here only implemented hooks are declared at all, so no id can name a withheld one. The blocklist becomes real the first time a declared hook is withdrawn. | `bpf_lsm/hooks.rs::HOOKS` declares exactly the hooks with call sites. | unowned |
| CLOSED | — | — | Earlier row claimed a hook argument slot handing the program a raw kernel address was a divergence. It is not: the reference's pointer-leak checks (`is_pointer_value`) are gated on `allow_ptr_leaks`, which is `CAP_PERFMON`, and loading an LSM program requires `CAP_PERFMON` — so both the "leaks addr as return value" and "leaks addr into map" refusals are disabled for every LSM program. Returning the argument is refused in both by the return-range check, not by a leak check; storing it into a map is admitted in both. Modelling the slot as a scalar loses nothing. | `bpf_verify/program/tests/lsm.rs::an_unbounded_exit_value_is_refused` pins the range refusal. | B1977 |
