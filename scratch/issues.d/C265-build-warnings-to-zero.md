# C265 — build warnings to zero

| Status | Severity | Issue | Evidence |
|---|---|---|---|
| OPEN | low | fanotify `FAN_AUDIT` verdicts record the daemon's audit-rule justification (`PermState::set_audit_rule`) but nothing emits it — this kernel has no audit log. `PermState::audit_rule()` is the read side with no consumer, kept under a narrow `#[allow(dead_code)]` with the reason stated at the item. | `crates/kernel/fs/src/inotify/types.rs` — `audit_rule()`; parse + validation covered by `inotify_fan_tests.rs` |
| OPEN | low | `HelperArgs::key` / `::authkey` exist for the `Upcall` actor contract (an in-kernel key type answering its own `request_key`), but the only production actor is `/sbin/request-key`, which reads them out of the prebuilt `argv`. No in-kernel key type implements `key_type->request_key` yet. | `crates/kernel/fs/src/keyring/construct/upcall.rs` |
| OPEN | info | `kalloc::sizeclass::MAX_CLASS_BYTES` has no reader in diagnostic builds: `debug-heappoison` / `debug-dealloc-diag` / `debug-hw-watchpoint` / `debug-efence` replace `class_index` with a stub that routes nothing, so the routing bound is unused there. Now derived from `CLASS_SIZES` so the two cannot drift. | `crates/shared/kalloc/src/sizeclass.rs` |
| FIXED C265 | low | `syscalls::net_common` re-exported `net_errno::errno_from_neterr`, so 42 call sites reached one function by two paths and the re-export read as dead on hosted builds. All call sites now import from the owner. | `crates/kernel/syscalls/src/net_common.rs` |
| FIXED C265 | low | `watch_queue::pipe_ops::queue` duplicated `registry::queue_of` (never re-exported, no callers). Deleted. | `crates/kernel/fs/src/watch_queue/pipe_ops.rs` |
| FIXED C265 | low | `keyring::perm::visible_for_search` collapsed validity+permission into a bool, discarding the skip reason the search path exists to preserve (negative-key caching). No callers; wiring it would have been a defect. Deleted. | `crates/kernel/fs/src/keyring/perm.rs` |
| FIXED C265 | low | `keyring::perm::Possess::Computed` was never constructed — computed possession is what `check_perm` already does; `check_perm_with` exists only for the `Yes`/`No` cases. Deleted. | `crates/kernel/fs/src/keyring/perm.rs` |
| FIXED C265 | low | `keyring::ops::Ctx::new` was a second constructor used only by tests, and it silently implied `set_uid = false`. Deleted; tests call `with_caps` explicitly. | `crates/kernel/fs/src/keyring/ops.rs` |
| FIXED C265 | low | `ext4` `debug-fillverify` defined `fill_error_label` with no caller: the readahead fill collapses every `MountError` to `EIO` at one point, discarding the class. Wired there. | `crates/kernel/ext4/src/rootfs/framecache.rs` |
| FIXED C265 | low | `mm-pmm::arm_mprotect_trace` compiled on x86_64 under `debug-arm-mprotect`, where only `checkpoint` had a call site and the counters it reports are never incremented (every increment site is `target_arch = "aarch64"`). Module now aarch64-only; the x86 `as_teardown` checkpoint call (which could only ever print zeros) removed. | `crates/kernel/mm-pmm/src/lib.rs`, `user_as/teardown.rs` |
| FIXED C265 | low | `dump_arm_vmas` and its `use` were gated on `debug-displaystack` alone while the only call site is the aarch64 fault path. Both now `target_arch = "aarch64"` too. | `crates/kernel/mm-pmm/src/user_as.rs`, `user_as/debug.rs` |
| FIXED C265 | info | Two `unsafe` blocks wrapped the SAFE `hal::MmuOps::translate` trait method (the unsafe one is the inherent `X86Mmu::translate`). The blocks and their SAFETY prose asserted a guarantee the call did not need. | `crates/kernel/mm-pmm/src/user_as/fault.rs`, `crates/kernel/syscalls/src/024_sched_yield.rs` |
| OPEN | info | No routine gate reads build warnings. Every warning fixed here was reachable only by running `cargo build --workspace`, `xtask kernel --check` and `make feature-gate` by hand and reading the output. A `-D warnings` ratchet would hold this at zero; nothing does today. | measured baseline 1 / 7 / 7 / 19 / 13 warnings across the five configurations |
