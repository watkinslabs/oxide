# D476 — resolved scope, measured

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| CLOSED | — | — | Negative result — kernel-side rtnetlink delivery to resolved is correct and must not be re-investigated. resolved subscribes via `NETLINK_ADD_MEMBERSHIP` to groups 1/5/9 as its source does; every notification reaches it (`subscribed=2 reached=2`, 63/63) and is read rather than queued (`/proc/net/netlink`: `Rmem=0 Drops=0`); `ip monitor` decodes the right ifindex, global scope, `UP,LOWER_UP` and operstate up — every field `link_relevant()` gates on. | C272 trace + B1749 `/proc/net/netlink` | — |
| CLOSED | — | — | Negative result — the link is not networkd-managed, so `link_relevant()`'s `is_managed` branch is inert: there is no `systemd-networkd.service` in the image and NetworkManager is the enabled manager. | `debugfs -R "ls -l /etc/systemd/system/multi-user.target.wants"` | — |
| CLOSED | — | — | Negative result — this was a pre-`NETLINK_PKTINFO` trace plus an ANSI-sensitive serial parser, not a current D-Bus wakeup defect. On current main, the UART-only `guest-resolved-check.py` found `systemd-resolved` active, `Current Scopes: DNS LLMNR/IPv4`, five successful `org.freedesktop.DBus.Peer.Ping` calls, and a successful stub `getent ahostsv4 one.one.one.one`. It never uses guest SSH. | D479 x86 serial probe on `fccdafd8e`; the older two samples were `638d7f354` / `2b69e2921`. | — |
| OPEN | INFRA | high | Guest serial RX duplicates characters on long typed lines — `sleep 1` → `sleep 11`, `busctl` → `busscl`, `--system` → `--systemm`. Every console-driven probe must use short commands, and a corrupted command yields a false result rather than an error. This blocked the latency measurement above. | probe transcripts on `638d7f354` | — |
| OPEN | INFRA | med | `git add` aborts the entire staging operation on one stale pathspec, so a commit can silently contain none of the intended work while every gate passes in the worktree. That put a broken `main` on origin once this session (reverted in `f1ab81fb6`). A pre-commit check that the staged set matches the working set would catch it. | `git show --stat 8b2211aa6` = 1 file, 0 insertions | — |
