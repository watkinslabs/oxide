# B1971 — socket-setup and process-creation PARTIAL rows

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| FIXED 1782993eb | DEFECT | HIGH | `accept(2)`/`accept4(2)` allocated the child's descriptor LAST, so a caller with a full descriptor table had already popped a connection off the listener's accept queue before EMFILE was decided; the dequeued child was then dropped and the peer's established connection destroyed. Linux reserves the descriptor before `do_accept` runs, so EMFILE outranks ENOTSOCK/EOPNOTSUPP and leaves the queue untouched. | `syscalls::sock_route::accept_route` tests `an_exhausted_descriptor_table_refuses_before_the_protocol_is_consulted`, `a_protocol_refusal_returns_the_descriptor_it_reserved`, `a_bad_descriptor_or_a_bad_flag_word_reserves_nothing`. Positive control: reserving after the protocol screen turns 2 of them RED. | B1971 |
| FIXED 335e72190 | DEFECT | MED | `listen(2)` asked the security hook BEFORE clamping the backlog to `net.core.somaxconn`, so the hook saw the caller's raw request rather than the backlog the socket gets. Linux clamps first. | `net::listen_admit::listen_ladder` test `the_namespace_ceiling_is_applied_before_the_security_decision_sees_it`. Positive control: restoring the old order turns it RED. | B1971 |
| FIXED 335e72190 | DEFECT | LOW | `listen(2)` returned ENODEV when the namespace carried no `somaxconn` entry — an errno `listen(2)` cannot produce. The ceiling is a tunable that always has a value; it now falls back to the compiled default. | `a_namespace_with_no_recorded_ceiling_uses_the_compiled_default`. Positive control: reinstating the ENODEV arm turns it RED. | B1971 |
| FIXED 90613d3bb | INFRA | MED | `net::net_ns::test_support::allocate_namespace` could publish a child network namespace before any thread had materialized the initial one; the registry then aborted whichever thread reached the initial namespace, with a panic naming neither test. Latent since the helper was written, exposed by a third concurrent test in the same filter. | `cargo test -p net --lib accept_finalize` failed 2 runs in 4 before the fix, 4/4 green after. | B1971 |
| OPEN | DEFECT | LOW | AF_VSOCK and AF_NETLINK apply the generic bind/connect security hook INSIDE the family implementation, after some address-shape screening, where Linux runs `security_socket_bind`/`security_socket_connect` in the generic layer before any `->bind()`/`->connect()`. So `bind(vsock_fd, short_addr)` under a denying module reports EINVAL where Linux reports EACCES; likewise a short `sockaddr_nl`. The INET/INET6/UNIX/PACKET path is already correct (`049_bind.rs` calls `net::sock::admit_bind` before every family length screen). | `049_bind.rs` `require_sockaddr_vm` / `storage.vsock()` precede `vsock_socket::lifecycle::bind`'s `security_admission::check`; `syscalls::netlink_fd::bind` parses family and port before its check at `netlink_fd.rs:109`. Closing it needs one family-agnostic admission token so the hook has a single call site above the family branch. | unowned |
| OPEN | MISSING | MED | `clone(CLONE_SYSVSEM)` WITHOUT `CLONE_THREAD` gets its own empty semaphore undo list. Linux shares a refcounted `sem_undo_list` whenever `CLONE_SYSVSEM` is set, independent of `CLONE_THREAD`. oxide keys the list on the thread-group id, which reproduces glibc's usage exactly (pthread_create always passes the flag, fork never does) and nothing else. | `crates/kernel/ipc/src/sysv/sem/undo.rs` `ProcUndo { tgid, .. }`; `056_clone.rs` carries no `CLONE_SYSVSEM` handling. Closing it needs a shareable per-task owner, which lives in `sched` — reported rather than edited, B1967 owns that tree. | B1967 / ipc |
| OPEN | MISSING | LOW | The socket security context carries no backlog, so `listen(2)`'s hook cannot see the value it is deciding about — Linux passes the clamped backlog to `security_socket_listen`. The clamp now runs first (335e72190), so the value exists at the hook; only the context field is missing. | `security::network::Context::op(ns, family, 0, 0, Listen)` has no backlog operand. `crates/kernel/security/**` is B1969's tree. | B1969 |
| OPEN | MISSING | LOW | `sockaddr_in6.sin6_flowinfo` is copied into `SockaddrStorage` and never read by any accessor. | `net::sockaddr::SockaddrStorage::inet6` returns address, port and scope id only. | unowned |
| OPEN | COVERAGE | MED | The bind/connect sockaddr length and shape decisions (`move_sockaddr_to_kernel_shape`, `require_sockaddr_in`/`in6`/`vm`) live in `syscalls::net_sockaddr`, declared inside the target-gated `kernel_body.rs` module list, and carry no tests at all. The only "coverage" is a source-text grep asserting `049_bind.rs` names the function. These decisions are pure and belong in an ungated module, as `sockaddr_encode.rs` already does for the getname encoders. | Row 49 evidence column; `net_common.rs` grep-based assertions. | unowned |
| OPEN | COVERAGE | LOW | N25 (TCP connect wait linearization) and the N22 guest differential channel remain open in `scratch/network-plan.md`; rows 42/43/50/288 carry them as their last substantive item. N26 is all-but-closed — only its `SOL_VSOCK` option differential (N26.4) is open, so the rows' "N26 VSOCK wait linearization" wording is stale. | `scratch/network-plan.md:1648` (N25 unchecked), `:1695` (N26 partial, 7/8 sub-items done). | net |

## Stale matrix claims corrected in this branch (no work needed)

- Rows 292 `dup3`, 293 `pipe2`, 53 `socketpair`: every "Remaining" clause verified closed. `pipe2`'s "EBUSY reserved-slot race parity" and "signal restart details" describe behaviour `pipe2(2)` does not have in the reference at all — there is no EBUSY path and no restart path. `socketpair`'s "oxide creates first" clause contradicts the B1625 sentence two lines later; the code copies both descriptor numbers out before constructing anything.
- Rows 59 `execve`, 322 `execveat`: `execveat`'s closing sentence claims `bprm_fill_uid` and the `MAY_EXEC`/`path_noexec` gate reach only `AT_EXECVE_CHECK`. Both are on the live exec path through `execve_common::open_exec_image` and `exec_transition::decide`; the sentence predates the fix recorded earlier in the same row.
- Rows 42/43/49/50/288: the "security hook" remainder is stale for the INET path — `admit_connect`, `admit_bind`, `admit_listen` and `admit_accept` all exist, are ungated, are tested, and are called. Row 50's "INET/UNIX listen permission-deny coverage" is likewise stale (`net::sock::admission` tests cover both operations against AF_INET).
- Row 56: `PTRACE_EVENT_FORK/VFORK/CLONE` producers, `CLONE_PARENT` and `CLONE_IO` are all implemented; only the `CLONE_SYSVSEM` clause of that compound claim survives.
