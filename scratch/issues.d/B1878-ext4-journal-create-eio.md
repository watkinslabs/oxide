# B1878 ext4 journal-create EIO

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| IN-PROGRESS B1878-ext4-journal-create-eio | DEFECT | high | **A clean release Firefox boot intermittently returns `EIO` while journald creates `/var/log/journal/<machine-id>/user-1000.journal`.** Journald treats the file as corrupt or unclean, renames it, retries, and falls back to the system journal; the kernel emits a synchronous `[NAMEI]` failure record for every attempt. This is both a wrong userspace result and a sustained filesystem/serial workload during the Firefox acceptance run. | B1878 run `2746116` on merge `5d637d985`: repeated `openat-create ... user-1000.journal err=5` from 23.9 s through 106 s; `journalctl -b` reports no journal files and Firefox never presents a readable window. Seven subsequent cold boots pass both graphical phases, including three on the queue-dispatch fix (`2768474`, `2769008`, `2771882`), proving intermittency rather than retirement. Syscall cost remains Linux-class at 0.153–0.173 us/call. Future recurrence retains real UART plus `[EXT4-ERROR] kind=<source>`. | B1878-ext4-journal-create-eio |
| OPEN | DEFECT | high | **JBD2 write-side batching can silently omit two metadata blocks from a 512-block transaction.** A 4 KiB v1 descriptor has room for 510 eight-byte tags after its 12-byte header, but the batch ceiling admits 512 target blocks and `build_descriptor_block` stops when the descriptor fills while the writer still emits all 512 payload blocks. Replay also clears pending payloads on each descriptor, so merely splitting the writer without fixing replay would remain wrong. | Source capacity proof: `(4096 - 12) / 8 = 510`, `BATCH_MAX_BLOCKS = 512`; the root Firefox image has no journal, so this cannot explain run `2746116`, but a journaled mount can publish an incomplete recovery transaction. | — |
