# B1948 — fsmount/open_tree namespace form: root shape, propagation, OPEN_TREE_NAMESPACE

Closes the four OPEN rows B1941 left in
`scratch/issues.d/B1941-fsconfig-fsmount-linux-parity.md` (that file is B1941's
and is not edited here). One row per closure, plus what this lane found.

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| FIXED B1948 | MISSING | Med | The named namespace `fsmount(FSMOUNT_NAMESPACE)` built had the new mount as its ROOT. The reference instead copies the caller's namespace root and mounts the new tree ON TOP of that copy, so a caller that unmounts the top still has a root underneath. (B1941 row 8.) | `vfs::mount::create_new_namespace` copies the caller's ns root (`copy_ns_root`) and stacks the tree on it. `vfs/tests/mount_fsmount_namespace.rs::the_namespace_is_rooted_on_a_copy_of_the_callers_root_with_the_mount_on_top`, `::unmounting_the_top_leaves_the_root_copy_underneath`, `::the_namespace_and_every_mount_in_it_die_with_the_caller_s_last_reference`. Positive control: re-pointing `ns_set_root` at the top mount turns 5 of 11 RED. | B1948 |
| FIXED B1948 | MISSING | Med | The slave-propagation transition for a caller whose user namespace differs from its mount namespace's owner was not applied — only the locked-attribute stamp was. (B1941 row 9.) | Both nodes of the new tree are cloned/stamped `Propagation::Slave` on that condition, and `locked::lock_mnt_ns` freezes the tree with the namespace root excluded. `vfs/tests/mount_fsmount_namespace.rs::a_cross_user_namespace_caller_gets_slave_copies_and_a_frozen_tree` + `::a_same_user_namespace_caller_gets_private_copies_and_no_freeze`. Positive controls: forcing `CloneType::Private` RED; removing the `lock_mnt_ns` call RED. | B1948 |
| FIXED B1948 | MISSING | Med | `open_tree(2)` had no `OPEN_TREE_NAMESPACE`: the bit was unassigned, so every use was EINVAL. (B1941 row 10.) | `open_tree_policy::OPEN_TREE_NAMESPACE` + the two new flag rules (AT_RECURSIVE is satisfied by either tree-copying flag; CLONE and NAMESPACE together are EINVAL) + `required_privilege`/`admit_privilege`; `428_open_tree.rs` namespace arm calls the SAME `vfs::mount::create_new_namespace`. Tests: `open_tree_policy` unit tests 12 -> 17; `vfs/tests/mount_fsmount_namespace.rs::the_open_tree_form_puts_a_copy_of_an_existing_tree_on_the_same_root_copy`, `::an_unbindable_source_is_refused_by_the_open_tree_form`. Positive control: dropping the bit from `OPEN_TREE_VALID` turns 4 unit tests RED. | B1948 |
| FIXED B1948 | COVERAGE | High | `syscalls/tests/mount_{too_revealing_dispatch,param_admission_hosted,cgroup_v1_honest_hosted}.rs` reached `dispatch_mount` by `#[path]`-including the source into the test binary. The shipped module sat under `fsmount_common/mod.rs`'s `#![cfg(target_os = "oxide-kernel")]`, so `cargo test` compiled none of it and all three could pass while the module the kernel links was broken. | `mount_dispatch.rs` moved to the crate root and declared ungated (`syscalls::mount_dispatch`); the `#[path]` includes are gone and the tests call the LINKED module. `errno_from_vfs` likewise moved to an ungated `crate::vfs_errno`, which the kernel-only `namei_common` now re-exports rather than owning. POSITIVE CONTROL, pre-fix: deleting `namei_common`'s `errno_from_vfs` re-export made `xtask kernel --arch x86_64 --check` RED while all 21 tests in the three files stayed GREEN. Post-fix the same class is unreachable — the tests link the lib, and breaking either the linked errno table (4 RED) or the linked `mount_capable` rung (1 RED) turns them red. | B1948 |
| FIXED B1948 | DEFECT | Low | `fsmount(2)` applied `lock_new_mount_bits` to BOTH forms. The reference applies the cross-user-namespace freeze only inside the namespace constructor; the anonymous form's mount is destined for the caller's own tree and is frozen by the attach path, not here. | The call is gone from `432_fsmount.rs`; the freeze is `locked::lock_mnt_ns` inside `create_new_namespace`. `lock_new_mount_bits` had no other caller and is deleted (its test now pins `lock_bits_for`, the surviving pure half). | B1948 |
| OPEN | MISSING | Low | `create_new_namespace` refuses with EINVAL when the caller's mount namespace has no root mount. The reference cannot reach that state (its namespace root is a synthetic always-present mount); ours can, before the root filesystem is mounted. EINVAL is honest but is not a reference behaviour, because the reference has no such case. | `vfs/src/mount/new_namespace.rs`: `mntns::ns_root_id(caller.id()).and_then(mount_by_id).ok_or(VfsError::Einval)?`. No syscall can reach it — `fsmount`/`open_tree` both require a resolved path or a realized superblock, which implies a mounted root. | unowned |
| OPEN | COVERAGE | Low | `FsContext::log_messages()` and `take_log()` still have no callers outside tests (B1941 row 12, untouched by this lane). | `grep -rn "log_messages\|take_log" crates/` — every hit is in `vfs/tests/fs_context_log.rs`. | unowned |
| INFO | INFRA | Low | B1941's row 13 (`syscalls` lib test `return_fastpath_tests::syscall_process_irqs_close_before_return_work` failing on main) does NOT reproduce here: `cargo test -p syscalls` is 0-failed on this branch's base (`d118cba9e`). Either intermittent or fixed between `5506b785e` and `d118cba9e`. | Full `cargo test -p syscalls` on B1948: no failures. | unowned |
