# C263 — per-crate `cargo test --no-run` gate

| Status | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|
| FIXED C263 | low | Closes the curated row opening "**`hosted-check.sh` type-checks each crate with `cargo check -p X`, which does not build its TESTS.**" That row's sweep was a one-off run by hand; it is now `tools/test-build-check.sh`, wired as `make test-build-gate`, into `make ci`, and into `.githooks/pre-push` (gate 1b, bypass `SKIP_TEST_BUILD_GATE=1`). | Positive control on this branch: commenting out procfs's `[dev-dependencies] sched = { features = ["hosted"] }` makes `make test-build-gate` exit 2 naming `procfs` with `error[E0433]: cannot find 'live' in 'sched'`, while `make hosted-gate` in the SAME tree still reports `PASS — 103 crates type-check in isolation` — the gap, reproduced. Restored → `PASS — 103 crates build their test targets in isolation`. | C263 |
| FIXED C263 | low | Also closes the curated row opening "`hosted-gate` checks lib/bin targets only, not test targets (`--all-targets` costs 43 s warm against 12 s, too heavy for every push)." The cost premise held for `cargo check --workspace --all-targets`; it does not hold for the per-crate `cargo test --no-run` sweep, which is cheaper warm than the number that row rejected. | Measured, 48-core box, 24 jobs: **2.0 s** no-op (nothing changed), **25 s** after `touch crates/kernel/sched/src/lib.rs` (the widest-fan-in crate), **2 m 34 s** from a fully cold `CARGO_TARGET_DIR` (one-time; `make test` pays the same build). `hosted-gate`'s own comparable numbers are ~5 s no-op / ~12 s after a core crate. | C263 |
| OPEN | low | **The new gate uses each crate's DEFAULT features, so it does not cover a test target that only compiles with `--features hosted`.** The live instance is the curated row opening "`crates/kernel/pidfd/src/tests.rs` (6 hosted tests) **does not compile**", still red today and NOT fixed here (it is a one-line test fix plus a decision about whether the gate should sweep `hosted` for every crate that declares it — a second pass with its own cost, which this lane did not measure). | Re-confirmed on this branch: `cargo test --no-run -p pidfd --features hosted` → 2 × `error[E0308]` at `crates/kernel/pidfd/src/tests.rs:20` (`replace_namespace` takes `NamespaceRef`, the test passes `Arc::clone`). `cargo test --no-run -p pidfd` (default features) passes, which is why the new gate is green on it. | unclaimed |
| OPEN | low | The curated pidfd/test-build rows name `B1674-hosted-check-gate` as owner. **That lane does not exist** — no local branch, no remote branch, no worktree; only the merged hosted-`cargo check` gate it was named for. C263 took the test-build half on that basis. Whoever takes the `--features hosted` half above should not wait on B1674. | `git branch -a --list '*B1674*'` and `git worktree list` both empty for it. | unclaimed |
| OPEN | low | **`.github/workflows/pr.yml` does not run `hosted-gate` and now does not run `test-build-gate` either**; both are local-only (pre-push + `make ci`). Adding a CI job was considered and rejected here: the one hosted job that could host it (`test-hosted`) is disabled behind `if: vars.OXIDE_RUN_HOSTED_TESTS == '1'`, so a step added to it would be a gate nothing reads. Recorded so the gap is visible rather than assumed covered. | `.github/workflows/pr.yml`, `test-hosted` job condition. | unclaimed |
| OPEN | low | Neither `Makefile` nor `tools/**` (other than `tools/xtask/*`) is in `.githooks/pre-push`'s trigger path list, so a push that changes only the gates themselves runs none of them. Not widened here — the list is deliberately scoped to boot-visible surfaces — but it means a broken gate script can be pushed without the gate ever executing. | `.githooks/pre-push` `need_gate` case list. | unclaimed |
