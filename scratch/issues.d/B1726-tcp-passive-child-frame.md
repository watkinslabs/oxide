# B1726 — the deepest softirq frame, removed

| Status | Class | Sev | Issue | Evidence | Owner |
|---|---|---|---|---|---|
| FIXED (this PR) | DEFECT | high | **`build_passive_child` held 1816 bytes on the softirq stack — the deepest frame on the receive path — by building the child's connection state and the child itself as two live stack temporaries.** `TcpConn` is 584 bytes and `TcpEntry` 688; holding a fully-initialised `conn` local ACROSS the entry construction put both in one frame. That runs on the 16 KiB per-CPU hardirq stack whose measured peak is already ~14.5 KiB, so the receive chain `deliver_rx_in` (888) → `deliver_tcp_to_listener` (520) → `build_passive_child` (1816) spent 3224 bytes against ~1.5 KiB of headroom — which is why an arriving UART RX interrupt could cross the guard page and double-fault. The connection state lives behind the entry's own `Spinlock`, so the child is now allocated first and its fields written through that lock, which is also the order the reference uses: the child socket is allocated and then initialised, never assembled on the stack. | Measured from the built release kernel, before and after, with the same `objdump -d` + `sub $imm,%rsp` extraction B1725 recorded: `build_passive_child` **1816 → absent from the ≥256-byte list**; the chain drops 3224 → ~1408. `cargo test -p net` 1946 passed / 0 failed; `make smoke` PASS both arches. Guard added: `a_tcp_conn_is_small_enough_to_build_on_the_softirq_stack` fails if `TcpConn` + `TcpEntry` grows past 1536 bytes, so the frame cannot silently come back. | B1726 |
| FIXED 4905b1ebd | DEFECT | high | **The `#DF` is not proven closed.** This removes 1.8 KiB from the deepest receive chain, which is the largest single contributor measured, but the fault is intermittent (~50% with an early-typing probe) and no run count yet establishes it is gone. The remaining ranked frames are unchanged: `tick_poll_combined` 1096, `linux_irq_dispatch` 1032, `ipv6_control_tick` 952, `deliver_rx_in` 888, `arp_timer` 888. Next step is N sequential runs of the B1723 early-typing probe on this commit — clean/total, not one boot — before any claim that the overflow is closed. | B1725's ranking; this lane changed only the top entry. | unowned |
