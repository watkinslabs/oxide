# Control refresh and keyboard input

Status: source and hosted/build validation complete; KI-0885 full control behavior open. Branch: B3630-paint-region-collapse.

- Control EnableScrollBar now returns only after drawing callback completion; WM_ENABLE updates arrow flags and uses that refresh. Cached client DC acquisition uses existing GUI/GDI lease owners. Original API result and DC travel in existing paint callback Queue; no additional pending registry. Cached DC revocation remains canonical destruction policy; cleanup tolerates already-revoked handles.
- Shared control drawing record carries arrow/interior selection and canonical geometry/state. Hidden controls or hidden/minimized ancestors skip drawing; clipping stays with DC owner. WM_PAINT uses the same record builder and retains existing paint-session completion.
- Keyboard navigation sends parent H/V scroll messages and discards final parent LRESULT. Previous-state bit prevents repeated caret hides; keyup restores caret. Unknown/full-width keys cannot accidentally become recognized keys through truncation.
- RED actual EnableScrollBar route: returned1 rather than pending259; `/tmp/B3630-control-refresh-red.log`, exit101. Removing actual completion-router arm fails real refresh test; `/tmp/B3630-control-refresh-hook-red.log`, exit101. Keyboard actual procedure previously returned STATUS_NOT_IMPLEMENTED; `/tmp/B3630-control-keyboard-red.log`, exit101.
- All51 joined scroll tests pass; `/tmp/B3630-control-refresh-final2.log`. Tests include real canonical GUI/GDI lease state, dirty backing pixel retained after release, actual callback-completion routing, nesting, failed DC/callback installation, wrong-thread completion, cancellation/revocation, partial drawing selection, both keyboard orientations, unknown keys and Send continuation. Scheduler/usercopy/client callback execution, caret publication and transport are fixture seams; these tests do not execute the Wine user32 drawing thunk or establish visual acceptance.
- Remaining KI-0885: ordinary pointer tracking, focus-driven caret creation/destruction, setters and accessibility. KI-0904 EnableWindow callbacks remain distinct; this refresh repair does not claim those notifications. No new VM run.

- First release build passed both arches, but x86 static comparison found2 newly oversized completion paths (finish13264/resume13552) and16-byte teardown-path growth; ARM278 rows unchanged. Frame-by-frame report shows the terminal by-value completion frame survives into nested redraw. Terminal dispatch is now forced inline; HDC retention uses the active callback resource record instead of repeating a completion-variant match. Second build removes both new completion paths; two teardown paths still grow16 bytes. Disassembly confirms cancellation is inlined into teardown (frame544→560), retaining scratch storage through later publication. Cancellation now has a separate call frame that ends before window teardown; third build validated below. Logs `/tmp/B3630-control-refresh-{stack-x86,path-x86,stack-arm,build2}.log`.
- Full IPC1532 and syscall3270 tests pass; both feature checks pass. Second-build frame gates pass both arches; no new ARM stack rows or increases. Final build after cancellation frame separation passes both arches; `/tmp/B3630-control-refresh-build3-corrected.log` records exit0/0. Initial `cargo xtask` invocation failed before building (no alias); corrected invocation uses `cargo run -p xtask -- kernel`. Final debug feature checks pass `/tmp/B3630-control-refresh-separated-feature.log`.

- Final frame gates pass. Static multiset comparison against committed control-query ELFs: x86336→335 and ARM278→278 primary rows; no added or increased path. Existing KI-0019 stack failures remain; no new exception. Logs `/tmp/B3630-control-refresh-separated-{frame,stack}-{x86,arm}.log`, comparison `/tmp/B3630-control-refresh-separated-compare.log`.
- Snapshots `target/B3630-control-refresh-separated-x86_64.elf` SHA256 `78103f377f6e5acb72fa7eb8c47a6b2595f4f5301aa5d0cba7fc829227ff2781`; `target/B3630-control-refresh-separated-aarch64.elf` SHA256 `2989454bbd58bf9a088f57797eac4b2a4df528d0be04f2824c0c9a041e77170d`.
- Restored adjacent boundary fixtures: redraw fixtures import the canonical IPC crate rather than recompile its module tree under a different root; unrelated control drawing branches fail explicitly if entered. Raw paint fixture includes actual trace policy, makes its intended painting window visible, checks current damage-only payload layout and leaves presentation milestones to output flush. Compile/old-expectation failures retained `/tmp/B3630-control-refresh-{final-boundaries,repaired-boundaries,repaired-boundaries2,repaired-boundaries3}.log`; final four targets44/9/51/29 pass `/tmp/B3630-control-refresh-repaired-boundaries4.log`. Existing retained-pixel/hole assertions remain, including poisoned source gaps. Fixture repairs change no runtime drawing.
