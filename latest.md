# Latest

Archive: `latest 2.md` holds the prior ledger through the xattr split-truth cleanup.

Active slice: filesystem syscall path identity. Goal is to remove lexical/string fallback authority from VFS-facing syscalls and make every path mutation operate on the resolved Linux `struct path` equivalent: `(mount id, dentry, inode)`.

Current diagnosis:
- Old `*at` helpers rendered dirfd/cwd/root to strings, normalized text, then re-walked from the global root on misses. That creates split truth after bind mounts, `MS_MOVE`, `pivot_root`, `chroot`, `open_tree`, and private mount namespace cloning.
- Parent-mutating syscalls (`mkdir*`, `unlink*`, `rmdir`, `rename`, `link*`, `symlink*`, `mknod*`, AF_UNIX `bind`) must resolve the parent once and mutate that parent inode/dentry directly. Whole-path cache invalidation and parent-string fsnotify hooks are wrong because multiple mounts can expose the same rendered string or the same dentry at different mount identities.
- Mount syscalls must preserve the walked mount id. `open_tree(AT_EMPTY_PATH)`, procfd targets, `move_mount`, `fspick`, `mount_setattr`, `umount2`, `pivot_root`, and `chroot(".")` all need live `VfsPath` inputs, not display strings.
- Mount namespace copy must remap task `cwd_vfs`/`root_vfs` mount ids from parent namespace mounts to cloned child namespace mounts. Keeping old ids after `CLONE_NEWNS` is stale-path identity.
- Inotify/fanotify watch setup and dirent hooks must key by inode/parent inode instead of re-resolving rendered parent paths.
- `/proc/<pid>/mounts`, `/proc/<pid>/mountinfo`, and `fdinfo mnt_id` must report the target task/open file mount namespace identity; hard-coded global mount views make userspace see the wrong namespace.

Harness coverage added/used:
- `crates/kernel/fs/tests/fs_syscall_model.rs`: hosted model over mounted ext4-like roots plus tmpfs/dev/proc/sys shapes, covering open/create, mkdir, rename, hardlink, symlink follow/nofollow, xattr follow/nofollow/dirfd, permissions, ownership, sticky directories, chmod/chown, mknod, cwd/fchdir, readonly xattr behavior, and logind-style cross-namespace visibility.
- `crates/kernel/vfs/tests/open_tree_recursive_clone.rs`: detached clone, recursive clone, hash-only clone, empty-path dirfd clone, and staged bind clone parent-mount behavior.
- `crates/kernel/vfs/tests/greeter_sysfs_after_pivot.rs`: sysfs visibility after pivot/root movement and shared propagation.
- `crates/kernel/vfs/tests/mount_propagation_pivot.rs`: namespace copy/remap and staged root/pivot mount behavior.

Verification so far:
- `cargo test -p fs --test fs_syscall_model -- --nocapture` passed.
- `cargo test -p vfs --test mount_propagation_pivot -- --nocapture` passed 10/10.
- `cargo test -p vfs --test open_tree_recursive_clone -- --nocapture` passed 4/4.
- `cargo test -p vfs --test greeter_sysfs_after_pivot -- --nocapture` passed 5/5.
- `cargo test -p vfs --lib -- --nocapture` passed 97/97.
- `cargo check -p syscalls` passed for x86_64 kernel target.
- `cargo check -p kmain` passed for x86_64 and aarch64 kernel targets.

Commit plan:
- Stage only the coherent path-identity slice: syscall pathresolve/namei/mount callers, VFS mount/namei/file-hook support, inotify watch resolution, proc mount/fdinfo namespace reporting, the hosted filesystem syscall model, and this ledger rotation.
- Exclude unrelated dirty work: ext4 internals, allocator poison/canary diagnostics, boot Cargo changes, netlink tests, MM debug, and generated boot logs.
- Commit as `fix(path): resolve filesystem syscalls by vfs identity` after staged diff/check pass.

Known remaining cleanup after this slice:
- Audit any remaining boot/rootfs-only `resolve_abs` or string-rendered setup helpers and either prove they are initialization-only or move them to identity-based helpers.
- Continue syscall surface modeling beyond the current filesystem harness until non-happy-path Linux semantics are covered across tmpfs/devfs/procfs/sysfs/ext4-backed operations.

Completed follow-up slice: Landlock path-beneath no longer stores or checks rendered path prefixes. Rules capture the parent fd's live `(mnt_id,dentry)` identity, and checks compare target `VfsPath` ancestry within the same mount. Existing-object checks (`openat`, `truncate`, `rename`, `unlink`, `rmdir`) pass resolved object identity; create/link/symlink/mknod checks pass the resolved parent identity before backend mutation because the destination child does not exist yet. This removes another string split-truth surface where bind mounts, chroot, or namespace movement could make a rendered prefix name the wrong object. Evidence: `cargo test -p security landlock -- --nocapture` passed 2/2; `cargo check -p syscalls` passed for x86_64 kernel target; `cargo test -p fs --test fs_syscall_model -- --nocapture` passed; `cargo check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: the shared `*at` empty-path resolver no longer treats `path_ptr == NULL` as a real empty string. With `AT_EMPTY_PATH` set, callers such as `fchmodat`, `fchownat`, and xattr-at helpers could previously operate on `dirfd` instead of returning Linux's required `EFAULT` for a bad pathname pointer. `at_path_empty` now returns `Result<bool, errno>`, rejects NULL/out-of-range/unreadable pointers with `EFAULT`, and only reports empty for a successfully read zero-length C string. Evidence: `cargo check -p syscalls` passed for x86_64 kernel target; `cargo check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: mount path rendering no longer strips a mount root by lexical byte prefix alone. `render_path_for_mount` and the internal recursive renderer now require the target dentry to be at/below the mount root by dentry ancestry before deriving a suffix. This prevents a bind rooted at `/foo` and mounted at `/mnt` from rendering sibling `/foobar` as `/mntbar`. Evidence: added `render_path_for_mount_rejects_lexical_prefix_sibling`; `cargo test -p vfs --test mount_path_projection -- --nocapture` passed 10/10; `cargo test -p vfs --test mount_propagation_pivot -- --nocapture` passed 10/10; `cargo test -p vfs --test open_tree_recursive_clone -- --nocapture` passed 4/4.

Completed follow-up slice: mountinfo field 4 (`root`) now uses dentry ancestry before trimming the superblock root prefix. The prior code used byte-prefix stripping, so a bind root `/foobar` with superblock root `/foo` could be reported as `/bar` even though it is not below `/foo`. Evidence: added `mountinfo_root_field_rejects_lexical_prefix_sibling`; `cargo test -p vfs --test mount_path_projection -- --nocapture` passed 11/11; `cargo test -p vfs --test mount_propagation_pivot -- --nocapture` passed 10/10; `cargo test -p vfs --test open_tree_recursive_clone -- --nocapture` passed 4/4; `cargo check -p syscalls` passed for x86_64 kernel target; `cargo check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: the raw syscall path resolver no longer turns all-slash nonempty paths into `ENOENT`. `trim_mount_raw("///")` previously trimmed every slash and rejected the empty result, so `chdir("///")`, `chroot("///")`, `fspick("///")`, mount target resolution, and other callers of `resolve_path_raw`/`resolve_mount_target_raw` diverged from Linux root-path semantics. It now preserves true empty string as `ENOENT` but canonicalizes all-slash input to `/`. Evidence: `cargo test -p fs --test fs_syscall_model -- --nocapture` passed; `cargo check -p syscalls` passed for x86_64 kernel target; `cargo check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: `move_mount(2)` destination path normalization now preserves all-slash root paths too. Its local `trim_move_path("///")` had the same Linux divergence as the shared raw resolver, returning `ENOENT` before target resolution. It now canonicalizes all-slash input to `/` while preserving empty string as `ENOENT`. Evidence: `cargo check -p syscalls` passed for x86_64 kernel target; `cargo check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: `MS_BIND|MS_REC` submount selection no longer scopes recursive bind clones by rendered path string prefixes. `copy_bind_subtree_from_arena` now receives the resolved source dentry and keeps only submounts whose mountpoint dentry is below that source dentry by plain ancestry. This fixes the syscall case where `mount --rbind /src /target` with `/src` inside the root mount could clone every sibling submount under `/`, and removes the `/foo` versus `/foobar` lexical-prefix hazard from recursive bind collection. Evidence: added `ms_rec_from_subdir_clones_only_submounts_below_source_dentry`; focused regression passed; `cargo test -p vfs --test mount_resolver -- --nocapture` passed 16/16; `cargo test -p vfs --test open_tree_recursive_clone -- --nocapture` passed 4/4; `cargo test -p vfs --test mount_propagation_pivot -- --nocapture` passed 10/10; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: new mount API syscall state no longer keeps a separate `(fstype, source, options)` string recipe for later `move_mount` materialization. `fsopen` now creates a real `vfs::fs::FsContext` for every accepted registered fstype, `fsconfig` requires that context, `fsmount` only packages an already-realized superblock/root, and `move_mount` returns `EINVAL` for detached mount objects that are neither realized SBs nor open_tree clones. This removes the syscall-side string-bag fallback while retaining the VFS fs_context adapter that Linux itself uses for filesystems without custom context ops. Evidence: grep found no syscall-side `LEGACY`/`string-bag`/materialize-by-fstype/MountObjectInode recipe path; `cargo test -p vfs --test fs_context -- --nocapture` passed 10/10; `cargo test -p vfs --test fs_context_attach_sb -- --nocapture` passed 2/2; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: procfs magic links no longer substitute canned path strings when their live backing state is missing. `/proc/<pid>/{exe,cwd,root,fd/*,ns/*}` resolution now returns `ENOENT` for a missing task, missing executable path, missing cwd/root state, missing fd table entry, or unknown proc-link leaf. `/proc/<pid>/fd/N` walking now either jumps to the live open file object or returns `ENOENT`; it no longer falls back to reopening stale captured pathname text after the fd disappears. `readlink(2)` now preserves VFS errno from `get_link` instead of collapsing every link backend error to `EINVAL`, while non-symlink readlink remains `EINVAL` through the default inode op. Evidence: grep found no proc-link fallback `/init` or `/` substitution path; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: the inert VFS inode metadata overlay has been removed instead of kept as a legacy compatibility path. `generic_fillattr`, `vfs_getattr`, `vfs_getattr_mask`, and `InodeOps::getattr` now report native inode fields only; there is no optional `InodeTimes`/owner/mode overlay argument and no no-op overlay store. Stat-family syscalls, ext4 inode ops, fuse inode ops, and xattr owner checks now consume the inode's real mode/uid/gid/timestamps directly. Overlay-era tests were deleted and replaced with native metadata/idmap coverage. Evidence: `cargo test -p vfs --lib -- --nocapture` passed 97/97; focused VFS getattr integration tests passed 27/27; `cargo test -p ext4 --test getattr_image -- --nocapture` passed 2/2; `cargo test -p fs --test fs_syscall_model -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets. Full `cargo test -p vfs --tests -- --nocapture` currently still fails in unrelated `executor_pivot_resolve::executor_msmove_root_then_resolve_binary_and_lib` with `ENOENT` after mount relocation; that remains in the mount/path identity backlog, not the metadata overlay cleanup.

Completed follow-up slice: mount path identity is now exact through namespace roots, recursive bind, move/detach/propagation, and the hosted mount harness. The strict rule is Linux-shaped: a path operation carries `(mount id, dentry)` identity; if that identity cannot be resolved, the operation returns the real errno instead of falling back to rendered strings or a dentry-only legacy view. Namespace root seeding now uses the actual root mount/root dentry; root attach adopts the global root as the mounted root; recursive bind computes submount membership by source-dentry ancestry and preserves source-relative paths; recursive bind target exclusion only excludes the crossed target mount, not every plain directory target inside the root mount; detach and propagation use exact parent mount ids instead of rendered parent paths. The test harness now registers mounted objects by exact parent mount id and uses mount-target walks once a root exists, so fake string success paths were removed from the main model. Fixtures that were pretending directories did not contain their requested children now return real child dentries, forcing ENOENT/ENOTDIR to come from the resolver rather than from test-only string shortcuts. Evidence: focused VFS mount tests passed (`mount_propagation_demote`, `mount_propagation_spread`, `mount_resolver`, `mount_setattr_attrs`, `mount_tree`, `mount_unbindable_bind`, `sandbox_nested_primary`, `sandbox_repro`, `sandbox_sysfs_move`, `sandbox_intermediate_mount`); full `cargo test -p vfs --tests` passed; `cargo test -p fs --test fs_syscall_model -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: procfs cwd/root magic links no longer follow rendered text. `/proc/<pid>/cwd` and `/proc/<pid>/root` now read the target task's live `cwd_vfs`/`root_vfs`; readlink renders text from that identity, while path following returns `LinkTarget::Jump` to the live `(mnt_id,dentry,inode)` instead of splicing the readlink string into the reader's namespace. The old `sched::proclink::resolve_proc_link(path: &str)` parser and its private fd/ns string arms were deleted, and procfs special links now store typed `(tid, leaf)` metadata rather than `/proc/<tid>/<leaf>` strings. The generic `make_proc_pid_link(tid, leaf: &str)` constructor was removed too; pid-dir entries call explicit `make_proc_pid_exe/cwd/root` constructors, so unknown link names cannot silently collapse into a fallback link kind. This closes the class where `/proc/1/root` text could name a valid but wrong object in the caller's namespace. Evidence: grep found no remaining proc-link `resolve:` field, `/proc/self/*` string constructors, generic pid-link constructor, unknown-link fallback, or `resolve_proc_link` parser; `cargo test -p vfs --test namei_magiclink_jump -- --nocapture` passed 6/6 including `magic_link_text_can_name_existing_wrong_object`; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `procfs` x86_64 and `syscalls` x86_64 kernel targets; `cargo test -p fs --test fs_syscall_model -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: new mount API user-string handling no longer converts bad pointers into empty strings. `fsconfig(2)` SET_STRING/SET_BINARY/SET_PATH/SET_PATH_EMPTY now reads required key/value strings through an errno-returning helper, so NULL/out-of-range/unreadable pointers return `EFAULT` and invalid UTF-8 returns `EINVAL`; only SET_FLAG ignores the value pointer. `move_mount(2)` now requires real from/to path strings and returns the pointer error instead of treating a failed read as the detached-empty-path mode. `mount(2)` keeps Linux's optional source/data NULL behavior, but a non-NULL bad source/data pointer now returns `EFAULT` instead of silently becoming `""`. `fsopen(2)` and `fspick(2)` also now enforce their documented CLOEXEC-only flag masks before doing work. Evidence: grep leaves `unwrap_or_default()` string reads only in debug logging blocks for mount tracing; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `syscalls` x86_64 kernel target and `kmain` x86_64/aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: `memfd_create(2)` no longer uses the generic UTF-8 user-string reader for its name. Linux copies a byte C string, prefixes `memfd:`, and enforces the memfd-specific `NAME_MAX - strlen("memfd:")` payload limit; the syscall now follows that shape. NULL/out-of-range/unreadable name pointers return `EFAULT`, missing NUL or a payload longer than 249 bytes returns `EINVAL`, and non-UTF-8 bytes are preserved through `vfs::path_from_bytes` instead of rejected or lossy-decoded. The front-door flag validation now also rejects the Linux-invalid `MFD_EXEC|MFD_NOEXEC_SEAL` combination before allocating an fd, and `MFD_NOEXEC_SEAL` creates a sealable tmpfs inode rather than accepting the flag on a non-sealable object. Remaining memfd correctness gap: `MFD_HUGETLB` still returns an unsupported-feature error instead of a real hugepage-backed memfd implementation, so it stays on the syscall-compliance backlog.

Completed follow-up slice: keyring syscall string fetch no longer rejects non-UTF-8 key descriptions or uses the old 256-byte cap. `add_key(2)`, `request_key(2)`, `KEYCTL_JOIN_SESSION_KEYRING`, and `KEYCTL_SEARCH` now fetch key descriptions/session names as byte C strings up to Linux `KEY_MAX_DESC_SIZE` (4096), preserve byte identity through `vfs::path_from_bytes`, and compare the preserved identity in the key store. Key type fetch now follows Linux's 32-byte front-door rule and rejects empty or dot-prefixed type names before lookup. Evidence: hosted `cargo test -p fs --lib keyring -- --nocapture` passed 10/10 including a raw `0xff` description identity regression; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p fs --target targets/x86_64-unknown-oxide-kernel.json` passed. A broad `cargo test -p fs keyring` is currently blocked by unrelated dirty integration tests still calling the removed `SuperBlock::fs()` API.

Completed follow-up slice: `execveat(2)` no longer treats a NULL `path` pointer as the empty string. The old dispatch considered `pathp == 0` to be `path_is_empty`, so `execveat(dirfd, NULL, argv, envp, AT_EMPTY_PATH)` could execute `dirfd` instead of returning the Linux pointer fault. NULL and out-of-range path pointers now return `EFAULT` before the AT_EMPTY_PATH branch. Remaining exec correctness gap: `AT_SYMLINK_NOFOLLOW` is still not modeled as a final-component no-follow exec walk; that needs the exec loader to carry resolved file identity instead of re-resolving strings.

Completed follow-up slice: `clone3(2)` no longer creates a child and then silently ignores requested placement/writeback features. `CLONE_PIDFD` now validates the pidfd output pointer before spawn and returns `EFAULT` for NULL/out-of-range storage instead of making a child with no pidfd writeback. `CLONE_INTO_CGROUP` now requires the caller's `clone_args` size to cover the `cgroup` field, validates the cgroup fd before spawn, rejects bad fds with `EBADF`, rejects non-cgroup directory targets with `EINVAL`, and attaches the child to the prevalidated cgroup id immediately after a successful spawn. `set_tid_size != 0` now returns `EINVAL` before spawn instead of pretending pid-array assignment happened. This removes the fake-success path where a child was born in the wrong cgroup or with ignored clone3 placement/writeback fields.

Completed follow-up slice: AF_UNIX abstract socket identity is no longer `String`-based. `read_sockaddr_un_path_len` now returns the raw `sun_path` bytes, `UnixAddrKey::Abstract` and registry/display storage use byte vectors, `getsockname`/`getpeername` write raw sockaddr bytes back out, and `/proc/net/unix` appends path bytes directly after the formatted columns. A hosted regression proves an abstract name containing `0xff` binds and looks up by exact byte identity instead of being rejected as invalid UTF-8. Remaining hard correctness item: pathname AF_UNIX still has to pass through the current VFS `&str` resolver, so non-UTF8 pathname socket names now fail explicitly with `EINVAL`; full Linux byte-path pathname support requires byte component support in namei/VFS rather than any lossy conversion.

Completed follow-up slice: VFS symlink following no longer decodes raw symlink targets with `String::from_utf8_lossy`. Ordinary symlink bodies are now converted through `vfs::path_from_bytes`, preserving invalid UTF-8 bytes with the existing reversible escape representation so the walker can resolve the exact byte-named target. Hosted regression: `/link_raw -> b"raw-\xff"` resolves to the file whose name is the exact escaped byte identity; a lossy `U+FFFD` decode would miss it.

Completed follow-up slice: mount path rendering no longer converts non-UTF8 dentry paths to fake `/` or empty suffixes. `mountinfo` root fields, mount-aware rendered paths, and absolute mount graph paths now use `vfs::path_from_bytes` for raw dentry bytes, preserving identity through the existing reversible byte escape instead of fabricating a different path. Hosted regressions cover a bind subroot named with `b"raw-\xff"` and a raw-byte suffix rendered through `render_path_for_mount`; the full `mount_path_projection` harness passes 13/13.

Completed follow-up slice: coredump `core_pattern` expansion no longer treats invalid UTF-8 pattern bytes as the fallback `"core"`. `expand_core_path` now decodes the sysctl byte template through `vfs::path_from_bytes`, so raw bytes survive `%` expansion and newline trimming instead of silently changing the dump path. Hosted regression: `b"core-\xff.%p\n"` expands to the byte-preserving escaped `/core-\xff.42` identity; the focused `core_pattern_tests` harness passes 6/6.

Completed follow-up slice: recursive bind subtree capture no longer collapses an unprovable source mount/dentry relation to an empty relative path. `copy_bind_subtree_from_arena` now returns no clones if the supplied source dentry is not actually beneath the supplied source mount root, and include-root `copy_tree` stops instead of fabricating an empty root-relative placement when its mountpoint relation cannot be derived. Hosted regression: `bind_submounts_rec_at(Some(/src mnt), /other dentry, /dst)` clones zero mounts and does not create `/dst/a`; the full `mount_copy_tree` harness passes 7/7.

Completed follow-up slice: `pivot_root`/`MS_MOVE` namespace rebase no longer collapses failed mount-aware relative-path derivation to the destination root. `pivot_root` now returns `EINVAL` if the root-relative `put_old` path cannot be proven instead of manufacturing `""`. `move_mount_m` now precomputes every descendant's rendered-path rebase before re-seating the moved root mount; if any descendant cannot be related to the moved source by either rendered-prefix-with-boundary or mount-aware ancestry, the syscall returns `EINVAL` before mutating the namespace. This removes the remaining runtime `rel_under_seeded(...).unwrap_or_default()` path in mount namespace rebasing. Evidence: `rg` finds no remaining `unwrap_or_default()` in `vfs/src/mount/namespace.rs`; `cargo test -p vfs --test mount_resolver -- --nocapture` passed 16/16; `cargo test -p vfs --test mount_pivot_validation -- --nocapture` passed 4/4; `cargo test -p vfs --test mount_move_validation -- --nocapture --test-threads=1` passed 7/7; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `kmain` x86_64/aarch64 kernel targets. Open unrelated harness failure observed: `cargo test -p vfs --test mount_propagation_pivot open_tree_clone_of_shared_mount_is_private -- --nocapture --test-threads=1` panics because `clone_mount_tree(&rootm, true)` returns an empty vector before any `MS_MOVE` path runs; that is now the next mount-clone correctness item.

Completed follow-up slice: `open_tree(OPEN_TREE_CLONE)` on a namespace-root mount now produces a detached root clone instead of an empty clone list. `copy_tree(include_root)` accepts a root mount with no mountpoint when the supplied base dentry is the mount's own `mnt_root`, recording the clone root as `rel=""`; non-root clones still require a proved mountpoint relation. The `open_tree_recursive_clone` hosted fixture also clears its process-global root provider before creating each fresh root mount, preventing one test's root dentry from being reused as another test's mount root. Evidence: `cargo test -p vfs --test open_tree_recursive_clone -- --nocapture --test-threads=1` passed 4/4; `cargo test -p vfs --test mount_propagation_pivot -- --nocapture --test-threads=1` passed 10/10; `cargo test -p vfs --test mount_resolver -- --nocapture --test-threads=1` passed 16/16.

Completed follow-up slice: remaining path-adjacent fallback string fabrications in procfs/QStr/debug mount tracing were removed without adding a new authority path. Procfs generated directory names now decode through a helper whose invariant is kernel-owned ASCII decimal bytes; CPUID/vendor fields decode through architected ASCII trimming; inline `QStr` names use the original `QStr::new(&str)` invariant instead of returning `""` on an impossible decode failure. Debug-only `mount`/`umount2`/`move_mount` traces no longer turn unreadable user pointers into empty path text; the trace is skipped and the syscall result remains the real Linux errno path. Evidence: targeted grep found no remaining `from_utf8(...).unwrap_or(...)`, `<bad>`, or `read_*cstr(...).unwrap_or_default()` hits in the touched path/proc/mount files; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `procfs`, `syscalls`, and `kmain` x86_64 plus `kmain` aarch64 kernel targets; focused `cargo test -p vfs --lib qstr -- --nocapture` passed; `git diff --check` clean.

Completed follow-up slice: chmod/chown-at flag handling no longer accepts fake success from unknown bits. `fchownat(2)` and the shared chmod/chown resolver now reject any flag outside `AT_SYMLINK_NOFOLLOW|AT_EMPTY_PATH` with `EINVAL` before lookup or mutation. `fchmodat2(2)` uses that same validator and passes the explicit flags to the common chmod body, while the old `fchmodat(2)` slot now correctly calls the common body with flags `0` instead of reading an unowned fourth register as authority. Hosted syscall-shape coverage asserts bad chmodat/chownat flags fail and leave mode/owner unchanged. Evidence: `cargo test -p fs --test fs_syscall_model -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: tracefs event filter parsing no longer treats invalid event-format bytes as an empty field table or lossy-rewrites invalid filter bytes. `parse_fields` now returns `BadFormat` on invalid UTF-8 event format data, `FilterSlot::set` rejects that write before replacing state, and tokenizer identifiers use strict UTF-8 over the already-ASCII identifier byte class instead of `String::from_utf8_lossy`. This makes malformed tracefs metadata/filter input fail closed with a parse error instead of changing filter authority through invented strings. Evidence: `cargo test -p tracefs predicate -- --nocapture` passed 11/11; targeted grep found no lossy/unwrap-empty conversions in the predicate parser; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p tracefs --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: tracefs record rendering no longer collapses non-UTF8 task `comm` bytes to `?`. The ring renderer now trims the null-padded byte field and appends an escaped byte representation directly into the trace output buffer, so every recorded byte is visible instead of being replaced by a fabricated fallback string. Evidence: `cargo test -p tracefs ring::tests -- --nocapture` passed 7/7; tracefs-wide grep found no `from_utf8_lossy`, `unwrap_or("?")`, `unwrap_or("")`, or `unwrap_or_default()` fallback hits; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p tracefs --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: SMBIOS/DMI identity no longer lossy-decodes firmware strings before publishing `/sys/{devices/virtual,class}/dmi/id/*`. The DMI cache now stores byte vectors, absent DMI state is represented as `None` instead of a fabricated default identity, and sysfs appends the Linux newline to the exact decoded field bytes. Invalid UTF-8 firmware bytes therefore remain visible to userspace instead of becoming `U+FFFD` or an empty fake value. Evidence: `cargo test -p firmware smbios -- --nocapture` passed 5/5 including non-UTF8 preservation; targeted grep found no `from_utf8_lossy`, `unwrap_or_default()`, or `unwrap_or("")` in the DMI decode/sysfs publisher path; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p firmware --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p sysfs --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: generic sysfs kobject attribute reads no longer turn a producer failure into a successful empty file. `SysfsOps::show` now returns `KResult<Vec<u8>>`, `AttrFileOps::read` propagates that errno, and block/bus/net producers distinguish absent attributes (`ENOENT`) from removed backing devices (`ENODEV`). The hosted sysfs test harness now enables `modules/hosted` so `/sys/module` code compiles under `cargo test` instead of losing coverage through a disabled dependency surface. Evidence: `cargo test -p sysfs kobject --features hosted -- --nocapture` passed 2/2; targeted grep found no optional `SysfsOps::show` signature or `ops.show(...).unwrap_or_default()` fallback in sysfs; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p sysfs --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: `/sys/module` no longer fabricates blank metadata files or blank parameter type fields. Optional module `license` and `vermagic` attributes are now omitted from readdir and return `ENOENT` on lookup when the loaded module did not provide that metadata; parameter bodies emit `type=...` only when `parmtype` was actually present. Evidence: `cargo test -p sysfs modules --features hosted -- --nocapture` passed 3/3; targeted grep found no `line_opt`, `unwrap_or("")`, or stale `MODULE_ENTRIES` fallback in `sysfs/src/modules.rs`; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p sysfs --target targets/x86_64-unknown-oxide-kernel.json` passed.

Completed follow-up slice: mount source identity no longer crosses the VFS/syscall boundary as a fake empty string. `read_optional_user_cstr_owned` now returns `Option<String>` so `mount(2)` preserves Linux NULL-source semantics while still returning `EFAULT` for a bad non-NULL source pointer. `FileSystemType::mount`, registered `FsConstructor`, and `FsType::construct` now take `Option<&str>`; the classic mount fs_context adapter passes `fc.source()` directly; ext4 requires `Some(source)` and pseudo filesystems explicitly ignore `None`. Hosted regression proves source-less `fs_context` get_tree reaches the backend as `None`, not `""`. Evidence: `cargo test -p vfs --test fs_context -- --nocapture` passed 11/11; `cargo test -p vfs --test fs_constructor_registry -- --nocapture` passed; `cargo test -p vfs --tests --no-run` passed; `cargo test -p devfs --lib -- --nocapture` passed 14/14; `cargo test -p ext4 --test fscontext_realize -- --nocapture` passed; targeted grep found no `fc.source().unwrap_or("")` or `FsConstructor = dyn Fn(&str, ...)` fallback; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: the VFS fs_context default adapter is no longer named as a fallback. The adapter is now `ClassicMountFsContextOps`, and its option renderer is `classic_mount_options()`, matching the Linux classic `->mount(source, options)` adapter role without suggesting a string fallback path. The unsupported typed-parameter error now reports `VFS: classic mount`. Evidence: targeted grep found no old adapter symbol, old option helper, old diagnostic prefix, or mount-adapter fallback-style test names in VFS source/tests; `cargo test -p vfs --test fs_context -- --nocapture` passed 11/11; `cargo test -p vfs --test fs_context_param_types -- --nocapture` passed 5/5; `cargo test -p vfs --test fs_context_security -- --nocapture` passed 6/6; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p vfs --target targets/x86_64-unknown-oxide-kernel.json` passed.

Completed follow-up slice: `/proc/net/dev` and `/proc/net/route` no longer repair split netdev truth with fabricated defaults. The interface registry now provides an atomic `IfaceSnapshot` carrying id, name, MTU, flags, and counters while the registry entry is live. `/proc/net/dev` renders those counters directly instead of snapshotting `(id,name,mtu)`, dropping the lock, re-looking up the id, and converting a missing device into zero stats. `/proc/net/route` now resolves route interface names from the live interface snapshot and skips stale route rows whose interface cannot be proven live; it no longer prints `lo` for an unknown route iface. Evidence: `cargo test -p net netdev -- --nocapture` passed 7/7; targeted grep found no remaining `/proc/net` second-lookup-zero-stats or `unwrap_or("lo")` route fallback; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `net` x86_64, `procfs` x86_64, and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: inotify/fanotify watch path setup no longer creates authority from global-root or UTF-8 fallback behavior. Watch path reads now use `PATH_MAX`, decode through `vfs::path_from_bytes`, preserve non-UTF8 byte names, return `ENOENT` for empty path, and return `ENAMETOOLONG` for PATH_MAX-sized names. `current_start_root` now requires the current task's live `cwd_vfs` and `root_vfs`; missing task/path state returns an errno instead of substituting the process-global root mount. VFS lookup errors are propagated as their Linux errno numbers instead of collapsed to `ENOENT`. Evidence: `cargo test -p fs inotify::syscalls::tests -- --nocapture` passed 3/3; `cargo test -p fs inotify -- --nocapture` passed; targeted grep found no `global_root_path`, UTF-8 watch-path decode, or old 256-byte `read_user_cstr(path_p, 256)` in `fs/src/inotify/syscalls.rs`; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `fs` x86_64 and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: xattr names no longer cross the syscall boundary as UTF-8 strings. `setxattr`/`getxattr`/`removexattr` now read raw NUL-terminated name bytes through `vfs::path_from_bytes`, validate `XATTR_NAME_MAX` against raw bytes, and store the reversible byte identity used by the VFS. `listxattr` now measures and writes raw name bytes via `vfs::path_into_bytes` instead of emitting the internal escaped `String` bytes. The stale ext4 fs_context test reference to `LegacyFsContextOps` was also corrected to the real `ClassicMountFsContextOps` adapter name. Evidence: `cargo test -p fs xattr -- --nocapture` passed 8/8 including raw `0xff` name and raw list payload coverage; `cargo test -p ext4 --test fscontext_realize -- --nocapture` passed; targeted grep found no stale `LegacyFsContextOps`, old xattr UTF-8 reader, or `n.as_bytes()` list emission in the touched files; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `fs` x86_64 and `kmain` x86_64/aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: new-mount-API path arguments no longer take the old UTF-8/256-byte side path. `open_tree(2)` now lets `resolve_at_lookup` be the sole path reader, preserving PATH_MAX, AT_EMPTY_PATH, and non-UTF8 byte semantics instead of pre-failing through a short UTF-8 helper. `move_mount(2)` source/target paths and `fsconfig(FSCONFIG_SET_PATH*)` values now read with a mount-API path helper that decodes via `vfs::path_from_bytes`, permits the empty path where Linux uses it, checks PATH_MAX with VFS byte length, and returns real `EFAULT`/`ENAMETOOLONG` instead of collapsing path bytes into invalid text. `fsopen` filesystem names and fsconfig keys remain strict strings. Evidence: targeted grep found no stale `read_cstr` helper/export or 256-byte new-mount path reader in the touched files; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; the same check with `--features debug-mount` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: classic mount-family syscall path arguments no longer use the old UTF-8/256-byte reader. `mount(2)` target, bind source, move source, `umount2(2)` target, and `pivot_root(2)` `new_root`/`put_old` now read user path bytes through a shared `PATH_MAX` helper, decode with `vfs::path_from_bytes`, preserve non-UTF8 byte identity, reject empty required operands with `ENOENT`, and return `ENAMETOOLONG` when no NUL appears inside the Linux pathname limit. Classic `mount(2)` filesystem type and data/option text remain strict strings; the optional source keeps Linux NULL-source semantics but no longer forces UTF-8. `legacyfs` audit result: no live `legacyfs` symbol/module remains; the relevant VFS adapter is the Linux-shaped `ClassicMountFsContextOps`, not a fallback filesystem. Evidence: targeted grep found no remaining `read_user_cstr_owned(..., 256)`/old path-argument hits or `unwrap_or_default()` fallback in the touched classic mount files; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; the same check with `--features debug-mount` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: `linkat(2)` no longer treats `AT_EMPTY_PATH` as a blanket path rejection or NULL-path fallback. The empty-path probe now lives in `pathresolve`, returns `EFAULT` for NULL/unreadable pointers, and only redirects the true empty string to the fd-backed link path. Non-empty `oldpath` with `AT_EMPTY_PATH` set now falls through to the normal `PATH_MAX`/byte-preserving path resolver instead of returning `EINVAL`, matching Linux's LOOKUP_EMPTY behavior. Stale comments in the shared `*at` users were corrected so `AT_EMPTY_PATH` is documented as empty-string-only; NULL remains a pointer fault. Evidence: targeted grep found no remaining `linkat` 256-byte oldpath reader, NULL-as-empty comment, or non-empty `AT_EMPTY_PATH` rejection in the touched files; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: pathname `AF_UNIX` no longer rejects non-UTF8 socket pathnames before VFS resolution. `bind(2)` now decodes raw `sockaddr_un.sun_path` bytes through `vfs::path_from_bytes` for creation while preserving the original bytes in the `UnixAddr` display, and `connect(2)`/`sendto(2)`/`sendmsg(2)` resolution now uses the same reversible path decode before inode lookup. Abstract UNIX addresses remain raw byte keys. Evidence: hosted `cargo test -p net pathname_addr_preserves_non_utf8_display_bytes -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: FUSE `READDIR` no longer silently drops daemon entries whose names are not valid UTF-8. `readdir_stream` now converts raw `fuse_dirent.name` bytes through `vfs::path_from_bytes` before `DirContext::emit`, so invalid byte names remain visible as reversible VFS names instead of disappearing from directory iteration. Evidence: hosted `cargo test -p fs fuse_dirent_name_preserves_non_utf8_bytes -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p fs --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: `execve(2)` executable lookup no longer rejects legal non-UTF8 path bytes before VFS resolution. Shared `pathresolve::read_exec` now converts the raw user pathname with `vfs::path_from_bytes` and passes the reversible VFS string into lookup, replacing the old `core::str::from_utf8(...).ok()?` gate that made byte-valid executable names look like `ENOENT`. Evidence: `cargo test -p vfs path_from_bytes_roundtrips_non_utf8 -- --nocapture` passed; hosted `cargo test -p syscalls exec_lookup_path_preserves_non_utf8_bytes -- --nocapture` ran zero tests because `syscalls` is `#![cfg(target_os = "oxide-kernel")]` on host, so it was not counted as coverage; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: ext4 xattr persistence no longer drops or rewrites non-UTF8 xattr suffix bytes. On decode, on-disk `e_name` bytes now pass through `vfs::path_from_bytes` before namespace-prefix reconstruction instead of `core::str::from_utf8(...).ok()` dropping the entry. On encode, ibody and external xattr blocks now sort, hash, size, and write `vfs::path_into_bytes(suffix)` instead of the internal escaped Rust string bytes, so a raw `user.raw-\xff` suffix persists as the exact byte `0xff`. Evidence: `cargo test -p ext4 xattr_name_preserves_non_utf8_suffix_bytes -- --nocapture` passed 2/2 raw-byte ibody/external tests; `cargo test -p ext4 xattr -- --nocapture` passed the 8 xattr unit tests plus xattr image coverage; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p ext4 --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed follow-up slice: ext4 rootfs directory iteration no longer drops non-UTF8 directory entries. `Ext4StatFileOps::iterate` now decodes raw ext4 `dir_entry.name` bytes through `vfs::path_from_bytes` before `DirContext::emit`, so byte-valid names remain visible through readdir/getdents instead of being skipped by `core::str::from_utf8` failure. Evidence: `cargo test -p ext4 ext4_dirent_name_preserves_non_utf8_bytes -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p ext4 --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check -p kmain` passed for x86_64 and aarch64 kernel targets.

Completed foundation slice: mounted superblocks no longer retain an internal `Arc<dyn FileSystem>` backend as a second live authority path. `SuperBlock::for_backend` now snapshots the constructor backend into Linux-shaped mounted state (`s_type`, `s_op`, `s_magic`, `s_blocksize`, `s_root`, `s_fs_info`) and drops the constructor object after `fill_super`/`set_sb`; `SuperBlock::s_fs`, `SuperBlock::fs()`, `Mount::fs()`, `FsBackedType`, `FsBackedSuperOps`, and `NullFs` were removed. `/proc/mounts`, `/proc/mountinfo`, `umount2`, `fspick`, and `statmount` now read filesystem identity/options/magic from `Mount + SuperBlock`, and the old `FileSystem::mounts_line` formatter hook plus the cgroup override were deleted. Removing the fallback exposed and fixed a real recursive-bind wiring bug: when `MS_BIND|MS_REC` receives a source mountpoint dentry before crossing, submount discovery must start at that mount's `mnt_root` dentry, not at the covered parent-fs dentry. Evidence: targeted grep found no remaining live `s_fs`, `sb.fs()`, `Mount::fs()`, `FsBacked*`, `NullFs`, or `mounts_line` use in VFS/procfs/syscalls/fs/ext4/cgroup source/tests; `cargo test -p vfs --test sandbox_intermediate_mount descend_crosses_intermediate_mount_in_cold_synthesis -- --nocapture` passed; `cargo test -p vfs --test fs_show_options -- --nocapture` passed 3/3; full `cargo test -p vfs --tests -- --nocapture` passed; direct `cargo -Zjson-target-spec -Zbuild-std=core,compiler_builtins,alloc check` passed for `syscalls` x86_64, `procfs` x86_64, and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: hosted filesystem harnesses no longer call the removed internal backend identity path. `fs_syscall_model` and `udev_runtime_mounts` now read filesystem names from `SuperBlock::s_type.name()`, matching Linux's mounted `file_system_type` identity instead of the old constructor/backend object. This unblocked broad fs/keyring validation after the `SuperBlock::fs()` removal. Evidence: targeted grep found no remaining `.sb.fs()`, `.fs().name()`, `SuperBlock::fs`, or `Mount::fs(` callers in the checked fs/vfs/syscall/procfs test/source surfaces; `cargo test -p fs --test fs_syscall_model -- --nocapture` passed; `cargo test -p fs --test udev_runtime_mounts -- --nocapture` passed 3/3; `cargo test -p fs keyring -- --nocapture` passed 10/10 and compiled the fs integration tests; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `fs` x86_64 and `kmain` x86_64/aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: `name_to_handle_at(2)` no longer treats the required `mount_id` output pointer as optional. The previous success path wrote the file handle and then silently skipped `*mount_id` when the pointer was NULL, out of range, or not writable, returning success with incomplete Linux ABI output. The syscall now validates and writes the mount-id output through `validate_user_buf_writable`, returning `EFAULT` on bad storage instead of fabricating success. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets. Hosted syscall execution coverage is still absent for this kernel-only module, so pointer-fault runtime tests remain on the syscall harness backlog.

Completed follow-up slice: `wait4(2)`/`waitpid(2)` no longer treat the status output pointer as a best-effort side effect. `wstatus == NULL` remains valid, but a bad non-NULL pointer is now validated with `validate_user_buf_writable` and returns `EFAULT` from every wait result path instead of reporting a successful child wait while silently dropping the status word. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets. Hosted syscall execution coverage is still absent for this kernel-only module, so bad-status-pointer runtime tests remain on the syscall harness backlog.

Completed follow-up slice: registered filesystem constructors no longer hand the syscall mount path an `Arc<dyn FileSystem>` plus optional bind-root recipe to interpret later. `MountSpec` now carries only an already-realized `Arc<SuperBlock>` and the strict/ignore-errors policy bit; `mount(2)` grafts that SB directly with `attach_sb_with_flags_at`. The constructor-side adapter is confined to `MountSpec::from_filesystem`, where it performs `fill_super` immediately and preserves device-keyed `sget` sharing before the object enters the mount engine. This removes the live production path where `fsmount_common::mount_ops` used `spec.fs`/`spec.bind_root` as delayed authority after path resolution. Evidence: targeted grep found no `MountSpec { fs, ... }`, `spec.fs`, `spec.bind_root`, `sb.fs()`, `Mount::fs()`, `FsBacked*`, or live `s_fs` authority references in the touched VFS/syscall/devfs/ext4 surfaces; `cargo test -p vfs --test fs_constructor_registry -- --nocapture` passed; `cargo test -p devfs --lib devtmpfs_fstype_realizes_devfs_sb -- --nocapture` passed; `cargo test -p ext4 --test fscontext_realize -- --nocapture` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `vfs` x86_64, `syscalls` x86_64, and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: `pselect6(2)` and `ppoll(2)` no longer turn bad non-NULL timeout/sigmask pointers into "no timeout/no sigmask" behavior. Both handlers now validate every non-NULL user pointer with `validate_user_buf` before reading, return `EFAULT` for bad storage, return `EINVAL` for invalid `timespec` values or non-8-byte sigset sizes, and strip non-blockable `SIGKILL`/`SIGSTOP` from temporary masks. This removes another direct `< USER_VA_END` split where syscall ABI validation and kernel memory access disagreed. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets; targeted grep confirms the touched handlers use `validate_user_buf` instead of raw range checks for their user timeout/sigmask inputs. Remaining adjacent backlog: `select(2)` timeout and `rt_sigprocmask(2)` oldset writeback still need the same bad-pointer audit in a separate focused slice.

Completed follow-up slice: registered filesystem construction now passes the actual registered `file_system_type` into fill-super instead of rebuilding mounted type identity from the constructor backend. `FsType` owns a self-reference and calls its constructor with `Arc<dyn FileSystemType>`; `MountSpec::from_filesystem` realizes the `SuperBlock` immediately with explicit `s_type`, `s_op`, `s_magic`, `s_blocksize`, `s_root`, and `s_fs_info`. `SuperBlock::for_backend` is now only a direct-register compatibility shim over the same `from_ops` path: it snapshots explicit mounted state, back-stamps `i_sb`, and drops the backend object; the live SB does not retain `Arc<dyn FileSystem>` as an authority source. `SimpleSuperOps` is an explicit pseudo/in-core `s_op`, replacing the old hidden generic adapter role. Evidence: `cargo test -p vfs --test fs_constructor_registry -- --nocapture` passed; `cargo test -p devfs --lib devtmpfs_fstype_realizes_devfs_sb -- --nocapture` passed; `cargo test -p ext4 --test fscontext_realize -- --nocapture` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `vfs` x86_64, `syscalls` x86_64, and `kmain` x86_64/aarch64 kernel targets; `git diff --check` clean for the touched VFS/syscall/devfs/devpts/ext4 files. Remaining object-model cleanup: convert boot/direct-register callers to pass realized `SuperBlock`s or registered `file_system_type` instances directly, then delete the `for_backend` compatibility shim.

Completed follow-up slice: direct mount materialization no longer fabricates `file_system_type` adapters from `Arc<dyn FileSystem>` constructor objects. `DirectFsType`, `BackendFsType`, and `SuperBlock::for_backend` were deleted. The mount attach path now builds a superblock only with an explicit registered `Arc<dyn FileSystemType>`; the old direct `register*` entrypoints resolve through the registry and return `ENODEV` if no real type is registered. Boot root/dev/proc/sys/tmpfs/home mounts and cgroup2 boot mount now pass registered Linux type identities (`ext4`, `devtmpfs`, `proc`, `sysfs`, `tmpfs`, `cgroup2`) instead of relying on backend instance names such as `devfs`/`procfs`. Evidence: targeted grep found no remaining `DirectFsType`, `BackendFsType`, or live `SuperBlock::for_backend` definition in VFS source; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `vfs` x86_64, `syscalls` x86_64, and `kmain` x86_64/aarch64 kernel targets.

Completed follow-up slice: hosted VFS/ext4/cgroup harnesses no longer depend on the deleted `SuperBlock::for_backend` constructor or document it as the expected shape. Integration-test helpers now realize test superblocks through explicit `s_type`, `s_op`, `s_root`, `s_fs_info`, and `from_ops`, then back-stamp the test backend's weak SB reference only for inode-cache identity. This keeps harness coverage aligned with the Linux object model instead of keeping a test-only memory of the removed backend authority path. Evidence: targeted grep found no `for_backend`, `SuperBlock::for_backend`, `DirectFsType`, `BackendFsType`, `FsBacked`, standalone `s_fs`, or `s_fs:` hits in VFS source/tests, ext4 source/tests, cgroup source, or fsmount registry; `cargo test -p vfs --tests --no-run`, `cargo test -p ext4 --tests --no-run`, and `cargo test -p cgroup --lib --no-run` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `vfs` x86_64, `syscalls` x86_64, and `kmain` x86_64/aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: `rt_sigprocmask(2)` no longer uses raw `USER_VA_END` range checks or silently drops a bad non-NULL `oldset`. Non-NULL `set` is validated/read through the user-buffer helper before mutation, invalid `how` returns `EINVAL` before oldset writeback when `set` is present, `SIGKILL`/`SIGSTOP` remain unmaskable, `set == NULL` stays query-only and ignores `how`, and a bad non-NULL `oldset` returns `EFAULT` instead of reporting success without the old mask. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check` passed for `syscalls` x86_64 and `kmain` x86_64/aarch64 kernel targets; `git diff --check` clean.

Completed follow-up slice: mount namespace attachment no longer contains its own backend-to-superblock builder. The namespace graft path now consumes an already-realized `MountSpec`/`SuperBlock` and `graft_realized` only sees mounted Linux objects (`s_type`, `s_op`, `s_root`, `s_fs_info`); the old `mount/attach.rs::build_sb` duplicate was deleted. The remaining `Arc<dyn FileSystem>` compatibility boundary is confined to constructor realization through `MountSpec::from_filesystem`, where `file_system_type` + `fill_super` produce the SB before any mount-table state is mutated. Recursive attach now routes through the same typed register path, so there is no second mount-engine authority path deriving identity/options/root from backend state during namespace grafting. Evidence: `cargo test -p vfs --tests --no-run` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p vfs --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p kmain` passed for x86_64 and aarch64 kernel targets; targeted grep found no `build_sb`, `FsBacked*`, `SuperBlock::for_backend`, live `s_fs`, or mount-layer `next_anon_dev`/`sget` builder references in the checked VFS surfaces.

Completed follow-up slice: `rt_sigpending(2)`, `rt_sigtimedwait(2)`, and `rt_sigsuspend(2)` no longer use raw one-sided `USER_VA_END` checks for their sigset/siginfo/timespec user buffers. Required sigset pointers now validate the full 8-byte object, optional `rt_sigtimedwait` `siginfo_t` and timeout pointers fault before state mutation when bad, and `rt_sigpending` relies on the writable user-buffer validator instead of duplicating a weaker range check. `rt_sigsuspend` now strips unblockable `SIGKILL|SIGSTOP` through the typed `Signum` bit helpers instead of hard-coded shifts. Evidence: `docs/54.md` is absent in this tree despite the `CLAUDE.md` reference, so the checked ABI sources were `docs/15-syscall-abi.md` and `docs/24-ipc.md`; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p kmain` passed for x86_64 and aarch64 kernel targets; targeted grep confirms these three handlers no longer use raw `USER_VA_END` checks.

Completed follow-up slice: `pipe(2)`/`pipe2(2)` and `socketpair(2)` no longer allocate kernel fds before proving the caller's `int[2]` writeback buffer is valid. Both handlers now validate the full 8-byte, 4-byte-aligned output array with `validate_user_buf_writable` before creating pipe/socket inodes or installing fd-table entries. This removes the old first-byte-only `USER_VA_END` guard that could write past the validated range or leave live fds behind after a bad user output pointer. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p kmain` passed for x86_64 and aarch64 kernel targets; targeted grep confirms `293_pipe2.rs` and `053_socketpair.rs` no longer import `USER_VA_END`.

Completed follow-up slice: deleted the remaining `MountSpec` constructor contract and the lenient mount-graft branch. Registered filesystem constructors now return an already-realized `Arc<SuperBlock>` directly, so the new-mount API and `mount(2)` both consume the same Linux object (`s_type`, `s_op`, `s_root`, `s_fs_info`) instead of a wrapper carrying separate strictness/policy state. The mount namespace still has a compatibility fill-super helper for not-yet-converted `FileSystem` implementations, but that helper returns a plain `SuperBlock` before grafting; namespace state never retains or consults the backend object. `mount(2)` graft failures now propagate as errno (`EEXIST` mapped to `EBUSY`) instead of the old non-strict success path. Evidence: `cargo test -p vfs --tests --no-run` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p vfs --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p kmain` passed for x86_64 and aarch64 kernel targets; targeted grep found no `MountSpec`, `from_filesystem`, `FsBacked`, live `s_fs`, `SuperBlock::for_backend`, or `spec.strict` in the checked VFS/syscall/boot surfaces.

Completed follow-up slice: `getsockname(2)`, `getpeername(2)`, and netlink `getsockname(2)` now implement Linux `socklen_t *addrlen` value-result semantics instead of raw one-sided sockaddr writes. The handlers validate the `addrlen` word, read the caller's buffer length, copy `min(caller_len, kernel_sockaddr_len)` bytes, and write the full kernel sockaddr length back to `addrlen`; a zero caller length copies no sockaddr bytes while still reporting the required size. AF_INET, AF_INET6, AF_UNIX, and AF_NETLINK now share the encoded-sockaddr writeback helper, and the top-level 51/52 syscall handlers no longer use `USER_VA_END` as a substitute for full user-buffer validation. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p kmain` passed for x86_64 and aarch64 kernel targets; targeted grep confirms `051_getsockname.rs`/`052_getpeername.rs` no longer import `USER_VA_END` and netlink `getsockname` routes through `copy_sockaddr_to_user`.

Completed follow-up slice: time syscall user-buffer handling no longer reports success on bad required pointers or partial struct probes. `clock_settime(2)` now rejects non-`CLOCK_REALTIME` clocks with `EINVAL`, validates the full 16-byte `timespec`, and rejects negative/out-of-range fields before updating realtime offset. `settimeofday(2)` still accepts `tv == NULL` as a no-op for the ignored legacy timezone path, but bad non-NULL `tv` now returns `EFAULT` and invalid `timeval` fields return `EINVAL`. `time(2)` now returns `EFAULT` for a bad non-NULL `tloc` instead of silently returning seconds without writeback. `nanosleep(2)` and relative `clock_nanosleep(2)` now validate full `timespec` inputs and validate optional `rem` writeback storage on signal interruption. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed after cleanup; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p kmain` passed for x86_64 and aarch64 kernel targets before the final unused-import-only cleanup; `git diff --check` clean for the touched time syscall files.

Completed follow-up slice: scheduler syscall user buffers no longer rely on raw `USER_VA_END` checks or alignment faults as ABI validation. `sched_setparam(2)`, `sched_setscheduler(2)`, `sched_setattr(2)`, and affinity input now validate full readable user objects before loading; `sched_getparam(2)`, `sched_rr_get_interval(2)`, `sched_getaffinity(2)`, and `sched_getattr(2)` validate full writable outputs before storing. User pointers are byte-range validated and scalar accesses use unaligned loads/stores where appropriate, matching Linux copy-to/from-user behavior instead of rejecting unaligned buffers. Bad masks now return `EFAULT` instead of `EINVAL`, and `sched_getparam(2)` now returns `ESRCH` for a missing pid instead of fabricating priority `0`. Evidence: direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p syscalls --target targets/x86_64-unknown-oxide-kernel.json` passed; direct `cargo -Zbuild-std=core,alloc,compiler_builtins -Zjson-target-spec check -p kmain` passed for x86_64 and aarch64 kernel targets; targeted grep confirms the touched scheduler handlers no longer import `USER_VA_END`; `git diff --check` clean.
