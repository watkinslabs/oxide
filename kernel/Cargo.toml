[package]
name = "kernel"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "kernel"
path = "src/lib.rs"

[dependencies]
hal     = { path = "../crates/hal" }
klog    = { path = "../crates/klog" }
kalloc  = { path = "../crates/kalloc" }
pmm     = { path = "../crates/pmm" }
vmm     = { path = "../crates/vmm" }

[target.'cfg(target_arch = "x86_64")'.dependencies]
hal-x86_64 = { path = "../crates/hal-x86_64" }

[target.'cfg(target_arch = "aarch64")'.dependencies]
hal-aarch64 = { path = "../crates/hal-aarch64" }

[features]
default = []
debug-all = []

