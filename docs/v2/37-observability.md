# 37 Observability — v2 deferred entries

Carried at freeze 2026-05-02.

## Logging back-pressure

v1 lean = drop on full + dropped-event counter (vs block-the-emitter).

## BPF subset depth in v1.x

Socket filters first, then kprobes. Tracked here so the v1.x scope is recorded.

## Crash dump format (ELF coredump-like)

Deferred.

## `ftrace` function-graph

Deferred.
