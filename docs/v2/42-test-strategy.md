# 42 Test Strategy — v2 deferred entries

Carried at freeze 2026-05-02.

## Property-test seed reproducibility

v1 = yes (proptest stores last-failing seeds in `proptest-regressions/`).

## Mutation testing (`cargo-mutants`)

Useful but expensive; deferred to v1.x.

## Symbolic execution (kani / creusot)

Aspirational; deferred.
