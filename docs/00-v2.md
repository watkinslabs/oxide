# 00-v2 Master Plan: oxide2 v2

DRAFT 2026-05-07. Dep:`00`,`02`,`MANIFEST`,`docs/v2/`.

Companion to `00-master-plan.md`. v1 = kernel substrate + minimum userspace. v2 = kernel parity (de-stub the deferred subsystems) + container/observability/hardware-rich substrate. Each v2 phase is independent — no global ordering — except where its spec lists explicit deps.

## 1 Scope

v2 covers everything v1 deferred:
- Kernel surfaces stubbed-graceful in v1 (xattr→ENOTSUP, pkey→0, kcmp→0, etc.) that need real implementations.
- Subsystems v1 categorically excluded (ptrace, IPC, namespaces, io_uring, bpf/seccomp/landlock, modern mount API, fanotify, userfaultfd, perf, coredumps, DRM/KMS+input).
- Network completion (AF_INET6 socket layer + DHCP + DNS + real virtio-net live driver).
- Memory completion (mremap proper, per-PTE mprotect, MAP_SHARED, MADV_DONTNEED).

v2 does NOT cover (those slip to v2.x or later):
Wayland/GNOME, USB stack, real ACPI runtime + AML interp, NUMA, hibernate/S3, KVM/hypervisor, NFS/CIFS, FUSE, SELinux/AppArmor/IMA full LSM (only hooks land v2 phase 24), DT overlays, full netfilter, vDSO, FSGSBASE, glibc compat surface (set_thread_area i386, IFUNC).

## 2 Entry criterion

v2 phase work begins after `v1.0` ships per `00§15`. v2 phases parallelize freely — they're independent unless their spec lists otherwise — so a v2 phase can land anytime the substrate it touches is ready.

## 3 Phase ladder

(Same table as `00§3.3`, repeated here for v2-local reference. Spec column points to where the v2 work lands; some phases need new specs.)

| # | Subsystem | Spec hook | Per-spec v2 entry |
|---|---|---|---|
| 18 | AF_INET6 socket layer + DHCP client + DNS resolver | `25` | `docs/v2/25-net.md` (extend) |
| 19 | Real virtio-net live driver — promotes virtio-net from "types in" to "packets fly" | `25`,`35` | `docs/v2/35-drivers.md` |
| 20 | mremap (real, including MREMAP_MAYMOVE), per-PTE mprotect with TLB shootdown, MAP_SHARED, MADV_DONTNEED zero-fill | `11`,`20§7` | `docs/v2/11-vmm.md` |
| 21 | Namespaces: unshare/setns/pivot_root + per-NS mount/uts/pid/user | `13`,`16`,`26` | `docs/v2/26-namespaces-cgroups.md` |
| 22 | ptrace family (PTRACE_ATTACH/SINGLESTEP/PEEK/POKE/SYSCALL + signal-stop integration) | `27`,`13` | `docs/v2/27-security.md` (new section) |
| 23 | io_uring (setup/enter/register, SQE/CQE rings, fixed-buffer registration, IORING_OP_*) | `30` | `docs/v2/30-io-uring.md` |
| 24 | bpf + seccomp + landlock — minimal verifier + cBPF/eBPF JIT for x86_64; seccomp filter; landlock LSM hooks | `27` | `docs/v2/27-security.md` |
| 25 | SysV IPC (shm/sem/msg) + POSIX MQ + keyring | `24` | `docs/v2/24-ipc.md` |
| 26 | xattr family (real, ext4-backed) + ACLs + capability bits on files | `16`,`27` | `docs/v2/16-vfs.md` (xattr); `docs/v2/27-security.md` (caps) |
| 27 | fanotify_init + fanotify_mark + finish inotify gaps (recursive watches) | `16` | `docs/v2/16-vfs.md` |
| 28 | userfaultfd + memfd_secret enforced isolation | `11` | `docs/v2/11-vmm.md` |
| 29 | Modern mount API (fsopen/fsconfig/fsmount/fspick + mount_setattr) + real mount/umount/chroot (drop EPERM) | `16` | `docs/v2/16-vfs.md` |
| 30 | perf_event_open + tracefs/ftrace + ebpf programs running tracepoints | `27`,`37` | `docs/v2/37-observability.md` |
| 31 | Core dump generation (sigaction SIGSEGV → ELF coredump in fs) | `27`,`16` | `docs/v2/27-security.md` |
| 32 | DRM/KMS framebuffer + virtio-gpu + input subsystem (evdev) | NEW | `docs/v2/35-drivers.md` (new sections) |

## 4 v2 release ladder

- **v2.0** — phases 18-23 land. Network completion + virtio-net live + mremap proper + namespaces + ptrace + io_uring. This is the "container-class workloads run" milestone.
- **v2.1** — phases 24-29. bpf/seccomp/landlock + SysV IPC + xattr/ACL + fanotify + userfaultfd + modern mount API. This is the "hardened systemd / podman" milestone.
- **v2.2** — phases 30-32. perf/ftrace + coredumps + DRM/KMS + input. This is the "graphical-capable-but-headless-by-default" milestone, gating the v2.x desktop ladder.

Phase ordering inside each release: free, gated only by spec-cited deps.

## 5 v2 exit criterion

`v2.0` tag ships when:
1. PR-time CI green on the tagged commit, both arches.
2. Phases 18-23 each have their per-spec §Test contract green.
3. Acceptance binary list extended (`43§3` or successor) — modern containerd / curl-with-DHCP / strace-against-self / liburing-test scenarios pass on QEMU.
4. Kernel-completeness audit refreshed: no regressions vs v1; v2-phases-18-23 stubs flipped to real impls.

`v2.1` and `v2.2` add their own phase-set test contracts; same shape.

## 6 v2.x

Reserved for desktop / hardware-rich work — Wayland, GNOME, USB, ACPI runtime, NUMA, hibernate, KVM, NFS/CIFS, FUSE, full LSM impls, DT overlays, vDSO, glibc compat surface, full netfilter. Each is its own subsystem; promotion from v2.x to a numbered v2 phase happens when scope is firm.

## 7 Cross-spec

`00` (v1 master plan, §3.3 mirror of this ladder), `02§7` (deferred-entry pattern via `docs/v2/<spec>.md`), `MANIFEST.md` (status table — v2 entries listed when promoted from `docs/v2/` parking lot to numbered phase).

## 8 Changelog

- 2026-05-07: created. v2 phase ladder split out from `00§3.3` so v2 work has its own master plan symmetric with v1.
