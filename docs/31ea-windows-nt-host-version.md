# Windows NT host version bridge

FROZEN 2026-08-31. Dep: 01,02,31dz,52,53. Provides the native wine_get_host_version export required by the installed 64-bit Wine Notepad graph.

The bridge publishes the kernel's canonical Linux sysname and release through checked, process-heap-backed strings and pointer outputs.
