[package]
name = "swapfile_probe"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "swapfile_probe"
path = "src/main.rs"

[dependencies]
libc = { workspace = true }
support = { workspace = true }
