[package]
name = "v4l2_capture_probe"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "v4l2_capture_probe"
path = "src/main.rs"

[dependencies]
libc = { workspace = true }
support = { workspace = true }
