[package]
name = "windows-exceptions"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[[bin]]
name = "windows-exceptions-probe"
path = "src/main.rs"

[dev-dependencies]
proptest = "1"
