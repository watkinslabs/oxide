[package]
name = "drm_probe"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "drm_probe"
path = "src/main.rs"

[dependencies]
libc = { workspace = true }
support = { workspace = true }
