[package]
name = "vulkan_probe"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "vulkan_probe"
path = "src/main.rs"

[dependencies]
libc = { workspace = true }

