[package]
name = "request_key_probe"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "request_key_probe"
path = "src/main.rs"

[dependencies]
libc = { workspace = true }
support = { workspace = true }
