[package]
name = "dl"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
elf     = { path = "../elf" }
modules = { path = "../modules" }

[features]
default = []
hosted = []
