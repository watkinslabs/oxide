[package]
name = "jbd2"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
block = { path = "../block" }

[dev-dependencies]
sync = { path = "../sync" }

[features]
default = []
hosted = []
