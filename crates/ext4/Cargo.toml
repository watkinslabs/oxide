[package]
name = "ext4"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
block = { path = "../block" }
sync  = { path = "../sync" }

[features]
default = []
hosted = []
