[package]
name = "elf"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
hal      = { path = "../hal" }
klog     = { path = "../klog" }
bitflags = { version = "2", default-features = false }

[features]
default = []
hosted  = []
