[package]
name = "modules"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
hal  = { path = "../hal" }
klog = { path = "../klog" }
sync = { path = "../sync" }
elf  = { path = "../elf" }

[features]
default = []
hosted  = []
