[package]
name = "pam"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[features]
default = []
hosted = []

[dependencies]
nss = { path = "../../user/nss" }
crypt = { path = "../../shared/crypt" }
