[package]
name = "dl"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
elf     = { path = "../../shared/elf" }
modules = { path = "../../kernel/modules" }

[features]
default = []
hosted = []
