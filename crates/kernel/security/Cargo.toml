[package]
name = "security"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
cgroup  = { path = "../../kernel/cgroup" }
hal     = { path = "../../arch/hal" }
klog    = { path = "../../shared/klog" }
kernfs  = { path = "../../kernel/kernfs" }
pmm     = { path = "../../kernel/mm-pmm" }
sched   = { path = "../../kernel/sched" }
sync    = { path = "../../shared/sync" }
syscall = { path = "../../kernel/syscall" }
uaccess = { path = "../../kernel/uaccess" }
vfs     = { path = "../../kernel/vfs" }

[lints]
workspace = true
