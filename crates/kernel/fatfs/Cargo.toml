[package]
name = "fatfs"
version = "0.1.0"
edition = "2021"
publish = false
[lib]
path = "src/lib.rs"
[dependencies]
syscall = { path = "../syscall" }
vfs     = { path = "../vfs" }
block   = { path = "../block" }

[features]
default = []
hosted = []

[lints]
workspace = true
