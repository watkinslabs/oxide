[package]
name = "ima"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
crypt = { path = "../../shared/crypt" }
tpm   = { path = "../tpm" }

[lints]
workspace = true
