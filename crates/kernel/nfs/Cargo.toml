[package]
name = "nfs"
version = "0.1.0"
edition = "2021"
publish = false
[lib]
path = "src/lib.rs"
[dependencies]
sync    = { path = "../../shared/sync" }
syscall = { path = "../syscall" }
sunrpc  = { path = "../sunrpc" }

[features]
default = []
hosted = []

[lints]
workspace = true
