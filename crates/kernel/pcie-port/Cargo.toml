[package]
name = "pcie-port"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
drv = { path = "../../drivers/drv" }
pci = { path = "../../drivers/pci" }
pci-irq = { path = "../pci-irq" }
sync = { path = "../../shared/sync" }

[lints]
workspace = true
