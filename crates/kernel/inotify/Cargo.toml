[package]
name = "inotify"
version = "0.1.0"
edition = "2021"
publish = false
license = "MPL-2.0"

# inotify(7) inode + watch list per `52§3` domain layer.

[dependencies]
devfs   = { path = "../../kernel/devfs" }
hal     = { path = "../../arch/hal" }
pipe    = { path = "../../kernel/pipe" }
sched   = { path = "../../kernel/sched" }
sync    = { path = "../../shared/sync" }
syscall = { path = "../../kernel/syscall" }
vfs     = { path = "../../kernel/vfs" }
