[package]
name = "network-namespace"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
sync = { path = "../../shared/sync" }
namespace-identity = { path = "../namespace-identity" }

[dev-dependencies]
loom = "0.7.2"


[lints]
workspace = true
