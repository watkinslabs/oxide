[package]
name = "block"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
sched  = { path = "../sched" }
cgroup = { path = "../cgroup" }
hal      = { path = "../../arch/hal" }
klog     = { path = "../../shared/klog" }
sync     = { path = "../../shared/sync" }
bitflags = { version = "2", default-features = false }

[features]
default = []
hosted  = []
