[package]
name = "input"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
sync = { path = "../../shared/sync" }
timer = { path = "../timer" }
timekeeper = { path = "../timekeeper" }
virtio = { path = "../../drivers/virtio" }

[lints]
workspace = true
