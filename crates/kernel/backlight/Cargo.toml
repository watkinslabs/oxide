[package]
name = "backlight"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
kstrtox = { path = "../../shared/kstrtox" }
sync    = { path = "../../shared/sync" }
vfs     = { path = "../vfs" }

[lints]
workspace = true
