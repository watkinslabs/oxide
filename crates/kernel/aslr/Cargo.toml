[package]
name = "aslr"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
crng = { path = "../crng" }
hal  = { path = "../../arch/hal" }

[features]
default = []
hosted  = []

[lints]
workspace = true
