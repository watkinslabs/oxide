[package]
name = "elf-load"
version = "0.1.0"
edition = "2021"
publish = false
license = "MPL-2.0"

[dependencies]
aslr = { path = "../aslr" }
ext4 = { path = "../ext4" }
elf      = { path = "../../shared/elf" }
hal      = { path = "../../arch/hal" }
klog     = { path = "../../shared/klog" }
vmm      = { path = "../mm-vmm" }

[features]
debug-boot = []
debug-execload = []  # per-exec ELF-loader trace (interp resolve/place); split from debug-boot per B1474
