[package]
name = "ata"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
sync = { path = "../../shared/sync" }
block = { path = "../block" }
scsi = { path = "../scsi" }

[lints]
workspace = true
