[package]
name = "ext4"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
block = { path = "../../kernel/block" }
sync  = { path = "../../shared/sync" }
jbd2  = { path = "../../kernel/jbd2" }
crc   = { path = "../../shared/crc" }

[features]
default = []
hosted = []
