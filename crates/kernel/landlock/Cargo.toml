[package]
name = "landlock"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
sync    = { path = "../../shared/sync" }
audit   = { path = "../audit" }
syscall = { path = "../syscall" }
vfs     = { path = "../../kernel/vfs" }

[lints]
workspace = true
