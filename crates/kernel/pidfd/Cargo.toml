[package]
name = "pidfd"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
namespace-identity = { path = "../namespace-identity" }
syscall = { path = "../syscall" }
sched = { path = "../sched" }
vfs = { path = "../vfs" }

[features]
default = []
hosted = ["sched/hosted"]

[lints]
workspace = true
