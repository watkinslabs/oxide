[package]
name = "procfs"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
hal  = { path = "../../arch/hal" }
klog = { path = "../../shared/klog" }
sync = { path = "../../shared/sync" }
pmm  = { path = "../mm-pmm" }
vfs  = { path = "../vfs" }
net  = { path = "../net" }
modules = { path = "../modules" }
sched = { path = "../sched" }
vmm   = { path = "../mm-vmm" }

[features]
default = []
hosted  = []
