[package]
name = "iommu"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
pci = { path = "../../drivers/pci" }
mmio-map = { path = "../mmio-map" }
firmware = { path = "../firmware" }
pmm = { path = "../mm-pmm" }

[lints]
workspace = true
