[package]
name = "klog"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[features]
default = []
# Pulls in `std` for hosted tests/benches; see crates/sync/Cargo.toml for
# the same pattern. Kernel builds never enable this.
hosted = []
