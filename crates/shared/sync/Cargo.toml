[package]
name = "sync"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[features]
# Enables `HostedCpuLocal` (uses std::thread_local). Off in kernel
# builds; on for hosted-test consumers (slab, sched, klog tests).
hosted = []

[dependencies]
hal = { path = "../../arch/hal" }
