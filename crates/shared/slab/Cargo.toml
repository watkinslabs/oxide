[package]
name = "slab"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
hal  = { path = "../../arch/hal" }
klog = { path = "../../shared/klog" }
pmm  = { path = "../../kernel/pmm" }
sync = { path = "../../shared/sync" }

[dev-dependencies]
proptest = "1"
sync = { path = "../../shared/sync", features = ["hosted"] }
