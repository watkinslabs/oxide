[package]
name = "hal-x86_64"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
hal      = { path = "../hal" }
sync     = { path = "../sync" }
syscall  = { path = "../syscall" }
bitflags = { version = "2", default-features = false }
