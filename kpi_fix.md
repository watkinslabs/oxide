# KPI Fix Ledger

Date: 2026-07-06

Scope: `kpi.md` is a Kernel Porting Interface checklist for loading Linux-style
drivers/modules. Status below is based on code inspection, not on checklist text.
Branch names marked `TBD-*` are ordered slugs; claim the actual next counter from
`metadata/index.md` immediately before starting each row.

## Current Truth

Have: Rust-native driver model, PCI config/BAR/MSI primitives, devtmpfs/sysfs/procfs
device views, char/block dev_t dispatch, PMM buddy allocator, slab cache, spin/rw
locks, RCU, wait queues, timer registry, softirq slots, netdev/block traits, ACPI
table parsing, user-buffer validation, x86_64 ET_REL loader prototype, and symbol
table/export registry.

Do not have: a Linux C module ABI, real `init_module`/`finit_module`/`delete_module`
lifecycle, `.modinfo`/vermagic/module parameter parsing, a Linux header/shim layer,
generic `request_irq`, generic DMA mapping API, generic MMIO/port-I/O API, devm
resources, mature Linux PCI/block/net/input/USB APIs, firmware loader, power/runtime
PM, config/autoconf/version glue, or nm-based validation against real `.ko` files.

## Buildout Order

| Status | Priority | Branch | Description |
| --- | --- | --- | --- |
| DONE | P0 | existing | PMM/slab primitives exist as Rust APIs: buddy allocator, page metadata, slab cache, and `kalloc`/`vmalloc`-style shared allocator code. Still not exposed as Linux `kmalloc/kzalloc/kcalloc/kfree/vmalloc/vfree` KPI symbols. |
| DONE | P0 | existing | Core synchronization exists as Rust APIs: `Spinlock`, `RwLock`, RCU basics, wait queues, IRQ-save lock gate, and softirq slots. Still not exposed as Linux `spinlock_t`, `mutex`, `completion`, `work_struct`, or `wait_queue_head_t` ABI shims. |
| DONE | P0 | existing | Rust driver model exists: `drv::Device`, `drv::Driver`, device add/remove, driver bind/unbind, devtmpfs/sysfs hooks, uevents/modalias/devname/dev_t. Not Linux `struct device`, `struct driver`, `bus_type`, `class`, `devm_*`. |
| PARTIAL | P0 | existing | Module infrastructure exists: x86_64 ET_REL parser/loader, relocation engine, symbol table with GPL gating, and kernel export registry. Missing executable mapping, module init/exit call path, unload safety, `.modinfo`, vermagic, parameters, taint/state, aarch64 relocations. |
| PARTIAL | P0 | existing | PCI primitives exist: config reader trait, BDF parsing, BAR decoding/resources, command register helpers, capability/MSI-X helpers, and PCI-backed driver registration through `drv`. Missing Linux `struct pci_dev`, `pci_driver`, `pci_register_driver`, region ownership, `pci_iomap`, IRQ-vector wrappers, drvdata helpers. |
| PARTIAL | P0 | existing | IRQ/MSI support exists for internal drivers: x86 MSI vector allocator/handler table, aarch64 fixed INTID handlers, GICv2m state. Missing generic Linux `request_irq/free_irq`, IRQ flags, threaded IRQs, affinity, enable/disable API. |
| PARTIAL | P0 | existing | Block/net internals exist: `BlockDevice`, `PageCache`, disk registry, `NetDev`, netdev registry, stats, virtio-net/blk drivers. Missing Linux `gendisk`, `request_queue`, blk-mq, bio/request APIs, `struct net_device`, `net_device_ops`, skb/NAPI compatibility wrappers. |
| PARTIAL | P0 | existing | Filesystem compatibility exists for sysfs/procfs/devtmpfs/tracefs and VFS special nodes. Missing debugfs/configfs compatibility layer and Linux kobject/kset/sysfs helper API surface used by out-of-tree drivers. |
| VERIFIED | P0 | F650-kpi-symbol-audit | Built `tools/kpi-audit`: ingests `.ko` files through `nm -u` or captured nm output, scans implemented module exports, classifies unresolved symbols by KPI area, emits a ledger-ready missing-symbol table, and can fail a gate with `--fail-on-missing`. |
| VERIFIED | P0 | F651-kpi-uapi-header-surface | Added `kpi/include` Linux-shaped module header surface: generated config/release headers, compiler attributes/types, section annotations, module license/init/exit/export macros, errno/types, `container_of`, list/hlist/rbtree/xarray/idr/bitmap/bitops declarations, plus x86_64/aarch64 `-nostdinc` compile smoke. |
| PARTIAL | P0 | F652-kpi-module-loader-lifecycle | Advanced module lifecycle: registry now tracks stable names, state, size, refcount fields, tombstoned slots, name-based unload, and `/proc/modules` uses the lifecycle snapshot; `delete_module(name)` now reads the Linux user string instead of treating the pointer as an index. Remaining: executable/W^X module memory, `module_init`/`module_exit` calls, full refcount users, taint flags, async drain. |
| VERIFIED | P0 | F653-kpi-modinfo-vermagic-params | Added `.modinfo` parsing for name, license, author, description, depends, vermagic, parm/parmtype, alias, firmware; module names can come from `.modinfo`, GPL-only export resolution now uses parsed license, incompatible vermagic is rejected, `/proc/modules` exposes metadata, and `/sys/module/<name>` exposes initstate/refcnt/license/vermagic plus read-only `parameters/<param>` entries. |
| VERIFIED | P0 | F654-kpi-aarch64-module-relocs | Added machine-dispatched module relocations with aarch64 ET_REL support for ABS/PREL data relocations, MOVW ABS/PREL groups, CALL26/JUMP26/CONDBR19/TSTBR14/LD_PREL_LO19 branch-literal forms, ADR/ADRP page relocations, and ADD/LDST lo12 instruction fixups; covered with hosted unit tests and x86_64/aarch64 module target checks. |
| VERIFIED | P1 | F655-kpi-linux-alloc-api | Exported Linux allocation KPI wrappers: `kmalloc`, `kzalloc`, `kcalloc`, `kfree`, `vmalloc`, `vfree`, `alloc_pages`, `__free_pages`, `__get_free_pages`, `get_free_pages`, `free_pages`, `page_address`, `page_to_phys`, `kstrdup`, `kasprintf`; added `gfp.h`, `slab.h`, `vmalloc.h`, `mm.h`; verified with hosted module tests, x86_64/aarch64 module target checks, and header smokes on host/x86_64/aarch64. |
| PARTIAL | P1 | F656-kpi-linux-sync-api | Added C-owned Linux sync KPI structs, headers, and module exports for spin/raw-spin locks, atomic-backed mutex/rwlock/rwsem/seqlock, completion counters, wait-queue wake counters, `atomic_t`, `refcount_t`, `kref`, and lockdep no-op hooks; verified with hosted module tests, x86_64/aarch64 module target checks, and host/x86_64/aarch64 header smokes. Remaining before VERIFIED: scheduler-backed sleeping mutex/wait queues and Linux wait-event/prepare-to-wait semantics instead of spin/counter-backed compatibility. |
| PARTIAL | P1 | F658-kpi-time-workqueues [CLAIMED F658-kpi-time-workqueues 2026-07-06] | Added real exported Linux time/async substrate: hook-backed `jiffies`/`jiffies_64`, `HZ` header constant, `ktime` conversions, scheduler-backed sleep/delay entry points, timer-driver one-shot callbacks, `timer_list`, `hrtimer`, kworker-backed workqueues, delayed work, wake-started kthreads, and tasklet compatibility headers/exports. Remaining before VERIFIED: full Linux struct layouts, workqueue attributes/pools/flush domains, precise hrtimer modes/restarts, complete kthread parking/freezer/signal semantics, and audit against real target `.ko` unresolved symbols. |
| VERIFIED | P1 | F659-kpi-mmio-portio-api | Added Linux MMIO/port-I/O KPI facade: `ioremap`/`ioremap_nocache` map owned device pages through `mmio-map` with tracked `iounmap`, volatile `readb/readw/readl/readq` and `writeb/writew/writel/writeq`, `memcpy_toio`, `memcpy_fromio`, `memset_io`, real x86 `inb/inw/inl/outb/outw/outl`, and architecture-backed `mb/rmb/wmb/mmiowb`; added `linux/io.h`; verified with hosted module tests, x86_64/aarch64 module target checks, and header smoke. |
| VERIFIED | P1 | F660-kpi-dma-api | Added Linux DMA mapping KPI facade: coherent page-run allocation/free, direct-DMA streaming `dma_map/unmap_single`, `dma_map/unmap_page`, `dma_map/unmap_sg`, CPU/device sync barriers, `dma_mapping_error`, mask negotiation, `struct scatterlist` helpers, `dma_addr_t` headers, and explicit no-IOMMU identity/direct policy for both arches; verified with hosted module tests, x86_64/aarch64 module target checks, and header smoke. |
| PARTIAL | P1 | F661-kpi-irq-api | Added Linux IRQ KPI facade over `arch-irq`: exported `request_irq`, `free_irq`, `enable_irq`, `disable_irq`, `disable_irq_nosync`, `synchronize_irq`, IRQ context probes, affinity hint no-ops, Linux IRQ flags/types, and line-aware arch dispatch for x86 MSI vectors plus aarch64 fixed/MSI INTIDs. Remaining before VERIFIED: scheduler-backed threaded IRQ worker path and broader Linux IRQ domain/MSI allocation helpers. |
| PARTIAL | P2 | F662-kpi-device-core-api | Added Linux device-core KPI facade: `struct device`, `struct device_driver`, `struct bus_type`, `struct class`, register/unregister/create helpers, `dev_*` diagnostics ABI, `dev_set/get_drvdata`, `devm_*` managed allocations/actions, header smoke coverage, and symbol export wiring. Remaining before VERIFIED: real kobject/sysfs attr storage, uevent/modalias emission, driver bind/probe/remove integration, refcounted device lifetimes, visible dev_* logging, and complete class device lookup/destruction semantics. |
| PARTIAL | P2 | F663-kpi-pci-api | Added Linux PCI KPI facade: `struct pci_dev`, `struct pci_driver`, `struct pci_device_id`, register/unregister, enable/disable, request/release regions, resource helpers, `pci_iomap`/`pci_iounmap`, bus mastering, drvdata, IRQ-vector helpers, config read/write, symbol exports, and header smoke coverage. Remaining before VERIFIED: real enumeration/bind/probe/remove against driver core, real region ownership across kernel PCI registry, MSI/MSI-X allocation, IO-port remapping semantics, full config-space/device model parity, hotplug, power, and PCI error paths. |
| PARTIAL | P2 | F664-kpi-char-misc-api | Added Linux char/misc KPI facade: `struct file_operations`, `struct cdev`, `struct miscdevice`, `MKDEV`/`MAJOR`/`MINOR` headers, `cdev_init`, `cdev_alloc`, `cdev_add`, `cdev_del`, `alloc_chrdev_region`, `register_chrdev_region`, `unregister_chrdev_region`, `register_chrdev`, `unregister_chrdev`, `misc_register`, `misc_deregister`, `noop_llseek`, `nonseekable_open`, VFS char-device read/write/open/ioctl bridging, symbol exports, and header smoke coverage. Remaining before VERIFIED: VFS-level poll/mmap/release routing, user-pointer fault-safe copy semantics for real module callbacks, devtmpfs node publication for misc devices, full cdev kobject/refcount lifetime, and audit against real target `.ko` unresolved symbols. |
| PARTIAL | P2 | F665-kpi-firmware-loader | Added Linux firmware loader KPI facade: `struct firmware`, `request_firmware`, `request_firmware_direct`, `firmware_request`, `firmware_request_nowarn`, `release_firmware`, Linux firmware search path lookup, mounted-rootfs-backed synchronous reads, symbol exports, and header smoke coverage. Remaining before VERIFIED: bootloader initramfs archive lookup before rootfs mount, async `request_firmware_nowait`, userspace-helper policy/events, firmware cache/refcount lifetime parity, namespace-aware firmware path policy, and audit against real target `.ko` unresolved symbols. |
| PARTIAL | P3 | F666-kpi-netdev-api | Added Linux netdev KPI facade: `struct net_device`, `struct net_device_ops`, `alloc_netdev_mqs`, `alloc_netdev`, `alloc_etherdev_mqs`, `alloc_etherdev`, `free_netdev`, `netdev_priv`, `ether_setup`, `eth_hw_addr_set`, `register_netdev`, `unregister_netdev`, skb allocation/free/reserve/push/pull/put helpers, `eth_type_trans`, `netif_rx`, queue start/stop/wake, carrier on/off, symbol exports, and header smoke coverage. Remaining before VERIFIED: full NAPI, ethtool_ops, checksum/offload helpers, skb clone/frags/checksum metadata, multi-queue TX semantics, rtnl/refcount lifetime parity, PHY/MDIO compatibility, and audit against real target `.ko` unresolved symbols. |
| PARTIAL | P3 | F667-kpi-block-api | Added Linux block KPI facade: `struct block_device`, `struct gendisk`, `struct request_queue`, `struct blk_mq_tag_set`, `struct bio`, queue allocation/cleanup/configuration, blk-mq tag set/init queue, `bio_alloc`/`bio_put`/`bio_set_dev`/`bio_add_page`, `submit_bio`, `alloc_disk`/`put_disk`, `add_disk`/`del_gendisk`, capacity helpers, flush/discard op mapping, symbol exports, and host/x86_64/aarch64 header smoke coverage. Remaining before VERIFIED: partition scan table synthesis, request-based blk-mq dispatch, Linux request lifecycle/completion semantics, full bio vec/page mapping, cache/writeback barriers, discard granularity limits, block ioctl compatibility, queue limits/sysfs parity, and audit against real target `.ko` unresolved symbols. |
| DONE | P3 | F668-kpi-input-api [DONE F668-kpi-input-api 2026-07-07] | Add Linux input compatibility over existing virtio-input/evdev: `input_dev`, allocate/register/unregister, report key/abs/sync helpers, capability bitmaps, LED/status path, evdev ABI parity checks. |
| DONE | P3 | F669-kpi-usb-api [DONE F669-kpi-usb-api 2026-07-07] | Added Linux USB core KPI facade: device/interface/driver structs, descriptor and match table headers, Linux-shaped register/deregister macros, control/bulk/interrupt transfer entry points, URBs, DMA-coherent buffers, hotplug bind/unbind hooks, endpoint helpers, and header smoke coverage. |
| DONE | P3 | F670-kpi-platform-acpi-dt [DONE F670-kpi-platform-acpi-dt 2026-07-07] | Added Linux platform/ACPI/DT KPI facade: `platform_device`, `platform_driver`, driver bind/probe/remove, shared `ioport.h` resources, resource lookup by type/name, IRQ resource translation, managed platform MMIO mapping, ACPI companion match/enumeration helpers, OF device-node matching/property helpers, and host/x86_64/aarch64 header smoke coverage. |
| DONE | P4 | F671-kpi-power-pm [DONE F671-kpi-power-pm 2026-07-07] | Added power-management compatibility: suspend/resume hooks, runtime PM get/put, device power states, PCI PM, wakeup events. |
| DONE | P4 | F672-kpi-crypto-random-crc [DONE F672-kpi-crypto-random-crc 2026-07-07] | Export helper APIs commonly needed by drivers: `get_random_bytes`, CRC helpers, hash helpers, and crypto API stubs or real implementation based on target driver demand. |
| DONE | P4 | F673-kpi-usercopy-api [DONE F673-kpi-usercopy-api 2026-07-07] | Consolidate current syscall-only user-buffer validation into exported driver-safe `access_ok`, `copy_to_user`, `copy_from_user`, `get_user`, `put_user` helpers with fault-safe behavior or explicit non-sleeping limits. |
| PARTIAL | P4 | F674-kpi-debugfs-configfs [CLAIMED F674-kpi-debugfs-configfs 2026-07-07] | Added shared debugfs/configfs kernfs roots, mountable configfs root, Linux `debugfs.h`/`configfs.h`, debugfs dir/file/numeric/bool create+lookup+remove exports backed by VFS inodes, and configfs subsystem/group/attribute registration backed by VFS show/store files. Remaining before VERIFIED: full debugfs helper surface (`seq_file`, `simple_attr`, blob/regset helpers, symlinks, automount), full configfs default groups/item lifetime/refcounting/bin attrs/symlinks, and audit against real target `.ko` unresolved symbols. |

## Minimum Useful Path

1. Build `kpi-audit` and choose 2-3 real target `.ko` files.
2. Finish module loader lifecycle and both-arch relocations.
3. Add header/macro compatibility so target drivers compile unchanged or with minimal config.
4. Export alloc/sync/time/MMIO/DMA/IRQ APIs.
5. Add device core + PCI facade.
6. Only then build block/net/input-specific facades, ordered by unresolved symbols from real drivers.
